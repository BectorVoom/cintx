---
phase: quick-260601-d7e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cintx-oracle/tests/oracle_gate_closure.rs
  - crates/cintx-oracle/tests/spinor_global_ao_order_parity.rs
  - .planning/quick/260601-d7e-spinor-3c2e-gate-and-global-ao-order/CONCLUSION-ao_loc_2c.md
autonomous: true
requirements:
  - QD-260601-d7e-A   # stale oracle_gate_3c2e_spinor must-reject gate
  - QD-260601-d7e-B   # spinor global AO (ao_loc_2c) ordering: prove + document

must_haves:
  truths:
    - "oracle_gate_3c2e_spinor no longer asserts the stale R5 UnsupportedApi must-reject contract; it is reconciled with the PRE-EXISTING passing vendor byte-identity test test_int3c2e_ip1_spinor_adversarial_parity (spinor_deriv_parity.rs) as the primary byte-identity source, and the two gates are CONSISTENT (never contradictory)."
    - "INT3C2E_IP1_SPINOR is confirmed byte-identical to vendor int3c2e_ip1_spinor (aux-k sized SPHERICAL = (2lk+1)*nctr): test_int3c2e_ip1_spinor_adversarial_parity passes under the double gate, establishing branch-3a (the family is correct, the must-reject gate is merely stale)."
    - "A multi-shell (>=3 shells), segmented same-l, spinor GLOBAL-assembly parity test exists comparing cintx's full spinor matrix vs vendored libcint element-by-element."
    - "The global-ordering test includes at least one l>0 shell so a non-square block participates and ordering is non-trivial."
    - "A CONCLUSION doc records the libcint-faithful disposition of cintx's global spinor AO ordering (prove + document; NO PySCF-compat ordering mode added)."
  artifacts:
    - path: "crates/cintx-oracle/tests/oracle_gate_closure.rs"
      provides: "oracle_gate_3c2e_spinor updated to an honest state consistent with the pre-existing passing adversarial parity (real vendor byte-identity OR thin evaluates-Ok+nonzero assertion cross-referencing test_int3c2e_ip1_spinor_adversarial_parity); honest RED/#[ignore]+BLOCKER ONLY if the adversarial test is found to actually fail"
      contains: "oracle_gate_3c2e_spinor"
    - path: "crates/cintx-oracle/tests/spinor_deriv_parity.rs"
      provides: "Pre-existing passing vendor byte-identity proof for INT3C2E_IP1_SPINOR (test_int3c2e_ip1_spinor_adversarial_parity, NOT #[ignore]d) — the primary/adversarial coverage the gate cross-references"
      contains: "test_int3c2e_ip1_spinor_adversarial_parity"
    - path: "crates/cintx-oracle/tests/spinor_global_ao_order_parity.rs"
      provides: "Multi-shell global spinor AO ordering vendor parity test"
      min_lines: 80
    - path: ".planning/quick/260601-d7e-spinor-3c2e-gate-and-global-ao-order/CONCLUSION-ao_loc_2c.md"
      provides: "Documented disposition of the ao_loc_2c ordering question"
  key_links:
    - from: "oracle_gate_3c2e_spinor"
      to: "test_int3c2e_ip1_spinor_adversarial_parity (spinor_deriv_parity.rs)"
      via: "doc-comment cross-reference to the pre-existing passing byte-identity test; gates kept consistent"
      pattern: "test_int3c2e_ip1_spinor_adversarial_parity"
    - from: "oracle_gate_3c2e_spinor"
      to: "vendor_ffi::vendor_int3c2e_ip1_spinor"
      via: "byte-identity reconciliation with SPHERICAL aux-k sizing (2lk+1) (if kept as its own STO-3G parity gate)"
      pattern: "vendor_int3c2e_ip1_spinor"
    - from: "spinor_global_ao_order_parity.rs"
      to: "RawApiId::INT1E_OVLP_SPINOR + vendor_int1e_ovlp_spinor"
      via: "per-shell-pair eval_raw, stitch_block global assembly, count_mismatches==0"
      pattern: "count_mismatches"
---

<objective>
Resolve two independent spinor follow-ups spun off from quick task 260601-aty, both
against vendored libcint byte-identity:

- **Sub-problem A:** The `oracle_gate_3c2e_spinor` test still encodes the obsolete
  Phase-21-06 R5 "must-reject" contract (asserts `INT3C2E_IP1_SPINOR` returns
  `UnsupportedApi`). cintx now returns `Ok(...)` — the family started evaluating.
  CRUCIALLY, a passing vendor byte-identity test for this exact family ALREADY EXISTS and
  is NOT `#[ignore]`d: `test_int3c2e_ip1_spinor_adversarial_parity`
  (`spinor_deriv_parity.rs:274`) evaluates vendor `int3c2e_ip1_spinor` with SPHERICAL aux-k
  sizing and asserts `count_mismatches == 0` on a p×d×s nctr_bra=2 adversarial fixture. So
  the family is ALREADY PROVEN byte-identical to vendor (branch-3a) — the `oracle_gate_3c2e_spinor`
  must-reject gate is purely STALE. Reconcile the stale gate with that pre-existing evidence
  into a CONSISTENT honest state; do NOT manufacture a false branch-3b BLOCKER.

- **Sub-problem B:** Prove + document the spinor global AO (`ao_loc_2c`) ordering question.
  Add a >=3-shell segmented multi-shell-same-l spinor GLOBAL-assembly parity test
  (cintx full matrix vs vendor libcint). Per the LOCKED decision: if it passes (expected),
  conclude cintx is libcint-faithful and the pyscf-rs permutation is a PySCF-convention
  concern owned by pyscf-rs — document it, do NOT change cintx ordering, do NOT add a
  PySCF-compat mode. Only a FAILURE means a real cintx-vs-libcint bug to fix minimally.

Purpose: Close two stale/unproven spinor items with libcint byte-identity evidence,
keeping the libcint-compat core untouched unless a real divergence forces a minimal fix.
Output: Updated gate test, new global-ordering parity test, and a CONCLUSION doc.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@.planning/quick/260601-aty-spinor-general-contraction-nctr-gt1/deferred-items.md

# Sub-problem A — the stale gate + its REAL vendor reference + the PRE-EXISTING passing parity
@crates/cintx-oracle/tests/oracle_gate_closure.rs   # oracle_gate_3c2e_spinor at ~1339; vendor_ffi_3c2e_spinor_nonzero at ~1296 (sizing pattern)
@crates/cintx-oracle/tests/spinor_deriv_parity.rs   # test_int3c2e_ip1_spinor_adversarial_parity at 274 — PRE-EXISTING passing vendor byte-identity proof (NOT #[ignore]d); collect_vendor_3c (186) uses SPHERICAL aux-k (shell_nsph_full); collect_cintx_3c (151); build_adversarial_spinor_fixture
@crates/cintx-oracle/src/vendor_ffi.rs              # vendor_int3c2e_ip1_spinor at 5115 (aux-k SPHERICAL doc at 5112-5114)
@crates/cintx-oracle/src/compare.rs                 # int3c2e_ip1_spinor -> RawApiId at 351

# Sub-problem B — global-assembly harness to extend (NOTE: N_SHELLS/N_ATOMS are const=2 here)
@crates/cintx-oracle/tests/one_electron_scalar_spinor_parity.rs  # collect_cintx_spinor / collect_vendor_spinor / stitch_block / count_mismatches / build_two_p_spinor
@crates/cintx-compat/src/helpers.rs                 # CINTshells_spinor_offset / write_offsets at 177-223 (shell-order, libcint-faithful)

<interfaces>
<!-- Contracts the executor uses directly — no codebase exploration needed. -->

From crates/cintx-compat/src/raw (re-exported, used in tests):
```rust
pub unsafe fn eval_raw(
    api_id: RawApiId,
    out: Option<&mut [f64]>,
    out_im: Option<&mut [f64]>,   // pass None — interleaved-complex goes in `out`
    shls: &[i32],
    atm: &[i32], bas: &[i32], env: &[f64],
    opt: Option<...>, cache: Option<...>,
) -> Result<RawEvalSummary, cintxRsError>;
// RawApiId::INT3C2E_IP1_SPINOR, RawApiId::INT1E_OVLP_SPINOR
// BAS_SLOTS, ATM_SLOTS, ANG_OF, NCTR_OF, etc. as already imported in the test files.
```

From crates/cintx-oracle/src/vendor_ffi.rs (only under has_vendor_libcint):
```rust
pub fn vendor_int3c2e_ip1_spinor(out:&mut[f64], shls:&[i32;3], atm:&[i32], natm:i32, bas:&[i32], nbas:i32, env:&[f64]) -> i32;
// out length = 3 * ni_sp * nj_sp * nk_SPHERICAL * 2  (aux-k = 2lk+1 * nctr_k, NOT spinor)
pub fn vendor_int1e_ovlp_spinor(out:&mut[f64], shls:&[i32;2], atm:&[i32], natm:i32, bas:&[i32], nbas:i32, env:&[f64]) -> i32;
pub fn vendor_CINTcgto_spinor(bas_id:i32, bas:&[i32]) -> i32;   // nctr*(4l+2)
pub fn vendor_CINTcgto_spheric(bas_id:i32, bas:&[i32]) -> i32;  // nctr*(2l+1) — use for aux-k!
```

Pre-existing passing 3c spinor parity proof in spinor_deriv_parity.rs (reuse / cross-reference,
do NOT rewrite):
```rust
// collect_vendor_3c sizes aux-k SPHERICAL via shell_nsph_full(bas, SK) (line 186-202).
// test_int3c2e_ip1_spinor_adversarial_parity (line 274) — NOT #[ignore]d:
//   vendor = collect_vendor_3c(vendor_int3c2e_ip1_spinor, 3, ...);
//   cintx  = collect_cintx_3c(RawApiId::INT3C2E_IP1_SPINOR, 3, ...);
//   assert count_mismatches(&vendor, &cintx, 1e-12, 0.0) == 0;  // PASSES (branch-3a established)
```

Existing harness helpers in one_electron_scalar_spinor_parity.rs (copy the patterns; do NOT
reuse the const N_SHELLS=2 collectors verbatim for the >=3-shell test — write a shell-count-
generic local collector instead):
```rust
fn stitch_block(matrix:&mut[f64], out:&[f64], ni:usize, nj:usize, n_sp:usize, row_offset:usize, col_offset:usize);
//   src=(jj*ni+ii)*2 ; dst=((col_offset+jj)*n_sp + (row_offset+ii))*2  (column-major, bra fastest)
fn count_mismatches(reference:&[f64], observed:&[f64], atol:f64, rtol:f64) -> usize;  // ATOL=1e-12, RTOL=0.0
fn spinor_len_kappa0(l:i32) -> usize;  // 4*l+2
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task A: Reconcile the stale oracle_gate_3c2e_spinor gate with the pre-existing passing parity</name>
  <files>crates/cintx-oracle/tests/oracle_gate_closure.rs</files>
  <action>
The test `oracle_gate_3c2e_spinor` (~line 1339) currently asserts cintx
`eval_raw(INT3C2E_IP1_SPINOR, ...)` returns `Err(UnsupportedApi)`. cintx now returns
`Ok(...)`, so the gate is stale. A passing vendor byte-identity test for this exact family
ALREADY EXISTS and is NOT `#[ignore]`d:
`crates/cintx-oracle/tests/spinor_deriv_parity.rs:274` →
`test_int3c2e_ip1_spinor_adversarial_parity`. It evals vendor via
`collect_vendor_3c(vendor_int3c2e_ip1_spinor, 3, ...)` with SPHERICAL aux-k sizing
(`shell_nsph_full(bas, SK)`), evals cintx via
`collect_cintx_3c(RawApiId::INT3C2E_IP1_SPINOR, 3, ...)`, and asserts
`count_mismatches(&vendor, &cintx, 1e-12, 1e-12) == 0` on a p×d×s nctr_bra=2 adversarial
fixture. So the 3c2e_ip1 spinor family is ALREADY PROVEN byte-identical to vendor — this is
branch-3a — and `oracle_gate_3c2e_spinor` is purely STALE.

1. **FIRST confirm the pre-existing parity still passes under the double gate.** Run
   `test_int3c2e_ip1_spinor_adversarial_parity` under `--features cpu` +
   `CINTX_ORACLE_BUILD_VENDOR=1`. If it passes (EXPECTED) → branch-3a confirmed: the family
   is correct, the `oracle_gate_3c2e_spinor` must-reject gate is stale. Proceed to step 2.
   Only if it actually FAILS do you fall to the branch-3b contingency in step 3.

2. **Branch-3a (expected): update `oracle_gate_3c2e_spinor` to a CONSISTENT honest state.**
   Choose ONE of:
   - **(preferred) Real vendor byte-identity gate on its own STO-3G triple.** Replace the
     `matches!(..., UnsupportedApi)` body with a vendor parity gate on H2O STO-3G shells
     (si,sj,sk)=(3,4,0). Size aux-k SPHERICAL (CRITICAL — memory:
     libcint_3c_spinor_auxk_spherical): bra i and ket j as SPINOR
     (`vendor_CINTcgto_spinor` = nctr*(4l+2)), but aux-k as SPHERICAL
     (`vendor_CINTcgto_spheric(sk, &bas)` = nctr*(2lk+1)). Buffer length =
     `3 * ni_sp * nj_sp * nk_sph * 2`. Evaluate cintx into a buffer sized with cintx's own
     aux-k dim, assert the cintx/vendor buffer lengths are EQUAL first (latent aux-k
     mis-sizing guard), then `count_mismatches(vendor, cintx, 1e-12, 0.0)==0` with
     assert_any_nonzero on both. Rename off the R5 framing (e.g.
     `oracle_gate_3c2e_ip1_spinor_vendor_parity`) and add a doc-comment cross-referencing
     `test_int3c2e_ip1_spinor_adversarial_parity` (spinor_deriv_parity.rs) as the
     primary/adversarial byte-identity coverage. Update the println to the parity message.
   - **(acceptable) Thin evaluates-Ok+nonzero assertion.** If duplicating the STO-3G parity
     here adds no value over the existing adversarial test, replace the must-reject body with
     a thin assertion that `eval_raw(INT3C2E_IP1_SPINOR, ...)` returns `Ok` and the output has
     a nonzero element, plus a doc-comment pointing to
     `test_int3c2e_ip1_spinor_adversarial_parity` for the byte-identity proof.
   Either way: DROP the R5 must-reject framing entirely, and keep the two gates CONSISTENT
   (the gate must NOT assert UnsupportedApi while the adversarial test proves Ok+byte-identity).
   Do NOT produce a false branch-3b BLOCKER when the adversarial test passes.

3. **Branch-3b (CONTINGENCY ONLY — if step 1 found the adversarial test actually FAILS):**
   do NOT keep a wrong-but-Ok result and do NOT silently relax. Leave `oracle_gate_3c2e_spinor`
   in a state that fails honestly (keep an assertion that the current behavior is wrong) OR
   mark it `#[ignore]` with a clear reason string, and record a BLOCKER in the SUMMARY
   presenting TWO options for the user: (1) RECOMMENDED — restore a genuine fail-closed
   `UnsupportedApi` guard in the int3c2e_ip1 spinor dispatch so the family does not emit wrong
   numbers (conservative honest stop), or (2) fix the family. Recommend (1) unless the
   divergence is a trivial layout/sizing fix you can prove against vendor in this same task. If
   you take (1) or (2), scope the edit minimally and stay OUT of the 260601-aty nctr>1 code
   paths (two_electron.rs / one_electron.rs Spinor arms).

Whichever branch: the gate must end in an HONEST state — green-by-real-parity (or thin
Ok+nonzero with cross-reference) in branch-3a, or red/ignored-with-BLOCKER in the 3b
contingency. Never green-by-stale-assumption, and never contradict the adversarial test.

Keep `vendor_ffi_3c2e_spinor_nonzero` (~1296) as-is; it is a separate plain-3c2e sanity check.
  </action>
  <verify>
    <automated>CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test spinor_deriv_parity test_int3c2e_ip1_spinor_adversarial_parity -- --nocapture && CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test oracle_gate_closure oracle_gate_3c2e_spinor -- --nocapture</automated>
  </verify>
  <done>Branch-3a (expected): `test_int3c2e_ip1_spinor_adversarial_parity` is confirmed PASSING under the double gate, AND `oracle_gate_3c2e_spinor` is updated to a consistent honest state — either a real vendor byte-identity gate (count_mismatches==0, nonzero>0, aux-k sized SPHERICAL 2lk+1) or a thin Ok+nonzero assertion — with a doc-comment cross-referencing `test_int3c2e_ip1_spinor_adversarial_parity` as the byte-identity source. The stale R5 "must return UnsupportedApi (Ok is wrong)" framing is gone and the two gates are CONSISTENT (no contradiction). Branch-3b (only if the adversarial test actually fails): oracle_gate_3c2e_spinor is RED or #[ignore] with a clear reason AND a BLOCKER + two-option recommendation recorded in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task B: Multi-shell global spinor AO ordering vendor parity test</name>
  <files>crates/cintx-oracle/tests/spinor_global_ao_order_parity.rs</files>
  <action>
Create a NEW test file proving cintx's global spinor AO ordering is libcint-faithful on a
SEGMENTED multi-shell-same-l basis (the configuration the pyscf-rs report flagged). The
existing harness in one_electron_scalar_spinor_parity.rs hardcodes `N_SHELLS=2`/`N_ATOMS=2`
consts, so do NOT reuse those collectors — write a shell-count-generic version in the new file.

1. **Fixture (>=3 shells, segmented, same-l repeats, at least one l>0).** Build an explicit
   shell order that interleaves repeated angular momenta so a cross-shell global permutation
   would surface, e.g. shells `[s, p, s, p]` (l = 0,1,0,1) on 2 atoms — or use H2O STO-3G's
   natural O 1s + O 2s (two l=0) + O 2p + H 1s + H 1s order. Requirements: >=3 shells, at
   least two shells sharing one l value (the segmented case), at least one l>0 shell (so a
   non-square block participates), all nctr==1 (this task is about global ORDERING, not
   contraction — keep it independent of 260601-aty). Reserve env[0..PTR_ENV_START) for libcint
   global slots; place coords/exps/coeffs after, mirroring build_two_p_spinor.

2. **Generic global collectors.** Write `collect_cintx_global(api_id, atm, bas, env, n_shells)`
   and `collect_vendor_global(vendor_fn, atm, bas, env, n_shells)`:
   - per-shell spinor dim = `spinor_len_kappa0(l)` (4l+2; nctr==1).
   - n_sp = sum over shells; allocate `n_sp*n_sp*2` interleaved-complex matrix.
   - loop ALL shell pairs (si,sj), eval each block, stitch into the global matrix via the SAME
     column-major bra-fastest formula as stitch_block: `dst=((col_offset+jj)*n_sp+(row_offset+ii))*2`,
     `src=(jj*ni+ii)*2`; advance row_offset by ni (shell order), col_offset by nj. This is the
     exact global-assembly that exposes a cross-shell ordering permutation. Copy stitch_block /
     count_mismatches / spinor_len_kappa0 patterns verbatim (private fns in the new file).

3. **Parity test (use INT1E_OVLP_SPINOR — a REAL vendor driver, scalar spinor, stable).**
   `#[cfg(has_vendor_libcint)] #[cfg(feature="cpu")]`: assemble cintx and vendor global
   matrices, assert_any_nonzero on both, assert lengths equal, then
   `count_mismatches(vendor, cintx, 1e-12, 0.0) == 0`. Add a fixture guard asserting >=3 shells,
   a repeated l, and at least one l>0 (self-documenting, like assert_fixture_asymmetric).
   Also add a non-vendor `#[cfg(feature="cpu")]` smoke test that just evaluates the cintx
   global matrix and asserts size + nonzero (so the file has coverage without vendor).

4. **Expected outcome = PASS.** cintx == libcint (both assemble in shell order via the same
   cumulative offset logic as CINTshells_spinor_offset / libcint shells_cgto_offset). If it
   PASSES, the deliverable is this test + the CONCLUSION doc in Task C. If it FAILS, that is a
   real cintx-vs-libcint bug: record it as a BLOCKER in the SUMMARY and scope a MINIMAL fix to
   the global stitch/offset to match libcint (do NOT add a PySCF-compat ordering mode — that is
   the LOCKED prohibition). Do not invent a permutation layer to make it pass.
  </action>
  <verify>
    <automated>CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test spinor_global_ao_order_parity -- --nocapture (double gate REQUIRED: --features cpu AND CINTX_ORACLE_BUILD_VENDOR=1, else parity SILENTLY SKIPS). The non-vendor smoke test must also pass under plain --features cpu.</automated>
  </verify>
  <done>spinor_global_ao_order_parity.rs exists with: a >=3-shell segmented same-l fixture (>=1 shell l>0, all nctr==1), generic shell-count global collectors, and a vendor byte-identity test that PASSES (count_mismatches==0, nonzero>0) — OR, if it fails, a BLOCKER + minimal libcint-matching fix recorded in the SUMMARY with NO PySCF-compat mode added. A fixture guard enforces the >=3-shell/repeated-l/l>0 shape.</done>
</task>

<task type="auto">
  <name>Task C: CONCLUSION doc for the ao_loc_2c ordering question</name>
  <files>.planning/quick/260601-d7e-spinor-3c2e-gate-and-global-ao-order/CONCLUSION-ao_loc_2c.md</files>
  <action>
Write a short conclusion doc capturing the Sub-problem B disposition (honoring the LOCKED
decision: prove + document, no PySCF-compat mode). Contents:
- **Question:** does cintx's global spinor AO (`ao_loc_2c`-equivalent) ordering match
  vendored libcint on a segmented multi-shell-same-l basis?
- **Evidence:** name the new test `spinor_global_ao_order_parity.rs`, its fixture shape
  (>=3 shells, segmented same-l, >=1 l>0), and the measured result
  (count_mismatches vs vendor at atol=1e-12 — fill in the ACTUAL number from Task B).
- **Finding (expected):** cintx's `CINTshells_spinor_offset` / `write_offsets` builds
  cumulative per-shell offsets in SHELL ORDER, mirroring libcint `shells_cgto_offset`
  (cint_bas.c) EXACTLY; neither groups by angular momentum. cintx == libcint.
- **Conclusion:** cintx's global spinor AO ordering is libcint-faithful. The pyscf-rs
  permutation report is a PySCF `ao_loc_2c` CONVENTION difference (PySCF groups/orders AOs
  differently than libcint's native shell order) and belongs in pyscf-rs's libcint→PySCF AO
  mapping layer — NOT a cintx defect. Per the locked decision, cintx ordering is unchanged and
  NO PySCF-compat ordering mode is added (that would risk the libcint-compat core).
- **Caveat:** if Task B's test actually FAILED, instead document the real cintx-vs-libcint
  divergence found and the minimal fix applied to match libcint.
Keep it tight (~30-50 lines). Do NOT touch any .planning/phases/** or .planning/research/**
historical docs — this new file lives only in the quick task dir.
  </action>
  <verify>
    <automated>test -f .planning/quick/260601-d7e-spinor-3c2e-gate-and-global-ao-order/CONCLUSION-ao_loc_2c.md && grep -qi "libcint-faithful\|ao_loc_2c" .planning/quick/260601-d7e-spinor-3c2e-gate-and-global-ao-order/CONCLUSION-ao_loc_2c.md && echo OK</automated>
  </verify>
  <done>CONCLUSION-ao_loc_2c.md exists, states the libcint-faithful finding (or the real divergence if Task B failed), the actual measured mismatch count, and explicitly records that NO PySCF-compat ordering mode was added per the locked decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cintx eval_raw output → oracle assertion | cintx-computed spinor integrals are the untrusted quantity being verified against the libcint ground truth |
| vendored libcint FFI → test buffer | raw C driver writes into a Rust-sized buffer; mis-sizing (aux-k spinor-vs-spherical) is an OOB/overread risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-d7e-01 | Tampering | aux-k buffer sizing in oracle_gate_3c2e_spinor | mitigate | Size aux-k SPHERICAL (vendor_CINTcgto_spheric = 2lk+1*nctr), assert cintx/vendor buffer lengths equal before value comparison; prevents the known latent aux-k mis-sizing class (memory: libcint_3c_spinor_auxk_spherical) writing/reading OOB |
| T-d7e-02 | Repudiation | green-by-stale-assumption gate | mitigate | Disposition logic forbids wrong-but-Ok AND forbids a false BLOCKER: branch-3a updates the gate consistently with the pre-existing passing adversarial parity; branch-3b (RED/ignore + BLOCKER) only if that test actually fails (Task A) |
| T-d7e-03 | Information disclosure | vendor FFI overread on undersized out buffer | accept | Buffers are computed from vendor cgto helpers with the documented SPHERICAL aux-k rule; test-only code, hermetic vendored build |
| T-d7e-04 | Elevation of privilege | accidental edit of libcint-compat core ordering | mitigate | LOCKED decision: NO PySCF-compat mode; Sub-problem B is prove+document; any fix is minimal-libcint-matching only and stays out of 260601-aty nctr>1 paths |
</threat_model>

<verification>
- Sub-problem A: `oracle_gate_3c2e_spinor` (renamed if 3a) ends HONEST and CONSISTENT with the
  pre-existing passing `test_int3c2e_ip1_spinor_adversarial_parity` — green vendor parity / thin
  Ok+nonzero with SPHERICAL aux-k and a cross-reference doc-comment, or (only if that test
  actually fails) RED/#[ignore] + BLOCKER. Stale R5 framing gone; gates never contradictory.
- Sub-problem B: `spinor_global_ao_order_parity.rs` vendor test PASSES (count_mismatches==0) on a
  >=3-shell segmented same-l (>=1 l>0) basis — or a BLOCKER + minimal libcint-matching fix.
- CONCLUSION-ao_loc_2c.md records the disposition; NO PySCF-compat mode added.
- All byte-identity verifies use the DOUBLE gate: `--features cpu` + `CINTX_ORACLE_BUILD_VENDOR=1`.
- No edits to 260601-aty nctr>1 code paths, nor to .planning/phases/** or .planning/research/**.
</verification>

<success_criteria>
- oracle_gate_3c2e_spinor reconciled with the pre-existing passing
  test_int3c2e_ip1_spinor_adversarial_parity; honest, consistent disposition (parity/thin
  Ok+nonzero green with a cross-reference; BLOCKER only if the adversarial test actually
  fails), no stale must-reject.
- A multi-shell global spinor AO ordering vendor parity test exists and passes (expected) on a
  segmented same-l basis with at least one l>0 shell.
- A CONCLUSION doc documents the libcint-faithful finding (prove + document, no PySCF-compat
  mode) — or the real divergence + minimal fix if the test failed.
</success_criteria>

<output>
After completion, create `.planning/quick/260601-d7e-spinor-3c2e-gate-and-global-ao-order/260601-d7e-SUMMARY.md`.
If either sub-problem hit a divergence/failure, the SUMMARY MUST contain a BLOCKER section with
the two-option recommendation (Task A) and/or the minimal-fix description (Task B) — do not
report green if either gate is not honestly green.
</output>
</output>
