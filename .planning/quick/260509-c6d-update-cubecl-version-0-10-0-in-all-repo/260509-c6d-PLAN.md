---
phase: quick-260509-c6d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/cintx-cubecl/Cargo.toml
  - Cargo.lock
  - crates/cintx-cubecl/src/**
  - CLAUDE.md
  - AGENTS.md
  - .planning/MILESTONES.md
  - .planning/STATE.md
  - .planning/research/SUMMARY.md
  - .planning/research/STACK.md
  - .planning/phases/**/*.md
autonomous: true
requirements:
  - QUICK-260509-c6d-CUBECL-BUMP
must_haves:
  truths:
    - "All Cargo.toml manifests pin cubecl, cubecl-wgpu, cubecl-runtime to 0.10.0"
    - "Cargo.lock resolves cubecl, cubecl-wgpu, cubecl-runtime, cubecl-cpu to 0.10.0"
    - "cargo build --workspace and cargo build --workspace --all-features both succeed"
    - "cargo test --workspace --features cpu passes (cubecl-related tests must pass; pre-existing unrelated failures must be isolated and documented)"
    - "CLAUDE.md and AGENTS.md tech stack tables list cubecl 0.10.0 with rationale referencing the 2026-05-07 release"
    - "No stale cubecl 0.9.0 / 0.9.x / cubecl-wgpu 0.9.0 / cubecl-runtime 0.9.0 strings remain in .planning/**/*.md historical docs"
  artifacts:
    - path: "Cargo.toml"
      provides: "Root workspace dep pin cubecl = \"0.10.0\""
      contains: "cubecl = \"0.10.0\""
    - path: "crates/cintx-cubecl/Cargo.toml"
      provides: "Backend crate dep pins for cubecl, cubecl-wgpu, cubecl-runtime at 0.10.0"
      contains: "cubecl-wgpu = \"0.10.0\""
    - path: "Cargo.lock"
      provides: "Resolved cubecl family at 0.10.0"
      contains: "name = \"cubecl\"\nversion = \"0.10.0\""
    - path: "CLAUDE.md"
      provides: "Tech stack guidance row reflecting 0.10.0 pin"
      contains: "cubecl"
    - path: "AGENTS.md"
      provides: "Mirror of CLAUDE.md tech stack guidance row"
      contains: "cubecl"
  key_links:
    - from: "Cargo.toml"
      to: "Cargo.lock"
      via: "cargo update -p cubecl -p cubecl-wgpu -p cubecl-runtime --precise 0.10.0"
      pattern: "version = \"0.10.0\""
    - from: "crates/cintx-cubecl/src/**"
      to: "cubecl 0.10.0 API surface"
      via: "cargo build --workspace --all-features"
      pattern: "compiles cleanly against cubecl 0.10.0"
    - from: "CLAUDE.md / AGENTS.md tech stack table"
      to: ".planning/**/*.md historical docs"
      via: "consistent 0.10.0 references"
      pattern: "cubecl.*0\\.10\\.0"
---

<objective>
Bump the cubecl family of dependencies (cubecl, cubecl-wgpu, cubecl-runtime, transitive cubecl-cpu) from 0.9.0 to 0.10.0 across every Cargo manifest, regenerate Cargo.lock, fix any source-level breakage exposed by the SemVer-breaking 0.9 -> 0.10 transition, and update both project-instruction files (CLAUDE.md, AGENTS.md) plus historical planning documents under .planning/ so version references stay coherent.

Purpose: cubecl 0.10.0 was published to crates.io on 2026-05-07. The user explicitly approved this version bump including the override of CLAUDE.md guidance (which previously said "Keep the current 0.9.x line unless a verified backend issue forces a change"). Keeping a single, current pin across manifests + tech-stack tables + historical docs prevents drift and keeps oracle / manifest reproducibility credible.

Output:
- Updated Cargo.toml + crates/cintx-cubecl/Cargo.toml with 0.10.0 pins
- Regenerated Cargo.lock
- Patched cintx-cubecl source code if cubecl 0.10.0 introduced API breaks
- Updated CLAUDE.md and AGENTS.md tech stack tables (one row each) with new pin + rationale
- Swept .planning/**/*.md for stale cubecl 0.9.x references and updated them to 0.10.x
- A 260509-c6d-SUMMARY.md capturing observed cubecl API deltas (if any) and verification commands run
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/user/Documents/workspace/cintx/CLAUDE.md
@/home/user/Documents/workspace/cintx/AGENTS.md
@/home/user/Documents/workspace/cintx/Cargo.toml
@/home/user/Documents/workspace/cintx/crates/cintx-cubecl/Cargo.toml
@/home/user/Documents/workspace/cintx/crates/cintx-rs/Cargo.toml
@/home/user/Documents/workspace/cintx/crates/cintx-compat/Cargo.toml

<facts>
- Today is 2026-05-09. cubecl 0.10.0 was published on 2026-05-07.
- Direct cubecl pins (all currently "0.9.0"):
  - Cargo.toml line 10: `cubecl = "0.9.0"`
  - crates/cintx-cubecl/Cargo.toml line 21: `cubecl = { version = "0.9.0", features = ["wgpu"] }`
  - crates/cintx-cubecl/Cargo.toml line 22: `cubecl-wgpu = "0.9.0"`
  - crates/cintx-cubecl/Cargo.toml line 23: `cubecl-runtime = "0.9.0"`
- crates/cintx-rs and crates/cintx-compat depend on cintx-cubecl by path; no version bump required there.
- Cargo.lock currently resolves cubecl, cubecl-wgpu, cubecl-runtime, cubecl-cpu all to 0.9.0 plus a transitive set; a precise update should pull 0.10.0 across the family. Use a scoped `cargo update -p cubecl -p cubecl-wgpu -p cubecl-runtime --precise 0.10.0` first, then a wider `cargo update` if needed for transitives.
- 0.9 -> 0.10 is a SemVer-breaking minor bump (pre-1.0). The cintx-cubecl source uses: WgpuDevice, init_setup / init_setup_async, init_device, RuntimeOptions, the #[cube] attribute macro (notably array indexing semantics — see Phase 08-01 SUMMARY note about TURNOVER_POINT[m]), ComputeClient, the Runtime trait, the cubecl `cpu` feature, and CpuRuntime. Any of these may have changed. Plan to surface breakage via `cargo build --workspace --all-features` and patch in cintx-cubecl/src/**.
- Historical planning docs with cubecl 0.9.x mentions to sweep:
  - .planning/MILESTONES.md
  - .planning/STATE.md
  - .planning/research/SUMMARY.md
  - .planning/research/STACK.md (recommended row + alternatives row)
  - .planning/phases/01-manifest-planner-foundation/01-RESEARCH.md
  - .planning/phases/02-execution-compatibility-stabilization/02-PLAN-SUMMARY.md
  - .planning/phases/02-execution-compatibility-stabilization/05-PLAN.md
  - .planning/phases/05-re-implement-detailed-design-gpu-path-with-cubecl-wgpu-backend/05-RESEARCH.md (line ~608 has explicit valid-until clause — leave the date, flip the version)
  - .planning/phases/05-re-implement-detailed-design-gpu-path-with-cubecl-wgpu-backend/02-PLAN-SUMMARY.md
  - .planning/phases/06-fix-raw-eval-staging-and-capability-fingerprint/06-RESEARCH.md
  - .planning/phases/07-executor-infrastructure-rewrite/07-RESEARCH.md
  - .planning/phases/07-executor-infrastructure-rewrite/07-01-SUMMARY.md
  - .planning/phases/08-gaussian-primitive-infrastructure-and-boys-function/08-01-SUMMARY.md
  - .planning/phases/09-1e-real-kernel-and-cart-to-sph-transform/09-RESEARCH.md
  - .planning/phases/13-f12-stg-yp-kernels/13-RESEARCH.md
  - .planning/phases/14-unstable-source-api-families/14-RESEARCH.md
- DO NOT blanket-replace `0.9.0` because other deps share that string (e.g., wgpu adapter strings, num-complex 0.4.6, etc.). Use targeted grep + Edit, scoped to cubecl/cubecl-wgpu/cubecl-runtime/cubecl-cpu mentions only.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump cubecl pins in all Cargo manifests and regenerate Cargo.lock</name>
  <files>Cargo.toml, crates/cintx-cubecl/Cargo.toml, Cargo.lock</files>
  <action>
    Step 1 — Verify availability on crates.io BEFORE editing (saves a roundtrip if 0.10.0 of a sibling crate is missing):
      cargo search cubecl --limit 5
      cargo search cubecl-wgpu --limit 5
      cargo search cubecl-runtime --limit 5
    All three must report 0.10.0 or newer. If any sibling crate has not published 0.10.0 yet, STOP and report — do not proceed to edits.

    Step 2 — Edit /home/user/Documents/workspace/cintx/Cargo.toml line 10:
      Change `cubecl = "0.9.0"` → `cubecl = "0.10.0"`

    Step 3 — Edit /home/user/Documents/workspace/cintx/crates/cintx-cubecl/Cargo.toml lines 21–23:
      Change `cubecl = { version = "0.9.0", features = ["wgpu"] }` → `cubecl = { version = "0.10.0", features = ["wgpu"] }`
      Change `cubecl-wgpu = "0.9.0"` → `cubecl-wgpu = "0.10.0"`
      Change `cubecl-runtime = "0.9.0"` → `cubecl-runtime = "0.10.0"`

    Step 4 — Regenerate Cargo.lock with a scoped precise update from the workspace root:
      cargo update -p cubecl --precise 0.10.0
      cargo update -p cubecl-wgpu --precise 0.10.0
      cargo update -p cubecl-runtime --precise 0.10.0
    If transitive cubecl-cpu remains at 0.9.0 after the above, also run:
      cargo update -p cubecl-cpu --precise 0.10.0
    If any precise update fails because a sibling crate of cubecl is not yet on 0.10.0 in the registry, fall back to a single broader update for the cubecl family ONLY:
      cargo update -p cubecl --precise 0.10.0 --aggressive
    Do NOT run a blanket `cargo update` — we only want the cubecl family to move.

    Step 5 — Sanity check the lockfile:
      grep -nE 'name = "(cubecl|cubecl-wgpu|cubecl-runtime|cubecl-cpu)"' Cargo.lock | head -40
    Every cubecl-family `name = "..."` block in Cargo.lock must have its `version = "0.10.0"` (or newer 0.10.x). Any 0.9.x line in a cubecl-family block is a bug — fix before proceeding.

    NOTE: This task does NOT run cargo build or cargo test. Build + source patches are Task 2's job. Keep this task purely about manifest + lockfile changes so any source-level breakage from the 0.9 → 0.10 SemVer bump shows up cleanly in Task 2.

    Commit at the end of the task with message:
      "chore(deps): bump cubecl family to 0.10.0 in manifests and lockfile"
    Files in commit: Cargo.toml, crates/cintx-cubecl/Cargo.toml, Cargo.lock
  </action>
  <verify>
    <automated>grep -nE 'cubecl(-wgpu|-runtime)?\s*=\s*"0\.10\.0"|cubecl\s*=\s*\{\s*version\s*=\s*"0\.10\.0"' /home/user/Documents/workspace/cintx/Cargo.toml /home/user/Documents/workspace/cintx/crates/cintx-cubecl/Cargo.toml &amp;&amp; ! grep -nE '"0\.9\.[0-9]+"' /home/user/Documents/workspace/cintx/Cargo.lock | grep -E 'cubecl|cubecl-wgpu|cubecl-runtime|cubecl-cpu' &amp;&amp; grep -nE '^name = "cubecl"' -A1 /home/user/Documents/workspace/cintx/Cargo.lock | grep -E 'version = "0\.10\.'</automated>
  </verify>
  <done>
    All three manifests show 0.10.0 pins for cubecl, cubecl-wgpu, cubecl-runtime; Cargo.lock has no 0.9.x version strings inside cubecl-family blocks; the changes are committed in a single atomic commit with message starting "chore(deps): bump cubecl family to 0.10.0".
  </done>
</task>

<task type="auto">
  <name>Task 2: Build and test the workspace; patch cintx-cubecl source for any 0.10.0 API deltas</name>
  <files>crates/cintx-cubecl/src/lib.rs, crates/cintx-cubecl/src/runtime_bootstrap.rs, crates/cintx-cubecl/src/executor.rs, crates/cintx-cubecl/src/capability.rs, crates/cintx-cubecl/src/specialization.rs, crates/cintx-cubecl/src/transfer.rs, crates/cintx-cubecl/src/resident_cache.rs, crates/cintx-cubecl/src/backend/**, crates/cintx-cubecl/src/kernels/**, crates/cintx-cubecl/src/math/**, crates/cintx-cubecl/src/transform/**</files>
  <action>
    Step 1 — Run a clean build to surface any cubecl 0.10.0 API breaks:
      cargo build --workspace --locked 2>&1 | tee /tmp/cintx-c6d-build-default.log
      cargo build --workspace --all-features --locked 2>&1 | tee /tmp/cintx-c6d-build-allfeat.log
    Both invocations must use --locked so the build cannot silently re-resolve away from the freshly pinned 0.10.0 lockfile.

    Step 2 — If either build fails, the failures are almost certainly in cintx-cubecl (it is the only crate with direct cubecl deps). Review the error messages, then read the relevant cintx-cubecl source files. Likely 0.9 → 0.10 break points to inspect first:
      - WgpuDevice variant set / Default impl (used in runtime_bootstrap.rs)
      - init_setup / init_setup_async signatures (used in runtime_bootstrap.rs)
      - init_device signature (used in runtime_bootstrap.rs and tests)
      - RuntimeOptions struct fields (used in runtime_bootstrap.rs)
      - #[cube] attribute macro behaviour, especially array indexing semantics (used in math/* and kernels/*) — Phase 08-01 SUMMARY documents the historical TURNOVER_POINT[m] workaround; verify it still applies.
      - ComputeClient / Runtime trait method renames or moved associated types (used in executor.rs, transfer.rs, resident_cache.rs)
      - cubecl `cpu` feature gate / CpuRuntime path (gated by feature = "cpu" in lib.rs)
    For every breaking change, apply the smallest patch that restores compilation. Document each delta inline in the SUMMARY (Step 5). If the manual at /home/user/Documents/workspace/cintx/docs/cubecl_error_guideline.md applies (per CLAUDE.md handling-cubecl-build-errors policy), consult it BEFORE proposing fixes.

    Step 3 — Re-run the two builds until both succeed cleanly. Do NOT use --offline — 0.10.0 may pull new transitives. Do NOT bypass --locked.

    Step 4 — Run the test suite (CPU feature path is the realistic CI surface here; GPU is advisory and not required for merge per Phase 04 release-policy decisions):
      cargo test --workspace --features cpu --locked 2>&1 | tee /tmp/cintx-c6d-test-cpu.log
    All cubecl-touching tests must pass. If a pre-existing failure exists that is unrelated to cubecl (e.g., known Phase 12 spinor edge case), isolate it: capture the failing test name, confirm by `git stash; cargo test --workspace --features cpu --locked; git stash pop` (or by checking git blame on the test) that the failure exists on main BEFORE the bump. Document such failures in the SUMMARY as "pre-existing, not caused by cubecl bump" — but DO NOT mark a cubecl-related failure as pre-existing.

    Step 5 — Write /home/user/Documents/workspace/cintx/.planning/quick/260509-c6d-update-cubecl-version-0-10-0-in-all-repo/260509-c6d-SUMMARY.md capturing:
      - The verify commands run and their outcomes
      - Each cubecl 0.10.0 API delta that required a source patch (file, symbol, before → after, brief rationale). If zero deltas were needed, say so explicitly.
      - Any pre-existing test failures isolated and confirmed unrelated to the bump
      - A pointer to /tmp/cintx-c6d-build-default.log, /tmp/cintx-c6d-build-allfeat.log, /tmp/cintx-c6d-test-cpu.log

    Commit at the end of the task with message:
      "fix(cintx-cubecl): adapt to cubecl 0.10.0 API"
    (or, if zero source patches were needed: "test(cintx-cubecl): verify cubecl 0.10.0 builds and tests cleanly")
    Files in commit: any cintx-cubecl/src/** files patched in Step 2, plus the SUMMARY.md.
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/cintx &amp;&amp; cargo build --workspace --locked &amp;&amp; cargo build --workspace --all-features --locked &amp;&amp; cargo test --workspace --features cpu --locked</automated>
  </verify>
  <done>
    Both `cargo build --workspace --locked` and `cargo build --workspace --all-features --locked` succeed; `cargo test --workspace --features cpu --locked` passes (or any failure is isolated and documented as pre-existing in the SUMMARY). 260509-c6d-SUMMARY.md exists with API-delta notes. Source patches and SUMMARY are in a single atomic commit.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update CLAUDE.md, AGENTS.md, and historical planning docs to reflect cubecl 0.10.0</name>
  <files>CLAUDE.md, AGENTS.md, .planning/MILESTONES.md, .planning/STATE.md, .planning/research/SUMMARY.md, .planning/research/STACK.md, .planning/phases/01-manifest-planner-foundation/01-RESEARCH.md, .planning/phases/02-execution-compatibility-stabilization/02-PLAN-SUMMARY.md, .planning/phases/02-execution-compatibility-stabilization/05-PLAN.md, .planning/phases/05-re-implement-detailed-design-gpu-path-with-cubecl-wgpu-backend/05-RESEARCH.md, .planning/phases/05-re-implement-detailed-design-gpu-path-with-cubecl-wgpu-backend/02-PLAN-SUMMARY.md, .planning/phases/06-fix-raw-eval-staging-and-capability-fingerprint/06-RESEARCH.md, .planning/phases/07-executor-infrastructure-rewrite/07-RESEARCH.md, .planning/phases/07-executor-infrastructure-rewrite/07-01-SUMMARY.md, .planning/phases/08-gaussian-primitive-infrastructure-and-boys-function/08-01-SUMMARY.md, .planning/phases/09-1e-real-kernel-and-cart-to-sph-transform/09-RESEARCH.md, .planning/phases/13-f12-stg-yp-kernels/13-RESEARCH.md, .planning/phases/14-unstable-source-api-families/14-RESEARCH.md</files>
  <action>
    Step 1 — Enumerate every cubecl-family version mention under .planning/ and at the repo root, scoped narrowly so unrelated 0.9.x strings (wgpu adapters, num-complex, etc.) are NOT touched. Run BOTH searches and merge the results:
      grep -rn -E "cubecl[^[:space:]]*[[:space:]]*[=:`'\"][[:space:]`'\"]*0\.9\.[0-9x]+" --include="*.md" /home/user/Documents/workspace/cintx/.planning /home/user/Documents/workspace/cintx/CLAUDE.md /home/user/Documents/workspace/cintx/AGENTS.md
      grep -rn -B1 -A1 -E "0\.9\.(0|x)" --include="*.md" /home/user/Documents/workspace/cintx/.planning /home/user/Documents/workspace/cintx/CLAUDE.md /home/user/Documents/workspace/cintx/AGENTS.md | grep -B1 -A1 -i "cubecl"
    Build the canonical list of (file, line, current text) triples. Save to /tmp/cintx-c6d-cubecl-mentions.txt for traceability.

    Step 2 — Update CLAUDE.md tech stack table row for `cubecl`:
      Find the row beginning `| `cubecl` |` (currently: `Keep the current `0.9.x` line unless a verified backend issue forces a change | Shared GPU compute backend | `docs.rs` shows `cubecl 0.9.0` as the latest published crate. ...`).
      Replace with: `Pin `0.10.0` | Shared GPU compute backend | cubecl 0.10.0 was published on 2026-05-07 and is the current published line as of 2026-05-09; pinning an exact version preserves oracle reproducibility. The public API stays backend-agnostic enough that a backend swap remains possible if the ecosystem shifts.`
      Keep the table column structure intact. Do not delete the row.

    Step 3 — Mirror the same change in AGENTS.md (it has the same row, around line 34 of the snapshot read in <files_to_read>). Use the exact same replacement text so the two files stay byte-identical in that row.

    Step 4 — Sweep .planning/**/*.md and apply targeted edits, NOT a blanket replace. For each match in the /tmp/cintx-c6d-cubecl-mentions.txt list:
      - If the match is `cubecl 0.9.0`, `cubecl-wgpu 0.9.0`, `cubecl-runtime 0.9.0`, `cubecl-cpu 0.9.0` → change `0.9.0` to `0.10.0`.
      - If the match is `cubecl 0.9.x` or `CubeCL 0.9.x` → change `0.9.x` to `0.10.x`.
      - If the match is the explicit valid-until clause in .planning/phases/05-re-implement-detailed-design-gpu-path-with-cubecl-wgpu-backend/05-RESEARCH.md (line ~608: `Valid until: 2026-05-03 (CubeCL 0.9.x is stable; check if 0.10+ releases before next phase)`) → leave the date, but flip the version: `Valid until: 2026-05-03 (CubeCL 0.10.x is stable as of 2026-05-07; tracked from now until next-phase reassessment)`.
      - If the surrounding sentence makes a claim that 0.9.x is the latest published line, update the claim to reference 0.10.x as of 2026-05-07.
      - Where a sentence refers to "CubeCL 0.9.x" as a code-pattern caveat (e.g., the Phase 08-01 SUMMARY note about `TURNOVER_POINT[m] ... in CubeCL 0.9.x`), replace with `CubeCL 0.9.x (carried forward into 0.10.0; revalidated by Task 2 of plan 260509-c6d)` so historical context is preserved without leaving a misleading version-only string.
      DO NOT touch any non-cubecl 0.9.x string. Verify by ensuring every Edit's surrounding context contains the substring `cubecl` or `CubeCL`.

    Step 5 — After all edits, prove the sweep is complete:
      grep -rn -E "cubecl[^[:space:]]*[[:space:]]*[=:`'\"][[:space:]`'\"]*0\.9\.[0-9x]+" --include="*.md" /home/user/Documents/workspace/cintx/.planning /home/user/Documents/workspace/cintx/CLAUDE.md /home/user/Documents/workspace/cintx/AGENTS.md
    must return zero matches. Likewise:
      grep -rn -i -E "cubecl.{0,40}0\.9\.(0|x)" --include="*.md" /home/user/Documents/workspace/cintx/.planning /home/user/Documents/workspace/cintx/CLAUDE.md /home/user/Documents/workspace/cintx/AGENTS.md
    must return zero matches.

    Commit at the end of the task with message:
      "docs: update cubecl pin to 0.10.0 in CLAUDE.md, AGENTS.md, and historical planning docs"
    Files in commit: every file actually edited (do not stage files that did not change).
  </action>
  <verify>
    <automated>! grep -rn -E "cubecl[^[:space:]]*[[:space:]]*[=:\`'\"][[:space:]\`'\"]*0\.9\.[0-9x]+" --include="*.md" /home/user/Documents/workspace/cintx/.planning /home/user/Documents/workspace/cintx/CLAUDE.md /home/user/Documents/workspace/cintx/AGENTS.md &amp;&amp; ! grep -rn -i -E "cubecl.{0,40}0\.9\.(0|x)" --include="*.md" /home/user/Documents/workspace/cintx/.planning /home/user/Documents/workspace/cintx/CLAUDE.md /home/user/Documents/workspace/cintx/AGENTS.md &amp;&amp; grep -E "cubecl.{0,40}0\.10\.0" /home/user/Documents/workspace/cintx/CLAUDE.md /home/user/Documents/workspace/cintx/AGENTS.md</automated>
  </verify>
  <done>
    Zero stale cubecl 0.9.x / 0.9.0 references remain across CLAUDE.md, AGENTS.md, and .planning/**/*.md. CLAUDE.md and AGENTS.md tech stack tables list cubecl 0.10.0 with the 2026-05-07 release rationale. The historical valid-until clause in 05-RESEARCH.md preserves its original date but reflects the new version. All edits are committed in a single atomic commit with message starting "docs:".
  </done>
</task>

</tasks>

<verification>
After all three tasks:

1. Manifest pins (root + cintx-cubecl):
   grep -nE 'cubecl(-wgpu|-runtime)?\s*=' /home/user/Documents/workspace/cintx/Cargo.toml /home/user/Documents/workspace/cintx/crates/cintx-cubecl/Cargo.toml
   Every cubecl-family line shows `"0.10.0"`.

2. Lockfile resolution:
   grep -A1 -E '^name = "(cubecl|cubecl-wgpu|cubecl-runtime|cubecl-cpu)"$' /home/user/Documents/workspace/cintx/Cargo.lock | grep version
   Every version line shows `"0.10.0"` (or newer 0.10.x).

3. Build + test:
   cd /home/user/Documents/workspace/cintx && cargo build --workspace --locked && cargo build --workspace --all-features --locked && cargo test --workspace --features cpu --locked
   All three succeed.

4. Doc consistency:
   grep -rn -i -E "cubecl.{0,40}0\.9\.(0|x)" --include="*.md" /home/user/Documents/workspace/cintx/.planning /home/user/Documents/workspace/cintx/CLAUDE.md /home/user/Documents/workspace/cintx/AGENTS.md
   Returns zero matches.

5. SUMMARY artifact:
   test -f /home/user/Documents/workspace/cintx/.planning/quick/260509-c6d-update-cubecl-version-0-10-0-in-all-repo/260509-c6d-SUMMARY.md
</verification>

<success_criteria>
- Cargo.toml + crates/cintx-cubecl/Cargo.toml pin cubecl, cubecl-wgpu, cubecl-runtime to "0.10.0"
- Cargo.lock resolves the cubecl family (cubecl, cubecl-wgpu, cubecl-runtime, cubecl-cpu) to 0.10.0
- `cargo build --workspace --locked` succeeds (default features)
- `cargo build --workspace --all-features --locked` succeeds (with-f12, with-4c1e, unstable-source-api, capi, cpu all on)
- `cargo test --workspace --features cpu --locked` passes; any pre-existing unrelated failure is isolated and documented in the SUMMARY
- CLAUDE.md and AGENTS.md tech stack tables show cubecl pinned to 0.10.0 with the 2026-05-07 release rationale
- No cubecl 0.9.x / 0.9.0 / cubecl-wgpu 0.9.0 / cubecl-runtime 0.9.0 strings remain anywhere in CLAUDE.md, AGENTS.md, or .planning/**/*.md
- Three atomic commits exist (manifest+lock, source+SUMMARY, docs sweep), each scoped to its task
- SUMMARY.md captures observed cubecl API deltas (or explicitly states none were needed)
</success_criteria>

<output>
After completion, ensure the following file exists and is committed:
.planning/quick/260509-c6d-update-cubecl-version-0-10-0-in-all-repo/260509-c6d-SUMMARY.md

The SUMMARY must include:
- Verify commands run and their pass/fail status
- Cubecl 0.10.0 API deltas observed (file + symbol + before/after) or an explicit "no source patches required" statement
- Pre-existing test failures isolated as unrelated to the bump (if any)
- Pointers to /tmp/cintx-c6d-build-default.log, /tmp/cintx-c6d-build-allfeat.log, /tmp/cintx-c6d-test-cpu.log
</output>
