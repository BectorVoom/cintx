---
phase: quick-260529-ne7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cintx-oracle/src/fixtures.rs
autonomous: true
requirements: [ORACLE-2E-PARITY]
must_haves:
  truths:
    - "Vendored libcint computes full-Coulomb 2e integrals for the sample() fixture (not range-separated), because env[8]=PTR_RANGE_OMEGA is 0.0."
    - "cintx vs vendor 2e legacy parity (cint2e_sph / cint2e_cart) now matches at atol=1e-12."
    - "1e legacy parity (ovlp/kin/nuc) remains green with identical values to before."
    - "The physical basis (4 single-primitive shells, exps/coeffs/coords, one grid point) and shls2/3/4 + per-shell sizes are unchanged."
    - "All env[0..PTR_ENV_START(=20)] slots are zero except the two legitimate grid slots env[NGRIDS(=11)] and env[PTR_GRIDS(=12)]."
  artifacts:
    - path: "crates/cintx-oracle/src/fixtures.rs"
      provides: "OracleRawInputs::sample() with conformant PTR_ENV_START-aligned env layout"
      contains: "PTR_ENV_START"
  key_links:
    - from: "crates/cintx-oracle/src/fixtures.rs sample()"
      to: "vendored libcint via compare.rs verify_legacy_wrapper_parity"
      via: "atm/bas pointers into env >= PTR_ENV_START"
      pattern: "PTR_ENV_START"
---

<objective>
Fix `OracleRawInputs::sample()` (crates/cintx-oracle/src/fixtures.rs ~line 483) to use a libcint-conformant `env` layout that reserves `env[0..PTR_ENV_START(=20)]` for libcint global params. The current layout packs shell coeffs into reserved slots — notably shell-2's coeff 0.6 lands at `env[8]=PTR_RANGE_OMEGA`, causing vendored libcint to compute RANGE-SEPARATED (erf, ω=0.6) 2e integrals while cintx computes full Coulomb. That divergence is the last known blocker in the vendor oracle gate.

Purpose: Make the shared `sample()` fixture conformant so 2e legacy parity passes, while preserving the exact physical basis (so 1e parity stays identical and all shell indexing in compare.rs stays valid). Root cause is CONFIRMED and prototype-verified — implement the verified recipe, do not re-derive.

Output: A rewritten `sample()` using named `PTR_*` constants and a PTR_ENV_START-aligned layout, plus a focused unit test guarding the reserved region; then a clean full vendor oracle gate run.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/user/Documents/workspace/cintx/CLAUDE.md
@.planning/STATE.md

<interfaces>
<!-- From crates/cintx-compat/src/raw.rs (already imported in fixtures.rs line 3-6) -->
- `PTR_ENV_START: usize = 20`  — first user-data slot; env[0..20] is reserved.
- `NGRIDS: usize = 11`         — env[11] holds the grid-point count.
- `PTR_GRIDS: usize = 12`      — env[12] holds the index into env where grid coords begin (must be >= 20).
- Reserved landmines inside env[0..20]: PTR_COMMON_ORIG=1..3, PTR_RINV_ORIG=4..6, PTR_RINV_ZETA=7, PTR_RANGE_OMEGA=8, PTR_F12_ZETA=9, PTR_GTG_ZETA=10.

<!-- ATM slot constants imported in fixtures.rs (cintx_compat::raw): -->
- `CHARGE_OF` (slot 0), `PTR_COORD` (slot 1), `NUC_MOD_OF` (slot 2), `PTR_ZETA` (slot 3), `ATM_SLOTS`.
- `POINT_NUC` — point-charge nuclear model constant.
- `BAS_SLOTS`, `ATOM_OF`, `ANG_OF`, `NPRIM_OF`, `NCTR_OF`, `PTR_EXP`, `PTR_COEFF`.

<!-- Sibling conformant builders to mirror, same file ~26-216 -->
- `build_h2o_sto3g()` uses `let mut env = vec![0.0_f64; PTR_ENV_START];` then `env.len()` as growing pointers — mirror this exact pattern.

<!-- Grids family (crates/cintx-compat/src/raw.rs extract_grids_env_params) reads NGRIDS and PTR_GRIDS, then reads 3*ngrids coords starting at env[PTR_GRIDS-value]. The conformant layout MUST keep env[11]=1.0 and env[12]=<grid-coord-index>=20> with valid coords there, or int1e_grids_* fixtures break. -->
</interfaces>

<!-- NOTE: No existing test asserts the OLD env contents/pointers (verified via grep). The in-file tests at ~1024-1129 only consume sample() structurally (symbols/profiles/component counts), and compare.rs tests at ~1609-1667 call parity helpers — none read literal env indices. So no test rewrites for old-layout assertions are expected; if any surfaces during the test run, treat per the <action> rule below. -->
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite sample() with conformant PTR_ENV_START layout + guard test</name>
  <files>crates/cintx-oracle/src/fixtures.rs</files>
  <behavior>
    New unit test `sample_env_reserves_libcint_global_slots` asserts:
    - `inputs.env[PTR_RANGE_OMEGA] == 0.0` (i.e. env[8] == 0.0) — the specific collision being fixed.
    - All of `env[0..PTR_ENV_START]` are 0.0 EXCEPT `env[NGRIDS]` (==1.0, one grid point) and `env[PTR_GRIDS]` (== the grid-coord index, which is >= PTR_ENV_START).
    - `inputs.shells_for_arity(2) == [0,1]`, `(3) == [0,1,2]`, `(4) == [0,1,2,3]` (unchanged).
    - `inputs.bas.len() == 4 * BAS_SLOTS` (4 shells) and the angular momenta per shell are [0,1,0,1] (s,p,s,p) — confirms physical basis shape preserved.
  </behavior>
  <action>
    Rewrite `OracleRawInputs::sample()` (~line 483-520) to build the SAME physical basis with a conformant env layout, mirroring the `build_h2o_sto3g()` pattern (~line 26-58) in the same file. Use named PTR_* / ATM-slot constants already imported at lines 3-6, NOT magic numbers.

    Physical basis to preserve EXACTLY (single atom at origin, 4 single-primitive shells, one grid point):
      shell 0: l=0, exp=1.0, coeff=1.0
      shell 1: l=1, exp=0.9, coeff=0.8
      shell 2: l=0, exp=0.7, coeff=0.6
      shell 3: l=1, exp=0.5, coeff=0.4
      grid point at (0,0,0); atom = point charge Z=1 at origin.

    env construction (VERIFIED recipe):
      1. `let mut env = vec![0.0_f64; PTR_ENV_START];` (20 reserved zero slots).
      2. Set the two legitimate grid slots that live in the reserved region:
         `env[NGRIDS] = 1.0;` (one grid point). PTR_GRIDS (env[12]) is set in step 5 once the grid index is known.
      3. Append atom coord (0,0,0): `let coord_ptr = env.len() as i32;` then `env.extend_from_slice(&[0.0,0.0,0.0]);`.
      4. Append each shell's exp then coeff in order, capturing the pointer (env.len() as i32) before each push:
         shell0 exp_ptr/coeff_ptr (1.0 / 1.0), shell1 (0.9 / 0.8), shell2 (0.7 / 0.6), shell3 (0.5 / 0.4).
      5. Append the grid coords: `let grid_ptr = env.len() as i32;` then `env.extend_from_slice(&[0.0,0.0,0.0]);` then `env[PTR_GRIDS] = grid_ptr as f64;`.

    atm (use ATM slot constants): single atom — CHARGE_OF=1 (Z=1), PTR_COORD=coord_ptr, NUC_MOD_OF=POINT_NUC, PTR_ZETA=0 (reserved zero slot, reads 0.0, fine for point nucleus), remaining slots up to ATM_SLOTS = 0. Build a Vec of length ATM_SLOTS.

    bas (use BAS slot constants / mirror sibling shape): per shell `[atom=0, l, nprim=1, nctr=1, kappa=0, ptr_exp, ptr_coeff, 0]` with ptr_exp/ptr_coeff being the per-shell pointers captured in step 4. Build a Vec of length 4 * BAS_SLOTS.

    Keep `shls2: vec![0,1]`, `shls3: vec![0,1,2]`, `shls4: vec![0,1,2,3]` exactly as before.

    Update the doc comment above sample() to describe the conformant layout (replace the now-wrong "0..3 coordinates ... 11=NGRIDS ... 13=grid" comment).

    Add the `sample_env_reserves_libcint_global_slots` test to the existing `#[cfg(test)] mod tests` block (~line 1020). Import PTR_RANGE_OMEGA / NGRIDS / PTR_GRIDS / BAS_SLOTS / ANG_OF as needed (they are in cintx_compat::raw; `use super::*` already re-exports the file's imports — add PTR_RANGE_OMEGA and NGRIDS to the fixtures.rs top-level `use` if not already imported).

    Do NOT modify compare.rs, the integral kernels, or any other fixture/builder. If `cargo test` surfaces a test that asserted OLD polluted sample() values, update it ONLY with a clear justification that the old assertion encoded the buggy layout — never weaken a test to hide a real regression.
  </action>
  <verify>
    <automated>cargo test -p cintx-oracle --features cpu sample_env_reserves_libcint_global_slots</automated>
  </verify>
  <done>
    sample() reserves env[0..20] (env[8]==0.0) with grid slots at 11/12; physical basis + shls2/3/4 unchanged; new guard test passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full cintx-oracle regression run, then verbatim vendor gate</name>
  <files>crates/cintx-oracle/src/fixtures.rs</files>
  <action>
    Step 1 — Fast regression (no vendor): run `cargo test -p cintx-oracle --features cpu` (lib + integration). The shared sample() fixture feeds many tests; confirm none regressed. If any test fails because it encoded the OLD polluted env layout, fix it with explicit justification only (per Task 1 rule). Do NOT touch compare.rs logic or kernels.

    Step 2 — FINAL MANDATORY vendor gate, run verbatim (vendor build is slow — allow generous time):
      CINTX_BACKEND=cpu CINTX_ORACLE_BUILD_VENDOR=1 cargo run --locked --manifest-path xtask/Cargo.toml -- oracle-compare --profiles "base,with-f12,with-4c1e,with-f12+with-4c1e" --include-unstable-source false

    Report honestly:
      - Confirm the cint2e_sph AND cint2e_cart legacy-parity mismatch (the constant per-quartet ratio, e.g. 5.308297) is GONE.
      - State whether ALL 4 PROFILES (base, with-f12, with-4c1e, with-f12+with-4c1e) now PASS CLEAN.
      - If a new downstream blocker surfaces (e.g. a missing /tmp/cintx_artifacts matrix json, or another integral family), report it VERBATIM and STOP — do NOT fix newly-surfaced downstream issues, just note them.
      - Never fabricate a pass.

    Step 3 — Commit CODE atomically (not docs): commit only crates/cintx-oracle/src/fixtures.rs.
  </action>
  <verify>
    <automated>cargo test -p cintx-oracle --features cpu</automated>
  </verify>
  <done>
    Full cintx-oracle suite green; vendor gate run verbatim with an honest report on whether all 4 profiles pass clean or the next verbatim blocker; fixtures.rs committed atomically.
  </done>
</task>

</tasks>

<verification>
- `cargo test -p cintx-oracle --features cpu` passes (lib + integration).
- New `sample_env_reserves_libcint_global_slots` test passes (env[8]==0.0, reserved region zero except grid slots, shls/basis unchanged).
- Vendor gate run verbatim; cint2e_sph/cint2e_cart divergence gone; honest pass/blocker report.
</verification>

<success_criteria>
- env[0..PTR_ENV_START] reserved (only env[11]/env[12] grid slots populated); env[8]=PTR_RANGE_OMEGA == 0.0.
- Physical basis, shls2/3/4, and per-shell sizes byte-for-byte equivalent to before (1e parity unchanged).
- 2e legacy parity passes at atol=1e-12 (full Coulomb both sides).
- Code committed atomically; vendor-gate outcome reported honestly (all-4-profiles-clean OR next verbatim blocker noted, not fixed).
</success_criteria>

<output>
After completion, create `.planning/quick/260529-ne7-fix-oraclerawinputs-sample-env-layout-to/260529-ne7-SUMMARY.md`
</output>
