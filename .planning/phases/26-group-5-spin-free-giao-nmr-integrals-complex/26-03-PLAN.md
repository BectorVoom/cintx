---
phase: 26-group-5-spin-free-giao-nmr-integrals-complex
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - crates/cintx-ops/generated/compiled_manifest.lock.json
  - crates/cintx-compat/src/raw.rs
  - crates/cintx-cubecl/src/kernels/two_electron.rs
  - crates/cintx-oracle/build.rs
  - crates/cintx-oracle/src/vendor_ffi.rs
  - crates/cintx-oracle/tests/giao_2e_parity.rs
autonomous: true
requirements: [GIAO-02]
user_setup: []
must_haves:
  truths:
    - "4 spin-free 2e GIAO families (int2e_g1, int2e_ig1, int2e_gg1, int2e_g1g2) are registered with their true component_rank and complex_output=true"
    - "int2e_g1g2 is IN scope (D-16) — registered, kernel-ported, vendor-parity-tested alongside the other three"
    - "Each 2e GIAO family is byte-identical to vendored libcint 6.1.3 at atol=1e-12 in cart AND sph"
    - "Every family parity test runs on a NON-ZERO gauge-origin, NON-SQUARE bra×ket quartet"
    - "Spin-carrying int2e_giao_sa10* / int2e_g1spsp2 are NOT registered as parity targets here (deferred to Phase 30)"
  artifacts:
    - path: "crates/cintx-cubecl/src/kernels/two_electron.rs"
      provides: "Giao2eKind dispatch + launch_two_electron_giao2e kernels (rank-3 + rank-9), generic over F"
      contains: "Giao2eKind"
    - path: "crates/cintx-compat/src/raw.rs"
      provides: "RawApiId consts for 4 families × {cart,sph,spinor}"
      contains: "INT2E_G1"
    - path: "crates/cintx-oracle/src/vendor_ffi.rs"
      provides: "real double* 4-shell vendor wrappers for 8 cart/sph GIAO-2e symbols"
      contains: "vendor_int2e_g1_sph"
    - path: "crates/cintx-oracle/tests/giao_2e_parity.rs"
      provides: "vendor-gated byte-identity parity for all 4 2e GIAO families"
      contains: "build_h2o_sto3g_common_orig"
  key_links:
    - from: "crates/cintx-cubecl/src/kernels/two_electron.rs operator_name() dispatch"
      to: "Giao2eKind { G1, Ig1, Gg1, G1g2 }"
      via: "operator-name match (not string-suffix)"
      pattern: "Giao2eKind"
    - from: "crates/cintx-oracle/tests/giao_2e_parity.rs"
      to: "vendor_ffi::vendor_int2e_*"
      via: "real-vs-real elementwise compare at atol=1e-12"
      pattern: "vendor_int2e_g"
---

<objective>
Register and implement the 4 spin-free 2e GIAO families (GIAO-02, per D-16) at byte-identity (cart+sph) vs vendored libcint 6.1.3 at atol=1e-12, on the FND-03 foundation from Plan 26-01. Families: `int2e_g1` (rank 3), `int2e_ig1` (rank 3), `int2e_gg1` (rank 9), `int2e_g1g2` (rank derived from intor2.c — D-16).

Purpose: GIAO-02 delivers the spin-free 2e magnetic-property integrals. `int2e_g1g2` (2nd-gauge-on-both-electrons, no spin block) is IN scope per D-16, which supersedes the RESEARCH-text assumption A2 that it was out of scope. Spin-carrying `int2e_giao_sa10*` / `int2e_g1spsp2` remain DEFERRED to Phase 30.

Output: 4 families registered via the D-10 5-step recipe, 2e `#[cube]` kernels extending the ERI engine generic over F, 8 real `double*` 4-shell vendor wrappers, and `giao_2e_parity.rs` vendor-gated on the non-zero gauge fixture with a non-square quartet.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-group-5-spin-free-giao-nmr-integrals-complex/26-CONTEXT.md
@.planning/phases/26-group-5-spin-free-giao-nmr-integrals-complex/26-RESEARCH.md
@.planning/phases/26-group-5-spin-free-giao-nmr-integrals-complex/26-PATTERNS.md
@.planning/phases/26-group-5-spin-free-giao-nmr-integrals-complex/26-01-SUMMARY.md

<interfaces>
<!-- GIAO-02 roster — derived from libcint 6.1.3 source (RESEARCH §Exact Roster GIAO-02 + §gout order). D-16 ADDS int2e_g1g2. Transcribe gout VERBATIM (D-12/D-13). -->

ROSTER (4 families × {cart, sph} = 8 symbols; spinor reps registered → UnsupportedApi):
| Family       | component_rank | gout def (libcint src)          | closest 2e clone        |
|--------------|----------------|---------------------------------|-------------------------|
| int2e_g1     | 3              | intor4.c:1255 CINTgout2e_int2e_g1   | int2e_ip1 (rank3,arity4) |
| int2e_ig1    | 3              | intor2.c:19 CINTgout2e_int2e_ig1    | int2e_ip1                |
| int2e_gg1    | 9              | intor2.c:148 CINTgout2e_int2e_gg1   | int2e_ipip1 (rank9)      |
| int2e_g1g2   | DERIVE         | src/autocode/intor2.c ng[]/gout for int2e_g1g2 — D-16: derive component_rank from ng[TENSOR], transcribe gout; do NOT guess | int2e_ipip1 (rank-9 candidate; CONFIRM rank from source) |

gout COMPONENT ORDER (copy VERBATIM — c, s as defined in each cited gout fn):
- int2e_g1 (intor4.c:1255): [0]=c[1]s[2]−c[2]s[1], [1]=c[2]s[0]−c[0]s[2], [2]=c[0]s[1]−c[1]s[0]
- int2e_ig1 (intor2.c:19): [0]=−c[1]s[2]+c[2]s[1], [1]=−c[2]s[0]+c[0]s[2], [2]=−c[0]s[1]+c[1]s[0]  (sign-flipped vs g1)
- int2e_gg1 (intor2.c:148): 9 components, 2nd-order gauge tensor, e.g. [0]=−c[4]s[8]+2c[5]s[7]−c[8]s[4] — TRANSCRIBE the full 9-component combos directly from intor2.c:148; do NOT guess
- int2e_g1g2: read CINTgout2e_int2e_g1g2 from intor2.c — transcribe the full combo set verbatim (D-16/D-13)

Phase-25 Hess2e dispatch (two_electron.rs:2347-2371) — the exact pattern to clone:
```rust
if let Some(kind) = match plan.descriptor.operator_name() {
    "ipip1"      => Some(Hess2eKind::Ipip1),
    "ipvip1"     => Some(Hess2eKind::Ipvip1),
    "ip1ip2"     => Some(Hess2eKind::Ip1ip2),
    "ipip1ipip2" => Some(Hess2eKind::Ipip1ipip2),
    _ => None,
} {
    return launch_two_electron_hess2e::<F>(plan, kind, li, lj, lk, ll,
        ri, rj, rk, rl, common_factor, staging);
}
```

RawApiId const pattern (raw.rs) — string MUST exactly match the manifest symbol_name:
```rust
pub const INT2E_IP1_SPH: Self = Self::Symbol("int2e_ip1_sph");
pub const INT2E_IP1_CART: Self = Self::Symbol("int2e_ip1_cart");
```

Vendor wrapper pattern (4-shell `shls: &[i32; 4]`) — REAL double* (D-15), out sized component_rank*ni*nj*nk*nl, clone an existing `vendor_int2e_*` wrapper in vendor_ffi.rs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register 4 2e GIAO families (manifest + RawApiId + vendor FFI) — derive int2e_g1g2 rank from source</name>
  <files>crates/cintx-ops/generated/compiled_manifest.lock.json, crates/cintx-compat/src/raw.rs, crates/cintx-oracle/build.rs, crates/cintx-oracle/src/vendor_ffi.rs</files>
  <read_first>
    - .planning/phases/26-group-5-spin-free-giao-nmr-integrals-complex/26-RESEARCH.md §"Exact Roster" GIAO-02 (g1/ig1/gg1 ranks) AND §"Assumptions Log" A2 — NOTE: A2 assumed g1g2 OUT of scope, but CONTEXT D-16 SUPERSEDES it: g1g2 IS in scope. Derive its rank from intor2.c.
    - .planning/phases/26-group-5-spin-free-giao-nmr-integrals-complex/26-CONTEXT.md D-16 (int2e_g1g2 IN scope; derive component_rank + gout from src/autocode/intor2.c ng[]/gout — do not guess)
    - .planning/phases/26-group-5-spin-free-giao-nmr-integrals-complex/26-PATTERNS.md §"RawApiId consts" + §"Vendor FFI wrapper" (4-shell vendor_int2e_* analog) + §"bindgen allowlist"
    - libcint-master/src/autocode/intor2.c (find `CINTgout2e_int2e_g1g2` and its `ng[]` tuple; component_rank = ng[TENSOR]=ng[7])
    - crates/cintx-ops/generated/compiled_manifest.lock.json (an `int2e_ip1` block to clone for rank-3, `int2e_ipip1` for rank-9 — arity=4)
    - crates/cintx-compat/src/raw.rs (INT2E_IP1_* / INT2E_IPIP1_* const block to clone)
    - crates/cintx-oracle/build.rs:374 (allowlist regex — intor4.c/intor2.c ALREADY in cc::Build per build.rs:62,229; only allowlist + wrappers needed)
    - crates/cintx-oracle/src/vendor_ffi.rs (an existing 4-shell `vendor_int2e_*` real-double wrapper to clone)
  </read_first>
  <action>
    Apply the D-10 5-step recipe for the 4 families × {cart, sph, spinor}.

    FIRST: derive `int2e_g1g2`'s `component_rank` from `src/autocode/intor2.c` — find `CINTgout2e_int2e_g1g2`, read its `ng[]` tuple, take `ng[TENSOR]` (= `ng[7]`). D-16 + D-13: do NOT guess (a too-low rank silently truncates). Record the derived value in the SUMMARY.

    (1) MANIFEST: clone `int2e_ip1` (rank 3) for `g1`/`ig1`; clone `int2e_ipip1` (rank 9) for `gg1`; for `g1g2` clone `int2e_ipip1` and set the DERIVED `component_rank`. Each new entry: arity=4, the exact component_rank ("3" for g1/ig1, "9" for gg1, derived for g1g2), `complex_output: true`. Register cart/sph/spinor. Then `cargo build -p cintx-ops` regenerates api_manifest.{rs,csv}; manifest-audit auto-syncs.

    (2) RawApiId: add `INT2E_G1_{CART,SPH,SPINOR}`, `INT2E_IG1_*`, `INT2E_GG1_*`, `INT2E_G1G2_*` consts in raw.rs cloning the INT2E_IP1_* block. Strings exactly match manifest symbol_name.

    (4) VENDOR FFI: append the 8 cart/sph symbols (`int2e_g1_cart|int2e_g1_sph|int2e_ig1_cart|int2e_ig1_sph|int2e_gg1_cart|int2e_gg1_sph|int2e_g1g2_cart|int2e_g1g2_sph`) to the build.rs:374 allowlist regex (no cc::Build source change — intor4.c/intor2.c already present). Add 8 real `double *out` 4-shell wrappers (`shls: &[i32; 4]`) in vendor_ffi.rs cloning an existing `vendor_int2e_*` wrapper (D-15 — real double*, NOT len-2N; `out` sized `component_rank * ni * nj * nk * nl`).

    OperatorId-shift fix (Pitfall 4): after regeneration, re-grep `OperatorId::new(` and `_OPERATOR_ID: u32 =` and re-anchor any positional hardcoded const by symbol name. NOTE: if Plan 26-02 (Cluster A) runs in a parallel worktree, both clusters add manifest rows — the OperatorId-shift re-anchoring in BOTH plans must be by symbol name (never integer) so the merge is order-independent. Confirm `cargo build --workspace` clean.
  </action>
  <verify>
    <automated>cargo build -p cintx-ops 2>&1 | tail -3 && cargo build -p cintx-oracle --features cpu 2>&1 | tail -5 && cargo build --workspace 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `compiled_manifest.lock.json` contains `int2e_g1`, `int2e_ig1`, `int2e_gg1`, `int2e_g1g2` symbols, arity 4, with `complex_output: true` and `int2e_gg1` `component_rank` "9"
    - `int2e_g1g2`'s `component_rank` in the lock equals the value derived from intor2.c ng[TENSOR] (documented in SUMMARY)
    - `crates/cintx-compat/src/raw.rs` contains `INT2E_G1_SPH` and `INT2E_G1G2_SPH` consts
    - `crates/cintx-oracle/build.rs` allowlist regex contains `int2e_g1_sph` and `int2e_g1g2_cart`
    - `crates/cintx-oracle/src/vendor_ffi.rs` contains `pub fn vendor_int2e_g1_sph` and `pub fn vendor_int2e_g1g2_cart`
    - NO spin-carrying `int2e_giao_sa10*` / `int2e_g1spsp2` symbol is registered as a parity target (Phase-30 leak guard — Pitfall 3)
    - `cargo build --workspace` exits 0 (OperatorId-shift consts re-anchored by symbol name)
  </acceptance_criteria>
  <done>All 4 2e GIAO families registered across manifest/RawApiId/vendor-FFI; int2e_g1g2 rank derived from intor2.c source; no Phase-30 spin family leaked; OperatorId-shift consts re-anchored by symbol name; workspace builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: Implement 2e GIAO #[cube] kernels (Giao2eKind dispatch, rank-3 + rank-9) generic over F</name>
  <files>crates/cintx-cubecl/src/kernels/two_electron.rs</files>
  <read_first>
    - docs/manual/Cubecl/Cubecl_generics.md (Float/Numeric bounds — D-08 MANDATORY before writing kernel code)
    - docs/manual/Cubecl/Cubecl_algebra.md (F::exp/F::sqrt — D-08)
    - docs/manual/Cubecl/Cubecl_basic_operations.md (supported arithmetic; D-08)
    - docs/manual/Cubecl/Cubecl_conditionals.md (statement-form if; D-08)
    - /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/INDEX.md (canonical manual — D-08)
    - crates/cintx-cubecl/src/kernels/two_electron.rs:2347-2371 (Hess2e operator_name() → kind dispatch — the exact clone target; PATTERNS.md §2e GIAO kernel) and :2373+ (the scalar ERI engine `fill_g_tensor_2e` / `run_2e_scalar_device` to extend)
    - .planning/phases/26-group-5-spin-free-giao-nmr-integrals-complex/26-RESEARCH.md §"gout Component-Index Order" 2e families (also mirrored in <interfaces>)
    - libcint-master/src/autocode/intor4.c:1255 (CINTgout2e_int2e_g1), src/autocode/intor2.c:19 (int2e_ig1), :148 (int2e_gg1), and CINTgout2e_int2e_g1g2 — the SOURCE OF TRUTH; transcribe gout exactly
  </read_first>
  <action>
    Implement the 2e GIAO `#[cube]` kernels following D-08 (generic over `F`; no plain-Rust-fn calls inside `#[cube]`; no `if`-expressions; `u32`/`i32` only; no `continue`/`break`). Extend the ERI engine in two_electron.rs.

    Add a `Giao2eKind { G1, Ig1, Gg1, G1g2 }` enum and a parallel match on `plan.descriptor.operator_name()` cloning the Hess2e dispatch at :2347-2371:
    ```
    "g1"  => Some(Giao2eKind::G1),
    "ig1" => Some(Giao2eKind::Ig1),
    "gg1" => Some(Giao2eKind::Gg1),
    "g1g2"=> Some(Giao2eKind::G1g2),
    ```
    dispatching to `launch_two_electron_giao2e::<F>(plan, kind, li, lj, lk, ll, ri, rj, rk, rl, common_factor, staging)` that extends the scalar ERI engine. rank-3 (`g1`,`ig1`) and rank-9 (`gg1`, and `g1g2` at its derived rank) gout combos TRANSCRIBED VERBATIM from intor4.c:1255 / intor2.c:19,148 and `CINTgout2e_int2e_g1g2` (D-12/D-13/D-16; a sign or index error fails byte-identity silently). Consume the Phase-22 gauge origin `plan.operator_env_params.common_orig` for the `g`-factor (already wired, no new env slot). Fail closed on `Representation::Spinor` → UnsupportedApi (D-11). Raise ket-side headroom via `ng[]`, not bra (D-12). Device emits REAL components; FND-03 host materialization (Plan 01) wraps them for the complex view.
  </action>
  <verify>
    <automated>cargo build -p cintx-cubecl --features cpu 2>&1 | tail -8 && cargo test -p cintx-cubecl --features cpu 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `crates/cintx-cubecl/src/kernels/two_electron.rs` contains `Giao2eKind` with `G1`, `Ig1`, `Gg1`, `G1g2` variants and a dispatch arm matching `"g1"` and `"g1g2"`
    - the kernel reads `plan.operator_env_params.common_orig` (no new env slot)
    - the kernel returns `UnsupportedApi` for `Representation::Spinor` (D-11)
    - kernel code is generic over `F`, no plain-Rust-fn calls inside `#[cube]`, no if-expressions, no continue/break (D-08)
    - `cargo build -p cintx-cubecl --features cpu` exits 0
    - `cargo test -p cintx-cubecl --features cpu` exits 0
  </acceptance_criteria>
  <done>2e GIAO kernels implemented generic over F via Giao2eKind dispatch with gout combos transcribed verbatim (incl int2e_g1g2 per D-16), consuming the Phase-22 gauge origin, failing closed on spinor; cintx-cubecl builds and unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Vendor-gated parity tests for all 4 2e GIAO families</name>
  <files>crates/cintx-oracle/tests/giao_2e_parity.rs</files>
  <read_first>
    - crates/cintx-oracle/tests/two_electron_ip1_parity.rs (the 4-shell vendor-parity analog to clone) and crates/cintx-oracle/tests/moment_common.rs (the rank-parameterized helper + non-square shell selection)
    - .planning/phases/26-group-5-spin-free-giao-nmr-integrals-complex/26-PATTERNS.md §"New test file: tests/giao_2e_parity.rs" (double-gating, non-zero gauge, non-square bra×ket quartet, ATOL=1e-12, the 4 families incl g1g2)
    - .planning/phases/26-group-5-spin-free-giao-nmr-integrals-complex/26-RESEARCH.md §"Non-zero Gauge-Origin Fixture (D-06/D-07)" (build_h2o_sto3g_common_orig, non-square requirement, real-vs-real per D-15)
  </read_first>
  <action>
    Create `crates/cintx-oracle/tests/giao_2e_parity.rs` cloning `two_electron_ip1_parity.rs` + the `moment_common.rs` rank-parameterized helper shape. Crate gate `#![cfg(any(feature = "cpu", feature = "rocm"))]`; per-test `#[cfg(has_vendor_libcint)] #[cfg(feature = "cpu")]`. One test fn per family (4 fns: g1, ig1, gg1, g1g2), each:
    - build the non-zero gauge fixture `build_h2o_sto3g_common_orig()` (D-06)
    - drive cintx `eval_raw` for sph + cart with `RawApiId::INT2E_<FAM>_{SPH,CART}` and call `vendor_ffi::vendor_int2e_<fam>_{sph,cart}` with a 4-shell `shls: &[i32; 4]`
    - use a NON-SQUARE bra×ket quartet (mismatched bra/ket angular momenta) so a transposed layout cannot pass (D-12)
    - compare elementwise with the correct rank (3 for g1/ig1, 9 for gg1, derived for g1g2); assert mismatch count 0 at ATOL=1e-12 / RTOL=0 (D-14). REAL-vs-REAL (D-15).

    Flip `oracle_covered: true` on the 4 families' manifest rows after parity is green (manifest-audit auto-syncs). NOTE: the folded todo `oracle-cart-offset-vendor-zero` (`compare::tests` `--lib` failures) is a CONFIRMED pre-existing harness bug — family parity is `--test` integration; do NOT let the `--lib` failures block this gate.
  </action>
  <verify>
    <automated>CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu giao_2e_parity 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `crates/cintx-oracle/tests/giao_2e_parity.rs` exists with 4 test fns (g1, ig1, gg1, g1g2), double-gated `#[cfg(has_vendor_libcint)] #[cfg(feature = "cpu")]`, calling `build_h2o_sto3g_common_orig` and a non-square 4-shell quartet
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu giao_2e_parity` exits 0 with all 4 families byte-identical at atol=1e-12 (cart + sph)
    - all 4 families' manifest rows have `oracle_covered: true`
    - `cargo xtask manifest-audit` (or the existing audit test) green
  </acceptance_criteria>
  <done>All 4 2e GIAO families (incl int2e_g1g2 per D-16) pass vendor byte-identity at atol=1e-12 (cart+sph) on the non-zero-gauge non-square quartet; oracle_covered flipped true; manifest-audit green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → eval_raw (atm/bas/env, 4 shells) | Numeric input arrays cross into the planner/2e kernel; validated by the shell-tuple validator and `assert_flat_buffer_contract` |

## STRIDE Threat Register

Per 26-RESEARCH.md §Security Domain: numerical library, no auth/session/network/crypto surface; no ASVS categories apply. Integrity controls: typed errors (thiserror v2, no silent partial writes), `cargo --locked`, byte-identity oracle gates.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05 | Tampering (data integrity) | int2e_g1g2 component_rank | mitigate | int2e_g1g2's rank is DERIVED from intor2.c ng[TENSOR] (D-16/D-13), not guessed; a too-low rank silently truncates. The vendor byte-identity gate at atol=1e-12 on a non-square quartet detects truncation/transpose. |
| T-26-06 | Spoofing (scope leak) | int2e_giao_* wildcard | mitigate | The `int2e_giao_*` wildcard resolves to spin-carrying `sa10*` families (ng[POS_E1]=4); these are NOT registered here (Pitfall 3, deferred to Phase 30). Only the 4 spin-free families are parity targets. |
</threat_model>

<verification>
- `cargo build --workspace` clean after registration + OperatorId-shift re-anchoring (Task 1)
- `cargo build -p cintx-cubecl --features cpu` + `cargo test -p cintx-cubecl --features cpu` (Task 2)
- `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu giao_2e_parity` — 4 families byte-identical (Task 3)
- `cargo xtask manifest-audit` green after oracle_covered flip
- Pre-existing `oracle-cart-offset-vendor-zero` `--lib` harness bug is NOT in scope and must NOT block
- WORKTREE INTEGRATION (D-09, project memory): this is a Wave-2 cluster running in a parallel worktree against Cluster A (Plan 26-02); both depend_on Plan 26-01 merged to main FIRST. Both clusters touch the same shared files (`compiled_manifest.lock.json`, `crates/cintx-compat/src/raw.rs`, `crates/cintx-oracle/build.rs`, `crates/cintx-oracle/src/vendor_ffi.rs`) — the OperatorId-shift re-anchoring by SYMBOL NAME makes the merge order-independent, but a manifest-lock merge conflict is expected. MANDATORY post-merge regeneration step: after resolving the `compiled_manifest.lock.json` merge conflict (take BOTH clusters' new rows), run `cargo build -p cintx-ops` to regenerate `api_manifest.{rs,csv}` from the unified lock (each worktree regenerated it against only its own row set, so the merged manifest is otherwise stale and inconsistent), then run `cargo build --workspace` to confirm the full workspace compiles against the merged manifest, then re-run the vendor-gated parity suite (`CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu giao_2e_parity`) to confirm this cluster still passes post-merge. Finally verify `git merge-base --is-ancestor <this-wave-branch> main`; merge manually if not an ancestor.
</verification>

<success_criteria>
- 4 spin-free 2e GIAO families registered (manifest + RawApiId + launcher + vendor-FFI + oracle) with correct component_rank and complex_output=true
- int2e_g1g2 IN scope (D-16) — rank derived from intor2.c, kernel-ported, vendor-parity-tested
- Each byte-identical to libcint 6.1.3 at atol=1e-12 in cart AND sph on the non-zero-gauge non-square quartet
- Spinor reps registered → UnsupportedApi; spin-carrying sa10*/g1spsp2 NOT registered (Phase-30 deferral preserved)
- All families oracle_covered=true; manifest-audit green; workspace builds clean
</success_criteria>

<output>
After completion, create `.planning/phases/26-group-5-spin-free-giao-nmr-integrals-complex/26-03-SUMMARY.md`
</output>
</output>
