---
phase: 26-group-5-spin-free-giao-nmr-integrals-complex
plan: 05
type: execute
wave: 2
depends_on: [04]
files_modified:
  - crates/cintx-cubecl/src/kernels/one_electron.rs
  - crates/cintx-oracle/tests/giao_1e_parity.rs
  - crates/cintx-ops/generated/compiled_manifest.lock.json
autonomous: true
gap_closure: true
requirements: [GIAO-01]

must_haves:
  truths:
    - "EITHER int1e_a01gp matches libcint 6.1.3 byte-identity at atol=1e-12 (cart+sph) on the non-zero-gauge non-square block AND the fail-closed guard is removed AND oracle_covered=true AND #[ignore] removed — OR the math fix is infeasible and the 26-04 guard REMAINS with a tracked follow-up"
    - "The public API is NEVER left dispatchable-with-wrong-output: if parity does not pass, the guard stays"
  artifacts:
    - path: "crates/cintx-cubecl/src/kernels/one_electron.rs"
      provides: "Corrected a01gp rank-9 ket-derivative s-table (math-fix path only) OR unchanged guard preserved (fallback)"
    - path: "crates/cintx-oracle/tests/giao_1e_parity.rs"
      provides: "test_int1e_a01gp_parity un-ignored (math-fix path only)"
    - path: "crates/cintx-ops/generated/compiled_manifest.lock.json"
      provides: "int1e_a01gp_{cart,sph,spinor} oracle_covered=true (math-fix path only)"
  key_links:
    - from: "a01gp 27-s table component 1..8"
      to: "libcint int1e_a01gp gout"
      via: "corrected ket-derivative (g2 = D_J + D_I) s-slot assignment"
      pattern: "a01gp"
---

<objective>
Attempt the `int1e_a01gp` kernel correctness fix: root-cause the ~2× ket-derivative
double-count on rank-9 components 1..8 of the 27-s table (component 0 is already
byte-identical). On success: remove the 26-04 fail-closed guard, un-ignore
`test_int1e_a01gp_parity`, and flip `oracle_covered=true`. On infeasibility: the
26-04 guard REMAINS and a follow-up is documented — the public API stays fail-closed
either way.

Purpose: Fully close GIAO-01 (all 11 1e GIAO/CG families byte-identical). The
structurally-identical rank-9 `cg_a11part`/`giao_a11part` pair and the 27-s `igkin`
all pass, isolating the defect to a01gp's specific `c × (∇⊗∇)` ket combination.

Output: Either a corrected kernel with parity + oracle_covered=true + guard removed,
OR an explicit, documented fallback that preserves the 26-04 guard.

CRITICAL: This plan is structured so the BLOCKING gap closure (26-04 guard) is never
undone unless vendor parity actually passes. The guard removal is gated on the parity
test passing — not on the executor's belief that the math is fixed.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-group-5-spin-free-giao-nmr-integrals-complex/26-02-SUMMARY.md
@.planning/phases/26-group-5-spin-free-giao-nmr-integrals-complex/26-VERIFICATION.md
@.planning/phases/26-group-5-spin-free-giao-nmr-integrals-complex/26-CONTEXT.md

<interfaces>
<!-- The nuclear-engine arm and the (now-guarded) a01gp dispatch. -->

From one_electron.rs:8604-8613 — a01gp is `Some((3, 9))` (op_kind=3, rank=9).
From one_electron.rs:8774+ — the nuclear-engine arm. The 26-04 guard (`if op_name == "a01gp"`)
sits at the top of this arm and must be removed ONLY after parity passes.

The a01gp defect (26-02-SUMMARY Known Stubs): "rank-9 27-s table is byte-identical on
component 0 but ~2× on a subset of ket-varying elements of components 1..8 (same sign,
factor 2)" — a g-tensor double-count in the combined `g2 = D_J + D_I` ket path. The
follow-up note says: re-derive the a01gp s[] slot assignment for the ket-derivative
components against the libcint autocode.
</interfaces>

<libcint_reference>
<!-- Math-fix authoritative source — executor MUST read before changing kernel math. -->
The libcint 6.1.3 int1e_a01gp autocode (vendored under the oracle build) is the source
of truth for the rank-9 27-s gout component order and the ket-derivative s-slot
assignment. The 26-02-SUMMARY follow-up cites the autocode region around the a01gp
`s[]` slot assignment (the SUMMARY references `intor1.c:521-540` for the ket-derivative
components). Compare against the WORKING structurally-identical families (cg_a11part /
giao_a11part rank-9, igkin 27-s) whose s-tables pass — the divergence is isolated to
a01gp's `c × (∇⊗∇)` combination.
</libcint_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root-cause and correct the a01gp rank-9 ket-derivative double-count</name>
  <files>crates/cintx-cubecl/src/kernels/one_electron.rs</files>
  <read_first>
    - docs/manual/Cubecl/Cubecl_generics.md, docs/manual/Cubecl/Cubecl_algebra.md,
      docs/manual/Cubecl/Cubecl_basic_operations.md, docs/manual/Cubecl/Cubecl_conditionals.md
      (D-08 USER-MANDATED: read before touching any #[cube] kernel code — no plain-fn
      calls inside #[cube], no if-expr, F::exp/F::sqrt, u32/i32 only, no continue/break)
    - The libcint int1e_a01gp autocode in the vendored oracle sources (autocode/intor1.c
      region cited in 26-02-SUMMARY ~lines 521-540) — the s-slot assignment for the
      ket-derivative components 1..8
    - crates/cintx-cubecl/src/kernels/one_electron.rs (the nuclear-engine arm 8774-8835
      and the flat-tensor s[] gout combination for a01gp; the working a11part/igkin
      s-tables for comparison)
  </read_first>
  <action>
    Root-cause the ~2× factor on a01gp rank-9 components 1..8. The 26-02-SUMMARY isolates
    it to the combined `g2 = D_J + D_I` ket-derivative path double-counting the
    ket-element contribution. Steps:
    1. Diff the a01gp s[] slot assignment / gout `c[]·s[]` combination against the
       WORKING cg_a11part/giao_a11part rank-9 and igkin 27-s tables (all pass), and
       against the libcint autocode gout index map for int1e_a01gp.
    2. Correct the ket-derivative s-slot assignment so the `∇⊗∇` ket components are not
       summed twice (the ~2× same-sign factor strongly suggests a D_J path counted in
       both the D_J and the combined g2 term). Component 0 is already correct — preserve
       it; only components 1..8 change.
    3. Keep all #[cube] authoring rules (D-08). Intermediate math stays f64; final write
       uses F::from_f64_lossy as in the existing path. Do NOT change `Some((3, 9))`.
    DO NOT remove the 26-04 guard in this task — guard removal is Task 2, gated on parity.
    If after genuine root-causing the math cannot be corrected, STOP and proceed to Task 2's
    FALLBACK branch (guard stays, follow-up documented).
  </action>
  <verify>
    <automated>cargo build -p cintx-cubecl --features cpu</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p cintx-cubecl --features cpu` exits 0 (MANDATORY whether or not the
      math is corrected — the kernel must still compile).
    - If the math fix was attempted: a code diff exists on the a01gp components-1..8 s-slot
      assignment in one_electron.rs (verify with git diff). (CONDITIONAL — only if attempted)
    - The `giao_nuc_op` table still maps `"a01gp" => Some((3, 9))` (MANDATORY — rank unchanged).
  </acceptance_criteria>
  <done>
    Either a corrected a01gp s-table that compiles, or a documented determination that the
    fix is infeasible (proceed to Task 2 fallback).
  </done>
</task>

<task type="auto">
  <name>Task 2: Gate guard-removal + oracle_covered on vendor parity (conditional)</name>
  <files>crates/cintx-oracle/tests/giao_1e_parity.rs, crates/cintx-cubecl/src/kernels/one_electron.rs, crates/cintx-ops/generated/compiled_manifest.lock.json</files>
  <read_first>
    - crates/cintx-oracle/tests/giao_1e_parity.rs (lines 209-228: the #[ignore]d
      test_int1e_a01gp_parity)
    - crates/cintx-cubecl/src/kernels/one_electron.rs (the 26-04 a01gp guard at the top
      of the nuclear-engine arm)
    - crates/cintx-ops/generated/compiled_manifest.lock.json (the int1e_a01gp_{cart,sph,spinor}
      rows carrying oracle_covered=false)
  </read_first>
  <action>
    FIRST, run the vendor parity test WITH the #[ignore] temporarily bypassed to learn the
    truth:
    `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu test_int1e_a01gp_parity -- --ignored --nocapture`

    BRANCH A — parity PASSES (exits 0, byte-identical at atol=1e-12 on the cross-center
    non-square block):
    1. Remove the `#[ignore = "..."]` annotation on `test_int1e_a01gp_parity` (keep the
       descriptive comment above it, updated to note 26-05 landed parity).
    2. Remove the entire 26-04 `if op_name == "a01gp" { return Err(UnsupportedApi...) }`
       guard from one_electron.rs.
    3. In compiled_manifest.lock.json, flip `oracle_covered` from false to true on the
       int1e_a01gp_cart, int1e_a01gp_sph, AND int1e_a01gp_spinor rows. (Lock edit only —
       manifest-audit derives both sides from the lock and auto-syncs.)
    4. Remove (or invert) the 26-04 `test_int1e_a01gp_is_fail_closed` test added in 26-04,
       since a01gp is no longer fail-closed — replace it with nothing (the parity test now
       covers correctness). Note this in the SUMMARY.
    5. Run `cargo build -p cintx-ops` to regenerate api_manifest.{rs,csv} from the lock.

    BRANCH B — parity FAILS or root-cause was infeasible (FALLBACK):
    1. Leave `#[ignore]` on test_int1e_a01gp_parity (update its comment to reference 26-05
       attempt + the remaining discrepancy).
    2. Leave the 26-04 guard in place (the public API stays fail-closed — the BLOCKING gap
       remains closed).
    3. Leave oracle_covered=false on all three a01gp rows.
    4. Leave test_int1e_a01gp_is_fail_closed (from 26-04) intact and passing.
    5. Document the remaining defect as a tracked follow-up in the SUMMARY Known Stubs.

    Record explicitly in the SUMMARY which branch was taken.
  </action>
  <verify>
    <automated>cargo build -p cintx-ops && node .claude/get-shit-done/bin/gsd-tools.cjs 2>/dev/null; cargo test -p cintx-cubecl --features cpu</automated>
  </verify>
  <acceptance_criteria>
    MANDATORY (both branches):
    - The public API is fail-closed-or-correct: `grep -c 'op_name == "a01gp"'
      one_electron.rs` is 0 ONLY IF the parity test passed; otherwise it is >= 1.
    - `cargo build -p cintx-ops` exits 0; manifest-audit reports `status: ok`
      (run the manifest-audit xtask / gsd-tools manifest check and confirm green).
    CONDITIONAL — Branch A (math-fix succeeded) ONLY:
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu test_int1e_a01gp_parity`
      exits 0 (NOT run with --ignored — the #[ignore] must be gone).
    - `grep -B1 'fn test_int1e_a01gp_parity' giao_1e_parity.rs` shows NO `#[ignore`.
    - `compiled_manifest.lock.json` int1e_a01gp_cart, int1e_a01gp_sph, int1e_a01gp_spinor
      rows each show `oracle_covered: true` (verify with grep/jq).
    - `grep -c 'op_name == "a01gp"' one_electron.rs` == 0 (guard removed).
    CONDITIONAL — Branch B (fallback) ONLY:
    - `grep 'op_name == "a01gp"' one_electron.rs` still matches (guard preserved).
    - `compiled_manifest.lock.json` a01gp rows remain `oracle_covered: false`.
    - `test_int1e_a01gp_parity` remains `#[ignore]`d; `test_int1e_a01gp_is_fail_closed`
      still present and passing.
    - The SUMMARY documents the tracked follow-up.
  </acceptance_criteria>
  <done>
    Branch recorded. Guard removed + parity green + oracle_covered=true (A), OR guard
    preserved + follow-up tracked (B). The API is never dispatchable-with-wrong-output.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → eval_raw | Untrusted op-id + shell input crosses into the a01gp kernel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-07 | Information disclosure (wrong data) | a01gp kernel s-table | mitigate | Guard removal is GATED on vendor parity passing at atol=1e-12; if parity fails the guard stays (Task 2 Branch B), so wrong output is never exposed |
| T-26-08 | Tampering (manifest drift) | compiled_manifest.lock.json | mitigate | oracle_covered flipped to true ONLY on the parity-pass branch; manifest-audit auto-syncs from the lock |
</threat_model>

<verification>
- `cargo build -p cintx-ops` exits 0; manifest-audit green.
- `cargo test -p cintx-cubecl --features cpu` exits 0.
- Branch A: vendor a01gp parity exits 0, #[ignore] gone, oracle_covered=true, guard removed.
- Branch B: guard preserved, oracle_covered=false, #[ignore] retained, follow-up tracked.
</verification>

<success_criteria>
- The single failing GIAO-01 family is EITHER byte-identical to libcint 6.1.3 with
  oracle_covered=true and the guard removed, OR the fail-closed guard from 26-04 is
  preserved with a tracked follow-up. The public API is never left returning wrong output.
</success_criteria>

<output>
After completion, create `.planning/phases/26-group-5-spin-free-giao-nmr-integrals-complex/26-05-SUMMARY.md`
(record explicitly which branch — A math-fix-succeeded or B fallback — was taken).
</output>
