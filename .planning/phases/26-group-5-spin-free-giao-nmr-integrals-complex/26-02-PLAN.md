---
phase: 26-group-5-spin-free-giao-nmr-integrals-complex
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - crates/cintx-ops/generated/compiled_manifest.lock.json
  - crates/cintx-compat/src/raw.rs
  - crates/cintx-cubecl/src/kernels/one_electron.rs
  - crates/cintx-oracle/build.rs
  - crates/cintx-oracle/src/vendor_ffi.rs
  - crates/cintx-oracle/tests/giao_1e_parity.rs
  - crates/cintx-oracle/tests/giao_complex_roundtrip.rs
autonomous: true
requirements: [GIAO-01]
user_setup: []
must_haves:
  truths:
    - "11 spin-free 1e GIAO/CG families are registered (manifest + RawApiId + launcher) with their true component_rank and complex_output=true"
    - "Each 1e GIAO family is byte-identical to vendored libcint 6.1.3 at atol=1e-12 in cart AND sph"
    - "Every family parity test runs on a NON-ZERO gauge-origin, NON-SQUARE bra×ket block (a transposed or zero-origin layout cannot pass)"
    - "Spinor GIAO representations are registered but return UnsupportedApi (D-11)"
    - "int1e_igovlp round-trips through the safe API as Complex<f64>: imag non-zero, real exactly 0.0 (upgrades the Plan-01 round-trip to full D-07)"
  artifacts:
    - path: "crates/cintx-cubecl/src/kernels/one_electron.rs"
      provides: "1e GIAO #[cube] kernels (rank-3 cross-product + rank-9 tensor), generic over F"
      contains: "govlp"
    - path: "crates/cintx-compat/src/raw.rs"
      provides: "RawApiId consts for all 11 families × {cart,sph,spinor}"
      contains: "INT1E_GOVLP"
    - path: "crates/cintx-oracle/src/vendor_ffi.rs"
      provides: "real double* vendor wrappers for 22 cart/sph GIAO-1e symbols"
      contains: "vendor_int1e_govlp_sph"
    - path: "crates/cintx-oracle/tests/giao_1e_parity.rs"
      provides: "vendor-gated byte-identity parity for all 11 1e GIAO families"
      contains: "build_h2o_sto3g_common_orig"
  key_links:
    - from: "crates/cintx-cubecl/src/kernels/one_electron.rs GIAO dispatch"
      to: "plan.operator_env_params.common_orig"
      via: "Phase-22 gauge origin read"
      pattern: "common_orig"
    - from: "crates/cintx-oracle/tests/giao_1e_parity.rs"
      to: "vendor_ffi::vendor_int1e_*"
      via: "real-vs-real elementwise compare at atol=1e-12"
      pattern: "vendor_int1e"
---

<objective>
Register and implement the 11 spin-free 1e GIAO/CG families (GIAO-01) at byte-identity (cart+sph) vs vendored libcint 6.1.3 at atol=1e-12, on the FND-03 foundation from Plan 26-01. Families: `int1e_govlp`, `int1e_gnuc`, `int1e_igovlp`, `int1e_ignuc`, `int1e_igkin`, `int1e_a01gp`, `int1e_ia01p`, `int1e_cg_irxp`, `int1e_giao_irjxp`, `int1e_cg_a11part`, `int1e_giao_a11part`.

Purpose: GIAO-01 delivers the spin-free 1e magnetic-property integrals that NMR/GIAO workflows need. These are purely imaginary — the vendor cart/sph symbol returns the REAL magnitude of the imaginary part (D-15: real `double *out`, NOT `double complex`), so vendor comparison is real-vs-real; the `Complex<f64>` safe-API view is materialized cintx-side (re=0, im=value).

Output: 11 families registered via the D-10 5-step recipe (rank 3 for cross-product families, rank 9 for `a01gp`/`cg_a11part`/`giao_a11part`), 1e `#[cube]` kernels generic over F, 22 real `double*` vendor wrappers, and `giao_1e_parity.rs` vendor-gated on the non-zero gauge fixture with a non-square block.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-group-5-spin-free-giao-nmr-integrals-complex/26-CONTEXT.md
@.planning/phases/26-group-5-spin-free-giao-nmr-integrals-complex/26-RESEARCH.md
@.planning/phases/26-group-5-spin-free-giao-nmr-integrals-complex/26-PATTERNS.md
@.planning/phases/26-group-5-spin-free-giao-nmr-integrals-complex/26-01-SUMMARY.md

<interfaces>
<!-- GIAO-01 roster, component_rank, gout component order — derived from libcint 6.1.3 source (RESEARCH §Exact Roster + §gout Component-Index Order). Transcribe gout VERBATIM (D-12/D-13). -->

ROSTER (11 families × {cart, sph} = 22 symbols; spinor reps registered → UnsupportedApi):
| Family            | component_rank | gout def (libcint src)        | closest clone        |
|-------------------|----------------|-------------------------------|----------------------|
| int1e_govlp       | 3              | intor3.c CINTgout1e_int1e_govlp        | int1e_r (rank3,1e)   |
| int1e_gnuc        | 3              | intor3.c                                | int1e_r              |
| int1e_igovlp      | 3              | intor1.c CINTgout1e_int1e_igovlp        | int1e_r              |
| int1e_ignuc       | 3              | intor1.c                                | int1e_r              |
| int1e_igkin       | 3              | intor1.c                                | int1e_ipkin (rank3)  |
| int1e_a01gp       | 9              | intor1.c CINTgout1e_int1e_a01gp (27 s[]→9) | int1e_rr (rank9,1e) |
| int1e_ia01p       | 3              | intor1.c:81 CINTgout1e_int1e_ia01p      | int1e_r              |
| int1e_cg_irxp     | 3              | intor1.c CINTgout1e_int1e_cg_irxp       | int1e_r              |
| int1e_giao_irjxp  | 3              | intor1.c (identical gout to cg_irxp)    | int1e_r              |
| int1e_cg_a11part  | 9              | intor1.c CINTgout1e_int1e_cg_a11part    | int1e_rr             |
| int1e_giao_a11part| 9              | intor1.c (identical structure to cg_a11part) | int1e_rr        |

gout COMPONENT ORDER (copy VERBATIM — c = r_i − r_j; s = ⟨∇φ_i|φ_j⟩ x/y/z):
- int1e_igovlp: [0]=−c[1]s[2]+c[2]s[1], [1]=−c[2]s[0]+c[0]s[2], [2]=−c[0]s[1]+c[1]s[0]
- int1e_ia01p, int1e_cg_irxp, int1e_giao_irjxp (identical gout): [0]=s[5]−s[7], [1]=s[6]−s[2], [2]=s[1]−s[3]
- int1e_a01gp (rank9, row-major 3×3, r_gauge index outer / ∇ index inner): full c×(∇⊗∇) contraction, 27 intermediate s[] → 9 outputs, e.g. [0]=c[1]s[23]−c[2]s[14]−c[1]s[25]+c[2]s[16] — TRANSCRIBE THE FULL 9-component combos directly from intor1.c CINTgout1e_int1e_a01gp; do NOT guess
- int1e_cg_a11part, int1e_giao_a11part (rank9): direct gout[n*9+k]=s[k] for k=0..8
- int1e_govlp/gnuc/ignuc/igkin: transcribe from the cited intor1.c/intor3.c gout fns (g* vs ig* differ by a factor/derivative — read the source)

Phase-22 gauge consumer (one_electron.rs:7271-7323) — the moment kernel to mirror:
```rust
if plan.representation == Representation::Spinor {                 // D-11: spinor → UnsupportedApi
    return Err(cintxRsError::UnsupportedApi { requested: format!("spinor int1e_{op_name}") });
}
let origin: [f64; 3] = plan.operator_env_params.common_orig.unwrap_or([0.0; 3]);  // Phase-22
let block_len = nci * ncj;
let total_len = (rank as usize) * block_len;
if li as u32 + lj as u32 + moment_order > 8 {                     // fail-closed, no truncation (D-13)
    return Err(cintxRsError::UnsupportedApi { /* headroom */ });
}
```

RawApiId const pattern (raw.rs:221-269) — string MUST exactly match the manifest symbol_name:
```rust
pub const INT1E_R_CART: Self = Self::Symbol("int1e_r_cart");
pub const INT1E_R_SPH:  Self = Self::Symbol("int1e_r_sph");
pub const INT1E_R_SPINOR: Self = Self::Symbol("int1e_r_spinor");
```

Vendor wrapper pattern (vendor_ffi.rs:1414-1470) — REAL double* (D-15), out sized component_rank*ni*nj, component-leading:
```rust
pub fn vendor_int1e_r_sph(out: &mut [f64], shls: &[i32; 2], atm: &[i32], natm: i32,
    bas: &[i32], nbas: i32, env: &[f64]) -> i32 {
    unsafe { ffi::int1e_r_sph(out.as_mut_ptr(), ptr::null_mut(),
        shls.as_ptr() as *mut i32, atm.as_ptr() as *mut i32, natm,
        bas.as_ptr() as *mut i32, nbas, env.as_ptr() as *mut f64,
        ptr::null_mut(), ptr::null_mut()) }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register 11 1e GIAO families (manifest + RawApiId + vendor FFI) + fix OperatorId-shift consts</name>
  <files>crates/cintx-ops/generated/compiled_manifest.lock.json, crates/cintx-compat/src/raw.rs, crates/cintx-oracle/build.rs, crates/cintx-oracle/src/vendor_ffi.rs</files>
  <read_first>
    - .planning/phases/26-group-5-spin-free-giao-nmr-integrals-complex/26-RESEARCH.md §"Exact Roster" GIAO-01 (the 11 families, ng[], component_rank, closest clone) and §"Vendor FFI — REVISED binding strategy (A1)" (real double* per D-15)
    - .planning/phases/26-group-5-spin-free-giao-nmr-integrals-complex/26-PATTERNS.md §"RawApiId consts" (raw.rs:221-269 clone block) and §"Vendor FFI wrapper" (vendor_ffi.rs:1414-1470) and §"bindgen allowlist" (build.rs:374)
    - crates/cintx-ops/generated/compiled_manifest.lock.json (an `int1e_r` block to clone for rank-3 families; an `int1e_rr` block for rank-9 families — copy arity=2, set component_rank "3"/"9", complex_output true)
    - crates/cintx-compat/src/raw.rs:221-269 (INT1E_R_* / INT1E_RR_* const block to clone)
    - crates/cintx-oracle/build.rs:374 (the single allowlist_function regex to append symbols to) — note intor1.c/intor3.c must be in the cc::Build source list; verify and add if absent (intor4.c/intor2.c are already present per build.rs:62,229)
    - crates/cintx-oracle/src/vendor_ffi.rs:1414-1470 (vendor_int1e_r_sph/_cart real-double pattern)
  </read_first>
  <action>
    Apply the D-10 5-step registration recipe for all 11 families × {cart, sph, spinor}.

    (1) MANIFEST: clone the closest manifest entry per the roster table (int1e_r for rank-3 families, int1e_rr for rank-9 families: `a01gp`, `cg_a11part`, `giao_a11part`). Each new entry: arity=2, the EXACT `component_rank` from the roster ("3" or "9" — D-13: too-low silently truncates, derive from libcint ng[TENSOR], do NOT guess), and `complex_output: true` (the FND-03 flag from Plan 01). Register all three representations (cart/sph/spinor); spinor rows get `complex_output: true` too. Then `cargo build -p cintx-ops` regenerates api_manifest.{rs,csv}; manifest-audit auto-syncs (no separate fixtures list — D-10).

    (2) RawApiId: add `INT1E_GOVLP_{CART,SPH,SPINOR}`, `INT1E_GNUC_*`, `INT1E_IGOVLP_*`, `INT1E_IGNUC_*`, `INT1E_IGKIN_*`, `INT1E_A01GP_*`, `INT1E_IA01P_*`, `INT1E_CG_IRXP_*`, `INT1E_GIAO_IRJXP_*`, `INT1E_CG_A11PART_*`, `INT1E_GIAO_A11PART_*` consts in raw.rs cloning the INT1E_R_* block. The `Self::Symbol("...")` string MUST exactly match the manifest symbol_name.

    (4) VENDOR FFI: extend the bindgen `allowlist_function` regex in build.rs:374 with the 22 cart/sph symbols (`int1e_govlp_cart|int1e_govlp_sph|...|int1e_giao_a11part_cart|int1e_giao_a11part_sph`). Verify `intor1.c` and `intor3.c` are in the cc::Build source list (build.rs:51-262); add them if absent. Add 22 real `double *out` wrappers in vendor_ffi.rs cloning `vendor_int1e_r_sph`/`_cart` (D-15 — NOT len-2N; `out` sized `component_rank * ni * nj`, component-leading). For rank-9 families size `out` to `9 * ni * nj`.

    OperatorId-shift fix (Pitfall 4 / project memory): adding ~33 GIAO rows re-points positional `OperatorId::new(<int>)` / `_OPERATOR_ID: u32 = N` test consts at a different family → InvalidShellTuple. After regeneration, `grep -rn 'OperatorId::new(' crates/` and `grep -rn '_OPERATOR_ID: u32 =' crates/` and re-anchor any hardcoded positional const by SYMBOL NAME (resolve through the manifest by symbol, not integer index). Confirm `cargo build` is clean afterward.
  </action>
  <verify>
    <automated>cargo build -p cintx-ops 2>&1 | tail -3 && cargo build -p cintx-oracle --features cpu 2>&1 | tail -5 && cargo build --workspace 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `compiled_manifest.lock.json` contains `int1e_govlp`, `int1e_a01gp`, `int1e_giao_a11part` symbols with `component_rank` "3"/"9" matching the roster and `complex_output: true`
    - `crates/cintx-compat/src/raw.rs` contains `INT1E_GOVLP_SPH` and `INT1E_A01GP_SPH` consts
    - `crates/cintx-oracle/build.rs` allowlist regex contains `int1e_govlp_sph` and `int1e_giao_a11part_cart`
    - `crates/cintx-oracle/src/vendor_ffi.rs` contains `pub fn vendor_int1e_govlp_sph` and `pub fn vendor_int1e_a01gp_cart`
    - `cargo build --workspace` exits 0 (OperatorId-shift consts re-anchored by symbol name — no InvalidShellTuple)
    - `cargo build -p cintx-ops` exits 0 (manifest regenerates)
  </acceptance_criteria>
  <done>All 11 1e GIAO families registered across manifest/RawApiId/vendor-FFI with correct component_rank and complex_output=true; OperatorId-shift consts re-anchored by symbol name; workspace builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: Implement 1e GIAO #[cube] kernels (rank-3 cross-product + rank-9 tensor) generic over F</name>
  <files>crates/cintx-cubecl/src/kernels/one_electron.rs</files>
  <read_first>
    - docs/manual/Cubecl/Cubecl_generics.md (Float/Numeric bounds — D-08 MANDATORY before writing kernel code)
    - docs/manual/Cubecl/Cubecl_algebra.md (F::exp/F::sqrt/F::sin — Gaussian/gauge terms; D-08)
    - docs/manual/Cubecl/Cubecl_basic_operations.md (supported arithmetic; D-08)
    - docs/manual/Cubecl/Cubecl_conditionals.md (statement-form if for boundary checks; D-08)
    - /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/INDEX.md (canonical manual — D-08)
    - crates/cintx-cubecl/src/kernels/one_electron.rs:7213-7323 (moment dispatch table + common_orig consumer + headroom guard — the exact clone target; PATTERNS.md §1e GIAO kernel)
    - .planning/phases/26-group-5-spin-free-giao-nmr-integrals-complex/26-RESEARCH.md §"gout Component-Index Order" (the VERBATIM c[]·s[] combos — also mirrored in <interfaces> above)
    - libcint-master/src/autocode/intor1.c (CINTgout1e_int1e_igovlp, _a01gp, _ia01p, _cg_irxp, _cg_a11part, _igkin, _ignuc) and src/autocode/intor3.c (CINTgout1e_int1e_govlp, _gnuc) — the SOURCE OF TRUTH for the gout combos; transcribe exactly
  </read_first>
  <action>
    Implement the 1e GIAO `#[cube]` kernels following D-08 (generic over `F`; no plain-Rust-fn calls inside `#[cube]`; no `if`-expressions, statement form only; `u32`/`i32` only for integers; no `continue`/`break`). Mirror the Phase-24 moment kernel at one_electron.rs:7271-7323 — it reads `plan.operator_env_params.common_orig` (Phase-22 gauge origin, already wired), fails closed on `Representation::Spinor` (D-11 → UnsupportedApi), and guards angular-momentum headroom on the KET (D-12).

    Add a GIAO dispatch table beside `moment_dispatch_name` mapping op_name → (op_kind, rank):
    - rank-3 cross-product group: `"govlp"|"gnuc"|"igovlp"|"ignuc"|"igkin"|"ia01p"|"cg_irxp"|"giao_irjxp"` → rank 3
    - rank-9 tensor group: `"a01gp"|"cg_a11part"|"giao_a11part"` → rank 9
    Each op_kind selects its gout combo. TRANSCRIBE the gout component combos VERBATIM from the cited libcint source (intor1.c/intor3.c) — they are NOT derivable by inspection; a sign or index error fails byte-identity silently (D-12/D-13). Use the combos in <interfaces> as the index map, but confirm each against the source function before committing. `c = r_i − r_j`, `s = ⟨∇φ_i|φ_j⟩` components.

    RECOMMENDED kernel structure (RESEARCH Open-Q3): a shared parameterized entry for the rank-3 cross-product families (they share curl structure) and per-family handling for the rank-9 families (`a01gp` has a distinct 27→9 contraction; `cg_a11part`/`giao_a11part` are direct s[k]). Implementer's call as long as D-02/D-08/D-12 hold. The device emits REAL components; the FND-03 host materialization (Plan 01) wraps them as re=0/im=value for the complex view. Raise ket headroom via `ng[]`, not bra (D-12).
  </action>
  <verify>
    <automated>cargo build -p cintx-cubecl --features cpu 2>&1 | tail -8 && cargo test -p cintx-cubecl --features cpu 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `crates/cintx-cubecl/src/kernels/one_electron.rs` contains a GIAO dispatch arm matching `"igovlp"` and `"a01gp"`
    - the kernel reads `plan.operator_env_params.common_orig` for the gauge origin (no new env slot)
    - the kernel returns `UnsupportedApi` for `Representation::Spinor` (D-11)
    - kernel code is generic over `F`, contains no plain-Rust-fn calls inside `#[cube]`, no if-expressions, no continue/break (D-08)
    - `cargo build -p cintx-cubecl --features cpu` exits 0
    - `cargo test -p cintx-cubecl --features cpu` exits 0
  </acceptance_criteria>
  <done>1e GIAO kernels implemented generic over F with gout combos transcribed verbatim from libcint, consuming the Phase-22 gauge origin, failing closed on spinor; cintx-cubecl builds and its unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Vendor-gated parity tests for all 11 1e GIAO families + upgrade FND-03 round-trip</name>
  <files>crates/cintx-oracle/tests/giao_1e_parity.rs, crates/cintx-oracle/tests/giao_complex_roundtrip.rs</files>
  <read_first>
    - crates/cintx-oracle/tests/moment_r_parity.rs (full file — the vendor-parity analog to clone) and crates/cintx-oracle/tests/moment_common.rs (the rank-parameterized `vendor_parity` helper at :9-14; `non_square_shell_pair()` at :76-79; `cross_center_non_square_shell_pair()`)
    - crates/cintx-oracle/tests/common_orig_roundtrip.rs (the existing round-trip; the giao_complex_roundtrip.rs from Plan 01 to upgrade)
    - .planning/phases/26-group-5-spin-free-giao-nmr-integrals-complex/26-PATTERNS.md §"New test file: tests/giao_1e_parity.rs" (the exact test-fn shape, double-gating, ATOL=1e-12) and §"New test file: tests/giao_complex_roundtrip.rs"
    - .planning/phases/26-group-5-spin-free-giao-nmr-integrals-complex/26-RESEARCH.md §"Non-zero Gauge-Origin Fixture (D-06/D-07)" (build_h2o_sto3g_common_orig, non-square requirement, the D-07 assertion target = cintx safe-API view)
  </read_first>
  <action>
    Create `crates/cintx-oracle/tests/giao_1e_parity.rs` cloning `moment_r_parity.rs` + `moment_common.rs`. Crate gate `#![cfg(any(feature = "cpu", feature = "rocm"))]`; per-test `#[cfg(has_vendor_libcint)] #[cfg(feature = "cpu")]`. One test fn per family (11 fns), each:
    - build the non-zero gauge fixture `build_h2o_sto3g_common_orig()` (D-06 — a zero-origin GIAO test is doubly trivial)
    - call the rank-parameterized `moment_common::vendor_parity(rank, RawApiId::INT1E_<FAM>_SPH, RawApiId::INT1E_<FAM>_CART, vendor_ffi::vendor_int1e_<fam>_sph, vendor_ffi::vendor_int1e_<fam>_cart, &atm, &bas, &env, "int1e_<fam>")` with the correct rank (3 or 9 from the roster)
    - use `non_square_shell_pair()` (O-1s × O-2p, p×d) for the D-12 transpose gate; use `cross_center_non_square_shell_pair()` for any origin-relative family that vanishes same-center
    - assert mismatch count 0 at ATOL=1e-12 / RTOL=0 (D-14) — this is a REAL-vs-REAL comparison (D-15: vendor cart/sph is real double*; cintx emits real components)

    Upgrade `giao_complex_roundtrip.rs` (created in Plan 01): now that `int1e_igovlp` is registered + kernel'd, replace the placeholder/seam assertion with the FULL D-07 check on `int1e_igovlp` driven via `eval_raw` on `build_h2o_sto3g_common_orig`, taking the safe-API `IntegralTensor::complex_values()` view: imaginary half NON-ZERO somewhere (content landed), real half EXACTLY `== 0.0` (no accidental real content).

    Verify with the vendor gate. NOTE: the folded todo `oracle-cart-offset-vendor-zero` (`compare::tests` `--lib` unit failures, CINTshells_cart_offset[4] cintx=8 vendor=0) is a CONFIRMED pre-existing harness bug — the family parity here is `--test` integration; do NOT let the `--lib` failures block this gate.
  </action>
  <verify>
    <automated>CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu giao_1e_parity 2>&1 | tail -15 && CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu giao_complex_roundtrip 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `crates/cintx-oracle/tests/giao_1e_parity.rs` exists with 11 test fns, double-gated `#[cfg(has_vendor_libcint)] #[cfg(feature = "cpu")]`, calling `build_h2o_sto3g_common_orig` and a non-square shell pair
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu giao_1e_parity` exits 0 with all 11 families byte-identical at atol=1e-12 (cart + sph)
    - `crates/cintx-oracle/tests/giao_complex_roundtrip.rs` asserts on `int1e_igovlp` via `complex_values()`: imag non-zero AND real part `== 0.0`
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu giao_complex_roundtrip` exits 0
    - all 11 families' manifest rows have `oracle_covered: true` (flip in the lock after parity is green; manifest-audit auto-syncs)
  </acceptance_criteria>
  <done>All 11 1e GIAO families pass vendor byte-identity at atol=1e-12 (cart+sph) on the non-zero-gauge non-square fixture; the FND-03 round-trip proves int1e_igovlp's Complex<f64> view (imag non-zero, real exactly 0.0); oracle_covered flipped true.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → eval_raw (atm/bas/env) | Numeric input arrays cross into the planner/kernel; validated by the shell-tuple validator and `assert_flat_buffer_contract` |

## STRIDE Threat Register

Per 26-RESEARCH.md §Security Domain: numerical library, no auth/session/network/crypto surface; no ASVS categories apply. Integrity controls: typed errors (thiserror v2, no silent partial writes), `cargo --locked`, byte-identity oracle gates.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03 | Tampering (data integrity) | 1e GIAO kernels (component_rank truncation) | mitigate | Each family registered with its true `component_rank` (3 or 9) derived from libcint ng[TENSOR]; a too-low rank silently truncates trailing components (D-13). The vendor byte-identity gate at atol=1e-12 on a non-square block detects any truncation or transpose. |
| T-26-04 | Spoofing (representation confusion) | spinor GIAO dispatch | mitigate | Spinor reps registered but return `UnsupportedApi` (D-11) — no spin block is silently computed as spin-free. |
</threat_model>

<verification>
- `cargo build --workspace` clean after registration + OperatorId-shift re-anchoring (Task 1)
- `cargo build -p cintx-cubecl --features cpu` + `cargo test -p cintx-cubecl --features cpu` (Task 2)
- `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu giao_1e_parity` — 11 families byte-identical (Task 3)
- `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu giao_complex_roundtrip` — FND-03 D-07 (Task 3)
- `cargo xtask manifest-audit` (or the existing audit test) green after oracle_covered flip
- Pre-existing `oracle-cart-offset-vendor-zero` `--lib` harness bug is NOT in scope and must NOT block
- WORKTREE INTEGRATION (D-09, project memory — worktree auto-merge is inconsistent): this Wave-2 cluster runs in a parallel worktree against Cluster B (Plan 26-03); both depend_on Plan 26-01 merged to main FIRST. Both clusters touch the same shared files (`compiled_manifest.lock.json`, `crates/cintx-compat/src/raw.rs`, `crates/cintx-oracle/build.rs`, `crates/cintx-oracle/src/vendor_ffi.rs`), so a merge conflict on the manifest lock is expected. Resolve it by taking BOTH clusters' new rows; re-anchor OperatorId consts by symbol name (never integer) so the merge is order-independent. MANDATORY post-merge regeneration step: after resolving the `compiled_manifest.lock.json` merge conflict (take BOTH clusters' new rows), run `cargo build -p cintx-ops` to regenerate `api_manifest.{rs,csv}` from the unified lock (each worktree regenerated it against only its own row set, so the merged manifest is otherwise stale and inconsistent), then run `cargo build --workspace` to confirm the full workspace compiles against the merged manifest, then re-run the vendor-gated parity suite (`CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu giao_1e_parity`) to confirm this cluster still passes post-merge. Finally verify `git merge-base --is-ancestor <this-wave-branch> main`; merge manually if not an ancestor.
</verification>

<success_criteria>
- 11 spin-free 1e GIAO/CG families registered (manifest + RawApiId + launcher + vendor-FFI + oracle) with correct component_rank and complex_output=true
- Each byte-identical to libcint 6.1.3 at atol=1e-12 in cart AND sph on the non-zero-gauge non-square fixture
- Spinor reps registered → UnsupportedApi (D-11)
- int1e_igovlp safe-API Complex<f64> round-trip: imag non-zero, real exactly 0.0 (full D-07)
- All families oracle_covered=true; manifest-audit green; workspace builds clean
</success_criteria>

<output>
After completion, create `.planning/phases/26-group-5-spin-free-giao-nmr-integrals-complex/26-02-SUMMARY.md`
</output>
</output>
