---
phase: 26-group-5-spin-free-giao-nmr-integrals-complex
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cintx-oracle/tests/giao_2e_parity.rs
autonomous: true
gap_closure: true
requirements: [GIAO-02]

must_haves:
  truths:
    - "giao_2e_parity.rs reuses the shared moment_common helpers (ATOL/RTOL/ncart/nsph/count_mismatches/matches_with_tol) instead of duplicated local copies, so a tolerance change in moment_common tracks both GIAO parity files"
    - "The 2e cross-center quartet shell selection is self-documenting / asserted against expected angular momenta rather than a bare [3,2,3,2] literal"
  artifacts:
    - path: "crates/cintx-oracle/tests/giao_2e_parity.rs"
      provides: "moment_common import + quartet AM assertion; local helper duplicates removed"
      contains: "moment_common"
  key_links:
    - from: "giao_2e_parity.rs"
      to: "moment_common.rs"
      via: "#[path = \"moment_common.rs\"] mod moment_common"
      pattern: "moment_common"
---

<objective>
Close WR-03 + IN-01: `giao_2e_parity.rs` re-implements `ncart`, `nsph`,
`matches_with_tol`, `count_mismatches`, `assert_any_nonzero`, and the `ATOL`/`RTOL`
consts locally, whereas the sibling `giao_1e_parity.rs` imports them from
`moment_common` via `#[path = "moment_common.rs"] mod moment_common;`. Duplicated
tolerance/mismatch logic can drift, weakening the parity gate. Additionally (IN-01),
`cross_center_non_square_quartet` hard-codes `[3, 2, 3, 2]` without sourcing or
asserting the selected angular momenta.

Purpose: Single source of truth for parity tolerances and mismatch logic across both
GIAO parity files; self-documenting, refactor-safe shell selection. Code-quality fix
that strengthens the GIAO-02 gate without changing what is tested.

Output: `giao_2e_parity.rs` importing shared helpers + an explicit AM assertion on the
quartet. This plan touches ONLY giao_2e_parity.rs — no overlap with any other gap plan.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-group-5-spin-free-giao-nmr-integrals-complex/26-REVIEW.md

<interfaces>
<!-- The duplicated local helpers to delete and the shared module to import. -->

From crates/cintx-oracle/tests/giao_1e_parity.rs:24-33 (the import pattern to mirror):
```rust
#[path = "moment_common.rs"]
mod moment_common;
// ...
use super::moment_common::{
    ATOL, RTOL, count_mismatches, cross_center_non_square_shell_pair, ncart, nsph,
};
```

From crates/cintx-oracle/tests/giao_2e_parity.rs:39-153 (the DUPLICATES to remove):
```rust
const ATOL: f64 = 1e-12;
const RTOL: f64 = 0.0;
fn ncart(l: i32) -> usize { ... }
fn nsph(l: i32) -> usize { ... }
fn matches_with_tol(reference: f64, observed: f64, atol: f64, rtol: f64) -> bool { ... }
fn count_mismatches(reference: &[f64], observed: &[f64]) -> usize { ... }
fn assert_any_nonzero(block: &[f64], label: &str) { ... }
```

From giao_2e_parity.rs:50-54 (IN-01 — the quartet literal):
```rust
/// H2O/STO-3G shells: 0=O-1s, 1=O-2s, 2=O-2p(l=1), 3=H1-1s(l=0), 4=H2-1s(l=0).
/// [H1-1s, O-2p, H1-1s, O-2p] → rirj≠0 (e1) AND rkrl≠0 (e2), non-square (D-12).
fn cross_center_non_square_quartet() -> [usize; 4] {
    [3, 2, 3, 2]
}
```

NOTE: moment_common's `count_mismatches` takes `(reference, observed)` and uses its own
ATOL/RTOL; the 2e local copy hard-codes ATOL/RTOL inside the fn. Confirm the moment_common
signatures before deleting locals; if any helper (e.g. `assert_any_nonzero`,
`matches_with_tol`, `nsph`/`ncart`) is NOT exported by moment_common, either add it there
(so both files share it) or keep a thin local that delegates — do not silently change the
tolerance semantics of the 2e gate.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Import shared moment_common helpers; delete local duplicates</name>
  <files>crates/cintx-oracle/tests/giao_2e_parity.rs</files>
  <read_first>
    - crates/cintx-oracle/tests/giao_1e_parity.rs (lines 24-33: the moment_common
      import pattern to mirror)
    - crates/cintx-oracle/tests/moment_common.rs (the exported helper signatures:
      ATOL, RTOL, ncart, nsph, count_mismatches, matches_with_tol, assert_any_nonzero,
      cross_center_non_square helpers — confirm which exist before deleting locals)
    - crates/cintx-oracle/tests/giao_2e_parity.rs (lines 39-153: the local duplicates
      and their call sites in the test bodies)
  </read_first>
  <action>
    In `giao_2e_parity.rs`, add `#[path = "moment_common.rs"] mod moment_common;` and import
    the shared helpers (`ATOL, RTOL, ncart, nsph, count_mismatches`, plus
    `matches_with_tol`/`assert_any_nonzero` if exported) exactly as `giao_1e_parity.rs:24-33`
    does. Delete the local re-implementations at 39-153 that are now imported.

    For any helper NOT exported by moment_common: prefer adding it to moment_common (so both
    GIAO files share one definition), OR factor a `giao_common` module shared by both parity
    files. Do NOT keep a divergent local copy with hard-coded ATOL/RTOL. Preserve the exact
    tolerance values (ATOL=1e-12, RTOL=0.0) — moment_common already defines these; the goal
    is one source of truth, not a tolerance change.

    Keep the 2e-specific `ncart`/`nsph` usage and test bodies functionally identical — only
    the helper SOURCE changes.
  </action>
  <verify>
    <automated>cargo build -p cintx-oracle --features cpu --tests && cargo test -p cintx-oracle --features cpu --test giao_2e_parity 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'mod moment_common' crates/cintx-oracle/tests/giao_2e_parity.rs` returns a
      match (the shared module is wired in) (MANDATORY).
    - `grep -c 'const ATOL' crates/cintx-oracle/tests/giao_2e_parity.rs` == 0 (the local
      const duplicate is removed; ATOL now comes from moment_common) (MANDATORY).
    - `grep -c 'fn count_mismatches' crates/cintx-oracle/tests/giao_2e_parity.rs` == 0
      (local duplicate removed) (MANDATORY).
    - `cargo build -p cintx-oracle --features cpu --tests` exits 0 (MANDATORY).
    - The 4 GIAO-02 parity test fns still exist and compile (verify
      `grep -c 'fn test_int2e' giao_2e_parity.rs` >= 4) (MANDATORY).
  </acceptance_criteria>
  <done>
    giao_2e_parity.rs uses shared moment_common helpers; no local tolerance/mismatch duplicates.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make the cross-center quartet self-documenting (IN-01)</name>
  <files>crates/cintx-oracle/tests/giao_2e_parity.rs</files>
  <read_first>
    - crates/cintx-oracle/tests/giao_2e_parity.rs (lines 50-54: cross_center_non_square_quartet)
    - crates/cintx-oracle/tests/moment_common.rs (whether a shared quartet/shell helper exists)
  </read_first>
  <action>
    Address IN-01 by either:
    (a) sourcing the quartet from a shared `moment_common` helper (preferred if a suitable
        cross-center helper exists), OR
    (b) asserting the selected shell angular momenta equal the expected `[0, 1, 0, 1]`
        (H1-1s l=0, O-2p l=1, H1-1s l=0, O-2p l=1) at the point of use, before the quartet
        is consumed in each test, so a future shell-ordering change in
        `build_h2o_sto3g_common_orig` fails loudly rather than silently selecting different
        shells.

    Use approach (b) if no shared quartet helper exists. Add an assertion like:
    `assert_eq!([l(q[0]), l(q[1]), l(q[2]), l(q[3])], [0, 1, 0, 1], "quartet AM drifted");`
    reading each shell's angular momentum from the fixture. Keep the existing
    `[3, 2, 3, 2]` index mapping comment.
  </action>
  <verify>
    <automated>cargo test -p cintx-oracle --features cpu --test giao_2e_parity 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Either `cross_center_non_square_quartet` is sourced from `moment_common`
      (`grep moment_common.*quartet` matches), OR an `assert_eq!(...[0, 1, 0, 1]...)`
      angular-momentum check guards the quartet before use (MANDATORY — verify with grep
      for `[0, 1, 0, 1]` or the moment_common quartet symbol).
    - `cargo build -p cintx-oracle --features cpu --tests` exits 0 (MANDATORY).
  </acceptance_criteria>
  <done>
    The 2e quartet selection is self-documenting / asserted, refactor-safe against fixture
    shell reordering.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (test-only) | No production trust boundary; parity-test code quality |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-11 | Tampering (silent gate drift) | giao_2e_parity.rs duplicated tolerances | mitigate | Single source of truth via moment_common import (Task 1) prevents a tolerance change in one file from being missed in the other |
| T-26-12 | Tampering (silent fixture drift) | quartet literal | mitigate | AM assertion / shared helper (Task 2) fails loudly if shell ordering changes |
</threat_model>

<verification>
- `cargo build -p cintx-oracle --features cpu --tests` exits 0.
- giao_2e_parity.rs has no local ATOL/RTOL/count_mismatches duplicates.
- Quartet selection asserted or shared-helper-sourced.
</verification>

<success_criteria>
- giao_2e_parity.rs shares moment_common's tolerance/mismatch helpers and the quartet
  shell selection is self-documenting — the GIAO-02 gate cannot silently drift from the
  shared parity convention.
</success_criteria>

<output>
After completion, create `.planning/phases/26-group-5-spin-free-giao-nmr-integrals-complex/26-07-SUMMARY.md`
</output>
