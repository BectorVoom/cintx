---
phase: 26-group-5-spin-free-giao-nmr-integrals-complex
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cintx-cubecl/src/kernels/one_electron.rs
  - crates/cintx-oracle/tests/giao_1e_parity.rs
autonomous: true
gap_closure: true
requirements: [GIAO-01]

must_haves:
  truths:
    - "Calling eval_raw(RawApiId::INT1E_A01GP_CART, ...) (or _SPH) returns Err(cintxRsError::UnsupportedApi) — never a silently-wrong numeric buffer"
    - "The a01gp guard returns BEFORE write_giao_complex_staging / any compute in the giao_nuc dispatch"
    - "A test asserts the UnsupportedApi outcome for the a01gp raw path"
  artifacts:
    - path: "crates/cintx-cubecl/src/kernels/one_electron.rs"
      provides: "Fail-closed UnsupportedApi guard for op_name == \"a01gp\" before compute"
      contains: "a01gp"
    - path: "crates/cintx-oracle/tests/giao_1e_parity.rs"
      provides: "Test asserting eval_raw a01gp returns UnsupportedApi"
      contains: "UnsupportedApi"
  key_links:
    - from: "one_electron.rs giao_nuc dispatch"
      to: "cintxRsError::UnsupportedApi"
      via: "early return on op_name == \"a01gp\" before write_giao_complex_staging"
      pattern: "a01gp.*UnsupportedApi|UnsupportedApi.*a01gp"
---

<objective>
Close the verified GIAO-01 gap (VERIFICATION.md / CR-01): `int1e_a01gp` is a
dispatchable public API that returns known-incorrect results (~2× on rank-9
components 1..8) with NO runtime guard. Add a fail-closed `UnsupportedApi` guard
so callers receive a typed error instead of silently-wrong numbers — BEFORE any
compute / `write_giao_complex_staging`.

Purpose: The project's core value is libcint byte-identity. A dispatchable entry
point emitting known-wrong numbers with no error violates the no-silent-wrong-output
contract. This guard alone closes the VERIFICATION gap and makes the public API
fail-closed while the kernel correction (Plan 26-05) is attempted.

Output: A typed guard in `one_electron.rs` + a test proving the raw path is
fail-closed. This is the BLOCKING gap closure and must land before 26-05/26-08
touch `one_electron.rs`.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-group-5-spin-free-giao-nmr-integrals-complex/26-VERIFICATION.md
@.planning/phases/26-group-5-spin-free-giao-nmr-integrals-complex/26-REVIEW.md
@.planning/phases/26-group-5-spin-free-giao-nmr-integrals-complex/26-02-SUMMARY.md

<interfaces>
<!-- Dispatch table and error type the executor needs. Extracted from codebase. -->

From crates/cintx-cubecl/src/kernels/one_electron.rs:25:
```rust
use cintx_core::{CintFloat, PrecisionKind, Representation, cintxRsError};
```

From one_electron.rs:8604-8613 (giao_nuc dispatch — the a01gp arm is line 8608):
```rust
let giao_nuc_op: Option<(u32, u32)> = match op_name {
    "gnuc" => Some((0, 3)),
    "ignuc" => Some((1, 3)),
    "ia01p" => Some((2, 3)),
    "a01gp" => Some((3, 9)),       // <-- known-wrong, must fail closed
    "cg_a11part" => Some((4, 9)),
    "giao_a11part" => Some((5, 9)),
    _ => None,
};
let is_giao_nuc = giao_nuc_op.is_some();
```

From one_electron.rs:8774 (top of the nuclear-engine arm — guard goes here, before any
buffer alloc / run_1e_giao_nuc / write_giao_complex_staging at ~8751-8754 pattern):
```rust
if let Some((op_kind, rank)) = giao_nuc_op {
    if plan.representation == Representation::Spinor {
        return Err(cintxRsError::UnsupportedApi { requested: format!("spinor int1e_{op_name}") });
    }
    // ... compute begins below ...
}
```

cintxRsError::UnsupportedApi shape (thiserror v2, already used throughout this file):
```rust
return Err(cintxRsError::UnsupportedApi { requested: format!("...") });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fail-closed UnsupportedApi guard for op_name == "a01gp"</name>
  <files>crates/cintx-cubecl/src/kernels/one_electron.rs</files>
  <read_first>
    - crates/cintx-cubecl/src/kernels/one_electron.rs (lines 8590-8835: the giao_ovlp_op
      table, the giao_nuc_op dispatch table at 8604-8613, and the nuclear-engine arm
      `if let Some((op_kind, rank)) = giao_nuc_op {` at 8774 — the guard must precede
      every buffer alloc and the `write_giao_complex_staging` call)
  </read_first>
  <action>
    In `one_electron.rs`, add a fail-closed guard so `int1e_a01gp` returns
    `UnsupportedApi` BEFORE any compute or `write_giao_complex_staging`. Place it at
    the TOP of the nuclear-engine arm `if let Some((op_kind, rank)) = giao_nuc_op {`
    (line ~8774), immediately after the existing spinor `UnsupportedApi` check and
    before the Rys-nroots guard / buffer allocation. Copy the CR-01 snippet from
    26-REVIEW.md verbatim:

    ```rust
    if op_name == "a01gp" {
        return Err(cintxRsError::UnsupportedApi {
            requested: format!(
                "int1e_a01gp is registered but not yet correct (rank-9 ket-derivative \
                 double-count; tracked, oracle_covered=false) — refusing to return \
                 non-parity output for {op_name}"
            ),
        });
    }
    ```

    Add a `// CR-01 / 26-04: fail-closed until kernel parity (26-05) lands` comment
    above it referencing that Plan 26-05 removes this guard once vendor parity passes.
    Do NOT touch the `giao_nuc_op` table (a01gp must stay registered/dispatchable so
    the descriptor still resolves — only the COMPUTE is refused). Do NOT add any
    `if dst < staging.len()` partial-write guard (project memory: monolithic writers).
  </action>
  <verify>
    <automated>grep -n 'op_name == "a01gp"' crates/cintx-cubecl/src/kernels/one_electron.rs && cargo build -p cintx-cubecl --features cpu</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A4 'op_name == "a01gp"' crates/cintx-cubecl/src/kernels/one_electron.rs`
      shows the guard returning `cintxRsError::UnsupportedApi` (MANDATORY).
    - The guard line number is LESS than the line number of the FIRST
      `write_giao_complex_staging` call inside the nuclear-engine arm — verify with
      `grep -n 'op_name == "a01gp"\|write_giao_complex_staging' one_electron.rs` and
      confirm the guard precedes the staging write that follows the nuclear arm
      (MANDATORY).
    - `cargo build -p cintx-cubecl --features cpu` exits 0 (MANDATORY).
  </acceptance_criteria>
  <done>
    op_name == "a01gp" returns UnsupportedApi before any compute; the package builds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Assert the a01gp raw path is fail-closed via UnsupportedApi</name>
  <files>crates/cintx-oracle/tests/giao_1e_parity.rs</files>
  <read_first>
    - crates/cintx-oracle/tests/giao_1e_parity.rs (lines 1-60 for the module/imports
      structure and the `giao_vendor_parity` helper signature; lines 209-228 for the
      `#[ignore]`d `test_int1e_a01gp_parity`)
  </read_first>
  <action>
    Add a NEW test (do NOT un-ignore `test_int1e_a01gp_parity` — that stays
    `#[ignore]`d; Plan 26-05 owns its removal). The new test drives the a01gp raw path
    through `eval_raw(RawApiId::INT1E_A01GP_CART, ...)` (or `_SPH`) on the existing
    `build_h2o_sto3g_common_orig` cross-center non-square block and asserts the call
    returns `Err(cintxRsError::UnsupportedApi { .. })`. This test is NOT vendor-gated —
    it must run on the default (non-vendor) test profile so the fail-closed contract is
    always covered. Name it `test_int1e_a01gp_is_fail_closed`. Add a comment:
    `// 26-04 / CR-01: a01gp is registered but the kernel is known-wrong; the public`
    `// raw path MUST fail closed with UnsupportedApi until 26-05 lands parity.`

    Reuse the existing fixture/shell-pair helpers (`cross_center_non_square_shell_pair`,
    `build_h2o_sto3g_common_orig`) already imported in this file. Match on the error
    variant; assert it is `cintxRsError::UnsupportedApi`.
  </action>
  <verify>
    <automated>cargo test -p cintx-oracle --features cpu test_int1e_a01gp_is_fail_closed -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'fn test_int1e_a01gp_is_fail_closed' crates/cintx-oracle/tests/giao_1e_parity.rs`
      returns a match (MANDATORY).
    - The new test body contains `UnsupportedApi` and `INT1E_A01GP_` (MANDATORY).
    - The new test does NOT carry `#[ignore]` and does NOT require
      `CINTX_ORACLE_BUILD_VENDOR=1` (it runs on the default profile) (MANDATORY).
    - `cargo test -p cintx-oracle --features cpu test_int1e_a01gp_is_fail_closed`
      exits 0 (MANDATORY).
    - `test_int1e_a01gp_parity` REMAINS `#[ignore]`d (verify with
      `grep -B1 'fn test_int1e_a01gp_parity' giao_1e_parity.rs` showing `#[ignore`)
      (MANDATORY — 26-05 owns its removal).
  </acceptance_criteria>
  <done>
    A non-vendor-gated test proves eval_raw(a01gp) returns UnsupportedApi; the old
    parity test stays ignored.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → eval_raw | Untrusted op-id + shell input crosses into the dispatcher |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05 | Information disclosure (wrong data) | one_electron.rs a01gp arm | mitigate | Fail-closed UnsupportedApi guard before compute (Task 1) — caller receives typed error, never silently-wrong numerical output |
| T-26-06 | Tampering (regression) | giao_1e_parity.rs | mitigate | Non-vendor-gated test (Task 2) locks the fail-closed contract so a future un-guarding regresses loudly |
</threat_model>

<verification>
- `cargo build -p cintx-cubecl --features cpu` exits 0.
- `cargo test -p cintx-oracle --features cpu test_int1e_a01gp_is_fail_closed` exits 0.
- `test_int1e_a01gp_parity` remains `#[ignore]`d.
</verification>

<success_criteria>
- int1e_a01gp (cart + sph) returns UnsupportedApi via eval_raw — no silently-wrong buffer.
- The guard returns before write_giao_complex_staging / any compute.
- A non-vendor test asserts the fail-closed outcome.
</success_criteria>

<output>
After completion, create `.planning/phases/26-group-5-spin-free-giao-nmr-integrals-complex/26-04-SUMMARY.md`
</output>
