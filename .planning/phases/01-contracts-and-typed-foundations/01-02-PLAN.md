---
phase: 01-contracts-and-typed-foundations
plan: "02"
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/lib.rs
  - src/runtime/mod.rs
  - src/runtime/validator.rs
  - src/runtime/workspace_query.rs
  - src/diagnostics/mod.rs
  - src/diagnostics/report.rs
  - src/api/safe.rs
  - src/api/raw.rs
  - tests/phase1_workspace_query.rs
  - tests/phase1_diagnostics_contract.rs
autonomous: true
requirements:
  - SAFE-02
  - MEM-03
must_haves:
  truths:
    - query_workspace returns deterministic WorkspaceQuery results for identical validated inputs.
    - Query path diagnostics always include machine-parseable context fields (api, representation, shell tuple, dims/bytes, backend candidate, feature flags).
    - Dims or buffer mismatches fail pre-execution with typed errors and correlated diagnostics metadata.
  artifacts:
    - src/runtime/workspace_query.rs implements deterministic workspace estimation contract and WorkspaceQuery struct.
    - src/runtime/validator.rs provides shared validated input/shape outputs used by safe and raw query surfaces.
    - src/diagnostics/report.rs defines structured diagnostics payload for MEM-03 error context.
    - tests/phase1_workspace_query.rs and tests/phase1_diagnostics_contract.rs lock determinism and diagnostics-field completeness.
  key_links:
    - src/api/safe.rs query_workspace delegates to src/runtime/validator.rs and src/runtime/workspace_query.rs without exposing raw dims.
    - src/api/raw.rs validates optional dims override against natural shape before returning WorkspaceQuery.
    - src/diagnostics/report.rs attaches correlation fields to LibcintRsError returns used in both safe and raw paths.
---

<objective>
Implement deterministic workspace query and structured diagnostics contracts on top of typed contracts from plan 01.

Purpose: Deliver SAFE-02 and MEM-03 behaviors before execution kernels so workspace sizing and failure diagnosis are stable and testable.
Output: Shared validator + workspace query modules, diagnostics report contract, safe/raw query entrypoints, and requirement-mapped determinism/diagnostics tests.
</objective>

<execution_context>
@/home/chemtech/.codex/get-shit-done/workflows/execute-plan.md
@/home/chemtech/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-contracts-and-typed-foundations/01-CONTEXT.md
@.planning/phases/01-contracts-and-typed-foundations/01-RESEARCH.md
@.planning/phases/01-contracts-and-typed-foundations/01-01-SUMMARY.md
@Cargo.toml
@src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build shared validator outputs and deterministic workspace estimator</name>
  <files>src/runtime/mod.rs, src/runtime/validator.rs, src/runtime/workspace_query.rs, src/lib.rs</files>
  <action>Implement pure validation and shape-normalization functions that produce canonical ValidatedInputs and ValidatedShape, then compute WorkspaceQuery deterministically from validated inputs and options. Preserve deterministic ordering and avoid mutable global state so identical input always yields identical bytes/alignment/shape metadata.</action>
  <verify>
    <automated>cargo test --workspace --test phase1_workspace_query deterministic_query_workspace</automated>
  </verify>
  <done>Repeated identical queries return identical WorkspaceQuery values in safe and raw-compatible paths.</done>
</task>

<task type="auto">
  <name>Task 2: Define structured diagnostics contract and tracing metadata</name>
  <files>src/diagnostics/mod.rs, src/diagnostics/report.rs, src/runtime/validator.rs, src/runtime/workspace_query.rs</files>
  <action>Create a diagnostics payload type that captures MEM-03 fields (`api`, `representation`, `shell_tuple`, `dims`, `required_bytes`, `provided_bytes`, `memory_limit_bytes`, `backend_candidate`, `feature_flags`) and attach it at validation/query failure boundaries. Emit consistent tracing events keyed to the same identifiers for correlation.</action>
  <verify>
    <automated>cargo test --workspace --test phase1_diagnostics_contract diagnostics_fields_complete</automated>
  </verify>
  <done>All query-path failures carry structured diagnostics with required fields and trace-correlatable identifiers.</done>
</task>

<task type="auto">
  <name>Task 3: Wire safe/raw query APIs and add contract tests</name>
  <files>src/api/safe.rs, src/api/raw.rs, src/lib.rs, tests/phase1_workspace_query.rs, tests/phase1_diagnostics_contract.rs</files>
  <action>Expose `query_workspace` entrypoints for safe and raw compatibility APIs using the shared validator and estimator. Ensure safe API hides raw dims, raw API validates dims overrides deterministically, and failures return typed errors plus diagnostics payloads. Add tests for determinism, invalid dims rejection, and diagnostics completeness.</action>
  <verify>
    <automated>cargo test --workspace --test phase1_workspace_query --test phase1_diagnostics_contract</automated>
  </verify>
  <done>Workspace query APIs are stable, deterministic, and produce typed, diagnosable failures for invalid layout/memory scenarios.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo test --workspace --test phase1_workspace_query
- [ ] cargo test --workspace --test phase1_diagnostics_contract
- [ ] cargo test --workspace --lib --quiet
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- SAFE-02 deterministic query_workspace contract is implemented and tested
- MEM-03 diagnostics payload includes required context fields for query-path failures
</success_criteria>

<output>
After completion, create `.planning/phases/01-contracts-and-typed-foundations/01-02-SUMMARY.md`
</output>
