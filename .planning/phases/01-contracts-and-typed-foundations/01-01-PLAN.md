---
phase: 01-contracts-and-typed-foundations
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib.rs
  - src/contracts/mod.rs
  - src/contracts/atom.rs
  - src/contracts/shell.rs
  - src/contracts/basis.rs
  - src/contracts/operator.rs
  - src/contracts/representation.rs
  - src/errors/mod.rs
  - src/errors/libcint_error.rs
  - tests/phase1_typed_contracts.rs
  - tests/phase1_error_taxonomy.rs
autonomous: true
requirements:
  - SAFE-01
  - SAFE-04
must_haves:
  truths:
    - Safe API callers construct Atom/Shell/Basis inputs through typed structs with invariant-checking constructors.
    - Public failures are returned as LibcintRsError variants (unsupported API, input/layout, memory, backend) instead of anyhow::Error.
    - Invalid typed inputs consistently map to stable error variants for downstream matching and contract tests.
  artifacts:
    - src/contracts/atom.rs defines Atom with validated constructor(s) and owned coordinate fields.
    - src/contracts/shell.rs defines Shell and contraction invariants used by safe API contracts.
    - src/errors/libcint_error.rs defines the public LibcintRsError enum with SAFE-04 categories.
    - tests/phase1_typed_contracts.rs and tests/phase1_error_taxonomy.rs lock typed constructor and error-category behavior.
  key_links:
    - src/contracts/* constructor/builders return Result<_, LibcintRsError> from src/errors/libcint_error.rs.
    - src/lib.rs re-exports contract modules and LibcintRsError as the stable public typed surface.
    - tests/phase1_error_taxonomy.rs asserts concrete LibcintRsError variant matching for representative invalid inputs.
---

<objective>
Establish the typed domain contract foundation and public typed error taxonomy required before any execution logic.

Purpose: Freeze SAFE-01 and SAFE-04 interfaces early so downstream workspace and execution plans consume stable types and error categories.
Output: Typed contract modules, a public LibcintRsError taxonomy, and requirement-mapped tests proving constructor invariants and error categorization.
</objective>

<execution_context>
@/home/chemtech/.codex/get-shit-done/workflows/execute-plan.md
@/home/chemtech/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-contracts-and-typed-foundations/01-CONTEXT.md
@.planning/phases/01-contracts-and-typed-foundations/01-RESEARCH.md
@Cargo.toml
@src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce typed contract modules for atoms, shells, basis, operators, and representations</name>
  <files>src/lib.rs, src/contracts/mod.rs, src/contracts/atom.rs, src/contracts/shell.rs, src/contracts/basis.rs, src/contracts/operator.rs, src/contracts/representation.rs</files>
  <action>Convert the crate to expose a library surface and implement typed domain models with explicit constructors/builders that enforce invariants (non-empty basis data, shell coefficient/exponent consistency, valid shell metadata). Keep safe-surface APIs pointer-free and avoid exposing raw layout arithmetic in public types.</action>
  <verify>
    <automated>cargo test --workspace --test phase1_typed_contracts typed_model_construction</automated>
  </verify>
  <done>Atom/Shell/Basis/Operator/Representation contracts compile, are re-exported via src/lib.rs, and invalid constructions fail through typed Result returns.</done>
</task>

<task type="auto">
  <name>Task 2: Define and wire the public typed error taxonomy</name>
  <files>src/errors/mod.rs, src/errors/libcint_error.rs, src/contracts/mod.rs, src/lib.rs</files>
  <action>Create LibcintRsError with stable SAFE-04 categories: unsupported API/representation, input/layout validation, dims/buffer mismatch, memory-limit/allocation failures, and backend failures. Ensure contract constructors and validators return these variants directly and do not leak anyhow into the public surface.</action>
  <verify>
    <automated>cargo test --workspace --test phase1_error_taxonomy typed_error_categories</automated>
  </verify>
  <done>All public contract fallible paths return LibcintRsError, and tests can pattern-match concrete variants without string parsing.</done>
</task>

<task type="auto">
  <name>Task 3: Add requirement-mapped contract and error tests</name>
  <files>tests/phase1_typed_contracts.rs, tests/phase1_error_taxonomy.rs</files>
  <action>Implement tests covering SAFE-01 constructor invariants and SAFE-04 variant differentiation. Include positive typed construction cases and representative invalid-input cases that must map to stable, explicit error variants.</action>
  <verify>
    <automated>cargo test --workspace --test phase1_typed_contracts --test phase1_error_taxonomy</automated>
  </verify>
  <done>Tests verify typed construction works for valid input and each invalid category maps to the expected LibcintRsError variant.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo test --workspace --test phase1_typed_contracts
- [ ] cargo test --workspace --test phase1_error_taxonomy
- [ ] cargo test --workspace --lib --quiet
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- SAFE-01 typed contract surface is stable and re-exported for downstream plans
- SAFE-04 error taxonomy is public, typed, and matchable by downstream callers/tests
</success_criteria>

<output>
After completion, create `.planning/phases/01-contracts-and-typed-foundations/01-01-SUMMARY.md`
</output>
