---
phase: 12-real-spinor-transform-c2spinor-replacement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cintx-cubecl/src/transform/c2spinor_coeffs.rs
  - crates/cintx-cubecl/src/transform/c2spinor.rs
  - crates/cintx-cubecl/src/transform/mod.rs
  - crates/cintx-compat/src/transform.rs
autonomous: true
requirements: [SPIN-01, SPIN-02, SPIN-04]

must_haves:
  truths:
    - "CG coefficient tables for l=0..4 contain values extracted from libcint cart2sph.c g_trans_cart2jR/g_trans_cart2jI arrays"
    - "cart_to_spinor_sf produces correct alpha/beta spinor output for a known d-shell (l=2) cartesian input"
    - "iket variants apply (re,im)->(-im,re) conjugation correctly"
    - "si variants include vx/vy/vz Pauli coupling terms in accumulation"
    - "kappa < 0 selects gt block (j=l+1/2), kappa > 0 selects lt block (j=l-1/2), kappa = 0 uses both"
    - "All four compat entry points delegate to the real transform with l and kappa passed through"
  artifacts:
    - path: "crates/cintx-cubecl/src/transform/c2spinor_coeffs.rs"
      provides: "CG coupling coefficient tables for l=0..4 (gt/lt x R/I)"
      contains: "CJ_GT_L0_R"
    - path: "crates/cintx-cubecl/src/transform/c2spinor.rs"
      provides: "Four transform functions: cart_to_spinor_sf, cart_to_spinor_iket_sf, cart_to_spinor_si, cart_to_spinor_iket_si"
      exports: ["cart_to_spinor_sf", "cart_to_spinor_iket_sf"]
    - path: "crates/cintx-compat/src/transform.rs"
      provides: "Compat entry points delegating to real c2spinor transforms"
      contains: "c2spinor::cart_to_spinor_sf"
  key_links:
    - from: "crates/cintx-cubecl/src/transform/c2spinor.rs"
      to: "crates/cintx-cubecl/src/transform/c2spinor_coeffs.rs"
      via: "use super::c2spinor_coeffs::"
      pattern: "c2spinor_coeffs::CJ_GT"
    - from: "crates/cintx-compat/src/transform.rs"
      to: "crates/cintx-cubecl/src/transform/c2spinor.rs"
      via: "c2spinor:: function calls"
      pattern: "c2spinor::cart_to_spinor"
    - from: "crates/cintx-cubecl/src/transform/mod.rs"
      to: "crates/cintx-cubecl/src/transform/c2spinor.rs"
      via: "apply_representation_transform Spinor arm"
      pattern: "c2spinor::"
---

<objective>
Extract Clebsch-Gordan coupling coefficient tables from vendored libcint cart2sph.c and implement all four CINTc2s_*spinor* transform variants with correct kappa dispatch. Delete the amplitude-averaging stub and its tests. Wire the compat entry points to the real transforms.

Purpose: This is the foundational transform work that all spinor oracle parity depends on. Without correct CG coefficients and transform logic, no spinor output can match libcint.

Output: Working c2spinor_coeffs.rs (coefficient tables), rewritten c2spinor.rs (four transform variants), updated compat transform.rs (real delegation), unit tests with value-correctness.
</objective>

<execution_context>
@/home/chemtech/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/chemtech/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-real-spinor-transform-c2spinor-replacement/12-CONTEXT.md
@.planning/phases/12-real-spinor-transform-c2spinor-replacement/12-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From crates/cintx-cubecl/src/transform/c2s.rs:
```rust
pub fn ncart(l: u8) -> usize { ((l as usize + 1) * (l as usize + 2)) / 2 }
pub fn nsph(l: u8) -> usize { 2 * l as usize + 1 }
// Coefficient layout: C2S_L{n}: [[f64; ncart]; nsph]
pub const C2S_L0: [[f64; 1]; 1] = [[1.0]];
// ... through C2S_L4
pub fn cart_to_spheric_staging(staging: &mut [f64]) -> Result<(), cintxRsError>;
```

From crates/cintx-core/src/shell.rs:
```rust
fn spinor_len(l: usize, kappa: i16) -> usize {
    match kappa {
        0 => 4 * l + 2,
        neg if neg < 0 => 2 * l + 2,
        _ => 2 * l,
    }
}
```

From crates/cintx-cubecl/src/transform/mod.rs:
```rust
pub mod c2s;
pub mod c2spinor;
pub fn apply_representation_transform(representation: Representation, staging: &mut [f64]) -> Result<(), cintxRsError>;
```

From crates/cintx-compat/src/transform.rs:
```rust
pub fn CINTc2s_ket_spinor_sf1(gsp: &mut [f64], gcart: &[f64], _lds: i32, _ldc: i32, _nctr: i32, _l: i32, _kappa: i32) -> Result<(), cintxRsError>;
pub fn CINTc2s_iket_spinor_sf1(...) -> Result<(), cintxRsError>;
pub fn CINTc2s_ket_spinor_si1(...) -> Result<(), cintxRsError>;
pub fn CINTc2s_iket_spinor_si1(...) -> Result<(), cintxRsError>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create c2spinor_coeffs.rs with CG coefficient tables and implement c2spinor.rs transforms</name>
  <files>
    crates/cintx-cubecl/src/transform/c2spinor_coeffs.rs
    crates/cintx-cubecl/src/transform/c2spinor.rs
    crates/cintx-cubecl/src/transform/mod.rs
  </files>
  <read_first>
    crates/cintx-cubecl/src/transform/c2s.rs
    crates/cintx-cubecl/src/transform/c2spinor.rs
    crates/cintx-cubecl/src/transform/mod.rs
    crates/cintx-core/src/shell.rs
    libcint-master/src/cart2sph.c
  </read_first>
  <behavior>
    - Test: CJ_GT_L0_R has shape [[f64; 2]; 2] (2 spinor rows x 2*ncart(0)=2 entries) and first row matches libcint g_trans_cart2jR[0..2]
    - Test: CJ_GT_L2_R has shape [[f64; 12]; 6] (6 spinor rows for l=2 gt, 2*6=12 entries per row)
    - Test: cart_to_spinor_sf with l=0, kappa=-1, known s-shell cart=[1.0] produces correct 2-component spinor (nd=2, total 8 f64: 4 alpha re/im + 4 beta re/im)
    - Test: cart_to_spinor_sf with l=1, kappa=-1, known p-shell cart=[1.0, 0.0, 0.0] produces correct 4-component spinor (nd=4)
    - Test: cart_to_spinor_iket_sf applies (re,im)->(-im,re) conjugation — same input as sf test but output re/im swapped with sign flip
    - Test: kappa=0 with l=1 produces nd=6 (4*1+2) components using both gt and lt blocks
    - Test: kappa=+1 with l=1 produces nd=2 (2*1) components using lt block only
    - Test: cart_to_spinor_si includes vx/vy/vz Pauli coupling terms — output differs from sf for non-s-shell input with non-zero vx/vy/vz
    - Test: old amplitude-averaging stub tests are deleted
  </behavior>
  <action>
    **Step 1: Create c2spinor_coeffs.rs** (per D-02)

    Read libcint-master/src/cart2sph.c and extract `g_trans_cart2jR[]` and `g_trans_cart2jI[]` arrays at the documented offsets from the `g_c2s[]` struct (line ~3561).

    For each l=0..4, extract four arrays:
    - `CJ_GT_L{l}_R`: Real part of j=l+1/2 (kappa<0) block. Shape: `[[f64; 2*ncart(l)]; 2*l+2]`
    - `CJ_GT_L{l}_I`: Imaginary part of j=l+1/2 block. Same shape.
    - `CJ_LT_L{l}_R`: Real part of j=l-1/2 (kappa>0) block. Shape: `[[f64; 2*ncart(l)]; 2*l]` (empty for l=0)
    - `CJ_LT_L{l}_I`: Imaginary part of j=l-1/2 block. Same shape.

    Row layout per upstream: `row[0..nf]` = alpha half coefficients for cart components, `row[nf..2*nf]` = beta half coefficients for cart components, where nf = ncart(l).

    Also provide lookup arrays:
    ```rust
    pub const CJ_GT_R: [&[&[f64]]; 5] = [&CJ_GT_L0_R, &CJ_GT_L1_R, ...];
    // etc for CJ_GT_I, CJ_LT_R, CJ_LT_I
    ```

    Add `pub mod c2spinor_coeffs;` to mod.rs.

    **Step 2: Rewrite c2spinor.rs** (per D-03, D-07)

    Delete the entire existing amplitude-averaging stub and its tests. Replace with:

    Import `c2s::ncart` and coefficient tables from `c2spinor_coeffs`.

    Implement `pub fn spinor_len(l: u8, kappa: i32) -> usize` mirroring cintx-core/src/shell.rs logic:
    - kappa < 0: 2*l+2
    - kappa > 0: 2*l
    - kappa == 0: 4*l+2

    Implement `fn select_cg_block(l: u8, kappa: i32) -> CgBlock` that returns coefficient references for the appropriate block(s).

    Implement four public transform functions per D-03 (distinct code paths, no shared-core-with-flags):

    1. `pub fn cart_to_spinor_sf(gsp: &mut [f64], cart: &[f64], l: u8, kappa: i32) -> Result<(), cintxRsError>`
       - Lines 6741-6788 of cart2sph.c: `gspaz_re += caR * v1`, `gspaz_im += caI * v1` (positive imaginary)
       - Alpha output at gsp[i*2] (re), gsp[i*2+1] (im) for i in 0..nd
       - Beta output at gsp[(nd+i)*2] (re), gsp[(nd+i)*2+1] (im)

    2. `pub fn cart_to_spinor_iket_sf(gsp: &mut [f64], cart: &[f64], l: u8, kappa: i32) -> Result<(), cintxRsError>`
       - Lines 6790-6837: iket = multiply by i: `gspaz_re -= caI * v1`, `gspaz_im += caR * v1`

    3. `pub fn cart_to_spinor_si(gsp: &mut [f64], cart_v1: &[f64], cart_vx: &[f64], cart_vy: &[f64], cart_vz: &[f64], l: u8, kappa: i32) -> Result<(), cintxRsError>`
       - Lines 6839-6897: alpha: `sa_re += caR*v1 - caI*vz + cbR*vy - cbI*vx`, `sa_im += caI*v1 + caR*vz + cbI*vy + cbR*vx`
       - Beta: `sb_re += cbR*v1 + cbI*vz - caR*vy - caI*vx`, `sb_im += cbI*v1 - cbR*vz - caI*vy + caR*vx`

    4. `pub fn cart_to_spinor_iket_si(gsp: &mut [f64], cart_v1: &[f64], cart_vx: &[f64], cart_vy: &[f64], cart_vz: &[f64], l: u8, kappa: i32) -> Result<(), cintxRsError>`
       - Lines 6899-6957: same Pauli terms as si but output multiplied by i: `(re,im)->(-im,re)`

    For kappa == 0: apply gt block first (writing nd_gt components), then lt block (writing nd_lt components after gt). Total nd = nd_gt + nd_lt = 4l+2.

    Keep the public `cart_to_spinor_interleaved_staging` function but rewrite it to call the appropriate variant based on parameters. Since `apply_representation_transform` currently passes only `staging`, the signature needs updating in mod.rs to also pass `l` and `kappa`. If the current executor does not pass these, add TODO comments noting the executor wiring will be completed when oracle tests exercise the full path.

    **Step 3: Update mod.rs**

    Add `pub mod c2spinor_coeffs;` to the module declarations. Update the Spinor arm of `apply_representation_transform` if the signature can be extended, or document the gap.

    **Unit tests in c2spinor.rs:**
    - Delete the two old stub tests (spinor_transform_preserves_interleaved_pairs, spinor_transform_rejects_non_interleaved_length)
    - Add value-correctness tests for each of the four variants using l=0 (s-shell) and l=1 (p-shell) with known cart inputs and CG-derived expected outputs
    - Add kappa dispatch tests: kappa=-1 (gt), kappa=+1 (lt), kappa=0 (both)
  </action>
  <verify>
    <automated>cargo test --package cintx-cubecl --lib transform::c2spinor -- --nocapture && cargo test --package cintx-cubecl --lib transform::c2spinor_coeffs -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - c2spinor_coeffs.rs contains `pub const CJ_GT_L0_R` through `pub const CJ_GT_L4_R` and corresponding `_I`, `CJ_LT_L*_R`, `CJ_LT_L*_I` arrays
    - c2spinor_coeffs.rs contains `pub const CJ_GT_L2_R: [[f64; 12]; 6]` (exact shape for l=2 gt: 6 rows x 12 entries)
    - c2spinor.rs contains `pub fn cart_to_spinor_sf`, `pub fn cart_to_spinor_iket_sf`, `pub fn cart_to_spinor_si`, `pub fn cart_to_spinor_iket_si`
    - c2spinor.rs contains `pub fn spinor_len(l: u8, kappa: i32) -> usize`
    - c2spinor.rs does NOT contain `(pair[0].abs() + pair[1].abs()) * 0.5` (amplitude-averaging stub deleted)
    - mod.rs contains `pub mod c2spinor_coeffs;`
    - `cargo test --package cintx-cubecl --lib transform::c2spinor` passes with 0 failures
    - `cargo test --package cintx-cubecl --lib transform::c2spinor_coeffs` passes with 0 failures
  </acceptance_criteria>
  <done>Four CG-correct spinor transform functions implemented with value-correctness unit tests passing. Amplitude-averaging stub fully removed. kappa dispatch working for all three branches (kappa < 0, > 0, == 0).</done>
</task>

<task type="auto">
  <name>Task 2: Rewire compat spinor entry points to real transforms</name>
  <files>
    crates/cintx-compat/src/transform.rs
  </files>
  <read_first>
    crates/cintx-compat/src/transform.rs
    crates/cintx-cubecl/src/transform/c2spinor.rs
  </read_first>
  <action>
    Rewrite the four spinor compat entry points in `crates/cintx-compat/src/transform.rs` to delegate to the real c2spinor transform functions instead of the deleted stub.

    Replace the `spinor_transform` helper and all four spinor functions:

    1. `CINTc2s_ket_spinor_sf1(gsp, gcart, lds, ldc, nctr, l, kappa)` ->
       Call `c2spinor::cart_to_spinor_sf(gsp, gcart, l as u8, kappa)`.
       The `lds`/`ldc`/`nctr` parameters are used by libcint for strided access across contractions; for the initial implementation, validate that gcart length matches ncart(l) * nctr and iterate contractions if nctr > 1.

    2. `CINTc2s_iket_spinor_sf1(...)` -> Call `c2spinor::cart_to_spinor_iket_sf(gsp, gcart, l as u8, kappa)`

    3. `CINTc2s_ket_spinor_si1(...)` -> Call `c2spinor::cart_to_spinor_si(gsp, cart_v1, cart_vx, cart_vy, cart_vz, l as u8, kappa)`.
       Note: The si variants need four Pauli-coupled components (v1, vx, vy, vz). If the current compat signature only passes `gcart` as a single slice, the si variants should split gcart into four equal segments: `gcart[0..nf]` = v1, `gcart[nf..2*nf]` = vx, `gcart[2*nf..3*nf]` = vy, `gcart[3*nf..4*nf]` = vz where nf = ncart(l). This matches libcint's four-component packing for spin-included integrals.

    4. `CINTc2s_iket_spinor_si1(...)` -> Call `c2spinor::cart_to_spinor_iket_si(gsp, cart_v1, cart_vx, cart_vy, cart_vz, l as u8, kappa)`

    Delete the old `spinor_transform` helper function and the old test `spinor_transform_entry_points_keep_interleaved_layout`.

    Add new tests:
    - `compat_ket_spinor_sf1_delegates_correctly`: Call CINTc2s_ket_spinor_sf1 with l=0, kappa=-1, known cart input, verify output matches cart_to_spinor_sf direct call
    - `compat_all_four_variants_produce_different_output`: Call all four variants with l=1, kappa=-1, same p-shell cart input, verify that at minimum ket vs iket differ in sign pattern and sf vs si differ when Pauli components are non-zero
  </action>
  <verify>
    <automated>cargo test --package cintx-compat --lib transform -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - transform.rs contains `c2spinor::cart_to_spinor_sf` (real delegation, not stub)
    - transform.rs contains `c2spinor::cart_to_spinor_iket_sf`
    - transform.rs contains `c2spinor::cart_to_spinor_si`
    - transform.rs contains `c2spinor::cart_to_spinor_iket_si`
    - transform.rs does NOT contain `cart_to_spinor_interleaved_staging` in the spinor function bodies (old stub call removed)
    - `cargo test --package cintx-compat --lib transform` passes with 0 failures
    - `cargo check --package cintx-compat` compiles without errors
  </acceptance_criteria>
  <done>All four compat spinor entry points delegate to the real CG-based transform functions. l and kappa parameters are passed through correctly. Old stub delegation removed.</done>
</task>

</tasks>

<verification>
- `cargo test --package cintx-cubecl --lib transform` passes (all c2spinor and c2spinor_coeffs tests)
- `cargo test --package cintx-compat --lib transform` passes (compat delegation tests)
- `cargo check --workspace` compiles without errors
- No amplitude-averaging code remains: `grep -r "amplitude\|0\.5.*abs" crates/cintx-cubecl/src/transform/c2spinor.rs` returns nothing
</verification>

<success_criteria>
- CG coefficient tables for l=0..4 extracted from libcint and verified in unit tests
- All four transform variants (sf, iket_sf, si, iket_si) implemented with distinct code paths per D-03
- kappa dispatch works for all three branches (< 0, > 0, == 0) per D-07
- Compat entry points delegate to real transforms per D-03
- Amplitude-averaging stub fully deleted per D-08
- All unit tests pass with value-correctness checks (not just buffer-length checks)
</success_criteria>

<output>
After completion, create `.planning/phases/12-real-spinor-transform-c2spinor-replacement/12-01-SUMMARY.md`
</output>
