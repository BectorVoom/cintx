---
phase: 01-manifest-planner-foundation
plan: 02
task: 2
total_tasks: 2
status: blocked
last_updated: 2026-03-21T06:53:29.620Z
---

<current_state>
Paused in Phase 01 / Plan 02 after implementing the runtime validator/planner/workspace scaffolding but before any Plan 02 commit. The Rust verification gate is green locally (`cargo test --workspace`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo fmt --all -- --check`), but the mandatory Rust review returned BLOCK on three correctness issues that must be fixed before commit.
</current_state>

<completed_work>

- Task 1: Define typed runtime errors and execution options - Done locally. `cintxRsError` was added in `cintx-core`, re-exported from `crates/cintx-core/src/lib.rs`, and `ExecutionOptions` now exists in `crates/cintx-runtime/src/options.rs`.
- Task 2: Implement validator/planner/workspace contract - In progress. Added `ValidatedShellTuple`, `WorkspaceQuery`, `ChunkPlanner`, `WorkspaceAllocator`, `ExecutionPlan`, `query_workspace`, and `evaluate`, plus runtime tests and workspace membership updates so the runtime crate is covered by workspace verification.
- Verification run completed locally - `cargo test -p cintx-runtime --lib`, `cargo test --workspace`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, and `cargo fmt --all -- --check` all passed before the pause.

</completed_work>

<remaining_work>

- Fix reviewer finding 1 in `crates/cintx-runtime/src/workspace.rs`: `chunk_size_override` must never produce a chunk whose actual required bytes exceed the memory cap.
- Fix reviewer finding 2 in `crates/cintx-runtime/src/planner.rs`: `evaluate` must not silently diverge from the `query_workspace` contract when planning options change.
- Fix reviewer finding 3 in `crates/cintx-runtime/src/validator.rs` and `crates/cintx-core/src/error.rs`: bad shell atom indices need a dedicated typed validation error, not `ChunkPlanFailed`.
- Re-run the full Rust gate after the fixes.
- Write `.planning/phases/01-manifest-planner-foundation/02-PLAN-SUMMARY.md` and commit the code/docs for Plan 02.

</remaining_work>

<decisions_made>

- Added `crates/cintx-runtime` to the root workspace/default-members so Phase 01 runtime verification can run from the repo root.
- Kept typed runtime errors in `cintx-core` so validator/planner/workspace code all share the same public error contract.
- Used manifest-driven resolver metadata as the source of planner arity/representation truth instead of raw symbol names.

</decisions_made>

<blockers>
- Rust review BLOCK: `chunk_size_override` can under-allocate under a memory cap, `evaluate` can drift from the queried workspace contract if planning options differ, and invalid shell atom indices are currently downgraded into `ChunkPlanFailed`.
- Repo noise: there are many unrelated pre-existing deletions/untracked files outside the current Phase 01 scope. They are not part of this work and should not be reverted.
</blockers>

<context>
Wave 1 is already committed and clean, including the generated-manifest formatting fix (`e43316d`). Plan 02 is entirely local at this point. The current code shape is close; the remaining work is focused and should stay inside `crates/cintx-runtime/src/{workspace,planner,validator}.rs`, `crates/cintx-core/src/error.rs`, plus any resulting `Cargo.lock` updates. No servers, watchers, or background processes are running.
</context>

<next_action>
Start with the reviewer blockers in this order: (1) fix chunk-fit calculation in `crates/cintx-runtime/src/workspace.rs`, (2) make `evaluate` honor or validate the queried workspace contract in `crates/cintx-runtime/src/planner.rs`, (3) add a dedicated typed invalid-shell-index error in `crates/cintx-core/src/error.rs` and route `crates/cintx-runtime/src/validator.rs` to it, then rerun the full Rust verification gate.
</next_action>
