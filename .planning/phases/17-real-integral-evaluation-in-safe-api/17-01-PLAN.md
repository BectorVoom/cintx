---
phase: 17
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - crates/cintx-oracle/Cargo.toml
autonomous: true
requirements:
  - RVAL-02
tags: [cargo, dependency, wave-0, cintx-oracle, cintx-rs]

must_haves:
  truths:
    - "`crates/cintx-oracle/Cargo.toml` declares a `cintx-rs` path-dependency under `[dependencies]` with `default-features = false`."
    - "`cargo build -p cintx-oracle --locked` (no extra features) succeeds with the new edge."
    - "`cargo build -p cintx-oracle --features cpu --locked` succeeds; the cpu feature chain (`cintx-oracle/cpu` → `cintx-compat/cpu` → `cintx-cubecl/cpu`) is unchanged."
    - "`cargo build -p cintx-oracle --features rocm --locked` succeeds when rocm-capable; the rocm feature chain is unchanged."
  artifacts:
    - path: "crates/cintx-oracle/Cargo.toml"
      provides: "cintx-rs path-dep so safe-API parity tests can compile against `cintx_rs::SessionRequest`"
      contains: "cintx-rs = { path = \"../cintx-rs\""
  key_links:
    - from: "crates/cintx-oracle/Cargo.toml"
      to: "crates/cintx-rs/Cargo.toml"
      via: "Cargo workspace path dependency"
      pattern: "cintx-rs.*path.*\\.\\./cintx-rs"
---

<objective>
Add a single Cargo edge so the new `safe_api_arity2_parity.rs` test file (added in Plan 03) can call `cintx_rs::SessionRequest::evaluate`. This is Wave 0 — every later Wave-1 plan in this phase compiles against this edge.

Purpose: RVAL-02 requires the new oracle parity test file to import `cintx_rs::SessionRequest` and related types. The existing `crates/cintx-oracle/Cargo.toml` declares only `cintx-compat`, `cintx-core`, `cintx-ops` — there is no edge to `cintx-rs` today (verified in 17-RESEARCH.md `Standard Stack` and `Common Pitfalls §1`).

Output: One modified Cargo.toml; no source changes; no Cargo.lock surgery.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/17-real-integral-evaluation-in-safe-api/17-CONTEXT.md
@.planning/phases/17-real-integral-evaluation-in-safe-api/17-RESEARCH.md
@.planning/phases/17-real-integral-evaluation-in-safe-api/17-PATTERNS.md
@crates/cintx-oracle/Cargo.toml
@crates/cintx-rs/Cargo.toml

<interfaces>
<!-- Path-dep declaration pattern (analog) — from crates/cintx-rs/Cargo.toml line 12 -->
```toml
cintx-compat = { path = "../cintx-compat", default-features = false }
```

<!-- Existing dependency block in cintx-oracle (verified read 2026-05-12) -->
```toml
# crates/cintx-oracle/Cargo.toml lines 23-28
[dependencies]
anyhow = "1.0.102"
cintx-compat = { path = "../cintx-compat" }
cintx-core = { path = "../cintx-core" }
cintx-ops = { path = "../cintx-ops" }
serde_json = "1.0.145"
```

<!-- Existing feature block — DO NOT change; cpu/rocm/with-* feature chains already reach cubecl through cintx-compat -->
```toml
[features]
default = []
cpu = ["cintx-compat/cpu"]
with-4c1e = ["cintx-compat/with-4c1e"]
with-f12 = ["cintx-compat/with-f12"]
unstable-source-api = ["cintx-compat/unstable-source-api"]
rocm = ["cintx-compat/rocm"]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cintx-rs path-dep to cintx-oracle [Cargo.toml]</name>
  <files>crates/cintx-oracle/Cargo.toml</files>
  <read_first>
    - crates/cintx-oracle/Cargo.toml (the file being modified; see its existing `[dependencies]` block before editing)
    - crates/cintx-rs/Cargo.toml (analog source — the `default-features = false` pattern on line 12 is the one to mirror)
    - .planning/phases/17-real-integral-evaluation-in-safe-api/17-CONTEXT.md (D-11: no public API change; the new dep does not modify cintx-rs surface)
    - .planning/phases/17-real-integral-evaluation-in-safe-api/17-RESEARCH.md (Standard Stack §New dependency to add; Common Pitfalls §1 and §2)
    - .planning/phases/17-real-integral-evaluation-in-safe-api/17-PATTERNS.md (final section: `crates/cintx-oracle/Cargo.toml (add cintx-rs dependency)`)
  </read_first>
  <action>
    In `crates/cintx-oracle/Cargo.toml`, under the existing `[dependencies]` table (currently containing `anyhow`, `cintx-compat`, `cintx-core`, `cintx-ops`, `serde_json`), add exactly one new line in alphabetical order with the other `cintx-*` path-deps (between `cintx-ops` and `serde_json`):

    ```toml
    cintx-rs = { path = "../cintx-rs", default-features = false }
    ```

    Rationale (per 17-PATTERNS.md `path-dep with default-features = false`):
    - `default-features = false` mirrors `cintx-rs/Cargo.toml` line 12 where `cintx-compat` uses the same flag — keeps the safe facade boundary lean and avoids forwarding cintx-rs default features (it has none today, but this preserves invariance under future feature additions).
    - The cpu/rocm backends are activated transitively through the existing `cpu = ["cintx-compat/cpu"]` chain — `cintx-rs` does NOT need its own `cpu` feature because it already depends on `cintx-cubecl` directly (`cintx-rs/Cargo.toml` line 14). Per 17-RESEARCH.md `Common Pitfalls §2`, the existing feature forwarding is correct.

    DO NOT add any new feature entries to `[features]`. DO NOT modify `[build-dependencies]`. DO NOT modify any other table.

    Per 17-CONTEXT.md D-12: this dep gives the new oracle test file (added in Plan 03) access to `cintx_rs::SessionRequest`, `SessionQuery`, `TypedEvaluationOutput`, and `IntegralTensor` — none of which change in this phase (RVAL-03 / D-11).
  </action>
  <verify>
    <automated>cargo build -p cintx-oracle --locked &amp;&amp; cargo build -p cintx-oracle --features cpu --locked &amp;&amp; grep -E '^cintx-rs = \{ path = "\.\./cintx-rs", default-features = false \}$' crates/cintx-oracle/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '^cintx-rs = \{ path = "\.\./cintx-rs", default-features = false \}$' crates/cintx-oracle/Cargo.toml` matches exactly one line.
    - `grep -c '^cintx-' crates/cintx-oracle/Cargo.toml` returns a count of at least 4 (cintx-compat, cintx-core, cintx-ops, cintx-rs — the four path-deps that are required under `[dependencies]`).
    - `cargo build -p cintx-oracle --locked` exits 0.
    - `cargo build -p cintx-oracle --features cpu --locked` exits 0.
    - `cargo build -p cintx-oracle --features rocm --locked` exits 0 on rocm-capable hosts (skip gracefully if non-rocm dev host; verify via `cargo metadata` that the `rocm` feature still lists `cintx-compat/rocm` only — i.e. no new feature edge was introduced).
    - The `[features]` table is byte-identical to the pre-edit version: `diff <(git show HEAD:crates/cintx-oracle/Cargo.toml | sed -n '/^\[features\]/,/^\[/p' | head -n -1) <(sed -n '/^\[features\]/,/^\[/p' crates/cintx-oracle/Cargo.toml | head -n -1)` produces no output.
    - `cargo metadata --format-version=1 --locked -p cintx-oracle | jq -r '.packages[] | select(.name=="cintx-oracle") | .dependencies[].name' | sort | grep -F cintx-rs` matches.
  </acceptance_criteria>
  <done>
    `cintx-oracle/Cargo.toml` lists `cintx-rs` as a path-dep with `default-features = false`. `cargo build -p cintx-oracle --locked` and `cargo build -p cintx-oracle --features cpu --locked` both succeed. No other table or feature edge changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan modifies a single workspace-internal Cargo.toml file. No new trust boundary is introduced. No new network/process/IPC boundary is crossed. The Cargo workspace is the boundary, and adding an intra-workspace path-dep does not cross it.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01-S | Spoofing | crates/cintx-oracle/Cargo.toml | accept | N/A — no auth/identity; path-dep is workspace-internal (`../cintx-rs`), not a registry source. |
| T-17-01-T | Tampering | crates/cintx-oracle/Cargo.toml | accept | N/A — no persisted state added; the file is version-controlled and reviewed via PR diff. |
| T-17-01-R | Repudiation | (none) | accept | N/A — Cargo.toml changes are auditable through git history. |
| T-17-01-I | Information Disclosure | (none) | accept | N/A — no secrets, env vars, or PII touched; the dep does not change crate visibility (cintx-rs is a public workspace crate already, but no new publishing surface is added — `cintx-oracle` is not published). |
| T-17-01-D | Denial of Service | (none) | accept | N/A — adding a path-dep cannot DoS; build-time cost is negligible (cintx-rs is already compiled by the workspace for cintx-rs's own tests). |
| T-17-01-E | Elevation of Privilege | (none) | accept | N/A — Cargo features stay scoped; the new dep uses `default-features = false` to avoid silently enabling any future cintx-rs default-on features. |
</threat_model>

<verification>
- `cargo build -p cintx-oracle --locked` exits 0.
- `cargo build -p cintx-oracle --features cpu --locked` exits 0.
- `grep -F 'cintx-rs = { path = "../cintx-rs", default-features = false }' crates/cintx-oracle/Cargo.toml` matches.
- `[features]` table is unchanged (diff vs HEAD produces no output for that table).
</verification>

<success_criteria>
- New dependency edge present in `crates/cintx-oracle/Cargo.toml`.
- All four `cintx-oracle` feature profiles (`""`, `cpu`, `with-4c1e`, `with-f12`) still build under `--locked`.
- No `[features]` table changes.
- No Cargo.lock entry-list churn beyond the one new `cintx-rs` graph edge (Cargo will re-resolve; this is expected).
</success_criteria>

<output>
After completion, create `.planning/phases/17-real-integral-evaluation-in-safe-api/17-01-SUMMARY.md` per the standard summary template.
</output>
