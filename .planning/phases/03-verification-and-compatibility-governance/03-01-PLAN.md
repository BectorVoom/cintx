---
phase: 03-verification-and-compatibility-governance
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/runtime/helpers.rs
  - src/runtime/mod.rs
  - src/lib.rs
  - tests/common/phase3_helper_cases.rs
  - tests/common/phase2_fixtures.rs
  - tests/phase3_helper_transform_parity.rs
autonomous: true
requirements:
  - COMP-02
  - VERI-03
must_haves:
  truths:
    - Helper/transform compatibility is proven against oracle-compatible expectations, not asserted by prose.
    - AO counts, shell offsets, normalization behavior, and cart/sph/spinor transform contracts are regression-protected.
    - Failure semantics for helper paths remain typed and diagnostics-rich, matching phase-1/2 contract style.
  artifacts:
    - src/runtime/helpers.rs exposes deterministic helper/transform functions used by test gates.
    - tests/common/phase3_helper_cases.rs and tests/common/phase2_fixtures.rs provide shared matrix fixtures for helper parity checks.
    - tests/phase3_helper_transform_parity.rs enforces COMP-02 parity and VERI-03 helper semantics.
  key_links:
    - tests/phase3_helper_transform_parity.rs consumes tests/common/phase3_helper_cases.rs and tests/common/oracle_runner.rs.
    - src/runtime/mod.rs and src/lib.rs re-export helper surfaces needed by compatibility tests and downstream plans.
---

<objective>
Implement the helper/transform parity foundation for Phase 3.

Purpose: Convert COMP-02 from design intent into executable parity evidence.
Output: helper/transform compatibility surface and regression tests for counts, offsets, normalization, and transforms.
</objective>

<execution_context>
@/home/chemtech/.codex/get-shit-done/workflows/execute-plan.md
@/home/chemtech/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-verification-and-compatibility-governance/03-CONTEXT.md
@.planning/phases/03-verification-and-compatibility-governance/03-RESEARCH.md
@.planning/phases/03-verification-and-compatibility-governance/03-VALIDATION.md
@tests/common/phase2_fixtures.rs
@tests/common/oracle_runner.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement deterministic helper parity surface for AO counts, offsets, and normalization</name>
  <files>src/runtime/helpers.rs, src/runtime/mod.rs, src/lib.rs</files>
  <action>Add a dedicated helper surface that computes compatibility-relevant counts, offsets, and normalization metadata with deterministic behavior and typed validation failures.</action>
  <verify>
    <automated>cargo test --workspace --test phase3_helper_transform_parity helper_counts_offsets_normalization_parity</automated>
  </verify>
  <done>Helper outputs needed for migration workflows are callable and deterministic across supported matrix inputs.</done>
</task>

<task type="auto">
  <name>Task 2: Add transform parity matrix tests for cart/sph/spinor helper behavior</name>
  <files>tests/common/phase3_helper_cases.rs, tests/common/phase2_fixtures.rs, tests/phase3_helper_transform_parity.rs</files>
  <action>Create matrix-driven tests validating helper transform expectations across phase-2-supported family/operator/representation envelopes, including explicit `3c1e` spinor coverage.</action>
  <verify>
    <automated>cargo test --workspace --test phase3_helper_transform_parity helper_transform_parity_matrix</automated>
  </verify>
  <done>COMP-02 parity matrix is executable and fails on helper/transform regressions.</done>
</task>

<task type="auto">
  <name>Task 3: Lock helper failure semantics and diagnostics contracts</name>
  <files>tests/phase3_helper_transform_parity.rs</files>
  <action>Add negative-path tests proving helper validation failures remain typed, deterministic, and diagnostics-complete for malformed inputs and out-of-envelope calls.</action>
  <verify>
    <automated>cargo test --workspace --test phase3_helper_transform_parity helper_failure_semantics_are_typed</automated>
  </verify>
  <done>VERI-03 helper failure contracts are regression-protected alongside parity success paths.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo test --workspace --test phase3_helper_transform_parity
- [ ] cargo test --workspace --lib --quiet
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- COMP-02 helper/transform parity evidence exists for AO counts, offsets, normalization, and representation transforms
- VERI-03 helper error and diagnostics semantics are explicitly regression-protected
</success_criteria>

<output>
After completion, create `.planning/phases/03-verification-and-compatibility-governance/03-01-SUMMARY.md`
</output>
