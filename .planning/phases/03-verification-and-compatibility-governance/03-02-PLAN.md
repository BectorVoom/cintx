---
phase: 03-verification-and-compatibility-governance
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/manifest/mod.rs
  - src/manifest/lock.rs
  - src/manifest/canonicalize.rs
  - src/lib.rs
  - tests/phase3_manifest_governance.rs
  - docs/phase3-manifest-governance.md
autonomous: true
requirements:
  - COMP-03
  - VERI-01
must_haves:
  truths:
    - API coverage claims are backed by a typed compiled-manifest lock and cannot rely on narrative-only assertions.
    - Manifest profile coverage is explicitly modeled as `{base, with-f12, with-4c1e, with-f12+with-4c1e}` with drift detection.
    - Unapproved lock drift is a blocking contract, not an advisory warning.
  artifacts:
    - src/manifest/* implements schema, canonicalization, and profile-aware lock validation utilities.
    - tests/phase3_manifest_governance.rs enforces schema invariants and lock drift behavior.
    - docs/phase3-manifest-governance.md documents manifest semantics and update policy.
  key_links:
    - src/manifest/canonicalize.rs is used by manifest lock generation and drift checks.
    - tests/phase3_manifest_governance.rs gates COMP-03/VERI-01 behavior used by CI plan 03-04.
---

<objective>
Build the manifest-governance contract for compatibility coverage claims.

Purpose: Turn COMP-03 and VERI-01 into enforceable artifact+test guarantees.
Output: typed lock schema, canonical symbol/profile handling, and blocking drift tests.
</objective>

<execution_context>
@/home/chemtech/.codex/get-shit-done/workflows/execute-plan.md
@/home/chemtech/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-verification-and-compatibility-governance/03-CONTEXT.md
@.planning/phases/03-verification-and-compatibility-governance/03-RESEARCH.md
@docs/libcint_detailed_design_resolved_en.md
@build.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define compiled-manifest lock schema with profile and stability metadata</name>
  <files>src/manifest/mod.rs, src/manifest/lock.rs, src/lib.rs</files>
  <action>Implement typed manifest lock structures that require canonical symbol identity, support profile membership, and stability classification for each entry.</action>
  <verify>
    <automated>cargo test --workspace --test phase3_manifest_governance manifest_schema_invariants</automated>
  </verify>
  <done>Manifest lock schema enforces the minimum fields required for auditable coverage claims.</done>
</task>

<task type="auto">
  <name>Task 2: Implement canonicalization and profile-union governance checks</name>
  <files>src/manifest/canonicalize.rs, src/manifest/lock.rs, tests/phase3_manifest_governance.rs</files>
  <action>Add canonicalization and profile-union checks that normalize symbol variants and ensure the lock reflects only approved profile combinations for this phase scope.</action>
  <verify>
    <automated>cargo test --workspace --test phase3_manifest_governance manifest_profile_union_is_stable</automated>
  </verify>
  <done>Profile-aware canonicalization contracts are executable and prevent ambiguous manifest entries.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce unapproved lock drift as a blocking regression</name>
  <files>tests/phase3_manifest_governance.rs, docs/phase3-manifest-governance.md</files>
  <action>Create drift-detection tests and documentation that codify explicit lock update policy (approved schema/upstream/profile changes only).</action>
  <verify>
    <automated>cargo test --workspace --test phase3_manifest_governance lock_drift_requires_explicit_update</automated>
  </verify>
  <done>VERI-01 lock drift behavior is test-proven and documented for CI gate integration.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo test --workspace --test phase3_manifest_governance
- [ ] cargo test --workspace --lib --quiet
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- COMP-03 coverage claims are represented by a typed compiled-manifest governance surface
- VERI-01 lock drift enforcement exists as an automated failing condition
</success_criteria>

<output>
After completion, create `.planning/phases/03-verification-and-compatibility-governance/03-02-SUMMARY.md`
</output>
