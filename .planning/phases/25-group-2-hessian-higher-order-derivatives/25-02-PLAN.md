---
phase: 25-group-2-hessian-higher-order-derivatives
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cintx-runtime/src/planner.rs
  - crates/cintx-cubecl/src/kernels/one_electron.rs
  - crates/cintx-cubecl/src/kernels/two_electron.rs
  - crates/cintx-cubecl/src/kernels/center_3c2e.rs
  - crates/cintx-cubecl/src/kernels/center_2c2e.rs
  - crates/cintx-cubecl/src/kernels/f12.rs
  - crates/cintx-cubecl/src/kernels/unstable/grids.rs
autonomous: true
requirements: [FND-06]

must_haves:
  truths:
    - "A single upfront BufferTooSmall-style assertion at the planner staging-allocation boundary proves the staging buffer is large enough before any scatter"
    - "All 19 per-element `if dst < staging.len()` scatter guards are removed across all kernel modules (scatter is unconditional once proven sized)"
    - "A rank-81 family under a sub-rank-81 memory limit produces a typed OOM/BufferTooSmall stop with NO partial write (output untouched)"
    - "The pre-existing oracle-cart-offset-vendor-zero lib-test failure is confirmed pre-existing and does not block the Phase-25 family gate"
  artifacts:
    - path: "crates/cintx-runtime/src/planner.rs"
      provides: "Single upfront staging-size assertion + rank-81 OOM no-partial-write test"
      contains: "BufferTooSmall"
  key_links:
    - from: "crates/cintx-runtime/src/planner.rs"
      to: "cintxRsError::BufferTooSmall"
      via: "upfront assertion at try_alloc_staging boundary"
      pattern: "BufferTooSmall"
    - from: "all kernel scatter loops"
      to: "proven-sized staging buffer"
      via: "unconditional scatter after guard strip"
      pattern: "staging\\[dst\\] ="
---

<objective>
Make high-rank (component_rank 9/27/81) staging **fail-closed** (FND-06). Replace the 19
per-element `if dst < staging.len()` scatter guards (silent partial-write risk) with ONE
upfront `BufferTooSmall`-style size assertion at the planner staging-allocation boundary (D-04),
and re-validate the chunk planner's OOM-safe-stop with a dedicated rank-81 OOM test that asserts
no partial write (D-05). Also confirm the pre-existing oracle-cart-offset lib-test failure is
pre-existing so it cannot be mistaken for a Phase-25 regression (folded todo cross-link).

Purpose: per-element guards silently DROP trailing components (the exact 260530-9ay
silent-truncation bug). One contract point, no silent partial writes anywhere — and the rank-81
families this phase introduces (int1e_ipipipiprinv, int2e_ipip1ipip2) are the first to exercise
rank-81 staging under memory pressure.

Output: single planner assertion + 19 stripped guards + rank-81 OOM no-partial-write test +
staging_buffer_too_small unit test + the pre-existing-failure confirmation note.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-group-2-hessian-higher-order-derivatives/25-RESEARCH.md
@.planning/phases/25-group-2-hessian-higher-order-derivatives/25-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Verified anchors from RESEARCH.md §FND-06 (re-grep all line numbers before editing — codebase churns). -->

Staging boundary (crates/cintx-runtime/src/planner.rs):
- parse_component_multiplier(component_rank: &str) -> Result<usize, _>   (~:403)
- component_multiplier_for(descriptor)                                   (~:452)
- staging_elements_for_chunk(...)                                        (~:321)
- try_alloc_staging(elements) -> Result<Vec<f64>, cintxRsError>          (~:341)  // OOM-safe-stop point
- existing OOM test template: try_alloc_staging_oom_safe_and_f32_lane_count_adequate (~:1000)

Error variant (already exists, crates/cintx-core/src/error.rs:66):
```rust
cintxRsError::BufferTooSmall { required: usize, provided: usize }
```

The 19 scatter guards to strip — pattern `if dst < staging.len() { staging[dst] = ... }`
(verified at one_electron.rs:6545; RE-GREP each file before editing):
| File | Lines (scout — VERIFY with `grep -n "if dst < staging.len()"`) |
|------|------|
| one_electron.rs   | 6545, 6569, 6736, 6760, 6973, 7028 |
| two_electron.rs   | 1600, 1641, 1845, 1886, 2173, 2231 |
| center_3c2e.rs    | 2525, 2559, 2767, 2801 |
| center_2c2e.rs    | 736, 761 |
| f12.rs            | 1784 |
| unstable/grids.rs | 1521 |

Guard becomes unconditional (one_electron.rs:6545 example):
```rust
let dst = staging_comp_base + ii + jj * ni_sph;
staging[dst] = F::from_f64_lossy(sph_tmp[mj * nsi + mi]);   // guard removed
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Single upfront staging-size assertion at the planner boundary</name>
  <read_first>
    - 25-RESEARCH.md §FND-06 Derivation (boundary functions, single-assertion site, error variant).
    - crates/cintx-runtime/src/planner.rs around :321 (staging_elements_for_chunk), :341 (try_alloc_staging), :403 (parse_component_multiplier), :452 (component_multiplier_for), :509 and :1005-1007.
    - crates/cintx-core/src/error.rs:66 (BufferTooSmall { required, provided }).
    - CLAUDE.md "fallible allocation + typed failure + no partial writes" non-negotiable.
  </read_first>
  <behavior>
    - Test (staging_buffer_too_small, unit in planner.rs test mod): when required_elements = component_multiplier * per_component_elements exceeds the allocated staging length, the boundary returns Err(cintxRsError::BufferTooSmall { required, provided }) and never returns an undersized Ok buffer.
    - For correctly-sized staging (rank 9/27/81 with adequate memory), the assertion passes and returns Ok.
  </behavior>
  <action>
    At the point in planner.rs where the staging Vec length is known (post try_alloc_staging, where
    parse_component_multiplier/component_multiplier_for already sizes staging by component_rank), add ONE upfront
    assertion: compute `required_elements = component_multiplier * per_component_elements` and assert
    `staging.len() >= required_elements`, else return `Err(cintxRsError::BufferTooSmall { required: required_elements,
    provided: staging.len() })`. This is the SINGLE contract point (D-04) — NOT a per-launcher assertion. Add the
    `staging_buffer_too_small` unit test (template the existing :1000 OOM test).
  </action>
  <acceptance_criteria>
    - `grep -c 'BufferTooSmall' crates/cintx-runtime/src/planner.rs` returns >=1 (the new assertion).
    - `grep -c 'fn staging_buffer_too_small' crates/cintx-runtime/src/planner.rs` returns 1.
    - `cargo test -p cintx-runtime --lib staging_buffer_too_small 2>&1 | grep -E "test result: ok"` passes.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p cintx-runtime --lib staging_buffer_too_small</automated>
  </verify>
  <done>Single upfront BufferTooSmall assertion present at the planner boundary; staging_buffer_too_small test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Strip all 19 per-element scatter guards (scatter unconditional once proven sized)</name>
  <read_first>
    - 25-RESEARCH.md §FND-06 "The 19 scatter guards to strip" table + the unconditional-scatter example.
    - project_unstable_derivative_ports memory: per-element guards are the silent-truncation root cause (260530-9ay).
    - Each kernel file's guard sites — RE-GREP with `grep -n "if dst < staging.len()"` per file before editing (line numbers drift; 260530-iiq touched center_3c1e.rs, 260530-9ay touched unstable derivatives).
  </read_first>
  <action>
    In each of the 6 kernel files, replace every `if dst < staging.len() { staging[dst] = v }` with the unconditional
    `staging[dst] = v` (the Task-1 upfront assertion now proves the buffer large enough before any scatter runs).
    Files + expected guard counts: one_electron.rs (6), two_electron.rs (6), center_3c2e.rs (4), center_2c2e.rs (2),
    f12.rs (1), unstable/grids.rs (1) = 19 total. Strip ALL of them across ALL kernels per D-04 — NOT a rank>=9-only
    partial strip. Run `grep -rn "if dst < staging.len()"` across crates/cintx-cubecl/src/kernels/ afterward to confirm
    ZERO remain.
  </action>
  <acceptance_criteria>
    - `grep -rc "if dst < staging.len()" crates/cintx-cubecl/src/kernels/ | grep -v ':0$'` returns NOTHING (zero guards remain in any kernel file).
    - `cargo build -p cintx-cubecl` succeeds.
    - `cargo test -p cintx-cubecl --lib 2>&1 | grep -E "test result: ok"` passes (no regression from guard removal).
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(grep -rl 'if dst < staging.len()' crates/cintx-cubecl/src/kernels/)" && cargo build -p cintx-cubecl && echo PASS</automated>
  </verify>
  <done>All 19 guards removed; cintx-cubecl builds; kernel unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rank-81 OOM no-partial-write test + pre-existing-failure confirmation</name>
  <read_first>
    - 25-RESEARCH.md §FND-06 "D-05 OOM test" + §Common Pitfalls 6 (the oracle-cart-offset cross-link).
    - crates/cintx-runtime/src/planner.rs:1000-1007 (the OOM-safe-stop test template).
    - project_oracle_vendor_lib_tests_uncovered + oracle-cart-offset-vendor-zero todo: 4 compare::tests lib unit tests fail under vendor gate at CINTshells_cart_offset[4] cintx=8 vendor=0 (compare.rs ~645); integration parity passes.
  </read_first>
  <behavior>
    - Test (rank81_oom_no_partial_write, unit in planner.rs): set a chunk memory limit smaller than rank-81 staging requires (rank-81 * per_component_elements * 8 bytes), drive a rank-81 component path; assert Err(BufferTooSmall {..}) (or ChunkPlanFailed) AND the output buffer is byte-for-byte unchanged from its pre-call state (no partial write).
  </behavior>
  <action>
    1. Add the rank81_oom_no_partial_write test (template planner.rs:1000): pre-fill the output buffer with a sentinel,
       set memory_limit < rank-81 staging requirement, attempt the rank-81 staging path, assert the typed OOM stop AND
       the output buffer still equals the sentinel (no partial write). This exercises the Task-1 upfront assertion +
       the existing ChunkPlanner OOM-safe-stop together (D-05).
    2. Confirm the pre-existing oracle-cart-offset-vendor-zero failure: run the affected lib unit tests under the
       vendor gate and reproduce the CINTshells_cart_offset[4] cintx=8/vendor=0 mismatch against a pre-phase-20 commit
       (`git stash` or a throwaway worktree at a pre-20 commit) to confirm it is PRE-EXISTING, not a Phase-25
       regression. Record the confirmation in the SUMMARY; do NOT let it block the Phase-25 family gate. If reproduced,
       leave the folded-todo handling (harness-fixture fix vs tracked standalone bug) to the family-gate phase — this
       task only CONFIRMS pre-existence.
  </action>
  <acceptance_criteria>
    - `grep -c 'fn rank81_oom_no_partial_write' crates/cintx-runtime/src/planner.rs` returns 1.
    - `cargo test -p cintx-runtime --lib rank81_oom_no_partial_write 2>&1 | grep -E "test result: ok"` passes.
    - The SUMMARY contains a line confirming oracle-cart-offset-vendor-zero is reproduced against a pre-phase-20 commit (pre-existing) OR documenting it did not reproduce.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p cintx-runtime --lib rank81_oom_no_partial_write</automated>
  </verify>
  <done>Rank-81 OOM stop with no partial write proven; pre-existing oracle-cart-offset failure confirmed (not a Phase-25 regression).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner -> staging Vec | the staging buffer must be provably >= required before any kernel scatter; an undersized buffer is the silent-corruption surface |
| kernel scatter -> staging[dst] | once the upfront assertion holds, the index is in-bounds by construction; the real threat is a partial write on OOM, not OOB |

## STRIDE Threat Register (ASVS L1 — real surface is memory/OOM correctness, not web threats)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-05 | Tampering (silent truncation) | per-element `if dst < len` scatter guards | mitigate | Replace all 19 guards with ONE upfront BufferTooSmall assertion; trailing components can no longer be silently dropped |
| T-25-06 | Denial of service (OOM partial write) | rank-81 staging under memory pressure | mitigate | Fallible try_alloc_staging + upfront assertion -> typed OOM stop, output buffer untouched (rank81_oom_no_partial_write test) |
| T-25-07 | Information disclosure (stale data) | partial write leaves prior buffer contents | mitigate | No-partial-write invariant asserted byte-for-byte in the OOM test |
| T-25-08 | Repudiation (regression masking) | pre-existing oracle-cart-offset lib failure mistaken for new regression | accept | Confirm pre-existing against pre-phase-20 commit; tracked as folded-todo cross-link, does not gate Phase 25 |
</threat_model>

<verification>
- `cargo test -p cintx-runtime --lib staging_buffer_too_small rank81_oom_no_partial_write` — both pass.
- `grep -rl "if dst < staging.len()" crates/cintx-cubecl/src/kernels/` — empty (zero guards remain).
- `cargo build -p cintx-cubecl && cargo test -p cintx-cubecl --lib` — clean.
</verification>

<success_criteria>
- Single upfront BufferTooSmall assertion at the planner staging boundary (D-04).
- All 19 per-element scatter guards removed across all kernels.
- Rank-81 OOM stop with no partial write (D-05).
- Pre-existing oracle-cart-offset failure confirmed pre-existing (does not block the family gate).
</success_criteria>

<output>
After completion, create `.planning/phases/25-group-2-hessian-higher-order-derivatives/25-02-SUMMARY.md`.
This plan MUST merge before any family cluster (Plans 3-6) starts (D-06).
</output>
