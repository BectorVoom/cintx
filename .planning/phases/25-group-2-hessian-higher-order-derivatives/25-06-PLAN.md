---
phase: 25-group-2-hessian-higher-order-derivatives
plan: 06
type: execute
wave: 2
depends_on: [25-01, 25-02]
files_modified:
  - crates/cintx-ops/generated/compiled_manifest.lock.json
  - crates/cintx-compat/src/raw.rs
  - crates/cintx-cubecl/src/kernels/one_electron.rs
  - crates/cintx-oracle/build.rs
  - crates/cintx-oracle/src/vendor_ffi.rs
  - crates/cintx-oracle/tests/deriv34_parity.rs
autonomous: true
requirements: [HESS-04]

must_haves:
  truths:
    - "The complete HESS-04 3rd/4th-order roster is locked by grepping the vendored libcint header/source (not guessed)"
    - "deriv3.c (rank 27) and deriv4.c (rank 81) are added to the oracle cc::Build .file() list with allowlist + extern decls"
    - "int1e_ipipipnuc (rank 27) and int1e_ipipipiprinv (rank 81) plus the enumerated siblings are registered (cart + sph)"
    - "deriv3 (rank 27) and deriv4 (rank 81) registration are SEPARATE tasks so a wide roster-grep cannot blow one task's context budget"
    - "deriv4 families raise BOTH bra +2 AND ket +2 headroom (ng={2,2,0,0,4,1,0,81})"
    - "Each family matches vendor at atol=1e-12 (cart + sph) on a NON-SQUARE block with bra != ket; oracle_covered=true; manifest-audit green"
  artifacts:
    - path: "crates/cintx-oracle/tests/deriv34_parity.rs"
      provides: "vendor_* parity test for the 3rd/4th-order families, NON-SQUARE bra!=ket block, ranks 27/81"
      contains: "fn deriv34_ipipip"
    - path: "crates/cintx-oracle/build.rs"
      provides: "deriv3.c + deriv4.c .file() additions + allowlist regex + extern decls"
      contains: "deriv4.c"
    - path: "crates/cintx-compat/src/raw.rs"
      provides: "RawApiId consts for the 3rd/4th-order roster (cart + sph)"
      contains: "INT1E_IPIPIPNUC"
  key_links:
    - from: "crates/cintx-oracle/build.rs"
      to: "deriv3.c / deriv4.c"
      via: ".file() additions (NOT yet in build) + allowlist"
      pattern: "deriv3\\.c|deriv4\\.c"
    - from: "crates/cintx-cubecl/src/kernels/one_electron.rs"
      to: "deriv4 bra+2 AND ket+2 headroom"
      via: "ng={2,2,0,0,4,1,0,81} dual headroom"
      pattern: "ipipipip|81"
---

<objective>
Cluster D (HESS-04): register and reach byte-identity for the 3rd-order (`deriv3.c`, rank 27) and
4th-order (`deriv4.c`, rank 81) derivative families — anchors `int1e_ipipipnuc` (rank 27) and
`int1e_ipipipiprinv` (rank 81), plus the complete sibling roster derived from the vendored source —
at cart + sph, atol=1e-12. Unlike Clusters A/B/C, `deriv3.c`/`deriv4.c` are NOT yet in the oracle
build, so this plan adds them to the `cc::Build` `.file()` list + suppl-header `extern` decls +
allowlist (ROADMAP SC7). deriv4 raises BOTH bra +2 AND ket +2 headroom (D-09 dual headroom).

Purpose: HESS-04 is the highest-rank cluster (D-06 low-rank-LAST). It is the first to exercise
the FND-06 rank-81 staging assertion on a 1e family, and the rank-27 path on any family. Because the
sibling roster is grep-derived and potentially wide (20+ cart/sph entries), deriv3 (rank 27) and
deriv4 (rank 81) registration are split into SEPARATE tasks so a wide grep result cannot blow one
task's context budget.

Output: the locked roster, deriv3.c/deriv4.c build wiring, manifest entries (ranks 27/81),
RawApiId consts, launcher dispatch, vendor FFI, NON-SQUARE bra!=ket vendor_* parity test.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-group-2-hessian-higher-order-derivatives/25-RESEARCH.md
@.planning/phases/25-group-2-hessian-higher-order-derivatives/25-CONTEXT.md
@.planning/phases/25-group-2-hessian-higher-order-derivatives/25-01-SUMMARY.md
@.planning/phases/25-group-2-hessian-higher-order-derivatives/25-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- ng[] + component_rank verbatim from 25-RESEARCH.md §HESS-04 (deriv3.c rank 27, deriv4.c rank 81). -->

Source files (NOT yet in oracle build — ADD via .file() + allowlist + extern, per ROADMAP SC7):
| File | ANCHOR family | ANCHOR ng[] | component_rank | Roster (LOCK by grep) |
|------|---------------|-------------|----------------|-----------------------|
| deriv3.c | int1e_ipipipnuc (bra+3) | {3,0,0,0,3,1,0,27} | 27 | int1e_ipipipnuc, int1e_ipipiprinv, int1e_ipipnucip, int1e_ipiprinvip, ... (siblings) |
| deriv4.c | int1e_ipipipiprinv (bra+2 ket+2) | {2,2,0,0,4,1,0,81} | 81 | int1e_ipipipiprinv, int1e_ipiprinvipip, ... (siblings) |

> ⚠ The ng[] tuples in this table are the ANCHOR families ONLY. `{3,0,0,0,3,1,0,27}` is the
> `int1e_ipipipnuc` (bra+3 / ket+0) anchor tuple. Mixed-distribution siblings carry their OWN `ng[]`
> read per-family from their libcint definition — e.g. `int1e_ipipnucip` is bra+2 / ket+1, so it does
> NOT share the bra+3 anchor tuple. A bra+2/ket+1 family cannot share a bra+3/ket+0 tuple. Copy each
> family's exact `ng[]` from its own deriv3.c/deriv4.c block (the Task-2/Task-3 prose enforces this).

Roster (from §HESS-04 — anchors CONFIRMED, full siblings MUST be locked by the Task-0 grep, A3):
3rd-order (deriv3.c, rank 27): int1e_ipipipnuc (bra+3 anchor), int1e_ipipiprinv,
  int1e_ipipnucip (bra+2 ket+1 — its OWN tuple, NOT the anchor's), int1e_ipiprinvip,
  int1e_ipippnucp, int1e_ipipprinvp, int1e_ipiprinvrip, int1e_rinvipiprip.
4th-order (deriv4.c, rank 81): int1e_ipipipiprinv (bra+2 AND ket+2 — confirms D-09 dual headroom),
  int1e_ipiprinvipip, + siblings as exported.
Spinor variants exist in cint_funcs.h but register -> UnsupportedApi (D-11).

ng headroom (D-09): the deriv3 ANCHOR raises bra +3 ({3,0,0,...}) for ipipip*nuc; the deriv4 ANCHOR
raises bra +2 AND ket +2 ({2,2,0,0,...}) for ipipip iprinv-style. Mixed-distribution siblings split
the headroom differently (e.g. bra+2/ket+1) and carry their own tuple. The 5th element (nf_max) = 3
(deriv3) / 4 (deriv4) is the polynomial-order headroom — trace how an existing rank-9 family (Phase-23
ipovlpip) populates the analogous field and mirror it (Open Question 3). Copy each family's exact tuple
from its own block.

gout component order: copy each family's s->gout permutation VERBATIM from its deriv3.c/deriv4.c block
(D-09); each family differs (27 or 81 components). component_rank = ng[] last element (27/81), NOT
lower (D-10: too-low rank silently truncates trailing components — the 260530-9ay bug class).

D-08 recipe (5 steps; here step 4 ADDS deriv3.c/deriv4.c .file() since they are NOT yet in the build),
D-11 surface scope (no capi/cint*; spinor -> UnsupportedApi),
D-12 gate (atol=1e-12, cart+sph, double-gated on --features cpu + CINTX_ORACLE_BUILD_VENDOR=1).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0 (roster lock + build wiring): grep the libcint roster, add deriv3.c/deriv4.c to the oracle build</name>
  <read_first>
    - 25-RESEARCH.md §HESS-04 Roster (the plan-time grep step + the deriv3/deriv4 ng[] tuples + A3 risk).
    - libcint-master/include/cint_funcs.h (the full ipip/ipipip/ipipipip family roster) + libcint-master/src/autocode/deriv3.c, deriv4.c.
    - crates/cintx-oracle/build.rs:51-80 (the `.file()` source list — note hess.c:74, int3c2e.c:64 already present), :169-205 (disabled defines + rys source), :358 (allowlist_function regex).
    - D-08 step 4 (confirm the autocode .c is in the build source list — here it is NOT, so ADD it).
  </read_first>
  <action>
    1. LOCK the exact roster: run
       `grep -oE "int1e_[a-z0-9]+" libcint-master/src/autocode/deriv3.c libcint-master/src/autocode/deriv4.c | grep -E "ipipip|ipipipip" | sort -u`
       and cross-reference against libcint-master/include/cint_funcs.h (dedupe _cart/_sph/_spinor/_optimizer). Record the
       deduped 3rd-order (deriv3.c) and 4th-order (deriv4.c) symbol list in the SUMMARY — this is the authoritative
       roster (do NOT guess; A3). For each, read its ng[] + gout block from its own definition. NOTE the per-family
       headroom split (anchors are bra+3 / bra+2+ket+2; mixed siblings like ipipnucip are bra+2/ket+1) so Task 2/Task 3
       copy each tuple verbatim.
    2. Add `deriv3.c` and `deriv4.c` to the `cc::Build` `.file()` list in build.rs (they are NOT yet there — unlike
       hess.c/int3c2e.c). Add the suppl-header `extern` decls for the new symbols (ROADMAP SC7). Confirm the build
       compiles the new translation units: `cargo build -p cintx-oracle --features cpu` (with CINTX_ORACLE_BUILD_VENDOR=1).
  </action>
  <acceptance_criteria>
    - `grep -c 'deriv3.c' crates/cintx-oracle/build.rs` returns >=1 AND `grep -c 'deriv4.c' crates/cintx-oracle/build.rs` returns >=1 (both added to .file()).
    - The SUMMARY contains the deduped roster list (the grep output) for deriv3 + deriv4, annotated with each family's headroom split.
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo build -p cintx-oracle --features cpu` succeeds (deriv3.c/deriv4.c compile).
  </acceptance_criteria>
  <verify>
    <automated>grep -qc 'deriv3.c' crates/cintx-oracle/build.rs && grep -qc 'deriv4.c' crates/cintx-oracle/build.rs && CINTX_ORACLE_BUILD_VENDOR=1 cargo build -p cintx-oracle --features cpu && echo PASS</automated>
  </verify>
  <done>Roster locked from source (with per-family headroom annotated); deriv3.c/deriv4.c added to the oracle build with extern decls; oracle compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: deriv34_parity vendor test scaffold (NON-SQUARE, bra!=ket, ranks 27/81)</name>
  <read_first>
    - 25-RESEARCH.md §HESS-04 + §Validation Architecture (tests/deriv34_parity.rs) + §Common Pitfalls 4 (bra!=ket for deriv4).
    - An existing rank-9 1e vendor_* parity test (Phase-23 ipovlpip) for the gating idiom.
    - The Task-0 locked roster (from the SUMMARY / grep).
  </read_first>
  <behavior>
    - Test deriv34_ipipip (vendor integration, gated): for the rank-27 anchor int1e_ipipipnuc (27 components) and the rank-81 anchor int1e_ipipipiprinv (81 components) plus the locked siblings, cart AND sph, on a NON-SQUARE bra!=ket block, all components match vendor within atol=1e-12.
  </behavior>
  <action>
    Create crates/cintx-oracle/tests/deriv34_parity.rs with `fn deriv34_ipipip` gated on `#[cfg(feature = "cpu")]` +
    a `CINTX_ORACLE_BUILD_VENDOR` runtime check. Use a NON-SQUARE block with DISTINCT bra and ket angular momenta
    (deriv4 dual-headroom families MUST exercise bra != ket so a transposed layout cannot pass — Pitfall 4). Cover
    both anchors and the locked siblings (rank 27 AND rank 81 families). RED until Task 2 (deriv3) + Task 3 (deriv4)
    register the families.
  </action>
  <acceptance_criteria>
    - `grep -c 'fn deriv34_ipipip' crates/cintx-oracle/tests/deriv34_parity.rs` returns 1.
    - `grep -cE 'int1e_ipipipnuc|int1e_ipipipiprinv' crates/cintx-oracle/tests/deriv34_parity.rs` returns >=2.
    - `grep -ciE 'bra.*!=.*ket|non.?square|distinct' crates/cintx-oracle/tests/deriv34_parity.rs` returns >=1 (manual: confirm bra l != ket l).
  </acceptance_criteria>
  <verify>
    <automated>grep -qc 'fn deriv34_ipipip' crates/cintx-oracle/tests/deriv34_parity.rs && grep -cqE 'int1e_ipipipnuc|int1e_ipipipiprinv' crates/cintx-oracle/tests/deriv34_parity.rs && echo PASS</automated>
  </verify>
  <done>NON-SQUARE bra!=ket vendor parity test exists for the rank-27 + rank-81 anchors and siblings (RED acceptable).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register + implement the deriv3 (rank-27) roster + allowlist + parity green</name>
  <read_first>
    - 25-RESEARCH.md §HESS-04 (deriv3 ng[] tuples, Open Question 3 on nf_max) + §Don't Hand-Roll (verbatim gout) + §Common Pitfalls 3 (rank truncation).
    - libcint-master/src/autocode/deriv3.c — each rank-27 family's OWN ng[] + gout block (anchor int1e_ipipipnuc is bra+3 {3,0,0,0,3,1,0,27}; mixed siblings like int1e_ipipnucip are bra+2/ket+1 and carry their OWN tuple — do NOT reuse the anchor tuple).
    - crates/cintx-compat/src/raw.rs (RawApiId pattern) + crates/cintx-cubecl/src/kernels/one_electron.rs (1e launcher ladder; compose the first-order nabla engine 3x).
    - crates/cintx-ops/generated/compiled_manifest.lock.json (clone the closest 1e family; set component_rank=27).
    - crates/cintx-oracle/build.rs allowlist_function regex (:358) + vendor_ffi.rs.
    - D-10 memory project_unstable_derivative_ports: rank too LOW silently truncates trailing components.
  </read_first>
  <behavior>
    - deriv34_ipipip (Task 1) passes for the locked deriv3 (rank-27) roster, cart + sph, all 27 components, atol=1e-12, NON-SQUARE bra!=ket block.
    - int1e_ipipipnuc component_rank=27. Each mixed-distribution sibling uses its OWN ng[] tuple (e.g. ipipnucip bra+2/ket+1), NOT the bra+3 anchor tuple.
  </behavior>
  <action>
    For each rank-27 family in the Task-0 locked deriv3 roster:
    1. Add a manifest lock entry (cart+sph) with component_rank = 27 and the ng[] tuple from its OWN deriv3.c block
       (anchor int1e_ipipipnuc {3,0,0,0,3,1,0,27}; mixed siblings carry their own bra/ket split, e.g. ipipnucip
       bra+2/ket+1 — copy verbatim, do NOT reuse the anchor tuple). Mirror how a rank-9 family populates the nf_max
       (5th) element (Open Question 3). Then `cargo build -p cintx-ops`.
    2. RawApiId consts in raw.rs (INT1E_IPIPIPNUC_CART/_SPH, INT1E_IPIPIPRINV_*, + sibling consts).
    3. Launcher dispatch in one_electron.rs on descriptor.operator_name(), composing the first-order nabla engine 3x.
       Emit each family's gout by copying its s->gout permutation VERBATIM (27 components) (D-09). Spinor reps ->
       UnsupportedApi (D-11).
    4. Extend the build.rs allowlist_function regex with all rank-27 roster cart+sph symbols. Add safe wrappers in
       vendor_ffi.rs.
    5. Run `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu deriv34_ipipip`; fix
       component-order / rank-27 truncation until all 27 components match cart+sph at atol=1e-12 for the deriv3 families.
  </action>
  <acceptance_criteria>
    - `grep -cE 'INT1E_IPIPIPNUC' crates/cintx-compat/src/raw.rs` returns >=2 (cart+sph; more with siblings).
    - `grep -A4 'int1e_ipipipnuc' crates/cintx-ops/generated/compiled_manifest.lock.json | grep -c '"27"'` returns >=2 (cart+sph rank 27).
    - `grep -cE 'int1e_ipipip' crates/cintx-oracle/build.rs` returns >=1 (allowlist extended for deriv3).
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu deriv34_ipipip 2>&1 | grep -E "test result"` shows the deriv3 (rank-27) families passing (deriv4 may still be RED until Task 3).
  </acceptance_criteria>
  <verify>
    <automated>grep -A4 'int1e_ipipipnuc' crates/cintx-ops/generated/compiled_manifest.lock.json | grep -qc '"27"' && cargo build -p cintx-ops && cargo build -p cintx-cubecl && echo PASS</automated>
  </verify>
  <done>deriv3 (rank-27) roster registered (cart+sph), each sibling using its OWN ng[] tuple, byte-identical to vendor at atol=1e-12 on a NON-SQUARE bra!=ket block; no anchor-tuple reuse for mixed siblings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Register + implement the deriv4 (rank-81) roster + dual headroom + parity green + manifest-audit</name>
  <read_first>
    - 25-RESEARCH.md §HESS-04 (deriv4 ng[] tuples, dual-headroom note, Open Question 3 on nf_max) + §Don't Hand-Roll (verbatim gout) + §Common Pitfalls 3 (rank truncation) + Pitfall 4 (bra!=ket / dual headroom).
    - libcint-master/src/autocode/deriv4.c — each rank-81 family's OWN ng[] + gout block (anchor int1e_ipipipiprinv {2,2,0,0,4,1,0,81} — bra+2 AND ket+2).
    - crates/cintx-compat/src/raw.rs (RawApiId pattern) + crates/cintx-cubecl/src/kernels/one_electron.rs (1e launcher ladder; compose the first-order nabla engine 4x).
    - crates/cintx-ops/generated/compiled_manifest.lock.json (clone the closest 1e family; set component_rank=81).
    - crates/cintx-oracle/build.rs allowlist_function regex (:358) + vendor_ffi.rs.
    - 25-02-SUMMARY.md (FND-06 rank-81 staging assertion — this is the first 1e family to exercise it).
    - D-10 memory project_unstable_derivative_ports: rank too LOW silently truncates trailing components.
  </read_first>
  <behavior>
    - deriv34_ipipip (Task 1) passes for the FULL locked roster (deriv3 rank-27 from Task 2 AND deriv4 rank-81), cart + sph, all components (27 and 81), atol=1e-12, NON-SQUARE bra!=ket block.
    - int1e_ipipipiprinv component_rank=81; ng={2,2,0,0,4,1,0,81} raises bra+2 AND ket+2 (dual headroom).
  </behavior>
  <action>
    For each rank-81 family in the Task-0 locked deriv4 roster:
    1. Add a manifest lock entry (cart+sph) with component_rank = 81 and the ng[] tuple from its OWN deriv4.c block
       (anchor int1e_ipipipiprinv {2,2,0,0,4,1,0,81} — note deriv4's bra+2 AND ket+2). Mirror how a rank-9 family
       populates the nf_max (5th) element (Open Question 3). Then `cargo build -p cintx-ops`.
    2. RawApiId consts in raw.rs (INT1E_IPIPIPIPRINV_CART/_SPH, + sibling consts).
    3. Launcher dispatch in one_electron.rs on descriptor.operator_name(), composing the first-order nabla engine 4x.
       For deriv4 raise BOTH bra +2 AND ket +2 headroom. Emit each family's gout by copying its s->gout permutation
       VERBATIM (81 components) (D-09). Spinor reps -> UnsupportedApi (D-11).
    4. Extend the build.rs allowlist_function regex with all rank-81 roster cart+sph symbols. Add safe wrappers in
       vendor_ffi.rs.
    5. Run `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu deriv34_ipipip`; fix
       component-order / rank-81 truncation / single-side-headroom bugs until all 81 components match cart+sph at
       atol=1e-12 on the NON-SQUARE bra!=ket block (a ket-headroom miss MUST be caught here).
    6. Flip oracle_covered=true for the full roster; `cargo build -p cintx-ops`; confirm `manifest-audit` green. NO
       capi/cint* surface.
  </action>
  <acceptance_criteria>
    - `grep -cE 'INT1E_IPIPIPIPRINV' crates/cintx-compat/src/raw.rs` returns >=2 (cart+sph for the rank-81 anchor; more with siblings).
    - `grep -A4 'int1e_ipipipiprinv' crates/cintx-ops/generated/compiled_manifest.lock.json | grep -c '"81"'` returns >=2 (cart+sph rank 81).
    - `grep -cE 'int1e_ipipipip' crates/cintx-oracle/build.rs` returns >=1 (allowlist extended for deriv4).
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu deriv34_ipipip 2>&1 | grep -E "test result: ok"` passes (FULL roster, ranks 27 AND 81).
    - `cargo run -p xtask -- manifest-audit 2>&1 | grep -iE "ok|green|pass"` passes.
  </acceptance_criteria>
  <verify>
    <automated>CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu deriv34_ipipip && cargo run -p xtask -- manifest-audit</automated>
  </verify>
  <done>deriv4 (rank-81) roster registered (cart+sph, bra+2 AND ket+2 dual headroom), byte-identical to vendor at atol=1e-12 on a NON-SQUARE bra!=ket block; full deriv3+deriv4 roster oracle_covered=true; manifest-audit green; no capi/legacy surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| roster derivation -> registered families | guessing the sibling roster risks incomplete coverage; the grep step is the integrity control |
| kernel gout emit -> rank-27/81 staging | the highest-rank families; too-low component_rank silently truncates (the dominant HESS-04 failure mode) |
| anchor tuple -> mixed-distribution sibling | reusing the bra+3 anchor tuple for a bra+2/ket+1 sibling is a headroom/truncation hazard |
| deriv4 dual headroom -> bra+ket ng | raising only bra (not ket) on a deriv4 family is a transpose/truncation hazard |
| wide roster-grep -> single task context | registering 20+ cart/sph entries in one task risks blowing the context budget — split deriv3/deriv4 |

## STRIDE Threat Register (ASVS L1 — real surface is roster completeness + rank-27/81 truncation + dual-headroom correctness)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-20 | Tampering (incomplete roster) | sibling families guessed not derived | mitigate | Lock the roster by grepping deriv3.c/deriv4.c + cint_funcs.h (Task 0); record in SUMMARY |
| T-25-21 | Tampering (rank truncation) | component_rank 27/81 cloned too low | mitigate | Set component_rank = ng[] last element (27/81); gate with full-component non-square parity; FND-06 assertion proves staging |
| T-25-22 | Tampering (dual-headroom miss) | deriv4 raises only bra, not ket | mitigate | ng={2,2,0,0,4,1,0,81} per source; NON-SQUARE bra!=ket block catches a ket-headroom miss (Task 3) |
| T-25-23 | Tampering (missing build source) | deriv3.c/deriv4.c not in cc::Build | mitigate | Add .file() + extern decls + allowlist (Task 0); confirm oracle compiles |
| T-25-24 | Spoofing (silent skip) | vendor parity without both gate flags | mitigate | Double-gate; verify "test result: ok" with N>0 |
| T-25-25 | Tampering (anchor-tuple reuse) | mixed sibling (e.g. ipipnucip bra+2/ket+1) reuses bra+3 anchor tuple | mitigate | Copy each family's OWN ng[] from its deriv3.c/deriv4.c block (Task 2/Task 3); interface table flags the anchor-only tuples |
| T-25-26 | DoS (context exhaustion) | 20+ roster entries registered in one task | mitigate | Split deriv3 (rank 27, Task 2) and deriv4 (rank 81, Task 3) into separate tasks |
</threat_model>

<verification>
- `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu deriv34_ipipip` — all roster families green (ranks 27 AND 81).
- `grep -c 'deriv3.c\|deriv4.c' crates/cintx-oracle/build.rs` — both present.
- `cargo run -p xtask -- manifest-audit` — green.
</verification>

<success_criteria>
- Complete 3rd/4th-order roster locked from source and registered (ranks 27/81, cart+sph), deriv3 and deriv4 in separate tasks.
- Each mixed-distribution sibling uses its OWN ng[] tuple (no bra+3 anchor-tuple reuse).
- deriv3.c/deriv4.c added to the oracle build with extern decls + allowlist.
- deriv4 families raise bra +2 AND ket +2 headroom.
- Each matches vendor at atol=1e-12 on a NON-SQUARE bra!=ket block.
- oracle_covered=true; manifest-audit green. No capi/legacy surface.
</success_criteria>

<output>
After completion, create `.planning/phases/25-group-2-hessian-higher-order-derivatives/25-06-SUMMARY.md`.
Depends on Plans 1+2 (foundations). Confirm worktree integration with `merge-base --is-ancestor` after this wave (D-06).
</output>
</content>
