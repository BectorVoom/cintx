---
phase: 25-group-2-hessian-higher-order-derivatives
plan: 05
type: execute
wave: 2
depends_on: [25-01, 25-02]
files_modified:
  - crates/cintx-ops/generated/compiled_manifest.lock.json
  - crates/cintx-compat/src/raw.rs
  - crates/cintx-cubecl/src/kernels/center_2c2e.rs
  - crates/cintx-cubecl/src/kernels/center_3c2e.rs
  - crates/cintx-oracle/build.rs
  - crates/cintx-oracle/src/vendor_ffi.rs
  - crates/cintx-oracle/tests/hess_multicenter_ipip_parity.rs
autonomous: true
requirements: [HESS-03]

must_haves:
  truths:
    - "int2c2e_ipip1, int3c2e_ipip1, int3c2e_ipip2 are registered with component_rank=9 (cart + sph)"
    - "int3c2e_ipip2 raises KET headroom (k_inc=2, third ng[] element), distinguishing it from the bra-side ipip1"
    - "Each family matches vendor libcint at atol=1e-12 (cart + sph) on a NON-SQUARE block"
    - "oracle_covered=true and manifest-audit is green"
  artifacts:
    - path: "crates/cintx-oracle/tests/hess_multicenter_ipip_parity.rs"
      provides: "vendor_* parity test for the 3 multi-center rank-9 Hessian families, NON-SQUARE block"
      contains: "fn hess_multicenter_ipip"
    - path: "crates/cintx-compat/src/raw.rs"
      provides: "RawApiId consts for int2c2e_ipip1, int3c2e_ipip1, int3c2e_ipip2 (cart + sph)"
      contains: "INT3C2E_IPIP2"
  key_links:
    - from: "crates/cintx-cubecl/src/kernels/center_3c2e.rs"
      to: "int3c2e_ipip2 ket-headroom path"
      via: "k_inc=2 ng tuple (ket-side ipip)"
      pattern: "ipip2"
    - from: "crates/cintx-ops/generated/compiled_manifest.lock.json"
      to: "component_rank=9"
      via: "manifest lock entry per family"
      pattern: "int3c2e_ipip"
---

<objective>
Cluster C (HESS-03): register and reach byte-identity for the three multi-center rank-9 Hessian
families `int2c2e_ipip1` (2-center, ∇² on center 1), `int3c2e_ipip1` (3-center, ∇² on bra
center 1), and `int3c2e_ipip2` (3-center, ∇² on center 2 — KET headroom) at cart + sph,
atol=1e-12. Source `int3c2e.c` is ALREADY in the oracle build (allowlist regex only, no new .file()).

Purpose: HESS-03 exercises ket-side Hessian headroom (int3c2e_ipip2 = k_inc=2) — the multi-center
analog of the Phase-24 ket-headroom plumbing this phase depends on. These ride the 2c2e/3c2e Rys
paths (FND-02 consumers).

Output: 3 manifest entries (component_rank=9), RawApiId consts, launcher dispatch in
center_2c2e.rs / center_3c2e.rs, vendor FFI, NON-SQUARE-block vendor_* parity test.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-group-2-hessian-higher-order-derivatives/25-RESEARCH.md
@.planning/phases/25-group-2-hessian-higher-order-derivatives/25-CONTEXT.md
@.planning/phases/25-group-2-hessian-higher-order-derivatives/25-01-SUMMARY.md
@.planning/phases/25-group-2-hessian-higher-order-derivatives/25-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- ng[] + component_rank verbatim from 25-RESEARCH.md §HESS-03 (int3c2e.c). -->

Families (source int3c2e.c, ALREADY in oracle build at build.rs:64,218 — allowlist regex only, no new .file()):
| Family | ng[] | component_rank | Notes |
|--------|------|----------------|-------|
| int2c2e_ipip1 | {2,0,0,0,2,1,1,9} | 9 | 2-center, ∇² on center 1 (bra) |
| int3c2e_ipip1 | {2,0,0,0,2,1,1,9} | 9 | 3-center, ∇² on bra center 1 |
| int3c2e_ipip2 | {0,0,2,0,2,1,1,9} | 9 | 3-center, ∇² on center 2 — KET headroom (k_inc=2, THIRD ng element; D-09 ket-side) |

The third ng[] element (k_inc) distinguishing ipip2 (ket) from ipip1 (bra) is load-bearing — the
Phase-24 ket-headroom precedent (24-CONTEXT D-07) is the proven pattern; mirror its ket-side ng plumbing.

gout component order: copy each family's s->gout permutation VERBATIM from its int3c2e.c block (D-09);
each family differs. component_rank=9 per family (D-10).

3c2e GPU-port precedent (project_3c2e_gpu_port_dual_path memory): int3c2e's registered families route
through the host/device 3c2e path; mirror the existing int3c2e gradient launcher routing for the ipip
variants. int2c2e_ipip1 mirrors center_2c2e.rs (project_first_gpu_family_2c2e memory).

D-08 recipe (5 steps), D-11 surface scope (no capi/cint*; spinor -> UnsupportedApi),
D-12 gate (atol=1e-12, cart+sph, double-gated on --features cpu + CINTX_ORACLE_BUILD_VENDOR=1).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0 (Wave 0): hess_multicenter_ipip_parity vendor test scaffold (NON-SQUARE block, 3 families)</name>
  <read_first>
    - 25-RESEARCH.md §HESS-03 + §Validation Architecture (tests/hess_multicenter_ipip_parity.rs).
    - An existing int3c2e vendor_* parity test (INT3C2E_IP1_SPH gradient) + an int2c2e parity test for the gating idiom + multi-center shell construction.
    - D-09 Pitfall 4: NON-SQUARE block; for int3c2e_ipip2 the KET (auxiliary) shell must differ from the bra pair so a transposed/bra-confused layout cannot pass.
  </read_first>
  <behavior>
    - Test hess_multicenter_ipip (vendor integration, gated): for int2c2e_ipip1, int3c2e_ipip1, int3c2e_ipip2, cart AND sph, on a NON-SQUARE block, all 9 components match vendor within atol=1e-12.
  </behavior>
  <action>
    Create crates/cintx-oracle/tests/hess_multicenter_ipip_parity.rs with `fn hess_multicenter_ipip` gated on
    `#[cfg(feature = "cpu")]` + a `CINTX_ORACLE_BUILD_VENDOR` runtime check (copy the int3c2e_ip1 / int2c2e parity
    idiom). Use a NON-SQUARE block per D-09; for int3c2e_ipip2 ensure the ket/auxiliary shell l differs from the bra
    pair l. RED until Task 1.
  </action>
  <acceptance_criteria>
    - `grep -c 'fn hess_multicenter_ipip' crates/cintx-oracle/tests/hess_multicenter_ipip_parity.rs` returns 1.
    - `grep -cE 'int2c2e_ipip1|int3c2e_ipip1|int3c2e_ipip2' crates/cintx-oracle/tests/hess_multicenter_ipip_parity.rs` returns >=3.
  </acceptance_criteria>
  <verify>
    <automated>grep -qc 'fn hess_multicenter_ipip' crates/cintx-oracle/tests/hess_multicenter_ipip_parity.rs && echo PASS</automated>
  </verify>
  <done>NON-SQUARE-block vendor parity test exists for all 3 families (RED acceptable).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Register + implement int2c2e_ipip1, int3c2e_ipip1, int3c2e_ipip2 (manifest + RawApiId + launcher)</name>
  <read_first>
    - 25-RESEARCH.md §HESS-03 (ng[] tuples, ket-headroom note) + §Don't Hand-Roll (verbatim gout permutation).
    - libcint-master/src/autocode/int3c2e.c — each family's OWN gout block (int2c2e_ipip1, int3c2e_ipip1, int3c2e_ipip2).
    - crates/cintx-cubecl/src/kernels/center_3c2e.rs (3c2e launcher + ket-headroom path) and center_2c2e.rs (2c2e launcher).
    - 24-CONTEXT.md D-07 (ket-headroom plumbing precedent) + project_3c2e_gpu_port_dual_path memory.
    - crates/cintx-ops/generated/compiled_manifest.lock.json (clone the closest int3c2e/int2c2e rank-9 family entry).
    - D-10 (rank truncation): component_rank=9 exactly per family.
  </read_first>
  <behavior>
    - hess_multicenter_ipip (Task 0) passes for all 3 families, cart + sph, 9 components, atol=1e-12, NON-SQUARE block.
    - int3c2e_ipip2 ng[] has k_inc=2 (third element) — ket headroom, distinct from ipip1's bra i_inc=2.
  </behavior>
  <action>
    For each family:
    1. Add a manifest lock entry (cart+sph) cloning the closest rank-9 multi-center family with component_rank=9 and the
       ng[] tuple from §HESS-03 (int2c2e_ipip1={2,0,0,0,2,1,1,9}; int3c2e_ipip1={2,0,0,0,2,1,1,9};
       int3c2e_ipip2={0,0,2,0,2,1,1,9} — note ipip2's k_inc=2). Then `cargo build -p cintx-ops`.
    2. RawApiId consts in raw.rs (INT2C2E_IPIP1_CART/_SPH, INT3C2E_IPIP1_*, INT3C2E_IPIP2_*).
    3. Launcher dispatch: int2c2e_ipip1 in center_2c2e.rs; int3c2e_ipip1 / int3c2e_ipip2 in center_3c2e.rs (mirror the
       int3c2e_ip1 gradient routing). For int3c2e_ipip2 raise KET headroom (k_inc=2) per the Phase-24 ket-headroom
       pattern — NOT bra. Emit each family's gout by copying its s->gout permutation VERBATIM from int3c2e.c (D-09).
    4. Spinor reps -> UnsupportedApi (D-11). NO capi variants, NO cint* wrappers.
  </action>
  <acceptance_criteria>
    - `grep -cE 'INT2C2E_IPIP1|INT3C2E_IPIP1|INT3C2E_IPIP2' crates/cintx-compat/src/raw.rs` returns >=6 (cart+sph for 3 families).
    - `grep -A4 'int3c2e_ipip2' crates/cintx-ops/generated/compiled_manifest.lock.json | grep -cE '"ng".*0, 0, 2|0,0,2'` returns >=1 (ket headroom k_inc=2 recorded).
    - `grep -A4 -E 'int(2c2e_ipip1|3c2e_ipip1|3c2e_ipip2)' crates/cintx-ops/generated/compiled_manifest.lock.json | grep -c '"9"'` returns >=6.
    - `cargo build -p cintx-ops && cargo build -p cintx-cubecl` succeed.
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p cintx-ops && cargo build -p cintx-cubecl && grep -cqE 'INT3C2E_IPIP2' crates/cintx-compat/src/raw.rs</automated>
  </verify>
  <done>3 families registered (component_rank=9, cart+sph), int3c2e_ipip2 ket-headroom (k_inc=2), launcher emits verbatim gout, spinor -> UnsupportedApi.</done>
</task>

<task type="auto">
  <name>Task 2: Vendor FFI allowlist + parity green + oracle_covered=true + manifest-audit</name>
  <read_first>
    - 25-RESEARCH.md §HESS-03, §Standard Stack (int3c2e.c already at build.rs:64,218 — allowlist only), §Validation Architecture.
    - crates/cintx-oracle/build.rs (allowlist_function regex) + crates/cintx-oracle/src/vendor_ffi.rs (safe wrappers).
    - reference_oracle_vendor_parity_invocation memory: double-gated; without both flags parity silently skips.
    - project_3c2e_f32_parity_preexisting_fail memory: test_f32_int3c2e_sph_parity is a pre-existing failure on this branch — baseline noise, not a regression from this plan.
  </read_first>
  <action>
    1. Extend the build.rs allowlist_function regex with the 3 families' cart+sph symbols (int2c2e_ipip1_cart/_sph,
       int3c2e_ipip1_*, int3c2e_ipip2_*). NO new `.file()` — int3c2e.c already compiles.
    2. Add safe wrappers in vendor_ffi.rs for the 6 symbols.
    3. Run `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu hess_multicenter_ipip`; fix
       component-order / ket-vs-bra mismatch until all 9 components match cart+sph at atol=1e-12. (Ignore the
       known-preexisting test_f32_int3c2e_sph_parity failure per memory.)
    4. Flip oracle_covered=true for the 3 entries; `cargo build -p cintx-ops`; confirm `manifest-audit` green.
  </action>
  <acceptance_criteria>
    - `grep -cE 'int2c2e_ipip1|int3c2e_ipip1|int3c2e_ipip2' crates/cintx-oracle/build.rs` returns >=3.
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu hess_multicenter_ipip 2>&1 | grep -E "test result: ok"` passes.
    - `grep -A6 -E 'int(2c2e_ipip1|3c2e_ipip1|3c2e_ipip2)' crates/cintx-ops/generated/compiled_manifest.lock.json | grep -c 'oracle_covered.*true\|"oracle_covered": true'` returns >=6.
    - `cargo run -p xtask -- manifest-audit 2>&1 | grep -iE "ok|green|pass"` passes.
  </acceptance_criteria>
  <verify>
    <automated>CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu hess_multicenter_ipip && cargo run -p xtask -- manifest-audit</automated>
  </verify>
  <done>All 3 multi-center Hessian families byte-identical (atol=1e-12, cart+sph, 9 components, non-square block, ipip2 ket-headroom); oracle_covered=true; manifest-audit green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw eval_raw -> 2c2e/3c2e launcher | descriptor selects bra-side (ipip1) vs ket-side (ipip2) headroom; confusing them is a transpose-class bug |
| kernel gout emit -> staging | component_rank must equal 9 or trailing components truncate |

## STRIDE Threat Register (ASVS L1 — real surface is ket/bra-headroom + component correctness)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-16 | Tampering (bra/ket confusion) | int3c2e_ipip2 raising bra headroom instead of ket | mitigate | Set ng k_inc=2 (third element) per source; gate with NON-SQUARE block where ket l != bra l |
| T-25-17 | Tampering (component truncation) | component_rank cloned too low | mitigate | Set component_rank=9; gate with 9-component non-square parity |
| T-25-18 | Tampering (transpose) | s->gout permutation inferred | mitigate | Copy each family's gout verbatim from int3c2e.c; NON-SQUARE block gate |
| T-25-19 | Spoofing (silent skip) | vendor parity without both gate flags | mitigate | Double-gate; verify "test result: ok" with N>0 |
</threat_model>

<verification>
- `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu hess_multicenter_ipip` — all 3 families green.
- `cargo run -p xtask -- manifest-audit` — green.
</verification>

<success_criteria>
- int2c2e_ipip1, int3c2e_ipip1, int3c2e_ipip2 registered (component_rank=9, cart+sph).
- int3c2e_ipip2 raises ket headroom (k_inc=2).
- Each matches vendor at atol=1e-12, 9 components, NON-SQUARE block.
- oracle_covered=true; manifest-audit green. No capi/legacy surface.
</success_criteria>

<output>
After completion, create `.planning/phases/25-group-2-hessian-higher-order-derivatives/25-05-SUMMARY.md`.
Depends on Plans 1+2 (foundations). Confirm worktree integration with `merge-base --is-ancestor` after this wave (D-06).
</output>
