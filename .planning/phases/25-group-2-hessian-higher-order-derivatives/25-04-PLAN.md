---
phase: 25-group-2-hessian-higher-order-derivatives
plan: 04
type: execute
wave: 2
depends_on: [25-01, 25-02]
files_modified:
  - crates/cintx-ops/generated/compiled_manifest.lock.json
  - crates/cintx-compat/src/raw.rs
  - crates/cintx-cubecl/src/kernels/two_electron.rs
  - crates/cintx-cubecl/src/kernels/unstable/two_e.rs
  - crates/cintx-oracle/build.rs
  - crates/cintx-oracle/src/vendor_ffi.rs
  - crates/cintx-oracle/tests/hess2e_parity.rs
autonomous: true
requirements: [HESS-02]

must_haves:
  truths:
    - "int2e_ipip1 and int2e_ipvip1 are MOVED from unstable::source::2e to the stable raw-api map (one canonical entry per symbol, no alias)"
    - "int2e_ip1ip2 (rank 9) and int2e_ipip1ipip2 (rank 81, 4th-order) are registered fresh in the stable family"
    - "All 4 families match vendor libcint at atol=1e-12 (cart + sph) on a NON-SQUARE bra x ket block"
    - "The unstable sph-only stubs for ipip1/ipvip1 are removed; no lingering unstable feature-gate on these symbols"
  artifacts:
    - path: "crates/cintx-oracle/tests/hess2e_parity.rs"
      provides: "vendor_* parity test for the 4 2e Hessian families, NON-SQUARE block, rank 9 and 81"
      contains: "fn hess2e_ipip"
    - path: "crates/cintx-compat/src/raw.rs"
      provides: "RawApiId consts for the 4 stable 2e Hessian families (cart + sph)"
      contains: "INT2E_IPIP1"
  key_links:
    - from: "crates/cintx-cubecl/src/kernels/two_electron.rs"
      to: "fill_g_tensor_2e host path"
      via: "Hessian d-quartets elevate to nroots 6 -> host path (FND-02 consumer)"
      pattern: "fill_g_tensor_2e"
    - from: "crates/cintx-ops/generated/compiled_manifest.lock.json"
      to: "stable canonical family (no unstable::source::2e)"
      via: "deleted unstable entries + new stable entries"
      pattern: "int2e_ipip1ipip2"
---

<objective>
Cluster B (HESS-02): bring the 2e Hessian set to byte-identity (cart + sph, atol=1e-12) and
PROMOTE the two existing unstable stubs to stable per D-07. `int2e_ipip1` and `int2e_ipvip1`
exist today as sph-only `unstable::source::2e` stubs (oracle_covered=false, empty component_rank)
— MOVE them to the stable raw-api map (add cart, set component_rank, wire stable launcher + vendor
FFI + vendor_* test, flip oracle_covered=true). Register `int2e_ip1ip2` (rank 9) and
`int2e_ipip1ipip2` (rank 81, 4th-order 2e) fresh in the same stable family.

Purpose: HESS-02 d-quartets on the H2O/STO-3G corpus are the PRIMARY in-phase FND-02 trigger
(Hessian elevation pushes d-quartets to nroots 6 -> host fill_g_tensor_2e path). Source hess.c
is ALREADY in the oracle build (allowlist only). int2e_ipip1ipip2 is the first rank-81 family
to exercise the FND-06 rank-81 staging path on a real 2e family.

Output: 2 unstable entries deleted + 4 stable entries (ranks 9/9/9/81), RawApiId consts, stable
launcher, vendor FFI, NON-SQUARE-block vendor_* parity test.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-group-2-hessian-higher-order-derivatives/25-RESEARCH.md
@.planning/phases/25-group-2-hessian-higher-order-derivatives/25-CONTEXT.md
@.planning/phases/25-group-2-hessian-higher-order-derivatives/25-01-SUMMARY.md
@.planning/phases/25-group-2-hessian-higher-order-derivatives/25-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- ng[] + component_rank verbatim from 25-RESEARCH.md §HESS-02 (hess.c). -->

Families (source hess.c, ALREADY in oracle build at build.rs:74,221 — allowlist regex only, no new .file()):
| Family | ng[] | component_rank | Status |
|--------|------|----------------|--------|
| int2e_ipip1       | {2,0,0,0,2,1,1,9}  | 9  | unstable sph stub at lock:3327 -> re-home (D-07) |
| int2e_ipvip1      | {1,1,0,0,2,1,1,9}  | 9  | unstable sph stub at lock:3360 -> re-home (D-07) |
| int2e_ip1ip2      | {1,0,1,0,2,1,1,9}  | 9  | NEW (register fresh) |
| int2e_ipip1ipip2  | {2,0,2,0,4,1,1,81} | 81 | NEW (register fresh) — 4th-order 2e, rank 81 |

D-07 re-home mechanics: the unstable entries at compiled_manifest.lock.json:3327 (int2e_ipip1_sph)
and :3360 (int2e_ipvip1_sph) have canonical_family "unstable::source::2e", oracle_covered=false,
empty component_rank. DELETE both; add stable cart+sph entries with component_rank=9, a stable
canonical family (e.g. "2e" or a new "2e::hess"), oracle_covered=true after parity. Wire the stable
launcher in two_electron.rs, vendor FFI, vendor_* test. REMOVE the unstable kernel module entries in
unstable/ (one canonical entry per symbol, NO alias — feedback_new_family_surface_scope memory + D-07).
NOT an in-place extend + alias.

gout component order: copy each family's s->gout permutation VERBATIM from its hess.c block (D-09);
each family differs. The rank-81 int2e_ipip1ipip2 emits 81 components — confirm component_rank=81
(D-10: too-low rank silently truncates trailing components, the 260530-9ay bug class).

Host-path routing (FND-02 / A2): gradient family int2e_ip1 host-routes via fill_g_tensor_2e
(two_electron.rs:1516,1765). The Hessian families MUST follow the same host routing so nroots>=6
hits the host path (not the MAX_DEVICE_NROOTS=5 device kernel). Verify per family.

D-08 recipe (5 steps), D-11 surface scope (no capi/cint*; spinor -> UnsupportedApi),
D-12 gate (atol=1e-12, cart+sph, double-gated on --features cpu + CINTX_ORACLE_BUILD_VENDOR=1).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0 (Wave 0): hess2e_parity vendor test scaffold (NON-SQUARE block, 4 families incl. rank 81)</name>
  <read_first>
    - 25-RESEARCH.md §HESS-02 + §Validation Architecture (tests/hess2e_parity.rs).
    - An existing 2e vendor_* parity test (e.g. an int2e_ip1 gradient parity test) for the gating idiom + 2e shell-quartet construction.
    - D-09 Pitfall 4: NON-SQUARE bra x ket block (the rank-81 ip1ip2 family must use bra l != ket l).
  </read_first>
  <behavior>
    - Test hess2e_ipip (vendor integration, gated): for int2e_ipip1/ipvip1/ip1ip2 (9 components) and int2e_ipip1ipip2 (81 components), cart AND sph, on a NON-SQUARE shell quartet, all components match vendor within atol=1e-12.
  </behavior>
  <action>
    Create crates/cintx-oracle/tests/hess2e_parity.rs with `fn hess2e_ipip` gated on `#[cfg(feature = "cpu")]` + a
    `CINTX_ORACLE_BUILD_VENDOR` runtime check (copy a 2e gradient parity test idiom). Use a NON-SQUARE shell quartet
    (distinct bra/ket angular momenta) per D-09; ensure the rank-81 int2e_ipip1ipip2 case exercises distinct bra and
    ket l. RED until Task 1 registers the families.
  </action>
  <acceptance_criteria>
    - `grep -c 'fn hess2e_ipip' crates/cintx-oracle/tests/hess2e_parity.rs` returns 1.
    - `grep -cE 'int2e_ipip1[^i]|int2e_ipvip1|int2e_ip1ip2|int2e_ipip1ipip2' crates/cintx-oracle/tests/hess2e_parity.rs` returns >=4.
  </acceptance_criteria>
  <verify>
    <automated>grep -qc 'fn hess2e_ipip' crates/cintx-oracle/tests/hess2e_parity.rs && echo PASS</automated>
  </verify>
  <done>NON-SQUARE-block vendor parity test exists for all 4 families incl. rank-81 (RED acceptable).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Re-home ipip1/ipvip1 to stable + register ip1ip2/ipip1ipip2 fresh (manifest + RawApiId + launcher)</name>
  <read_first>
    - 25-RESEARCH.md §HESS-02 (D-07 re-home mechanics, ng[] tuples, rank 81 note) + §Don't Hand-Roll (compose first-order engine twice; verbatim gout).
    - libcint-master/src/autocode/hess.c — each family's OWN gout block (int2e_ipip1, int2e_ipvip1, int2e_ip1ip2, int2e_ipip1ipip2).
    - crates/cintx-ops/generated/compiled_manifest.lock.json:3327 (int2e_ipip1_sph), :3360 (int2e_ipvip1_sph) — the unstable entries to DELETE.
    - crates/cintx-cubecl/src/kernels/unstable/ (the int2e_ipip1/ipvip1 stub modules to REMOVE) + crates/cintx-cubecl/src/kernels/two_electron.rs (the stable launcher + fill_g_tensor_2e host path at :1516,1765).
    - crates/cintx-compat/src/raw.rs (RawApiId pattern + the unstable->stable raw-api map move).
    - feedback_new_family_surface_scope memory: one canonical stable entry per symbol, no alias, no unstable feature-gate.
    - D-10 (rank truncation): int2e_ipip1ipip2 MUST be component_rank=81.
  </read_first>
  <behavior>
    - hess2e_ipip (Task 0) passes for all 4 families, cart + sph, all components (9/9/9/81), atol=1e-12, NON-SQUARE block.
    - int2e_ipip1ipip2 emits exactly 81 components; component_rank=81.
    - No `unstable::source::2e` entry remains for ipip1/ipvip1.
  </behavior>
  <action>
    1. DELETE the unstable lock entries int2e_ipip1_sph (:3327) and int2e_ipvip1_sph (:3360) and REMOVE their stub
       modules in unstable/. Add stable cart+sph lock entries for all 4 families: ipip1/ipvip1/ip1ip2 component_rank=9,
       ipip1ipip2 component_rank=81, with the ng[] tuples from §HESS-02 and a stable canonical_family. Then
       `cargo build -p cintx-ops` to regenerate api_manifest.{rs,csv}.
    2. Add RawApiId consts in raw.rs (INT2E_IPIP1_CART/_SPH, INT2E_IPVIP1_*, INT2E_IP1IP2_*, INT2E_IPIP1IPIP2_*),
       moving the ipip1/ipvip1 ids out of any unstable map into the stable map (no alias).
    3. Wire stable launcher dispatch in two_electron.rs on descriptor.operator_name(), routing each via the host
       fill_g_tensor_2e path (mirror int2e_ip1 :1516,1765 so nroots>=6 Hessian d-quartets hit the host path). Emit each
       family's gout by copying its s->gout permutation VERBATIM from hess.c (D-09). Set ng headroom per tuple
       (ipip1 bra+2; ipvip1 bra+1 ket+1; ip1ip2 bra+1 ket+1; ipip1ipip2 bra+2 ket+2).
    4. Spinor reps -> UnsupportedApi (D-11). NO capi variants, NO cint* wrappers.
  </action>
  <acceptance_criteria>
    - `grep -c 'unstable::source::2e' crates/cintx-ops/generated/compiled_manifest.lock.json` returns 0 (unstable 2e Hessian entries deleted).
    - `grep -cE 'INT2E_IPIP1[^I]|INT2E_IPVIP1|INT2E_IP1IP2|INT2E_IPIP1IPIP2' crates/cintx-compat/src/raw.rs` returns >=8 (cart+sph for 4 families).
    - `grep -A4 'int2e_ipip1ipip2' crates/cintx-ops/generated/compiled_manifest.lock.json | grep -c '"81"'` returns >=2 (cart+sph rank 81).
    - `cargo build -p cintx-ops && cargo build -p cintx-cubecl` succeed.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'unstable::source::2e' crates/cintx-ops/generated/compiled_manifest.lock.json | grep -qx 0 && cargo build -p cintx-ops && cargo build -p cintx-cubecl && echo PASS</automated>
  </verify>
  <done>ipip1/ipvip1 re-homed to stable (no unstable entry), ip1ip2/ipip1ipip2 registered (ranks 9/81), launcher emits verbatim gout, host-routed.</done>
</task>

<task type="auto">
  <name>Task 2: Vendor FFI allowlist + parity green + oracle_covered=true + manifest-audit</name>
  <read_first>
    - 25-RESEARCH.md §HESS-02, §Standard Stack (hess.c already in build — allowlist only), §Validation Architecture (per-wave full vendor suite).
    - crates/cintx-oracle/build.rs (allowlist_function regex) + crates/cintx-oracle/src/vendor_ffi.rs (safe wrappers).
    - reference_oracle_vendor_parity_invocation memory: double-gated; without both flags parity silently skips.
    - project_3c2e_f32_parity_preexisting_fail memory: test_f32_int3c2e_sph_parity fails on this branch independent of 2e work — baseline noise, not a regression from this plan.
  </read_first>
  <action>
    1. Extend the build.rs allowlist_function regex with the 4 families' cart+sph symbols (int2e_ipip1_cart/_sph,
       int2e_ipvip1_*, int2e_ip1ip2_*, int2e_ipip1ipip2_*). NO new `.file()` — hess.c already compiles.
    2. Add safe wrappers in vendor_ffi.rs for the 8 symbols.
    3. Run `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu hess2e_ipip`; fix component-order /
       rank-81 truncation until all components match cart+sph at atol=1e-12. (Ignore the known-preexisting
       test_f32_int3c2e_sph_parity failure per memory — it is baseline noise on this branch.)
    4. Flip oracle_covered=true for the 4 entries; `cargo build -p cintx-ops`; confirm `manifest-audit` green.
  </action>
  <acceptance_criteria>
    - `grep -cE 'int2e_ipip1[^i_]|int2e_ipvip1|int2e_ip1ip2|int2e_ipip1ipip2' crates/cintx-oracle/build.rs` returns >=4.
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu hess2e_ipip 2>&1 | grep -E "test result: ok"` passes.
    - `grep -A6 -E 'int2e_ip(ip1|vip1|1ip2|ip1ipip2)' crates/cintx-ops/generated/compiled_manifest.lock.json | grep -c 'oracle_covered.*true\|"oracle_covered": true'` returns >=8.
    - `cargo run -p xtask -- manifest-audit 2>&1 | grep -iE "ok|green|pass"` passes.
  </acceptance_criteria>
  <verify>
    <automated>CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu hess2e_ipip && cargo run -p xtask -- manifest-audit</automated>
  </verify>
  <done>All 4 2e Hessian families byte-identical (atol=1e-12, cart+sph, ranks 9/9/9/81, non-square block); oracle_covered=true; manifest-audit green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unstable map -> stable raw-api map | the re-home must leave exactly one canonical entry; a lingering alias/unstable gate is a duplicate-symbol correctness hazard |
| kernel gout emit -> rank-81 staging | int2e_ipip1ipip2 (rank 81) is the first 2e family on the FND-06 rank-81 path; too-low rank truncates |

## STRIDE Threat Register (ASVS L1 — real surface is component/rank correctness + the re-home integrity)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-12 | Tampering (duplicate symbol) | unstable stub left aliased after re-home | mitigate | Delete unstable lock entries + stub modules; grep-assert zero `unstable::source::2e` remains |
| T-25-13 | Tampering (rank-81 truncation) | int2e_ipip1ipip2 component_rank too low | mitigate | Set component_rank=81 (ng[] last element); gate with 81-component non-square parity; FND-06 assertion proves staging |
| T-25-14 | Tampering (transpose) | s->gout permutation inferred | mitigate | Copy each family's gout verbatim from hess.c; NON-SQUARE quartet gate |
| T-25-15 | Spoofing (silent skip) | vendor parity without both gate flags | mitigate | Double-gate; verify "test result: ok" with N>0 |
</threat_model>

<verification>
- `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu hess2e_ipip` — all 4 families green.
- `grep -c 'unstable::source::2e' crates/cintx-ops/generated/compiled_manifest.lock.json` — 0.
- `cargo run -p xtask -- manifest-audit` — green.
</verification>

<success_criteria>
- ipip1/ipvip1 re-homed to stable (one canonical entry, no alias, no unstable gate).
- ip1ip2 (rank 9) + ipip1ipip2 (rank 81) registered fresh.
- All 4 match vendor at atol=1e-12, cart+sph, NON-SQUARE block.
- oracle_covered=true; manifest-audit green. No capi/legacy surface.
</success_criteria>

<output>
After completion, create `.planning/phases/25-group-2-hessian-higher-order-derivatives/25-04-SUMMARY.md`.
Depends on Plans 1+2 (foundations). Confirm worktree integration with `merge-base --is-ancestor` after this wave (D-06).
</output>
