---
phase: 25-group-2-hessian-higher-order-derivatives
plan: 03
type: execute
wave: 2
depends_on: [25-01, 25-02]
files_modified:
  - crates/cintx-ops/generated/compiled_manifest.lock.json
  - crates/cintx-compat/src/raw.rs
  - crates/cintx-cubecl/src/kernels/one_electron.rs
  - crates/cintx-oracle/build.rs
  - crates/cintx-oracle/src/vendor_ffi.rs
  - crates/cintx-oracle/tests/hess1e_ipip_parity.rs
autonomous: true
requirements: [HESS-01]

must_haves:
  truths:
    - "int1e_ipipovlp, int1e_ipipnuc, int1e_ipipkin, int1e_ipiprinv are registered with component_rank=9 (cart + sph)"
    - "Each family matches vendor libcint 6.1.3 at atol=1e-12, every one of the 9 components, on a NON-SQUARE bra x ket block"
    - "The s->gout 3x3 second-derivative permutation is copied verbatim per family from hess.c"
    - "oracle_covered=true for all 4 families and manifest-audit is green"
  artifacts:
    - path: "crates/cintx-oracle/tests/hess1e_ipip_parity.rs"
      provides: "vendor_* parity test for the 4 rank-9 1e Hessian families, NON-SQUARE block"
      contains: "fn hess1e_ipip"
    - path: "crates/cintx-compat/src/raw.rs"
      provides: "RawApiId consts for the 4 families (cart + sph)"
      contains: "INT1E_IPIPNUC"
  key_links:
    - from: "crates/cintx-compat/src/raw.rs"
      to: "one_electron.rs ipip launcher"
      via: "eval_raw dispatch on descriptor.operator_name()"
      pattern: "ipipovlp|ipipnuc|ipipkin|ipiprinv"
    - from: "crates/cintx-ops/generated/compiled_manifest.lock.json"
      to: "component_rank=9"
      via: "manifest lock entry per family"
      pattern: "ipip"
---

<objective>
Cluster A (HESS-01): register and reach byte-identity for the four rank-9 1e Hessian families
`int1e_ipipovlp`, `int1e_ipipnuc`, `int1e_ipipkin`, `int1e_ipiprinv` (cart + sph, atol=1e-12).
Each is the second nabla applied to the Phase-23 first-order 1e engine; ovlp/kin reuse the
no-Rys overlap-derivative engine, nuc/rinv ride the nuclear/Rys 1e path (the in-phase FND-02
consumer — Hessian-elevated d-shells can hit nroots 6).

Purpose: HESS-01 is the lowest-rank family cluster (D-06 low-rank-first). The source `hess.c`
is ALREADY in the oracle build, so only the bindgen allowlist needs extending (no new .file()).

Output: 4 manifest entries (component_rank=9), RawApiId consts, launcher dispatch, vendor FFI
wrappers, and a NON-SQUARE-block vendor_* parity test.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-group-2-hessian-higher-order-derivatives/25-RESEARCH.md
@.planning/phases/25-group-2-hessian-higher-order-derivatives/25-CONTEXT.md
@.planning/phases/25-group-2-hessian-higher-order-derivatives/25-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- ng[] tuples + component_rank + gout permutation verbatim from 25-RESEARCH.md §HESS-01 (hess.c). -->

Families (source hess.c, ALREADY in oracle build at build.rs:74,221 — allowlist regex only, no new .file()):
| Family | ng[] | component_rank | Engine | Headroom |
|--------|------|----------------|--------|----------|
| int1e_ipipovlp | (overlap-deriv, no Rys; ng[6]=0 no 3rd center) | 9 | one_electron overlap-deriv | bra +2 |
| int1e_ipipnuc  | {2,0,0,0,2,1,0,9} | 9 | nuclear/Rys 1e (FND-02 consumer) | bra +2 (i_l+2) |
| int1e_ipipkin  | (overlap-deriv engine) | 9 | one_electron overlap-deriv | bra +2 |
| int1e_ipiprinv | (rinv/Rys 1e path) | 9 | nuclear/Rys 1e | bra +2 |

gout permutation for int1e_ipipnuc (VERBATIM from hess.c:548-557 — copy each family's OWN block;
each family's gout differs, read its own definition in hess.c):
```c
gout[n*9+0] = s[0];  gout[n*9+1] = s[3];  gout[n*9+2] = s[6];   // xx yx zx
gout[n*9+3] = s[1];  gout[n*9+4] = s[4];  gout[n*9+5] = s[7];   // xy yy zy
gout[n*9+6] = s[2];  gout[n*9+7] = s[5];  gout[n*9+8] = s[8];   // xz yz zz
```
This s->gout permutation is the column-major 3x3 second-derivative tensor order. Load-bearing (D-09).

Registration recipe (D-08, 5 steps — lock edits auto-sync manifest-audit, NO separate fixtures list):
(1) lock entry cloning closest family with component_rank=9, then `cargo build -p cintx-ops` regenerates
    api_manifest.{rs,csv}; (2) RawApiId consts in raw.rs (cart+sph); (3) launcher dispatch on
    descriptor.operator_name() in one_electron.rs; (4) vendor FFI — extend allowlist_function regex in
    build.rs + safe wrappers in vendor_ffi.rs (hess.c already compiles); (5) vendor_* test.

Surface scope (D-11): manifest + RawApiId + kernel + vendor-FFI + oracle ONLY. NO capi enum variants,
NO legacy cint* wrappers. Spinor reps registered -> UnsupportedApi.

Verification gate (D-12): atol=1e-12 vs vendor, cart+sph, every component, double-gated on
`--features cpu` + `CINTX_ORACLE_BUILD_VENDOR=1` (without both, parity SILENTLY SKIPS).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0 (Wave 0): hess1e_ipip_parity vendor test scaffold (NON-SQUARE block, 4 families)</name>
  <read_first>
    - 25-RESEARCH.md §HESS-01 + §Validation Architecture (test map: tests/hess1e_ipip_parity.rs).
    - An existing rank-9 vendor_* parity test (e.g. the Phase-23 ipovlpip test) for the gating idiom + NON-SQUARE block selection.
    - project_1e_gpu_port_scalar_only memory: spinor/transpose gotchas; here the gate is the NON-SQUARE bra x ket block requirement (D-09 Pitfall 4).
  </read_first>
  <behavior>
    - Test hess1e_ipip (vendor integration, gated): for each of int1e_ipip{ovlp,nuc,kin,rinv}, cart AND sph, on a NON-SQUARE bra x ket block (e.g. p x d), all 9 components match vendor libcint within atol=1e-12.
  </behavior>
  <action>
    Create crates/cintx-oracle/tests/hess1e_ipip_parity.rs with `fn hess1e_ipip` gated on `#[cfg(feature = "cpu")]` +
    a `CINTX_ORACLE_BUILD_VENDOR` runtime check (copy the Phase-23 ipovlpip gating idiom). Select a NON-SQUARE
    bra x ket shell tuple (p x d) per D-09 so a transposed layout cannot pass. The test will be RED until the families
    are registered in Task 1 — that is expected.
  </action>
  <acceptance_criteria>
    - `grep -c 'fn hess1e_ipip' crates/cintx-oracle/tests/hess1e_ipip_parity.rs` returns 1.
    - The test references all 4 families: `grep -cE 'ipipovlp|ipipnuc|ipipkin|ipiprinv' crates/cintx-oracle/tests/hess1e_ipip_parity.rs` returns >=4.
    - The block is non-square: `grep -ciE 'p.*d|non.?square' crates/cintx-oracle/tests/hess1e_ipip_parity.rs` returns >=1 (manual confirm bra l != ket l).
  </acceptance_criteria>
  <verify>
    <automated>grep -qc 'fn hess1e_ipip' crates/cintx-oracle/tests/hess1e_ipip_parity.rs && grep -cqE 'ipipovlp|ipipnuc|ipipkin|ipiprinv' crates/cintx-oracle/tests/hess1e_ipip_parity.rs && echo PASS</automated>
  </verify>
  <done>NON-SQUARE-block vendor parity test exists for all 4 families (RED is acceptable at this task).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Register + implement the 4 rank-9 1e Hessian families (manifest + RawApiId + launcher + kernel)</name>
  <read_first>
    - 25-RESEARCH.md §HESS-01 (ng[] + gout) and §Don't Hand-Roll (compose the Phase-23 first-order engine twice; copy the gout permutation verbatim).
    - libcint-master/src/autocode/hess.c — read each family's OWN gout block (int1e_ipipovlp, int1e_ipipnuc :548-557, int1e_ipipkin, int1e_ipiprinv).
    - 23-CONTEXT.md registration recipe (D-11) + 24-CONTEXT.md (D-05) — the proven 5-step recipe.
    - crates/cintx-compat/src/raw.rs (RawApiId const pattern + eval_raw dispatch) and crates/cintx-cubecl/src/kernels/one_electron.rs (the overlap-deriv + nuclear/Rys 1e engines + the is_<op> launcher ladder).
    - crates/cintx-ops/generated/compiled_manifest.lock.json (clone the closest rank-9 1e family entry).
    - D-10 memory project_unstable_derivative_ports: component_rank too LOW silently truncates — set it to 9 exactly.
  </read_first>
  <behavior>
    - After registration, hess1e_ipip (Task 0) passes for all 4 families, cart + sph, all 9 components, atol=1e-12, NON-SQUARE block.
    - component_rank for each family == 9 (the ng[] last element).
  </behavior>
  <action>
    For each of int1e_ipip{ovlp,nuc,kin,rinv}:
    1. Add a manifest lock entry (cart + sph) cloning the closest rank-9 1e family with `component_rank=9` and the
       ng[] tuple from §HESS-01 (ipipnuc = {2,0,0,0,2,1,0,9}; read ovlp/kin/rinv ng[] from their hess.c blocks).
       Then `cargo build -p cintx-ops` to regenerate api_manifest.{rs,csv}.
    2. Add RawApiId consts in raw.rs (e.g. INT1E_IPIPOVLP_CART/_SPH, INT1E_IPIPNUC_*, INT1E_IPIPKIN_*, INT1E_IPIPRINV_*).
    3. Wire launcher dispatch in one_electron.rs on descriptor.operator_name(): ovlp/kin -> overlap-deriv engine (no Rys),
       nuc/rinv -> nuclear/Rys 1e path. Implement the gout emit by copying each family's s->gout 3x3 permutation
       VERBATIM from its hess.c block (D-09). Set the bra +2 (i_l+2) ng headroom. Hessian = first-order nabla engine
       applied twice (G1E_D_I / overlap-deriv applied to the already-derived first-order intermediate).
    4. Register spinor reps -> UnsupportedApi (D-11). NO capi variants, NO cint* wrappers.
  </action>
  <acceptance_criteria>
    - `grep -cE 'INT1E_IPIPOVLP|INT1E_IPIPNUC|INT1E_IPIPKIN|INT1E_IPIPRINV' crates/cintx-compat/src/raw.rs` returns >=8 (cart+sph for 4 families).
    - `python3 -c "import json,sys; d=json.load(open('crates/cintx-ops/generated/compiled_manifest.lock.json')); ks=[e for e in (d if isinstance(d,list) else d.get('entries',d.values())) ]" ` — confirm via: every ipip 1e entry has component_rank == "9" (manual/grep): `grep -A3 -E 'int1e_ipip(ovlp|nuc|kin|rinv)' crates/cintx-ops/generated/compiled_manifest.lock.json | grep -c '"9"'` returns >=8.
    - `cargo build -p cintx-ops` succeeds (manifest regenerates).
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p cintx-ops && grep -cqE 'INT1E_IPIPNUC' crates/cintx-compat/src/raw.rs</automated>
  </verify>
  <done>4 families registered with component_rank=9 (cart+sph), launcher emits the verbatim gout permutation, spinor -> UnsupportedApi.</done>
</task>

<task type="auto">
  <name>Task 2: Vendor FFI allowlist + parity green + oracle_covered=true + manifest-audit</name>
  <read_first>
    - 25-RESEARCH.md §HESS-01, §Standard Stack (hess.c already at build.rs:74,221 — allowlist regex ONLY, no new .file()), §Validation Architecture (sampling rates).
    - crates/cintx-oracle/build.rs (the allowlist_function regex) + crates/cintx-oracle/src/vendor_ffi.rs (safe-wrapper pattern).
    - reference_oracle_vendor_parity_invocation memory: double-gated on `--features cpu` + `CINTX_ORACLE_BUILD_VENDOR=1`.
  </read_first>
  <action>
    1. Extend the bindgen `allowlist_function` regex in build.rs to include the 4 families' cart + sph symbols
       (int1e_ipipovlp_cart/_sph, int1e_ipipnuc_*, int1e_ipipkin_*, int1e_ipiprinv_*). NO new `.file()` — hess.c already compiles.
    2. Add safe wrappers in vendor_ffi.rs for the 8 symbols.
    3. Run the vendor gate: `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu hess1e_ipip` and
       fix any component-order/rank mismatch until all 9 components match cart + sph at atol=1e-12.
    4. Flip oracle_covered=true for the 4 manifest entries; `cargo build -p cintx-ops` to regenerate; confirm
       `manifest-audit` green (lock edits auto-sync, no fixtures list).
  </action>
  <acceptance_criteria>
    - `grep -cE 'int1e_ipip(ovlp|nuc|kin|rinv)' crates/cintx-oracle/build.rs` returns >=4 (allowlist extended).
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu hess1e_ipip 2>&1 | grep -E "test result: ok"` passes.
    - `grep -A6 -E 'int1e_ipip(ovlp|nuc|kin|rinv)' crates/cintx-ops/generated/compiled_manifest.lock.json | grep -c 'oracle_covered.*true\|"oracle_covered": true'` returns >=8.
    - `cargo run -p xtask -- manifest-audit 2>&1 | grep -iE "ok|green|pass"` (or `node .claude/get-shit-done/bin/gsd-tools.cjs` per memory) passes.
  </acceptance_criteria>
  <verify>
    <automated>CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu hess1e_ipip && cargo run -p xtask -- manifest-audit</automated>
  </verify>
  <done>All 4 families byte-identical (atol=1e-12, cart+sph, 9 components, non-square block); oracle_covered=true; manifest-audit green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw eval_raw -> kernel launcher | descriptor.operator_name() selects the family; a wrong gout permutation is a transpose/truncation bug, not injection |
| kernel gout emit -> staging | component_rank must equal 9 or trailing components are silently dropped |

## STRIDE Threat Register (ASVS L1 — real surface is numerical/component correctness)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-09 | Tampering (component truncation) | component_rank cloned too low | mitigate | Set component_rank=9 (ng[] last element) per family; gate with 9-component non-square vendor parity |
| T-25-10 | Tampering (transpose) | s->gout permutation inferred not copied | mitigate | Copy each family's gout block verbatim from hess.c; gate with NON-SQUARE p x d block (D-09 Pitfall 4) |
| T-25-11 | Spoofing (silent skip) | vendor parity runs without both gate flags | mitigate | Double-gate on --features cpu + CINTX_ORACLE_BUILD_VENDOR=1; verify "test result: ok" with N>0 |
</threat_model>

<verification>
- `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu hess1e_ipip` — all 4 families green.
- `cargo run -p xtask -- manifest-audit` — green.
- `cargo build -p cintx-ops` — manifest regenerates clean.
</verification>

<success_criteria>
- int1e_ipipovlp/ipipnuc/ipipkin/ipiprinv registered (component_rank=9, cart+sph).
- Each matches vendor at atol=1e-12, all 9 components, NON-SQUARE block.
- oracle_covered=true; manifest-audit green. No capi/legacy surface.
</success_criteria>

<output>
After completion, create `.planning/phases/25-group-2-hessian-higher-order-derivatives/25-03-SUMMARY.md`.
Depends on Plans 1+2 (foundations). Confirm worktree integration with `merge-base --is-ancestor` after this wave (D-06; worktree auto-merge inconsistent).
</output>
