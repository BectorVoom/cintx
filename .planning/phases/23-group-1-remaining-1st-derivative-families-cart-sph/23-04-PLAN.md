---
phase: 23-group-1-remaining-1st-derivative-families-cart-sph
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - crates/cintx-cubecl/src/kernels/center_3c1e.rs
  - crates/cintx-compat/src/raw.rs
  - crates/cintx-ops/generated/compiled_manifest.lock.json
  - crates/cintx-oracle/build.rs
  - crates/cintx-oracle/src/vendor_ffi.rs
  - crates/cintx-oracle/tests/int3c1e_ip_parity.rs
autonomous: true
requirements: [DRV1-03]
must_haves:
  truths:
    - "int3c1e_ip1 (∇ on bra i of the 3-center OVERLAP) matches vendored libcint 6.1.3 at atol=1e-12 cart + sph, all 3 components"
    - "int3c1e_iprinv (∇ on bra i of the 3-center rinv-COULOMB integral, Rys-driven) matches vendored libcint at atol=1e-12 cart + sph"
    - "int3c1e_iprinv reuses the existing PTR_RINV_ORIG env slot (env[4..6]) — no new env plumbing (D-08)"
    - "int3c1e_ip1 has NO nroots guard (no Rys); int3c1e_iprinv fails closed (UnsupportedApi) at nroots>5 (fff → nroots 6) (D-13)"
    - "spinor representations registered but return UnsupportedApi (D-06)"
  artifacts:
    - path: "crates/cintx-cubecl/src/kernels/center_3c1e.rs"
      provides: "ip1 overlap-deriv path + a NEW Rys-driven fill_g_tensor_3c1e_nuc base + iprinv deriv path + operator dispatch"
      contains: "fill_g_tensor_3c1e_nuc"
    - path: "crates/cintx-oracle/tests/int3c1e_ip_parity.rs"
      provides: "DRV1-03 byte-identity vendor parity test (ip1 + iprinv, rank 3, non-square block) + fff fail-closed assertion"
      contains: "vendor_int3c1e_iprinv"
  key_links:
    - from: "crates/cintx-cubecl/src/kernels/center_3c1e.rs fill_g_tensor_3c1e_nuc"
      to: "libcint-master/src/g3c1e.c:192-235 CINTg3c1e_nuc (t2-folded recurrence)"
      via: "Rys t2 parameter + rys_roots_host + cr=env[PTR_RINV_ORIG]"
      pattern: "fill_g_tensor_3c1e_nuc"
    - from: "int3c1e_iprinv launcher"
      to: "OperatorEnvParams.rinv_orig / PTR_RINV_ORIG=env[4..6]"
      via: "already-plumbed env slot (Phase 21 D-08)"
      pattern: "rinv_orig"
---

<objective>
Register and implement the cluster-B 3c1e derivative pair (DRV1-03). RESEARCH overturned the
CONTEXT framing of these as a symmetric "pair": **they are NOT the same base integral** (Pitfall 1):

- **int3c1e_ip1** (overlap): ∇ on bra i of the 3-center OVERLAP (Gaussian product, NO Rys roots).
  cintx ALREADY has this base — `fill_g_tensor_3c1e` (center_3c1e.rs:555, "no Rys quadrature").
  Pure Phase-21-style reuse: build at `li+1` headroom, apply a 1e nabla on i
  (`CINTnabla1i_1e`, one_electron.rs:1864), contract 3 components (clone `contract_3c1e_ovlp`
  center_3c1e.rs:656 × 3 axes). NO nroots guard (skip the guard — Pitfall 4 says skip ONLY for
  ip1). Max-l = f (most permissive family in the phase). gout authority: int3c1e.c:133.

- **int3c1e_iprinv** (rinv-Coulomb): ∇ on bra i of the 3-center COULOMB-1/r-at-rinv-origin
  integral (`INT1E_TYPE_RINV`, Rys-quadrature-based). **cintx has NO base 3c1e-nuclear kernel** —
  this is THE ONLY genuinely-new (small) base kernel in clusters A & B (Pitfall 1, the single
  highest-risk task in the phase). Build a NEW `fill_g_tensor_3c1e_nuc` = `fill_g_tensor_3c1e`
  EXTENDED with the Rys `t2` parameter (math: g3c1e.c:192-235):
    `aijk1 = 0.5*(1 - t2)/aijk`  (overlap had 0.5/aijk, i.e. t2=0)
    `rjr0[d] = rj[d] - (rijk[d] + t2*(cr[d] - rijk[d]))`,  cr = env[PTR_RINV_ORIG]
  summed over `nrys_roots` roots; `x = aijk·dist²(rijk, cr)·tau²` (cint3c1e.c:303-327). REUSE
  the existing `rys_roots_host` (same CINTrys_roots convention per cint3c1e.c:327) — do NOT
  hand-roll a new root finder. The rinv origin comes from the ALREADY-plumbed
  `OperatorEnvParams.rinv_orig` / `PTR_RINV_ORIG = env[4..6]` (Phase 21 D-08) — NO new env
  plumbing. Charge factor dropped (single origin, `CINT3c1e_nuc_loop_nopt(..., 1, -1, ...)`).
  Then the same `li+1` headroom + 1e nabla-on-i + 3-component contraction as ip1. nroots =
  `(li+1 + lj + lk)/2 + 1`; ddd=4 ✓, fff=6 → fail-closed (D-13). Max-l = d. gout is
  byte-identical to ip1's (int3c1e.c:78) — the difference is the base, NOT the gout: do NOT
  clone ip1's kernel for iprinv.

`center_3c1e.rs` has NO operator dispatch today (launch_center_3c1e_typed:701 is scalar-only) —
ADD it. Port the new base to `#[cube]` per the CubeCL-primary constraint + cluster-C precedent
(docs/manual/Cubecl/*.md rules); the radial/Rys machinery is already device-proven in 2c2e/3c2e.

Purpose: Banks the last phase requirement (DRV1-03) and completes the new-base risk. Sequenced
after plan 03 to avoid conflicts on the shared registration files. Output: both 3c1e derivative
families registered + a vendor parity test green at atol=1e-12 + an fff fail-closed assertion.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-group-1-remaining-1st-derivative-families-cart-sph/23-CONTEXT.md
@.planning/phases/23-group-1-remaining-1st-derivative-families-cart-sph/23-RESEARCH.md
@.planning/phases/23-group-1-remaining-1st-derivative-families-cart-sph/23-PATTERNS.md
@.planning/phases/23-group-1-remaining-1st-derivative-families-cart-sph/23-VALIDATION.md

<interfaces>
<!-- Verified line numbers. -->

center_3c1e.rs:
  :16   header: "3c1e has no Rys quadrature"
  :555  fn fill_g_tensor_3c1e(...)         // OVERLAP base; extend with t2 → fill_g_tensor_3c1e_nuc
  :656  fn contract_3c1e_ovlp(g, li, lj, lk, g_size) -> Vec<f64>   // clone × 3 axes
  :701  fn launch_center_3c1e_typed<F: CintFloat>(...)   // scalar-only, NO operator dispatch

one_electron.rs:
  :1864 CINTnabla1i_1e (bra-side 1e nabla: ∂χ = -2α χ_{l+1} + l χ_{l-1})

raw.rs:
  :34/:58 PTR_RINV_ORIG = 4 (env[4..6]); :621-627 eval_raw reads rinv_orig from env (already plumbed)

planner.rs:
  :44 OperatorEnvParams.rinv_orig: Option<[f64;3]>   (already plumbed, Phase 21)

rys: rys_roots_host (the existing 2e/2c2e/3c2e Rys path — REUSE for the new nuclear base)

vendor_ffi.rs:
  :173 pub fn vendor_int3c1e_sph(...)   // 3-shell wrapper analog (3c1e is arity-3)

compiled_manifest.lock.json:
  :307-400 int3c2e_ip1 entries — clone shape; set canonical_family "3c1e", arity 3, operator "ip1"/"iprinv"

build.rs:
  :358 single allowlist_function regex — append int3c1e_ip1_{sph,cart}|int3c1e_iprinv_{sph,cart} (int3c1e.c already in source list)

libcint reference (the layout source of truth):
  g3c1e.c:41   nrys_roots = (li_ceil + lj_ceil + lk_ceil)/2 + 1
  g3c1e.c:192-235  CINTg3c1e_nuc (the t2-folded g-tensor to port)
  cint3c1e.c:267,303-340  INT1E_TYPE_RINV routing, Rys loop, cr=&env[PTR_RINV_ORIG]
  int3c1e.c:78 (iprinv gout) / :133 (ip1 gout) — byte-identical gouts; difference is int_type (0=OVLP / 1=RINV)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — vendor FFI + allowlist + int3c1e_ip parity test scaffold (failing)</name>
  <files>crates/cintx-oracle/src/vendor_ffi.rs, crates/cintx-oracle/build.rs, crates/cintx-oracle/tests/int3c1e_ip_parity.rs</files>
  <read_first>
    - crates/cintx-oracle/src/vendor_ffi.rs:173 (vendor_int3c1e_sph — 3-shell wrapper analog; 3c1e is arity-3)
    - crates/cintx-oracle/build.rs:358 (single allowlist_function regex)
    - crates/cintx-oracle/tests/one_electron_grad_both_parity.rs (cluster-C template)
    - libcint-master/src/autocode/int3c1e.c:78,133 (iprinv/ip1 gout — same 3-component ordering)
    - .planning/phases/.../23-VALIDATION.md "Manual-Only Verifications" (the iprinv fff fail-closed assertion) + the assert-rules
  </read_first>
  <action>
    1. vendor_ffi.rs — add `vendor_int3c1e_ip1_sph` / `_cart` and `vendor_int3c1e_iprinv_sph` /
       `_cart` (3-shell: `shls: &[i32; 3]`), cloning vendor_int3c1e_sph:173 body shape. NOTE:
       iprinv's vendor symbol reads `env[PTR_RINV_ORIG]` — the test must set a non-zero rinv
       origin in env[4..6] before calling (mirror the Phase-21 int1e_iprinv oracle setup).
    2. build.rs:358 — append `|int3c1e_ip1_sph|int3c1e_ip1_cart|int3c1e_iprinv_sph|int3c1e_iprinv_cart`
       (int3c1e.c already in the source list).
    3. Create `tests/int3c1e_ip_parity.rs` (covers BOTH ip1 and iprinv) by cloning
       one_electron_grad_both_parity.rs: NCOMP 9 → 3, 3-shell loop, two vendor calls. MANDATORY:
       element count == 3*n_ao_product, assert_any_nonzero on cintx AND vendor, NON-SQUARE i×j
       (and i×aux) block. For iprinv, set env[4..6] to a non-zero rinv origin. Also stub the
       fff fail-closed assertion (filled in Task 3). Tests RED until kernels land.
  </action>
  <verify>
    <automated>cargo build -p cintx-oracle --features cpu 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "vendor_int3c1e_ip1_sph\|vendor_int3c1e_iprinv_sph" crates/cintx-oracle/src/vendor_ffi.rs` is >= 2.
    - `grep -o "int3c1e_iprinv_sph" crates/cintx-oracle/build.rs | head -1` returns `int3c1e_iprinv_sph`.
    - `crates/cintx-oracle/tests/int3c1e_ip_parity.rs` exists, contains an any-nonzero assertion, a non-square shell tuple, and sets a non-zero env[4..6] rinv origin for the iprinv case.
    - `cargo build -p cintx-oracle --features cpu` exits 0.
  </acceptance_criteria>
  <done>Vendor FFI (ip1 + iprinv) + allowlist + parity scaffold exist and compile (RED).</done>
</task>

<task type="auto">
  <name>Task 2: int3c1e_ip1 overlap-derivative path + dispatch + manifest + RawApiId (DRV1-03 half 1)</name>
  <files>crates/cintx-cubecl/src/kernels/center_3c1e.rs, crates/cintx-compat/src/raw.rs, crates/cintx-ops/generated/compiled_manifest.lock.json</files>
  <read_first>
    - crates/cintx-cubecl/src/kernels/center_3c1e.rs:16 (no-Rys header), :555 (fill_g_tensor_3c1e overlap base), :656 (contract_3c1e_ovlp), :701 (launch_center_3c1e_typed — scalar-only, ADD dispatch)
    - crates/cintx-cubecl/src/kernels/one_electron.rs:1864 (CINTnabla1i_1e — 1e nabla on i)
    - crates/cintx-compat/src/raw.rs (plan-02/03 const triples — clone for INT3C1E_IP1_*)
    - crates/cintx-ops/generated/compiled_manifest.lock.json (int3c2e_ip1 entries — clone, canonical_family "3c1e", arity 3, operator "ip1", component_rank "3")
    - libcint-master/src/autocode/int3c1e.c:133 (CINTgout1e_int3c1e_ip1 — G1E_D_I, no Rys sum)
    - .planning/phases/.../23-PATTERNS.md "center_3c1e.rs — int3c1e_ip1 (overlap)"
  </read_first>
  <action>
    1. center_3c1e.rs — ADD an operator dispatch at the top of launch_center_3c1e_typed (:701),
       before the existing scalar path:
       ```
       match plan.descriptor.operator_name() {
           "ip1"    => return launch_center_3c1e_ip1::<F>(/* same args */),
           "iprinv" => return launch_center_3c1e_iprinv::<F>(/* same args */),  // Task 3
           _ => {} // fall through to existing scalar overlap path
       }
       ```
    2. Add `launch_center_3c1e_ip1`: build `fill_g_tensor_3c1e` with `li+1` headroom (li_ceil =
       li+1, ng {1,0,0,...}), apply the 1e nabla on i (CINTnabla1i_1e: -2α χ_{l+1} + l χ_{l-1}),
       contract 3 components (clone contract_3c1e_ovlp × 3 axes, i-fastest). NO nroots guard
       (overlap, no Rys — Pitfall 4: skip the guard for ip1 ONLY). Spinor reject (D-06). Port to
       `#[cube]` per cluster-C precedent (docs/manual/Cubecl/*.md). Max-l = f.
    3. raw.rs — add `INT3C1E_IP1_{CART,SPH,SPINOR}` const triple.
    4. compiled_manifest.lock.json — clone int3c1e_ip1 cart/sph/spinor with `"arity": 3,
       "canonical_family": "3c1e", "category": "3c1e", "component_rank": "3", "helper_kind":
       "operator", "oracle_covered": true`, operator "ip1". `cargo build -p cintx-ops`.
    5. Device-vs-host unit test in center_3c1e.rs `#[cfg(test)]` on a NON-SQUARE i×j block.
  </action>
  <verify>
    <automated>cargo build -p cintx-ops && cargo test -p cintx-cubecl int3c1e_ip1 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "operator_name()" crates/cintx-cubecl/src/kernels/center_3c1e.rs` returns >= 1 line (dispatch ADDED — none before).
    - `grep -c "int3c1e_ip1_cart" crates/cintx-ops/generated/compiled_manifest.lock.json` is >= 1, with `"component_rank": "3"`.
    - `grep -n "INT3C1E_IP1_SPH" crates/cintx-compat/src/raw.rs` returns 1 line.
    - The ip1 launcher has NO `nroots > 5` guard (overlap path) — confirm by reading launch_center_3c1e_ip1.
    - `cargo test -p cintx-cubecl int3c1e_ip1` prints `test result: ok.` (device == host, non-square block).
  </acceptance_criteria>
  <done>int3c1e_ip1 (overlap derivative) registered + dispatched + device==host on a non-square block; no Rys guard.</done>
</task>

<task type="auto">
  <name>Task 3: int3c1e_iprinv NEW Rys 3c1e-nuclear base + deriv + manifest/RawApiId + close oracle (DRV1-03 half 2) — highest-risk</name>
  <files>crates/cintx-cubecl/src/kernels/center_3c1e.rs, crates/cintx-compat/src/raw.rs, crates/cintx-ops/generated/compiled_manifest.lock.json, crates/cintx-oracle/tests/int3c1e_ip_parity.rs</files>
  <read_first>
    - crates/cintx-cubecl/src/kernels/center_3c1e.rs:555 (fill_g_tensor_3c1e — the OVERLAP base to EXTEND with t2; the iprinv base is fill_g_tensor_3c1e_nuc = this + t2 folding)
    - libcint-master/src/g3c1e.c:13-60 (CINTinit_int3c1e_EnvVars nroots formula :41) + :192-235 (CINTg3c1e_nuc — the t2-folded recurrence; aijk1 = .5*(1-t2)/aijk, rjr0 with cr)
    - libcint-master/src/cint3c1e.c:267,303-340 (INT1E_TYPE_RINV routing, Rys loop, x = aijk·dist²(rijk,cr)·tau², cr = &env[PTR_RINV_ORIG])
    - crates/cintx-compat/src/raw.rs:34,58,621-627 (PTR_RINV_ORIG=4, eval_raw rinv_orig read — already plumbed)
    - the existing rys_roots_host call sites in 2e/2c2e/3c2e (REUSE — same CINTrys_roots convention per cint3c1e.c:327)
    - .planning/phases/.../23-PATTERNS.md "center_3c1e.rs — int3c1e_iprinv (THE ONLY NEW BASE KERNEL, Pitfall 1)" + "No Analog Found"
    - .planning/phases/.../23-VALIDATION.md "Manual-Only Verifications" (the fff fail-closed assertion)
    - docs/manual/Cubecl/*.md (the #[cube] authoring rules — this is a device kernel)
  </read_first>
  <action>
    1. Build a NEW `fill_g_tensor_3c1e_nuc` in center_3c1e.rs = `fill_g_tensor_3c1e` (:555)
       EXTENDED with the Rys `t2` parameter, per g3c1e.c:192-235:
       - `aijk1 = 0.5 * (1 - t2) / aijk`  (the overlap base used 0.5/aijk, i.e. t2=0).
       - `rjr0[d] = rj[d] - (rijk[d] + t2 * (cr[d] - rijk[d]))`, where `cr` = the rinv origin =
         `env[PTR_RINV_ORIG..PTR_RINV_ORIG+3]` from `OperatorEnvParams.rinv_orig` (already
         plumbed, D-08 — NO new env plumbing).
       - Wrap in a Rys-root loop over `nrys_roots = (li+1 + lj + lk)/2 + 1` (g3c1e.c:41 with the
         ip headroom li_ceil=li+1), with `x = aijk · dist²(rijk, cr) · tau²` (cint3c1e.c:303-327).
       - REUSE `rys_roots_host` for the roots/weights — do NOT write a new root finder (Don't
         Hand-Roll). Charge factor dropped (single origin, fac = 1, -1 per
         CINT3c1e_nuc_loop_nopt(..., 1, -1, ...)).
       Port to `#[cube]` (radial/Rys already device-proven in 2c2e/3c2e). Follow
       docs/manual/Cubecl/*.md: no plain-fn calls, no if-expr, F::exp/F::sqrt, u32/i32 only,
       no continue/break.
    2. Add `launch_center_3c1e_iprinv`: fail-closed `if nroots > 5 { return UnsupportedApi{
       requested: format!("unsupported_nrys_roots:{}", nroots) } }` BEFORE any rys_roots_host
       call (D-13; fff → nroots 6 → reject). Spinor reject (D-06). Build the new nuclear base at
       li+1 headroom, apply the SAME 1e nabla-on-i + 3-component contraction as ip1 (do NOT clone
       ip1's overlap kernel — same gout, different base, Pitfall 1). Wire into the dispatch
       `match` added in Task 2.
    3. raw.rs — add `INT3C1E_IPRINV_{CART,SPH,SPINOR}` const triple.
    4. compiled_manifest.lock.json — clone int3c1e_iprinv cart/sph/spinor with `"arity": 3,
       "canonical_family": "3c1e", "category": "3c1e", "component_rank": "3", "helper_kind":
       "operator", "oracle_covered": true`, operator "iprinv". `cargo build -p cintx-ops`.
    5. Fill in the int3c1e_ip_parity.rs body to drive BOTH real kernels (ip1 + iprinv, iprinv
       with a non-zero env[4..6] rinv origin), run the double-gated vendor parity → 0 mismatches
       at atol=1e-12 cart + sph. Add the fff fail-closed unit assertion (VALIDATION Manual-Only):
       assert the iprinv launcher returns `UnsupportedApi` for an fff shell tuple (do not silently
       truncate).
  </action>
  <verify>
    <automated>cargo build -p cintx-ops && CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test int3c1e_ip_parity -- --test-threads=1 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "fill_g_tensor_3c1e_nuc" crates/cintx-cubecl/src/kernels/center_3c1e.rs` returns >= 1 line (the NEW Rys-driven base — distinct from the overlap fill_g_tensor_3c1e).
    - `grep -n "rys_roots_host\|rinv_orig" crates/cintx-cubecl/src/kernels/center_3c1e.rs` returns >= 1 line each (Rys reuse + the already-plumbed rinv origin).
    - `grep -c "int3c1e_iprinv_cart" crates/cintx-ops/generated/compiled_manifest.lock.json` is >= 1, with `"component_rank": "3"`.
    - `grep -n "INT3C1E_IPRINV_SPH" crates/cintx-compat/src/raw.rs` returns 1 line.
    - A unit test asserts the iprinv launcher returns `UnsupportedApi` for an fff tuple (fail-closed at nroots>5); it passes.
    - Under `CINTX_ORACLE_BUILD_VENDOR=1` + `--features cpu`, `int3c1e_ip_parity` prints `running N tests` with N>0 and `test result: ok.` (0 mismatches at atol=1e-12, cart + sph, BOTH ip1 and iprinv).
    - `crates/cintx-capi/src/shim.rs` and `crates/cintx-compat/src/legacy.rs` are UNCHANGED in this plan (D-09 / ROADMAP SC6): `git diff --name-only HEAD -- crates/cintx-capi/src/shim.rs crates/cintx-compat/src/legacy.rs` is empty.
    - `cargo run -p xtask -- manifest-audit` exits 0.
  </acceptance_criteria>
  <done>int3c1e_iprinv built on a NEW Rys-driven 3c1e-nuclear base reusing rys_roots_host + the plumbed rinv origin; both 3c1e families vendor byte-identical at atol=1e-12 cart+sph; fff fail-closed; capi/legacy untouched; manifest-audit green. Cluster B complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw eval_raw → kernel (env[PTR_RINV_ORIG]) | Pre-validated atm/bas/env f64 arrays + the rinv origin (env[4..6], already plumbed in Phase 21) cross into the device staging buffer. No NEW env slot, no network, no auth, no file parsing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04-01 | Denial of Service | int3c1e_iprinv fff shell tuple drives nroots=6 past the device Rys ceiling and panics in rys_root1..5 | mitigate | Explicit `nroots > 5` fail-closed guard returning UnsupportedApi BEFORE any rys_roots_host call (D-13); a dedicated unit test asserts the fff fail-closed path (VALIDATION Manual-Only). |
| T-23-04-02 | Tampering / Information Disclosure | Out-of-bounds env read of env[PTR_RINV_ORIG..+3] on a too-short env | mitigate | iprinv reads rinv_orig via the EXISTING Phase-21 plumbing (raw.rs:621-627) which already guards `env.len() >= PTR_RINV_ORIG + 3`; this plan adds no new env indexing. |
| T-23-04-03 | Tampering | too-low component_rank or wrong-base (cloning ip1's overlap kernel for iprinv, Pitfall 1) produces silently-wrong output | mitigate | Manifest pinned at component_rank "3" + oracle element-count==3*shape + any_nonzero (D-14); iprinv built on the distinct fill_g_tensor_3c1e_nuc Rys base (not ip1's overlap kernel); non-square block defeats a transposed layout. |

No high-severity threats: numeric kernel reusing an already-plumbed env slot; no new external surface (per security_gate).
</threat_model>

<verification>
- `cargo build -p cintx-ops` regenerates manifest clean.
- `cargo test -p cintx-cubecl int3c1e_ip1` / `int3c1e_iprinv` device-vs-host green; iprinv fff fail-closed.
- `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test int3c1e_ip_parity` → 0 mismatches, N>0 tests, ip1 + iprinv.
- `cargo run -p xtask -- manifest-audit` exits 0; capi/legacy untouched.
</verification>

<success_criteria>
- DRV1-03 (int3c1e_ip1 overlap + int3c1e_iprinv rinv-Coulomb) byte-identical to libcint 6.1.3 at atol=1e-12, cart + sph, all 3 components, non-square block.
- iprinv built on a NEW Rys-driven 3c1e-nuclear base (fill_g_tensor_3c1e_nuc) reusing rys_roots_host + the plumbed PTR_RINV_ORIG; ip1 reuses the existing overlap base (no Rys).
- iprinv fails closed at nroots>5 (fff); ip1 has no Rys guard. Spinor → UnsupportedApi.
- Registered (manifest rank 3 ×3 reps ×2 families, RawApiId, dispatch, vendor FFI, oracle); capi/legacy untouched (D-09).
- manifest-audit green.
</success_criteria>

<output>
After completion, create `.planning/phases/23-group-1-remaining-1st-derivative-families-cart-sph/23-04-SUMMARY.md`
</output>
