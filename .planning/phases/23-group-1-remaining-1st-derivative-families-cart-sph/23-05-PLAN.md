---
phase: 23-group-1-remaining-1st-derivative-families-cart-sph
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [DRV1-02]
must_haves:
  truths:
    - "int1e_ipovlpip / int1e_ipkinip / int1e_ipnucip (rank-9 both-side, shipped in commit 319d055) still match vendored libcint 6.1.3 at atol=1e-12 cart + sph — no regression from the cluster A/B work"
  artifacts:
    - path: "crates/cintx-oracle/tests/one_electron_grad_both_parity.rs"
      provides: "the EXISTING cluster-C rank-9 vendor byte-identity test (regression guard — already present, not created)"
      contains: "vendor"
  key_links:
    - from: "DRV1-02 requirement"
      to: "the existing one_electron_grad_both_parity vendor test (commit 319d055)"
      via: "re-run under the double gate as a regression guard"
      pattern: "one_electron_grad_both_parity"
---

<objective>
DRV1-02 (`int1e_ipovlpip`, `int1e_ipkinip`, `int1e_ipnucip` — rank-9 both-side 1e) is ALREADY
COMPLETE: cluster C shipped in commit `319d055`, vendor-verified byte-identical to libcint 6.1.3
(0 mismatches at atol=1e-12, cart + sph). This plan does NOT re-implement it — it is a single
regression-guard line item that RE-RUNS the existing cluster-C vendor parity test to confirm the
cluster A/B work in plans 01-04 introduced no regression, so the phase can close DRV1-02 with a
green gate on record.

`files_modified` is intentionally empty — this plan touches no source. It runs an existing test
and records the result. This satisfies the requirement-coverage audit (DRV1-02 appears in a
plan's `requirements` field) without re-doing finished work, per the critical_scope_facts.

Purpose: Close the DRV1-02 coverage loop and prove no regression. Output: a recorded green run of
`one_electron_grad_both_parity` under the double gate.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-group-1-remaining-1st-derivative-families-cart-sph/23-CONTEXT.md
@.planning/phases/23-group-1-remaining-1st-derivative-families-cart-sph/23-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run the existing cluster-C rank-9 vendor parity (DRV1-02 regression guard)</name>
  <files></files>
  <read_first>
    - crates/cintx-oracle/tests/one_electron_grad_both_parity.rs (the EXISTING cluster-C test — read to confirm it covers all three families ipovlpip/ipkinip/ipnucip at NCOMP=9, cart + sph, atol=1e-12, non-square p×d block; do NOT modify it)
    - .planning/phases/.../23-CONTEXT.md <implementation_status> (cluster C COMPLETE in commit 319d055)
  </read_first>
  <action>
    Run the existing cluster-C rank-9 both-side vendor parity test under the MANDATORY double gate
    (`--features cpu` + `CINTX_ORACLE_BUILD_VENDOR=1` — without BOTH, parity silently skips,
    Pitfall 5). Do NOT create or modify any source file. Confirm the banner shows `running N
    tests` with N>0 (NOT 0) and `test result: ok.` with 0 mismatches at atol=1e-12 for cart +
    sph across all three families (int1e_ipovlpip, int1e_ipkinip, int1e_ipnucip). Record the
    pass/fail and mismatch counts in the SUMMARY. If this test FAILS (i.e. cluster A/B work
    regressed the rank-9 1e path — e.g. via the f12.rs engine plumbing in plan 01 or the shared
    raw.rs/manifest edits), STOP and report the regression rather than masking it.
  </action>
  <verify>
    <automated>CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test one_electron_grad_both_parity -- --test-threads=1 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - The command prints `running N tests` with N>0 (the double gate is active — NOT a silent 0-test skip).
    - The command prints `test result: ok.` (0 vendor mismatches at atol=1e-12, cart + sph, all three rank-9 families).
    - No source file is modified by this plan: `git diff --name-only HEAD -- crates/` shows no change attributable to this plan's task (it runs a test only).
  </acceptance_criteria>
  <done>The existing cluster-C rank-9 vendor parity test passes under the double gate; DRV1-02 confirmed regression-free and covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan runs an existing test and modifies no source. No trust boundary is crossed or introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05-01 | (regression risk, not a security threat) | cluster A/B engine plumbing silently regresses the already-shipped rank-9 1e path | mitigate | The re-run is the guard itself; a failure halts the phase rather than shipping a regression. |

No security-relevant threats: test-execution-only plan, no source change, no input surface.
</threat_model>

<verification>
- `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test one_electron_grad_both_parity` → N>0 tests, 0 mismatches, test result: ok.
</verification>

<success_criteria>
- DRV1-02 confirmed green (no regression) via the existing cluster-C vendor parity test under the double gate.
- No source modified.
</success_criteria>

<output>
After completion, create `.planning/phases/23-group-1-remaining-1st-derivative-families-cart-sph/23-05-SUMMARY.md`
</output>
