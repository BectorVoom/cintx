---
phase: 23-group-1-remaining-1st-derivative-families-cart-sph
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - crates/cintx-cubecl/src/kernels/two_electron.rs
  - crates/cintx-cubecl/src/kernels/center_2c2e.rs
  - crates/cintx-compat/src/raw.rs
  - crates/cintx-ops/generated/compiled_manifest.lock.json
  - crates/cintx-oracle/build.rs
  - crates/cintx-oracle/src/vendor_ffi.rs
  - crates/cintx-oracle/tests/int2e_ip2_parity.rs
  - crates/cintx-oracle/tests/int2c2e_ip_parity.rs
autonomous: true
requirements: [DRV1-01, DRV1-04]
must_haves:
  truths:
    - "int2e_ip2 (∇ on the ket bra-center k) matches vendored libcint 6.1.3 at atol=1e-12 for cart + sph, all 3 components"
    - "int2c2e_ip1 (∇ on bra center i) matches vendored libcint at atol=1e-12 cart + sph"
    - "int2c2e_ip2 (∇ on ket center k) matches vendored libcint at atol=1e-12 cart + sph"
    - "each family's spinor representation is registered but returns UnsupportedApi (D-06)"
    - "each launcher fails closed (UnsupportedApi) when the elevated-headroom nroots > 5 (D-13)"
  artifacts:
    - path: "crates/cintx-oracle/tests/int2e_ip2_parity.rs"
      provides: "DRV1-01 byte-identity vendor parity test (rank 3, non-square quad)"
      contains: "vendor_int2e_ip2"
    - path: "crates/cintx-oracle/tests/int2c2e_ip_parity.rs"
      provides: "DRV1-04 byte-identity vendor parity test (ip1 + ip2)"
      contains: "vendor_int2c2e_ip1"
    - path: "crates/cintx-ops/generated/compiled_manifest.lock.json"
      provides: "int2e_ip2 / int2c2e_ip1 / int2c2e_ip2 cart/sph/spinor entries at component_rank 3"
      contains: "int2e_ip2_cart"
  key_links:
    - from: "crates/cintx-cubecl/src/kernels/two_electron.rs operator_name() dispatch"
      to: "launch_two_electron_ip2 (nabla1k_2e on k, exponent ak)"
      via: "operator_name() == \"ip2\""
      pattern: "ip2"
    - from: "crates/cintx-cubecl/src/kernels/center_2c2e.rs operator dispatch (ADDED)"
      to: "launch_center_2c2e_ip1/ip2 (nabla1i/k_2e, lj=ll=0)"
      via: "operator_name() match"
      pattern: "operator_name"
---

<objective>
Register and implement three rank-3 first-derivative families that reuse the Phase-21 2e Rys
engine (now unblocked by plan 01): `int2e_ip2` (DRV1-01) and `int2c2e_ip1` + `int2c2e_ip2`
(DRV1-04). All three are pure "existing base g-tensor at elevated headroom → existing nabla →
existing single-side contraction → D-11 registration recipe" — no new base math.

Per RESEARCH per-family findings:
- **int2e_ip2** (DRV1-01): gout `CINTgout2e_int2e_ip2` (grad2.c:101) uses `G2E_D_K` — ∇ on the
  2nd-electron bra center `k`. Clone `launch_two_electron_ip1` (two_electron.rs:1434), swap
  headroom `li+1 → lk+1` (`build_2e_shape(li, lj, lk+1, ll)`), swap the nabla to `nabla1k_2e`,
  pass exponent `ak`. Add the `"ip2"` operator branch after the existing `"ip1"` branch
  (two_electron.rs:1770). nroots = `(li + lj + (lk+1) + ll)/2 + 1`; max-l = d.
- **int2c2e_ip1** (DRV1-04): gout `CINTgout2e_int2c2e_ip1` (int3c2e.c:314) uses `G2E_D_I` with
  j-slot and l-slot = 0 (2-center: only centers i, k). ∇ on i, headroom `li+1`, exponent `ai`,
  use the parameterized contraction with center=I and `lj=ll=0`. nroots = `(li+1 + lk)/2 + 1`;
  max-l = f (the real cap is executor.rs:11 l>4, NOT nroots — do NOT cap at d).
- **int2c2e_ip2** (DRV1-04): gout `CINTgout2e_int2c2e_ip2` (int3c2e.c:392) uses `G2E_D_K`.
  ∇ on k, headroom `lk+1`, exponent `ak`, center=K, `lj=ll=0`. nroots = `(li + lk+1)/2 + 1`;
  max-l = f.

`center_2c2e.rs` has NO operator dispatch today (launch_center_2c2e_typed:608 is scalar-only) —
it MUST be ADDED (PATTERNS center_2c2e.rs assignment).

Purpose: Banks 2 of the 5 phase requirements (DRV1-01, DRV1-04) on the lowest-risk pure-reuse
families. Output: 3 manifest families (×3 reps), 2 launchers/dispatch branches, vendor FFI +
2 oracle parity tests, all green at atol=1e-12.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-group-1-remaining-1st-derivative-families-cart-sph/23-CONTEXT.md
@.planning/phases/23-group-1-remaining-1st-derivative-families-cart-sph/23-RESEARCH.md
@.planning/phases/23-group-1-remaining-1st-derivative-families-cart-sph/23-PATTERNS.md
@.planning/phases/23-group-1-remaining-1st-derivative-families-cart-sph/23-VALIDATION.md

<interfaces>
<!-- Verified line numbers. Executor uses these directly — no codebase exploration needed. -->

two_electron.rs:
  :94   pub(crate) fn build_2e_shape(li, lj, lk, ll) -> TwoEShape   (TwoEShape has .nroots)
  :381  pub(crate) fn fill_g_tensor_2e(...)
  :1434 fn launch_two_electron_ip1<F: CintFloat>(plan, li, lj, lk, ll, ri, rj, rk, rl, common_factor, staging)
  :1770 if plan.descriptor.operator_name() == "ip1" { return launch_two_electron_ip1::<F>(...); }

center_2c2e.rs:
  :608  fn launch_center_2c2e_typed<F: CintFloat>(...)   // scalar-only, NO operator dispatch
  :638  nroots formula (li + lk)/2 + 1 + MAX_DEVICE_NROOTS guard at :639-647

f12.rs (from plan 01):
  pub(crate) nabla1i_2e / nabla1j_2e / nabla1k_2e / nabla1l_2e   (i,k used here)
  pub(crate) parameterized single-side contraction (gout_ip1 generalized OR gout_ipk sibling)

raw.rs:
  :188 pub const INT1E_IPOVLPIP_CART: Self = Self::Symbol("int1e_ipovlpip_cart");  // cluster-C template

vendor_ffi.rs:
  :142 pub fn vendor_int2c2e_sph(...)            // 2-shell wrapper analog
  :859 pub fn vendor_int2e_ip1_sph(...)          // 4-shell wrapper analog (clone for int2e_ip2)

compiled_manifest.lock.json:
  :307-400 int3c2e_ip1 entries (cart :328 / sph :360 / spinor :392) — clone shape, component_rank "3"

build.rs:
  :358 .allowlist_function("...|int1e_ipovlpip_sph|int1e_ipovlpip_cart|...")  // single regex, append here
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — vendor FFI wrappers + parity test scaffolds (failing)</name>
  <files>crates/cintx-oracle/src/vendor_ffi.rs, crates/cintx-oracle/build.rs, crates/cintx-oracle/tests/int2e_ip2_parity.rs, crates/cintx-oracle/tests/int2c2e_ip_parity.rs</files>
  <read_first>
    - crates/cintx-oracle/tests/one_electron_grad_both_parity.rs (the cluster-C template: #[cfg(has_vendor_libcint)], collect_Nc_matrix, count_mismatches at ATOL=1e-12, assert_any_nonzero, vendor_parity harness)
    - crates/cintx-oracle/src/vendor_ffi.rs:142 (vendor_int2c2e_sph, 2-shell) and :859-891 (vendor_int2e_ip1_sph/_cart, 4-shell)
    - crates/cintx-oracle/build.rs:358 (the single allowlist_function regex; the cluster-C int1e_ipovlpip_* symbols are already appended there — clone that)
    - libcint-master/src/autocode/grad2.c:101 (CINTgout2e_int2e_ip2 — 3-component i-fastest ordering authority)
    - libcint-master/src/autocode/int3c2e.c:314,392 (CINTgout2e_int2c2e_ip1/ip2 ordering authority)
    - .planning/phases/.../23-VALIDATION.md (the assert-rules: element-count = 3*shape, any_nonzero, NON-SQUARE block)
  </read_first>
  <action>
    1. vendor_ffi.rs — add safe wrappers (clone the null-opt/null-cache body shape from
       vendor_int2e_ip1_sph:859 for arity-4, vendor_int2c2e_sph:142 for arity-2):
         `vendor_int2e_ip2_sph` / `vendor_int2e_ip2_cart`   (shls: &[i32; 4])
         `vendor_int2c2e_ip1_sph` / `vendor_int2c2e_ip1_cart` (shls: &[i32; 2])
         `vendor_int2c2e_ip2_sph` / `vendor_int2c2e_ip2_cart` (shls: &[i32; 2])
       Body calls `ffi::int2e_ip2_sph(out.as_mut_ptr(), ptr::null_mut(), shls.as_ptr() as *mut i32,
       atm..., natm, bas..., nbas, env... as *mut f64, ptr::null_mut(), ptr::null_mut())` etc.
    2. build.rs:358 — append to the single allowlist_function regex (after the existing
       `...|int1e_ipnucip_cart`):
       `|int2e_ip2_sph|int2e_ip2_cart|int2c2e_ip1_sph|int2c2e_ip1_cart|int2c2e_ip2_sph|int2c2e_ip2_cart`
       NO new .c file — grad2.c and int3c2e.c are already in the build.rs source list.
    3. Create `tests/int2e_ip2_parity.rs` and `tests/int2c2e_ip_parity.rs` by cloning
       one_electron_grad_both_parity.rs. Change NCOMP 9 → 3, the shell loop to the family's
       arity (4 shells for int2e_ip2; 2 shells for int2c2e), and the vendor call to the new
       wrapper. The component-leading re-layout reads `out[comp * n_product + n]`. MANDATORY
       asserts (VALIDATION.md): (a) observed element count == 3 * n_ao_product, (b)
       assert_any_nonzero on BOTH cintx and vendor outputs, (c) use a NON-SQUARE block (p×d /
       p-d-s-s style quad — a square block is transpose-symmetric and hides axis bugs). At this
       Wave-0 stage the kernel is not implemented yet, so the test compiles and FAILS (or skips
       with the #[cfg] gate off) — that is correct: it is the load-bearing RED test.
    Use the same `#[cfg(has_vendor_libcint)]` gate + the double-gate doc comment
    (`--features cpu` + `CINTX_ORACLE_BUILD_VENDOR=1`) as the cluster-C test.
  </action>
  <verify>
    <automated>cargo build -p cintx-oracle --features cpu 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "vendor_int2e_ip2_sph\|vendor_int2e_ip2_cart\|vendor_int2c2e_ip1_sph\|vendor_int2c2e_ip2_sph" crates/cintx-oracle/src/vendor_ffi.rs` is >= 4.
    - `grep -o "int2e_ip2_sph" crates/cintx-oracle/build.rs | head -1` returns `int2e_ip2_sph`.
    - Both `crates/cintx-oracle/tests/int2e_ip2_parity.rs` and `crates/cintx-oracle/tests/int2c2e_ip_parity.rs` exist and contain `assert_any_nonzero` (or an equivalent any-nonzero assertion) and a non-square shell tuple.
    - `cargo build -p cintx-oracle --features cpu` exits 0 (FFI symbols bind; tests compile).
  </acceptance_criteria>
  <done>Vendor FFI + allowlist + 2 parity test files exist and compile; tests are RED pending kernels.</done>
</task>

<task type="auto">
  <name>Task 2: int2e_ip2 launcher + dispatch + manifest + RawApiId (DRV1-01)</name>
  <files>crates/cintx-cubecl/src/kernels/two_electron.rs, crates/cintx-compat/src/raw.rs, crates/cintx-ops/generated/compiled_manifest.lock.json</files>
  <read_first>
    - crates/cintx-cubecl/src/kernels/two_electron.rs:1434-1475 (launch_two_electron_ip1 prologue: spinor reject, build_2e_shape(li+1,...), nroots>5 guard) and :1770 (ip1 dispatch branch)
    - crates/cintx-cubecl/src/kernels/f12.rs (plan 01: pub(crate) nabla1k_2e + parameterized contraction)
    - crates/cintx-compat/src/raw.rs:188-190 (INT1E_IPOVLPIP_* const triple — clone shape)
    - crates/cintx-ops/generated/compiled_manifest.lock.json:307-400 (int3c2e_ip1 cart/sph/spinor entries — clone, set family/operator/symbol/canonical_family/arity, component_rank "3")
    - libcint-master/src/autocode/grad2.c:101 (CINTgout2e_int2e_ip2 — G2E_D_K, component order authority)
    - .planning/phases/.../23-PATTERNS.md "two_electron.rs — int2e_ip2" assignment (the exact dispatch + prologue diff)
  </read_first>
  <action>
    1. Add `fn launch_two_electron_ip2<F: CintFloat>(plan, li, lj, lk, ll, ri, rj, rk, rl,
       common_factor, staging)` cloning launch_two_electron_ip1 (:1434) with these swaps:
       - spinor reject message → "spinor int2e_ip2 gradient" (D-06 escape hatch unchanged).
       - headroom: `build_2e_shape(li as usize, lj as usize, lk as usize + 1, ll as usize)`
         (the `+1` goes on the KET bra-center k, NOT i).
       - fail-closed: keep the `if grad_shape.nroots > 5 { return Err(UnsupportedApi{ requested:
         format!("unsupported_nrys_roots:{}", grad_shape.nroots) }); }` guard (D-13).
       - the contraction: use plan 01's parameterized single-side contraction with center=K and
         exponent `ak` (the k-shell exponent), applying `nabla1k_2e`. Mixing is identical to ip1
         (s[0]=g1x·g0y·g0z, s[1]=g0x·g1y·g0z, s[2]=g0x·g0y·g1z, i-fastest) per grad2.c:101.
    2. two_electron.rs:1770 — ADD immediately after the existing `"ip1"` branch:
       `if plan.descriptor.operator_name() == "ip2" { return launch_two_electron_ip2::<F>(plan, li, lj, lk, ll, ri, rj, rk, rl, common_factor, staging); }`
    3. raw.rs — add the const triple after the cluster-C consts:
       `pub const INT2E_IP2_CART: Self = Self::Symbol("int2e_ip2_cart");`
       `pub const INT2E_IP2_SPH: Self = Self::Symbol("int2e_ip2_sph");`
       `pub const INT2E_IP2_SPINOR: Self = Self::Symbol("int2e_ip2_spinor");`
    4. compiled_manifest.lock.json — clone the int3c2e_ip1 cart/sph/spinor entries to
       int2e_ip2 with `"arity": 4, "canonical_family": "2e", "category": "2e",
       "component_rank": "3", "helper_kind": "operator", "oracle_covered": true,
       "id": { "family": "2e", "operator": "ip2", "representation": <rep>, "symbol":
       "int2e_ip2_<rep>" }, "stability": "stable"`. Then `cargo build -p cintx-ops` to
       regenerate api_manifest.{rs,csv} (no separate fixtures list — D-11 KEY FINDING).
    5. Add a device-vs-host unit test in two_electron.rs `#[cfg(test)]` cloning the cluster-C
       `test_device_*_matches_host_reference` pattern for int2e_ip2 on a NON-SQUARE quad.
  </action>
  <verify>
    <automated>cargo build -p cintx-ops && cargo test -p cintx-cubecl int2e_ip2 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "int2e_ip2_cart\|int2e_ip2_sph\|int2e_ip2_spinor" crates/cintx-ops/generated/compiled_manifest.lock.json` is >= 3.
    - `grep -v '^#' crates/cintx-ops/generated/compiled_manifest.lock.json | grep -c '"int2e_ip2_cart"'` is >= 1, and its entry has `"component_rank": "3"` (D-14 — rank must be 3, not 1).
    - `grep -n "INT2E_IP2_SPH" crates/cintx-compat/src/raw.rs` returns 1 line.
    - `grep -n "operator_name() == \"ip2\"" crates/cintx-cubecl/src/kernels/two_electron.rs` returns 1 line.
    - `cargo build -p cintx-ops` exits 0 (manifest regenerates).
    - `cargo test -p cintx-cubecl int2e_ip2` prints `test result: ok.` (device == host on a non-square quad).
  </acceptance_criteria>
  <done>int2e_ip2 is registered (manifest rank 3 ×3 reps, RawApiId, dispatch) and the device kernel matches the host reference on a non-square quad.</done>
</task>

<task type="auto">
  <name>Task 3: int2c2e_ip1/ip2 launcher + dispatch + manifest + RawApiId + close oracle (DRV1-04)</name>
  <files>crates/cintx-cubecl/src/kernels/center_2c2e.rs, crates/cintx-compat/src/raw.rs, crates/cintx-ops/generated/compiled_manifest.lock.json, crates/cintx-oracle/tests/int2e_ip2_parity.rs, crates/cintx-oracle/tests/int2c2e_ip_parity.rs</files>
  <read_first>
    - crates/cintx-cubecl/src/kernels/center_2c2e.rs:608 (launch_center_2c2e_typed — scalar-only, NO operator dispatch; the shell/canonical_family guards at :614-630) and :638-647 (nroots (li+lk)/2+1 + MAX_DEVICE_NROOTS guard)
    - crates/cintx-cubecl/src/kernels/f12.rs (plan 01: nabla1i_2e/nabla1k_2e + parameterized contraction; 2c2e is 2e-style Rys so the engine applies with lj=ll=0 — see RESEARCH assumption A1)
    - libcint-master/src/autocode/int3c2e.c:314 (int2c2e_ip1, G2E_D_I, j=l=0) and :392 (int2c2e_ip2, G2E_D_K)
    - crates/cintx-ops/generated/compiled_manifest.lock.json (the int2e_ip2 entries just added — clone for int2c2e, arity 2, canonical_family "2c2e")
    - .planning/phases/.../23-PATTERNS.md "center_2c2e.rs" assignment (dispatch must be ADDED before the scalar path)
  </read_first>
  <action>
    1. center_2c2e.rs — ADD an operator dispatch at the top of launch_center_2c2e_typed (after
       the canonical_family/shell-count guards ~:614-630, BEFORE the existing scalar path):
       ```
       match plan.descriptor.operator_name() {
           "ip1" => return launch_center_2c2e_ip1::<F>(/* same args */),
           "ip2" => return launch_center_2c2e_ip2::<F>(/* same args */),
           _ => {} // fall through to existing scalar path
       }
       ```
    2. Add `launch_center_2c2e_ip1` and `launch_center_2c2e_ip2`. Clone the scalar
       `center_2c2e_scalar_kernel` g-tensor build (2e-style Rys), then:
       - ip1: headroom on i (`li+1`), apply the parameterized contraction with center=I,
         exponent `ai`, `lj = ll = 0` (2-center). nroots = `(li+1 + lk)/2 + 1` — keep the
         existing MAX_DEVICE_NROOTS / `> 5` fail-closed guard (D-13).
       - ip2: headroom on k (`lk+1`), center=K, exponent `ak`, `lj = ll = 0`.
         nroots = `(li + lk+1)/2 + 1`.
       - Spinor reject (D-06) at the top of each.
       Max-l = f within ceiling — do NOT artificially cap at d (RESEARCH State-of-the-Art:
       the real cap is the executor.rs:11 l>4 gate). Verify the 2c2e g-tensor strides match the
       2e shape the nabla expects (A1); if they differ, adjust the stride arg, do not change the nabla.
    3. raw.rs — add `INT2C2E_IP1_{CART,SPH,SPINOR}` and `INT2C2E_IP2_{CART,SPH,SPINOR}` const
       triples (clone the cluster-C shape).
    4. compiled_manifest.lock.json — clone int2c2e_ip1 + int2c2e_ip2 cart/sph/spinor entries
       with `"arity": 2, "canonical_family": "2c2e", "category": "2c2e", "component_rank": "3",
       "helper_kind": "operator", "oracle_covered": true`, operator "ip1"/"ip2". `cargo build -p cintx-ops`.
    5. Fill in the device-vs-host body of both parity tests so they exercise the real kernels,
       then run the double-gated vendor parity (int2e_ip2 + int2c2e_ip) and confirm 0 mismatches
       at atol=1e-12 for cart + sph, every component, on a NON-SQUARE block. Flip nothing extra —
       oracle_covered is already true in the manifest clone.
  </action>
  <verify>
    <automated>cargo build -p cintx-ops && CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test int2c2e_ip_parity --test int2e_ip2_parity -- --test-threads=1 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "operator_name()" crates/cintx-cubecl/src/kernels/center_2c2e.rs` returns >= 1 line (dispatch ADDED — there was none before).
    - `grep -c "int2c2e_ip1_cart\|int2c2e_ip2_cart" crates/cintx-ops/generated/compiled_manifest.lock.json` is >= 2, each with `"component_rank": "3"`.
    - `grep -n "INT2C2E_IP1_SPH\|INT2C2E_IP2_SPH" crates/cintx-compat/src/raw.rs` returns 2 lines.
    - Under `CINTX_ORACLE_BUILD_VENDOR=1` + `--features cpu`, `int2e_ip2_parity` and `int2c2e_ip_parity` each print `running N tests` with N>0 (NOT 0 — Pitfall 5) and `test result: ok.` (0 mismatches at atol=1e-12, cart + sph).
    - `cargo run -p xtask -- manifest-audit` (no flags) exits 0.
  </acceptance_criteria>
  <done>int2c2e_ip1/ip2 registered + dispatched; all three families (int2e_ip2, int2c2e_ip1, int2c2e_ip2) pass vendor byte-identity at atol=1e-12 cart+sph; manifest-audit green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw eval_raw → kernel | Pre-validated atm/bas/env f64 arrays cross into the device staging buffer. No new external input surface; no network/auth/file parsing; no new env slot. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02-01 | Tampering / Information Disclosure | Out-of-bounds device staging write from a too-low component_rank or wrong headroom size | mitigate | Manifest entries pinned at component_rank "3"; each oracle test asserts element count == 3*n_ao_product AND any_nonzero (D-14), catching a short/truncated buffer before it can pass parity. |
| T-23-02-02 | Denial of Service | High angular-momentum quartet drives nroots past the device Rys ceiling and panics in rys_root1..5 | mitigate | Each launcher computes grad_shape.nroots and returns UnsupportedApi fail-closed BEFORE any rys_roots_host call (D-13); spinor reps reject early (D-06). |

No high-severity threats: numeric kernels on already-validated inputs, no new attack surface (per security_gate).
</threat_model>

<verification>
- `cargo build -p cintx-ops` regenerates the manifest clean.
- `cargo test -p cintx-cubecl int2e_ip2` / `int2c2e` device-vs-host green.
- `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test int2e_ip2_parity --test int2c2e_ip_parity` → 0 mismatches, N>0 tests.
- `cargo run -p xtask -- manifest-audit` exits 0.
</verification>

<success_criteria>
- DRV1-01 (int2e_ip2) and DRV1-04 (int2c2e_ip1 + int2c2e_ip2) byte-identical to libcint 6.1.3 at atol=1e-12, cart + sph, all 3 components, on a non-square block.
- All three families registered (manifest rank 3 ×3 reps, RawApiId, launcher dispatch, vendor FFI, oracle test).
- Spinor reps return UnsupportedApi; nroots>5 fails closed.
- manifest-audit green.
</success_criteria>

<output>
After completion, create `.planning/phases/23-group-1-remaining-1st-derivative-families-cart-sph/23-02-SUMMARY.md`
</output>
