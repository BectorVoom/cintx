---
phase: 20-precision-generic-f64-f32-switch
plan: 05
type: execute
wave: 4
depends_on: ["20-04"]
files_modified:
  - crates/cintx-cubecl/src/kernels/center_3c1e.rs
  - crates/cintx-cubecl/src/kernels/center_3c2e.rs
  - crates/cintx-cubecl/src/kernels/center_4c1e.rs
  - crates/cintx-cubecl/src/kernels/f12.rs
autonomous: true
requirements: [PREC-01, PREC-02, PREC-07]
must_haves:
  truths:
    - "launch_center_3c1e, launch_center_3c2e, launch_center_4c1e, launch_f12 keep their FamilyLaunchFn signatures but dispatch on plan.precision to a generic _typed::<F>() inner"
    - "Each launcher casts the &mut [f64] staging to &mut [F] via bytemuck on the F32 arm (the Plan 04 dispatcher pattern)"
    - "The f64 monomorphization of these kernels is byte-identical to today (Phase 10/11/13 oracle gates pass unchanged)"
    - "f12_zeta stays Option<f64> on ExecutionOptions and is cast to F only at the kernel boundary (env stays f64 per D-06)"
  artifacts:
    - path: "crates/cintx-cubecl/src/kernels/f12.rs"
      provides: "precision-dispatched f12 launcher with generic inner"
      contains: "PrecisionKind"
    - path: "crates/cintx-cubecl/src/kernels/center_3c2e.rs"
      provides: "precision-dispatched 3c2e launcher"
      contains: "PrecisionKind"
  key_links:
    - from: "crates/cintx-cubecl/src/kernels/center_3c2e.rs"
      to: "crates/cintx-cubecl/src/math/rys.rs"
      via: "rys_root*::<F> Rys quadrature calls"
      pattern: "rys_root|::<F>"
    - from: "crates/cintx-cubecl/src/kernels/f12.rs"
      to: "ExecutionOptions.f12_zeta (Option<f64>)"
      via: "from_f64_lossy cast at kernel boundary"
      pattern: "f12_zeta|from_f64_lossy"
---

<objective>
Wave 2 (kernel launchers, group B): genericize the remaining multi-center and geminal kernel launchers — 3c1e, 3c2e, 4c1e, and f12 — by copying the precision-dispatcher pattern established in Plan 04 (outer registered launcher → `match plan.precision` → generic `_typed::<F>()` inner + bytemuck staging cast).

Purpose: These four launchers complete the kernel-side genericization. They all consume the now-generic Rys quadrature (Plan 03) and shared math (Plan 02). The f12 launcher additionally reads the physics parameter `f12_zeta` from `ExecutionOptions`, which STAYS `Option<f64>` (env-side, D-06) and is cast to `F` only at the kernel boundary.

Output: 3c1e/3c2e/4c1e/f12 launchers with precision-dispatching outers + generic `_typed::<F>()` inners and the bytemuck cast on the F32 arm. f64 monomorphization byte-identical (Phase 10/11/13 oracle gates unaffected). After this plan, every compute-path kernel is precision-aware.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-precision-generic-f64-f32-switch/20-CONTEXT.md
@.planning/phases/20-precision-generic-f64-f32-switch/20-PATTERNS.md
@.planning/phases/20-precision-generic-f64-f32-switch/20-RESEARCH.md
@.planning/phases/20-precision-generic-f64-f32-switch/20-04-SUMMARY.md

<interfaces>
<!-- The dispatcher pattern is fully specified in 20-04-SUMMARY.md. Copy it. -->

The precision-dispatcher pattern (from Plan 04): keep the registered `pub fn launch_<family>(backend, plan, specialization, staging: &mut [f64]) -> Result<ExecutionStats, cintxRsError>` signature; extract a generic `fn launch_<family>_typed<F: Float>(backend, plan, specialization, staging: &mut [F]) -> ...` inner; outer dispatches `match plan.precision { F64 => _typed::<f64>(.., staging), F32 => { let s: &mut [f32] = bytemuck::cast_slice_mut(staging); _typed::<f32>(.., s) } }`.

Registry (mod.rs:26-37): "3c1e"→launch_center_3c1e, "3c2e"→launch_center_3c2e, "4c1e"→launch_center_4c1e (with-4c1e gated), "f12"→launch_f12. KEEP all `as FamilyLaunchFn` casts compiling — do NOT change launcher signatures.

f12 zeta plumbing (STATE.md Phase 13): `OperatorEnvParams` defaults all-None in `ExecutionPlan::new()`; callers populate `f12_zeta` from env[9]. `launch_f12` passes 'f12' to `validate_f12_env_params` and strips 'stg'/'yp' prefix for the variant suffix. F12 canonical_family is 'f12' (routes via resolve_family_name). KEEP `f12_zeta: Option<f64>` (Open Question 3 resolution: env param stays f64, cast at kernel boundary via `from_f64_lossy`).

STATE.md algorithm decisions to PRESERVE verbatim (only float type changes): 3c2e li>=lj canonicalization + transpose-back (Phase 10-04); g_alloc `(dli*dlj*dlk).max(dli*vrr_nmax)` formula; 4c1e polynomial-recurrence G-tensor within the Validated4C1E envelope (Phase 11); f12 STG/YP weight post-processing + nabla1i_2e ceil angular momenta (Phase 13).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Precision-dispatch launch_center_3c1e and launch_center_3c2e</name>
  <files>crates/cintx-cubecl/src/kernels/center_3c1e.rs, crates/cintx-cubecl/src/kernels/center_3c2e.rs</files>
  <read_first>
    - crates/cintx-cubecl/src/kernels/center_3c1e.rs (entire launcher)
    - crates/cintx-cubecl/src/kernels/center_3c2e.rs (entire launcher — li>=lj canonicalization + transpose-back)
    - crates/cintx-cubecl/src/kernels/one_electron.rs (the proven dispatcher shape from Plan 04 — copy it)
    - .planning/phases/20-precision-generic-f64-f32-switch/20-04-SUMMARY.md (the dispatcher pattern)
    - .planning/phases/20-precision-generic-f64-f32-switch/20-PATTERNS.md (§kernels GENERICIZE)
    - STATE.md Phase 10 3c1e/3c2e decisions (canonicalization, g_alloc formula)
  </read_first>
  <behavior>
    - Test: launch_center_3c1e at f64 byte-identical to baseline (center_3c1e_parity)
    - Test: launch_center_3c2e at f64 byte-identical to baseline (center_3c2e_parity)
    - Test: F32 path runs without panic for both
  </behavior>
  <action>
    Apply the Plan-04 dispatcher pattern to `launch_center_3c1e` and `launch_center_3c2e` via serena (`replace_symbol_body` on the outer, `insert_after_symbol` for the `_typed` inner). Keep registered signatures; extract generic `_typed<F: Float>` inners with `Array<F>` device arrays and `::<F>` math/Rys calls (const-injected `F` params); dispatch on `plan.precision` with bytemuck cast on the F32 arm. Preserve the 3c2e li>=lj canonicalization + transpose-back-to-caller-shell-order and the `g_alloc` MAX formula verbatim. Keep the per-symbol nonzero sentinel.
  </action>
  <verify>
    <automated>CINTX_BACKEND=cpu cargo test -p cintx-oracle --features cpu center_3c1e center_3c2e 2>&1 | tail -25 && cargo check -p cintx-cubecl --features cpu 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Both launchers retain their `&mut [f64]` registered signatures; the `as FamilyLaunchFn` casts in mod.rs compile unchanged
    - Each contains a `_typed<F: Float>` inner + `match plan.precision` dispatcher; `grep -l "PrecisionKind::F32" crates/cintx-cubecl/src/kernels/center_3c1e.rs crates/cintx-cubecl/src/kernels/center_3c2e.rs` lists both
    - `CINTX_BACKEND=cpu cargo test -p cintx-oracle --features cpu center_3c1e center_3c2e` passes at atol=1e-12 (f64 byte-identity)
    - `cargo check -p cintx-cubecl --features cpu` exits 0
  </acceptance_criteria>
  <done>3c1e and 3c2e launchers dispatch on plan.precision to generic _typed::<F> inners; f64 byte-identical; canonicalization preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Precision-dispatch launch_center_4c1e and launch_f12 (with f12_zeta boundary cast)</name>
  <files>crates/cintx-cubecl/src/kernels/center_4c1e.rs, crates/cintx-cubecl/src/kernels/f12.rs</files>
  <read_first>
    - crates/cintx-cubecl/src/kernels/center_4c1e.rs (entire launcher — Validated4C1E envelope, with-4c1e gate)
    - crates/cintx-cubecl/src/kernels/f12.rs (entire launcher — STG/YP weight post-processing, f12_zeta read, validate_f12_env_params)
    - crates/cintx-cubecl/src/kernels/one_electron.rs (the proven dispatcher shape from Plan 04)
    - .planning/phases/20-precision-generic-f64-f32-switch/20-04-SUMMARY.md (dispatcher pattern)
    - .planning/phases/20-precision-generic-f64-f32-switch/20-RESEARCH.md (Open Question 3 — f12_zeta stays Option<f64>)
    - STATE.md Phase 11 4c1e + Phase 13 f12 decisions
  </read_first>
  <behavior>
    - Test: launch_f12 base STG variant at f64 byte-identical to baseline (f12_oracle_parity, atol=1e-12)
    - Test: launch_center_4c1e at f64 byte-identical to baseline (with-4c1e gated test)
    - Test: F32 path runs without panic; zeta=0 rejection still fires (validate_f12_env_params unchanged)
  </behavior>
  <action>
    Apply the Plan-04 dispatcher pattern to `launch_center_4c1e` and `launch_f12` via serena. Keep registered signatures and the `#[cfg(feature = "with-4c1e")]` gating on the 4c1e launcher; extract generic `_typed<F: Float>` inners; dispatch on `plan.precision` with the bytemuck F32 cast. For f12: KEEP `f12_zeta: Option<f64>` on `ExecutionOptions`/`OperatorEnvParams` (env stays f64, D-06 / Open Q3) — inside the `_typed::<F>` inner, cast the zeta to `F` via `F::from_f64_lossy(zeta)` at the point it enters the kernel math. Preserve the STG vs YP weight post-processing, the operator_name prefix-strip, `validate_f12_env_params('f12', ...)`, the zeta=0 rejection, and `nabla1i_2e` ceil-angular-momentum headroom verbatim. Preserve the 4c1e polynomial-recurrence G-tensor within the Validated4C1E envelope.
  </action>
  <verify>
    <automated>CINTX_BACKEND=cpu cargo test -p cintx-oracle --features cpu,with-f12 f12 2>&1 | tail -20 && CINTX_BACKEND=cpu cargo test -p cintx-oracle --features cpu,with-4c1e center_4c1e 2>&1 | tail -15 && cargo check -p cintx-cubecl --features cpu 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `launch_center_4c1e` and `launch_f12` retain registered signatures + `with-4c1e`/`with-f12` gating; each contains a `_typed<F: Float>` inner + `match plan.precision` dispatcher
    - `f12_zeta` remains `Option<f64>` on ExecutionOptions/OperatorEnvParams (env stays f64); `grep -n "f12_zeta" crates/cintx-cubecl/src/kernels/f12.rs` shows `from_f64_lossy` at the cast point
    - `CINTX_BACKEND=cpu cargo test -p cintx-oracle --features cpu,with-f12 f12` passes at atol=1e-12; the with-4c1e gated test passes at atol=1e-12 (f64 byte-identity)
    - the zeta=0 rejection test still fails closed (validate_f12_env_params unchanged)
    - `cargo check -p cintx-cubecl --features cpu` exits 0
  </acceptance_criteria>
  <done>4c1e and f12 launchers dispatch on plan.precision to generic _typed::<F> inners; f12_zeta stays Option<f64> cast to F at the kernel boundary; f64 byte-identical; STG/YP and Validated4C1E behavior preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `&mut [f64]` staging ↔ `&mut [F]` typed view (bytemuck) | Internal reinterpretation; risk is memory-safety on a mis-sized buffer, not external attack. |
| f12_zeta (f64 env param) ↔ generic `F` kernel math | Internal numeric conversion at the kernel boundary; D-06 keeps env f64. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-13 | Tampering / Elevation | bytemuck staging cast on 3c1e/3c2e/4c1e/f12 F32 arms | mitigate | Reuses the Plan-01-proven cast + Plan-06 byte-sizing; F32-arm only; per-symbol nonzero sentinel + oracle parity catch mis-sized writes; F64 arm performs zero cast. |
| T-20-14 | Tampering | f12_zeta retyped to Option<F> (would break env ABI contract) | mitigate | f12_zeta stays Option<f64> (D-06 / Open Q3); cast to F only at the kernel boundary via from_f64_lossy; acceptance criteria assert the field type is unchanged. |
| T-20-15 | Tampering | accidental genericization breaking with-4c1e / with-f12 cfg gates | mitigate | cfg gating preserved verbatim; serena symbol-scoped edits; the gated oracle tests guard regression. |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No new packages; deps already in Cargo.lock (RESEARCH §Package Legitimacy Audit). |
</threat_model>

<verification>
- `CINTX_BACKEND=cpu cargo test -p cintx-oracle --features cpu center_3c1e center_3c2e` green at atol=1e-12.
- `CINTX_BACKEND=cpu cargo test -p cintx-oracle --features cpu,with-f12 f12` and `--features cpu,with-4c1e center_4c1e` green at atol=1e-12.
- `cargo check -p cintx-cubecl --features cpu` exits 0.
- Per-task commit: `CINTX_BACKEND=cpu cargo check --workspace --features cpu` exits 0.
</verification>

<success_criteria>
- 3c1e, 3c2e, 4c1e, f12 launchers keep their FamilyLaunchFn signatures, dispatch on plan.precision to generic _typed::<F> inners, cast staging via bytemuck on the F32 arm.
- f12_zeta stays Option<f64>, cast to F at the kernel boundary (env stays f64, D-06).
- f64 monomorphization byte-identical (Phase 10/11/13 oracle gates pass unchanged).
- with-4c1e / with-f12 cfg gating preserved.
- All edits via serena symbol tools (D-11). After this plan, every compute-path kernel is precision-aware.
</success_criteria>

<output>
Create `.planning/phases/20-precision-generic-f64-f32-switch/20-05-SUMMARY.md` when done. Confirm all seven family launchers (1e/2e/2c2e/3c1e/3c2e/4c1e/f12) now dispatch on precision, and note any observed f32 error magnitudes per family (input to Wave 5 floors).
</output>
