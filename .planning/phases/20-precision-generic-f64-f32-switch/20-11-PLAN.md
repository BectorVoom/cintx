---
phase: 20-precision-generic-f64-f32-switch
plan: 11
type: execute
wave: 2
depends_on: ["20-10"]
files_modified:
  - crates/cintx-oracle/tests/f32_parity.rs
  - crates/cintx-oracle/src/compare.rs
autonomous: true
requirements: [PREC-05, PREC-04, PREC-07]
gap_closure: true
must_haves:
  truths:
    - "PREC-05: the f32 oracle gate now covers at least one MULTI-COMPONENT operator AND one F12 DERIVATIVE operator (the exact cases CR-01/CR-02 would have corrupted), verified against libcint at the per-family f32 rtol"
    - "The new f32 tests are vendor-gated and assert against libcint via compare::f32_tolerance_for_family for the relevant family"
    - "The new tests FAIL against the pre-20-10 code (silent f32 corruption) and PASS after the 20-10 fixes — confirming the fixes are load-bearing"
    - "The FROZEN f64 oracle gate (UNIFIED_ATOL=1e-12, tolerance_for_family) stays byte-identical and green (PREC-04 / D-08)"
  artifacts:
    - path: "crates/cintx-oracle/tests/f32_parity.rs"
      provides: "new multi-component + f12-derivative f32 parity tests driving evaluate::<f32>()"
      contains: "stg_ip1"
    - path: "crates/cintx-oracle/src/compare.rs"
      provides: "f32 per-family tolerance floor for the f12/derivative families (extends the existing f32_tolerance_for_family)"
      contains: "f32_tolerance_for_family"
  key_links:
    - from: "crates/cintx-oracle/tests/f32_parity.rs (new tests)"
      to: "cintx_rs SessionRequest::evaluate_generic::<f32>"
      via: "f32 evaluate over a multi-component / f12-derivative operator"
      pattern: "evaluate_generic::<f32>|evaluate::<f32>"
    - from: "crates/cintx-oracle/tests/f32_parity.rs (new tests)"
      to: "cintx_oracle::vendor_ffi::vendor_int2e_stg_ip1_sph"
      via: "f64 libcint reference for the derivative operator"
      pattern: "vendor_int2e_stg_ip1"
---

<objective>
GAP 2 closure (PREC-05) — the missing oracle coverage. Plan 20-10 fixed CR-01/CR-02 (the unsound f32 staging length contract) for multi-component / f12-derivative outputs. This plan adds the vendor-gated `f32_parity.rs` tests that PROVE the fixes: at least one MULTI-COMPONENT operator and one F12 DERIVATIVE operator — the exact cases the pre-fix code corrupted silently. These tests must FAIL against pre-20-10 code and PASS after, making the f32 oracle gate genuinely cover the regime VERIFICATION.md flagged as untested.

Purpose: after this plan, the verification truth "PREC-05: f32 path has a separate oracle gate verified against libcint" is TRUE for the full regime (not just base-family scalar cases). The f64 byte-identity gate is FROZEN and untouched (PREC-04 / D-08).

Output: new f32 parity tests in `tests/f32_parity.rs` for an f12 derivative operator (`int2e_stg_ip1_sph`, ncomp=3 — the canonical multi-component derivative whose `staging_elements = base*3 > chunk_len` triggered CR-01/CR-02), plus per-family f32 floors for those families in `f32_tolerance_for_family` (empirically derived). The f12 vendor derivative reference (`vendor_int2e_stg_ip1_sph`) already exists in the oracle crate.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-precision-generic-f64-f32-switch/20-CONTEXT.md
@.planning/phases/20-precision-generic-f64-f32-switch/20-VERIFICATION.md
@.planning/phases/20-precision-generic-f64-f32-switch/20-10-SUMMARY.md
@.planning/phases/20-precision-generic-f64-f32-switch/20-08-SUMMARY.md

<interfaces>
<!-- Concrete current oracle structures (verified by direct read 2026-05-21). Read in full before editing. -->

THE MULTI-COMPONENT / DERIVATIVE OPERATORS (the CR-01/CR-02 corruption cases — verified):
- f12 derivative variants have ncomp > 1 (cintx-cubecl/src/kernels/f12.rs:73-77): `int2e_stg_ip1_*` ncomp=3, `int2e_stg_ipip1_*`/`ipvip1_*`/`ip1ip2_*` ncomp=9. With ncomp=3, the planner sets `staging_elements = base_elements*3` (planner.rs:297-309) — exactly the `staging_elements > chunk_len` regime that the pre-20-10 `staging.len()`-derived copy_len corrupted on the f32 arm. THIS is the multi-component case to test.
- vendor f64 reference for it ALREADY EXISTS: `cintx_oracle::vendor_ffi::vendor_int2e_stg_ip1_sph(&mut out, &shls, atm, natm, bas, nbas, env)` (vendor_ffi.rs:706) and `vendor_int2e_stg_ipip1_sph` (:734), `vendor_int2e_stg_sph` (:678). All gated behind the `with-f12` feature.

f64 f12-DERIVATIVE PARITY ANALOG (the structural template — f64, uses eval_raw not safe API):
  crates/cintx-oracle/tests/f12_oracle_parity.rs — `#![cfg(feature = "with-f12")]`. KEY reusable helpers:
  :80 `fn eval_f12_sph_ncomp(symbol, shls, atm, bas, env, ncomp) -> Vec<f64>` (sizes output `ncomp * n_sph_elements(shls, bas)`) — adapt the FIXTURE shape (shls quartets, atm/bas/env builders).
  :215 `fn oracle_parity_int2e_stg_ip1_sph()` (ncomp=3) and :237 `oracle_parity_int2e_stg_ipip1_sph()` (ncomp=9) — the f64 parity tests; the new f32 tests mirror these but drive the SAFE API `evaluate::<f32>()` and compare `f32_out as f64` against the same vendor reference at the f32 floor.

f32 gate STRUCTURE (the file being extended — Plan 08):
  crates/cintx-oracle/tests/f32_parity.rs (953 lines). Existing tests are `#[cfg(has_vendor_libcint)]`, named `test_f32_int1e_*_parity` / `test_f32_int2c2e_*_parity`, each: build atm/bas/env, drive cintx via `evaluate_generic::<f32>()` (read `output.tensor.owned_values: Vec<f32>`), cast `as f64`, diff against a `collect_*_vendor_ref` f64 matrix using `f32_tolerance_for_family(family)` + a mismatch counter. The file does NOT yet gate on `with-f12` — the new f12 tests must add `#[cfg(all(has_vendor_libcint, feature = "with-f12"))]`.
  :24 `use cintx_oracle::compare::f32_tolerance_for_family;`
  :503-704 the existing per-operator test pattern (build env → cintx f32 → vendor f64 ref → mismatch count at tol).
  VERIFICATION.md:93 confirms the established call spelling is `evaluate_generic::<f32>()` (at lines 125, 168, 198, 942). Match that exact spelling — read those lines before writing the new helper.

compare.rs (Plan 08 — extend, do NOT touch the FROZEN f64 model):
  :20-22 FROZEN `UNIFIED_ATOL/RTOL = 1e-12`, `ZERO_THRESHOLD = 1e-18` (PREC-04).
  `pub fn f32_tolerance_for_family(family: &str) -> FamilyTolerance` + `F32_UNIFIED_RTOL=1e-4`, `F32_UNIFIED_ATOL=1e-7` (Plan 08). If "f12"/"stg"/the derivative family is not yet a distinct match arm, the catch-all already returns the unified f32 floor, so the new tests work at the unified floor unless empirically tightened.

evaluate::<f32>() ENTRY POINT (Plan 07): `req.evaluate_generic::<f32>()` returns `TypedEvaluationOutput<f32>` with `owned_values: Vec<f32>`. `evaluate()` is the f64 shim (20-07-SUMMARY.md). Use the same call shape the existing f32_parity.rs tests already use.

EMPIRICAL FLOOR PROCEDURE (mirror Plan 08): run the sweep recording `max(abs(f32_out as f64 - ref)/abs(ref))` over elements where `abs(ref) > zero_threshold`; set the family rtol to `10 * max_rel_error` rounded up; re-run to 0 mismatches. f12 derivative outputs accumulate more f32 rounding (more terms) than base 1e — expect a looser floor; record the measured value in the SUMMARY.

CONSTRAINT: do NOT add the f32 tests into the f64 test files; they live in f32_parity.rs (separate parallel gate). Do NOT change UNIFIED_ATOL/tolerance_for_family/diff_summary (PREC-04). When editing compare.rs use serena symbol tools (PREC-07); test-file additions are new `#[test]` fns appended (serena `insert_after_symbol` on an existing test fn, or append at module end).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add an f12-derivative (multi-component, ncomp=3) f32 parity test driving evaluate::<f32>()</name>
  <files>crates/cintx-oracle/tests/f32_parity.rs, crates/cintx-oracle/src/compare.rs</files>
  <read_first>
    - crates/cintx-oracle/tests/f32_parity.rs (entire file — the established f32 test pattern: env builders, evaluate_generic::<f32>() call shape at lines 125/168/198/942, owned_values as Vec<f32>, cast as f64, mismatch counter, f32_tolerance_for_family)
    - crates/cintx-oracle/tests/f12_oracle_parity.rs (the f64 f12-derivative analog: eval_f12_sph_ncomp, oracle_parity_int2e_stg_ip1_sph fixture shape, ncomp=3 sizing, the shls quartets + atm/bas/env builders)
    - crates/cintx-oracle/src/vendor_ffi.rs:678-744 (vendor_int2e_stg_sph / vendor_int2e_stg_ip1_sph / vendor_int2e_stg_ipip1_sph signatures — the f64 reference)
    - crates/cintx-oracle/src/compare.rs (f32_tolerance_for_family + the FROZEN f64 constants — confirm what NOT to touch)
    - .planning/phases/20-precision-generic-f64-f32-switch/20-VERIFICATION.md:16-27 (the Gap 2 truth + the explicit "Add f32 oracle tests for multi-component and f12 derivative operators" missing item)
    - .planning/phases/20-precision-generic-f64-f32-switch/20-10-SUMMARY.md (confirmation the CR-01/CR-02 fixes landed; the out_elems derivation)
  </read_first>
  <behavior>
    - Test (the load-bearing one): `int2e_stg_ip1_sph` (ncomp=3) driven through the safe API `evaluate_generic::<f32>()` — read `output.tensor.owned_values: Vec<f32>`, cast each `as f64`, diff against `vendor_int2e_stg_ip1_sph` (f64 libcint reference) using `f32_tolerance_for_family("f12")` (or the descriptor's canonical_family key): 0 mismatches AFTER the 20-10 fixes
    - Test: the f32 output is finite and has at least one nonzero element per symbol (anti-zero-fill sentinel — Phase 18 convention; guards an all-zero false pass)
    - Test (pre-fix expectation, documented as acceptance not a CI assertion): against pre-20-10 code this test FAILS (the doubled-lane copy/scan corrupted the ncomp=3 output) — confirming the test exercises the CR-01/CR-02 regime and the fixes are load-bearing
    - Diagnostic (records, not asserts): the per-family max relative error printed so the f12-derivative f32 floor can be set to 10x rounded up
  </behavior>
  <action>
    PART A (compare.rs — only if needed, via serena `replace_symbol_body` on `f32_tolerance_for_family`): ensure there is an f32 floor for the f12/stg/derivative family. If `f32_tolerance_for_family` already returns the unified f32 floor via a catch-all, the test works immediately; after the empirical sweep (Part B) add/tighten a match arm for "f12" (or whatever family key `int2e_stg_ip1_sph`'s descriptor reports — read the descriptor's canonical_family) to the measured `10 * max_rel_error`. Do NOT touch UNIFIED_ATOL/UNIFIED_RTOL/tolerance_for_family/diff_summary.

    PART B (f32_parity.rs — append a new test): add a vendor+feature-gated test mirroring the existing f32 test pattern but for the f12 derivative. Concrete shape (adapt the env/quartet builders from f12_oracle_parity.rs's ip1 test and the f32-driving + mismatch-counting helpers already in f32_parity.rs):

    ```rust
    #[test]
    #[cfg(all(has_vendor_libcint, feature = "with-f12"))]
    fn test_f32_int2e_stg_ip1_sph_parity() {
        // Build the SAME fixture the f12_oracle_parity.rs ip1 test uses (atm/bas/env, shls quartet).
        // ncomp = 3 (stg gradient on electron 1) -> staging_elements = base*3 > chunk_len:
        // this is the exact CR-01/CR-02 corruption regime (pre-20-10 returned wrong f32 values silently).
        let (atm, bas, env) = build_stg_env();        // reuse/adapt f12_oracle_parity fixture
        let natm = ...; let nbas = ...;
        for shls in stg_shell_quartets() {
            // cintx f32 via the safe API (the entry point the f32 gate drives):
            let f32_out: Vec<f32> = collect_stg_ip1_f32(&atm, &bas, &env, &shls); // drives evaluate_generic::<f32>(), reads owned_values
            // f64 libcint reference (already exists), sized ncomp(3) * n_sph_elements:
            let mut vendor_out = vec![0.0f64; f32_out.len()];
            vendor_ffi::vendor_int2e_stg_ip1_sph(&mut vendor_out, &shls, &atm, natm, &bas, nbas, &env);
            let tol = f32_tolerance_for_family("f12"); // or the descriptor's canonical_family key
            // anti-zero-fill sentinel:
            assert!(f32_out.iter().any(|&v| (v as f64).abs() > 1e-12), "stg_ip1 f32 output all-zero for {shls:?}");
            assert!(f32_out.iter().all(|v| v.is_finite()), "stg_ip1 f32 output non-finite for {shls:?}");
            // diagnostic max rel error (records the empirical floor):
            let max_rel = max_rel_error_f32(&vendor_out, &f32_out); // helper: max over abs(ref)>zero_threshold
            eprintln!("test_f32_int2e_stg_ip1_sph shls {shls:?} max_rel_error = {max_rel:e}");
            // parity at the f32 floor (0 mismatches AFTER 20-10):
            let mc = count_mismatches_f32(&vendor_out, &f32_out, tol.atol, tol.rtol);
            assert_eq!(mc, 0, "int2e_stg_ip1_sph f32 parity FAIL: {mc} mismatches (CR-01/CR-02 regime) for {shls:?}");
        }
    }
    ```

    Reuse the existing f32_parity.rs helpers for env-building, the `evaluate_generic::<f32>()` driving, and mismatch counting — copy the established shape from `test_f32_int2c2e_sph_parity` (the multi-center reference pattern) and the f64 `oracle_parity_int2e_stg_ip1_sph` fixture (for the quartet + ncomp=3 sizing). Add the `collect_stg_ip1_f32` helper (builds the stg_ip1 `SessionRequest`, calls `req.evaluate_generic::<f32>()`, returns `output.tensor.owned_values`). Match the EXACT `evaluate_generic::<f32>()` call spelling already used in f32_parity.rs (read lines 125/168/198 first). EMPIRICAL FLOOR: run, record `max_rel` via the eprintln diagnostic, set the f12 family floor in Part A to 10x rounded up, re-run to 0 mismatches.

    Do NOT modify the existing base-family tests or the f64 oracle test files.
  </action>
  <verify>
    <automated>CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu,with-f12 --test f32_parity test_f32_int2e_stg_ip1 2>&1 | tail -25 && CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu,with-f12 --test f32_parity 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `crates/cintx-oracle/tests/f32_parity.rs` has a new f12-derivative test driving evaluate_generic::<f32>(): `grep -n "test_f32_int2e_stg_ip1" crates/cintx-oracle/tests/f32_parity.rs` matches AND `grep -n "evaluate_generic::<f32>\|evaluate::<f32>" crates/cintx-oracle/tests/f32_parity.rs` matches inside the new helper
    - the test references the f64 vendor derivative reference: `grep -n "vendor_int2e_stg_ip1_sph" crates/cintx-oracle/tests/f32_parity.rs` matches
    - the test is feature-gated for f12: `grep -n 'feature = "with-f12"' crates/cintx-oracle/tests/f32_parity.rs` matches on the new test
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu,with-f12 --test f32_parity test_f32_int2e_stg_ip1` exits 0 (0 mismatches at the empirical f12 floor; finite + nonzero sentinel green) — this PASSES on the 20-10-fixed code
    - the empirical f12-derivative f32 max rel error is recorded (eprintln diagnostic + SUMMARY); the f12 family floor in `f32_tolerance_for_family` is set to 10x that value (if tightened beyond the unified floor)
    - the FROZEN f64 oracle model is untouched: `tolerance_for_family`, `UNIFIED_ATOL`, `diff_summary` are byte-identical (no diff in those symbols)
    - all compare.rs edits via serena symbol tools (PREC-07)
  </acceptance_criteria>
  <done>An f12-derivative (ncomp=3, the CR-01/CR-02 regime) f32 parity test drives evaluate::<f32>() and passes against libcint at the empirical f12 floor; the FROZEN f64 tolerance model is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Confirm load-bearing (pre-fix FAIL) + f64 byte-identity regression gate (PREC-04)</name>
  <files>crates/cintx-oracle/tests/f32_parity.rs</files>
  <read_first>
    - crates/cintx-oracle/tests/f32_parity.rs (the new test from Task 1)
    - .planning/phases/20-precision-generic-f64-f32-switch/20-VERIFICATION.md:47 (the FROZEN f64 integration oracle pass-set this must reproduce: one_electron 6/6, two_electron 2/2, center_2c2e/3c1e/3c2e, f12 15/15, safe_api, ecp at atol=1e-12)
    - .planning/phases/20-precision-generic-f64-f32-switch/20-10-SUMMARY.md (the CR-01/CR-02 fix the new test proves)
  </read_first>
  <behavior>
    - Test: the new f12-derivative f32 test PASSES on the 20-10-fixed code (already shown in Task 1)
    - Test: the FROZEN f64 integration oracle passes unchanged at atol=1e-12 (PREC-04 — the new test file additions are purely additive and do not touch the f64 path)
    - Test: the existing base-family f32 tests (Plan 08) still pass unchanged at their per-family floors
  </behavior>
  <action>
    This is a verification/gate task (no new production edits beyond Task 1). Establish the two facts that close Gap 2:

    1. LOAD-BEARING CONFIRMATION (document in SUMMARY, optional CI gate): demonstrate that the Task 1 test exercises the CR-01/CR-02 regime — i.e. it would FAIL on pre-20-10 code. Do this by reasoning from the data-flow (ncomp=3 ⇒ staging_elements = base*3 > chunk_len ⇒ pre-fix `staging.len()`-derived copy on the f32 arm overran/under-wrote) and, if cheaply reproducible, by temporarily stashing the 20-10 f12 fix and confirming the test fails, then restoring (do NOT commit the revert). Record the observed pre-fix failure mode in the SUMMARY so the verifier can confirm the test is genuinely load-bearing.
    2. f64 BYTE-IDENTITY GATE: run the full FROZEN f64 integration oracle plus the base-family f32 gate and the workspace check to prove nothing regressed.
    3. Do NOT touch the pre-existing `compare::tests` lib `CINTshells_cart_offset[4]` failure (out of scope, tracked in .planning/todos/).
  </action>
  <verify>
    <automated>CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu,with-f12 --test f32_parity 2>&1 | tail -20 && CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu,with-f12,with-4c1e 2>&1 | tail -30 && CINTX_BACKEND=cpu cargo check --workspace --features cpu,with-f12,with-4c1e 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - the full f32 gate (base families + the new f12-derivative test) passes: `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu,with-f12 --test f32_parity` exits 0
    - the FROZEN f64 integration oracle passes unchanged at atol=1e-12: `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu,with-f12,with-4c1e` reproduces the VERIFICATION.md:47 pass-set (PREC-04 byte-identity preserved)
    - `CINTX_BACKEND=cpu cargo check --workspace --features cpu,with-f12,with-4c1e` exits 0
    - the SUMMARY records that the new f12-derivative test is load-bearing (would FAIL pre-20-10) — with the observed/derived pre-fix failure mode
    - the pre-existing `compare::tests` `CINTshells_cart_offset[4]` failure is NOT chased or "fixed" (out of scope)
  </acceptance_criteria>
  <done>The f32 gate now covers the multi-component/f12-derivative regime and passes against libcint; the new test is load-bearing (fails pre-20-10); the FROZEN f64 integration oracle is byte-identical and green (PREC-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| f32 cintx output (multi-component/derivative) ↔ f64 libcint reference | Comparison across precisions for the exact regime CR-01/CR-02 corrupted; risk is a too-loose floor masking residual f32 corruption (false-positive pass). |
| new f32 tests ↔ FROZEN f64 gate | The two gates share `FamilyTolerance`; risk is the f32 work accidentally loosening the f64 floor (PREC-04 violation). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-33 | Information disclosure (false confidence — DOMINANT) | the new f12-derivative f32 floor set too loose, masking residual silent corruption in the ncomp>1 path | mitigate | Floor derived empirically (measured max rel error × 10), not guessed; the test is load-bearing (FAILS pre-20-10, documented in SUMMARY) so it provably detects the CR-01/CR-02 corruption; the nonzero + finite sentinels guard an all-zero/NaN false pass. |
| T-20-34 | Tampering | the f32 test work loosening the FROZEN f64 tolerance (UNIFIED_ATOL / tolerance_for_family) | mitigate | Only `f32_tolerance_for_family` is touched (serena, additive); the f64 symbols are not in any edit; the full f64 integration oracle at atol=1e-12 (Task 2) is the regression gate. |
| T-20-35 | Tampering | the f32 test added into the f64 test files (would couple the gates) | mitigate | The new tests live in `tests/f32_parity.rs` only; acceptance criteria require the f64 oracle test files untouched and the f64 oracle green. |
| T-20-SC | Tampering | cargo install / supply chain | accept | No new packages — the vendor derivative FFI (`vendor_int2e_stg_ip1_sph`) and helpers already exist in the oracle crate. |
</threat_model>

<verification>
- `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu,with-f12 --test f32_parity` green (base families + new multi-component/f12-derivative test at the empirical floors).
- `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu,with-f12,with-4c1e` green at atol=1e-12 (FROZEN f64 integration oracle — PREC-04).
- `CINTX_BACKEND=cpu cargo check --workspace --features cpu,with-f12,with-4c1e` exits 0.
</verification>

<success_criteria>
- The f32 oracle gate covers a multi-component operator and an f12-derivative operator (the CR-01/CR-02 cases) verified against libcint at the per-family f32 floor (PREC-05).
- The new tests are load-bearing: they FAIL pre-20-10 and PASS post-20-10.
- The FROZEN f64 byte-identity gate is untouched and green at atol=1e-12 (PREC-04 / D-08).
- The f32 floors are empirically derived and recorded.
- All compare.rs edits via serena symbol tools (PREC-07 / D-11).
</success_criteria>

<output>
Create `.planning/phases/20-precision-generic-f64-f32-switch/20-11-SUMMARY.md` when done. Record: the operator(s) tested (int2e_stg_ip1_sph ncomp=3, plus any second multi-component case), the measured per-family f32 max rel error and the floor set, the observed pre-20-10 failure mode (proving the test is load-bearing), and confirmation the FROZEN f64 integration oracle is byte-identical and green. Note for the verifier that the Gap 2 truth "PREC-05: f32 path has a separate oracle gate verified against libcint" is now TRUE for the multi-component/derivative regime, not just base-family scalar cases.
</output>
