---
phase: 20-precision-generic-f64-f32-switch
plan: 08
type: execute
wave: 7
depends_on: ["20-07"]
files_modified:
  - crates/cintx-oracle/src/compare.rs
  - crates/cintx-oracle/tests/f32_parity.rs
autonomous: true
requirements: [PREC-05, PREC-04]
must_haves:
  truths:
    - "A separate f32 oracle gate drives evaluate::<f32>() over the base-family fixtures and compares f32 output (cast up to f64) against the f64 libcint reference"
    - "Per-family f32 tolerance floors are derived empirically (run, measure max rel error, set rtol = 10x rounded up) and frozen in f32_tolerance_for_family"
    - "The f32 gate does NOT replace or loosen the existing f64 gate: UNIFIED_ATOL=1e-12 and tolerance_for_family stay byte-identical (PREC-04)"
    - "The f32 gate passes for all base families at its empirical per-family rtol"
  artifacts:
    - path: "crates/cintx-oracle/src/compare.rs"
      provides: "f32_tolerance_for_family + F32 tolerance constants (parallel to the frozen f64 model)"
      contains: "f32_tolerance_for_family"
    - path: "crates/cintx-oracle/tests/f32_parity.rs"
      provides: "separate f32 oracle gate driving evaluate::<f32>()"
      contains: "evaluate::<f32>"
  key_links:
    - from: "crates/cintx-oracle/tests/f32_parity.rs"
      to: "cintx_rs SessionRequest::evaluate::<f32>"
      via: "f32 evaluate over fixtures"
      pattern: "evaluate::<f32>"
    - from: "crates/cintx-oracle/tests/f32_parity.rs"
      to: "compare::f32_tolerance_for_family"
      via: "per-family f32 tolerance lookup"
      pattern: "f32_tolerance_for_family"
---

<objective>
Wave 5 (f32 oracle gate): add a SEPARATE, parallel oracle gate that verifies `evaluate::<f32>()` against the f64 libcint reference at a realistic single-precision tolerance (~1e-4 rtol, per-family floors derived empirically) — mirroring the Phase 15 per-family tolerance model at looser tolerance. The existing four-profile f64 byte-identity gate is FROZEN and unchanged (PREC-04 / D-08); the f32 gate is additive (D-09).

Purpose: This closes the verification half of the milestone. f32 is verified against libcint — just not byte-identically. The empirical floor procedure (run, measure, set rtol = 10× max observed rel error) replaces the ASSUMED ~1e-4 estimate with measured per-family values. The gate is a parallel CI job that never weakens the f64 gate.

Output: `f32_tolerance_for_family()` + F32 tolerance constants in `compare.rs` (parallel to the frozen `tolerance_for_family`), a new `tests/f32_parity.rs` driving `evaluate::<f32>()` over the base-family fixtures, and the measured per-family floors recorded in the SUMMARY.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-precision-generic-f64-f32-switch/20-CONTEXT.md
@.planning/phases/20-precision-generic-f64-f32-switch/20-PATTERNS.md
@.planning/phases/20-precision-generic-f64-f32-switch/20-RESEARCH.md
@.planning/phases/20-precision-generic-f64-f32-switch/20-07-SUMMARY.md
@.planning/phases/15-oracle-tolerance-unification-manifest-lock-closure/15-CONTEXT.md

<interfaces>
<!-- Concrete current oracle structures (read in full before editing). -->

compare.rs:20-22  `const UNIFIED_ATOL: f64 = 1e-12; const UNIFIED_RTOL: f64 = 1e-12; const ZERO_THRESHOLD: f64 = 1e-18;` — FROZEN (PREC-04 / D-08).
compare.rs:67     `pub struct FamilyTolerance { pub family: &'static str, pub atol: f64, pub rtol: f64, pub zero_threshold: f64 }`
compare.rs:126-149 `pub fn tolerance_for_family(family: &str) -> FamilyTolerance { ... atol: UNIFIED_ATOL, rtol: UNIFIED_RTOL, zero_threshold: ZERO_THRESHOLD }` — FROZEN; all families map to the unified f64 floor.
compare.rs:152    `fn diff_summary(reference: &[f64], observed: &[f64], tolerance: FamilyTolerance) -> DiffSummary` — reuse this for the f32 comparison: pass `f32_out as f64` as `observed` against the f64 `reference`.

tests/safe_api_arity2_parity.rs — the structural analog. KEY reusable helpers (copy/adapt their shape):
  :236 `fn collect_safe_api_matrix(...)` drives `SessionRequest::...evaluate()` (:274) over operators — adapt to call `.evaluate::<f32>()` and collect `Vec<f32>`.
  :300 `fn count_mismatches(reference: &[f64], observed: &[f64], atol, rtol) -> usize`
  :352/:416 `collect_1e_{sph,cart}_matrix_vendor`, :480/:530 `collect_2c2e_{sph,cart}_matrix_vendor` — the f64 libcint reference matrices. These stay f64 (the reference side is f64-only; FREEZE Inventory).
  sibling tests safe_api_arity3_parity.rs / safe_api_arity4_parity.rs cover 3c1e/3c2e and int2e/4c1e references.

cintx-oracle features (Cargo.toml): `cpu`, `with-f12`, etc. cintx-rs is a path dep. No new feature is required for the f32 gate — it runs under `--features cpu`.

From Plan 07: `req.evaluate::<f32>()` returns `TypedEvaluationOutput<f32>` with `owned_values: Vec<f32>`. The f32 gate reads `output.tensor.owned_values` (Vec<f32>), casts each to f64, and diffs against the f64 vendor reference.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add f32_tolerance_for_family + F32 constants to compare.rs (FREEZE the f64 model)</name>
  <files>crates/cintx-oracle/src/compare.rs</files>
  <read_first>
    - crates/cintx-oracle/src/compare.rs:20-22 (FROZEN f64 constants), :67-72 (FamilyTolerance), :126-149 (tolerance_for_family FROZEN), :152 (diff_summary)
    - .planning/phases/20-precision-generic-f64-f32-switch/20-PATTERNS.md (§compare.rs FREEZE existing + ADD f32 profile, lines 318-352)
    - .planning/phases/20-precision-generic-f64-f32-switch/20-RESEARCH.md (§Per-Family f32 Tolerance Floor Strategy, lines 500-536)
    - .planning/phases/15-oracle-tolerance-unification-manifest-lock-closure/15-CONTEXT.md (the f64 model the f32 gate mirrors)
  </read_first>
  <behavior>
    - Test: tolerance_for_family("1e") still returns atol=1e-12, rtol=1e-12 (FROZEN, unchanged)
    - Test: f32_tolerance_for_family("1e") returns the F32 constants (initially F32_UNIFIED_RTOL=1e-4, F32_UNIFIED_ATOL=1e-7); per-family floors tightened in Task 2
    - Test: F32_UNIFIED_RTOL and F32_UNIFIED_ATOL constants exist and are distinct from the f64 ones
  </behavior>
  <action>
    Using serena `insert_after_symbol` (do NOT touch the frozen f64 symbols): add `pub const F32_UNIFIED_RTOL: f64 = 1e-4;` and `pub const F32_UNIFIED_ATOL: f64 = 1e-7;` near the f64 constants, and a new `pub fn f32_tolerance_for_family(family: &str) -> FamilyTolerance` that mirrors `tolerance_for_family`'s match shape but returns the F32 constants per family (start with a catch-all returning `FamilyTolerance { family: <static family str>, atol: F32_UNIFIED_ATOL, rtol: F32_UNIFIED_RTOL, zero_threshold: ZERO_THRESHOLD }`; Task 2 tightens per-family rtol after the empirical sweep). Reuse the existing `FamilyTolerance` struct and `diff_summary` (the f32 gate passes `f32_out as f64` as `observed`). Do NOT change `UNIFIED_ATOL`, `UNIFIED_RTOL`, `tolerance_for_family`, or `diff_summary` (PREC-04). Add unit tests asserting the f64 tolerance is unchanged and the f32 tolerance is the new constant.
  </action>
  <verify>
    <automated>cargo test -p cintx-oracle --features cpu --lib f32_tolerance tolerance_for_family 2>&1 | tail -15 && cargo check -p cintx-oracle --features cpu 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `compare.rs` contains `pub fn f32_tolerance_for_family` and `pub const F32_UNIFIED_RTOL`/`F32_UNIFIED_ATOL`; `grep -n "f32_tolerance_for_family\|F32_UNIFIED_RTOL" crates/cintx-oracle/src/compare.rs` matches
    - `UNIFIED_ATOL`, `UNIFIED_RTOL`, `tolerance_for_family`, `diff_summary` are byte-identical to before (FROZEN); a unit test asserts `tolerance_for_family("1e").atol == 1e-12`
    - `cargo test -p cintx-oracle --features cpu --lib f32_tolerance tolerance_for_family` passes
    - `cargo check -p cintx-oracle --features cpu` exits 0
  </acceptance_criteria>
  <done>compare.rs has a parallel f32 tolerance model (constants + f32_tolerance_for_family) reusing FamilyTolerance/diff_summary; the f64 model is FROZEN byte-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add tests/f32_parity.rs driving evaluate::<f32>() + empirically derive per-family floors</name>
  <files>crates/cintx-oracle/tests/f32_parity.rs, crates/cintx-oracle/src/compare.rs</files>
  <read_first>
    - crates/cintx-oracle/tests/safe_api_arity2_parity.rs (entire file — the structural analog: fixture builders, collect_safe_api_matrix, count_mismatches, vendor reference collectors)
    - crates/cintx-oracle/tests/safe_api_arity3_parity.rs and safe_api_arity4_parity.rs (3c1e/3c2e + int2e/4c1e fixtures + vendor references)
    - crates/cintx-oracle/src/compare.rs (f32_tolerance_for_family from Task 1; diff_summary)
    - .planning/phases/20-precision-generic-f64-f32-switch/20-07-SUMMARY.md (evaluate::<f32>() entry point)
    - .planning/phases/20-precision-generic-f64-f32-switch/20-PATTERNS.md (§tests/f32_parity.rs NEW, lines 356-362; empirical floor procedure)
    - .planning/phases/20-precision-generic-f64-f32-switch/20-RESEARCH.md (§Per-Family f32 Tolerance Floor Strategy — the run/measure/×10 procedure)
  </read_first>
  <behavior>
    - Test: each base family (1e, 2e, 2c2e, 3c1e, 3c2e) driven through evaluate::<f32>() compares to the f64 libcint reference (f32_out as f64) within f32_tolerance_for_family(family) — 0 mismatches at the frozen floor
    - Test: the f32 output is finite and has at least one nonzero element per symbol (anti-zero-fill sentinel, Phase 18 convention)
    - Diagnostic (records, not asserts): per-family max relative error printed/written so the floor can be set to 10× rounded up
  </behavior>
  <action>
    Create `crates/cintx-oracle/tests/f32_parity.rs` by copying the structure of `safe_api_arity2_parity.rs` (fixture builders + vendor reference collectors + count_mismatches) and its arity3/arity4 siblings for the multi-center families. Swap the cintx side from `.evaluate()` to `.evaluate::<f32>()`, read `output.tensor.owned_values` (Vec<f32>), and cast each element `as f64` before differencing against the FROZEN f64 vendor reference matrices (which stay f64). Use `compare::f32_tolerance_for_family(family)` (Task 1) for the threshold. EMPIRICAL FLOOR PROCEDURE: first run the sweep recording `max(abs(f32_out as f64 - ref)/abs(ref))` per family (where `abs(ref) > zero_threshold`); then set each family's rtol in `f32_tolerance_for_family` (Task 1's fn) to `10 * max_rel_error` rounded up to a clean value (per RESEARCH); re-run until 0 mismatches. Keep the per-symbol nonzero sentinel (Phase 18). Write a small artifact (per-family max rel error) to the existing oracle artifact path convention (`/mnt/data` with `CINTX_ARTIFACT_DIR`/`/tmp/cintx_artifacts` fallback per STATE.md Phase 4/9) if that helper is available; otherwise record floors in the SUMMARY. Do NOT add the f32 gate to the existing f64 oracle test files — it is a SEPARATE test file (parallel CI job `oracle_parity_gate_f32`, advisory first per D-09).
  </action>
  <verify>
    <automated>CINTX_BACKEND=cpu cargo test -p cintx-oracle --features cpu --test f32_parity 2>&1 | tail -25 && CINTX_BACKEND=cpu cargo test -p cintx-oracle --features cpu 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `crates/cintx-oracle/tests/f32_parity.rs` exists and drives `evaluate::<f32>()`; `grep -n "evaluate::<f32>" crates/cintx-oracle/tests/f32_parity.rs` matches
    - the f32 gate compares `f32_out as f64` against the f64 libcint reference via `f32_tolerance_for_family`; `grep -n "f32_tolerance_for_family" crates/cintx-oracle/tests/f32_parity.rs` matches
    - `CINTX_BACKEND=cpu cargo test -p cintx-oracle --features cpu --test f32_parity` passes (0 mismatches at the empirically derived per-family floors; finite + nonzero sentinel green)
    - the FROZEN f64 oracle suite still passes: `CINTX_BACKEND=cpu cargo test -p cintx-oracle --features cpu` green at atol=1e-12 (PREC-04 — f64 gate unchanged)
    - per-family empirical f32 floors are recorded (artifact and/or SUMMARY)
  </acceptance_criteria>
  <done>A separate f32 oracle gate drives evaluate::<f32>() over the base families, verified against the f64 libcint reference at empirically derived per-family floors; the f64 byte-identity gate is untouched and still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| f32 cintx output ↔ f64 libcint reference | Comparison across precisions; risk is a too-loose floor masking a real f32 kernel bug (false-positive pass), not external attack. |
| f32 gate ↔ existing f64 gate | The two gates share `FamilyTolerance`/`diff_summary`; risk is the f32 work accidentally loosening the f64 floor (PREC-04 violation). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-22 | Information disclosure (false confidence) | f32 oracle floor set too loose (false positive) | mitigate | Floors derived empirically (measured max rel error × 10), not guessed; gate runs advisory-first per D-09; per-family floors recorded for review; the nonzero sentinel guards against an all-zero pass. |
| T-20-23 | Tampering | f32 work loosening the FROZEN f64 tolerance (UNIFIED_ATOL/tolerance_for_family) | mitigate | f32 constants/fn added via serena `insert_after_symbol` only; the f64 symbols are not in any edit; a unit test asserts `tolerance_for_family("1e").atol == 1e-12`; the full f64 suite is a wave gate. |
| T-20-24 | Tampering | f32 gate added into the f64 test files (would couple the gates) | mitigate | The f32 gate is a SEPARATE `tests/f32_parity.rs` file (parallel CI job); acceptance criteria require the file to be new and the f64 test files untouched. |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No new packages; deps already in Cargo.lock (RESEARCH §Package Legitimacy Audit). |
</threat_model>

<verification>
- `CINTX_BACKEND=cpu cargo test -p cintx-oracle --features cpu --test f32_parity` green at the empirical per-family floors.
- `CINTX_BACKEND=cpu cargo test -p cintx-oracle --features cpu` green at atol=1e-12 (FROZEN f64 gate unchanged — PREC-04).
- `cargo check -p cintx-oracle --features cpu` exits 0.
- `CINTX_BACKEND=cpu cargo test --workspace --features cpu` green (phase gate).
</verification>

<success_criteria>
- A separate f32 oracle gate verifies evaluate::<f32>() against the f64 libcint reference (PREC-05).
- Per-family f32 floors derived empirically (run/measure/×10) and frozen in f32_tolerance_for_family.
- The f64 byte-identity gate (UNIFIED_ATOL=1e-12, tolerance_for_family) is FROZEN and still green (PREC-04 / D-08).
- The f32 gate is a parallel test file, advisory-first (D-09), never replacing the f64 gate.
- All edits via serena symbol tools (D-11).
</success_criteria>

<output>
Create `.planning/phases/20-precision-generic-f64-f32-switch/20-08-SUMMARY.md` when done. Record the measured per-family f32 rtol floors (the empirical result that replaces the ASSUMED ~1e-4), confirm the f64 gate is byte-identical and green, and note the parallel CI job name `oracle_parity_gate_f32` for the verifier.
</output>
