---
phase: 20-precision-generic-f64-f32-switch
plan: 06
type: execute
wave: 5
depends_on: ["20-05"]
files_modified:
  - crates/cintx-cubecl/src/executor.rs
  - crates/cintx-runtime/src/options.rs
  - crates/cintx-runtime/src/planner.rs
autonomous: true
requirements: [PREC-03, PREC-06, PREC-07]
must_haves:
  truths:
    - "The f32 path does NOT gate on SHADER_F64: a check_capability wrapper returns Ok early when plan.precision == F32, before any check_shader_f64_in_features call"
    - "The f64 path still requires SHADER_F64 on wgpu/metal (existing check_shader_f64_in_features unit tests pass unchanged)"
    - "ExecutionOptions carries a precision: PrecisionKind field (default F64) threaded into ExecutionPlan.precision via ExecutionPlan::new / query_workspace / evaluate"
    - "The staging buffer (Vec<f64>) is large enough for the f32 reinterpretation (f64 sizing over-allocates for f32) and the OOM-safe fallible-alloc contract is unchanged (no partial writes)"
    - "raw compat env/atm/bas stay &[f64] (PREC-03 regression: cintx-compat tests pass unchanged)"
  artifacts:
    - path: "crates/cintx-cubecl/src/executor.rs"
      provides: "precision-aware check_capability bypassing SHADER_F64 for f32"
      contains: "PrecisionKind::F32"
    - path: "crates/cintx-runtime/src/options.rs"
      provides: "ExecutionOptions.precision field"
      contains: "precision"
  key_links:
    - from: "crates/cintx-cubecl/src/executor.rs"
      to: "plan.precision"
      via: "early Ok return for F32 in check_capability"
      pattern: "precision.*F32|check_capability"
    - from: "crates/cintx-runtime/src/options.rs"
      to: "crates/cintx-runtime/src/planner.rs ExecutionPlan.precision"
      via: "ExecutionPlan::new copies opts.precision"
      pattern: "precision"
---

<objective>
Wave 3 (executor capability branch + precision plumbing): make the f32 path bypass the `SHADER_F64` capability gate (PREC-06 — the primary motivation), thread `precision: PrecisionKind` from `ExecutionOptions` into `ExecutionPlan.precision` (so the kernel dispatchers from Plans 04/05 actually receive the requested precision), and confirm the staging-buffer allocation is sound for the f32 reinterpretation while keeping the OOM-safe contract and the env/atm/bas f64 ABI frozen (PREC-03).

Purpose: Until now `plan.precision` defaulted to F64 everywhere. This plan wires the real precision selection through the runtime entry points and removes the `SHADER_F64` gate for f32 in the executor — the change that genuinely unlocks wgpu adapters lacking native f64. The f64 path keeps its `SHADER_F64` requirement byte-identically.

Output: a `check_capability` wrapper in executor.rs (f32 → early Ok; f64 → existing `check_f64_capability`), a `precision` field on `ExecutionOptions` threaded into `ExecutionPlan::new`, and a confirmed-sound staging allocation. Raw compat ABI untouched.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-precision-generic-f64-f32-switch/20-CONTEXT.md
@.planning/phases/20-precision-generic-f64-f32-switch/20-PATTERNS.md
@.planning/phases/20-precision-generic-f64-f32-switch/20-RESEARCH.md
@.planning/phases/20-precision-generic-f64-f32-switch/20-05-SUMMARY.md
@.planning/phases/20-precision-generic-f64-f32-switch/20-01-SUMMARY.md

<interfaces>
<!-- Concrete current contracts. -->

executor.rs:73-94  `fn check_f64_capability(&self, backend: &ResolvedBackend, _plan: &ExecutionPlan<'_>) -> Result<(), cintxRsError>` — matches backend; Wgpu/Metal arms call `check_shader_f64_in_features(backend.wgpu_features())`; Cpu/Cuda/Rocm → Ok.
executor.rs:160-167  `pub fn check_shader_f64_in_features(features: &[String]) -> Result<(), cintxRsError>` — FROZEN; returns UnsupportedApi { requested: "wgpu-capability:missing_shader_f64" } when absent. Its unit tests at executor.rs:303-334 MUST stay green.
executor.rs:178-180  query_workspace impl calls `self.check_f64_capability(&backend, plan)?;`
executor.rs:185-191  execute impl calls `self.check_f64_capability(&backend, plan)?;`

options.rs:104  `pub struct ExecutionOptions { ... pub backend_intent: BackendIntent, pub f12_zeta: Option<f64>, pub aosym: Option<cintx_core::AoSymmetry>, ... }` — derives Default; add `pub precision: PrecisionKind` here.
planner.rs:76  `ExecutionPlan::new(op, rep, basis, shells, workspace) -> Result<Self, cintxRsError>` — does NOT currently take opts; precision must be set by the caller after `new` OR `new` extended. NOTE: f12_zeta uses the "caller populates after new" pattern (planner.rs:106-108). Follow that precedent: set `plan.precision = opts.precision` in `query_workspace`/`evaluate` (planner.rs:113/172) where `opts` is in scope, right after building the plan.
planner.rs:238  `let mut staging = try_alloc_staging(staging_elements_for_chunk(&plan, chunk)?)?;`
planner.rs:318-327 `try_alloc_staging(elements) -> Result<Vec<f64>, cintxRsError>` — fallible `try_reserve_exact`, no partial writes (FROZEN OOM contract).

STAGING SIZING FACT (verified): the staging buffer is `Vec<f64>` of `elements` f64 = `elements*8` bytes. For f32, `bytemuck::cast_slice_mut::<f64,f32>` yields `elements*2` f32 lanes — DOUBLE what the f32 kernel needs (it writes `elements` f32 values). So the existing f64-sized allocation is ALREADY large enough for the f32 path (over-allocated, never under). Keep `try_alloc_staging` returning `Vec<f64>` sized by element count; no byte-sizing change is required for soundness. Optionally document this over-allocation in the SUMMARY.

PREC-03 freeze: cintx-compat raw env/atm/bas arrays stay `&[f64]`; cintx-capi stays f64. Do NOT touch those crates.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add precision-aware check_capability bypassing SHADER_F64 for f32</name>
  <files>crates/cintx-cubecl/src/executor.rs</files>
  <read_first>
    - crates/cintx-cubecl/src/executor.rs:73-94 (check_f64_capability), :160-167 (check_shader_f64_in_features FROZEN), :178-191 (the two call sites), :303-334 (the FROZEN unit tests)
    - crates/cintx-core/src/precision.rs (PrecisionKind)
    - .planning/phases/20-precision-generic-f64-f32-switch/20-PATTERNS.md (§executor.rs before→after, lines 185-223)
    - .planning/phases/20-precision-generic-f64-f32-switch/20-RESEARCH.md (Pattern 4; RESEARCH FLAG RESOLUTION: wgpu f32 — f32 is WebGPU-baseline universal)
  </read_first>
  <behavior>
    - Test: with plan.precision == F32, check_capability returns Ok even when wgpu features lack "SHADER_F64" (no UnsupportedApi)
    - Test: with plan.precision == F64 and wgpu features lacking "SHADER_F64", check_capability returns the existing UnsupportedApi { requested: "wgpu-capability:missing_shader_f64" }
    - Test: the existing check_shader_f64_in_features unit tests (executor.rs:303-334) pass unchanged
  </behavior>
  <action>
    Using serena `insert_after_symbol` / `replace_symbol_body`: add a new method `fn check_capability(&self, backend: &ResolvedBackend, plan: &ExecutionPlan<'_>) -> Result<(), cintxRsError>` that returns `Ok(())` early when `plan.precision == PrecisionKind::F32` (with a comment citing D-10 / "f32 is WebGPU-baseline universal — no SHADER_F64 gate"), otherwise delegates to the existing `self.check_f64_capability(backend, plan)`. Keep `check_f64_capability` and `check_shader_f64_in_features` byte-identical (do NOT modify their bodies — the f64 arm must stay exactly as today, PREC-04). Update the two call sites at executor.rs:180 (query_workspace) and :191 (execute) from `check_f64_capability` to `check_capability`. Add a unit test asserting the F32 bypass and the F64 retention. Import `cintx_core::PrecisionKind`. Do NOT make `BackendExecutor` generic (Pitfall 3).
  </action>
  <verify>
    <automated>cargo test -p cintx-cubecl --features cpu executor 2>&1 | tail -20 && cargo test -p cintx-cubecl --features cpu shader_f64 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `executor.rs` contains `fn check_capability` with an early `Ok(())` guarded by `plan.precision == PrecisionKind::F32`; `grep -n "PrecisionKind::F32" crates/cintx-cubecl/src/executor.rs` matches
    - `check_shader_f64_in_features` body is unchanged; its unit tests (executor.rs:303-334) pass unchanged
    - both call sites (query_workspace, execute) now call `check_capability`; `grep -c "check_capability" crates/cintx-cubecl/src/executor.rs` >= 3 (def + 2 call sites)
    - new unit test proves: F32 bypasses SHADER_F64; F64 still fails closed without SHADER_F64
    - `cargo check -p cintx-cubecl --features cpu` exits 0
  </acceptance_criteria>
  <done>The f32 path bypasses the SHADER_F64 gate via check_capability; the f64 path keeps its SHADER_F64 requirement byte-identically; the frozen check_shader_f64_in_features tests pass unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread precision from ExecutionOptions into ExecutionPlan + confirm staging soundness</name>
  <files>crates/cintx-runtime/src/options.rs, crates/cintx-runtime/src/planner.rs</files>
  <read_first>
    - crates/cintx-runtime/src/options.rs:104-125 (ExecutionOptions struct + Default derive)
    - crates/cintx-runtime/src/planner.rs:76-110 (ExecutionPlan::new), :106-108 (the "caller populates after new" precedent for f12_zeta), :113-172 (query_workspace + evaluate where opts is in scope), :238 + :318-327 (staging alloc, OOM contract)
    - crates/cintx-core/src/precision.rs (PrecisionKind)
    - crates/cintx-cubecl/tests/bytemuck_staging_cast_spike.rs (Plan 01 cast proof)
    - .planning/phases/20-precision-generic-f64-f32-switch/20-PATTERNS.md (§planner.rs FROZEN fallible-alloc; §dispatch.rs)
  </read_first>
  <behavior>
    - Test: ExecutionOptions::default().precision == PrecisionKind::F64
    - Test: a plan built via query_workspace with opts.precision == F32 has plan.precision == F32
    - Test: a plan built with default opts has plan.precision == F64 (f64 path default unchanged)
    - Test: try_alloc_staging still returns the OOM-safe Vec<f64>; the buffer length in f32 lanes (elements*2 via bytemuck) is >= the f32 element count the kernel writes
  </behavior>
  <action>
    PART A (ExecutionOptions): using serena `replace_symbol_body` on `ExecutionOptions`, add `pub precision: PrecisionKind` (import `cintx_core::PrecisionKind`). Because PrecisionKind derives Default (F64), the existing `#[derive(Default)]` / `ExecutionOptions::default()` keeps the f64 path unchanged. Document the field: "Output precision; F64 (default) = byte-identity, F32 = loose-tolerance + unlocks non-SHADER_F64 wgpu adapters (D-01/D-10)."

    PART B (thread into plan): follow the existing f12_zeta "caller populates after new" precedent (planner.rs:106-108). In `query_workspace` and `evaluate` (planner.rs:113/172), after building the `ExecutionPlan`, set `plan.precision = opts.precision;` (opts is in scope at both entry points). Use serena to locate and edit those functions. Do NOT change `ExecutionPlan::new`'s parameter list (preserve its signature for existing callers).

    PART C (staging soundness — confirm, do NOT change the contract): keep `try_alloc_staging` returning `Vec<f64>` sized by `elements` (FROZEN OOM-safe `try_reserve_exact`, no partial writes). Add a unit/assertion-level test confirming that for the f32 path the bytemuck reinterpretation yields at least `elements` f32 lanes (it yields `elements*2`, an over-allocation that is always safe). Record in the SUMMARY that the f64-sized buffer over-allocates for f32 (never under) so no byte-sizing change to the OOM contract is needed.
  </action>
  <verify>
    <automated>cargo test -p cintx-runtime --features cpu precision options planner 2>&1 | tail -20 && CINTX_BACKEND=cpu cargo test -p cintx-compat --features cpu 2>&1 | tail -10 && CINTX_BACKEND=cpu cargo check --workspace --features cpu 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ExecutionOptions` has `pub precision: PrecisionKind`; `ExecutionOptions::default().precision == PrecisionKind::F64`; `grep -n "precision" crates/cintx-runtime/src/options.rs` matches
    - `query_workspace` and `evaluate` set `plan.precision = opts.precision`; `grep -c "plan.precision = opts.precision" crates/cintx-runtime/src/planner.rs` >= 1 (or per-entry-point equivalent)
    - `try_alloc_staging` is unchanged (still `try_reserve_exact` + typed HostAllocationFailed; no partial writes); `ExecutionPlan::new` signature unchanged
    - `CINTX_BACKEND=cpu cargo test -p cintx-compat --features cpu` passes (PREC-03: env/atm/bas still &[f64], unchanged)
    - `CINTX_BACKEND=cpu cargo check --workspace --features cpu` exits 0
  </acceptance_criteria>
  <done>precision flows ExecutionOptions → ExecutionPlan via the f12_zeta caller-populates precedent; default stays F64; staging Vec<f64> over-allocates safely for f32 with the OOM contract unchanged; raw compat ABI frozen (PREC-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wgpu adapter capability set ↔ executor precision gate | Internal capability decision; risk is a correctness/safety regression (silently re-introducing or wrongly removing the f64 SHADER_F64 requirement), not an external attacker. |
| `ExecutionOptions.precision` (caller-chosen) ↔ kernel dispatch | The precision tag selects f32/f64 monomorphization; risk is a caller getting f32 results while expecting f64 — handled by the type system at the safe-API layer (Plan 07). |
| staging `Vec<f64>` allocation ↔ f32 reinterpretation | Internal buffer sizing; risk is an under-sized buffer (OOB), mitigated by f64-sizing over-allocating for f32. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-16 | Spoofing / Elevation | f32 path silently re-introducing the f64 SHADER_F64 requirement (or f64 path silently dropping it) | mitigate | `check_capability` branches explicitly on `plan.precision`; the f64 arm calls the byte-identical `check_f64_capability`; a unit test asserts both directions (F32 bypass + F64 fail-closed without SHADER_F64). |
| T-20-17 | Elevation | under-sized staging buffer for the f32 bytemuck reinterpret (OOB write) | mitigate | f64-sized `Vec<f64>` over-allocates for f32 (elements*2 f32 lanes ≥ elements needed); OOM-safe `try_reserve_exact` contract unchanged; a test asserts the lane count. |
| T-20-18 | Tampering | accidental modification of raw env/atm/bas ABI (PREC-03 violation) | mitigate | cintx-compat / cintx-capi NOT in files_modified; PREC-03 regression test (`cargo test -p cintx-compat`) gates the wave; serena edits scoped to runtime/executor symbols only. |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No new packages; deps already in Cargo.lock (RESEARCH §Package Legitimacy Audit). |
</threat_model>

<verification>
- `cargo test -p cintx-cubecl --features cpu executor shader_f64` green (F32 bypass + F64 retention; frozen tests unchanged).
- `cargo test -p cintx-runtime --features cpu` green (precision plumbing; default F64).
- `CINTX_BACKEND=cpu cargo test -p cintx-compat --features cpu` green (PREC-03: env/atm/bas unchanged).
- `CINTX_BACKEND=cpu cargo check --workspace --features cpu` exits 0.
</verification>

<success_criteria>
- f32 path bypasses SHADER_F64 (PREC-06); f64 path keeps it byte-identically (PREC-04).
- precision threads ExecutionOptions → ExecutionPlan; default F64.
- staging Vec<f64> over-allocates safely for f32; OOM-safe fallible-alloc contract unchanged.
- raw compat env/atm/bas + C ABI stay f64 (PREC-03).
- BackendExecutor object safety preserved.
- All edits via serena symbol tools (D-11).
</success_criteria>

<output>
Create `.planning/phases/20-precision-generic-f64-f32-switch/20-06-SUMMARY.md` when done. Confirm the f32 SHADER_F64 bypass, the precision plumbing path (ExecutionOptions.precision → plan.precision → kernel dispatchers), and the staging over-allocation soundness note. Flag for Plan 07 that the safe API's evaluate::<F>() must set ExecutionOptions.precision from F.
</output>
