---
phase: 20-precision-generic-f64-f32-switch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cintx-core/src/precision.rs
  - crates/cintx-core/src/lib.rs
  - crates/cintx-core/Cargo.toml
  - crates/cintx-runtime/src/planner.rs
  - crates/cintx-cubecl/tests/bytemuck_staging_cast_spike.rs
autonomous: true
requirements: [PREC-01, PREC-07]
must_haves:
  truths:
    - "CintFloat sealed trait exists and is implemented for exactly f64 and f32 (no other type)"
    - "PrecisionKind { F64, F32 } enum exists with F64 as the default and element_size() returning 8/4"
    - "ExecutionPlan carries a precision: PrecisionKind field defaulting to F64"
    - "The bytemuck f64<->f32 staging-buffer reinterpretation (A5) is proven sound by a passing test before any kernel wave depends on it"
    - "The f64 path compiles and all existing tests pass unchanged after scaffolding (zero behavior change)"
  artifacts:
    - path: "crates/cintx-core/src/precision.rs"
      provides: "CintFloat sealed trait + PrecisionKind enum"
      contains: "pub trait CintFloat"
    - path: "crates/cintx-cubecl/tests/bytemuck_staging_cast_spike.rs"
      provides: "A5 bytemuck cast soundness proof"
      contains: "cast_slice_mut"
  key_links:
    - from: "crates/cintx-runtime/src/planner.rs"
      to: "cintx_core::PrecisionKind"
      via: "ExecutionPlan.precision field"
      pattern: "precision.*PrecisionKind"
---

<objective>
Wave 0 scaffolding: introduce the precision type vocabulary the whole refactor threads, wire the serena process gate (D-11), and de-risk the one open architectural judgment call (the A5 bytemuck staging-buffer cast) BEFORE any kernel or staging wave depends on it. No compute-path behavior changes — every existing f64 test must pass unchanged.

Purpose: Establish `CintFloat` (sealed to f64|f32, host-side `num_traits::Float` bridge) and `PrecisionKind` (object-safe runtime tag) so device kernels (Wave 2), the executor capability branch (Wave 3), and the safe API (Wave 4) all have a stable, shared foundation. Prove the bytemuck cast is sound so the staging strategy is locked, not assumed.

Output: `cintx-core/src/precision.rs` (new), `precision` re-exported from `cintx-core`, `num-traits` as a direct `cintx-core` dep, a `precision: PrecisionKind` field on `ExecutionPlan` (default `F64`), and a passing bytemuck-cast spike test.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-precision-generic-f64-f32-switch/20-CONTEXT.md
@.planning/phases/20-precision-generic-f64-f32-switch/20-PATTERNS.md
@.planning/phases/20-precision-generic-f64-f32-switch/20-RESEARCH.md

<interfaces>
<!-- Concrete current contracts the executor needs. Do NOT explore the codebase to rediscover these. -->

ExecutionPlan is defined in crates/cintx-runtime/src/planner.rs:61 as `pub struct ExecutionPlan<'a>` (NOT generic — keep it non-generic; add a plain enum field).

The staging buffer flows as `&mut [f64]`:
- crates/cintx-runtime/src/dispatch.rs:104  `pub struct ExecutionIo<'a> { staging_output: &'a mut [f64], ... }`
- crates/cintx-runtime/src/dispatch.rs:137  `pub fn staging_output(&mut self) -> &mut [f64]`
- crates/cintx-runtime/src/planner.rs:318    `fn try_alloc_staging(elements: usize) -> Result<Vec<f64>, cintxRsError>` (fallible `try_reserve_exact` — OOM-safe, no partial writes)

The launcher function-pointer type (Wave 2 will dispatch through this) is:
- crates/cintx-cubecl/src/kernels/mod.rs:19  `pub type FamilyLaunchFn = fn(&ResolvedBackend, &ExecutionPlan<'_>, &SpecializationKey, &mut [f64]) -> Result<ExecutionStats, cintxRsError>;`

cintx-core dependents (no circular-dep risk placing precision.rs here): cintx-cubecl, cintx-rs, cintx-runtime, cintx-compat all `cintx-core = { path = "../cintx-core" }`.
cintx-core/Cargo.toml currently deps only `thiserror = "2"` and `smallvec = "1.13"` — `num-traits` is transitive (0.2.19 in Cargo.lock) but NOT a direct dep yet.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Serena onboarding process gate (D-11)</name>
  <read_first>
    - .planning/phases/20-precision-generic-f64-f32-switch/20-CONTEXT.md (D-11)
    - .planning/phases/20-precision-generic-f64-f32-switch/20-PATTERNS.md (§Serena symbol-aware edit; FREEZE Inventory)
  </read_first>
  <action>
    Call the serena MCP `check_onboarding_performed` tool first; if it reports onboarding is not done, call `initial_instructions` and complete onboarding for this repository. This is a HARD PROCESS GATE per D-11: every symbol edit in Plans 02-08 MUST use serena symbol-aware tools (`find_symbol`, `find_referencing_symbols`, `replace_symbol_body`, `insert_before_symbol`, `insert_after_symbol`), never blind text replacement. Record in the SUMMARY that onboarding ran and which serena tools are available. Do NOT proceed to Task 1 until serena onboarding is confirmed. This gate applies to the entire phase; later plans inherit it.
  </action>
  <verify>
    <automated>MISSING — process gate; verified by serena tool response in execution log, not a shell command</automated>
    <human-check>Executor's SUMMARY records that serena check_onboarding_performed / initial_instructions ran before any symbol edit</human-check>
  </verify>
  <acceptance_criteria>
    - serena `check_onboarding_performed` returns a positive onboarding state (or `initial_instructions` was run to establish it)
    - SUMMARY explicitly names the serena symbol tools that will be used in Waves 1-5
    - No symbol edit in this plan or any later plan in Phase 20 uses blind text replacement
  </acceptance_criteria>
  <done>Serena onboarding confirmed; the symbol-aware refactor mandate is recorded as active for the phase.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Create CintFloat sealed trait + PrecisionKind enum in cintx-core</name>
  <files>crates/cintx-core/src/precision.rs, crates/cintx-core/src/lib.rs, crates/cintx-core/Cargo.toml</files>
  <read_first>
    - crates/cintx-core/src/lib.rs (existing module declarations + re-export style)
    - crates/cintx-core/Cargo.toml (current [dependencies])
    - crates/cintx-cubecl/src/math/boys.rs:25-26 (const/comment conventions to mirror)
    - .planning/phases/20-precision-generic-f64-f32-switch/20-PATTERNS.md (§precision.rs trait body, lines 51-82)
    - .planning/phases/20-precision-generic-f64-f32-switch/20-RESEARCH.md (Pattern 3 + §Recommended Sealed Super-Trait)
  </read_first>
  <behavior>
    - Test: f64 implements CintFloat; `<f64 as CintFloat>::from_f64_lossy(0.886226925452758) == 0.886226925452758` (exact, no precision loss)
    - Test: f32 implements CintFloat; `<f32 as CintFloat>::from_f64_lossy(0.886226925452758) == 0.886226925452758_f64 as f32`
    - Test: PrecisionKind::default() == PrecisionKind::F64
    - Test: PrecisionKind::F64.element_size() == 8 and PrecisionKind::F32.element_size() == 4
    - Compile-negative (documented, not a test): no type other than f64/f32 can implement CintFloat because the `sealed::Sealed` supertrait is private
  </behavior>
  <action>
    Add `num-traits = "0.2"` to `crates/cintx-core/Cargo.toml [dependencies]` (it is already transitive 0.2.19 in Cargo.lock; this makes it a direct dep). Create `crates/cintx-core/src/precision.rs` with: a private `mod sealed { pub trait Sealed {} }`; `pub trait CintFloat: Copy + Send + Sync + 'static + num_traits::Float + num_traits::FromPrimitive + sealed::Sealed { fn from_f64_lossy(x: f64) -> Self; }`; `impl sealed::Sealed for f64 {}` + `impl CintFloat for f64 { fn from_f64_lossy(x: f64) -> Self { x } }`; `impl sealed::Sealed for f32 {}` + `impl CintFloat for f32 { fn from_f64_lossy(x: f64) -> Self { x as f32 } }`; and `#[derive(Clone, Copy, Debug, Default, PartialEq, Eq)] pub enum PrecisionKind { #[default] F64, F32 }` with `impl PrecisionKind { pub const fn element_size(self) -> usize { match self { Self::F64 => 8, Self::F32 => 4 } } }`. Add `pub mod precision;` to `crates/cintx-core/src/lib.rs` and re-export `pub use precision::{CintFloat, PrecisionKind};` at the crate root. Add a `#[cfg(test)] mod tests` covering the behaviors above. Use serena `insert_after_symbol` to add the `pub mod precision;` line and the re-export; create the new file with the Write tool (new file, no symbol to target). Document in the module rustdoc that device `#[cube]` kernels bound `F: cubecl::prelude::Float` (NOT CintFloat) and only host wrappers + the public `evaluate::<F>` bound `F: CintFloat` (per PATTERNS §Sealed-trait precision bound).
  </action>
  <verify>
    <automated>cargo test -p cintx-core precision 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `crates/cintx-core/src/precision.rs` contains `pub trait CintFloat` and `pub enum PrecisionKind`
    - `crates/cintx-core/Cargo.toml` lists `num-traits` under `[dependencies]`
    - `cargo test -p cintx-core precision` passes (all CintFloat/PrecisionKind unit tests green)
    - `cargo check -p cintx-core` exits 0
    - `cintx_core::CintFloat` and `cintx_core::PrecisionKind` are reachable from the crate root (re-exported)
  </acceptance_criteria>
  <done>CintFloat is sealed to exactly f64+f32 with `from_f64_lossy`; PrecisionKind defaults to F64 with correct element sizes; both are re-exported from cintx-core; unit tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Add precision field to ExecutionPlan (default F64) + prove A5 bytemuck staging cast</name>
  <files>crates/cintx-runtime/src/planner.rs, crates/cintx-runtime/Cargo.toml, crates/cintx-cubecl/tests/bytemuck_staging_cast_spike.rs</files>
  <read_first>
    - crates/cintx-runtime/src/planner.rs:61 (`pub struct ExecutionPlan<'a>` — the struct to extend; read its full field list and every constructor/literal that builds it)
    - crates/cintx-runtime/src/planner.rs:318-328 (`try_alloc_staging` — the OOM-safe alloc contract that must NOT change here)
    - crates/cintx-runtime/Cargo.toml (confirm cintx-core dep + whether bytemuck is available)
    - crates/cintx-cubecl/src/kernels/mod.rs:19 (FamilyLaunchFn signature — staging is `&mut [f64]`)
    - .planning/phases/20-precision-generic-f64-f32-switch/20-RESEARCH.md (Pattern 5; Assumption A5; Open Question 1)
    - .planning/phases/20-precision-generic-f64-f32-switch/20-PATTERNS.md (§dispatch.rs / §planner.rs FROZEN shapes)
  </read_first>
  <action>
    PART A (ExecutionPlan field): Use serena `find_symbol("ExecutionPlan")` then `find_referencing_symbols` to enumerate every site that constructs an `ExecutionPlan` literal. Add a `pub precision: PrecisionKind` field (import `cintx_core::PrecisionKind`). Because `PrecisionKind` derives `Default`, prefer initializing existing constructors with `precision: PrecisionKind::default()` so the f64 path is byte-identical. Use serena `replace_symbol_body` on the struct and each constructor — do NOT blind-edit. Confirm `cintx-runtime/Cargo.toml` already deps `cintx-core` (it does); no new dep needed for Part A.

    PART B (A5 spike — prove the bytemuck cast is sound, the ONE open judgment call): Create `crates/cintx-cubecl/tests/bytemuck_staging_cast_spike.rs` (cintx-cubecl already deps `bytemuck = { version = "1", features = ["derive"] }`). Write tests that: (1) allocate a `Vec<f64>` sized for N f64 elements, then `bytemuck::cast_slice_mut::<f64, f64>` round-trips identically; (2) allocate a `Vec<u8>` byte buffer sized `N * size_of::<f32>()`, reinterpret as `&mut [f32]` via `bytemuck::cast_slice_mut`, write f32 values, read them back bit-exact; (3) assert `bytemuck::cast_slice_mut::<f64, f32>` over a buffer sized `M*8` bytes yields `2*M` f32 lanes (size relationship the planner relies on for byte-sizing); (4) assert alignment: an 8-byte-aligned f64 buffer is validly reinterpretable as f32 (4-byte align is a divisor of 8). If ANY cast panics or is rejected by bytemuck (the A5 fallback condition), STOP and record in the SUMMARY a `## BLOCKER: A5 cast unsound` note recommending the separate `Vec<F>` staging-path fallback (which widens Wave 3) so the orchestrator can replan Wave 3 before it starts. Otherwise record A5 as PROVEN.
  </action>
  <verify>
    <automated>cargo test -p cintx-cubecl --test bytemuck_staging_cast_spike --features cpu 2>&1 | tail -15 && CINTX_BACKEND=cpu cargo check --workspace --features cpu 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ExecutionPlan` has a `pub precision: PrecisionKind` field; `grep -n "precision" crates/cintx-runtime/src/planner.rs` shows the field
    - `crates/cintx-cubecl/tests/bytemuck_staging_cast_spike.rs` exists and `cargo test -p cintx-cubecl --test bytemuck_staging_cast_spike --features cpu` passes (A5 PROVEN) OR the SUMMARY contains `## BLOCKER: A5 cast unsound`
    - `CINTX_BACKEND=cpu cargo check --workspace --features cpu` exits 0 (all crates compile with the new field)
    - `try_alloc_staging` in planner.rs is unchanged (still uses `try_reserve_exact` + typed `HostAllocationFailed`; no partial-write semantics introduced)
  </acceptance_criteria>
  <done>ExecutionPlan carries precision (default F64, f64 path byte-identical); the A5 bytemuck f64↔f32 staging reinterpretation is proven sound (or a blocker is recorded for orchestrator replan); workspace compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host f64 staging buffer ↔ device-typed `&mut [F]` view | Internal reinterpretation of an owned byte buffer; no external input crosses here. The risk is memory-safety (mis-sized / mis-aligned cast), not an attacker. |
| const f64 tables ↔ generic `F` injection | Internal numeric conversion; risk is correctness (precision loss on the wrong path), not a security surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering / Elevation | `bytemuck::cast_slice_mut` over the staging buffer (A5) | mitigate | Wave-0 spike test proves size + alignment soundness for f64↔f32 BEFORE any kernel wave depends on it; `F: bytemuck::Pod` (both f32/f64) enforced; cast rejection triggers a documented BLOCKER + fallback path, never an unchecked reinterpret. |
| T-20-02 | Tampering | `CintFloat::from_f64_lossy` const-table injection | mitigate | Sealed trait restricts implementors to f64 (lossless) and f32 (documented lossy at the threshold boundary only); no NaN/Inf is introduced by the conversion (V5 partial — env validation unchanged). |
| T-20-03 | Tampering | accidental genericization of a FROZEN ABI/const site | mitigate | FREEZE inventory (PATTERNS §FREEZE Inventory) enumerates every f64 site to skip; D-11 serena symbol-scoped edits prevent the blind-replace corruption of env/atm/bas, C ABI, and const-table values. |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No new external packages installed this phase; `num-traits 0.2.19` already in Cargo.lock and slopcheck-approved (RESEARCH §Package Legitimacy Audit). No `[ASSUMED]`/`[SUS]` packages. |
</threat_model>

<verification>
- `CINTX_BACKEND=cpu cargo check --workspace --features cpu` exits 0 (scaffolding does not break any crate).
- `cargo test -p cintx-core precision` green (CintFloat/PrecisionKind contracts).
- `cargo test -p cintx-cubecl --test bytemuck_staging_cast_spike --features cpu` green (A5 proven) or BLOCKER recorded.
- No existing test regresses (f64 path is the default monomorphization; behavior unchanged).
</verification>

<success_criteria>
- CintFloat sealed to exactly {f64, f32}; PrecisionKind enum with F64 default and 8/4 element sizes; both re-exported from cintx-core.
- ExecutionPlan carries `precision: PrecisionKind` defaulting to F64.
- num-traits is a direct cintx-core dependency.
- The A5 bytemuck staging cast is proven sound (or a blocker for orchestrator replan is recorded).
- Serena onboarding gate confirmed; symbol-aware refactor mandate active for the phase.
- Zero f64-path behavior change.
</success_criteria>

<output>
Create `.planning/phases/20-precision-generic-f64-f32-switch/20-01-SUMMARY.md` when done. Record: serena onboarding state, the A5 spike result (PROVEN or BLOCKER + fallback recommendation), and the exact precision-vocabulary symbols (`CintFloat`, `PrecisionKind`) downstream waves consume.
</output>
