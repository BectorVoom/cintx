---
phase: 21-coulomb-gradient-intors
plan: 08
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05, 06, 07]
files_modified:
  - crates/cintx-ops/generated/compiled_manifest.lock.json
  - .planning/notes/phase-21-pyscf-rs-handoff.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
requirements: [GRAD-10]
autonomous: true
must_haves:
  truths:
    - "The component-leading F-order layout of int2e_ip1 / int3c2e_ip1 is validated against the vendor layout (Risk R3) and documented"
    - "All 6 gradient families + int3c2e_ip1 are flipped to oracle_covered=true in the manifest after their parity gates are green, and manifest-audit passes"
    - "A pyscf_rs hand-off note records exactly which Phase 7 workflow_dispatch gradient arms un-gate, plus the int3c2e_ip1 re-gating history"
    - "cintx ROADMAP/STATE/REQUIREMENTS are updated to mark Phase 21 complete and GRAD-01..GRAD-10 done"
    - "The full vendor-gated oracle suite for the affected families is green at atol=1e-12"
  artifacts:
    - path: ".planning/notes/phase-21-pyscf-rs-handoff.md"
      provides: "the consumer-un-gate hand-off note (which Phase 7 arms flip, int3c2e_ip1 history)"
      contains: "workflow_dispatch"
    - path: "crates/cintx-ops/generated/compiled_manifest.lock.json"
      provides: "oracle_covered=true for all 6 gradient families + int3c2e_ip1"
      contains: "oracle_covered"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "GRAD-01..GRAD-10 traceability rows"
      via: "status flipped from Pending to Complete"
      pattern: "GRAD-"
    - from: ".planning/notes/phase-21-pyscf-rs-handoff.md"
      to: "pyscf_rs Phase 7 analytical-gradient arms"
      via: "the un-gate list (RHF/UHF/RKS/UKS/MP2/CCSD + CPHF + geomopt)"
      pattern: "workflow_dispatch"
---

<objective>
Close out Phase 21: validate the component-leading F-order layout against the vendor layout (Risk R3), flip `oracle_covered=true` for all 6 gradient families + `int3c2e_ip1` in the manifest, run the full vendor-gated oracle suite green, write the pyscf_rs hand-off note (which Phase 7 `workflow_dispatch` gradient arms un-gate + the `int3c2e_ip1` re-gating history), and update cintx ROADMAP/STATE/REQUIREMENTS to mark the phase and GRAD-01..GRAD-10 complete.

Purpose: This is the verification + consumer-hand-off plan. The "real acceptance test" is the consumer un-gate (CONTEXT §Specific Ideas): once these families are green, pyscf_rs flips its Phase 7 `workflow_dispatch` gradient arms to always-on, and RHF/UHF/RKS/UKS/MP2/CCSD analytical gradients ride the existing FD gate (`grad.verify_fd`) at ≤1e-6 Ha/Bohr + upstream-PySCF parity at ≤1e-7 Ha/Bohr — with ZERO pyscf_rs rework (the dispatch shape + component-leading layout are already wired in pyscf-gto). The F-order validation (R3) is the load-bearing correctness check: if cintx's `[3, …]` component-leading layout does not match pyscf-gto `layout_table.rs`, the consumer repacks wrong.

Output: the F-order layout validation result; the manifest oracle_covered flips + green manifest-audit; the full oracle suite green; `.planning/notes/phase-21-pyscf-rs-handoff.md`; updated ROADMAP (Phase 21 → Complete, 8/8 plans), STATE (position + decisions), REQUIREMENTS (GRAD-01..GRAD-10 → Complete).
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-coulomb-gradient-intors/21-CONTEXT.md
@.planning/phases/21-coulomb-gradient-intors/21-RESEARCH.md
@.planning/phases/21-coulomb-gradient-intors/21-PATTERNS.md
@.planning/notes/pyscf-rs-as-cintx-consumer.md

<interfaces>
<!-- Concrete current contracts the executor needs. -->

Manifest oracle_covered flips: crates/cintx-ops/generated/compiled_manifest.lock.json. 21-02 registered the new families with oracle_covered:false (and set int3c2e_ip1 to false pending 21-06). After each family's parity gate is green (21-03..21-07), flip its operator rows' `oracle_covered` to true. The xtask `oracle-covered-update` subcommand (xtask/src/main.rs:80) stamps helper/transform/optimizer/legacy entries; for operator entries, edit the lock directly (Phase 19 precedent, STATE.md:255) and `cargo build -p cintx-ops` to regenerate. manifest-audit (xtask/src/main.rs:75) checks oracle_covered only for stability=stable entries (Phase 15 D-07).

xtask invocation: `cargo run -p xtask -- manifest-audit` (the crate is at ./xtask, package name "xtask"). `cargo run -p xtask -- oracle-covered-update` stamps the helper/optimizer/legacy siblings.

pyscf_rs consumer: .planning/notes/pyscf-rs-as-cintx-consumer.md documents the path-dep wiring; pyscf-gto/src/intor.rs wraps SessionRequest; pyscf-grad/src/hooks.rs:24 is the DF-grad path consuming int3c2e_ip1 as a derivative (the R1 consumer). pyscf_rs Phase 7 (Gradients + Geomopt) `.planning/phases/07-gradients-geomopt/07-RESEARCH.md` §"Gradient-Integral Availability Matrix" lists the arms. The grad arms that un-gate: RHF/UHF/RKS/UKS/MP2/CCSD analytical gradients + CPHF + geomopt.

F-order layout (Risk R3): pyscf-gto `layout_table.rs` declares component-leading `[3, …]` F-order. The validation is the element-for-element oracle comparison in 21-05 (int2e_ip1) and 21-06 (int3c2e_ip1) vs libcint's own ip1 output — a layout mismatch produces nonzero count_mismatches. This plan documents the validation result (it does not re-run the kernels; it confirms the 21-05/21-06 oracle tests pass and records the layout-parity conclusion).

ROADMAP: Phase 21 entry at the "## v1.3 Milestone" section (the `### Phase 21` block + the `| Phase 21 ... | 0/8 | Planned |` Progress row + the per-plan checkboxes). Flip to 8/8 Complete + check the 8 plan boxes.
REQUIREMENTS: the Traceability table rows GRAD-01..GRAD-10 (currently "Phase 21 | Pending") + the v1.3 requirement checkboxes (currently `- [ ]`) + the Coverage summary.
STATE: Current Position + Accumulated Context Decisions + the milestone progress counters.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate F-order layout (R3), flip oracle_covered, run the full oracle suite green</name>
  <files>crates/cintx-ops/generated/compiled_manifest.lock.json</files>
  <read_first>
    - crates/cintx-ops/generated/compiled_manifest.lock.json (the 6 gradient family operator rows + int3c2e_ip1 — read their current oracle_covered values left by 21-02..21-07)
    - .planning/phases/21-coulomb-gradient-intors/21-05-SUMMARY.md (the int2e_ip1 F-order transpose stride + R3 validation result)
    - .planning/phases/21-coulomb-gradient-intors/21-06-SUMMARY.md (the int3c2e_ip1 F-order + oracle-flip result)
    - xtask/src/main.rs:75-80 (manifest-audit + oracle-covered-update subcommands)
    - .planning/phases/21-coulomb-gradient-intors/21-CONTEXT.md (Risk R3 / D-10 — F-order layout vs vendor, byte-identity gate)
  </read_first>
  <action>
    PART A (R3 layout validation): Confirm the int2e_ip1 (21-05) and int3c2e_ip1 (21-06) oracle parity tests pass element-for-element vs the vendor ip1 references — this element-wise byte-identity IS the component-leading `[3, …]` F-order validation (a layout mismatch yields nonzero mismatches). Re-run `cargo test -p cintx-oracle --features cpu --test two_electron_ip1_parity` and `... --test center_3c2e_parity` and record the result. Read the 21-05/21-06 SUMMARYs for the documented transpose strides and confirm they match the pyscf-gto `layout_table.rs` component-leading convention. Document the conclusion (R3 validated) in this plan's SUMMARY.

    PART B (oracle_covered flips): In compiled_manifest.lock.json, flip `oracle_covered` from false to true for the operator rows of int1e_ipovlp, int1e_ipkin, int1e_ipnuc, int1e_iprinv, int2e_ip1, int1e_ecp_iprinv (cart+sph; leave spinor oracle_covered=false since spinor returns UnsupportedApi, R5), and confirm int3c2e_ip1 (cart+sph) is flipped to true (21-06's repair). Run `cargo run -p xtask -- oracle-covered-update` to stamp the helper/optimizer/legacy siblings, then `cargo build -p cintx-ops` to regenerate api_manifest.rs.

    PART C (full oracle suite): Run the full vendor-gated oracle suite for all affected families and confirm 0 mismatches at atol=1e-12. Then run `cargo run -p xtask -- manifest-audit` and confirm it is GREEN (every stable oracle_covered=true entry has a passing parity record; no missing/extra rows). Keep JSON well-formed (python3 -m json.tool).
  </action>
  <verify>
    <automated>cargo test -p cintx-oracle --features cpu --test two_electron_ip1_parity --test center_3c2e_parity --test one_electron_grad_parity --test one_electron_nuc_grad_parity --test ecp_iprinv_parity 2>&1 | tail -25 && python3 -m json.tool crates/cintx-ops/generated/compiled_manifest.lock.json > /dev/null && cargo run -p xtask -- manifest-audit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cintx-oracle --features cpu --test two_electron_ip1_parity --test center_3c2e_parity --test one_electron_grad_parity --test one_electron_nuc_grad_parity --test ecp_iprinv_parity` exits 0 (full gradient oracle suite green at atol=1e-12)
    - `grep -c '"oracle_covered": true' crates/cintx-ops/generated/compiled_manifest.lock.json` increased by the new gradient cart+sph rows (12 operator rows: 6 families × 2 reps + int3c2e_ip1 cart/sph already counted)
    - The int3c2e_ip1 cart/sph rows now have `oracle_covered: true` — `grep -A8 '"symbol": "int3c2e_ip1_cart"' crates/cintx-ops/generated/compiled_manifest.lock.json` shows true
    - `cargo run -p xtask -- manifest-audit` exits 0 (green: every stable oracle_covered=true entry verified, no missing/extra rows)
    - `python3 -m json.tool crates/cintx-ops/generated/compiled_manifest.lock.json` exits 0
  </acceptance_criteria>
  <done>The R3 F-order layout is validated against vendor; all 6 gradient families (cart+sph) + int3c2e_ip1 are oracle_covered=true; the full gradient oracle suite is green at atol=1e-12; manifest-audit is green.</done>
</task>

<task type="auto">
  <name>Task 2: Write the pyscf_rs hand-off note + update ROADMAP/STATE/REQUIREMENTS</name>
  <files>.planning/notes/phase-21-pyscf-rs-handoff.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/notes/pyscf-rs-as-cintx-consumer.md (the consumer path-dep wiring + the files that bind cintx's safe API)
    - .planning/ROADMAP.md (the `### Phase 21` block + the Progress table row + the per-plan checkboxes — the exact strings to flip)
    - .planning/REQUIREMENTS.md (the GRAD-01..GRAD-10 v1.3 checkboxes + the Traceability table rows + the Coverage summary)
    - .planning/STATE.md (Current Position + Accumulated Context Decisions + milestone progress)
    - .planning/phases/21-coulomb-gradient-intors/21-CONTEXT.md (§Specific Ideas — the consumer-un-gate criterion + Risk R1 history)
    - all 21-01..21-07 SUMMARYs (for the decisions + outcomes to roll up into STATE)
  </read_first>
  <action>
    PART A (hand-off note): Create `.planning/notes/phase-21-pyscf-rs-handoff.md` recording:
    - The 6 gradient families + int3c2e_ip1 are now byte-identical to libcint 6.1.3 at atol=1e-12 (cart+sph; spinor is register-but-UnsupportedApi, R5).
    - Which pyscf_rs Phase 7 `workflow_dispatch` gradient arms un-gate: RHF/UHF/RKS/UKS/MP2/CCSD analytical gradients + CPHF + geomopt — they ride the existing `grad.verify_fd` FD gate (≤1e-6 Ha/Bohr) and upstream-PySCF parity (≤1e-7 Ha/Bohr). NO pyscf_rs rework needed (dispatch shape + component-leading layout already wired in pyscf-gto).
    - The `int3c2e_ip1` re-gating history (Risk R1): it was a registered-but-stubbed operator-blind scalar kernel that silently returned the plain 3c2e integral; the oracle "passed" only because it referenced plain `vendor_int3c2e_*`. 21-06 shipped the real derivative kernel and flipped the oracle to `vendor_int3c2e_ip1_*`. pyscf-grad/src/hooks.rs:24 (the DF-grad consumer) now receives a correct derivative.
    - The l-ceiling caveat (Risk R2): gradients with nroots > 5 (high-l f/g quartets after the li→li+1 raise) return UnsupportedApi — same ceiling as base int2e; the consumer should not request f/g gradient quartets until the deferred Wheeler-fallback work lands.
    - The int2e_ip1 safe-API vs raw-path note (Risk R6, from the 21-05 SUMMARY): which entry point pyscf-gto calls and whether the safe-API arm shipped or is deferred to Phase 18 completion.

    PART B (ROADMAP): Flip the Phase 21 Progress row from `0/8 | Planned` to `8/8 | Complete` with the completion date; check all 8 plan checkboxes `- [x] 21-0N-PLAN.md`; if the `### Phase 21` block has any `[To be planned]` placeholder leave the real Goal text intact.

    PART C (REQUIREMENTS): Flip the 10 v1.3 GRAD checkboxes from `- [ ]` to `- [x]`; change the Traceability rows `GRAD-01..GRAD-10 | Phase 21 | Pending` to `Complete`; update the Coverage summary (v1.3 gradient: 10/10 Complete).

    PART D (STATE): Update Current Position to Phase 21 complete; add the Phase 21 decisions to Accumulated Context (the rinv-origin 4-step pattern, the gout_ip1 verbatim reuse for plain-Coulomb grads, the int3c2e_ip1 R1 repair, the ecp_iprinv single-atom selection, the R3 F-order validation); update the milestone progress counters and Session Continuity. Use the Write tool for the new note; use Edit/targeted edits for the planning-doc updates (these are LIVE planning docs, not historical phase docs — they are editable).
  </action>
  <verify>
    <automated>test -f .planning/notes/phase-21-pyscf-rs-handoff.md && grep -c "workflow_dispatch\|int3c2e_ip1\|RHF\|geomopt" .planning/notes/phase-21-pyscf-rs-handoff.md && grep -c "GRAD-0[1-9]\|GRAD-10" .planning/REQUIREMENTS.md && grep -n "Phase 21.*8/8\|Phase 21.*Complete" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/notes/phase-21-pyscf-rs-handoff.md` exists and contains the un-gate arm list (`grep -c "RHF\|UHF\|RKS\|UKS\|MP2\|CCSD\|geomopt" .planning/notes/phase-21-pyscf-rs-handoff.md` ≥ 1) and the int3c2e_ip1 R1 re-gating history
    - `.planning/REQUIREMENTS.md` shows GRAD-01..GRAD-10 as `- [x]` and Traceability rows as Complete (`grep -c "GRAD-.*Complete" .planning/REQUIREMENTS.md` == 10)
    - `.planning/ROADMAP.md` shows Phase 21 as `8/8 | Complete` and all 8 plan checkboxes checked (`grep -c "\[x\] 21-0" .planning/ROADMAP.md` == 8)
    - `.planning/STATE.md` Current Position reflects Phase 21 complete and the Phase 21 decisions are added to Accumulated Context
  </acceptance_criteria>
  <done>The pyscf_rs hand-off note records the un-gate arms + the int3c2e_ip1 R1 history + the R2/R6 caveats; ROADMAP/STATE/REQUIREMENTS are updated to mark Phase 21 and GRAD-01..GRAD-10 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cintx gradient output ↔ pyscf_rs consumer repack | The consumer trusts the component-leading [3, …] F-order; a layout drift silently corrupts every downstream gradient. This is the R3 boundary. |
| manifest oracle_covered=true ↔ CI verification claim | Flipping oracle_covered to true asserts the family is libcint-verified; a premature flip (before parity is green) is a false verification claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-08-01 | Denial of Service (silent-wrong) | F-order layout drift vs pyscf-gto layout_table.rs (R3) | mitigate | The element-for-element oracle comparison (21-05/21-06) vs libcint's own ip1 output validates the component-leading layout; this plan re-confirms those tests pass before signing off, and documents the layout-parity conclusion in the hand-off note so the consumer can rely on it. |
| T-21-08-02 | Repudiation / Tampering | premature oracle_covered=true flip (false verification claim) | mitigate | oracle_covered is flipped to true ONLY after the family's parity gate is green; manifest-audit (stable-entries-only, Phase 15 D-07) re-checks every flipped entry has a passing record before the phase is marked complete. |
| T-21-08-03 | Denial of Service (silent-wrong) | int3c2e_ip1 R1 history lost (consumer unaware the kernel was once stubbed) | mitigate | The hand-off note records the full R1 re-gating history (stub → real derivative + oracle flip) so the consumer knows pyscf-grad now gets a correct derivative and can drop any local workaround. |
| T-21-08-04 | Tampering | historical phase docs edited (MEMORY: .planning/phases/** immutable) | accept | Only LIVE planning docs (ROADMAP, STATE, REQUIREMENTS) + a new note are edited; the historical 21-CONTEXT/RESEARCH/PATTERNS and prior phase docs are NOT touched (per the user-memory immutability rule). |
| T-21-08-SC | Tampering | npm/pip/cargo installs | accept | No new external packages; only existing workspace crates + planning-doc edits. |
</threat_model>

<verification>
- `cargo test -p cintx-oracle --features cpu` full gradient suite green (two_electron_ip1, center_3c2e, one_electron_grad, one_electron_nuc_grad, ecp_iprinv) at atol=1e-12.
- `cargo run -p xtask -- manifest-audit` green (oracle_covered flips verified).
- `.planning/notes/phase-21-pyscf-rs-handoff.md` exists with the un-gate arm list + R1 history.
- ROADMAP/STATE/REQUIREMENTS updated (Phase 21 → Complete; GRAD-01..GRAD-10 → Complete).
- `CINTX_BACKEND=cpu cargo check --workspace --features cpu` exits 0.
</verification>

<success_criteria>
- The component-leading F-order layout is validated against the vendor layout (Risk R3) (GRAD-10).
- All 6 gradient families + int3c2e_ip1 are oracle_covered=true; manifest-audit green (GRAD-10).
- The pyscf_rs hand-off note records the un-gating Phase 7 arms + the int3c2e_ip1 re-gating history (GRAD-10).
- cintx ROADMAP/STATE/REQUIREMENTS updated to mark Phase 21 and GRAD-01..GRAD-10 complete (GRAD-10).
</success_criteria>

<output>
Create `.planning/phases/21-coulomb-gradient-intors/21-08-SUMMARY.md` when done. Record: the R3 F-order validation conclusion, the final oracle_covered count, the manifest-audit result, and a one-line pointer to the hand-off note for the pyscf_rs maintainer.
</output>
