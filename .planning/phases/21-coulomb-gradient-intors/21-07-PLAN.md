---
phase: 21-coulomb-gradient-intors
plan: 07
type: execute
wave: 4
depends_on: [01, 02]
files_modified:
  - crates/cintx-cubecl/src/kernels/ecp.rs
  - crates/cintx-oracle/src/vendor_ffi.rs
  - crates/cintx-oracle/tests/ecp_iprinv_parity.rs
requirements: [GRAD-09]
autonomous: true
must_haves:
  truths:
    - "Phase 19's scalar-ECP K-Taylor byte-identity path (ECPrad_part / K_TAB) is confirmed exercised by the Cu/LANL2DZ gate BEFORE building ECPscalar_iprinv (Risk R4 pre-req)"
    - "ECPscalar_iprinv (cart + sph, 3 components) matches vendored libcint at atol=1e-12 on Cu/LANL2DZ"
    - "launch_ecp adds an ecp_iprinv arm that selects a SINGLE ECP shell (the atom matching the rinv origin), not the all-slot ipnuc accumulation"
    - "ecp_iprinv reuses the deriv1_cart_pair driver (comp=3) shared with ecp_ipnuc; it differs only in single-atom selection and no all-slot accumulation"
    - "Spinor reps return UnsupportedApi (Risk R5)"
  artifacts:
    - path: "crates/cintx-cubecl/src/kernels/ecp.rs"
      provides: "ecp_iprinv per-nucleus selector arm in launch_ecp"
      contains: "ecp_iprinv"
    - path: "crates/cintx-oracle/tests/ecp_iprinv_parity.rs"
      provides: "byte-identity parity tests for ECPscalar_iprinv cart+sph (Cu/LANL2DZ, per-nucleus sweep)"
      contains: "ECPscalar_iprinv"
  key_links:
    - from: "crates/cintx-cubecl/src/kernels/ecp.rs"
      to: "plan.operator_env_params.rinv_orig"
      via: "single-atom ECP-shell selection by matching the rinv origin to atoms[c].coord_bohr"
      pattern: "rinv_orig"
    - from: "crates/cintx-oracle/tests/ecp_iprinv_parity.rs"
      to: "cintx_oracle::vendor_ffi::vendor_ECPscalar_iprinv_sph"
      via: "count_mismatches at atol=1e-12; env sets the rinv origin per nucleus"
      pattern: "vendor_ECPscalar_iprinv"
---

<objective>
Implement `ECPscalar_iprinv` (per-nucleus scalar-ECP force, single rinv origin, no all-slot `-Z_C` accumulation) as a new `ecp_iprinv` arm in `launch_ecp`, reusing the Phase-19 `deriv1_cart_pair` gradient driver (comp=3) but selecting only the single ECP shell whose atom matches the rinv origin. Add byte-identity oracle parity tests vs vendored libcint `ECPscalar_iprinv_{cart,sph}` at atol=1e-12 on Cu/LANL2DZ. The FIRST task is the mandatory Risk-R4 confirmation that Phase 19's scalar ECP path is K-Taylor byte-identical, not the old direct-quadrature approximation.

Purpose: `ECPscalar_iprinv` is the per-nucleus ECP force term. It differs from the already-shipped `ECPscalar_ipnuc` (Phase 19-07, byte-identity confirmed) only in that `ipnuc` sums the gradient over ALL ECP slots while `iprinv` selects the SINGLE atom matching the rinv origin and drops the all-slot accumulation (D-09). The vendor reference `ECPscalar_iprinv_cart` (nr_ecp_deriv.c:333) confirms this: it reads `atm_id = env[AS_RINV_ORIG_ATOM]`, selects one shell via `_one_shell_ecpbas`, and runs the SAME `_deriv1_cart` (comp=3) driver as ipnuc on that single shell. Byte-identity is reachable ONLY because Phase 19's K-Taylor port (`ECPrad_part`/`K_TAB`) made the scalar ECP primitives PySCF-exact — Risk R4. The blocker is RESOLVED per STATE.md (the 19-05..08 K-Taylor replan shipped; all four ECP rows are oracle_covered=true) but Task 0 re-confirms it on the actual tree before building.

Output: the R4 confirmation note; the `ecp_iprinv` single-atom-selection arm in `launch_ecp`; the `vendor_ECPscalar_iprinv_{cart,sph}` FFI wrappers; `ecp_iprinv_parity.rs` with per-nucleus byte-identity tests on Cu/LANL2DZ.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-coulomb-gradient-intors/21-CONTEXT.md
@.planning/phases/21-coulomb-gradient-intors/21-RESEARCH.md
@.planning/phases/21-coulomb-gradient-intors/21-PATTERNS.md

<interfaces>
<!-- Concrete current contracts the executor needs. -->

launch_ecp (ecp.rs:1351) dispatcher (relative line ~1370):
```rust
let operator_name = plan.descriptor.operator_name();
let is_gradient = match operator_name {
    "ecp"        => false,
    "ecp_ipnuc"  => true,
    other => { return Err(cintxRsError::UnsupportedApi { requested: format!("unknown ecp operator name: {other}") }); }
};
let n_comp = if is_gradient { 3 } else { 1 };   // ecp.rs:~1432
```
Extend the match with `"ecp_iprinv" => true,` and track `let is_iprinv = operator_name == "ecp_iprinv";`.

deriv1_cart_pair (ecp.rs:1181) is the gradient driver (comp=3); it accumulates gpx/gpy/gpz into `gctr[g] / gctr[dij+g] / gctr[2*dij+g]` (lines 1278-1280) — the component-leading F-order [axis, ao_j, ao_i] (D-11 from Phase 19). `compute_type1_pair_grad` (ecp.rs:1295, lc=-1) and `compute_type2_pair_grad` (ecp.rs:1316, lc=l) are the thin aliases. The ipnuc launch loops over ALL ECP shells/atoms accumulating; the gradient inner loop is at ecp.rs:~1480-1500 (`match (is_gradient, slot.lc < 0)`).

K-Taylor confirmation (R4): the scalar ECP primitives use ECPrad_part / type1_rad_part / type2_facs_rad (the PySCF-exact recurrences ported in Phase 19-05..06; ecp.rs module docs lines 13-20 reference them). The Phase 19 Cu/LANL2DZ gate (int1e_ecp_{cart,sph} + int1e_ecp_ipnuc_{cart,sph}) is oracle_covered=true (STATE.md "RESOLVED 2026-05-20").

rinv origin → atom selection: `plan.operator_env_params.rinv_orig: Option<[f64;3]>` (from 21-01). The selected atom is the one whose `atoms[c].coord_bohr` matches the origin (within a tight tolerance, e.g. 1e-10). The vendor uses `env[AS_RINV_ORIG_ATOM]` (an integer atom index); cintx matches by coordinate since the safe API supplies a coordinate origin. Select the single ECP shell for that atom; skip all others (no -Z_C, no accumulation over atoms — D-09).

Salvaged 19-05 tables (Y_ADDR/Z_ADDR/CART_POW_*): per .planning/notes/19-05-partial-gradient-scaffolding.md these were a reference for an interrupted approach; the SHIPPED gradient (Phase 19-07) uses deriv1_cart_pair's gpx/gpy/gpz directly, NOT those tables. They are NOT in the tree and are NOT needed — reuse deriv1_cart_pair as-is. If any [usize;135]-vs-[usize;120] sizing artifact is encountered, it is irrelevant to this plan (the shipped path does not use those tables). Record this in the SUMMARY so the CONTEXT note is closed out.

Spinor → UnsupportedApi (R5). 3-component staging auto-allocated (manifest component_rank:"3").

Oracle FFI: ECPscalar_iprinv_cart (nr_ecp_deriv.c:333) + ECPscalar_iprinv_sph (nr_ecp_deriv.c:420) EXIST in the vendored PySCF nr_ecp tree (verified). vendor_ECPscalar_ipnuc_sph (vendor_ffi.rs:1368) is the comp=3 ECP-gradient wrapper template (the ECP slab-packing convention is documented at vendor_ffi.rs:1283). The vendor iprinv reads env[AS_RINV_ORIG_ATOM]; the test must set that env slot to the atom index for the vendor call.

Oracle harness: crates/cintx-oracle/tests/safe_api_ecp_parity.rs — build_cu_lanl2dz fixture (line 52 import), build_cu_lanl2dz_safe_basis (line 95), count_mismatches (line 326), ATOL=1e-12/RTOL=0.0 (line 58), per-symbol `#[cfg(has_vendor_libcint)]` tests (line 350+). The ipnuc parity pattern at safe_api_ecp_parity.rs:550-588 is the closest analog.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Confirm Phase 19 scalar-ECP K-Taylor byte-identity (Risk R4 pre-req — BLOCKING)</name>
  <files>crates/cintx-cubecl/src/kernels/ecp.rs</files>
  <read_first>
    - .planning/STATE.md (the "RESOLVED 2026-05-20" K-Taylor blocker entry under Blockers/Concerns)
    - crates/cintx-cubecl/src/kernels/ecp.rs:1-80 (module docs referencing ECPrad_part / K_TAB / type1_rad_part / type2_facs_rad)
    - crates/cintx-ops/generated/compiled_manifest.lock.json (the int1e_ecp_* and int1e_ecp_ipnuc_* rows — confirm oracle_covered:true)
    - .planning/phases/21-coulomb-gradient-intors/21-CONTEXT.md (D-09 / Risk R4 — the K-Taylor pre-requisite)
    - crates/cintx-oracle/tests/safe_api_ecp_parity.rs (the Cu/LANL2DZ byte-identity gate that exercises the scalar + ipnuc K-Taylor path)
  </read_first>
  <action>
    Run the existing Phase 19 ECP byte-identity gate to CONFIRM the scalar ECP primitives are PySCF-exact (K-Taylor `ECPrad_part`/`K_TAB`), not the old direct-quadrature approximation. Execute `cargo test -p cintx-oracle --features cpu --test safe_api_ecp_parity` and confirm all four ECP parity tests (int1e_ecp_{cart,sph} + int1e_ecp_ipnuc_{cart,sph}) pass at atol=1e-12 with 0 mismatches vs vendored PySCF nr_ecp. Also grep ecp.rs for `ECPrad_part`/`type1_rad_part`/`type2_facs_rad` to confirm the K-Taylor functions are present (not stubbed). Record the result in the SUMMARY. If the gate PASSES → proceed to Task 1 (R4 cleared; ECPscalar_iprinv can reach byte-identity on top). If the gate FAILS (the scalar path is NOT byte-identical) → STOP and record `## BLOCKER: R4 K-Taylor not byte-identical` in the SUMMARY recommending a K-Taylor-port pre-plan BEFORE 21-07's kernel, so the orchestrator can insert it. This is a HARD GATE: do not build ECPscalar_iprinv on an approximate scalar foundation.
  </action>
  <verify>
    <automated>cargo test -p cintx-oracle --features cpu --test safe_api_ecp_parity 2>&1 | tail -15 && grep -c "ECPrad_part\|type1_rad_part\|type2_facs_rad" crates/cintx-cubecl/src/kernels/ecp.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cintx-oracle --features cpu --test safe_api_ecp_parity` exits 0 (the 4 Phase-19 ECP tests pass at atol=1e-12) OR the SUMMARY contains `## BLOCKER: R4 K-Taylor not byte-identical`
    - `grep -c "ECPrad_part\|type1_rad_part\|type2_facs_rad" crates/cintx-cubecl/src/kernels/ecp.rs` ≥ 1 (K-Taylor recurrences present, not stubbed)
    - The SUMMARY records R4 as CLEARED (with the test output) or BLOCKED (with the pre-plan recommendation)
  </acceptance_criteria>
  <done>The Phase-19 Cu/LANL2DZ scalar-ECP K-Taylor byte-identity gate is re-confirmed green on the current tree (R4 cleared), or a blocker is recorded for a K-Taylor-port pre-plan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Implement the ecp_iprinv per-nucleus selector arm in launch_ecp</name>
  <files>crates/cintx-cubecl/src/kernels/ecp.rs</files>
  <read_first>
    - crates/cintx-cubecl/src/kernels/ecp.rs:1351-1500 (launch_ecp — the dispatcher, the is_gradient match, n_comp, the ECP-shell/atom loop, the gradient inner-loop match at ~1480)
    - crates/cintx-cubecl/src/kernels/ecp.rs:1181-1340 (deriv1_cart_pair + compute_type1/2_pair_grad — the comp=3 driver to reuse)
    - vendor/pyscf-nr-ecp/src/nr_ecp_deriv.c:333-360 (ECPscalar_iprinv_cart — the single-atom _one_shell_ecpbas selection; the reference for "select one atom, run the same _deriv1_cart, no all-slot accumulation")
    - .planning/phases/21-coulomb-gradient-intors/21-PATTERNS.md (§21-07 ecp.rs — the ecp_iprinv arm + single-origin selection)
    - .planning/phases/21-coulomb-gradient-intors/21-CONTEXT.md (D-09 — per-nucleus selector, drop -Z_C/all-slot)
  </read_first>
  <behavior>
    - Test (component count): ECPscalar_iprinv output length == 3 × nfi × nfj for a shell pair (comp=3)
    - Test (single-atom selection): with the rinv origin set to the Cu atom's coordinate, the output equals deriv1_cart_pair over ONLY that atom's ECP shell — NOT the sum over all ECP slots (the differentiator from ipnuc). Concretely: ECPscalar_iprinv at the Cu origin != ECPscalar_ipnuc for the same shell pair when there is more than one ECP-bearing atom (Cu/LANL2DZ has the ECP on Cu only, so for the single-ECP case assert iprinv-at-Cu == ipnuc; document this and add a synthetic two-ECP-atom unit case if feasible to prove the selection actually restricts)
    - Test (origin not matching any atom): an ecp_iprinv origin matching no atom returns 0 output or a typed error (mirrors vendor `_one_shell_ecpbas` returning shl_id < 0 → 0), never a panic
    - Test (determinism): repeated evaluation is bit-identical (D-10)
    - Test (UnsupportedApi for spinor): ecp_iprinv spinor returns UnsupportedApi
  </behavior>
  <action>
    Extend the launch_ecp `is_gradient` match (ecp.rs:~1370) with `"ecp_iprinv" => true,` and add `let is_iprinv = operator_name == "ecp_iprinv";`. Add the spinor guard → UnsupportedApi (R5). The `n_comp = if is_gradient { 3 } else { 1 }` line already covers comp=3 for the new arm.

    For the ecp_iprinv path (per D-09 + the vendor ECPscalar_iprinv_cart reference): instead of the ipnuc loop over ALL ECP shells/atoms, SELECT the single atom whose `atoms[c].coord_bohr` matches `plan.operator_env_params.rinv_orig` (defensive: `let origin = rinv_orig.ok_or(InvalidEnvParam { param: "PTR_RINV_ORIG", reason: ... })?;` then find `atom_c` with `(atoms[atom_c].coord_bohr - origin).norm() < 1e-10`; if none matches, return 0-filled output / typed `UnsupportedApi`, mirroring vendor `_one_shell_ecpbas` shl_id<0 → 0). Run `deriv1_cart_pair` (comp=3, via compute_type1_pair_grad lc=-1 and compute_type2_pair_grad lc=l) on ONLY that atom's ECP shell — NO accumulation over other atoms, NO -Z_C factor. Write component-leading F-order [axis, ao_j, ao_i] (the same layout deriv1_cart_pair already produces via gctr[g]/gctr[dij+g]/gctr[2*dij+g]). Run the per-component cart→sph transform for the sph rep (reuse the ECPscalar_c2s path Phase 19-07 used for ipnuc). Use serena `replace_symbol_body` on launch_ecp. Add the unit tests in the `#[cfg(test)] mod tests` block (the single-atom-selection test is the key proof iprinv ≠ all-slot ipnuc).
  </action>
  <verify>
    <automated>cargo test -p cintx-cubecl --features cpu ecp_iprinv 2>&1 | tail -15 && cargo test -p cintx-cubecl --features cpu launch_ecp 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"ecp_iprinv"\|is_iprinv' crates/cintx-cubecl/src/kernels/ecp.rs` matches (arm added)
    - `grep -n "rinv_orig" crates/cintx-cubecl/src/kernels/ecp.rs` matches (origin consumed for atom selection)
    - The ecp_iprinv path does NOT accumulate over all atoms — `grep` for the single-atom selection logic (the coord-match) and confirm no `-Z_C`/all-slot sum in the iprinv branch
    - `cargo test -p cintx-cubecl --features cpu ecp_iprinv` exits 0; the single-atom-selection + origin-mismatch + spinor-reject tests pass
    - `cargo test -p cintx-cubecl --features cpu launch_ecp` exits 0 (the ecp + ecp_ipnuc paths did not regress)
  </acceptance_criteria>
  <done>launch_ecp routes ecp_iprinv to a single-atom ECP-shell selection (by rinv-origin coordinate match) reusing deriv1_cart_pair (comp=3), dropping the all-slot/-Z_C accumulation; spinor returns UnsupportedApi; the ecp/ecp_ipnuc paths are unchanged; the selection unit tests are green.</done>
</task>

<task type="auto">
  <name>Task 2: Add vendor_ECPscalar_iprinv FFI + byte-identity oracle parity tests (Cu/LANL2DZ, per-nucleus)</name>
  <files>crates/cintx-oracle/src/vendor_ffi.rs, crates/cintx-oracle/tests/ecp_iprinv_parity.rs</files>
  <read_first>
    - crates/cintx-oracle/src/vendor_ffi.rs:1280-1420 (vendor_ECPscalar_ipnuc_sph/cart — the comp=3 ECP-gradient wrapper template + the ECP slab-packing convention doc)
    - vendor/pyscf-nr-ecp/src/nr_ecp_deriv.c:333-462 (ECPscalar_iprinv_cart/_sph — the vendor symbols to bind; note env[AS_RINV_ORIG_ATOM] must be set)
    - crates/cintx-oracle/tests/safe_api_ecp_parity.rs:52-100 (build_cu_lanl2dz + build_cu_lanl2dz_safe_basis) + :326-390 (count_mismatches + the ipnuc per-symbol test pattern)
    - .planning/phases/21-coulomb-gradient-intors/21-PATTERNS.md (§Oracle tests — ECP iprinv tests + the env[PTR_RINV_ORIG] setup note)
    - crates/cintx-compat/src/raw.rs (PTR_RINV_ORIG const from 21-01 — import for the cintx env setup)
  </read_first>
  <action>
    PART A (vendor_ffi.rs): Add `vendor_ECPscalar_iprinv_cart` and `vendor_ECPscalar_iprinv_sph` mirroring `vendor_ECPscalar_ipnuc_*` (vendor_ffi.rs:1368), calling `ffi::ECPscalar_iprinv_cart` / `ffi::ECPscalar_iprinv_sph` (these are in the vendored PySCF nr_ecp tree — confirm they are declared in the oracle build's FFI bindings; if NOT auto-bound, add `extern` declarations to the oracle build.rs supplemental header following the existing ECPscalar_ipnuc declaration pattern). The `out` buffer is `3 * nao_i * nao_j` (assert this, mirroring vendor_ffi.rs:1380). Document that the caller MUST set `env[AS_RINV_ORIG_ATOM]` to the target atom index before calling the vendor fn. Use serena `insert_after_symbol`.

    PART B (oracle test): Create `crates/cintx-oracle/tests/ecp_iprinv_parity.rs` following safe_api_ecp_parity.rs (header gate, ATOL=1e-12, RTOL=0.0, build_cu_lanl2dz fixture). For each ECP-bearing atom in Cu/LANL2DZ (Cu), set the rinv origin: for the cintx path use the safe-API `with_rinv_origin(cu_coord)` or set env[PTR_RINV_ORIG..+3]; for the vendor path set env[AS_RINV_ORIG_ATOM] = the Cu atom index. Add `#[test] #[cfg(has_vendor_libcint)]` tests `test_ECPscalar_iprinv_{cart,sph}_cu_lanl2dz_parity` driving cintx via `eval_raw RawApiId::INT1E_ECP_IPRINV_{CART,SPH}` (or the safe API) and asserting `count_mismatches(&vendor, &cintx, ATOL, RTOL) == 0` + the any_nonzero sentinel. Sweep each ECP-bearing atom to prove the per-nucleus selection.
  </action>
  <verify>
    <automated>cargo test -p cintx-oracle --features cpu --test ecp_iprinv_parity 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "fn vendor_ECPscalar_iprinv_cart\|fn vendor_ECPscalar_iprinv_sph" crates/cintx-oracle/src/vendor_ffi.rs` matches
    - `crates/cintx-oracle/tests/ecp_iprinv_parity.rs` exists with `#[cfg(has_vendor_libcint)]` cart+sph tests sweeping each ECP-bearing atom
    - `grep -in "rinv\|with_rinv_origin\|AS_RINV_ORIG_ATOM\|PTR_RINV_ORIG" crates/cintx-oracle/tests/ecp_iprinv_parity.rs` matches (origin setup on both paths)
    - `cargo test -p cintx-oracle --features cpu --test ecp_iprinv_parity` exits 0 (0 mismatches at atol=1e-12 vs vendored PySCF nr_ecp)
  </acceptance_criteria>
  <done>vendor_ECPscalar_iprinv_{cart,sph} wrappers exist; ecp_iprinv_parity.rs passes byte-identity at atol=1e-12 on Cu/LANL2DZ with a per-nucleus origin sweep and a nonzero sentinel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rinv origin coordinate ↔ ECP-atom selection | The origin selects which atom's ECP shell is differentiated; a no-match must fail typed (0 output), never select the wrong atom or panic. |
| approximate scalar ECP primitives ↔ gradient byte-identity (R4) | If the scalar path were direct-quadrature (not K-Taylor), the gradient could never reach atol=1e-12 — a correctness boundary gated by Task 0. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-07-01 | Denial of Service (silent-wrong) | ECPscalar_iprinv built on an approximate (non-K-Taylor) scalar foundation (R4) | mitigate | Task 0 is a HARD BLOCKING gate: re-run the Phase-19 Cu/LANL2DZ scalar+ipnuc byte-identity suite; only proceed if it passes at atol=1e-12. A failure records a BLOCKER recommending a K-Taylor-port pre-plan — no kernel is built on an approximate base. |
| T-21-07-02 | Tampering / Elevation | rinv origin matching no atom, or matching the wrong atom | mitigate | Coordinate match within 1e-10; a no-match returns 0 output / typed UnsupportedApi (mirroring vendor `_one_shell_ecpbas` shl_id<0 → 0), never a panic or wrong-atom selection. The single-atom-selection unit test + the per-nucleus oracle sweep prove correct selection. |
| T-21-07-03 | Denial of Service (silent-wrong) | iprinv accidentally accumulating over all atoms (the ipnuc behavior) | mitigate | The iprinv branch selects ONE atom and drops the all-slot/-Z_C accumulation (D-09); the byte-identity oracle vs `vendor_ECPscalar_iprinv` (single-atom) catches an accidental all-slot sum. |
| T-21-07-04 | Tampering | spinor ECP gradient producing unverified output | mitigate | Spinor → UnsupportedApi (R5). |
| T-21-07-05 | Tampering | OOM-safe stop on the comp=3 ECP staging | accept | Staging allocated by the existing planner fallible path (manifest component_rank:"3"); this plan only writes into the allocated buffer + asserts BufferTooSmall (the Phase-19 pattern). |
| T-21-07-SC | Tampering | npm/pip/cargo installs | accept | No new external packages; only existing workspace crates + the already-vendored PySCF nr_ecp tree. |
</threat_model>

<verification>
- `cargo test -p cintx-oracle --features cpu --test safe_api_ecp_parity` green (Task 0: R4 K-Taylor confirmation).
- `cargo test -p cintx-cubecl --features cpu ecp_iprinv` green (single-atom selection, origin-mismatch, determinism, spinor-reject).
- `cargo test -p cintx-oracle --features cpu --test ecp_iprinv_parity` green (byte-identity vs vendor at atol=1e-12, per-nucleus sweep).
- `CINTX_BACKEND=cpu cargo check --workspace --features cpu` exits 0.
- The ecp + ecp_ipnuc paths do not regress.
</verification>

<success_criteria>
- Phase 19 scalar-ECP K-Taylor byte-identity re-confirmed (R4 cleared) before building (GRAD-09 pre-req).
- ECPscalar_iprinv (cart+sph, 3 components, single rinv origin, no all-slot -Z_C) byte-identical at atol=1e-12 on Cu/LANL2DZ (GRAD-09).
- launch_ecp ecp_iprinv arm selects a single atom and reuses deriv1_cart_pair (comp=3); spinor returns UnsupportedApi.
</success_criteria>

<output>
Create `.planning/phases/21-coulomb-gradient-intors/21-07-SUMMARY.md` when done. Record: the R4 confirmation result (test output proving K-Taylor byte-identity), the single-atom-selection mechanism (coord match vs vendor's AS_RINV_ORIG_ATOM index), the close-out of the salvaged-19-05-tables note (not needed; deriv1_cart_pair gpx/gpy/gpz used instead), and note to flip oracle_covered=true for int1e_ecp_iprinv once green.
</output>
