---
phase: 21-coulomb-gradient-intors
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cintx-ops/generated/compiled_manifest.lock.json
  - crates/cintx-compat/src/raw.rs
  - crates/cintx-compat/src/legacy.rs
  - crates/cintx-capi/src/shim.rs
requirements: [GRAD-02]
autonomous: true
must_haves:
  truths:
    - "All 6 gradient families (int1e_ipovlp, int1e_ipkin, int1e_ipnuc, int1e_iprinv, int2e_ip1, int1e_ecp_iprinv) are registered in compiled_manifest.lock.json with component_rank:\"3\" per cart/sph/spinor representation"
    - "The int3c2e_ip1 entries are corrected: full entry fields added, operator changed to ip1, component_rank set to \"3\""
    - "cargo build regenerates crates/cintx-ops/src/generated/api_manifest.rs from the lock with the new gradient symbols"
    - "Every new symbol resolves through eval_raw (kernels may return UnsupportedApi until Wave 2/3/4 lands them)"
    - "Spinor gradient representations are registered but their kernels return UnsupportedApi (Risk R5)"
    - "The manifest-audit xtask is green (no missing/extra rows, legacy-wrapper sync holds)"
  artifacts:
    - path: "crates/cintx-ops/generated/compiled_manifest.lock.json"
      provides: "manifest source-of-truth rows for the 6 gradient families + int3c2e_ip1 correction"
      contains: "int1e_ipovlp_cart"
    - path: "crates/cintx-compat/src/raw.rs"
      provides: "RawApiId consts for the new gradient symbols"
      contains: "INT1E_IPOVLP_CART"
    - path: "crates/cintx-compat/src/legacy.rs"
      provides: "all_cint_wrappers! blocks + LEGACY_WRAPPER_SYMBOLS + misc_wrapper_macro arms"
      contains: "cint1e_ipovlp_cart"
    - path: "crates/cintx-capi/src/shim.rs"
      provides: "CintxRawApi #[repr(i32)] variants + from_i32 + raw_id arms"
      contains: "Int1eIpovlpCart"
  key_links:
    - from: "crates/cintx-ops/build.rs"
      to: "crates/cintx-ops/src/generated/api_manifest.rs"
      via: "cargo build reads compiled_manifest.lock.json and regenerates the .rs + .csv"
      pattern: "component_rank"
    - from: "crates/cintx-compat/src/legacy.rs"
      to: "crates/cintx-compat/src/legacy.rs (misc match)"
      via: "legacy_wrapper_surface_matches_misc test enforces sync"
      pattern: "misc_wrapper_macro"
---

<objective>
Register all 6 gradient families plus the `int3c2e_ip1` correction in the manifest source-of-truth (`compiled_manifest.lock.json`) with `"component_rank":"3"` per representation, add the matching `RawApiId` consts, legacy wrappers, and CAPI enum variants, regenerate `api_manifest.rs` via `cargo build`, and confirm the manifest-audit xtask is green. This is the surface-registration foundation Waves 2-4 land kernels behind.

Purpose: cintx's runtime planner reads `"component_rank":"3"` (cintx-runtime/src/planner.rs:395,432) to auto-allocate `3 × ni × nj[× nk × nl]` staging — no manual layout code. Registering the gradient symbols with this tell is the prerequisite for any gradient kernel to receive a correctly-sized output buffer. The `int3c2e_ip1` correction is the critical fix for Risk R1: its three manifest entries (lock lines 307-354) are SHORT-FORM stubs carrying only `id/oracle_covered/profiles/stability` — they lack `component_rank` entirely (proof the kernel never produced 3 components) and carry `operator:"electron-repulsion"` instead of `"ip1"`. Until Wave 2/3/4 land kernels, every new symbol resolves through `eval_raw` but the kernel returns `UnsupportedApi` — this is the Phase 18/19 "compiled-but-unverified" escape hatch (D-03).

Output: 18 new cart/sph/spinor operator rows (6 families × 3 reps) + their `optimizer`/`legacy` siblings; the corrected `int3c2e_ip1` entries; new `RawApiId` consts; `all_cint_wrappers!` blocks + `LEGACY_WRAPPER_SYMBOLS` + `misc_wrapper_macro` arms; `CintxRawApi` variants + `from_i32`/`raw_id` arms; regenerated `api_manifest.rs`/`.csv`; green manifest-audit.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-coulomb-gradient-intors/21-CONTEXT.md
@.planning/phases/21-coulomb-gradient-intors/21-RESEARCH.md
@.planning/phases/21-coulomb-gradient-intors/21-PATTERNS.md

<interfaces>
<!-- Concrete current contracts the executor needs. -->

Manifest is the source-of-truth lock at crates/cintx-ops/generated/compiled_manifest.lock.json. crates/cintx-ops/build.rs reads it on `cargo build` and regenerates crates/cintx-ops/src/generated/api_manifest.rs (+ .csv). There is NO `--update` flag on the manifest-audit xtask; edit the lock JSON directly (Phase 19 decision, STATE.md line 255).

FULL-FORM entry shape (the template — copy int1e_ecp_ipnuc_cart at lock lines 507-538) carries these top-level fields:
`arity`, `canonical_family`, `category`, `compiled_in_profiles`, `component_rank`, `declared_in`, `feature_flag`, `forms`, `helper_kind`, `id { family, operator, representation, symbol }`, `oracle_covered`, `profiles`, `stability`.

int1e_ecp_ipnuc_cart (lock line 517) carries `"component_rank": "3"` and `"operator": "ecp_ipnuc"`. Plain scalar families carry `"component_rank": "scalar"` (e.g. lock line 581+). The 4c1e family carries `"component_rank": ""` (lock line 395).

The BROKEN int3c2e_ip1 entries (lock lines 307-354) are SHORT-FORM — only `id/oracle_covered/profiles/stability`, NO `component_rank`, NO `arity/canonical_family/category`. The `id.operator` is `"electron-repulsion"`. They must become full-form with `arity:3`, `canonical_family:"3c2e"`, `category:"3c2e"`, `component_rank:"3"`, `id.operator:"ip1"`.

RawApiId consts live in crates/cintx-compat/src/raw.rs:111-151. INT1E_ECP_IPNUC_CART/SPH at lines 150-151; INT4C1E_CART/SPH at 143-144. Format: `pub const INT1E_IPOVLP_CART: Self = Self::Symbol("int1e_ipovlp_cart");`. Routing is manifest-driven — adding the const + manifest row is sufficient for eval_raw resolution.

Legacy wrappers (crates/cintx-compat/src/legacy.rs): `all_cint_wrappers!(...)` blocks at lines 161-237 (the cint3c2e_ip1 block is at 227-233); `LEGACY_WRAPPER_SYMBOLS: &[&str]` array at line 239 (cint3c2e_ip1 entries at 279-284); `misc_wrapper_macro` match at PATTERNS-cited line ~312. The `legacy_wrapper_surface_matches_misc` test enforces sync between the macro invocations, the symbol array, and the misc match — all three must list the same symbols.

CAPI shim (crates/cintx-capi/src/shim.rs): `#[repr(i32)] enum CintxRawApi` at line 7; existing max discriminant is `Int4c1eSph = 22` (line 32). NOTE: `Int3c2eIp1Spinor = 20` ALREADY exists (line 30) — int3c2e_ip1 already has CAPI variants; do NOT re-add them, only the 6 NEW families need variants starting at 23. `from_i32` match at line 62 (arms 21/22 at lines 85-86); `raw_id` match at line 91 (arms at 114-115).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register 6 gradient families + correct int3c2e_ip1 in compiled_manifest.lock.json + regenerate</name>
  <files>crates/cintx-ops/generated/compiled_manifest.lock.json</files>
  <read_first>
    - crates/cintx-ops/generated/compiled_manifest.lock.json:507-549 (int1e_ecp_ipnuc_cart/sph — the FULL-FORM component_rank:"3" template)
    - crates/cintx-ops/generated/compiled_manifest.lock.json:307-354 (the SHORT-FORM int3c2e_ip1 entries to correct)
    - crates/cintx-ops/generated/compiled_manifest.lock.json:581-613 (a plain scalar 1e/2e family entry — for the operator/symbol/profile field conventions)
    - crates/cintx-ops/build.rs (the regenerator — confirm it reads the lock on cargo build)
    - .planning/phases/21-coulomb-gradient-intors/21-PATTERNS.md (§21-02 manifest — full JSON template + the int3c2e_ip1 correction + optimizer/legacy sibling note)
    - .planning/phases/21-coulomb-gradient-intors/21-CONTEXT.md (D-02, D-03 — component_rank tell + spinor-as-UnsupportedApi)
  </read_first>
  <action>
    Add 18 full-form operator entries to `compiled_manifest.lock.json` (6 families × {cart, sph, spinor}), each carrying `"component_rank": "3"`, using the int1e_ecp_ipnuc_cart entry (lines 507-538) as the structural template. Per-family field values:
    - int1e_ipovlp: `arity:2, canonical_family:"1e", category:"1e", id.operator:"ipovlp", id.symbol:"int1e_ipovlp_{cart,sph,spinor}"`, profiles all four (base, with-f12, with-4c1e, with-f12+with-4c1e), `helper_kind:"operator"`, `stability:"stable"`, `feature_flag:"none"`, `forms:["cart"]`/`["sph"]`/`["spinor"]`, `oracle_covered:false` (flips true when Wave 2 lands the kernel + oracle).
    - int1e_ipkin: same as ipovlp with `id.operator:"ipkin"`, `id.symbol:"int1e_ipkin_{cart,sph,spinor}"`.
    - int1e_ipnuc: same with `id.operator:"ipnuc"`, `id.symbol:"int1e_ipnuc_{cart,sph,spinor}"`.
    - int1e_iprinv: same with `id.operator:"iprinv"`, `id.symbol:"int1e_iprinv_{cart,sph,spinor}"`.
    - int2e_ip1: `arity:4, canonical_family:"2e", category:"2e", id.operator:"ip1", id.symbol:"int2e_ip1_{cart,sph,spinor}"`, profiles all four.
    - int1e_ecp_iprinv: `arity:2, canonical_family:"ecp", category:"ecp", id.operator:"ecp_iprinv", id.symbol:"int1e_ecp_iprinv_{cart,sph,spinor}"` (mirror the existing int1e_ecp_ipnuc rows' canonical_family/category/profiles exactly).

    Correct the THREE int3c2e_ip1 entries (lines 307-354): convert each from short-form to full-form by adding `arity:3, canonical_family:"3c2e", category:"3c2e", component_rank:"3", compiled_in_profiles:[all four], declared_in:"unknown", feature_flag:"none", forms` (per rep), `helper_kind:"operator"`; change `id.operator` from `"electron-repulsion"` to `"ip1"`; keep `id.symbol` (int3c2e_ip1_{cart,sph,spinor}) and `oracle_covered:true`→ set to `false` until Wave 3 (21-06) lands the real kernel + flips the oracle reference (D-07). Preserve `stability:"stable"`.

    Add `optimizer` and `legacy` sibling entries for each new operator symbol following the existing int3c2e_ip1_cart_optimizer / cint3c2e_ip1_cart entries already in the file (helper_kind:"optimizer" / "legacy"). For ipovlp/ipkin/ipnuc/iprinv (1e) add cart+sph+spinor optimizer + cint legacy; for int2e_ip1 (2e) the same; for int1e_ecp_iprinv mirror the int1e_ecp_ipnuc optimizer/legacy siblings.

    Then run `cargo build -p cintx-ops` to regenerate `src/generated/api_manifest.rs` + `.csv`. Keep JSON well-formed (validate with `python3 -m json.tool` or `cargo build` failing fast on a parse error). Edit the lock by hand (no xtask --update flag exists).
  </action>
  <verify>
    <automated>python3 -m json.tool crates/cintx-ops/generated/compiled_manifest.lock.json > /dev/null && echo JSON_OK && cargo build -p cintx-ops 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m json.tool crates/cintx-ops/generated/compiled_manifest.lock.json` exits 0 (valid JSON)
    - `grep -c '"int1e_ipovlp_' crates/cintx-ops/generated/compiled_manifest.lock.json` ≥ 3 (cart/sph/spinor) and similarly for ipkin/ipnuc/iprinv/int2e_ip1/int1e_ecp_iprinv operator rows
    - The three int3c2e_ip1 operator entries now contain `"component_rank": "3"` and `"operator": "ip1"` — `grep -A6 '"symbol": "int3c2e_ip1_cart"' crates/cintx-ops/generated/compiled_manifest.lock.json` shows `"operator": "ip1"`
    - `cargo build -p cintx-ops` exits 0 and regenerates api_manifest.rs (file mtime changes; `grep -c "int1e_ipovlp" crates/cintx-ops/src/generated/api_manifest.rs` ≥ 1)
    - Spinor rows are present (registered) — `grep -c "int1e_iprinv_spinor" crates/cintx-ops/generated/compiled_manifest.lock.json` ≥ 1
  </acceptance_criteria>
  <done>18 gradient operator rows (+ optimizer/legacy siblings) carry component_rank:"3"; the int3c2e_ip1 entries are full-form with operator:"ip1" and component_rank:"3"; cargo build regenerated api_manifest.rs from the corrected lock.</done>
</task>

<task type="auto">
  <name>Task 2: Add RawApiId consts, legacy wrappers, CAPI variants + green manifest-audit</name>
  <files>crates/cintx-compat/src/raw.rs, crates/cintx-compat/src/legacy.rs, crates/cintx-capi/src/shim.rs</files>
  <read_first>
    - crates/cintx-compat/src/raw.rs:143-151 (INT4C1E_* and INT1E_ECP_IPNUC_* const format — the template for new consts)
    - crates/cintx-compat/src/legacy.rs:227-237 (the cint3c2e_ip1 all_cint_wrappers! block — the macro invocation template)
    - crates/cintx-compat/src/legacy.rs:239-285 (LEGACY_WRAPPER_SYMBOLS array — append the new symbol triples)
    - crates/cintx-compat/src/legacy.rs (the misc_wrapper_macro match arm ~line 312 — extend per PATTERNS §21-02 legacy)
    - crates/cintx-capi/src/shim.rs:7-33 (CintxRawApi enum; Int4c1eSph = 22 is the current max; Int3c2eIp1Spinor = 20 ALREADY exists)
    - crates/cintx-capi/src/shim.rs:62-116 (from_i32 + raw_id matches — one arm per variant)
    - .planning/phases/21-coulomb-gradient-intors/21-PATTERNS.md (§21-02 legacy + §21-02 C-ABI — exact variant numbering 23..39)
  </read_first>
  <action>
    PART A (raw.rs consts): Use serena `insert_after_symbol` targeting the INT1E_ECP_IPNUC_SPH const region (raw.rs:151) to add the consts from PATTERNS §21-01 Step 2: `INT1E_IPOVLP_{CART,SPH,SPINOR}`, `INT1E_IPKIN_{CART,SPH,SPINOR}`, `INT1E_IPNUC_{CART,SPH,SPINOR}`, `INT1E_IPRINV_{CART,SPH,SPINOR}`, `INT2E_IP1_{CART,SPH,SPINOR}`, `INT1E_ECP_IPRINV_{CART,SPH}` — each `pub const X: Self = Self::Symbol("<lowercase_symbol>");`. (int3c2e_ip1 consts already exist; do not re-add.)

    PART B (legacy.rs): Add 6 `all_cint_wrappers!(...)` blocks mirroring the cint3c2e_ip1 block (legacy.rs:227): one per family with the 3 cart/sph/base symbols + 3 optimizer symbols + the 3 RawApiId refs (cart/sph/spinor). E.g. `all_cint_wrappers!(cint1e_ipovlp_cart, cint1e_ipovlp_sph, cint1e_ipovlp, cint1e_ipovlp_cart_optimizer, cint1e_ipovlp_sph_optimizer, cint1e_ipovlp_optimizer, RawApiId::INT1E_IPOVLP_CART, RawApiId::INT1E_IPOVLP_SPH, RawApiId::INT1E_IPOVLP_SPINOR);` — repeat for ipkin, ipnuc, iprinv, int2e_ip1 (cint2e_ip1_*), and int1e_ecp_iprinv (cint legacy mirroring the ecp_ipnuc pattern; ECP may use ECPscalar_* legacy names — match whatever the existing int1e_ecp_ipnuc legacy entry uses). Append the 6 symbol-sets to `LEGACY_WRAPPER_SYMBOLS` (legacy.rs:239). Extend the `misc_wrapper_macro` match arm (per PATTERNS §21-02 legacy line 312) so its `Some(MiscWrapperMacro::AllCint)` branch lists `"int1e_ipovlp" | "int1e_ipkin" | "int1e_ipnuc" | "int1e_iprinv" | "int2e_ip1" | "int1e_ecp_iprinv"`. Use serena `replace_symbol_body` on the array + the match fn, `insert_after_symbol` for the macro blocks. The `legacy_wrapper_surface_matches_misc` test must pass — all three sites (macro blocks, symbol array, misc match) MUST list identical base symbols.

    PART C (shim.rs): Append new `#[repr(i32)]` variants starting at 23 (PATTERNS §21-02 C-ABI exact numbering): `Int1eIpovlpCart=23 .. Int1eEcpIprinvSph=39` (17 variants — note int3c2e_ip1 variants already exist at 18/19/20). Add matching `from_i32` arms (`23 => Some(Self::Int1eIpovlpCart)` …) and `raw_id` arms (`Self::Int1eIpovlpCart => RawApiId::INT1E_IPOVLP_CART` …). Use serena `replace_symbol_body` on the enum + the two match fns.

    PART D: Run the manifest-audit xtask and the legacy-sync test. Both must be green. If manifest-audit reports a missing optimizer/legacy row, add it to the lock (Task 1's file) and re-run `cargo build -p cintx-ops`.
  </action>
  <verify>
    <automated>cargo test -p cintx-compat legacy_wrapper_surface_matches_misc 2>&1 | tail -10 && cargo run -p xtask -- manifest-audit 2>&1 | tail -20 && CINTX_BACKEND=cpu cargo check --workspace --features cpu 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "INT1E_IPOVLP_CART\|INT2E_IP1_SPH\|INT1E_ECP_IPRINV_CART" crates/cintx-compat/src/raw.rs` matches all three (consts added)
    - `grep -c "cint1e_ipovlp_cart\|cint2e_ip1_cart" crates/cintx-compat/src/legacy.rs` ≥ 2 (wrapper blocks added)
    - `grep -n "Int1eIpovlpCart\|Int1eEcpIprinvSph" crates/cintx-capi/src/shim.rs` matches (CAPI variants added)
    - `cargo test -p cintx-compat legacy_wrapper_surface_matches_misc` exits 0 (legacy/misc sync holds)
    - `cargo run -p xtask -- manifest-audit` exits 0 (green: no missing/extra rows)
    - `CINTX_BACKEND=cpu cargo check --workspace --features cpu` exits 0
  </acceptance_criteria>
  <done>RawApiId consts, legacy wrappers (macro + symbol array + misc match in sync), and CAPI #[repr(i32)] variants are added for all 6 families; manifest-audit is green; the workspace compiles; every symbol resolves through eval_raw (kernels return UnsupportedApi until later waves).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| C-ABI `api: i32` ↔ CintxRawApi::from_i32 | Untrusted integer from a C caller; an unknown discriminant must return `None` (typed reject), never index an out-of-range table. |
| manifest lock JSON ↔ build.rs codegen | Malformed JSON or a row missing required fields must fail the build, not emit a half-formed api_manifest.rs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02-01 | Spoofing / Tampering | CintxRawApi::from_i32 for the new 23..39 discriminants | mitigate | Every new variant gets an explicit `from_i32` arm; any integer outside the known set returns `None` (the existing fail-closed contract) — no silent fallback, no out-of-range indexing into raw_id. |
| T-21-02-02 | Denial of Service (silent-wrong) | int3c2e_ip1 short-form entries (Risk R1) | mitigate | Convert the short-form entries to full-form with `component_rank:"3"` and `operator:"ip1"`, and set `oracle_covered:false` so CI cannot mark the family verified until Wave 3 (21-06) ships a real derivative kernel and flips the oracle reference — closes the latent silent-wrong path. |
| T-21-02-03 | Tampering | spinor gradient rows registered but unimplemented (D-03 / Risk R5) | mitigate | Spinor reps are registered for surface completeness but their kernels return `UnsupportedApi`; the manifest carries `oracle_covered:false` for spinor so no false "verified" claim is made — mirrors the Phase 18/19 compiled-but-unverified escape hatch. |
| T-21-02-04 | Tampering | manifest lock manual edit corrupting an existing row | mitigate | `python3 -m json.tool` well-formedness gate + `cargo run -p xtask -- manifest-audit` (no missing/extra rows) + `legacy_wrapper_surface_matches_misc` (legacy sync) run before commit; any drift fails the verify command. |
| T-21-02-SC | Tampering | npm/pip/cargo installs | accept | No new external packages; only existing workspace crates edited. |
</threat_model>

<verification>
- `python3 -m json.tool compiled_manifest.lock.json` exits 0 (valid JSON).
- `cargo build -p cintx-ops` regenerates api_manifest.rs with the 6 new families + the corrected int3c2e_ip1.
- `cargo run -p xtask -- manifest-audit` green.
- `cargo test -p cintx-compat legacy_wrapper_surface_matches_misc` green.
- `CINTX_BACKEND=cpu cargo check --workspace --features cpu` exits 0.
</verification>

<success_criteria>
- All 6 gradient families + int3c2e_ip1 correction registered with component_rank:"3" per cart/sph/spinor (GRAD-02).
- RawApiId consts, legacy wrappers (in sync), and CAPI variants added for all 6 families (GRAD-02).
- cargo build regenerates api_manifest.rs; manifest-audit green (GRAD-02).
- Every symbol resolves through eval_raw; kernels return UnsupportedApi until later waves; spinor reps registered-but-UnsupportedApi (GRAD-02, D-03).
</success_criteria>

<output>
Create `.planning/phases/21-coulomb-gradient-intors/21-02-SUMMARY.md` when done. Record: the exact CAPI discriminant range used (23..39), the int3c2e_ip1 oracle_covered flip (set to false pending 21-06), the legacy symbol names used for int1e_ecp_iprinv, and confirm manifest-audit + the legacy-sync test are both green.
</output>
