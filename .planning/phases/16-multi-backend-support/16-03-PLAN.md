---
phase: 16-multi-backend-support
plan: 03
type: execute
wave: 2
depends_on: ["16-02"]
files_modified:
  - .github/workflows/compat-governance-pr.yml
autonomous: false
requirements:
  - BACK-03
  - BACK-07

must_haves:
  truths:
    - "compat-governance-pr.yml contains a new feature_matrix_gate job alongside the existing manifest_drift_gate / oracle_parity_gate / helper_legacy_parity_gate / oom_contract_gate / api_value_baseline_gate"
    - "feature_matrix_gate is a 3-cell matrix: cpu-only / cpu+wgpu / all-features (D-13)"
    - "Each cell runs cargo check + cargo test (excluding ignored), per D-14"
    - "all-features cell installs ROCm runtime headers (hipconfig) before cargo check, gated by `if: matrix.cell == 'all-features'`"
    - "fail-fast: false so cells fail independently"
    - "The 4-step preamble (Checkout / Resolve pinned Rust channel / Install pinned Rust toolchain / Cache Rust artifacts) is identical to the existing required gates"
    - "User confirms the new gate's three matrix entries are added to branch protection's required-status-checks list (manual repo settings step, post-merge)"
  artifacts:
    - path: ".github/workflows/compat-governance-pr.yml"
      provides: "feature_matrix_gate job (3-cell matrix; cpu-only / cpu+wgpu / all-features) with conditional ROCm install on all-features"
      contains: "feature_matrix_gate"
  key_links:
    - from: ".github/workflows/compat-governance-pr.yml"
      to: "crates/cintx-cubecl/Cargo.toml"
      via: "cargo check / cargo test invocations exercise the [features] table from Wave 1"
      pattern: "wgpu,cuda,rocm,metal"
    - from: ".github/workflows/compat-governance-pr.yml"
      to: "GitHub branch protection settings (manual)"
      via: "user adds feature_matrix_gate (cpu-only) / (cpu+wgpu) / (all-features) to required-status-checks"
      pattern: "feature_matrix_gate"
---

<objective>
Add the new required CI gate `feature_matrix_gate` to `.github/workflows/compat-governance-pr.yml` that exercises the 3-cell feature matrix (cpu-only / cpu+wgpu / all-features) on every PR. The all-features cell installs ROCm runtime headers via `amdgpu-install` so `cubecl-hip-sys`'s build script can find `hipconfig` on `ubuntu-latest`.

Purpose: D-13/D-14/D-16 require a CI matrix that catches feature-flag regressions on PRs. ROADMAP success criterion 7 says "feature matrix exercised in CI on existing runners (no new GPU runners required)". This plan turns Wave 1's local `cargo check` discipline into a fail-closed PR gate. Without it, a future PR could break `--features cuda` (or any combo) silently.

Output:
- `feature_matrix_gate` job added inside the existing `compat-governance-pr.yml` workflow (NOT a new file).
- 3-cell matrix per D-13: cpu-only (no extra features), cpu+wgpu (`--features wgpu`), all-features (`--features wgpu,cuda,rocm,metal`).
- Each cell runs `cargo check` and then `cargo test` (excluding ignored).
- all-features cell installs ROCm via `amdgpu-install` before cargo steps.
- Standard 4-step preamble (Checkout / Resolve channel / Install toolchain / Cache) copied verbatim from existing gates.
- `fail-fast: false`.
- Manual user step: add the three new matrix-named status checks to branch protection (post-merge).

This plan is `autonomous: false` because Step 5 (branch-protection registration) is a manual repo-settings step the user owns; the planner cannot do it from a PR.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-multi-backend-support/16-CONTEXT.md
@.planning/phases/16-multi-backend-support/16-RESEARCH.md
@.planning/phases/16-multi-backend-support/16-PATTERNS.md
@.planning/phases/16-multi-backend-support/16-02-SUMMARY.md
@.planning/notes/cuda-metal-verification-gap.md
@.planning/seeds/gpu-ci-runners.md

<interfaces>
<!-- Verbatim CI gate skeleton from RESEARCH §6 + PATTERNS Pattern E. -->

**Existing 4-step preamble** (must be copied verbatim — appears identically in `manifest_drift_gate:41-67`, `oracle_parity_gate:81-107`, `helper_legacy_parity_gate:117-143`, `oom_contract_gate:155-181`, `api_value_baseline_gate:197-223`):

```yaml
- name: Checkout
  uses: actions/checkout@v6
- name: Resolve pinned Rust channel
  id: rust
  run: |
    python <<'PY'
    import os, tomllib
    from pathlib import Path
    data = tomllib.loads(Path("rust-toolchain.toml").read_text())
    channel = data.get("toolchain", {}).get("channel")
    if not channel:
        raise SystemExit("failed to resolve channel from rust-toolchain.toml")
    with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as fh:
        fh.write(f"channel={channel}\n")
    PY
- name: Install pinned Rust toolchain
  uses: dtolnay/rust-toolchain@master
  with:
    toolchain: ${{ steps.rust.outputs.channel }}
- name: Cache Rust artifacts
  uses: Swatinem/rust-cache@v2
```

**Full feature_matrix_gate job** (RESEARCH §6.1):

```yaml
feature_matrix_gate:
    name: feature_matrix_gate (${{ matrix.cell }})
    runs-on: ubuntu-latest
    strategy:
        fail-fast: false
        matrix:
            include:
                - cell: cpu-only
                  features: ""
                - cell: cpu+wgpu
                  features: "wgpu"
                - cell: all-features
                  features: "wgpu,cuda,rocm,metal"
    steps:
        - name: Checkout
          uses: actions/checkout@v6
        - name: Resolve pinned Rust channel
          id: rust
          run: |
              python <<'PY'
              import os, tomllib
              from pathlib import Path
              data = tomllib.loads(Path("rust-toolchain.toml").read_text())
              channel = data.get("toolchain", {}).get("channel")
              if not channel:
                  raise SystemExit("failed to resolve channel from rust-toolchain.toml")
              with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as fh:
                  fh.write(f"channel={channel}\n")
              PY
        - name: Install pinned Rust toolchain
          uses: dtolnay/rust-toolchain@master
          with:
              toolchain: ${{ steps.rust.outputs.channel }}
        - name: Cache Rust artifacts
          uses: Swatinem/rust-cache@v2
        - name: Install ROCm runtime headers (cubecl-hip-sys build script needs hipconfig)
          if: matrix.cell == 'all-features'
          run: |
              wget https://repo.radeon.com/amdgpu-install/6.0/ubuntu/jammy/amdgpu-install_6.0.60000-1_all.deb
              sudo apt-get install -y ./amdgpu-install_6.0.60000-1_all.deb
              sudo amdgpu-install --usecase=rocm --no-dkms -y
              echo "/opt/rocm/bin" >> $GITHUB_PATH
        - name: cargo check
          run: |
              if [ -z "${{ matrix.features }}" ]; then
                cargo check -p cintx-cubecl
              else
                cargo check -p cintx-cubecl --features "${{ matrix.features }}"
              fi
        - name: cargo test (excluding ignored)
          run: |
              if [ -z "${{ matrix.features }}" ]; then
                cargo test -p cintx-cubecl
              else
                cargo test -p cintx-cubecl --features "${{ matrix.features }}"
              fi
```

**Per-cell expectation table** (RESEARCH §6.2):

| Cell | Features | `cargo check` | `cargo test` | Expected runtime |
|------|---------|--------------|-------------|------------------|
| cpu-only | (default) | compiles | runs cpu unit tests | ~3 min |
| cpu+wgpu | wgpu | compiles | runs cpu + wgpu non-ignored tests | ~5 min |
| all-features | wgpu,cuda,rocm,metal | compiles (after ROCm install) | runs all non-ignored, non-oracle tests | ~10 min (ROCm install dominates) |

**Branch-protection registration** (RESEARCH §8.6 — manual user step, NOT a workflow file edit):

After the workflow lands, repo admin opens GitHub `Settings → Branches → main → Require status checks`, adds the three new entries:
- `feature_matrix_gate (cpu-only)`
- `feature_matrix_gate (cpu+wgpu)`
- `feature_matrix_gate (all-features)`

[ASSUMED A4 from RESEARCH §8.4] `amdgpu-install` continues to work on `ubuntu-latest` 2026 GHA images. Verification step is in Task 1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add feature_matrix_gate job to compat-governance-pr.yml + smoke-test on a feature branch</name>
  <files>.github/workflows/compat-governance-pr.yml</files>
  <read_first>
    - .planning/phases/16-multi-backend-support/16-RESEARCH.md (§6 in full)
    - .planning/phases/16-multi-backend-support/16-PATTERNS.md (Pattern E, "NEW CI job feature_matrix_gate" section)
    - .github/workflows/compat-governance-pr.yml (full file — focus on lines 37-71 manifest_drift_gate, 73-111 oracle_parity_gate, and the 4-step preambles repeated across all 5 existing gates)
  </read_first>
  <action>
**Step A — Locate insertion point**:

`compat-governance-pr.yml` currently has 5 gate jobs at lines 37 (manifest_drift_gate), 73 (oracle_parity_gate), 113 (helper_legacy_parity_gate), 151 (oom_contract_gate), 190 (api_value_baseline_gate). Append `feature_matrix_gate` after `api_value_baseline_gate` (it's the natural sixth gate).

**Step B — Insert the verbatim job block from the `<interfaces>` section above**.

Important YAML hygiene:
- Indentation MUST match the existing gates (8 spaces for nested keys is what the existing file uses). Run `yamllint` mentally against the existing manifest_drift_gate to confirm spacing before saving.
- The `python <<'PY' ... PY` heredoc must be byte-identical to the existing gates' version. Copy it from `manifest_drift_gate:46-59` rather than retyping.
- The `if: matrix.cell == 'all-features'` step must come AFTER the cache step but BEFORE the cargo steps — order matters because hipconfig must be on PATH before cargo invokes the build script.

**Step C — Verify the YAML parses**:

```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/compat-governance-pr.yml').read()); print('YAML OK')"
```

If it errors, indentation drift is the most likely cause. Re-check against the manifest_drift_gate template.

**Step D — Smoke test the gate on a feature branch (operator-driven, not blocking this task's automated verify)**:

The user (or operator) pushes the branch with this change to GitHub. The new `feature_matrix_gate (cpu-only)`, `feature_matrix_gate (cpu+wgpu)`, `feature_matrix_gate (all-features)` jobs should appear on the PR. Each must turn green:

- `cpu-only`: ~3 min, no special install
- `cpu+wgpu`: ~5 min, no special install (Vulkan/DX12 swiftshader compile path)
- `all-features`: ~10 min, includes the `amdgpu-install` ROCm setup step

If `all-features` flakes or exceeds 15 min: per RESEARCH §6.4, fall back to one of:
1. Apt-pin the explicit `rocm-dev` package via Radeon's Jammy archive (smaller install footprint).
2. Demote `all-features` to a separate weekly cron job and replace with a 4-cell matrix: `cpu+wgpu+cuda` and `cpu+wgpu+metal` as additional cells (drops the rocm runtime install requirement from per-PR).

If `cubecl-hip-sys` build fails because of an HIP version mismatch on the GHA runner (RESEARCH §8.2), capture the diagnostics and STOP — do not commit a workaround that hides the version-skew issue. Report back to the user.

**Step E — Update the workflow's job summary / required-jobs list if one exists**:

Some `.github/workflows/*.yml` files have a final `verify-required-gates` aggregator job that depends on all required gates. Check `compat-governance-pr.yml` for such a `needs: [...]` aggregator and add `feature_matrix_gate` to its `needs:` list. If no aggregator exists, skip this step.
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/cintx &amp;&amp; python3 -c "import yaml; yaml.safe_load(open('.github/workflows/compat-governance-pr.yml').read()); print('YAML OK')" &amp;&amp; grep -c "feature_matrix_gate" .github/workflows/compat-governance-pr.yml &amp;&amp; grep -c "all-features" .github/workflows/compat-governance-pr.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/compat-governance-pr.yml` contains a `feature_matrix_gate:` job key
    - The job has `runs-on: ubuntu-latest`
    - The job has `strategy.matrix.include` with three entries (cpu-only / cpu+wgpu / all-features)
    - The job has `strategy.fail-fast: false`
    - The job's preamble (Checkout / Resolve pinned Rust channel / Install pinned Rust toolchain / Cache Rust artifacts) is byte-identical to the existing gates' preamble
    - The job has a `if: matrix.cell == 'all-features'` step that installs ROCm via `amdgpu-install`
    - The job has a `cargo check` step and a `cargo test (excluding ignored)` step
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/compat-governance-pr.yml').read())"` exits 0
    - `grep -c "feature_matrix_gate" .github/workflows/compat-governance-pr.yml` returns >= 2 (job key + name)
    - On a feature-branch push to GitHub, all three matrix cells turn green within 15 minutes
  </acceptance_criteria>
  <done>feature_matrix_gate is wired into the existing workflow file; YAML parses; the three matrix cells run and pass on a feature-branch push.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Manual — register the three feature_matrix_gate matrix entries in branch protection</name>
  <what-built>
    The new `feature_matrix_gate` workflow job is committed and running on PRs. GitHub Actions auto-discovers it as a status check, but does NOT auto-add it to branch protection's required-status-checks list. This must be done manually by a repo admin to make the gate fail-closed for PR merges.
  </what-built>
  <how-to-verify>
    1. Open the repo on GitHub: https://github.com/{owner}/{repo}/settings/branches
    2. Edit the branch-protection rule for `main`.
    3. Under "Require status checks to pass before merging", click "Add" / search.
    4. Add each of these three entries (must match the workflow's `name:` template exactly):
       - `feature_matrix_gate (cpu-only)`
       - `feature_matrix_gate (cpu+wgpu)`
       - `feature_matrix_gate (all-features)`
    5. Save the rule.
    6. Open any open PR (or push a no-op commit). Confirm the three new status-checks appear in the PR's check list and are required.
    7. Confirm "merge" is blocked until all three are green.
  </how-to-verify>
  <resume-signal>Type "registered" once all three matrix entries are required in branch protection, or describe what failed (e.g., "GitHub UI shows only one entry — let me investigate matrix-name resolution").</resume-signal>
</task>

</tasks>

<verification>
- `.github/workflows/compat-governance-pr.yml` parses as valid YAML
- Push to a feature branch shows three new GitHub Actions checks: `feature_matrix_gate (cpu-only)`, `feature_matrix_gate (cpu+wgpu)`, `feature_matrix_gate (all-features)` — all green
- All three new matrix entries are registered in branch protection's required-status-checks list (manual step, confirmed by the user in Task 2)
- A PR that breaks `--features cuda` or `--features rocm` is now blocked from merging (regression coverage that did not exist before this plan)
</verification>

<success_criteria>
- BACK-03 (full): `cargo check` with the 3-cell feature matrix builds cleanly on PRs (locally already verified in Wave 1; CI now enforces it)
- BACK-07: feature matrix exercised in CI on existing runners (`ubuntu-latest`); no new GPU runners required (cuda + metal are compile-only here, exercised via the all-features cell's `cargo check` only — runtime tests for those backends remain out of scope per `notes/cuda-metal-verification-gap.md`)
- D-13 wired (3-cell matrix: cpu-only / cpu+wgpu / all-features)
- D-14 wired (each cell runs `cargo check` + `cargo test` excluding `#[ignore]`)
- D-16 wired (feature_matrix_gate is a required gate, post-Task-2 branch-protection registration)
</success_criteria>

<output>
After completion, create `.planning/phases/16-multi-backend-support/16-03-SUMMARY.md` capturing:
- The exact diff applied to `compat-governance-pr.yml` (the new ~50-line job block)
- The YAML-parse confirmation output
- The GitHub-Actions screenshot or log link from the feature-branch smoke test (all three cells green) — pasted as a markdown link
- The wall-clock duration of each cell from the smoke test (target: cpu-only ~3 min, cpu+wgpu ~5 min, all-features ~10 min)
- A note from the user confirming branch-protection registration (Task 2 resume-signal echoed)
- If any RESEARCH §6.4 fallback was used (apt-pin or 4-cell matrix), document it here with the rationale
</output>
