---
phase: 30-group-5-giao-slice-spin-giao-integrals-spinor
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - crates/cintx-oracle/src/fixtures.rs
  - crates/cintx-cubecl/src/kernels/sigma_p.rs
  - crates/cintx-oracle/src/vendor_ffi.rs
  - crates/cintx-oracle/tests/giao_sigma_1e_parity.rs
autonomous: true
requirements: [GIAO-03]

must_haves:
  truths:
    - "A combined gauge-origin!=0 AND kappa!=0 spinor fixture (1e form) exists and is non-square with a GT/LT kappa mix and >=1 nctr>1 shell"
    - "The gauge-origin x1i-with-origin G-tensor step is implemented in sigma_p.rs as f[i]=g[i+1]+origin[axis]*g[i] (NOT a cross-product)"
    - "int1e_cg_sa10sp_spinor through the extended sigma_p.rs gauge variant is byte-identical to vendor_int1e_cg_sa10sp_spinor at atol=1e-12"
    - "With common_orig=[0,0,0] the cg_sa10sp result collapses to int1e_giao_sa10sp_spinor (proves the gauge term is live, not zeroed)"
  artifacts:
    - path: "crates/cintx-oracle/src/fixtures.rs"
      provides: "build_gauge_kappa_spinor_fixture (1e combined gauge+kappa fixture)"
      contains: "fn build_gauge_kappa_spinor_fixture"
    - path: "crates/cintx-cubecl/src/kernels/sigma_p.rs"
      provides: "gauge-origin x1i-with-origin device recurrence step + origin: [f64;3] kernel input"
      contains: "origin"
    - path: "crates/cintx-oracle/tests/giao_sigma_1e_parity.rs"
      provides: "Wave-0 gauge-gout byte-identity micro-test + cg->giao collapse differential check"
      contains: "fn giao_sigma_micro"
    - path: "crates/cintx-oracle/src/vendor_ffi.rs"
      provides: "vendor_int1e_cg_sa10sp_spinor + vendor_int1e_giao_sa10sp_spinor shims"
      contains: "vendor_int1e_cg_sa10sp_spinor"
  key_links:
    - from: "crates/cintx-cubecl/src/kernels/sigma_p.rs"
      to: "plan.operator_env_params.common_orig (PTR_COMMON_ORIG)"
      via: "host reads common_orig.unwrap_or([0;3]) -> dri = ri - origin -> kernel origin input"
      pattern: "common_orig"
    - from: "crates/cintx-oracle/tests/giao_sigma_1e_parity.rs"
      to: "crates/cintx-oracle/src/fixtures.rs::build_gauge_kappa_spinor_fixture"
      via: "micro-test builds the combined fixture"
      pattern: "build_gauge_kappa_spinor_fixture"
---

<objective>
Land the Phase-30 de-risk before any family is wired (D-03, D-04 Wave 0): (1) the combined gauge!=0 AND kappa!=0 spinor fixture (1e form, D-02), (2) the gauge-origin `x1i`-with-origin G-tensor step folded into the `sigma_p.rs` σ·p device assembler — the ONLY genuinely-new device math this phase — and (3) the gauge-gout byte-identity micro-test that proves that fold against vendored `int1e_cg_sa10sp` at atol=1e-12, plus a cg→giao-at-origin=0 differential check proving the gauge term is live.

Purpose: The gauge fold is the single highest-rework-risk piece (a wrong origin-read/sign/stride surfaces only at the gate). Proving it on the smallest common-origin-reading family before wiring all 15 families buys spike-level rigor without a separate spike phase.
Output: `build_gauge_kappa_spinor_fixture` (1e), the gauge-origin variant scaffolding in `sigma_p.rs`, two vendor FFI shims, and a green micro-test gate.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/30-group-5-giao-slice-spin-giao-integrals-spinor/30-RESEARCH.md
@.planning/phases/30-group-5-giao-slice-spin-giao-integrals-spinor/30-PATTERNS.md
@.claude/skills/spike-findings-cintx/SKILL.md

<interfaces>
<!-- Extracted contracts the executor uses directly — no codebase exploration needed. -->

The gauge fold to port (libcint-master/src/g1e.c:446-448, called by G1E_RCI/R0I) — this is a
position-recurrence step, NOT a cross-product post-multiply (RESEARCH Pitfall 1):
  fx[i] = gx[i+1] + origin[0] * gx[i];
  fy[i] = gy[i+1] + origin[1] * gy[i];
  fz[i] = gz[i+1] + origin[2] * gz[i];
where origin = dri = ri - env[PTR_COMMON_ORIG] for cg_sa10* families.

Host gauge-origin read (mirror Phase-26 ket precedent one_electron.rs:9258-9259, but BRA-side):
  let origin: [f64; 3] = plan.operator_env_params.common_orig.unwrap_or([0.0; 3]);
  let dri = [ri[0]-origin[0], ri[1]-origin[1], ri[2]-origin[2]];   // bra-side for cg_sa10*

PTR_COMMON_ORIG = 1 (crates/cintx-compat/src/raw.rs:50).

sigma_p.rs kernel extension point (sigma_p.rs:116-157): sigma_p_kernel<F> already carries
#[comptime] tensor_rank; add a runtime origin: [f64;3] + a #[comptime] family/variant selector.
N_GC = 4 (gc_x/gc_y/gc_z/gc_1). VRR/HRR helpers to reuse: sigma_p_vrr_axis (L72),
sigma_p_hrr_axis (L86). Contraction reads coeff_i[pi*nctr_i+ci] (L237).
run_sigma_p_on_backend wrapper ~L330, gated `if tensor_rank == 1` at L404 — add gauge variants
alongside, do NOT touch the int1e_sp (tensor_rank=1) path.

Fixture template (crates/cintx-oracle/src/fixtures.rs): build_kappa_spinor_fixture (L323) —
p kappa=+1 (LT, di=2, nctr=2) x d kappa=-1 (GT, dj=6); non-square; already sets PTR_RINV_ORIG
at L342-344 the same way the D-02 addition sets PTR_COMMON_ORIG.

Vendor shim template (vendor_ffi.rs:4366-4389 vendor_int1e_sigma_spinor): out sized ni_sp*nj_sp*2
interleaved re/im; ffi::int1e_<family>_spinor(out, NULL dims, shls&[i32;2], atm, natm, bas, nbas,
env, NULL opt, NULL cache). All in-scope symbols are in cint_funcs.h — no suppl-header decls.

Test scaffold model: crates/cintx-oracle/tests/rel_1e_sigma_parity.rs (whole file) —
#![cfg(any(feature="cpu",feature="rocm"))], ATOL=1e-12, RTOL=0.0, double-gated
#[cfg(has_vendor_libcint)] #[cfg(feature="cpu")], count_mismatches(..., ATOL, RTOL)==0.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Combined gauge!=0 AND kappa!=0 1e spinor fixture (D-02)</name>
  <files>crates/cintx-oracle/src/fixtures.rs</files>
  <read_first>
    - crates/cintx-oracle/src/fixtures.rs (read build_kappa_spinor_fixture at L323-389, including the PTR_RINV_ORIG block at L342-344 and the COLUMN-major env coeff layout at L329-333)
    - crates/cintx-compat/src/raw.rs:50 (confirm PTR_COMMON_ORIG = 1)
    - .planning/phases/30-.../30-RESEARCH.md §"Combined gauge!=0 AND kappa!=0 Fixture (D-02)" (the 5 hard constraints + the one-block PTR_COMMON_ORIG addition)
    - .claude/skills/spike-findings-cintx/SKILL.md (interleaved-complex spinor layout authority — read before touching any fixture sizing)
  </read_first>
  <action>
    Add a NEW function `build_gauge_kappa_spinor_fixture()` to fixtures.rs that internally builds the same geometry as `build_kappa_spinor_fixture` (p kappa=+1 LT nctr=2 x d kappa=-1 GT, non-square dims (2,6), COLUMN-major env coeffs) and then ADDITIONALLY sets a non-zero gauge origin in env at slot PTR_COMMON_ORIG (=1, from cintx_compat::raw::PTR_COMMON_ORIG):
      env[PTR_COMMON_ORIG]     = 0.30;
      env[PTR_COMMON_ORIG + 1] = -0.45;
      env[PTR_COMMON_ORIG + 2] = 0.60;
    Do NOT mutate the existing build_kappa_spinor_fixture (Phase-29 REL tests depend on it). The new fixture MUST satisfy all 5 D-02 hard constraints: (1) env[PTR_COMMON_ORIG..+3] != [0,0,0]; (2) kappa!=0 GT/LT mix (kappa=+1 LT AND kappa=-1 GT → both spinor_len branches); (3) non-square block dims; (4) >=1 shell with nctr>1 (the p shell keeps nctr=2); (5) the existing PTR_RINV_ORIG block stays for the nucsp/sa01 Rys arms. Return the same (atm, bas, env) tuple shape as the analog.
  </action>
  <verify>
    <automated>cargo build -p cintx-oracle --features cpu 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'fn build_gauge_kappa_spinor_fixture' crates/cintx-oracle/src/fixtures.rs` returns a match
    - `grep -n 'PTR_COMMON_ORIG' crates/cintx-oracle/src/fixtures.rs` shows the three env slot writes (0.30, -0.45, 0.60)
    - `build_kappa_spinor_fixture` body is unchanged: `git diff crates/cintx-oracle/src/fixtures.rs` shows only ADDED lines for the new fn, no edits inside the existing fn
    - `cargo build -p cintx-oracle --features cpu` exits 0
  </acceptance_criteria>
  <done>build_gauge_kappa_spinor_fixture exists, sets PTR_COMMON_ORIG to a non-zero asymmetric origin, satisfies all 5 D-02 constraints, leaves the Phase-29 fixture untouched, and the crate builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gauge-origin x1i-with-origin device step in sigma_p.rs + cg_sa10sp variant</name>
  <files>crates/cintx-cubecl/src/kernels/sigma_p.rs, crates/cintx-oracle/src/vendor_ffi.rs</files>
  <read_first>
    - crates/cintx-cubecl/src/kernels/sigma_p.rs (read the header comment L12-43, the kernel signature + #[comptime] tensor_rank at L116-157, sigma_p_vrr_axis L72, sigma_p_hrr_axis L86, the gc-block packing description L26-36, the contraction loop coeff read L237, and run_sigma_p_on_backend ~L330 incl. the `if tensor_rank == 1` gate at L404)
    - libcint-master/src/g1e.c:429-451 (CINTx1i_1e — the exact recurrence f[i]=g[i+1]+origin*g[i]; this is THE math to port, line-for-line)
    - libcint-master/src/g1e.h:48-62 (G1E_RCI/R0I/R_I/D_I/D_J macro definitions — confirms RCI uses dri, R0I uses ri, D_J is the ket nabla)
    - libcint-master/src/autocode/intor3.c:1141-1160 (int1e_cg_sa10sp gout body + the G1E_RCI(g2,g0,...)+dri build at L1154-1155, and dri = ri - env[PTR_COMMON_ORIG] at L1149-1151)
    - crates/cintx-cubecl/src/kernels/one_electron.rs:9258-9259 (Phase-26 ket-side gauge precedent — mirror BRA-side)
    - .claude/skills/spike-findings-cintx/SKILL.md references/spinor-layout.md (gc-block / interleaved-complex output layout — the gauge variant must emit the SAME 4-gc-block KET-major component-leading shape)
    - crates/cintx-oracle/src/vendor_ffi.rs:4366-4389 (vendor_int1e_sigma_spinor shim template)
  </read_first>
  <behavior>
    - Test (micro, vendor): int1e_cg_sa10sp_spinor on a minimal gauge!=0 ∧ kappa!=0 p(+1)xs(-1) block == vendor_int1e_cg_sa10sp_spinor at atol=1e-12 (RED until the gauge fold is ported)
    - Test (differential): the same cg_sa10sp block computed with common_orig=[0,0,0] == int1e_giao_sa10sp_spinor on the same block (proves origin plumbing is live; a silently-zeroed gauge term would also pass byte-identity vs vendor only if vendor also zeroes — this collapse check is the independent witness)
    - Edge: the gauge fold is a recurrence f[i]=g[i+1]+origin*g[i], NOT (r-c)x s — a cross-product implementation fails the micro-test
  </behavior>
  <action>
    Extend `sigma_p.rs` (DO NOT create a giao_sigma module — RESEARCH Architecture Patterns resolves Claude's Discretion to extend in place). Steps:
    1. Add a runtime `origin: [f64; 3]` input to `sigma_p_kernel<F>` and a `#[comptime]` family/variant selector alongside the existing `#[comptime] tensor_rank`. Keep the int1e_sp (tensor_rank=1) path untouched.
    2. Port `CINTx1i_1e` (g1e.c:446-448) as a device `x1i`-with-origin G-tensor step: for each (j,k) block, for i in [ptr..ptr+li]: f{x,y,z}[i] = g{x,y,z}[i+1] + origin[{0,1,2}] * g{x,y,z}[i]. This is a sibling of the existing VRR/HRR axis helpers — author it #[cube]-legally (no plain-fn calls, no if-expr, u32/i32 only, no continue/break, per docs/manual/Cubecl/*.md).
    3. Add the ket-nabla `G1E_D_J` build step (CINTnabla1j_1e) — same recurrence family as the existing bra-nabla.
    4. Implement the `int1e_cg_sa10sp` gout variant (rank 3, transform c2s_si_1ei): build g0 (overlap base) → g1 = ∇_j(g0) (G1E_D_J) → g2 = x1i(g0, origin=dri) → g3 = x1i(g1, origin=dri), then the s[0..8] gout mix transcribed verbatim from intor3.c:1141-1160 into the 4 gc blocks. The host reads `origin = plan.operator_env_params.common_orig.unwrap_or([0.0;3])` and passes `dri = [ri[k]-origin[k]]` into the kernel.
    5. In `run_sigma_p_on_backend`, add the cg_sa10sp gauge variant alongside the existing `if tensor_rank == 1` gate without modifying the int1e_sp branch.
    6. Add `vendor_int1e_cg_sa10sp_spinor` and `vendor_int1e_giao_sa10sp_spinor` shims to vendor_ffi.rs (clone vendor_int1e_sigma_spinor L4366; swap the ffi driver symbol; both symbols are in cint_funcs.h, no suppl-header decls).
    Honor: KET→BRA transpose is owned by the c2s transform (emit gc blocks KET-major component-leading); spinor sizing via spinor_len (never 4l+2). The Wave-0 micro-test (Task 3) is the gate for this task's math.
  </action>
  <verify>
    <automated>cargo build -p cintx-cubecl --features cpu 2>&1 | tail -5 && cargo build -p cintx-oracle --features cpu 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'origin' crates/cintx-cubecl/src/kernels/sigma_p.rs` shows the new origin kernel input and the x1i-with-origin step (formula `+ origin` multiply present)
    - `grep -vn '^//' crates/cintx-cubecl/src/kernels/sigma_p.rs | grep -c 'cross'` returns 0 in the new code path (NO cross-product implementation of the gauge factor)
    - `grep -n 'vendor_int1e_cg_sa10sp_spinor\|vendor_int1e_giao_sa10sp_spinor' crates/cintx-oracle/src/vendor_ffi.rs` returns both shims
    - The int1e_sp path is unchanged: `git diff crates/cintx-cubecl/src/kernels/sigma_p.rs` shows no edits inside the `if tensor_rank == 1` branch
    - `cargo build -p cintx-cubecl --features cpu` and `cargo build -p cintx-oracle --features cpu` both exit 0
  </acceptance_criteria>
  <done>sigma_p.rs has the gauge-origin x1i-with-origin step + ket-nabla + cg_sa10sp gout variant reading common_orig; the two vendor shims compile; the int1e_sp path is untouched; both crates build.</done>
</task>

<task type="auto">
  <name>Task 3: Gauge-gout byte-identity micro-test (D-03 de-risk gate)</name>
  <files>crates/cintx-oracle/tests/giao_sigma_1e_parity.rs</files>
  <read_first>
    - crates/cintx-oracle/tests/rel_1e_sigma_parity.rs (whole file — copy the #![cfg], ATOL/RTOL consts, double-gate #[cfg(has_vendor_libcint)] #[cfg(feature="cpu")], count_mismatches, assert_any_nonzero, the per-family collector pattern, and the spinor_len sizing helper)
    - crates/cintx-oracle/src/fixtures.rs (the build_gauge_kappa_spinor_fixture added in Task 1)
    - .planning/phases/30-.../30-RESEARCH.md §"Wave-0 Micro-Test" (the cg_sa10sp vehicle + the cg->giao-at-origin=0 collapse differential check)
    - .planning/phases/30-.../30-PATTERNS.md (the giao_sigma_micro test sketch)
  </read_first>
  <action>
    Create crates/cintx-oracle/tests/giao_sigma_1e_parity.rs. Copy the rel_1e_sigma_parity.rs scaffold: `#![cfg(any(feature="cpu",feature="rocm"))]`, `const ATOL: f64 = 1e-12; const RTOL: f64 = 0.0;`, the collectors, the always-on `test_kappa_sizing_non_4l_plus_2` guard, and the `spinor_len` helper. Add `fn giao_sigma_micro` (double-gated `#[cfg(has_vendor_libcint)] #[cfg(feature="cpu")]`) that:
    1. Builds the combined fixture via `build_gauge_kappa_spinor_fixture()` (or a minimal p(+1)xs(-1) gauge!=0 ∧ kappa!=0 sub-block).
    2. Drives `int1e_cg_sa10sp_spinor` through the extended sigma_p.rs gauge variant; collects vendor via `vendor_int1e_cg_sa10sp_spinor`.
    3. Asserts both arms non-zero (assert_any_nonzero) and `count_mismatches(&vendor, &cintx, ATOL, RTOL) == 0`.
    4. Differential check: recompute the same block with common_orig = [0,0,0] and assert it byte-equals `int1e_giao_sa10sp_spinor` (vendor or cintx — giao uses the natural center, so a zero gauge origin MUST collapse cg→giao). This proves the gauge term is live, not silently zeroed.
    Leave the full 9-family parity scaffolding (collectors + the byte-identity gate macro + test_no_silent_skip) as `#[ignore]`-stubbed RED for Plan 01, so this file is the same one Wave 1 extends.
  </action>
  <verify>
    <automated>CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu giao_sigma_micro -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'fn giao_sigma_micro' crates/cintx-oracle/tests/giao_sigma_1e_parity.rs` returns a match
    - `grep -n 'common_orig' crates/cintx-oracle/tests/giao_sigma_1e_parity.rs` shows the [0,0,0] collapse differential check
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu giao_sigma_micro` exits 0 (the micro-test passes byte-identity at atol=1e-12 AND the cg→giao collapse holds)
    - The test file uses `const ATOL: f64 = 1e-12;` and `count_mismatches(..., ATOL, RTOL) == 0` (not a looser tolerance)
  </acceptance_criteria>
  <done>giao_sigma_micro is green under both gate flags: int1e_cg_sa10sp matches vendor at atol=1e-12 and collapses to int1e_giao_sa10sp at common_orig=[0,0,0]. The gauge fold is proven before Wave 1 wires the full set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host env → device gout | env[PTR_COMMON_ORIG] (gauge origin) crosses into the device x1i-with-origin step; a non-finite or wrong-slot read corrupts every cg_sa10* result |
| device staging → spinor output buffer | the gauge variant writes the 4-gc-block interleaved-complex buffer; a mis-sized write overruns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-00-01 | Tampering (data integrity) | gauge fold as cross-product instead of x1i recurrence | mitigate | The Wave-0 micro-test (Task 3) gates the exact fold against vendored libcint at atol=1e-12; a wrong sign/stride/origin fails closed (test red). Acceptance criterion forbids a `cross` implementation in the new path. |
| T-30-00-02 | Tampering | non-finite gauge origin in env[PTR_COMMON_ORIG] | accept | FND-01 `validate_common_orig_env_params` (validator.rs:210-217) already gates PTR_COMMON_ORIG finiteness upstream of this kernel; no new control needed. |
| T-30-00-03 | Tampering | silent zeroing of the gauge term (origin not actually wired) | mitigate | The cg→giao-at-origin=0 differential check (Task 3 step 4) is an independent witness that the origin plumbing is live. |
| T-30-00-04 | Tampering / DoS | buffer overrun on the interleaved spinor output of the gauge variant | mitigate | The gauge variant emits the SAME 4-gc-block KET-major shape the existing transform consumes; sizing via spinor_len (never 4l+2). Plan 01 adds the per-arm fail-closed staging guard when the launcher arm lands. |
</threat_model>

<verification>
- `cargo build -p cintx-cubecl --features cpu` and `cargo build -p cintx-oracle --features cpu` exit 0.
- `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu giao_sigma_micro` exits 0 (byte-identity at atol=1e-12 + cg→giao collapse).
- The int1e_sp (tensor_rank=1) path in sigma_p.rs and build_kappa_spinor_fixture are unchanged (git diff shows additions only).
</verification>

<success_criteria>
The gauge-origin x1i-with-origin device fold (the only genuinely-new device math) is implemented in sigma_p.rs and PROVEN byte-identical to vendored libcint via int1e_cg_sa10sp at atol=1e-12, with a live-gauge collapse witness. The combined gauge∧kappa 1e fixture (D-02) exists. Wave 1 can now wire the remaining 8 families onto a proven fold.
</success_criteria>

<output>
After completion, create `.planning/phases/30-group-5-giao-slice-spin-giao-integrals-spinor/30-00-SUMMARY.md`
</output>
