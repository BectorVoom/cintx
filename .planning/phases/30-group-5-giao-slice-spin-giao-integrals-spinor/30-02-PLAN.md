---
phase: 30-group-5-giao-slice-spin-giao-integrals-spinor
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - crates/cintx-oracle/src/fixtures.rs
  - crates/cintx-cubecl/src/kernels/two_electron.rs
  - crates/cintx-ops/generated/compiled_manifest.lock.json
  - crates/cintx-oracle/src/vendor_ffi.rs
  - crates/cintx-oracle/tests/giao_sigma_2e_parity.rs
autonomous: true
requirements: [GIAO-03]

must_haves:
  truths:
    - "All 6 2e GIAO×σ families (spgsp1, spgsp1spsp2, cg_sa10sp1, cg_sa10sp1spsp2, giao_sa10sp1, giao_sa10sp1spsp2) match vendored libcint at atol=1e-12 spinor on a 4-shell gauge∧kappa fixture"
    - "All 6 2e families carry component_rank 3"
    - "bra-1 always routes through c2s_si_2e1i; bra-2 routes through c2s_sf_2e2 (sp1) or c2s_si_2e2 (spsp2)"
    - "Every one of the 6 inline Spinor quartet launcher arms has its own fail-closed BufferTooSmall staging guard sized full-block (ni_sp*nj_sp*nk_sp*nl_sp*2)"
    - "All 6 families are oracle_covered=true spinor-only; the full vendor parity suite + manifest-audit are green (GIAO-03 fully closed)"
  artifacts:
    - path: "crates/cintx-oracle/src/fixtures.rs"
      provides: "build_gauge_kappa_spinor_2e_fixture (4-shell combined gauge+kappa quartet)"
      contains: "fn build_gauge_kappa_spinor_2e_fixture"
    - path: "crates/cintx-cubecl/src/kernels/two_electron.rs"
      provides: "rel2e_family_dispatch arms + gauge-origin gout build for the 6 families + per-arm staging guards"
      contains: "spgsp1"
    - path: "crates/cintx-ops/generated/compiled_manifest.lock.json"
      provides: "6 spinor manifest rows (arity 4, component_rank 3) for the 2e GIAO×σ families"
      contains: "int2e_spgsp1_spinor"
    - path: "crates/cintx-oracle/tests/giao_sigma_2e_parity.rs"
      provides: "6-family byte-identity gate + test_no_silent_skip"
      contains: "GIAO_2E_FAMILIES"
  key_links:
    - from: "crates/cintx-cubecl/src/kernels/two_electron.rs"
      to: "launch_rel2e_sigma_spinor_quartet (Phase-29 2e σ launcher)"
      via: "rel2e_family_dispatch returns (gout_kind, e1, e2) -> generic quartet launcher with gauge origin"
      pattern: "rel2e_family_dispatch"
    - from: "crates/cintx-oracle/tests/giao_sigma_2e_parity.rs"
      to: "cintx_oracle::fixtures::build_gauge_kappa_spinor_2e_fixture"
      via: "2e parity gate drives the 4-shell combined fixture"
      pattern: "build_gauge_kappa_spinor_2e_fixture"
---

<objective>
Register and prove all 6 2e GIAO×σ families at spinor byte-identity (atol=1e-12) on a 4-shell combined gauge∧kappa fixture (D-04 Wave 2): `int2e_spgsp1`, `int2e_spgsp1spsp2`, `int2e_cg_sa10sp1`, `int2e_cg_sa10sp1spsp2`, `int2e_giao_sa10sp1`, `int2e_giao_sa10sp1spsp2`. These reuse the Phase-29 2e si/sf transform suite and the rel2e σ quartet launcher; the only new piece is threading the gauge origin into the 2e gout build (G2E_RCI/R0I/R_I — the same x1i-with-origin structure as the 1e fold, now on CINTx1i_2e). This wave closes GIAO-03 entirely.

Purpose: Closes the 2e half of GIAO-03 — the final wave, gated after Wave 1 green. No new launch_breit dependency (all 6 use CINT2e_spinor_drv — Gaunt ssa10ssp2 deferred to Phase 31 per D-01a).
Output: the 2e fixture, 6 manifest rows, 6 two_electron.rs dispatch arms, 6 vendor shims, the 2e parity test, all flipped oracle_covered=true spinor-only, full suite + manifest-audit green.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/30-group-5-giao-slice-spin-giao-integrals-spinor/30-RESEARCH.md
@.planning/phases/30-group-5-giao-slice-spin-giao-integrals-spinor/30-PATTERNS.md
@.claude/skills/spike-findings-cintx/SKILL.md
@.planning/phases/30-group-5-giao-slice-spin-giao-integrals-spinor/30-00-SUMMARY.md
@.planning/phases/30-group-5-giao-slice-spin-giao-integrals-spinor/30-01-SUMMARY.md

<interfaces>
<!-- Authoritative 2e per-family map (RESEARCH §Per-Family gout & Transform Map / 2e). Transcribe, do NOT pick by analogy. -->

2e families (intor4.c). Columns: family | gout line | bra-1 c2s | bra-2 c2s | builder/origin | reads COMMON_ORIG | spinor component_rank
  int2e_spgsp1            | L1384 | c2s_si_2e1i | c2s_sf_2e2 | G2E_R0I (ri)+rirj | no  | 3
  int2e_spgsp1spsp2       | L1706 | c2s_si_2e1i | c2s_si_2e2 | G2E_R0I (ri)+rirj | no  | 3
  int2e_cg_sa10sp1        | L547  | c2s_si_2e1i | c2s_sf_2e2 | G2E_RCI (dri)     | YES | 3
  int2e_cg_sa10sp1spsp2   | L642  | c2s_si_2e1i | c2s_si_2e2 | G2E_RCI (dri)     | YES | 3
  int2e_giao_sa10sp1      | L905  | c2s_si_2e1i | c2s_sf_2e2 | G2E_R_I (natural) | no  | 3
  int2e_giao_sa10sp1spsp2 | L996  | c2s_si_2e1i | c2s_si_2e2 | G2E_R_I (natural) | no  | 3

Transform rule (2e): bra-1 ALWAYS c2s_si_2e1i (cart_to_spinor_si_2e1i, imaginary GIAO σ·p on e1);
bra-2 = c2s_sf_2e2 (cart_to_spinor_sf_2e2) for the plain sp1 families, c2s_si_2e2 (cart_to_spinor_si_2e2) for spsp2.
All symbols exist at c2spinor.rs:1529/1547/1563 — NO new transform built.
ALL 6 use CINT2e_spinor_drv (NOT launch_breit) — verified. component_rank = 3 for all 6 (ng[7]=3).
The 2e gauge fold = CINTx1i_2e (g2e.h:93-104, G2E_RCI dri / G2E_R0I ri / G2E_R_I natural) — same
x1i-with-origin structure as the 1e fold (Plan 00), now on the 2e G-tensor.

two_electron.rs dispatch (PATTERNS): rel2e_family_dispatch(op) -> Option<(gout_kind, e1, e2)> (L3452-3465)
feeding launch_rel2e_sigma_spinor_quartet (L3005, the rank-3 σ⊗σ launcher). Detailed quartet-build
reference: launch_int2e_spsp1_spinor_quartet (L2651-2780) — staging guard L2680-2687
(staging_required = ni_sp*nj_sp*nk_sp*nl_sp*2, full-block, before any write), spinor_len sizing
L2671-2678 (never 4l+2), gout-interleaved → component-leading cart-block transpose + contraction
weight L2730-2753, e1 si-transform → opij then e2 sf/si-transform → spinor sub-block scattered
contraction-major L2759-2780. Gate Spinor-only: `if plan.representation != Representation::Spinor
{ return Err(UnsupportedApi{..}) }`.

2e fixture template: build_kappa_spinor_2e_fixture (fixtures.rs:428) — 4-shell quartet
i(p,+1,nctr=2) j(d,-1) k(s,-1) l(p,-1), dims (2,6,2,4), non-square, GT/LT mix, nctr>1.
D-02 2e form = clone + set env[PTR_COMMON_ORIG..+3] = [0.30,-0.45,0.60] (PTR_COMMON_ORIG=1).

Manifest row: same template as 1e but family:"2e", arity:4, component_rank:"3" for all 6.
Vendor shim: clone vendor_int2e_spsp1_spinor (vendor_ffi.rs:4176) — shls:&[i32;4]; all 6 in cint_funcs.h.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 4-shell gauge∧kappa 2e fixture + 6 manifest rows + two_electron.rs dispatch arms</name>
  <files>crates/cintx-oracle/src/fixtures.rs, crates/cintx-ops/generated/compiled_manifest.lock.json, crates/cintx-cubecl/src/kernels/two_electron.rs</files>
  <read_first>
    - crates/cintx-oracle/src/fixtures.rs:428+ (build_kappa_spinor_2e_fixture — the 4-shell quartet template; clone + add PTR_COMMON_ORIG)
    - crates/cintx-cubecl/src/kernels/two_electron.rs:3005,3452-3465 (rel2e_family_dispatch + launch_rel2e_sigma_spinor_quartet) and 2651-2780 (launch_int2e_spsp1_spinor_quartet detailed build incl. staging guard L2680-2687)
    - libcint-master/src/autocode/intor4.c gout bodies VERBATIM: spgsp1 L1384, spgsp1spsp2 L1706, cg_sa10sp1 L547, cg_sa10sp1spsp2 L642, giao_sa10sp1 L905, giao_sa10sp1spsp2 L996 (the cg/giao gouts share a body per arm; only the builder G2E_RCI vs G2E_R_I differs; spg uses G2E_R0I + rirj)
    - libcint-master/src/g1e.c:429-451 + g2e.h:93-104 (CINTx1i_2e / G2E_RCI/R0I/R_I — the 2e gauge fold, same recurrence as the 1e Plan-00 fold)
    - crates/cintx-ops/generated/compiled_manifest.lock.json (an existing 2e spinor row e.g. int2e_spsp1_spinor as the arity-4 template)
    - .planning/phases/30-.../30-RESEARCH.md §"Per-Family gout & Transform Map" (2e table) + §"Phase-31 Boundary Confirmation" (all 6 use CINT2e_spinor_drv, not launch_breit)
    - .claude/skills/spike-findings-cintx/SKILL.md references/spinor-layout.md (4-index i-bra1-fastest block ordering for the quartet output)
  </read_first>
  <action>
    (a) Add `build_gauge_kappa_spinor_2e_fixture()` to fixtures.rs: clone the build_kappa_spinor_2e_fixture geometry (4-shell quartet dims (2,6,2,4), p kappa=+1 nctr=2 / d kappa=-1 / s kappa=-1 / p kappa=-1, non-square, GT/LT mix) and set env[PTR_COMMON_ORIG..+3] = [0.30,-0.45,0.60]. Do NOT mutate the Phase-29 fixture. Satisfies all 5 D-02 constraints (constraint 5: exactly 4 spinor shells, dims distinct).
    (b) Add 6 spinor rows to compiled_manifest.lock.json (edit ONLY generated/...): family:"2e", arity:4, forms:["spinor"], complex_output:true, component_rank:"3" (all 6), oracle_covered:false initially, id.operator/id.symbol per the family list.
    (c) Extend two_electron.rs: add the 6 families to rel2e_family_dispatch returning (gout_kind, e1, e2) per the 2e map (e1 always c2s_si_2e1i; e2 = c2s_sf_2e2 for sp1, c2s_si_2e2 for spsp2). Thread the gauge `origin = plan.operator_env_params.common_orig.unwrap_or([0.0;3])` into the 2e gout build: G2E_RCI dri for cg, G2E_R0I ri + rirj for spg, G2E_R_I natural (origin-free branch) for giao — using the CINTx1i_2e recurrence (f[i]=g[i+1]+origin*g[i]). Gate Spinor-only (UnsupportedApi otherwise). EVERY one of the 6 quartet arms MUST have its own fail-closed staging guard `staging_required = ni_sp*nj_sp*nk_sp*nl_sp*2; if staging.len() < staging_required { return Err(BufferTooSmall{..}) }` — full-block, before any write (Phase-28 CR-01 / RESEARCH Pitfall 5). spinor sizing via spinor_len (never 4l+2). Author device code #[cube]-legally (docs/manual/Cubecl/*.md).
    (d) OperatorId shift: re-grep `OperatorId::new(` / `_OPERATOR_ID: u32 =` after editing the lock; confirm int4c1e_cart still resolves to OperatorId::new(24) (resolver.rs:556) or resolve by symbol name (RESEARCH Pitfall 6).
  </action>
  <verify>
    <automated>cargo build -p cintx-ops 2>&1 | tail -3 && cargo build -p cintx-cubecl --features cpu 2>&1 | tail -3 && cargo build -p cintx-oracle --features cpu 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'fn build_gauge_kappa_spinor_2e_fixture' crates/cintx-oracle/src/fixtures.rs` returns a match; `grep -n 'PTR_COMMON_ORIG' crates/cintx-oracle/src/fixtures.rs` shows the 2e env writes
    - all 6 2e rows in the lock have arity 4 and component_rank "3": `grep -c 'int2e_spgsp1_spinor\|int2e_spgsp1spsp2_spinor\|int2e_cg_sa10sp1_spinor\|int2e_cg_sa10sp1spsp2_spinor\|int2e_giao_sa10sp1_spinor\|int2e_giao_sa10sp1spsp2_spinor' crates/cintx-ops/generated/compiled_manifest.lock.json` returns >= 6
    - `grep -c 'spgsp1\|cg_sa10sp1\|giao_sa10sp1' crates/cintx-cubecl/src/kernels/two_electron.rs` shows the 6 operators in the dispatch
    - `grep -n 'BufferTooSmall' crates/cintx-cubecl/src/kernels/two_electron.rs` shows a guard per new arm (full-block `ni_sp*nj_sp*nk_sp*nl_sp*2`)
    - `grep -n 'OperatorId::new(24)' crates/cintx-compat/src/resolver.rs` still resolves int4c1e_cart (or the assert resolves by symbol name)
    - `cargo build -p cintx-ops`, `-p cintx-cubecl --features cpu`, `-p cintx-oracle --features cpu` all exit 0
  </acceptance_criteria>
  <done>The 4-shell gauge∧kappa 2e fixture exists; 6 arity-4 rank-3 manifest rows registered; two_electron.rs dispatches all 6 families with the correct e1/e2 transforms, gauge-origin gout build, per-arm fail-closed staging guards, and Spinor-only gating; all crates build.</done>
</task>

<task type="auto">
  <name>Task 2: 6 vendor FFI shims + 6-family 2e parity test</name>
  <files>crates/cintx-oracle/src/vendor_ffi.rs, crates/cintx-oracle/tests/giao_sigma_2e_parity.rs</files>
  <read_first>
    - crates/cintx-oracle/src/vendor_ffi.rs:4176-4199 (vendor_int2e_spsp1_spinor shim template — shls:&[i32;4])
    - crates/cintx-oracle/tests/rel_1e_sigma_parity.rs (the scaffold: GIAO_*_FAMILIES list, byte-identity gate macro, test_no_silent_skip reading MANIFEST_ENTRIES, test_kappa_sizing guard) — adapt for the 2e collectors
    - crates/cintx-oracle/tests/giao_sigma_1e_parity.rs (the Plan-01 1e file — mirror its structure for the 2e file)
    - libcint-master/include/cint_funcs.h:488-497 (confirm all 6 int2e_*_spinor symbols declared — no suppl-header)
    - .planning/phases/30-.../30-RESEARCH.md §"Validation Architecture" (double-gate + no-silent-skip)
  </read_first>
  <action>
    (a) Add 6 `vendor_int2e_<family>_spinor` shims to vendor_ffi.rs by cloning vendor_int2e_spsp1_spinor (L4176); swap the ffi::int2e_<family>_spinor driver symbol per family; shls:&[i32;4]; out sized ni_sp*nj_sp*nk_sp*nl_sp*2. All 6 symbols are in cint_funcs.h → no suppl-header.
    (b) Create crates/cintx-oracle/tests/giao_sigma_2e_parity.rs mirroring the 1e file: `#![cfg(any(feature="cpu",feature="rocm"))]`, ATOL=1e-12/RTOL=0.0, `GIAO_2E_FAMILIES` (the 6 symbols), `collect_vendor_giao_2e`/`collect_cintx_giao_2e` (4-index collectors over build_gauge_kappa_spinor_2e_fixture()), the per-family byte-identity gate macro (double-gated, count_mismatches(..., ATOL, RTOL) == 0), `test_no_silent_skip` (both arms run + non-zero + byte-identity + entry.oracle_covered for each), and `test_kappa_sizing_non_4l_plus_2`.
  </action>
  <verify>
    <automated>CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu giao_sigma_2e -- --nocapture 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'vendor_int2e_spgsp1_spinor\|vendor_int2e_spgsp1spsp2_spinor\|vendor_int2e_cg_sa10sp1_spinor\|vendor_int2e_cg_sa10sp1spsp2_spinor\|vendor_int2e_giao_sa10sp1_spinor\|vendor_int2e_giao_sa10sp1spsp2_spinor' crates/cintx-oracle/src/vendor_ffi.rs` returns 6
    - `grep -n 'GIAO_2E_FAMILIES' crates/cintx-oracle/tests/giao_sigma_2e_parity.rs` shows the 6-family list; `grep -n 'fn test_no_silent_skip' ...` returns a match
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu giao_sigma_2e` exits 0 — all 6 byte-identity gates pass at atol=1e-12 AND test_no_silent_skip passes (no `#[ignore]`)
    - Without the env var, test_no_silent_skip FAILS or is gated out (not a silent pass)
  </acceptance_criteria>
  <done>6 vendor shims compile; all 6 2e families are byte-identical to vendored libcint at atol=1e-12 on the 4-shell gauge∧kappa fixture; test_no_silent_skip green under both flags.</done>
</task>

<task type="auto">
  <name>Task 3: Flip oracle_covered=true spinor-only + full-suite + manifest-audit phase gate</name>
  <files>crates/cintx-ops/generated/compiled_manifest.lock.json</files>
  <read_first>
    - xtask/src/oracle_covered_update.rs (the flip mechanism + SC#4 skipped-fixture-flip-refusal guard)
    - crates/cintx-ops/generated/compiled_manifest.lock.json (the 6 rows added in Task 1)
    - .planning/phases/30-.../30-RESEARCH.md §"Registration Mechanics" step 5 + §"GIAO-03 closure note" (the glob excludes ssa10 → GIAO-03 fully closed by this set)
  </read_first>
  <action>
    With the Wave-2 vendor gate green (Task 2), flip `oracle_covered` false→true for the 6 2e GIAO×σ spinor rows — SPINOR-ONLY. Use the xtask flip mechanism if it targets these symbols, else edit the lock rows directly. Run `cargo xtask manifest-audit` (green). Then run the FULL phase gate: the complete vendor parity suite (`CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu giao_sigma`) covering all 15 families (9 1e + 6 2e) green. Confirm NO capi enum variants / NO legacy cint* wrappers were added (surface policy). GIAO-03 is fully closed by this set (the glob int2e_cg_sa10* excludes ssa10 — the Gaunt families are deferred to Phase 31, NOT a GIAO-03 gap).
  </action>
  <verify>
    <automated>cargo run -p xtask -- manifest-audit 2>&1 | tail -10 && CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu giao_sigma -- --nocapture 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - All 6 2e rows in compiled_manifest.lock.json have `"oracle_covered": true` and `"forms": ["spinor"]`
    - `cargo run -p xtask -- manifest-audit` exits 0 (green)
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu giao_sigma` exits 0 — all 15 families (9 1e + 6 2e) byte-identical, both test_no_silent_skip green
    - `git diff` shows no additions to crates/cintx-capi/ and no new `cint2e_*`/legacy wrapper symbols
  </acceptance_criteria>
  <done>All 6 2e GIAO×σ families are oracle_covered=true spinor-only; manifest-audit green; the full 15-family GIAO×σ vendor suite passes at atol=1e-12 under both gate flags; no capi/legacy surface. GIAO-03 is fully closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host dispatch → 2e device quartet kernel | gauge origin + e1/e2 transform selection cross into the quartet build; a wrong origin-class or transform corrupts the 4-index output |
| device staging → interleaved 4-index spinor output | 6 quartet launcher arms scatter into the complex buffer; an unguarded arm overruns the much larger 4-index buffer |
| vendor test gate → coverage flip | flipping oracle_covered on a silently-skipped 2e family over-claims coverage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-02-01 | Tampering / DoS | missing per-arm fail-closed staging guard on a 2e quartet arm → silent partial write / mid-scatter panic on the large 4-index buffer (Phase-28/29 CR-01 repeat) | mitigate | Every one of the 6 arms asserts `staging.len() >= ni_sp*nj_sp*nk_sp*nl_sp*2` (full-block) before any write; no partial guards. Acceptance criterion greps BufferTooSmall per arm. |
| T-30-02-02 | Tampering | wrong bra-2 transform (sf_2e2 vs si_2e2) on sp1 vs spsp2 → byte mismatch | mitigate | rel2e_family_dispatch sets e2 per the verified 2e map (sp1→sf_2e2, spsp2→si_2e2), not by analogy; the atol=1e-12 gate catches a swap. |
| T-30-02-03 | Spoofing/Repudiation | silent vendor-test skip masks a 2e parity failure, then oracle_covered flipped | mitigate | test_no_silent_skip (Task 2) requires both arms to RUN under the double gate; SC#4 skipped-fixture-flip-refusal (Task 3) refuses to flip a skipped family. |
| T-30-02-04 | Tampering | a 2e family secretly needing launch_breit → wrong driver / wrong result | accept | RESEARCH §Phase-31 Boundary verified all 6 in-scope families use CINT2e_spinor_drv (NOT launch_breit); the Gaunt ssa10ssp2 families are out of scope (D-01a). No new launch_breit dependency. |
| T-30-02-05 | Tampering | CubeCL CpuRuntime FP-env side effect (~1e-11) trips the flat atol=1e-12 gate even with a bit-identical kernel | mitigate | If a 2e family is off by ~1e-11, suspect the launch FP-env side effect FIRST (RESEARCH Pitfall 7 / memory) before chasing kernel numerics; mitigate by keeping the band host or batching launches. |
</threat_model>

<verification>
- `cargo build -p cintx-ops`, `-p cintx-cubecl --features cpu`, `-p cintx-oracle --features cpu` exit 0.
- `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu giao_sigma` exits 0 (all 15 families byte-identical + both test_no_silent_skip green).
- `cargo run -p xtask -- manifest-audit` exits 0.
- All 6 2e rows oracle_covered=true spinor-only, component_rank "3". No capi/legacy surface.
</verification>

<success_criteria>
All 6 2e GIAO×σ families match vendored libcint at atol=1e-12 (spinor) on the 4-shell gauge∧kappa fixture, each with a non-skipped vendor_* test, oracle_covered=true spinor-only, and manifest-audit green. The full 15-family GIAO×σ suite passes. GIAO-03 is fully closed (no Gaunt spillover — that is Phase 31's BREIT-03).
</success_criteria>

<output>
After completion, create `.planning/phases/30-group-5-giao-slice-spin-giao-integrals-spinor/30-02-SUMMARY.md`
</output>
