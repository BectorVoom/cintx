---
phase: 30-group-5-giao-slice-spin-giao-integrals-spinor
plan: 01
type: execute
wave: 1
depends_on: [00]
files_modified:
  - crates/cintx-cubecl/src/kernels/sigma_p.rs
  - crates/cintx-cubecl/src/kernels/sigma_1e.rs
  - crates/cintx-ops/generated/compiled_manifest.lock.json
  - crates/cintx-oracle/src/vendor_ffi.rs
  - crates/cintx-oracle/tests/giao_sigma_1e_parity.rs
autonomous: true
requirements: [GIAO-03]

must_haves:
  truths:
    - "All 9 1e GIAO×σ families (spgsp, spgnucsp, spgsa01, cg_sa10{sp,nucsp,sa01}, giao_sa10{sp,nucsp,sa01}) match vendored libcint at atol=1e-12 spinor on the combined gauge∧kappa fixture"
    - "The 3 *_sa01 families carry component_rank 9; the 6 sp/nucsp families carry component_rank 3"
    - "sp/nucsp arms route through c2s_si_1ei (imaginary); sa01 arms route through c2s_si_1e (real)"
    - "Every one of the 9 inline Spinor launcher arms has its own fail-closed BufferTooSmall staging guard sized full-block"
    - "All 9 families are oracle_covered=true spinor-only with a non-skipped vendor_* test; manifest-audit is green"
  artifacts:
    - path: "crates/cintx-ops/generated/compiled_manifest.lock.json"
      provides: "9 spinor manifest rows for the 1e GIAO×σ families with correct component_rank (9 for sa01, 3 else)"
      contains: "int1e_spgsa01_spinor"
    - path: "crates/cintx-cubecl/src/kernels/sigma_1e.rs"
      provides: "family_id/family_rank/family_transform dispatch arms + fail-closed staging guards for the 9 families"
      contains: "spgsp"
    - path: "crates/cintx-oracle/src/vendor_ffi.rs"
      provides: "9 vendor_int1e_*_spinor shims"
      contains: "vendor_int1e_spgsa01_spinor"
    - path: "crates/cintx-oracle/tests/giao_sigma_1e_parity.rs"
      provides: "per-family byte-identity gate + test_no_silent_skip over all 9 families"
      contains: "GIAO_1E_FAMILIES"
  key_links:
    - from: "crates/cintx-cubecl/src/kernels/sigma_1e.rs"
      to: "crates/cintx-cubecl/src/kernels/sigma_p.rs gauge variant"
      via: "family_id #[comptime] selector -> build_sigma_cart -> run_sigma_p_on_backend gauge variant"
      pattern: "family_id"
    - from: "crates/cintx-oracle/tests/giao_sigma_1e_parity.rs"
      to: "cintx_ops::generated::MANIFEST_ENTRIES"
      via: "test_no_silent_skip asserts entry.oracle_covered for each family"
      pattern: "oracle_covered"
---

<objective>
Register and prove all 9 1e GIAO×σ families at spinor byte-identity (atol=1e-12) on the combined gauge∧kappa fixture (D-04 Wave 1): `int1e_spgsp`, `int1e_spgnucsp`, `int1e_spgsa01`, `int1e_cg_sa10sp`, `int1e_cg_sa10nucsp`, `int1e_cg_sa10sa01`, `int1e_giao_sa10sp`, `int1e_giao_sa10nucsp`, `int1e_giao_sa10sa01`. This is a TRANSCRIBE+REGISTER wave onto the gauge fold proven in Plan 00 and the existing sigma_1e.rs dispatch table.

Purpose: Closes the 1e half of GIAO-03 — the lowest-risk wave (no 2e launcher), gated green before Wave 2.
Output: 9 manifest rows, 9 sigma_1e.rs dispatch arms + gout variants, 9 vendor shims, the full 1e parity test, all flipped oracle_covered=true spinor-only.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/30-group-5-giao-slice-spin-giao-integrals-spinor/30-RESEARCH.md
@.planning/phases/30-group-5-giao-slice-spin-giao-integrals-spinor/30-PATTERNS.md
@.claude/skills/spike-findings-cintx/SKILL.md
@.planning/phases/30-group-5-giao-slice-spin-giao-integrals-spinor/30-00-SUMMARY.md

<interfaces>
<!-- The authoritative per-family map (RESEARCH §Per-Family gout & Transform Map) — transcribe, do NOT pick by analogy. -->

1e families (intor3.c). Columns: family | gout line | c2s transform symbol | builder/origin | reads COMMON_ORIG | spinor component_rank | Rys?
  int1e_spgsp          | L1724 | cart_to_spinor_si_2di (SiI) | G1E_R0I (ri)  + rirj | no  | 3 | no
  int1e_spgnucsp       | L1878 | cart_to_spinor_si_2di (SiI) | G2E_R0I (ri)  + rirj | no  | 3 | yes
  int1e_spgsa01        | L2036 | cart_to_spinor_si_2d  (Si)  | G2E_R0I (ri)  + rirj | no  | 9 | yes
  int1e_cg_sa10sp      | L1141 | cart_to_spinor_si_2di (SiI) | G1E_RCI (dri)        | YES | 3 | no
  int1e_cg_sa10nucsp   | L1230 | cart_to_spinor_si_2di (SiI) | G2E_RCI (dri)        | YES | 3 | yes
  int1e_cg_sa10sa01    | L998  | cart_to_spinor_si_2d  (Si)  | G2E_RCI (dri)        | YES | 9 | yes
  int1e_giao_sa10sp    | L1462 | cart_to_spinor_si_2di (SiI) | G1E_R_I (natural)    | no  | 3 | no
  int1e_giao_sa10nucsp | L1547 | cart_to_spinor_si_2di (SiI) | G2E_R_I (natural)    | no  | 3 | yes
  int1e_giao_sa10sa01  | L1323 | cart_to_spinor_si_2d  (Si)  | G2E_R_I (natural)    | no  | 9 | yes

Transform rule: sp/nucsp → c2s_si_1ei (SiI, imaginary); sa01 → c2s_si_1e (Si, real).
The cg and giao gout BODIES are byte-identical per arm — only the builder (G1E_RCI dri vs G1E_R_I natural) differs (RESEARCH §Family-class structure, verified line-for-line).
spg* uses G1E_R0I (origin=ri) PLUS a London rirj = ri - rj post-multiply in the gout (intor3.c:1736-1741, c[0..2]), distinct from the gauge fold.

sigma_1e.rs dispatch (PATTERNS): TransformKind {Sf, Si, SiI} (L609-626); family_transform(op) (L78);
family_id(op)->Option<u32> (L64-80); family_rank(op)->usize (return 9 for *_sa01, else 3);
fold_group (L631-665); launch_int1e_sigma_family_spinor_pair signature with origin_coords/origin_charges
(L680-719) — thread the gauge origin (common_orig) through; staging guard at L712-719:
  let staging_required = ni_sp * nj_sp * 2 * rank;
  if staging.len() < staging_required { return Err(cintxRsError::BufferTooSmall { required, provided }); }
build_sigma_cart routes *nucsp*/*sa01* Rys arms to super::sigma_1e_nuc::run_sigma_nuc_on_backend (L817);
add a fail-closed nroots > MAX_DEVICE_NROOTS → UnsupportedApi guard mirroring one_electron.rs:9340.

Manifest row template (compiled_manifest.lock.json:11470-11500, int1e_sigma_spinor):
  arity:2, canonical_family/category:"1e", complex_output:true, forms:["spinor"],
  component_rank:"3" (→ "9" for the three sa01), id.operator e.g. "spgsp",
  id.symbol e.g. "int1e_spgsp_spinor", oracle_covered:false initially (flip after gate green),
  helper_kind:"operator", profiles/compiled_in_profiles same as analog.
  EDIT ONLY crates/cintx-ops/generated/compiled_manifest.lock.json; build.rs:16,195-196 regenerates
  api_manifest.rs + .csv (both audit sides auto-sync).

Vendor shim (vendor_ffi.rs:4366): clone vendor_int1e_sigma_spinor per family; all 9 symbols in cint_funcs.h.

Test scaffold (rel_1e_sigma_parity.rs): GIAO_1E_FAMILIES list, per-family component_rank helper
(9 for sa01), giao_1e_byte_identity_gate! macro, test_no_silent_skip (reads MANIFEST_ENTRIES,
asserts entry.oracle_covered), test_kappa_sizing_non_4l_plus_2. Use build_gauge_kappa_spinor_fixture.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 9 manifest rows + sigma_1e.rs/sigma_p.rs dispatch arms + gout variants</name>
  <files>crates/cintx-ops/generated/compiled_manifest.lock.json, crates/cintx-cubecl/src/kernels/sigma_1e.rs, crates/cintx-cubecl/src/kernels/sigma_p.rs</files>
  <read_first>
    - crates/cintx-cubecl/src/kernels/sigma_1e.rs (family_id/family_rank/family_transform L64-98, fold_group L631-665, launch_int1e_sigma_family_spinor_pair + staging guard L680-719, build_sigma_cart + Rys branch L775-820)
    - crates/cintx-cubecl/src/kernels/sigma_p.rs (the gauge variant added in Plan 00 — extend with spgsp/spgnucsp/spgsa01/cg_sa10nucsp/cg_sa10sa01/giao_* gout variants alongside cg_sa10sp)
    - libcint-master/src/autocode/intor3.c gout bodies VERBATIM: spgsp L1724, spgnucsp L1878, spgsa01 L2036 (incl. the rirj London block L1736-1741), cg_sa10nucsp L1230, cg_sa10sa01 L998, giao_sa10sp L1462, giao_sa10nucsp L1547, giao_sa10sa01 L1323 (giao arms share the cg gout body; only the builder differs — see L1471-1472 G1E_R_I vs cg L1154-1155 G1E_RCI)
    - libcint-master/src/g1e.h:48-62 (G1E_R0I origin=ri for spg; G1E_RCI dri for cg; G1E_R_I natural for giao)
    - crates/cintx-ops/generated/compiled_manifest.lock.json:11470-11500 (int1e_sigma_spinor row template)
    - .planning/phases/30-.../30-RESEARCH.md §"Per-Family gout & Transform Map" (the authoritative ng[7]/rank table) and Open Q1 (the full 36-component sa01 gout mix transcribed verbatim)
    - .claude/skills/spike-findings-cintx/SKILL.md references/spinor-layout.md (output layout for the rank-9 sa01 gout — 9 component slices, no truncation)
  </read_first>
  <action>
    (a) Add 9 spinor rows to compiled_manifest.lock.json (edit ONLY generated/compiled_manifest.lock.json). For each family clone the int1e_sigma_spinor row, set id.operator + id.symbol per the family list, arity:2, forms:["spinor"], complex_output:true, oracle_covered:false (flip in Task 3 after gate green). Set component_rank="9" for int1e_spgsa01, int1e_cg_sa10sa01, int1e_giao_sa10sa01; component_rank="3" for the other 6. (RESEARCH Pitfall 3 — rank truncation: sa01 = rank 9.)
    (b) Extend sigma_1e.rs dispatch: add the 9 operators to family_id (#[comptime] selector), family_transform (sp/nucsp → SiI, sa01 → Si), and family_rank (sa01 → 9, else 3). Thread the gauge `origin` (plan.operator_env_params.common_orig.unwrap_or([0.0;3])) through launch_int1e_sigma_family_spinor_pair to the kernel; per family build dri (cg), ri (spg-R0I), or natural-center origin-free branch (giao). EVERY one of the 9 launcher arms MUST have its own fail-closed staging guard `staging_required = ni_sp*nj_sp*2*rank; if staging.len() < staging_required { return Err(BufferTooSmall{..}) }` — full-block, before any write, NO partial `if dst < staging.len()` guards (Phase-28 CR-01 / RESEARCH Pitfall 5). spinor sizing via spinor_len (never 4l+2 / Pitfall 4).
    (c) Extend sigma_p.rs with the remaining gout variants alongside Plan 00's cg_sa10sp: spgsp/spgnucsp/spgsa01 (G1E_R0I origin=ri + the rirj=ri-rj London post-multiply in the gout, c[0..2] per intor3.c:1736-1741), cg_sa10nucsp/cg_sa10sa01 (G2E_RCI dri), giao_sa10{sp,nucsp,sa01} (G1E_R_I/G2E_R_I natural center — reuse the cg gout body, origin-free branch). sa01 families produce the rank-9 (s[0..8]→n*36) gout mix transcribed verbatim. Author all device code #[cube]-legally (docs/manual/Cubecl/*.md: no plain-fn calls, no if-expr, F::exp/F::sqrt, u32/i32 only, no continue/break).
    (d) nucsp/sa01 Rys arms reuse sigma_1e_nuc::run_sigma_nuc_on_backend; add the nroots > MAX_DEVICE_NROOTS → UnsupportedApi fail-closed guard (RESEARCH Open Q2).
    (e) OperatorId shift (RESEARCH Pitfall 6): after editing the lock, re-grep `OperatorId::new(` and `_OPERATOR_ID: u32 =`; confirm int4c1e_cart still resolves to OperatorId::new(24) (resolver.rs:556) — if rows shifted it, fix the assert to resolve by symbol name. The OperatorId::new(0) consts in planner.rs/builder.rs are dummy ops (safe).
  </action>
  <verify>
    <automated>cargo build -p cintx-ops 2>&1 | tail -3 && cargo build -p cintx-cubecl --features cpu 2>&1 | tail -3 && cargo build -p cintx-oracle --features cpu 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import json,sys; d=json.load(open('crates/cintx-ops/generated/compiled_manifest.lock.json')); [print(e['id']['symbol'], e['component_rank']) for e in d['entries'] if e['id']['symbol'] in ('int1e_spgsa01_spinor','int1e_cg_sa10sa01_spinor','int1e_giao_sa10sa01_spinor','int1e_spgsp_spinor','int1e_cg_sa10sp_spinor')]"` (or `grep`) shows the three sa01 families at component_rank "9" and the sp families at "3"
    - `grep -c 'spgsp\|spgnucsp\|spgsa01\|cg_sa10\|giao_sa10' crates/cintx-cubecl/src/kernels/sigma_1e.rs` shows all 9 operators present in the dispatch
    - `grep -n 'BufferTooSmall' crates/cintx-cubecl/src/kernels/sigma_1e.rs` shows a guard per new arm (full-block sized `ni_sp*nj_sp*2*rank`)
    - `grep -vn '^//' crates/cintx-cubecl/src/kernels/sigma_p.rs | grep -c 'cross'` returns 0 (no cross-product gauge implementation)
    - `grep -n 'OperatorId::new(24)' crates/cintx-compat/src/resolver.rs` still resolves int4c1e_cart (or the assert was changed to resolve by symbol name)
    - `cargo build -p cintx-ops`, `cargo build -p cintx-cubecl --features cpu`, `cargo build -p cintx-oracle --features cpu` all exit 0
  </acceptance_criteria>
  <done>9 manifest rows registered with correct ranks (9 for sa01); sigma_1e.rs dispatches all 9 families with per-arm fail-closed staging guards and the correct c2s transform; sigma_p.rs has all 9 gout variants with the correct builder/origin; OperatorId consts re-verified; all crates build.</done>
</task>

<task type="auto">
  <name>Task 2: 9 vendor FFI shims + full 9-family parity test</name>
  <files>crates/cintx-oracle/src/vendor_ffi.rs, crates/cintx-oracle/tests/giao_sigma_1e_parity.rs</files>
  <read_first>
    - crates/cintx-oracle/src/vendor_ffi.rs:4366-4389 (vendor_int1e_sigma_spinor shim template)
    - crates/cintx-oracle/tests/giao_sigma_1e_parity.rs (the Plan-00 file with giao_sigma_micro — extend it, do not recreate)
    - crates/cintx-oracle/tests/rel_1e_sigma_parity.rs:32-54,61-66,313,387-472 (GIAO_1E_FAMILIES list, component_rank helper, test_kappa_sizing guard, giao_1e_byte_identity_gate! macro, test_no_silent_skip)
    - libcint-master/include/cint_funcs.h:488-501 (confirm all 9 int1e_*_spinor symbols are declared — no suppl-header)
    - .planning/phases/30-.../30-RESEARCH.md §"Validation Architecture" (double-gate invocation + no-silent-skip semantics)
  </read_first>
  <action>
    (a) Add 9 `vendor_int1e_<family>_spinor` shims to vendor_ffi.rs by cloning vendor_int1e_sigma_spinor (L4366); swap the ffi::int1e_<family>_spinor driver symbol per family. All 9 symbols are in cint_funcs.h → no suppl-header extern decls. out sized ni_sp*nj_sp*2 interleaved re/im.
    (b) Extend giao_sigma_1e_parity.rs: define `GIAO_1E_FAMILIES` (the 9 symbols), the per-family component_rank helper (9 for *_sa01, 3 else), `collect_vendor_giao_1e`/`collect_cintx_giao_1e`, the `giao_1e_byte_identity_gate!` macro instantiated once per family (double-gated `#[cfg(has_vendor_libcint)] #[cfg(feature="cpu")]`, drives both collectors on build_gauge_kappa_spinor_fixture(), `count_mismatches(..., ATOL, RTOL) == 0`), `test_no_silent_skip` (verbatim from the analog — asserts both arms non-zero, byte-identity, AND `entry.oracle_covered` in MANIFEST_ENTRIES for each family), and `test_kappa_sizing_non_4l_plus_2`. Remove any `#[ignore]` stubs left from Plan 00.
  </action>
  <verify>
    <automated>CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu giao_sigma_1e -- --nocapture 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'vendor_int1e_spgsp_spinor\|vendor_int1e_spgnucsp_spinor\|vendor_int1e_spgsa01_spinor\|vendor_int1e_cg_sa10sp_spinor\|vendor_int1e_cg_sa10nucsp_spinor\|vendor_int1e_cg_sa10sa01_spinor\|vendor_int1e_giao_sa10sp_spinor\|vendor_int1e_giao_sa10nucsp_spinor\|vendor_int1e_giao_sa10sa01_spinor' crates/cintx-oracle/src/vendor_ffi.rs` returns 9
    - `grep -n 'GIAO_1E_FAMILIES' crates/cintx-oracle/tests/giao_sigma_1e_parity.rs` shows the 9-family list; `grep -n 'fn test_no_silent_skip' ...` returns a match
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu giao_sigma_1e` exits 0 — all 9 per-family byte-identity gates pass at atol=1e-12 AND test_no_silent_skip passes (no `#[ignore]`, no skip)
    - Without the env var, `cargo test -p cintx-oracle --features cpu giao_sigma_1e` shows test_no_silent_skip FAILS or is gated out (not a silent pass) — confirms the double-gate (memory reference_oracle_vendor_parity_invocation)
  </acceptance_criteria>
  <done>9 vendor shims compile; all 9 1e families are byte-identical to vendored libcint at atol=1e-12 on the combined gauge∧kappa fixture; test_no_silent_skip is green under both flags.</done>
</task>

<task type="auto">
  <name>Task 3: Flip oracle_covered=true spinor-only + manifest-audit gate</name>
  <files>crates/cintx-ops/generated/compiled_manifest.lock.json</files>
  <read_first>
    - xtask/src/oracle_covered_update.rs (the flip mechanism + the SC#4 skipped-fixture-flip-refusal guard — flip only after the gate is green and the fixture is non-skipped)
    - crates/cintx-ops/generated/compiled_manifest.lock.json (the 9 rows added in Task 1 — flip oracle_covered false→true)
    - .planning/phases/30-.../30-RESEARCH.md §"Registration Mechanics" step 5
  </read_first>
  <action>
    With the Wave-1 vendor gate green (Task 2), flip `oracle_covered` from false to true for the 9 1e GIAO×σ spinor rows — SPINOR-ONLY (do NOT flip any cart/sph GIAO×σ intermediate; this family set is registered spinor-only). Use the xtask flip mechanism (xtask/src/oracle_covered_update.rs) if it targets these symbols, else edit the lock rows directly. Then run `cargo xtask manifest-audit` and confirm it is green (both audit sides derive from the lock, so the edit auto-syncs). Confirm NO capi enum variants and NO legacy cint* wrappers were added anywhere (new-family surface policy).
  </action>
  <verify>
    <automated>cargo run -p xtask -- manifest-audit 2>&1 | tail -10 && CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu giao_sigma_1e -- --nocapture 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All 9 1e rows in compiled_manifest.lock.json have `"oracle_covered": true` and `"forms": ["spinor"]` (spinor-only)
    - `cargo run -p xtask -- manifest-audit` exits 0 (green)
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu giao_sigma_1e` still exits 0 (test_no_silent_skip now passes the oracle_covered=true assertion for all 9)
    - `git diff` shows no additions to crates/cintx-capi/ and no new `cint1e_*`/legacy wrapper symbols (surface policy)
  </acceptance_criteria>
  <done>All 9 1e GIAO×σ families are oracle_covered=true spinor-only; manifest-audit is green; the full 1e parity suite passes under both gate flags; no capi/legacy surface added. Wave 1 is gated green — Wave 2 may begin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host dispatch → device kernel | the gauge origin + per-family selector cross into the device; a wrong origin-class or rank truncates/corrupts output |
| device staging → interleaved spinor output | 9 inline launcher arms scatter into the complex buffer; an unguarded arm overruns/partial-writes |
| vendor test gate → coverage flip | flipping oracle_covered=true on a silently-skipped family over-claims coverage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01-01 | Tampering (data integrity) | component_rank truncation — sa01 family registered at rank 3 drops 6 of 9 tensor components | mitigate | Task 1 sets component_rank="9" for the three sa01 families; acceptance criterion greps the lock to confirm 9 vs 3. The rank-9 byte-identity gate (Task 2) fails closed if a component is dropped. |
| T-30-01-02 | Tampering / DoS | missing per-inline-arm fail-closed staging guard → silent partial write / mid-scatter panic (Phase-28 CR-01 repeat) | mitigate | Every one of the 9 arms asserts `staging.len() >= ni_sp*nj_sp*2*rank` (full-block) before any write; no partial `if dst < staging.len()` guards. Acceptance criterion greps for BufferTooSmall per arm. |
| T-30-01-03 | Spoofing/Repudiation | silent vendor-test skip masks a parity failure, then oracle_covered flipped | mitigate | test_no_silent_skip (Task 2) requires both arms to RUN and produce nonzero output under the double gate; the SC#4 skipped-fixture-flip-refusal guard (Task 3) refuses to flip a skipped family. Acceptance criterion confirms the test FAILS (not skips) without CINTX_ORACLE_BUILD_VENDOR=1. |
| T-30-01-04 | Tampering | wrong c2s transform (si_1ei vs si_1e) corrupts the re/im lane split | mitigate | family_transform is set per the verified RESEARCH per-family map (sp/nucsp→SiI, sa01→Si), not by analogy; the atol=1e-12 byte-identity gate catches a re/im swap. |
| T-30-01-05 | Tampering | nroots out of device envelope on nucsp/sa01 Rys arms → silent truncation | mitigate | fail-closed `nroots > MAX_DEVICE_NROOTS → UnsupportedApi` guard (Task 1d) mirroring one_electron.rs:9340. |
</threat_model>

<verification>
- `cargo build -p cintx-ops`, `-p cintx-cubecl --features cpu`, `-p cintx-oracle --features cpu` exit 0.
- `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu giao_sigma_1e` exits 0 (9 families byte-identical + test_no_silent_skip green).
- `cargo run -p xtask -- manifest-audit` exits 0.
- The three sa01 families carry component_rank "9"; all 9 rows are oracle_covered=true spinor-only.
- No capi/legacy surface added.
</verification>

<success_criteria>
All 9 1e GIAO×σ families match vendored libcint at atol=1e-12 (spinor) on the combined gauge∧kappa fixture, each with a non-skipped vendor_* test, correct component_rank (9 for sa01), per-arm fail-closed staging guards, oracle_covered=true spinor-only, and manifest-audit green. The 1e half of GIAO-03 is closed.
</success_criteria>

<output>
After completion, create `.planning/phases/30-group-5-giao-slice-spin-giao-integrals-spinor/30-01-SUMMARY.md`
</output>
