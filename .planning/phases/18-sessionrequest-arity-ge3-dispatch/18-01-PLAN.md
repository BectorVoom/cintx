---
phase: 18
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - crates/cintx-ops/generated/compiled_manifest.lock.json
  - crates/cintx-ops/src/resolver.rs
  - crates/cintx-oracle/src/vendor_ffi.rs
  - crates/cintx-oracle/build.rs
  - crates/cintx-core/src/operator.rs
  - crates/cintx-core/src/lib.rs
  - crates/cintx-runtime/src/options.rs
  - crates/cintx-rs/src/prelude.rs
autonomous: true
requirements:
  - ARITY-01
  - ARITY-02
  - ARITY-04
tags: [manifest, vendor-ffi, aosym, foundation, wave-0]

must_haves:
  truths:
    - "Plain `int3c2e_cart` (OperatorId 22) and `int3c2e_sph` (OperatorId 23) operator-kind rows exist in `crates/cintx-ops/generated/compiled_manifest.lock.json` under family `3c2e`, operator `electron-repulsion`, stability `stable`, all four profiles (per CONTEXT.md D-06 and PATTERNS.md §Step 1)."
    - "Build of `cintx-ops` regenerates `src/generated/api_manifest.rs` and `src/generated/api_manifest.csv` to contain `int3c2e_cart` and `int3c2e_sph` rows; downstream `int4c1e_cart` shifts to OperatorId 24 and `int4c1e_sph` shifts to OperatorId 25 (per PATTERNS.md §Step 2)."
    - "Vendor FFI wrappers `vendor_int3c1e_p2_sph` and `vendor_int3c2e_cart` exist in `crates/cintx-oracle/src/vendor_ffi.rs` mirroring the existing arity-3 wrapper shape (per RESEARCH.md R4 and PATTERNS.md §vendor_ffi.rs). NOTE: `vendor_int3c2e_ip1_sph` is NOT added in Phase 18 because the `int3c2e_ip1_*` parity reference is `vendor_int3c2e_{cart,sph}` plain — see Plan 18-03 Task 1 disposition and RESEARCH.md A6 / Item 5."
    - "`pub enum AoSymmetry { S1, S2ij, S2kl, S4, S8 }` is declared in `crates/cintx-core/src/operator.rs` with derive set `Copy, Clone, Debug, PartialEq, Eq, Hash, Default`, `#[default]` on `S1`, `#[repr(u8)]`, and a `Display` impl emitting lowercase pyscf form (`s1`, `s2ij`, `s2kl`, `s4`, `s8`) — per CONTEXT.md D-03 + PATTERNS.md §operator.rs."
    - "`AoSymmetry` is re-exported from `crates/cintx-core/src/lib.rs` alongside `OperatorId` and `Representation`, and from `crates/cintx-rs/src/prelude.rs` next to other `cintx_core::*` re-exports."
    - "`pub aosym: Option<cintx_core::AoSymmetry>` field is added to `ExecutionOptions` in `crates/cintx-runtime/src/options.rs` after the existing `f12_zeta` field; field default is `None` (preserved by the existing `#[derive(Default)]` on `ExecutionOptions`)."
    - "Resolver's `misc_wrapper_macro` (`crates/cintx-ops/src/resolver.rs:314-322`) has an explicit `\"int3c2e\" => None` arm so `legacy_wrapper_manifest_matches_misc` accepts the new base symbol without requiring synthetic `cint3c2e_*` legacy rows (per PATTERNS.md §Step 3 Option A); the test body at lines 391-417 uses early-continue on `None` instead of panicking."
    - "`cargo build --workspace --locked` exits 0 after all wave-0 edits."
    - "`cargo test -p cintx-ops --locked` exits 0 — the resolver tests including `legacy_wrapper_manifest_matches_misc` pass."
    - "`cargo test -p cintx-core --locked` exits 0 — `AoSymmetry` compiles and the existing `Representation` tests are unaffected."
    - "`cargo test -p cintx-runtime --locked` exits 0 — `ExecutionOptions` with new `aosym` field compiles, default is `None`."
  artifacts:
    - path: "crates/cintx-ops/generated/compiled_manifest.lock.json"
      provides: "Source-of-truth manifest with two new arity-3 operator rows (int3c2e_cart, int3c2e_sph)"
      contains: '"symbol": "int3c2e_cart"'
    - path: "crates/cintx-ops/src/generated/api_manifest.rs"
      provides: "Regenerated Rust manifest with int3c2e_{cart,sph} ManifestEntry blocks (auto-regenerated by build.rs)"
      contains: 'symbol_name: "int3c2e_cart"'
    - path: "crates/cintx-ops/src/generated/api_manifest.csv"
      provides: "Regenerated CSV manifest including int3c2e_{cart,sph} rows (auto-regenerated by build.rs)"
      contains: "int3c2e_cart"
    - path: "crates/cintx-core/src/operator.rs"
      provides: "AoSymmetry enum sibling to Representation"
      contains: "pub enum AoSymmetry"
    - path: "crates/cintx-core/src/lib.rs"
      provides: "AoSymmetry re-export"
      contains: "AoSymmetry"
    - path: "crates/cintx-runtime/src/options.rs"
      provides: "ExecutionOptions::aosym field (Option<AoSymmetry>)"
      contains: "pub aosym: Option<cintx_core::AoSymmetry>"
    - path: "crates/cintx-rs/src/prelude.rs"
      provides: "AoSymmetry re-export through prelude"
      contains: "pub use cintx_core::AoSymmetry"
    - path: "crates/cintx-oracle/src/vendor_ffi.rs"
      provides: "Two new vendor FFI wrappers: vendor_int3c1e_p2_sph, vendor_int3c2e_cart"
      contains: "pub fn vendor_int3c1e_p2_sph"
    - path: "crates/cintx-ops/src/resolver.rs"
      provides: "misc_wrapper_macro arm and test early-continue for the new int3c2e base symbol"
      contains: '"int3c2e" => None'
  key_links:
    - from: "crates/cintx-ops/generated/compiled_manifest.lock.json (entries[21], entries[22])"
      to: "crates/cintx-ops/src/generated/api_manifest.rs OPERATOR_DESCRIPTORS table"
      via: "build.rs regeneration from canonical lock file"
      pattern: 'symbol_name: "int3c2e_(cart|sph)"'
    - from: "crates/cintx-runtime/src/options.rs::ExecutionOptions::aosym"
      to: "crates/cintx-core/src/operator.rs::AoSymmetry"
      via: "Option<cintx_core::AoSymmetry> field"
      pattern: "Option<cintx_core::AoSymmetry>"
    - from: "crates/cintx-rs/src/prelude.rs"
      to: "crates/cintx-core/src/operator.rs::AoSymmetry"
      via: "pub use cintx_core::AoSymmetry"
      pattern: "pub use cintx_core::AoSymmetry"
---

<objective>
Wave 0 foundation for Phase 18. Eight independent edits across seven crates that any subsequent plan depends on. All edits target different files (no overlap) so this plan is structured as four parallelizable tasks executed in sequence within one agent run. The work falls into three concerns:

1. **Manifest expansion (per CONTEXT.md D-06 + RESEARCH.md R1 resolution + PATTERNS.md §Step 1-3):** add plain `int3c2e_cart`/`int3c2e_sph` rows to `compiled_manifest.lock.json` so the safe-API resolver routes `OperatorId::new(22)`/`OperatorId::new(23)` to the existing `launch_center_3c2e` kernel. Side-effects: `int4c1e_*` IDs shift +2 (handled in Plan 02); resolver `misc_wrapper_macro` test needs a `"int3c2e" => None` arm because upstream libcint 6.1.3 has no `cint3c2e_*` legacy wrappers.
2. **Domain primitive additions (per CONTEXT.md D-02, D-03 + PATTERNS.md §operator.rs):** `AoSymmetry` enum in `cintx-core::operator`, re-exports through `cintx-core::lib` and `cintx-rs::prelude`, and a `pub aosym: Option<AoSymmetry>` field on `ExecutionOptions` mirroring the existing `f12_zeta` pattern.
3. **Oracle vendor wrappers (per RESEARCH.md R4 + PATTERNS.md §vendor_ffi.rs):** two new wrappers `vendor_int3c1e_p2_sph`, `vendor_int3c2e_cart` in `cintx-oracle::vendor_ffi` so the arity-3 parity tests in Plan 03 can call vendored libcint 6.1.3 directly. NOTE: `vendor_int3c2e_ip1_sph` is NOT added in Phase 18. Per RESEARCH.md Item 5 + A6 (and the existing `center_3c2e_parity.rs:222-287` precedent), the cintx `int3c2e_ip1_*` kernel currently computes the plain 3c2e integral (`component_rank: ""` → multiplier 1, output buffer size `ni*nj*nk`). Plan 18-03's parity tests for `int3c2e_ip1_{cart,sph}` therefore reference `vendor_int3c2e_{cart,sph}` (plain), not `vendor_int3c2e_ip1_{cart,sph}`. The plain sph wrapper `vendor_int3c2e_sph` already exists at vendor_ffi.rs:204; only `vendor_int3c2e_cart` needs to be added here.

Purpose:
- **ARITY-01:** The manifest expansion is the routing prerequisite that lets `SessionRequest::evaluate` accept the plain `int3c2e_{cart,sph}` shell triples via `Resolver::descriptor(OperatorId::new(22))` and `OperatorId::new(23)`.
- **ARITY-02:** The vendor FFI wrappers are the byte-identity oracle counterpart for the new arity-3 tests.
- **ARITY-04:** The `AoSymmetry` enum plus `ExecutionOptions::aosym` field is the foundation the safe-API preflight (Plan 02) sits on top of.

Output: Eight modified files plus two regenerated files in `crates/cintx-ops/src/generated/`. Estimated diff: ~110 net new lines (lock-file additions ~50, AoSymmetry ~30, ExecutionOptions field ~7, re-exports ~4, two vendor wrappers ~46, resolver arm ~5). Context cost: medium (one Cargo build to verify regeneration, one workspace test pass).

This plan lives in **wave 0** with no upstream dependencies. Plans 02, 03, 04 all depend on this plan's manifest expansion (OperatorId stability) and `AoSymmetry` + `ExecutionOptions::aosym` (Plan 02's preflight and Plan 03/04's tests need them to compile).
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/18-sessionrequest-arity-ge3-dispatch/18-CONTEXT.md
@.planning/phases/18-sessionrequest-arity-ge3-dispatch/18-RESEARCH.md
@.planning/phases/18-sessionrequest-arity-ge3-dispatch/18-PATTERNS.md
@.planning/phases/17-real-integral-evaluation-in-safe-api/17-CONTEXT.md
@CLAUDE.md
@crates/cintx-ops/generated/compiled_manifest.lock.json
@crates/cintx-ops/src/resolver.rs
@crates/cintx-ops/build.rs
@crates/cintx-oracle/src/vendor_ffi.rs
@crates/cintx-oracle/build.rs
@crates/cintx-core/src/operator.rs
@crates/cintx-core/src/lib.rs
@crates/cintx-runtime/src/options.rs
@crates/cintx-rs/src/prelude.rs

<interfaces>
<!-- Existing `Representation` pattern (the immediate neighbor for AoSymmetry — verbatim verified at cintx-core/src/operator.rs:4-20) -->
```rust
#[derive(Copy, Clone, Debug, PartialEq, Eq, Hash)]
#[repr(u8)]
pub enum Representation {
    Cart,
    Spheric,
    Spinor,
}

impl fmt::Display for Representation {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        match self {
            Representation::Cart => write!(f, "Cart"),
            Representation::Spheric => write!(f, "Spheric"),
            Representation::Spinor => write!(f, "Spinor"),
        }
    }
}
```

<!-- AoSymmetry to ADD (PATTERNS.md §operator.rs verbatim) -->
```rust
/// AO symmetry packing convention (pyscf-compatible naming).
///
/// Phase 18 ships `S1` only; every other variant returns
/// `FacadeError::UnsupportedAoSymmetry` from `SessionRequest::query_workspace`.
/// `Display` emits the lowercase pyscf form (`s1`, `s2ij`, `s2kl`, `s4`, `s8`).
#[derive(Copy, Clone, Debug, PartialEq, Eq, Hash, Default)]
#[repr(u8)]
pub enum AoSymmetry {
    #[default]
    S1,
    S2ij,
    S2kl,
    S4,
    S8,
}

impl fmt::Display for AoSymmetry {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        match self {
            AoSymmetry::S1 => write!(f, "s1"),
            AoSymmetry::S2ij => write!(f, "s2ij"),
            AoSymmetry::S2kl => write!(f, "s2kl"),
            AoSymmetry::S4 => write!(f, "s4"),
            AoSymmetry::S8 => write!(f, "s8"),
        }
    }
}
```

<!-- cintx-core/src/lib.rs line 18 (verbatim verified) -->
```rust
pub use operator::{OperatorId, Representation};
```
<!-- Change to (alphabetical order): -->
```rust
pub use operator::{AoSymmetry, OperatorId, Representation};
```

<!-- ExecutionOptions f12_zeta pattern (verbatim verified at cintx-runtime/src/options.rs:103-117) -->
```rust
#[derive(Clone, Debug, Default)]
pub struct ExecutionOptions {
    pub memory_limit_bytes: Option<usize>,
    pub trace_span: Option<Span>,
    pub chunk_size_override: Option<usize>,
    pub profile_label: Option<&'static str>,
    pub backend_intent: BackendIntent,
    pub backend_capability_token: BackendCapabilityToken,
    /// F12/STG/YP zeta parameter ...
    pub f12_zeta: Option<f64>,
}
```
<!-- Change: append `aosym` AFTER f12_zeta -->

<!-- prelude.rs current re-exports (lines 31-34, verbatim verified) -->
```rust
pub use cintx_core::BasisSet;
pub use cintx_core::OperatorId;
pub use cintx_core::Representation;
pub use cintx_core::ShellTuple;
```
<!-- Insert `pub use cintx_core::AoSymmetry;` alphabetically BEFORE `BasisSet` -->

<!-- Existing vendor_int3c1e_p2_cart pattern (PATTERNS.md analog at vendor_ffi.rs:465-490) - shape to mirror for vendor_int3c1e_p2_sph -->
```rust
/// Evaluate int3c1e_p2_cart for a single shell triple using vendored libcint.
///
/// `shls` is `[i, j, k]` — three shell indices.
pub fn vendor_int3c1e_p2_cart(
    out: &mut [f64],
    shls: &[i32; 3],
    atm: &[i32],
    natm: i32,
    bas: &[i32],
    nbas: i32,
    env: &[f64],
) -> i32 {
    unsafe {
        ffi::int3c1e_p2_cart(
            out.as_mut_ptr(),
            ptr::null_mut(),
            shls.as_ptr() as *mut i32,
            atm.as_ptr() as *mut i32,
            natm,
            bas.as_ptr() as *mut i32,
            nbas,
            env.as_ptr() as *mut f64,
            ptr::null_mut(),
            ptr::null_mut(),
        )
    }
}
```

<!-- Existing resolver misc_wrapper_macro (verbatim verified at cintx-ops/src/resolver.rs:314-322) -->
```rust
fn misc_wrapper_macro(base_symbol: &str) -> Option<MiscWrapperMacro> {
    match base_symbol {
        "int1e_ovlp" | "int1e_nuc" | "int2e" | "int2c2e" | "int3c1e" | "int3c1e_p2" | "int3c2e_ip1" => {
            Some(MiscWrapperMacro::AllCint)
        }
        "int1e_kin" => Some(MiscWrapperMacro::AllCint1e),
        _ => None,
    }
}
```

<!-- Existing test arm currently panicking on unknown base symbols (verbatim verified at cintx-ops/src/resolver.rs:400-405) -->
```rust
for base_symbol in base_symbols {
    let macro_kind = misc_wrapper_macro(&base_symbol).unwrap_or_else(|| {
        panic!("missing misc.h wrapper macro classification for {base_symbol}")
    });
    expected.extend(expected_legacy_wrapper_symbols(&base_symbol, macro_kind));
}
```
<!-- Per PATTERNS.md §Step 3 Option A: add `"int3c2e" => None` arm to misc_wrapper_macro AND change `.unwrap_or_else(panic!)` to an early `continue` so the new base symbol is tolerated without synthetic legacy rows. -->

<!-- Manifest lock entry shape — existing int3c1e_cart (verbatim verified at compiled_manifest.lock.json:275-290) -->
```json
{
  "id": {
    "family": "3c1e",
    "operator": "overlap",
    "representation": "cart",
    "symbol": "int3c1e_cart"
  },
  "oracle_covered": true,
  "profiles": [
    "base",
    "with-f12",
    "with-4c1e",
    "with-f12+with-4c1e"
  ],
  "stability": "stable"
}
```

<!-- Position in entries array: between int3c2e_ip1_spinor (ends at line 354) and int4c1e_cart (begins at line 355). -->

<!-- Existing oracle build.rs supplemental header (verified at crates/cintx-oracle/build.rs:227 — int3c1e_p2_sph already in the supplemental whitelist; int3c2e_cart is also in cint_funcs.h). Pre-impl grep below confirms binding availability. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add AoSymmetry enum + ExecutionOptions::aosym field + re-exports (independent domain primitives)</name>
  <files>
    crates/cintx-core/src/operator.rs,
    crates/cintx-core/src/lib.rs,
    crates/cintx-runtime/src/options.rs,
    crates/cintx-rs/src/prelude.rs
  </files>
  <read_first>
    - crates/cintx-core/src/operator.rs (read in full — verify the existing `Representation` enum at lines 4-20 is exactly as quoted in `<interfaces>`; this is the immediate neighbor and the structural template for `AoSymmetry`)
    - crates/cintx-core/src/lib.rs (read in full — verify line 18 is `pub use operator::{OperatorId, Representation};` and find the alphabetical insertion point for `AoSymmetry`)
    - crates/cintx-runtime/src/options.rs (read in full — verify the `f12_zeta` pattern at lines 103-117; identify any `use cintx_core::*` imports already present so the new field's type path resolves)
    - crates/cintx-rs/src/prelude.rs (read in full — verify the existing `pub use cintx_core::*` lines and the alphabetical position for `AoSymmetry`)
    - crates/cintx-runtime/Cargo.toml (verify cintx-runtime already depends on cintx-core — fix is automatic via existing `cintx-core = { path = ... }` entry; no Cargo.toml edit needed)
    - .planning/phases/18-sessionrequest-arity-ge3-dispatch/18-PATTERNS.md §operator.rs / §lib.rs / §options.rs / §prelude.rs (full AoSymmetry block, re-export shapes, ExecutionOptions field placement)
    - .planning/phases/18-sessionrequest-arity-ge3-dispatch/18-CONTEXT.md D-02, D-03 (locked decisions on `aosym` ExecutionOptions field and AoSymmetry enum location/variants)
  </read_first>
  <action>
    Four independent edits. None depends on the others; all compile together. No Cargo.toml change is required (cintx-runtime already has cintx-core as a direct dep, verified via `cargo metadata`).

    **Edit A — `crates/cintx-core/src/operator.rs`:** Add the `AoSymmetry` enum AFTER the `Representation` block (current lines 4-20) and BEFORE the next item (`OperatorId` block). Use the verbatim block from `<interfaces>` above:

    ```rust
    /// AO symmetry packing convention (pyscf-compatible naming).
    ///
    /// Phase 18 ships `S1` only; every other variant returns
    /// `FacadeError::UnsupportedAoSymmetry` from `SessionRequest::query_workspace`.
    /// `Display` emits the lowercase pyscf form (`s1`, `s2ij`, `s2kl`, `s4`, `s8`).
    #[derive(Copy, Clone, Debug, PartialEq, Eq, Hash, Default)]
    #[repr(u8)]
    pub enum AoSymmetry {
        #[default]
        S1,
        S2ij,
        S2kl,
        S4,
        S8,
    }

    impl fmt::Display for AoSymmetry {
        fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
            match self {
                AoSymmetry::S1 => write!(f, "s1"),
                AoSymmetry::S2ij => write!(f, "s2ij"),
                AoSymmetry::S2kl => write!(f, "s2kl"),
                AoSymmetry::S4 => write!(f, "s4"),
                AoSymmetry::S8 => write!(f, "s8"),
            }
        }
    }
    ```

    Per CONTEXT.md D-03 + PATTERNS.md §operator.rs: derive set `Copy, Clone, Debug, PartialEq, Eq, Hash, Default`; `#[default]` on `S1`; `#[repr(u8)]`. Note `Default` is NOT on `Representation` (per PATTERNS.md note); it IS on `AoSymmetry` here so `ExecutionOptions::default()` works. The `fmt::Display` impl reuses the existing `fmt` import at top of file (no new import needed — verified the file already imports `core::fmt` via the `Representation` block).

    **Edit B — `crates/cintx-core/src/lib.rs`:** Extend the existing re-export at line 18:

    Before:
    ```rust
    pub use operator::{OperatorId, Representation};
    ```
    After (alphabetical order):
    ```rust
    pub use operator::{AoSymmetry, OperatorId, Representation};
    ```

    **Edit C — `crates/cintx-runtime/src/options.rs`:** Add `aosym` field AFTER the `f12_zeta` field on `ExecutionOptions`. Final field block:

    ```rust
        pub f12_zeta: Option<f64>,
        /// AO symmetry packing requested by the caller. Phase 18 implements `S1` only;
        /// every other variant returns `FacadeError::UnsupportedAoSymmetry` from
        /// `SessionRequest::query_workspace`. `None` is the default and is treated as
        /// `Some(AoSymmetry::S1)`.
        pub aosym: Option<cintx_core::AoSymmetry>,
    ```

    The fully-qualified `cintx_core::AoSymmetry` path is used because `options.rs` does not currently re-import `AoSymmetry` (verified — only `BackendIntent`, `BackendCapabilityToken`, `Span` are in scope at the top). DO NOT add `use cintx_core::AoSymmetry;` unless the file already has a `use cintx_core::*` block; the fully-qualified path is cleaner.

    The existing `#[derive(Clone, Debug, Default)]` on `ExecutionOptions` (line 103) gives `aosym: None` for free — no `impl Default` body required.

    **Edit D — `crates/cintx-rs/src/prelude.rs`:** Insert `pub use cintx_core::AoSymmetry;` alphabetically BEFORE the existing `pub use cintx_core::BasisSet;` line. Final block:

    ```rust
    pub use cintx_core::AoSymmetry;
    pub use cintx_core::BasisSet;
    pub use cintx_core::OperatorId;
    pub use cintx_core::Representation;
    pub use cintx_core::ShellTuple;
    ```

    Per CONTEXT.md D-03: `AoSymmetry` MUST appear in `cintx-rs::prelude` so callers can do `use cintx_rs::prelude::*;` and access it.

    DO NOT add a builder setter `.aosym(...)` to `crates/cintx-rs/src/builder.rs` in this task — that is OPTIONAL (CONTEXT.md "Claude's Discretion"); Plan 02 handles it as part of the safe-API surface task if the planner judges it free. Default for this plan: skip the builder setter.
  </action>
  <verify>
    <automated>cargo build -p cintx-core --locked &amp;&amp; cargo build -p cintx-runtime --locked &amp;&amp; cargo build -p cintx-rs --locked &amp;&amp; cargo test -p cintx-core --locked &amp;&amp; grep -q 'pub enum AoSymmetry' crates/cintx-core/src/operator.rs &amp;&amp; grep -q '#\[default\]' crates/cintx-core/src/operator.rs &amp;&amp; grep -Eq 'pub use operator::\{AoSymmetry, OperatorId, Representation\}' crates/cintx-core/src/lib.rs &amp;&amp; grep -q 'pub aosym: Option<cintx_core::AoSymmetry>' crates/cintx-runtime/src/options.rs &amp;&amp; grep -q 'pub use cintx_core::AoSymmetry;' crates/cintx-rs/src/prelude.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p cintx-core --locked` exits 0.
    - `cargo build -p cintx-runtime --locked` exits 0.
    - `cargo build -p cintx-rs --locked` exits 0 (prelude re-export resolves).
    - `cargo test -p cintx-core --locked` exits 0 — existing Representation tests unaffected; AoSymmetry compiles.
    - `grep -q 'pub enum AoSymmetry' crates/cintx-core/src/operator.rs` matches.
    - `grep -c 'AoSymmetry::S' crates/cintx-core/src/operator.rs` is at least 10 (5 variant declarations in match arms + variant uses).
    - `grep -q '#\[default\]' crates/cintx-core/src/operator.rs` matches (S1 is the default variant — verifiable via grep; the aosym error-path runtime test that exercises `Default for AoSymmetry` lives in Plan 18-02 Task 2's `aosym_none_and_s1_both_succeed_through_query_workspace` per CONTEXT.md D-05, not in this plan).
    - `grep -q '#\[repr(u8)\]' crates/cintx-core/src/operator.rs` matches (repr stamp present).
    - `grep -Eq 'pub use operator::\{AoSymmetry, OperatorId, Representation\}' crates/cintx-core/src/lib.rs` matches (alphabetical order preserved).
    - `grep -q 'pub aosym: Option<cintx_core::AoSymmetry>' crates/cintx-runtime/src/options.rs` matches.
    - `grep -q 'pub use cintx_core::AoSymmetry;' crates/cintx-rs/src/prelude.rs` matches.
    - Negative — no scope reduction: `! grep -F 'TODO' crates/cintx-core/src/operator.rs | grep -i 'aosym'` (no TODO markers on AoSymmetry).
    - Negative — no Cargo.toml drift: `git diff -- crates/cintx-core/Cargo.toml crates/cintx-runtime/Cargo.toml crates/cintx-rs/Cargo.toml` produces no output.
  </acceptance_criteria>
  <done>
    Four files modified; AoSymmetry enum exists in cintx-core::operator with the exact derive set, repr, default variant, and Display impl; AoSymmetry is re-exported through cintx-core::lib and cintx-rs::prelude; ExecutionOptions has the new aosym field defaulting to None; cintx-core / cintx-runtime / cintx-rs all build clean; no Cargo.toml drift.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Expand manifest lock — add plain int3c2e_cart / int3c2e_sph operator-kind rows + resolver misc_wrapper_macro arm</name>
  <files>
    crates/cintx-ops/generated/compiled_manifest.lock.json,
    crates/cintx-ops/src/resolver.rs
  </files>
  <read_first>
    - crates/cintx-ops/generated/compiled_manifest.lock.json (read offset 270 limit 100 — the existing int3c1e_cart / int3c1e_sph rows at lines 275-306 are the JSON shape to mirror; identify entries[20] = int3c2e_ip1_spinor end-line (354) and entries[21] = int4c1e_cart start-line (355))
    - crates/cintx-ops/build.rs (read lines 1-200 — confirm `canonical_path = Path::new("generated/compiled_manifest.lock.json")` at line 16 is the read site and `fs::write(Path::new("src/generated/api_manifest.rs"), ...)` is the regeneration target at lines 189-190; verify the build.rs loop preserves field/ordering for both target files)
    - crates/cintx-ops/src/resolver.rs (read lines 290-420 in full — misc_wrapper_macro function at 314-322, base_symbol_from_operator at 301-312, the legacy_wrapper_manifest_matches_misc test at 390-417 are all in the same module; verify the panic site at line 401 is the only failure path for unknown base symbols)
    - crates/cintx-ops/src/generated/api_manifest.rs (read offset 2300 limit 30 — confirm the current end of the OPERATOR_DESCRIPTORS table shows int4c1e_cart and int4c1e_sph at OperatorId 22 and 23 respectively, so the shift to 24 and 25 after this task is observable)
    - .planning/phases/18-sessionrequest-arity-ge3-dispatch/18-PATTERNS.md §Manifest-Row Additions §Steps 1-4 (full edit-by-edit pattern map including the `"int3c2e" => None` arm rationale + early-continue test edit)
    - .planning/phases/18-sessionrequest-arity-ge3-dispatch/18-CONTEXT.md D-06 (the locked 12-symbol set including the two new plain int3c2e operators)
    - .planning/phases/18-sessionrequest-arity-ge3-dispatch/18-RESEARCH.md §R1 (research finding that triggered the user's "add manifest rows" decision; references manifest rows + cint3c2e_* legacy absence)
  </read_first>
  <action>
    Two edits. The first edits the canonical lock file; the second updates the resolver test helper so the regenerated manifest still passes its self-consistency test.

    **Edit A — `crates/cintx-ops/generated/compiled_manifest.lock.json`:** Insert two new entries between the existing `int3c2e_ip1_spinor` entry (which ends near line 354) and the `int4c1e_cart` entry (which begins near line 355). The insertion preserves family ordering (3c2e family before 4c1e family) and forces `int4c1e_*` to shift +2 in OperatorId space (22→24, 23→25). DO NOT preserve the old `int4c1e_*` IDs by inserting elsewhere (per PATTERNS.md §Step 2 anti-pattern: lock-file ordering must follow family grouping for manifest_audit consistency).

    Insert these two entries verbatim (preserve indentation matching the existing JSON formatter — two-space indent inside the `entries` array; trailing comma between blocks):

    ```json
    {
      "id": {
        "family": "3c2e",
        "operator": "electron-repulsion",
        "representation": "cart",
        "symbol": "int3c2e_cart"
      },
      "oracle_covered": true,
      "profiles": [
        "base",
        "with-f12",
        "with-4c1e",
        "with-f12+with-4c1e"
      ],
      "stability": "stable"
    },
    {
      "id": {
        "family": "3c2e",
        "operator": "electron-repulsion",
        "representation": "sph",
        "symbol": "int3c2e_sph"
      },
      "oracle_covered": true,
      "profiles": [
        "base",
        "with-f12",
        "with-4c1e",
        "with-f12+with-4c1e"
      ],
      "stability": "stable"
    },
    ```

    Per PATTERNS.md §Step 1 pattern-key mapping:
    - `family: "3c2e"` — routes through `kernels::resolve_family_name("3c2e") → launch_center_3c2e` (existing kernel, no new code).
    - `operator: "electron-repulsion"` — same operator_name as `int3c2e_ip1_*` siblings; resolver iteration order keeps the existing `int3c2e_ip1_cart` (OperatorId 19) as the first match for `Resolver::resolve("3c2e", "electron-repulsion", Cart)`. The new plain `int3c2e_cart` (OperatorId 22) is reachable only via `OperatorId::new(22)` through `Resolver::descriptor(...)`.
    - `stability: "stable"`, `profiles: [base|with-f12|with-4c1e|with-f12+with-4c1e]` — matches `int3c1e_{cart,sph}` (no feature flag, stable in every profile).
    - `oracle_covered: true` — these symbols will be Phase 18 parity-gated in Plan 03 (so the manifest-audit `oracle_covered` gate is satisfied on land).

    DO NOT touch the entries themselves before/after — only insert the two new blocks. The existing `int3c1e_cart`/`int3c1e_sph` entries at lines 275-306 are the format reference, not edits.

    After the JSON edit, run `cargo build -p cintx-ops --locked` once to trigger the build.rs regeneration of `src/generated/api_manifest.rs` and `src/generated/api_manifest.csv`. The generator writes the entries to both files automatically — DO NOT hand-edit either file. Verify the auto-regeneration via the post-build grep in `<verify>` below.

    **Edit B — `crates/cintx-ops/src/resolver.rs`:** Two changes to make the existing `legacy_wrapper_manifest_matches_misc` test (at lines 390-417) stay green after the new base symbol `int3c2e` becomes visible.

    **Edit B.1:** In the `misc_wrapper_macro` function at lines 314-322, add an explicit `"int3c2e" => None` arm BEFORE the catch-all `_ => None` — this makes the intent obvious to future readers and the test edit below tolerable. After:

    ```rust
    fn misc_wrapper_macro(base_symbol: &str) -> Option<MiscWrapperMacro> {
        match base_symbol {
            "int1e_ovlp" | "int1e_nuc" | "int2e" | "int2c2e" | "int3c1e" | "int3c1e_p2" | "int3c2e_ip1" => {
                Some(MiscWrapperMacro::AllCint)
            }
            "int1e_kin" => Some(MiscWrapperMacro::AllCint1e),
            // int3c2e (plain) has no misc.h wrapper in libcint 6.1.3 upstream — distinct from int3c2e_ip1.
            // Returning None here keeps `legacy_wrapper_manifest_matches_misc` test green without
            // requiring synthetic legacy entries (per Phase 18 PATTERNS.md §Step 3 Option A).
            "int3c2e" => None,
            _ => None,
        }
    }
    ```

    **Edit B.2:** In the `legacy_wrapper_manifest_matches_misc` test body at lines 400-405, change the `.unwrap_or_else(panic!)` into an early-`continue`:

    Before:
    ```rust
    for base_symbol in base_symbols {
        let macro_kind = misc_wrapper_macro(&base_symbol).unwrap_or_else(|| {
            panic!("missing misc.h wrapper macro classification for {base_symbol}")
        });
        expected.extend(expected_legacy_wrapper_symbols(&base_symbol, macro_kind));
    }
    ```
    After:
    ```rust
    for base_symbol in base_symbols {
        // Base symbols without misc.h wrappers (e.g. int3c2e plain in libcint 6.1.3)
        // do not contribute legacy entries — skip rather than panic.
        let Some(macro_kind) = misc_wrapper_macro(&base_symbol) else {
            continue;
        };
        expected.extend(expected_legacy_wrapper_symbols(&base_symbol, macro_kind));
    }
    ```

    Rationale (PATTERNS.md §Step 3): no `cint3c2e_*` legacy wrappers exist in upstream libcint 6.1.3 `src/misc.h`. Adding synthetic `cint3c2e_*` rows would make the resolver test pass but would FAIL build-time linkage to non-existent C symbols downstream (compat raw path). Option A — exempt `int3c2e` from the misc.h coverage check — is the minimal, correct fix.

    DO NOT add synthetic `cint3c2e_*` legacy rows to the lock file (PATTERNS.md §Step 3 Option B explicitly rejected).
  </action>
  <verify>
    <automated>cargo build -p cintx-ops --locked &amp;&amp; cargo test -p cintx-ops --locked &amp;&amp; grep -Fq '"symbol": "int3c2e_cart"' crates/cintx-ops/generated/compiled_manifest.lock.json &amp;&amp; grep -Fq '"symbol": "int3c2e_sph"' crates/cintx-ops/generated/compiled_manifest.lock.json &amp;&amp; grep -Fq 'symbol_name: "int3c2e_cart"' crates/cintx-ops/src/generated/api_manifest.rs &amp;&amp; grep -Fq 'symbol_name: "int3c2e_sph"' crates/cintx-ops/src/generated/api_manifest.rs &amp;&amp; grep -Fq 'int3c2e_cart' crates/cintx-ops/src/generated/api_manifest.csv &amp;&amp; grep -Fq 'int3c2e_sph' crates/cintx-ops/src/generated/api_manifest.csv &amp;&amp; grep -Fq '"int3c2e" => None' crates/cintx-ops/src/resolver.rs &amp;&amp; ! grep -F 'panic!("missing misc.h wrapper macro classification' crates/cintx-ops/src/resolver.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p cintx-ops --locked` exits 0 (build.rs regeneration completes without error).
    - `cargo test -p cintx-ops --locked` exits 0 — `legacy_wrapper_manifest_matches_misc` and all other resolver tests pass with the new arm + early-continue.
    - `grep -Fq '"symbol": "int3c2e_cart"' crates/cintx-ops/generated/compiled_manifest.lock.json` matches.
    - `grep -Fq '"symbol": "int3c2e_sph"' crates/cintx-ops/generated/compiled_manifest.lock.json` matches.
    - `grep -c '"family": "3c2e"' crates/cintx-ops/generated/compiled_manifest.lock.json` is at least 5 (existed 3 — ip1_cart, ip1_sph, ip1_spinor; +2 new for plain cart/sph).
    - `grep -Fq 'symbol_name: "int3c2e_cart"' crates/cintx-ops/src/generated/api_manifest.rs` matches (regeneration succeeded).
    - `grep -Fq 'symbol_name: "int3c2e_sph"' crates/cintx-ops/src/generated/api_manifest.rs` matches.
    - `grep -Fq 'int3c2e_cart' crates/cintx-ops/src/generated/api_manifest.csv` matches.
    - `grep -Fq 'int3c2e_sph' crates/cintx-ops/src/generated/api_manifest.csv` matches.
    - Post-shift OperatorId verification — `awk '/OperatorId::new\(24\)/,/symbol_name/' crates/cintx-ops/src/generated/api_manifest.rs | grep -q '"int4c1e_cart"'` matches (int4c1e_cart now at 24).
    - Post-shift OperatorId verification — `awk '/OperatorId::new\(25\)/,/symbol_name/' crates/cintx-ops/src/generated/api_manifest.rs | grep -q '"int4c1e_sph"'` matches (int4c1e_sph now at 25).
    - `grep -Fq '"int3c2e" => None' crates/cintx-ops/src/resolver.rs` matches (explicit arm present per PATTERNS.md §Step 3 Option A).
    - `! grep -F 'panic!("missing misc.h wrapper macro classification' crates/cintx-ops/src/resolver.rs` (the old panic is replaced by `continue`).
    - `grep -F 'let Some(macro_kind) = misc_wrapper_macro' crates/cintx-ops/src/resolver.rs` matches (early-continue pattern present).
    - Anti-pattern check — `! grep -F 'cint3c2e_cart' crates/cintx-ops/generated/compiled_manifest.lock.json` (no synthetic `cint3c2e_*` legacy entries added per PATTERNS.md §Step 3 Option B rejection).
    - Anti-hand-edit guard — the regenerated files contain the new rows AND the diff matches the mechanical regeneration of the lock-file insertion: `git diff HEAD -- crates/cintx-ops/src/generated/api_manifest.csv crates/cintx-ops/src/generated/api_manifest.rs | grep -q '^[+-]'` succeeds (diff has insertions/deletions matching the two new operator rows, deterministically derivable from the new lock-file rows; no extraneous hand edits).
  </acceptance_criteria>
  <done>
    Lock file has two new entries; build.rs regenerates api_manifest.rs and api_manifest.csv to include them; int4c1e_cart shifts to OperatorId 24 and int4c1e_sph shifts to OperatorId 25 (verified via grep on generated files); resolver test passes with the new "int3c2e" => None arm + early-continue; no synthetic legacy rows added.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add two vendor FFI wrappers (vendor_int3c1e_p2_sph, vendor_int3c2e_cart)</name>
  <files>
    crates/cintx-oracle/src/vendor_ffi.rs,
    crates/cintx-oracle/build.rs
  </files>
  <read_first>
    - crates/cintx-oracle/src/vendor_ffi.rs (read in full — observe the existing wrappers `vendor_int3c1e_p2_cart` at lines 465-490, `vendor_int3c2e_ip1_cart` at lines 493-518, `vendor_int3c1e_sph` at line 173, `vendor_int3c2e_sph` at line 204; verify the module-level `#[cfg(has_vendor_libcint)]` block governs the file or per-item annotation is needed)
    - crates/cintx-oracle/build.rs (read in full — confirm the supplemental header at line 227 declares `int3c1e_p2_sph` and verify that `int3c2e_cart` is either in `cint_funcs.h` already or needs a supplemental declaration; RESEARCH §A3 confirmed int3c1e_p2_sph is in the supplemental whitelist)
    - .planning/phases/18-sessionrequest-arity-ge3-dispatch/18-PATTERNS.md §vendor_ffi.rs (full body of the new wrapper functions — copy verbatim; ignore the `vendor_int3c2e_ip1_sph` block since it is not added in Phase 18)
    - .planning/phases/18-sessionrequest-arity-ge3-dispatch/18-RESEARCH.md §R4 (the R4 sph wrappers; only `vendor_int3c1e_p2_sph` is added here — `vendor_int3c2e_ip1_sph` is NOT added because the int3c2e_ip1_* parity reference is `vendor_int3c2e_{cart,sph}` plain per RESEARCH.md A6 / Item 5) and §R1 (the R1 wrapper — vendor_int3c2e_cart)
  </read_first>
  <action>
    Two new wrappers in `crates/cintx-oracle/src/vendor_ffi.rs` plus an optional supplemental header check in `build.rs`. Each wrapper is ~22 lines, mirroring the existing arity-3 wrapper shape.

    **Why only two wrappers (not three):** RESEARCH.md Item 5 + A6 verified (via `center_3c2e_parity.rs:222-287`) that cintx's `int3c2e_ip1_*` kernel currently computes the plain 3c2e integral, NOT an IP1 derivative. The manifest entries have `component_rank: ""` → multiplier 1 (planner.rs:382-384), so output buffer size is `ni*nj*nk`. The parity reference for `int3c2e_ip1_{cart,sph}` is therefore `vendor_int3c2e_{cart,sph}` (plain), and the pre-existing `vendor_int3c2e_sph` at vendor_ffi.rs:204 is reused. Only `vendor_int3c2e_cart` needs adding (the cart sibling). `vendor_int3c2e_ip1_sph` is intentionally NOT created in Phase 18.

    **Edit A — Pre-flight verification grep (record output in plan summary):**

    ```bash
    grep -n "vendor_int3c1e_p2_sph\|vendor_int3c2e_cart\b" crates/cintx-oracle/src/vendor_ffi.rs
    grep -n "ffi::int3c1e_p2_sph\|ffi::int3c2e_cart\b" crates/cintx-oracle/src/vendor_ffi.rs
    grep -n "int3c1e_p2_sph\|int3c2e_cart" crates/cintx-oracle/build.rs
    ```

    Expected: the two `vendor_*` symbols are ABSENT from `vendor_ffi.rs` (the wrappers do not exist yet). The two `ffi::int3c*` symbols MAY or MAY NOT exist depending on whether the supplemental header in build.rs already declares them. If `ffi::int3c2e_cart` is missing from the bindings, Edit C adds it to the build.rs supplemental whitelist.

    **Edit B — Add two new wrapper functions to `crates/cintx-oracle/src/vendor_ffi.rs`.** Place each wrapper next to its closest existing analog for readability (per PATTERNS.md §vendor_ffi.rs):

    1. Insert `vendor_int3c1e_p2_sph` AFTER `vendor_int3c1e_p2_cart` (currently lines 465-490 — place the new sph wrapper immediately after, at approximately the same indentation):

    ```rust
    /// Evaluate int3c1e_p2_sph for a single shell triple using vendored libcint.
    ///
    /// `shls` is `[i, j, k]` — three shell indices (3-center 1-electron integral, p2 variant, spherical).
    pub fn vendor_int3c1e_p2_sph(
        out: &mut [f64],
        shls: &[i32; 3],
        atm: &[i32],
        natm: i32,
        bas: &[i32],
        nbas: i32,
        env: &[f64],
    ) -> i32 {
        unsafe {
            ffi::int3c1e_p2_sph(
                out.as_mut_ptr(),
                ptr::null_mut(),
                shls.as_ptr() as *mut i32,
                atm.as_ptr() as *mut i32,
                natm,
                bas.as_ptr() as *mut i32,
                nbas,
                env.as_ptr() as *mut f64,
                ptr::null_mut(),
                ptr::null_mut(),
            )
        }
    }
    ```

    2. Insert `vendor_int3c2e_cart` AFTER the existing `vendor_int3c2e_sph` (at line 198+). This is the R1 promotion — the plain 3c2e cart variant. Mirror the shape of `vendor_int3c2e_sph` lines 198-227 but change every `sph` to `cart`:

    ```rust
    /// Evaluate int3c2e_cart for a single shell triple using vendored libcint.
    ///
    /// `shls` is `[i, j, k]` — three shell indices (3-center 2-electron integral, Cartesian).
    pub fn vendor_int3c2e_cart(
        out: &mut [f64],
        shls: &[i32; 3],
        atm: &[i32],
        natm: i32,
        bas: &[i32],
        nbas: i32,
        env: &[f64],
    ) -> i32 {
        unsafe {
            ffi::int3c2e_cart(
                out.as_mut_ptr(),
                ptr::null_mut(),
                shls.as_ptr() as *mut i32,
                atm.as_ptr() as *mut i32,
                natm,
                bas.as_ptr() as *mut i32,
                nbas,
                env.as_ptr() as *mut f64,
                ptr::null_mut(),
                ptr::null_mut(),
            )
        }
    }
    ```

    Both wrappers must be reachable through the same `cfg(has_vendor_libcint)` guard as the surrounding file. The existing file uses either a top-level cfg or per-item annotation — match whichever convention the file currently follows (check by reading the file). If per-item annotation is needed, prepend `#[cfg(has_vendor_libcint)]` to each new wrapper.

    DO NOT add `vendor_int3c2e_ip1_sph` in Phase 18 — Plan 18-03's `test_int3c2e_ip1_sph_safe_api_parity` uses the existing `vendor_int3c2e_sph` (plain) as the parity reference per RESEARCH.md A6.

    **Edit C — Verify FFI bindings are available; add supplemental declarations if needed.** After Edit B, run `cargo build -p cintx-oracle --features cpu --locked --tests`. If the build fails on `unresolved import: ffi::int3c2e_cart` (or `ffi::int3c1e_p2_sph`), add the missing function declaration to the supplemental whitelist in `crates/cintx-oracle/build.rs` near line 220-260 (the existing `int3c1e_p2_sph` whitelist site at line 227). The supplemental header pattern is:

    ```
    // In crates/cintx-oracle/build.rs near line 220-260 (existing supplemental block):
    // Add to the bindgen allowlist or supplemental header lines as appropriate.
    ```

    Per RESEARCH §A3: `int3c1e_p2_sph` is already in the supplemental header (build.rs:227). `int3c2e_cart` is a stable cart variant and should be in upstream `cint_funcs.h` already (auto-bound via bindgen at full file scan). If it is missing, replicate the existing supplemental pattern.

    DO NOT add a `vendor_int3c1e_p2_spinor` or any spinor 3-center wrapper — out of Phase 18 scope per CONTEXT.md D-07.
  </action>
  <verify>
    <automated>cargo build -p cintx-oracle --features cpu --locked --tests &amp;&amp; grep -q 'pub fn vendor_int3c1e_p2_sph' crates/cintx-oracle/src/vendor_ffi.rs &amp;&amp; grep -q 'pub fn vendor_int3c2e_cart' crates/cintx-oracle/src/vendor_ffi.rs &amp;&amp; grep -q 'ffi::int3c1e_p2_sph' crates/cintx-oracle/src/vendor_ffi.rs &amp;&amp; grep -Eq 'ffi::int3c2e_cart\b' crates/cintx-oracle/src/vendor_ffi.rs &amp;&amp; ! grep -q 'pub fn vendor_int3c2e_ip1_sph' crates/cintx-oracle/src/vendor_ffi.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p cintx-oracle --features cpu --locked --tests` exits 0 (the new wrappers compile against the bindgen-emitted ffi module).
    - `grep -q 'pub fn vendor_int3c1e_p2_sph' crates/cintx-oracle/src/vendor_ffi.rs` matches.
    - `grep -q 'pub fn vendor_int3c2e_cart' crates/cintx-oracle/src/vendor_ffi.rs` matches.
    - `grep -Eq 'ffi::int3c2e_cart\b' crates/cintx-oracle/src/vendor_ffi.rs` matches (the FFI call uses the bindgen-generated symbol, distinct from `ffi::int3c2e_sph` which already existed).
    - Each new wrapper has the same signature shape as `vendor_int3c1e_p2_cart`: 7 args (`out, shls: &[i32; 3], atm, natm, bas, nbas, env`), returns `i32`, contains exactly two `ptr::null_mut()` for `opt` and `cache` (lines 6 and 9 inside `unsafe { ffi::... }`).
    - Verify the wrapper-count grep — `grep -cE '^pub fn vendor_int(3c1e|3c2e)' crates/cintx-oracle/src/vendor_ffi.rs` increments by exactly 2 vs the pre-task baseline.
    - Anti-pattern check (scope discipline) — `! grep -q 'pub fn vendor_int3c2e_ip1_sph' crates/cintx-oracle/src/vendor_ffi.rs` (NOT added in Phase 18 per RESEARCH.md A6 / Item 5; the int3c2e_ip1 parity reference is vendor_int3c2e_* plain).
    - Anti-pattern check — `! grep -q 'pub fn vendor_int3c1e_p2_spinor\|pub fn vendor_int3c2e_ip1_spinor' crates/cintx-oracle/src/vendor_ffi.rs` (no spinor 3-center wrappers added — out of Phase 18 scope per CONTEXT.md D-07).
    - If supplemental header changes were needed: `grep -F 'int3c2e_cart' crates/cintx-oracle/build.rs` matches (binding declared). If no change was needed: build.rs unchanged is acceptable; verify by `git diff -- crates/cintx-oracle/build.rs` showing either zero output or only an additive entry.
    - On a host without `CINTX_ORACLE_BUILD_VENDOR=1` (the default in this dev env): `cargo build -p cintx-oracle --locked` still exits 0 (the new wrappers are correctly cfg-gated by `has_vendor_libcint` so they compile out when the vendor build is absent).
  </acceptance_criteria>
  <done>
    Two new vendor FFI wrappers exist in `crates/cintx-oracle/src/vendor_ffi.rs`, each mirroring the closest analog wrapper shape; `cargo build -p cintx-oracle --features cpu --locked --tests` exits 0 with vendor build available and `cargo build -p cintx-oracle --locked` exits 0 without it; `vendor_int3c2e_ip1_sph` is NOT added (Phase 18 reuses `vendor_int3c2e_sph` plain as the int3c2e_ip1_sph parity reference); no spinor wrappers added.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Plan 18-01 modifies seven crates' source files and one canonical manifest lock file. All edits target in-process behavior:

- **Manifest lock + resolver:** in-process operator catalog routing — no new IPC, no network, no untrusted input, no new file I/O at runtime (the lock is a build-time artifact compiled into the binary via `build.rs`).
- **AoSymmetry + ExecutionOptions field + re-exports:** pure type-level additions in `cintx-core::operator`, `cintx-runtime::options`, `cintx-rs::prelude`. No serialization, no network, no auth/identity, no privilege change.
- **Vendor FFI wrappers:** new Rust→C FFI calls to vendored libcint 6.1.3 functions that are already linked into the oracle test harness. The FFI boundary is build-time-only (gated by `CINTX_ORACLE_BUILD_VENDOR=1` and `has_vendor_libcint` cfg) and is exercised by every existing oracle test — Phase 18 reuses the established pattern verbatim.

No new threat vectors introduced. Phase 18 touches in-process integral evaluation only; no untrusted input enters the FFI calls (shell tuples are validated via `ShellTuple::try_from_iter` upstream of any wrapper call).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01-S | Spoofing | (none) | accept | No authentication or identity in this plan — domain primitives and FFI wrappers only. |
| T-18-01-T | Tampering | compiled_manifest.lock.json | accept | The lock file is part of the source tree, signed by git history; `cargo --locked` enforces deterministic resolution per CLAUDE.md. The two new entries follow the existing JSON schema verbatim and are validated by `cargo test -p cintx-ops --locked` post-edit. |
| T-18-01-R | Repudiation | (none) | accept | Existing git audit trail covers all edits; no log/audit code added. |
| T-18-01-I | Information Disclosure | vendor FFI output buffer | accept | Output buffers carry pure numeric integrals over publicly-known basis sets (H2O/STO-3G in the parity tests). No PII, secrets, or proprietary data flow through the wrappers. Identical to the existing 14+ vendor wrappers shipped since Phase 4. |
| T-18-01-D | Denial of Service | vendor FFI 3-shell evaluations | accept | Same per-call resource envelope as the existing `vendor_int3c1e_p2_cart`/`vendor_int3c2e_ip1_cart` wrappers exercised by `center_3c1e_parity.rs`/`center_3c2e_parity.rs` today. No new resource-exhaustion vector — wrappers receive caller-provided buffers and only call into vendored C code that has been stable across upstream libcint 6.1.x. |
| T-18-01-E | Elevation of Privilege | (none) | accept | No privilege boundary crossed. The new FFI wrappers run at the same privilege as existing oracle tests. The `AoSymmetry` enum is data, not capability. |

**ASVS L1 applicability:** N/A — no authentication, session management, access control, or input-validation surface introduced by this plan (per phase 18 security gate in planning context: "no new threat vectors — phase touches in-process integral evaluation only").
</threat_model>

<verification>
- `cargo build --workspace --locked` exits 0 (all crates compile with the new manifest rows + AoSymmetry enum + ExecutionOptions field + vendor wrappers).
- `cargo test -p cintx-core --locked` exits 0.
- `cargo test -p cintx-ops --locked` exits 0 — `legacy_wrapper_manifest_matches_misc` passes with the new arm + early-continue.
- `cargo test -p cintx-runtime --locked` exits 0.
- `cargo build -p cintx-oracle --features cpu --locked --tests` exits 0 (vendor wrappers compile when vendor build is active).
- `cargo build -p cintx-oracle --locked` exits 0 (gracefully cfg-gates out wrappers without vendor build).
- `grep -F '"symbol": "int3c2e_cart"' crates/cintx-ops/generated/compiled_manifest.lock.json` matches.
- `grep -F 'symbol_name: "int3c2e_cart"' crates/cintx-ops/src/generated/api_manifest.rs` matches (build.rs regenerated the Rust manifest).
- `grep -F 'pub enum AoSymmetry' crates/cintx-core/src/operator.rs` matches.
- `grep -F 'pub aosym: Option<cintx_core::AoSymmetry>' crates/cintx-runtime/src/options.rs` matches.
- `grep -F 'pub use cintx_core::AoSymmetry;' crates/cintx-rs/src/prelude.rs` matches.
- `grep -F 'pub fn vendor_int3c1e_p2_sph' crates/cintx-oracle/src/vendor_ffi.rs` matches.
- `grep -F 'pub fn vendor_int3c2e_cart' crates/cintx-oracle/src/vendor_ffi.rs` matches.
- `! grep -F 'pub fn vendor_int3c2e_ip1_sph' crates/cintx-oracle/src/vendor_ffi.rs` (NOT added — Phase 18 reuses `vendor_int3c2e_sph` plain as the int3c2e_ip1_sph parity reference per RESEARCH.md A6 / Item 5).
- `grep -F '"int3c2e" => None' crates/cintx-ops/src/resolver.rs` matches.
- `! grep -F 'panic!("missing misc.h wrapper macro classification' crates/cintx-ops/src/resolver.rs` (panic site replaced by `continue`).
- Anti-pattern: `! grep -F '"symbol": "cint3c2e_cart"' crates/cintx-ops/generated/compiled_manifest.lock.json` — no synthetic legacy entries added per PATTERNS.md §Step 3 Option B rejection.
- OperatorId shift verified: `awk '/OperatorId::new\(24\)/,/symbol_name/' crates/cintx-ops/src/generated/api_manifest.rs | grep -q '"int4c1e_cart"'` matches (`int4c1e_cart` now at OperatorId 24).
</verification>

<success_criteria>
- Eight files modified across 5 crates (cintx-core, cintx-runtime, cintx-rs, cintx-ops, cintx-oracle). Two regenerated files (`api_manifest.rs`, `api_manifest.csv`) automatically rebuilt by `build.rs`.
- `AoSymmetry` enum exists with the exact derive set, repr, default variant, and Display impl spec'd in CONTEXT.md D-03 + PATTERNS.md.
- `AoSymmetry` is re-exported through `cintx-core::lib` and `cintx-rs::prelude`.
- `ExecutionOptions::aosym: Option<cintx_core::AoSymmetry>` is added; default is `None` via the existing `#[derive(Default)]`.
- `compiled_manifest.lock.json` contains the two new arity-3 `int3c2e_{cart,sph}` operator-kind entries with stability `stable`, all 4 profiles, `oracle_covered: true`.
- `int4c1e_cart` and `int4c1e_sph` OperatorIds shift to 24 and 25 respectively (visible in the regenerated `api_manifest.rs`).
- Two new vendor FFI wrappers (`vendor_int3c1e_p2_sph`, `vendor_int3c2e_cart`) exist in `cintx-oracle/src/vendor_ffi.rs`. NO `vendor_int3c2e_ip1_sph` is added (Phase 18 reuses `vendor_int3c2e_sph` plain as the parity reference per RESEARCH.md A6 / Item 5).
- Resolver `misc_wrapper_macro` has explicit `"int3c2e" => None` arm; `legacy_wrapper_manifest_matches_misc` test uses early-continue instead of panic.
- `cargo build --workspace --locked` and `cargo test -p cintx-{core,runtime,ops} --locked` all exit 0.
- No Cargo.toml drift; no synthetic legacy entries; no spinor wrappers added.
</success_criteria>

<output>
After completion, create `.planning/phases/18-sessionrequest-arity-ge3-dispatch/18-01-SUMMARY.md` per the standard summary template. Specifically record:

1. **Pre-flight grep output from Task 3 Edit A** — the absence of `vendor_int3c1e_p2_sph` and `vendor_int3c2e_cart` from `vendor_ffi.rs` before the edit; confirm `vendor_int3c2e_sph` (plain) already exists at vendor_ffi.rs:204 and is reused for the `int3c2e_ip1_sph` parity reference.

2. **Final post-shift OperatorId table** — the exact post-Plan-18-01 OperatorIds for the four key symbols, derived from the regenerated `crates/cintx-ops/src/generated/api_manifest.rs`. Verify and record the actual values (expected: `int3c2e_cart=22, int3c2e_sph=23, int4c1e_cart=24, int4c1e_sph=25`; record the grep output that proves each value). This table lets Plan 18-02's executor deterministically cross-reference the `INT4C1E_CART_OPERATOR_ID: u32 = 24` constant value and lets Plan 18-03's executor confirm the `OperatorId::new(22)` / `OperatorId::new(23)` routings.

3. **Confirmation that `vendor_int3c2e_ip1_sph` was NOT added** — record the negative grep result so Plan 18-03 doesn't try to call a wrapper that doesn't exist.
</output>
</content>
