---
phase: 24-group-3-position-multipole-moment-integrals
plan: 04
type: execute
wave: 3
depends_on: [24-01, 24-02, 24-03]
files_modified:
  - crates/cintx-ops/generated/compiled_manifest.lock.json
  - crates/cintx-compat/src/raw.rs
  - crates/cintx-cubecl/src/kernels/one_electron.rs
autonomous: true
requirements: [MOM-04]
tags: [cubecl, kernel, p4, overlap-derivative, parity]
user_setup: []

must_haves:
  truths:
    - "int1e_p4 (∇⁴, rank 1) matches vendored libcint at atol=1e-12 (cart+sph) on a non-square block"
    - "p4 raises headroom on BOTH bra and ket (ng={2,2,...}), unlike Cluster A's ket-only — internal nmax = li+lj+4 ≤ 6 on STO-3G"
    - "The Laplacian-of-Laplacian contraction s0+2s4+2s8+s40+2s44+s80 is emitted (rank 1)"
  artifacts:
    - path: "crates/cintx-ops/generated/compiled_manifest.lock.json"
      provides: "int1e_p4 (rank 1) × {cart,sph,spinor}"
      contains: "int1e_p4_cart"
    - path: "crates/cintx-compat/src/raw.rs"
      provides: "RawApiId consts INT1E_P4_*"
      contains: "INT1E_P4_CART"
    - path: "crates/cintx-cubecl/src/kernels/one_electron.rs"
      provides: "p4 overlap-derivative kernel arm (D_I² applied to D_J² block, both-side headroom)"
      contains: "is_p4"
  key_links:
    - from: "crates/cintx-cubecl/src/kernels/one_electron.rs"
      to: "overlap-derivative engine (G1E_D_I / G1E_D_J)"
      via: "∇⁴ = D_I² · D_J², no Rys"
      pattern: "is_p4"
---

<objective>
Cluster C — register and implement int1e_p4 (∇⁴): the rank-1 Laplacian-of-Laplacian, built on the overlap-derivative engine (no Rys), raising angular-momentum headroom on BOTH bra and ket (ng={2,2,...}).

Purpose: Covers MOM-04 (p4). Reuses the overlap-derivative machinery (D-04). Distinct from Cluster A because p4 raises bra AND ket headroom, not ket-only (Pitfall 4).
Output: 1 family × {cart,sph,spinor} manifest entries, RawApiId consts, the p4 overlap-derivative kernel arm.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-group-3-position-multipole-moment-integrals/24-CONTEXT.md
@.planning/phases/24-group-3-position-multipole-moment-integrals/24-RESEARCH.md
@.planning/phases/24-group-3-position-multipole-moment-integrals/24-PATTERNS.md
@.planning/phases/24-group-3-position-multipole-moment-integrals/24-01-SUMMARY.md

<interfaces>
component_rank: p4 = "1" (D-08). ng = {2,2,0,0,4,1,1,1} — BRA and KET each +2 (Pitfall 4, RESEARCH table).
Manifest template: iprinv cart entry (compiled_manifest.lock.json:4450-4465), set component_rank "1".
RawApiId: INT1E_P4_{CART,SPH,SPINOR} (raw.rs:122-243 pattern).
Kernel anchors (crates/cintx-cubecl/src/kernels/one_electron.rs):
  one_electron_scalar_kernel :200-247 (nmax/lj_ext sizing; for p4 raise li_ext AND lj_ext by 2)
  the overlap-derivative path (G1E_D_I / G1E_D_J nabla cascade) used by the existing gradient ops
  dispatch ladder :3764-3790; spinor reject :3805-3809
vendor wrapper (plan 01): vendor_int1e_p4_{cart,sph}. Test (plan 01): moment_nontensor_parity.rs p4 case (build_h2o_sto3g_common_orig).
</interfaces>

<gout_order>
<!-- VERBATIM from libcint-master/src/autocode/intor1.c:2433-2536 — D-07. -->
int1e_p4 (rank 1): apply G1E_D_J to build g1,g2,g3 to j_l+1, then G1E_D_I cascades g4..g15 (bra ∇²);
  rank-1 Laplacian² contraction = s0 + 2·s4 + 2·s8 + s40 + 2·s44 + s80  (intor1.c:2533,2539).
  Headroom ng={2,2,...}: BOTH bra and ket +2 (NOT ket-only — Pitfall 4).
</gout_order>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register + implement int1e_p4 (∇⁴, overlap-derivative, both-side headroom)</name>
  <files>crates/cintx-ops/generated/compiled_manifest.lock.json, crates/cintx-compat/src/raw.rs, crates/cintx-cubecl/src/kernels/one_electron.rs</files>
  <behavior>
    - int1e_p4 cart on build_h2o_sto3g_common_orig, non-square O-s×O-p block: 1 component, byte-identical to vendor_int1e_p4_cart at atol=1e-12.
    - int1e_p4 sph: byte-identical to vendor_int1e_p4_sph.
    - For an l>0 shell the both-side headroom is exercised; a ket-only-headroom mistake fails for p×* but might pass s×s — the non-square l-bearing block catches it (Pitfall 4).
    - Internal nmax = li+lj+4 ≤ 6 on STO-3G (within li+lj<=8 engine limit); if exceeded, fail-closed (UnsupportedApi), never truncate.
  </behavior>
  <read_first>
    - crates/cintx-cubecl/src/kernels/one_electron.rs:200-247 (nmax/lj_ext sizing — for p4 raise li_ext AND lj_ext by 2) and the existing gradient (G1E_D_I/G1E_D_J nabla) path used by ipovlp/ipkin; dispatch :3764-3809
    - crates/cintx-compat/src/raw.rs:122-243 (RawApiId consts)
    - crates/cintx-ops/generated/compiled_manifest.lock.json:4450-4465 (iprinv rank-1-shaped template; set component_rank "1")
    - libcint-master/src/autocode/intor1.c:2433-2536 (p4 G1E_D cascade + the s0+2s4+2s8+s40+2s44+s80 contraction at :2533,2539)
    - docs/manual/Cubecl/*.md (#[cube] rules — MANDATORY; the nabla cascade builds a larger g-tensor, watch index types)
    - .planning/phases/24-group-3-position-multipole-moment-integrals/24-RESEARCH.md (Cluster C p4 pattern; Pitfall 4)
  </read_first>
  <action>
    Step 1 (manifest): clone iprinv cart/sph/spinor entries for int1e_p4, component_rank "1", operator:"p4",
    symbol:"int1e_p4_cart"/sph/spinor. spinor oracle_covered:false; cart/sph oracle_covered:false until green.
    `cargo build -p cintx-ops`.
    Step 2 (raw.rs): add RawApiId consts INT1E_P4_{CART,SPH,SPINOR}. NO new env-slot read (p4 reads no origin).
    Step 3 (kernel): add a p4 arm on the overlap-derivative engine (no Rys). CRITICAL (Pitfall 4): raise headroom
    on BOTH bra and ket — li_ext = li+2 AND lj_ext = lj+2 (ng={2,2,...}), NOT ket-only like Cluster A. Build the
    bra Laplacian (D_I²) AND the ket Laplacian (D_J²) explicitly: apply G1E_D_J to build g1..g3 to j_l+1, then
    G1E_D_I to cascade g4..g15. Emit the rank-1 contraction s0+2·s4+2·s8+s40+2·s44+s80 (intor1.c:2533,2539) —
    copy verbatim, do NOT re-derive. Internal nmax = li+lj+4; assert ≤ engine limit (li+lj<=8); fail-closed
    (UnsupportedApi) if a corpus shell exceeds it.
    Step 4 (dispatch): add is_p4 to the :3764 ladder, drop from rejection guard. Spinor → UnsupportedApi.
    Step 5: flip oracle_covered:true for p4 cart/sph; `cargo build -p cintx-ops`. Obey ALL #[cube] authoring rules.
  </action>
  <verify>
    <automated>cargo build -p cintx-ops && cargo test -p cintx-cubecl --features cpu --lib 2>&1 | tail -5 && CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test moment_nontensor_parity -- p4 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test moment_nontensor_parity -- p4` shows `running N>0 tests`, `0 failed`; p4 cart+sph pass at atol=1e-12 on the NON-SQUARE block.
    - `grep -c '"int1e_p4_cart"' crates/cintx-ops/generated/compiled_manifest.lock.json` ≥ 1 with `"component_rank": "1"`.
    - `grep -c 'INT1E_P4_CART' crates/cintx-compat/src/raw.rs` ≥ 1.
    - `grep -c 'is_p4' crates/cintx-cubecl/src/kernels/one_electron.rs` ≥ 1 (both-side-headroom arm present).
    - `cargo test -p cintx-cubecl --features cpu --lib` passes (no regression).
  </acceptance_criteria>
  <done>int1e_p4 (rank 1) matches vendor at atol=1e-12 (cart+sph) on a non-square block; both-side headroom ng={2,2,...}; Laplacian² contraction verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell angular momentum → kernel g-tensor sizing | p4's both-side +2 headroom raises internal nmax; an undersized g-tensor would truncate or read out of bounds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04-01 | Information Disclosure (silent truncation) | p4 ket-only headroom mistake → wrong result for l>0 | mitigate | ng={2,2,...} raises BOTH bra and ket; the non-square l-bearing parity block at atol=1e-12 catches a ket-only error (Pitfall 4). component_rank "1" matches the true output. |
| T-24-04-02 | Denial of Service (out-of-bounds) | internal nmax = li+lj+4 exceeding the engine limit | mitigate | Assert nmax ≤ engine limit (li+lj<=8); fail-closed (UnsupportedApi) if a corpus shell exceeds it. On STO-3G (li,lj≤1) nmax≤6 (RESEARCH A1) — within limit. No new env-validation surface (p4 reads no origin). |
</threat_model>

<verification>
- moment_nontensor_parity p4 case `running N>0 tests`, `0 failed` at atol=1e-12 on a non-square block.
- both-side headroom ng={2,2,...} present.
- `cargo build -p cintx-ops` green.
</verification>

<success_criteria>
- MOM-04 (p4 portion) byte-identical to vendored libcint 6.1.3 at atol=1e-12 (cart+sph).
- No capi/legacy surface added (D-09).
</success_criteria>

<output>
After completion, create `.planning/phases/24-group-3-position-multipole-moment-integrals/24-04-SUMMARY.md`
</output>
