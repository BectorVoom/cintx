---
phase: 24-group-3-position-multipole-moment-integrals
plan: 02
type: execute
wave: 1
depends_on: [24-01]
files_modified:
  - crates/cintx-ops/generated/compiled_manifest.lock.json
  - crates/cintx-compat/src/raw.rs
  - crates/cintx-cubecl/src/kernels/one_electron.rs
autonomous: true
requirements: [MOM-01, MOM-02, MOM-03]
tags: [cubecl, kernel, moment, gauge-origin, manifest, parity]
user_setup: []

must_haves:
  truths:
    - "int1e_r (dipole, rank 3) matches vendored libcint at atol=1e-12 (cart+sph) on the non-zero gauge-origin fixture, result NOT transposed (verified on a non-square block)"
    - "int1e_rr (rank 9), int1e_r2 (rank 1), int1e_z (rank 1), int1e_zz (rank 1) match at atol=1e-12 (cart+sph)"
    - "int1e_rrr (rank 27), int1e_rrrr (rank 81), int1e_r4 (rank 1) match at atol=1e-12 (cart+sph), ket headroom from ng[1]"
    - "All six _origj families (r, rr, r2, r4, z, zz) match at atol=1e-12 reading the ket basis center rj (NOT the common origin)"
    - "Every family dispatches through eval_raw; angular-momentum headroom raised on the KET (ng[1]), not the bra"
  artifacts:
    - path: "crates/cintx-ops/generated/compiled_manifest.lock.json"
      provides: "manifest entries for r/rr/rrr/rrrr/r2/r4/z/zz + 6 _origj × {cart,sph,spinor} with exact component_rank"
      contains: "int1e_rrrr_cart"
    - path: "crates/cintx-compat/src/raw.rs"
      provides: "RawApiId consts for all Cluster A families; common_orig already read live (no new env code)"
      contains: "INT1E_RRRR_CART"
    - path: "crates/cintx-cubecl/src/kernels/one_electron.rs"
      provides: "parameterized moment kernel (origin-source branch + ket headroom + verbatim gout order) + launcher dispatch"
      contains: "is_origj"
  key_links:
    - from: "crates/cintx-cubecl/src/kernels/one_electron.rs"
      to: "plan.operator_env_params.common_orig"
      via: "Cluster A base-family origin = env[PTR_COMMON_ORIG]"
      pattern: "common_orig"
    - from: "crates/cintx-cubecl/src/kernels/one_electron.rs"
      to: "ket basis center rj"
      via: "_origj origin-source branch (G1E_R_J, no drj)"
      pattern: "is_origj"
    - from: "crates/cintx-compat/src/raw.rs"
      to: "compiled_manifest.lock.json symbol strings"
      via: "RawApiId::Symbol exact-match"
      pattern: "INT1E_R_CART"
---

<objective>
Cluster A — register and implement the overlap-derived position-tensor moment families through ONE parameterized #[cube] moment kernel: r, rr, rrr, rrrr, r2, r4, z, zz and their six _origj variants. The kernel branches on origin-source (common origin vs ket center) and emits components in the verbatim libcint gout order, raising headroom on the ket.

Purpose: Banks MOM-01/02/03 and walks the rank 1→81 ramp through one kernel (D-01). This is the first kernel consumer of the gauge-origin slot (D-06).
Output: ~42 manifest entries (14 families × cart/sph/spinor), RawApiId consts, the parameterized moment kernel + dispatch.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-group-3-position-multipole-moment-integrals/24-CONTEXT.md
@.planning/phases/24-group-3-position-multipole-moment-integrals/24-RESEARCH.md
@.planning/phases/24-group-3-position-multipole-moment-integrals/24-PATTERNS.md
@.planning/phases/24-group-3-position-multipole-moment-integrals/24-01-SUMMARY.md

<interfaces>
component_rank STRINGS (D-08, parse_component_multiplier multiplies numeric segments — set the literal multiplier):
  r="3"  rr="9"  rrr="27"  rrrr="81"  r2="1"  r4="1"  z="1"  zz="1"
  _origj mirrors its base. NO rrr_origj / rrrr_origj exist (OQ-3 grep-confirmed).

Manifest clone templates (crates/cintx-ops/generated/compiled_manifest.lock.json):
  iprinv cart entry :4450-4465 (rank-3 template for r / r_origj)
  ipovlpip cart entry :4051-4081 (rank-9 template for rr / rr_origj — and the high-rank shape for rrr/rrrr)
  iprinv_spinor :4499-4521 (forms:["spinor"], oracle_covered:false → UnsupportedApi, D-09)

RawApiId pattern (crates/cintx-compat/src/raw.rs:122-243, closest analog :183-185):
  pub const INT1E_R_CART: Self = Self::Symbol("int1e_r_cart"); (+ _SPH, _SPINOR; + _ORIGJ_* for the six)
  string MUST exactly match the manifest symbol.

common_orig already read live in eval_raw (crates/cintx-compat/src/raw.rs:677-686) — Cluster A needs NO new env-read code (D-06).

Kernel anchors (crates/cintx-cubecl/src/kernels/one_electron.rs):
  one_electron_scalar_kernel :200-247 (#[comptime] op_kind, nmax/lj_ext sizing — Cluster A base)
  launch_one_electron_typed :3725; dispatch ladder :3764-3790 (is_<op> flat ladder + rejection guard)
  rj already in scope :3761 — `let rj = atoms[shell_j.atom_index as usize].coord_bohr;`
  spinor reject :3805-3809 (Representation::Spinor → UnsupportedApi)
  parse_component_multiplier (crates/cintx-runtime/src/planner.rs:403-450) sizes staging to rank 81 (D-03; no manual layout)
</interfaces>

<gout_order>
<!-- VERBATIM from libcint-master/src/autocode/intor1.c — copy s[k]→gout[n*RANK+k], DO NOT re-derive (D-07). -->
int1e_r (rank 3, intor1.c:1144-1158):  s0=g1x·g0·g0=x, s1=y, s2=z. Order (x,y,z).
int1e_rr (rank 9, intor1.c:1434-1462):
  s0=g3x·g0·g0, s1=g2x·g1y·g0, s2=g2x·g0·g1z, s3=g1x·g2y·g0, s4=g0·g3y·g0,
  s5=g0·g2y·g1z, s6=g1x·g0·g2z, s7=g0·g1y·g2z, s8=g0·g0·g3z. (canonical row-major 3×3 r⊗r)
int1e_rrr (rank 27, intor1.c gout n*27 block ~:1700): copy s[0..26] VERBATIM from the source (3×3×3 nesting).
int1e_rrrr (rank 81, intor1.c:1762 gout n*81 block): copy s[0..80] VERBATIM.
int1e_r2 (rank 1, intor1.c:1220,1225): trace = s0+s4+s8 of the rr s-list.
int1e_r4 (rank 1, intor1.c:1380): s0+2·s4+2·s8+s40+2·s44+s80 of the rrrr s-list.
int1e_z (rank 1, intor1.c:1034,1039): z-axis only → emits the rr/r z component s[2] of r (g0·g0·g1z).
int1e_zz (rank 1, intor1.c:1098,1103): zz only → s[8] of rr (g0·g0·g3z).
int1e_*_origj: SAME s[k] lists, but origin = ket basis center rj (G1E_R_J, NO drj subtraction), intor1.c:2067-2092.
</gout_order>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register + implement r / z / zz (low rank) through the parameterized moment kernel with origin-source branch</name>
  <files>crates/cintx-ops/generated/compiled_manifest.lock.json, crates/cintx-compat/src/raw.rs, crates/cintx-cubecl/src/kernels/one_electron.rs</files>
  <behavior>
    - int1e_r cart on build_h2o_sto3g_common_orig (origin [0.5,-0.3,0.8]), non-square O-s × O-p block: 3 components, byte-identical to vendor_int1e_r_cart at atol=1e-12.
    - int1e_r sph: byte-identical to vendor_int1e_r_sph.
    - int1e_z / int1e_zz: rank-1, byte-identical (z = g0·g0·g1z component; zz = g0·g0·g3z).
    - int1e_r_origj / z_origj / zz_origj: origin = ket center rj (NOT common origin), byte-identical to vendor _origj symbols.
    - Transpose guard: the non-square block makes a swapped bra/ket layout fail; ket headroom is ng[1].
  </behavior>
  <read_first>
    - crates/cintx-cubecl/src/kernels/one_electron.rs:200-247 (one_electron_scalar_kernel nmax/lj_ext sizing) and :3725, :3761-3809 (launcher dispatch + rj in scope + spinor reject)
    - crates/cintx-compat/src/raw.rs:122-243 (RawApiId consts) and :677-686 (common_orig live read — confirm no new code needed)
    - crates/cintx-ops/generated/compiled_manifest.lock.json:4450-4465 (iprinv rank-3 template) and :4499-4521 (spinor template)
    - libcint-master/src/autocode/intor1.c:1144-1158 (r gout), :1034-1039 (z), :1098-1103 (zz), :2067-2092 (_origj G1E_R_J branch)
    - docs/manual/Cubecl/*.md (the #[cube] authoring rules — no plain-fn calls, no if-expr, F::exp/F::sqrt, u32/i32 indices, no continue/break) — MANDATORY before editing the kernel
    - .planning/phases/24-group-3-position-multipole-moment-integrals/24-PATTERNS.md (Cluster A kernel + origin-source branch sections)
  </read_first>
  <action>
    Step 1 (manifest): clone the iprinv cart/sph/spinor entries for int1e_r, int1e_z, int1e_zz and their _origj variants
    (9 families × 3 forms = 27 entries; r=rank "3", z/zz="1"; _origj mirrors base). Set operator/symbol exactly
    (operator:"r", symbol:"int1e_r_cart"; operator:"r_origj", symbol:"int1e_r_origj_cart"; etc.).
    spinor form: forms:["spinor"], oracle_covered:false. cart/sph: oracle_covered:false FOR NOW (flip to true in this
    task's last step once parity is green). Then `cargo build -p cintx-ops` to regenerate api_manifest.{rs,csv}.
    Step 2 (raw.rs): add RawApiId consts INT1E_R_{CART,SPH,SPINOR}, INT1E_Z_*, INT1E_ZZ_*, and the three _ORIGJ_*
    sets — Self::Symbol("int1e_<...>") matching the manifest. NO new env-read code (common_orig is read live at :677).
    Step 3 (kernel): add the parameterized moment kernel path. Per CONTEXT discretion this MAY be a comptime
    moment-order parameter on one_electron_scalar_kernel OR a new moment launcher — implementer's call. It MUST:
      - raise lj_ext (ket headroom) by the moment order (1 for r/z/zz), NOT the bra (D-07).
      - branch on origin-source: `let origin = if is_origj { rj } else { common_orig };` where rj is the ket basis
        center (:3761) and common_orig = plan.operator_env_params.common_orig.unwrap_or([0.0;3]). G1E_RCJ uses
        drj = rj - origin; G1E_R_J (origj) uses origin = rj directly (drj=0 effectively → position relative to ket center).
      - emit components in the VERBATIM gout s[k] order (r: x,y,z; z: g0·g0·g1z; zz: g0·g0·g3z — see <gout_order>).
    Step 4 (dispatch): add is_r, is_z, is_zz, is_r_origj, is_z_origj, is_zz_origj to the :3764 ladder, drop them
    from the rejection guard, route to the moment kernel. Spinor reps → UnsupportedApi (:3805).
    Step 5: flip oracle_covered:true for the r/z/zz (+_origj) cart/sph entries; rerun `cargo build -p cintx-ops`.
    Obey ALL #[cube] authoring rules from docs/manual/Cubecl/*.md.
  </action>
  <verify>
    <automated>cargo build -p cintx-ops && cargo test -p cintx-cubecl --features cpu --lib 2>&1 | tail -5 && CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test moment_r_parity 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test moment_r_parity` shows `running N>0 tests` and `0 failed`; the r/r_origj cart+sph cases pass at atol=1e-12 on the NON-SQUARE O-s×O-p block.
    - `cargo build -p cintx-ops` succeeds (manifest-audit auto-syncs from the lock).
    - `grep -c 'INT1E_R_CART\|INT1E_Z_CART\|INT1E_ZZ_ORIGJ_CART' crates/cintx-compat/src/raw.rs` ≥ 3.
    - `grep -c '"oracle_covered": true' ...` for int1e_r_cart/_sph entries (the r cart/sph entries are flipped true).
    - `grep -c 'is_origj\|is_r_origj' crates/cintx-cubecl/src/kernels/one_electron.rs` ≥ 1 (origin-source branch present).
    - `cargo test -p cintx-cubecl --features cpu --lib` passes (no regression in existing kernels).
  </acceptance_criteria>
  <done>r/z/zz + their _origj match vendor at atol=1e-12 (cart+sph) on a non-square block via the parameterized kernel; origin-source branch live; oracle_covered=true.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register + implement rr / r2 / rrr / rrrr / r4 (high-rank tensors + trace contractions)</name>
  <files>crates/cintx-ops/generated/compiled_manifest.lock.json, crates/cintx-compat/src/raw.rs, crates/cintx-cubecl/src/kernels/one_electron.rs</files>
  <behavior>
    - int1e_rr (rank 9) on the non-square block: 9 components in the exact intor1.c:1434-1462 s[0..8] order, byte-identical to vendor at atol=1e-12 (cart+sph). A transposed/permuted order FAILS on the non-square block.
    - int1e_rrr (rank 27), int1e_rrrr (rank 81): s[0..26]/s[0..80] verbatim, byte-identical; ket headroom ng[1]=3/4; rank-81 staging sized by parse_component_multiplier (D-03, no manual layout).
    - int1e_r2 (rank 1) = trace s0+s4+s8; int1e_r4 (rank 1) = s0+2s4+2s8+s40+2s44+s80. Byte-identical.
    - rr_origj / r2_origj / r4_origj: origin = ket center rj. (NO rrr_origj/rrrr_origj — they do not exist.)
  </behavior>
  <read_first>
    - crates/cintx-cubecl/src/kernels/one_electron.rs (the moment kernel added in Task 1 — extend it; and the lj_ext/nmax sizing for higher ket headroom)
    - crates/cintx-ops/generated/compiled_manifest.lock.json:4051-4081 (ipovlpip rank-9 template for rr/rrr/rrrr)
    - libcint-master/src/autocode/intor1.c:1434-1462 (rr s[0..8]), the n*27 block ~:1700 (rrr s[0..26]), :1762 n*81 block (rrrr s[0..80]), :1220-1225 (r2 trace), :1380 (r4)
    - docs/manual/Cubecl/*.md (#[cube] rules — MANDATORY; rank-81 means a large comptime-unrolled emission, watch index types u32/i32)
    - .planning/phases/24-group-3-position-multipole-moment-integrals/24-RESEARCH.md (Verbatim gout component order; component_rank table)
  </read_first>
  <action>
    Step 1 (manifest): clone entries (ipovlpip rank-9 template) for int1e_rr("9"), int1e_rrr("27"), int1e_rrrr("81"),
    int1e_r2("1"), int1e_r4("1") × {cart,sph,spinor}; plus rr_origj("9"), r2_origj("1"), r4_origj("1") × 3 forms.
    DO NOT create rrr_origj or rrrr_origj (no vendor symbol — OQ-3). `cargo build -p cintx-ops`.
    Step 2 (raw.rs): RawApiId consts INT1E_RR_*, INT1E_RRR_*, INT1E_RRRR_*, INT1E_R2_*, INT1E_R4_* + _ORIGJ_* for
    rr/r2/r4.
    Step 3 (kernel): extend the moment kernel to higher moment orders. Raise lj_ext on the KET by moment order
    (rr:+2, rrr:+3, rrrr:+4) — NOT the bra. Emit the verbatim s[k]→gout[n*RANK+k] lists copied from intor1.c
    (see <gout_order>; for rrr/rrrr copy the full s[0..26]/s[0..80] list character-for-character from the source —
    DO NOT re-derive an analytic ordering). For r2/r4, emit the single scalar trace contraction. Reuse the same
    origin-source branch from Task 1 for the _origj variants. Staging is auto-sized to rank 81 by
    parse_component_multiplier — do NOT add manual layout/scatter code (D-03). Confirm internal nmax = li+lj+4 ≤ 6
    on STO-3G (within the li+lj<=8 engine limit, RESEARCH A1) — if a corpus shell exceeds it, fail-closed
    (UnsupportedApi), do NOT silently truncate.
    Step 4 (dispatch): add is_rr, is_rrr, is_rrrr, is_r2, is_r4 (+ rr/r2/r4 _origj) to the ladder; drop from
    rejection guard. Step 5: flip oracle_covered:true for cart/sph; `cargo build -p cintx-ops`.
  </action>
  <verify>
    <automated>cargo build -p cintx-ops && CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test moment_low_parity --test moment_high_parity 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test moment_low_parity` shows `running N>0 tests`, `0 failed`; rr(9)/r2/z/zz + _origj pass at atol=1e-12 on the non-square block.
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test moment_high_parity` shows `running N>0 tests`, `0 failed`; rrr(27)/rrrr(81)/r4 pass at atol=1e-12.
    - `grep -c '"int1e_rrrr_cart"' crates/cintx-ops/generated/compiled_manifest.lock.json` ≥ 1 with `"component_rank": "81"`.
    - `grep -E 'int1e_rrr_origj|int1e_rrrr_origj' crates/cintx-ops/generated/compiled_manifest.lock.json` returns NOTHING.
    - `grep -c 'INT1E_RRRR_CART\|INT1E_R2_ORIGJ_CART' crates/cintx-compat/src/raw.rs` ≥ 2.
  </acceptance_criteria>
  <done>rr/rrr/rrrr/r2/r4 (+ rr/r2/r4 _origj) match vendor at atol=1e-12 (cart+sph); rank-81 emission verbatim; no rrr/rrrr _origj registered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller env-array → eval_raw → kernel | gauge origin env[PTR_COMMON_ORIG] (and ket coord rj) feed the moment kernel; non-finite origins must be rejected |
| planner staging sizing → device scatter | rank-81 staging must be sized correctly so no component is truncated and no out-of-bounds write occurs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-02-01 | Tampering (input validation, ASVS V5) | non-finite (NaN/inf) gauge origin in env[1..3] | mitigate | validate_common_orig_env_params (validator.rs:210-223) rejects NaN/inf BEFORE the kernel runs; eval_raw bounds-guards the env read (raw.rs:677). No NEW validation gap introduced — Cluster A is the first consumer of an already-validated slot. |
| T-24-02-02 | Information Disclosure (silent truncation) | component_rank set too low → trailing components dropped | mitigate | component_rank set to the exact multiplier per family (D-08); parse_component_multiplier sizes staging to rank 81; the per-component non-square parity test at atol=1e-12 catches any missing/permuted component. |
| T-24-02-03 | Denial of Service (memory) | rank-81 staging over-allocation / OOM | accept | Phase 24 uses existing staging as-is, sized correctly for the gate corpus (no partial writes). Full OOM-safety hardening (FND-06, BufferTooSmall upfront assertion + rank-81 OOM re-validation) is deferred to Phase 25 (D-03) — cross-linked, NOT implemented here. Gate corpus (H2O/STO-3G ≤ d) does not stress allocation. |
</threat_model>

<verification>
- moment_r_parity, moment_low_parity, moment_high_parity all `running N>0 tests`, `0 failed` under the vendor gate at atol=1e-12.
- All 14 Cluster A families registered with exact component_rank; rrr/rrrr _origj absent.
- Origin-source branch live (common origin for base, ket rj for _origj); ket headroom only (ng[1]).
- `cargo build -p cintx-ops` green (manifest-audit auto-synced).
</verification>

<success_criteria>
- MOM-01 (r/r_origj), MOM-02 (rr/r2/z/zz +_origj), MOM-03 (rrr/rrrr/r4) all byte-identical to vendored libcint 6.1.3 at atol=1e-12 (cart+sph) on the non-zero gauge-origin fixture.
- Result not transposed (verified on a non-square block).
- No capi/legacy surface added (D-09).
</success_criteria>

<output>
After completion, create `.planning/phases/24-group-3-position-multipole-moment-integrals/24-02-SUMMARY.md`
</output>
