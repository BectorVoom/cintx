---
phase: 24-group-3-position-multipole-moment-integrals
plan: 03
type: execute
wave: 2
depends_on: [24-01, 24-02]
files_modified:
  - crates/cintx-ops/generated/compiled_manifest.lock.json
  - crates/cintx-compat/src/raw.rs
  - crates/cintx-cubecl/src/kernels/one_electron.rs
autonomous: true
requirements: [MOM-04]
tags: [cubecl, kernel, rinv, rys, rinv-origin, parity]
user_setup: []

must_haves:
  truths:
    - "plain int1e_rinv (rank 1) matches vendored libcint at atol=1e-12 (cart+sph) with a NON-ZERO rinv_orig, single-center Rys, charge=+1, NO atom-sum"
    - "int1e_drinv (rank 3) matches at atol=1e-12 (cart+sph) — the gradient wrt the rinv center"
    - "rinv/drinv read env[PTR_RINV_ORIG] (env[4..6]), NOT PTR_COMMON_ORIG (D-04 correction / OQ-1)"
    - "Both fail-closed when nroots > 5 (MAX_DEVICE_NROOTS); the H2O/STO-3G corpus stays within the ceiling"
  artifacts:
    - path: "crates/cintx-ops/generated/compiled_manifest.lock.json"
      provides: "int1e_rinv (rank 1) + int1e_drinv (rank 3) × {cart,sph,spinor}"
      contains: "int1e_drinv_cart"
    - path: "crates/cintx-compat/src/raw.rs"
      provides: "RawApiId consts INT1E_RINV_*, INT1E_DRINV_*; rinv_orig populated for plain rinv/drinv symbols"
      contains: "INT1E_RINV_CART"
    - path: "crates/cintx-cubecl/src/kernels/one_electron.rs"
      provides: "single-center Rys rinv/drinv kernel arm cloned from the nuclear/iprinv path"
      contains: "is_rinv"
  key_links:
    - from: "crates/cintx-compat/src/raw.rs"
      to: "plan.operator_env_params.rinv_orig"
      via: "PTR_RINV_ORIG env[4..6] read for rinv/drinv symbols"
      pattern: "PTR_RINV_ORIG"
    - from: "crates/cintx-cubecl/src/kernels/one_electron.rs"
      to: "rys_root1..5 single-center evaluation"
      via: "charge=+1, no -Z_C, no atom-loop, fail-closed nroots>5"
      pattern: "MAX_DEVICE_NROOTS"
---

<objective>
Cluster B — register and implement plain int1e_rinv and int1e_drinv: the single-center 1/r Coulomb potential evaluated via the existing nuclear/Rys engine, reading PTR_RINV_ORIG (env[4..6]), charge=+1, NO atom-sum. drinv is the rank-3 gradient wrt the rinv center.

Purpose: Covers MOM-04 (rinv/drinv). Reuses the iprinv single-origin Rys precedent (D-04). Corrects D-04's origin slot per OQ-1 (source reads PTR_RINV_ORIG, not PTR_COMMON_ORIG).
Output: 2 families × {cart,sph,spinor} manifest entries, RawApiId consts, rinv_orig population for the new symbols, the single-center Rys kernel arm.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-group-3-position-multipole-moment-integrals/24-CONTEXT.md
@.planning/phases/24-group-3-position-multipole-moment-integrals/24-RESEARCH.md
@.planning/phases/24-group-3-position-multipole-moment-integrals/24-PATTERNS.md
@.planning/phases/24-group-3-position-multipole-moment-integrals/24-01-SUMMARY.md

<interfaces>
component_rank: rinv="1", drinv="3" (D-08). Both × {cart,sph,spinor}.
Manifest template: iprinv cart entry (crates/cintx-ops/generated/compiled_manifest.lock.json:4450-4465).

Env-slot read (crates/cintx-compat/src/raw.rs:662-673) — the rinv_orig model:
  if is_iprinv_family_symbol(plan.descriptor.operator_symbol()) {
    if env.len() >= PTR_RINV_ORIG + 3 { rinv_orig = Some([env[PTR_RINV_ORIG], +1, +2]); }
    validate_rinv_orig_env_params(...)?;
  }
  PTR_RINV_ORIG = 4 (raw.rs:58). PTR_COMMON_ORIG = 1 (raw.rs:50).

Kernel anchors (crates/cintx-cubecl/src/kernels/one_electron.rs):
  nuclear/Rys path :415-503 (atom-loop + rys_root1..5 + fac1 = 2π·(-Z_C)·fac/zeta — the clone target)
  iprinv single-origin arm :3954-3969, :4041-4043 (rinv_orig resolution; factor +1.0, no -Z_C, no atom-loop)
  MAX_DEVICE_NROOTS = 5 (:42); fail-closed guard :4005-4013
  iprinv is ∂/∂A_bra; drinv is −(∂_i+∂_j) wrt the rinv center C (translational invariance) — distinct derivative, same machinery.

vendor wrappers (from plan 01): vendor_int1e_rinv_{cart,sph}, vendor_int1e_drinv_{cart,sph}.
Test (from plan 01): moment_nontensor_parity.rs rinv/drinv cases use env_with_rinv_origin(env, [0.5,-0.3,0.8]).
</interfaces>

<gout_order>
<!-- VERBATIM from libcint-master/src/autocode/intor1.c + g1e.c — D-07. -->
int1e_rinv (rank 1, g1e.c:226-228 + intor1.c:3621-3642): single output;
  fac1 = 2π · envs->fac[0] · tau / aij   (charge=+1, NO -Z_C); cr = env + PTR_RINV_ORIG;
  s += g0[ix+i]*g0[iy+i]*g0[iz+i] summed over i in nrys_roots.
int1e_drinv (rank 3, intor1.c:3671-3702): order (x,y,z) of the derivative direction;
  g1 = D_J(g0) + D_I(g0) accumulated over roots → rank 3 (gradient wrt the rinv center C).
</gout_order>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register + implement plain int1e_rinv and int1e_drinv (single-center Rys, PTR_RINV_ORIG)</name>
  <files>crates/cintx-ops/generated/compiled_manifest.lock.json, crates/cintx-compat/src/raw.rs, crates/cintx-cubecl/src/kernels/one_electron.rs</files>
  <behavior>
    - int1e_rinv cart on a fixture with NON-ZERO rinv_orig [0.5,-0.3,0.8], non-square O-s×O-p block: 1 component, byte-identical to vendor_int1e_rinv_cart at atol=1e-12. (A zero rinv_orig would be trivially-passing — disallowed.)
    - int1e_rinv sph: byte-identical to vendor_int1e_rinv_sph.
    - int1e_drinv (rank 3, x/y/z derivative order): byte-identical to vendor_int1e_drinv_{cart,sph} at atol=1e-12.
    - With nroots ≤ 5 on the corpus both pass; if a shell pair pushed nroots > 5 the kernel returns UnsupportedApi (fail-closed), never a wrong/truncated result.
  </behavior>
  <read_first>
    - crates/cintx-cubecl/src/kernels/one_electron.rs:415-503 (nuclear/Rys atom-loop — the clone target), :3954-3969 + :4005-4013 + :4041-4043 (iprinv single-origin arm + fail-closed nroots guard)
    - crates/cintx-compat/src/raw.rs:50,58 (PTR_COMMON_ORIG/PTR_RINV_ORIG consts), :662-673 (is_iprinv_family_symbol rinv_orig read — extend it), :122-243 (RawApiId consts)
    - crates/cintx-ops/generated/compiled_manifest.lock.json:4450-4465 (iprinv template)
    - libcint-master/src/g1e.c:226-228 (CINTg1e_nuc nuc_id<0: PTR_RINV_ORIG, charge +1, fac1) and intor1.c:3621-3642 (rinv gout), :3671-3702 (drinv gout)
    - docs/manual/Cubecl/*.md (#[cube] rules — MANDATORY; Rys roots use comptime nroots, F::exp/F::sqrt)
    - .planning/phases/24-group-3-position-multipole-moment-integrals/24-PATTERNS.md (Cluster B sections)
  </read_first>
  <action>
    Step 1 (manifest): clone iprinv cart/sph/spinor entries for int1e_rinv (component_rank "1") and int1e_drinv
    (component_rank "3"), operator/symbol exact (operator:"rinv", symbol:"int1e_rinv_cart"; operator:"drinv",
    symbol:"int1e_drinv_cart"). spinor: oracle_covered:false. cart/sph: oracle_covered:false until parity green.
    `cargo build -p cintx-ops`.
    Step 2 (raw.rs): add RawApiId consts INT1E_RINV_{CART,SPH,SPINOR}, INT1E_DRINV_*. Extend the env-slot read at
    :662-673 so plain rinv/drinv symbols ALSO populate rinv_orig from env[PTR_RINV_ORIG] — either add their symbols
    to is_iprinv_family_symbol or add a sibling guard `is_rinv_family_symbol`. Run validate_rinv_orig_env_params for
    them. CRITICAL: rinv/drinv read env[PTR_RINV_ORIG] (env[4..6]), NOT PTR_COMMON_ORIG (OQ-1 / D-04 correction).
    Step 3 (kernel): add a rinv/drinv arm by cloning the nuclear/Rys path (:415-503) BUT:
      - DROP the `while atom < natm` atom-loop (single center).
      - set the center to rinv_orig (resolved like iprinv at :3958-3964; error InvalidEnvParam if absent), NOT a nucleus.
      - replace fac1 = 2π·(-Z_C)·fac/zeta with fac1 = 2π·fac·tau/aij — charge = +1, NO -Z_C, NO atom-sum (g1e.c:226-228).
      - rinv: rank 1, accumulate g0[ix+i]*g0[iy+i]*g0[iz+i] over nrys roots. drinv: add the (D_i+D_j) derivative level
        (+1 effective Rys root), emit (x,y,z) — rank 3 (gradient wrt the rinv center C, NOT ∂/∂A_bra like iprinv).
      - extend the MAX_DEVICE_NROOTS=5 fail-closed guard to is_rinv/is_drinv.
    Step 4 (dispatch): add is_rinv, is_drinv to the :3764 ladder, drop from rejection guard. Spinor → UnsupportedApi.
    Step 5: flip oracle_covered:true for rinv/drinv cart/sph; `cargo build -p cintx-ops`.
    Obey ALL #[cube] authoring rules.
  </action>
  <verify>
    <automated>cargo build -p cintx-ops && cargo test -p cintx-cubecl --features cpu --lib 2>&1 | tail -5 && CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test moment_nontensor_parity -- rinv drinv 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test moment_nontensor_parity -- rinv` shows `running N>0 tests`, `0 failed`; rinv + drinv cart+sph pass at atol=1e-12 on the NON-SQUARE block with a NON-ZERO rinv_orig.
    - `grep -c 'PTR_RINV_ORIG' crates/cintx-compat/src/raw.rs` ≥ 2 (rinv/drinv read the rinv slot, not common).
    - `grep -E 'rinv|drinv' crates/cintx-compat/src/raw.rs | grep -c 'PTR_COMMON_ORIG'` returns 0 (rinv must NOT read the common origin).
    - `grep -c '"int1e_drinv_cart"' crates/cintx-ops/generated/compiled_manifest.lock.json` ≥ 1 with `"component_rank": "3"`.
    - `grep -c 'is_rinv\|is_drinv\|MAX_DEVICE_NROOTS' crates/cintx-cubecl/src/kernels/one_electron.rs` ≥ 2 (kernel arm + fail-closed guard).
    - `cargo test -p cintx-cubecl --features cpu --lib` passes (no regression).
  </acceptance_criteria>
  <done>rinv (rank 1) + drinv (rank 3) match vendor at atol=1e-12 (cart+sph) with a non-zero PTR_RINV_ORIG center, single-center charge=+1 no atom-sum, fail-closed nroots>5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller env-array → eval_raw → rinv kernel | rinv center env[PTR_RINV_ORIG] (env[4..6]); non-finite center must be rejected |
| Rys root count → device kernel | nroots > 5 exceeds the device Rys ceiling and would index out of bounds / panic if not gated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03-01 | Tampering (input validation, ASVS V5) | non-finite (NaN/inf) rinv_orig in env[4..6] | mitigate | validate_rinv_orig_env_params (validator.rs:210-223) rejects NaN/inf before the kernel runs; eval_raw bounds-guards the read (env.len() >= PTR_RINV_ORIG+3, raw.rs:663). No NEW validation gap. |
| T-24-03-02 | Denial of Service (panic) | nroots > 5 on the device Rys path | mitigate | MAX_DEVICE_NROOTS=5 fail-closed guard extended to rinv/drinv returns UnsupportedApi instead of an out-of-bounds Rys-root access. Corpus (H2O/STO-3G ≤ d) stays ≤ ceiling. The nroots≥6 Wheeler fallback is Phase 25 (FND-02) — cross-linked, NOT implemented. |
| T-24-03-03 | Spoofing (wrong-origin trivial pass) | rinv parity at zero rinv_orig | mitigate | The parity test sets a NON-ZERO rinv center via env_with_rinv_origin([0.5,-0.3,0.8]); a zero origin is trivially-passing and disallowed (D-04 correction / OQ-1). |
</threat_model>

<verification>
- moment_nontensor_parity rinv/drinv cases `running N>0 tests`, `0 failed` at atol=1e-12 on a non-square block with non-zero rinv_orig.
- rinv/drinv read PTR_RINV_ORIG, never PTR_COMMON_ORIG.
- fail-closed nroots>5 guard covers rinv/drinv.
- `cargo build -p cintx-ops` green.
</verification>

<success_criteria>
- MOM-04 (rinv, drinv portion) byte-identical to vendored libcint 6.1.3 at atol=1e-12 (cart+sph).
- Single-center charge=+1, no atom-sum, reads the rinv origin slot.
- No capi/legacy surface added (D-09).
</success_criteria>

<output>
After completion, create `.planning/phases/24-group-3-position-multipole-moment-integrals/24-03-SUMMARY.md`
</output>
