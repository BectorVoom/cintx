---
phase: 24-group-3-position-multipole-moment-integrals
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - crates/cintx-oracle/build.rs
  - crates/cintx-oracle/src/vendor_ffi.rs
  - crates/cintx-oracle/tests/moment_r_parity.rs
  - crates/cintx-oracle/tests/moment_low_parity.rs
  - crates/cintx-oracle/tests/moment_high_parity.rs
  - crates/cintx-oracle/tests/moment_nontensor_parity.rs
  - crates/cintx-oracle/tests/moment_common.rs
autonomous: true
requirements: [MOM-01, MOM-02, MOM-03, MOM-04]
tags: [oracle, vendor-ffi, parity, libcint, moment]
user_setup: []

must_haves:
  truths:
    - "Vendor libcint symbols int1e_{r,rr,rrr,rrrr,r2,r4,z,zz,p4,irp,rinv,drinv}_{cart,sph} + the six _origj symbols are reachable through bindgen and safe wrappers"
    - "Four moment parity test files compile and (without the vendor gate) skip cleanly; under the vendor gate they FAIL with 'no kernel' until the kernel plans land (RED state)"
    - "A non-zero rinv_orig env setter helper exists for the rinv/drinv tests"
    - "The pre-existing oracle-cart-offset-vendor-zero lib-unit failure is triaged and either fixed or converted to a tracked standalone bug so it does not block the Phase 24 gate"
  artifacts:
    - path: "crates/cintx-oracle/build.rs"
      provides: "bindgen allowlist_function regex extended with all 26 base symbols + 12 _origj symbols"
      contains: "int1e_drinv_cart"
    - path: "crates/cintx-oracle/src/vendor_ffi.rs"
      provides: "safe vendor_int1e_*_{cart,sph} wrappers for every new symbol"
      contains: "vendor_int1e_r_cart"
    - path: "crates/cintx-oracle/tests/moment_common.rs"
      provides: "rank-parameterized vendor_parity helper + env_with_rinv_origin + non-square shell-pair selectors"
      contains: "env_with_rinv_origin"
    - path: "crates/cintx-oracle/tests/moment_r_parity.rs"
      provides: "MOM-01 parity test scaffold (r, r_origj)"
    - path: "crates/cintx-oracle/tests/moment_low_parity.rs"
      provides: "MOM-02 parity test scaffold (rr, r2, z, zz + _origj)"
    - path: "crates/cintx-oracle/tests/moment_high_parity.rs"
      provides: "MOM-03 parity test scaffold (rrr, rrrr, r4)"
    - path: "crates/cintx-oracle/tests/moment_nontensor_parity.rs"
      provides: "MOM-04 parity test scaffold (p4, rinv, drinv, irp)"
  key_links:
    - from: "crates/cintx-oracle/build.rs"
      to: "vendor libcint static lib"
      via: "bindgen allowlist regex re-run"
      pattern: "allowlist_function"
    - from: "crates/cintx-oracle/tests/moment_common.rs"
      to: "build_h2o_sto3g_common_orig"
      via: "Cluster A fixture import"
      pattern: "build_h2o_sto3g_common_orig"
    - from: "crates/cintx-oracle/tests/moment_nontensor_parity.rs"
      to: "env_with_rinv_origin"
      via: "non-zero rinv center injection"
      pattern: "env_with_rinv_origin"
---

<objective>
Build the Wave-0 validation surface for all of Phase 24: the vendor-FFI wrappers, the bindgen allowlist extension, the four moment parity test scaffolds, the shared rank-parameterized parity helper, the non-zero rinv_orig env setter, and the OQ-2 triage. These are the tests the kernel/registration plans (02-05) verify against.

Purpose: Per the Nyquist rule, every kernel task in plans 02-05 verifies against a parity test that MUST already exist. This plan creates that surface so kernel work has an automated RED→GREEN target from the first commit.
Output: 1 modified build file, 1 modified FFI file, 1 shared test-common module, 4 new parity test files (compiling, skipping cleanly until the vendor gate runs), and a triaged OQ-2 disposition.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-group-3-position-multipole-moment-integrals/24-CONTEXT.md
@.planning/phases/24-group-3-position-multipole-moment-integrals/24-RESEARCH.md
@.planning/phases/24-group-3-position-multipole-moment-integrals/24-PATTERNS.md
@.planning/phases/24-group-3-position-multipole-moment-integrals/24-VALIDATION.md

<interfaces>
<!-- The executor uses these directly — no codebase exploration needed for the contracts. -->

RawApiId const pattern (crates/cintx-compat/src/raw.rs:122-243) — these consts are CREATED in plans 02-05; the test files reference them by name:
  RawApiId::INT1E_R_CART / INT1E_R_SPH / INT1E_R_ORIGJ_CART / INT1E_R_ORIGJ_SPH
  RawApiId::INT1E_RR_CART / _SPH / _ORIGJ_*    RawApiId::INT1E_R2_CART / _SPH / _ORIGJ_*
  RawApiId::INT1E_Z_*, INT1E_ZZ_*, INT1E_R4_* (+ _ORIGJ_*)
  RawApiId::INT1E_RRR_CART / _SPH             RawApiId::INT1E_RRRR_CART / _SPH
  RawApiId::INT1E_P4_*, INT1E_RINV_*, INT1E_DRINV_*, INT1E_IRP_*

vendor_ffi.rs analog — vendor_int1e_iprinv_sph / _cart (crates/cintx-oracle/src/vendor_ffi.rs:755-811):
  pub fn vendor_int1e_iprinv_sph(out: &mut [f64], shls: &[i32;2], atm: &[i32], natm: i32,
      bas: &[i32], nbas: i32, env: &[f64]) -> i32 { unsafe { ffi::int1e_iprinv_sph(
      out.as_mut_ptr(), ptr::null_mut(), shls.as_ptr() as *mut i32,
      atm.as_ptr() as *mut i32, natm, bas.as_ptr() as *mut i32, nbas,
      env.as_ptr() as *mut f64, ptr::null_mut(), ptr::null_mut()) } }

Parity helper analog — vendor_parity<FS,FC> (crates/cintx-oracle/tests/one_electron_grad_both_parity.rs:307-332):
  signature parameterizes over (api_sph, api_cart, vendor_sph: FS, vendor_cart: FC, label).
  NCOMP is hardcoded to 9 at :20 — Phase 24 ranks span 1/3/9/27/81 so the helper MUST be
  rank-parameterized (pass rank as an argument; size out buffers as rank*ni*nj).

env_with_rinv_origin analog (crates/cintx-oracle/tests/one_electron_nuc_grad_parity.rs:150-155):
  fn env_with_rinv_origin(env: &[f64], origin: [f64;3]) -> Vec<f64> {
      let mut e = env.to_vec();
      e[PTR_RINV_ORIG] = origin[0]; e[PTR_RINV_ORIG+1] = origin[1]; e[PTR_RINV_ORIG+2] = origin[2]; e }
  Use a PTR_ENV_START=20-aligned env (atom coords at env[20..]) so env[4..6] does not collide
  (one_electron_nuc_grad_parity.rs:37-39).

Fixture (crates/cintx-oracle/src/fixtures.rs:148-176):
  build_h2o_sto3g_common_orig() -> (atm, bas, env)  with COMMON_ORIG_FIXTURE_ORIGIN = [0.5, -0.3, 0.8].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend bindgen allowlist + add vendor FFI wrappers for all 38 new symbols</name>
  <files>crates/cintx-oracle/build.rs, crates/cintx-oracle/src/vendor_ffi.rs</files>
  <read_first>
    - crates/cintx-oracle/build.rs (read the full file; the allowlist_function alternation is at :358 and the source list at :51 — confirm src/autocode/intor1.c is already present, NO new .c to add)
    - crates/cintx-oracle/src/vendor_ffi.rs:740-815 (the vendor_int1e_iprinv_sph/_cart analog to clone verbatim)
    - .planning/phases/24-group-3-position-multipole-moment-integrals/24-PATTERNS.md (the vendor_ffi.rs + build.rs sections)
  </read_first>
  <action>
    OQ-3 RESOLVED (grep of libcint-master/src/autocode/intor1.c): the ONLY _origj symbols that exist
    in libcint 6.1.3 are EXACTLY: int1e_{r,rr,r2,r4,z,zz}_origj_{cart,sph} (12 symbols).
    There is NO int1e_rrr_origj and NO int1e_rrrr_origj — DO NOT add them anywhere (no vendor target exists).

    1. build.rs (:358): append to the existing `allowlist_function(...)` regex alternation these 38 symbols
       (base 26 + origj 12), pipe-separated, matching the existing `int1e_iprinv_sph|int1e_iprinv_cart|...` style:
         int1e_r_sph|int1e_r_cart|int1e_rr_sph|int1e_rr_cart|int1e_rrr_sph|int1e_rrr_cart|
         int1e_rrrr_sph|int1e_rrrr_cart|int1e_r2_sph|int1e_r2_cart|int1e_r4_sph|int1e_r4_cart|
         int1e_z_sph|int1e_z_cart|int1e_zz_sph|int1e_zz_cart|int1e_p4_sph|int1e_p4_cart|
         int1e_irp_sph|int1e_irp_cart|int1e_rinv_sph|int1e_rinv_cart|int1e_drinv_sph|int1e_drinv_cart|
         int1e_r_origj_sph|int1e_r_origj_cart|int1e_rr_origj_sph|int1e_rr_origj_cart|
         int1e_r2_origj_sph|int1e_r2_origj_cart|int1e_r4_origj_sph|int1e_r4_origj_cart|
         int1e_z_origj_sph|int1e_z_origj_cart|int1e_zz_origj_sph|int1e_zz_origj_cart
       Do NOT touch the source list at :51 (intor1.c already present). Use exact-match regex anchoring
       consistent with the existing entries (avoid `int1e_r` accidentally matching `int1e_rr` — the existing
       pattern uses full-symbol alternation incl. the `_sph`/`_cart` suffix, which is already exact; preserve that).
    2. vendor_ffi.rs: clone vendor_int1e_iprinv_sph/_cart verbatim for each of the 38 symbols — change ONLY the
       `ffi::int1e_*` call and the doc comment. Naming: `vendor_int1e_<family>_<rep>` (e.g. vendor_int1e_r_cart,
       vendor_int1e_rr_origj_sph). Each `out` slice is sized RANK*ni*nj component-leading (the caller in the test
       sizes it; the wrapper just forwards the pointer). Keep the `ptr::null_mut()` opt/cache args as the analog does.
    Do NOT write any kernel or RawApiId code here — those are plans 02-05.
  </action>
  <verify>
    <automated>CINTX_ORACLE_BUILD_VENDOR=1 cargo build -p cintx-oracle --features cpu 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo build -p cintx-oracle --features cpu` succeeds (bindgen re-runs, vendor static lib rebuilds with the new symbols).
    - `grep -c 'vendor_int1e_r_cart\|vendor_int1e_drinv_sph\|vendor_int1e_zz_origj_cart' crates/cintx-oracle/src/vendor_ffi.rs` ≥ 3.
    - `grep -E 'int1e_rrr_origj|int1e_rrrr_origj' crates/cintx-oracle/build.rs` returns NOTHING (those symbols must NOT be added).
    - `grep -c 'int1e_drinv_cart' crates/cintx-oracle/build.rs` ≥ 1.
  </acceptance_criteria>
  <done>All 38 vendor symbols (26 base + 12 _origj) compile through bindgen and have safe wrappers; rrr/rrrr _origj are absent.</done>
</task>

<task type="auto">
  <name>Task 2: Create the four moment parity test scaffolds + shared rank-parameterized helper + rinv_orig setter</name>
  <files>crates/cintx-oracle/tests/moment_common.rs, crates/cintx-oracle/tests/moment_r_parity.rs, crates/cintx-oracle/tests/moment_low_parity.rs, crates/cintx-oracle/tests/moment_high_parity.rs, crates/cintx-oracle/tests/moment_nontensor_parity.rs</files>
  <read_first>
    - crates/cintx-oracle/tests/one_electron_grad_both_parity.rs:1-20 (cfg gates) and :307-374 (vendor_parity<FS,FC> helper + per-family #[test] wrappers — the clone target)
    - crates/cintx-oracle/tests/one_electron_nuc_grad_parity.rs:37-39, :150-155 (PTR_ENV_START-aligned env + env_with_rinv_origin)
    - crates/cintx-oracle/src/fixtures.rs:148-176 (build_h2o_sto3g_common_orig + COMMON_ORIG_FIXTURE_ORIGIN)
    - .planning/phases/24-group-3-position-multipole-moment-integrals/24-PATTERNS.md (the moment_*_parity.rs section, item 1-5 of "Phase-24-specific adaptations")
    - .planning/phases/24-group-3-position-multipole-moment-integrals/24-RESEARCH.md (component_rank table + Phase Requirements→Test Map)
  </read_first>
  <action>
    Create moment_common.rs as a shared module (declared via `#[path = ...] mod moment_common;` or a `pub mod` in each test) containing:
    (a) a RANK-PARAMETERIZED `vendor_parity` helper cloned from one_electron_grad_both_parity.rs:307 but taking
        `rank: usize` and sizing every out/collect buffer as rank*ni*nj (generalize the hardcoded NCOMP=9).
        It must: build the fixture, call cintx api (sph then cart), call the vendor fn, assert_any_nonzero on BOTH,
        count_mismatches at ATOL=1e-12 RTOL=0, assert_eq!(mm, 0, ...).
    (b) `env_with_rinv_origin(env, origin)` copied verbatim from one_electron_nuc_grad_parity.rs:150-155.
    (c) a `non_square_shell_pair()` helper returning a NON-SQUARE bra×ket shell-index pair from H2O/STO-3G
        (e.g. O-s shell × O-p shell, indices documented inline) — D-07 transpose gate. A square p×p pair is
        FORBIDDEN (transpose-symmetric, hides the bug).
    Then create the 4 test files. Each: `#![cfg(any(feature="cpu",feature="rocm"))]`, `#[cfg(has_vendor_libcint)]`
    + `#[cfg(feature="cpu")]` per test, importing moment_common. Each #[test] calls vendor_parity with the
    correct rank and RawApiId consts (named per the <interfaces> block — these consts come ONLINE in plans 02-05):
      - moment_r_parity.rs: int1e_r (rank 3), int1e_r_origj (rank 3). Fixture: build_h2o_sto3g_common_orig.
      - moment_low_parity.rs: rr(9), r2(1), z(1), zz(1) + rr_origj(9), r2_origj(1), z_origj(1), zz_origj(1).
        Fixture: build_h2o_sto3g_common_orig.
      - moment_high_parity.rs: rrr(27), rrrr(81), r4(1) + r4_origj(1). NO rrr_origj/rrrr_origj (don't exist).
        Fixture: build_h2o_sto3g_common_orig.
      - moment_nontensor_parity.rs: p4(1), irp(9) on build_h2o_sto3g_common_orig; rinv(1), drinv(3) on a fixture
        whose env is wrapped with env_with_rinv_origin(env, [0.5,-0.3,0.8]) (NON-ZERO rinv center — a zero rinv_orig
        is trivially-passing and disallowed, D-04 correction / OQ-1). irp reads PTR_COMMON_ORIG (use the common-orig env).
    Every test MUST use non_square_shell_pair(). The tests are EXPECTED to be RED (kernel "UnsupportedApi") until
    plans 02-05 land — that is the intended Nyquist target, not a failure of this plan.
    To keep the workspace building before the RawApiId consts exist, gate each #[test] body that references a
    not-yet-existing const behind a feature or leave the const references and accept that the test CRATE only
    compiles after plan 02+ adds the consts — DOCUMENT this ordering in the file header so plan 02 knows it
    unblocks compilation. (Preferred: stub the missing consts in a `// TODO(plan-02..05)` cfg block is NOT allowed;
    instead, the RawApiId consts for ALL families are added in their respective plan's Task 0, and this test file's
    `cargo test --no-run` is verified in plan 02's first task. For THIS plan, verify the helper module + non-rawapi
    helpers compile in isolation.)
  </action>
  <verify>
    <automated>cargo test -p cintx-oracle --features cpu --no-run 2>&1 | tail -20 || echo "EXPECTED: may not compile until RawApiId consts land in plan 02 — verify moment_common helpers instead"</automated>
  </verify>
  <acceptance_criteria>
    - The 5 files exist: `ls crates/cintx-oracle/tests/moment_common.rs crates/cintx-oracle/tests/moment_r_parity.rs crates/cintx-oracle/tests/moment_low_parity.rs crates/cintx-oracle/tests/moment_high_parity.rs crates/cintx-oracle/tests/moment_nontensor_parity.rs` all succeed.
    - `grep -c 'env_with_rinv_origin' crates/cintx-oracle/tests/moment_common.rs` ≥ 1.
    - `grep -c 'non_square_shell_pair' crates/cintx-oracle/tests/moment_common.rs` ≥ 1.
    - `grep -c 'ATOL' crates/cintx-oracle/tests/moment_common.rs` ≥ 1 AND the value is `1e-12`.
    - `grep -E 'rrr_origj|rrrr_origj' crates/cintx-oracle/tests/moment_high_parity.rs` returns NOTHING.
    - `grep -c 'build_h2o_sto3g_common_orig' crates/cintx-oracle/tests/moment_r_parity.rs` ≥ 1 (NON-ZERO gauge origin, not the zero fixture).
    - moment_nontensor_parity.rs rinv/drinv tests call `env_with_rinv_origin(.., [0.5, -0.3, 0.8])` (non-zero): `grep -c 'env_with_rinv_origin' crates/cintx-oracle/tests/moment_nontensor_parity.rs` ≥ 1.
  </acceptance_criteria>
  <done>All 4 parity scaffolds + shared helper exist with rank-parameterized vendor_parity, ATOL=1e-12, env_with_rinv_origin, and a non-square shell pair; rrr/rrrr _origj absent; rinv/drinv use a non-zero rinv center.</done>
</task>

<task type="auto">
  <name>Task 3: Triage the pre-existing oracle-cart-offset-vendor-zero lib-unit failure (OQ-2)</name>
  <files>.planning/todos/pending/oracle-cart-offset-vendor-zero.md</files>
  <read_first>
    - .planning/todos/pending/oracle-cart-offset-vendor-zero.md (the folded todo)
    - .planning/phases/24-group-3-position-multipole-moment-integrals/24-RESEARCH.md (OQ-2 section + Validation Architecture "Vendor lib+unit caveat")
    - .planning/phases/24-group-3-position-multipole-moment-integrals/24-VALIDATION.md (Manual-Only Verifications: OQ-2 row)
  </read_first>
  <action>
    Determine whether the 4 `compare::tests` lib-unit failures (`CINTshells_cart_offset[4] cintx=8 vendor=0`,
    compare.rs ~645) reproduce on a PRE-phase-20 commit. Steps:
    1. Run the vendor lib-unit tests on the current tree to capture the failure:
       `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --lib 2>&1 | grep -A2 cart_offset`.
    2. Check out a pre-phase-20 commit in a throwaway worktree (do NOT disturb the working branch) and re-run
       the same lib-unit test to confirm the failure pre-dates Phase 24 work.
    3. Disposition:
       - If it REPRODUCES pre-phase-20: it is pre-existing harness noise. Update the todo to a tracked standalone
         oracle-harness bug (mark `blocks_phase_24_gate: false`, record the reproduction commit + the hypothesis
         that vendor `ao_loc` returns 0 in the lib-unit context). The Phase 24 gate uses `--test` integration
         parity (which passes) — this lib-unit failure MUST NOT block it.
       - If it is NEW (introduced by Phase 24 wave-0 work): fix the lib-test fixture before proceeding.
    Do NOT spend effort fixing a pre-existing harness bug — just confirm + track + de-block.
  </action>
  <verify>
    <automated>grep -c 'blocks_phase_24_gate\|standalone\|pre-existing' .planning/todos/pending/oracle-cart-offset-vendor-zero.md</automated>
  </verify>
  <acceptance_criteria>
    - The todo records a reproduction result (pre-phase-20 commit hash + reproduced yes/no).
    - If reproduced pre-phase-20: todo explicitly states the failure does NOT block the Phase 24 integration-parity gate.
    - `grep -c 'blocks_phase_24_gate\|standalone oracle-harness bug\|pre-existing' .planning/todos/pending/oracle-cart-offset-vendor-zero.md` ≥ 1.
  </acceptance_criteria>
  <done>OQ-2 triaged: failure confirmed pre-existing (or fixed if new) and the Phase 24 integration gate is documented as not blocked by it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → vendor libcint FFI | the wrapper forwards raw `*mut` pointers from Rust slices into C; out-buffer sizing is the only memory-safety contract |
| caller env-array → eval_raw env-slot reads | non-finite (NaN/inf) origins crossing into env[1..3]/env[4..6] (validated downstream; this plan only sets them in tests) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01-01 | Information Disclosure / Tampering | vendor_ffi.rs wrappers passing under-sized `out` buffers to C | mitigate | Each wrapper's caller sizes `out` as rank*ni*nj; the rank-parameterized helper in moment_common.rs sizes buffers from the family's exact component_rank (no truncation). Verified by the assert_any_nonzero + mismatch==0 gate. |
| T-24-01-02 | Tampering (silent trivial pass) | rinv/drinv parity at zero rinv_orig | mitigate | env_with_rinv_origin sets a NON-ZERO [0.5,-0.3,0.8] rinv center; a zero origin is trivially-passing and disallowed (D-04 correction). Acceptance criteria assert the non-zero call. |
| T-24-01-03 | Denial of Service (gate blockage by pre-existing noise) | OQ-2 lib-unit failure | accept | Pre-existing harness bug (CINTshells_cart_offset, vendor ao_loc→0 in lib-unit context); triaged + tracked standalone; integration parity is the gate, not lib-unit. No NEW validation gap introduced (ASVS V5 env-slot finiteness already enforced by validate_*_env_params). |
</threat_model>

<verification>
- `CINTX_ORACLE_BUILD_VENDOR=1 cargo build -p cintx-oracle --features cpu` succeeds (vendor symbols compile).
- The 5 test files exist; moment_common helpers (env_with_rinv_origin, non_square_shell_pair, rank-parameterized vendor_parity at ATOL=1e-12) are present.
- rrr_origj / rrrr_origj absent everywhere.
- OQ-2 triaged and de-blocked.
</verification>

<success_criteria>
- All 38 vendor wrappers + allowlist entries land and compile under the vendor gate.
- 4 parity scaffolds + shared helper exist; the kernel plans (02-05) have an automated RED→GREEN target.
- rinv/drinv tests use a non-zero rinv center; every test uses a non-square shell pair.
- OQ-2 disposition recorded; Phase 24 gate not blocked by pre-existing lib-unit noise.
</success_criteria>

<output>
After completion, create `.planning/phases/24-group-3-position-multipole-moment-integrals/24-01-SUMMARY.md`
</output>
