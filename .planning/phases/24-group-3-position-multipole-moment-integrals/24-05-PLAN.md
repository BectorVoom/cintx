---
phase: 24-group-3-position-multipole-moment-integrals
plan: 05
type: execute
wave: 2
depends_on: [24-01, 24-02]
files_modified:
  - crates/cintx-ops/generated/compiled_manifest.lock.json
  - crates/cintx-compat/src/raw.rs
  - crates/cintx-cubecl/src/kernels/one_electron.rs
autonomous: true
requirements: [MOM-04]
tags: [cubecl, kernel, irp, overlap-derivative, gauge-origin, parity]
user_setup: []

must_haves:
  truths:
    - "int1e_irp (i·r×∇, rank 9) matches vendored libcint at atol=1e-12 (cart+sph) on a non-square block"
    - "irp is the 3×3 (∇-axis ⊗ r-axis) tensor, reads PTR_COMMON_ORIG (it is a gauge-origin family), ket headroom +2"
    - "The 9 components are emitted in the verbatim intor1.c:2788-2816 s[0..8] order"
  artifacts:
    - path: "crates/cintx-ops/generated/compiled_manifest.lock.json"
      provides: "int1e_irp (rank 9) × {cart,sph,spinor}"
      contains: "int1e_irp_cart"
    - path: "crates/cintx-compat/src/raw.rs"
      provides: "RawApiId consts INT1E_IRP_*"
      contains: "INT1E_IRP_CART"
    - path: "crates/cintx-cubecl/src/kernels/one_electron.rs"
      provides: "irp overlap-derivative kernel arm (D_J on r-level block, reads common_orig)"
      contains: "is_irp"
  key_links:
    - from: "crates/cintx-cubecl/src/kernels/one_electron.rs"
      to: "plan.operator_env_params.common_orig"
      via: "irp reads PTR_COMMON_ORIG (G1E_RCJ) — gauge-origin family"
      pattern: "is_irp"
    - from: "crates/cintx-cubecl/src/kernels/one_electron.rs"
      to: "overlap-derivative engine (G1E_D_J + G1E_RCJ)"
      via: "3×3 r⊗∇ tensor, no Rys"
      pattern: "is_irp"
---

<objective>
Cluster D — register and implement int1e_irp (i·r×∇): the rank-9 3×3 (∇-axis ⊗ r-axis) tensor on the overlap-derivative engine (no Rys), reading PTR_COMMON_ORIG (it is a gauge-origin family), ket headroom +2.

Purpose: Covers MOM-04 (irp). Reuses the overlap-derivative machinery (D-04); reads the gauge origin like Cluster A. Rank 9 — CONTEXT left irp open; research resolved it to a 3×3 r⊗∇ tensor (NOT a smaller rank).
Output: 1 family × {cart,sph,spinor} manifest entries, RawApiId consts, the irp overlap-derivative kernel arm.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-group-3-position-multipole-moment-integrals/24-CONTEXT.md
@.planning/phases/24-group-3-position-multipole-moment-integrals/24-RESEARCH.md
@.planning/phases/24-group-3-position-multipole-moment-integrals/24-PATTERNS.md
@.planning/phases/24-group-3-position-multipole-moment-integrals/24-01-SUMMARY.md

<interfaces>
component_rank: irp = "9" (D-08, NOT a smaller rank — research-resolved 3×3 tensor). ng = {0,2,0,0,2,1,1,9} — ket +2.
Manifest template: ipovlpip cart entry (compiled_manifest.lock.json:4051-4081, rank-9 shape).
RawApiId: INT1E_IRP_{CART,SPH,SPINOR} (raw.rs:122-243 pattern).
common_orig read live in eval_raw (raw.rs:677-686) — irp consumes it, NO new env code (like Cluster A).
Kernel anchors (crates/cintx-cubecl/src/kernels/one_electron.rs):
  moment kernel (from plan 02) for the r-level (G1E_RCJ) block + the gradient (G1E_D_J nabla) path; ket headroom +2.
  dispatch ladder :3764-3790; spinor reject :3805-3809; rj/common_orig in scope :3761,:677.
vendor wrapper (plan 01): vendor_int1e_irp_{cart,sph}. Test (plan 01): moment_nontensor_parity.rs irp case (build_h2o_sto3g_common_orig).
</interfaces>

<gout_order>
<!-- VERBATIM from libcint-master/src/autocode/intor1.c:2781-2816 — D-07. -->
int1e_irp (rank 9): g1 = G1E_D_J(g0) (∇ on ket), g2 = G1E_RCJ(g0) (r on ket, drj = rj - env[PTR_COMMON_ORIG]),
  g3 = G1E_D_J(g2). Emit the 3×3 (∇-axis ⊗ r-axis) tensor s[0..8] in the verbatim intor1.c:2788-2816 order —
  copy s[0]..s[8] character-for-character from the source; DO NOT re-derive the axis nesting.
  gout[n*9+0..8] = s[0..8]. (A permuted/transposed order passes a square block but fails a non-square one.)
</gout_order>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register + implement int1e_irp (i·r×∇, rank 9, overlap-derivative, reads common origin)</name>
  <files>crates/cintx-ops/generated/compiled_manifest.lock.json, crates/cintx-compat/src/raw.rs, crates/cintx-cubecl/src/kernels/one_electron.rs</files>
  <behavior>
    - int1e_irp cart on build_h2o_sto3g_common_orig (origin [0.5,-0.3,0.8]), non-square O-s×O-p block: 9 components in the exact intor1.c:2788-2816 s[0..8] order, byte-identical to vendor_int1e_irp_cart at atol=1e-12.
    - int1e_irp sph: byte-identical to vendor_int1e_irp_sph.
    - irp reads the gauge origin (PTR_COMMON_ORIG); a zero-origin test is trivially-passing — the non-zero fixture exercises it.
    - A transposed/permuted 9-component order fails on the non-square block (D-07).
  </behavior>
  <read_first>
    - crates/cintx-cubecl/src/kernels/one_electron.rs (the moment kernel from plan 02 — the r-level G1E_RCJ block to reuse; and the gradient G1E_D_J path); dispatch :3764-3809; common_orig live read (raw.rs:677)
    - crates/cintx-compat/src/raw.rs:122-243 (RawApiId consts)
    - crates/cintx-ops/generated/compiled_manifest.lock.json:4051-4081 (ipovlpip rank-9 template)
    - libcint-master/src/autocode/intor1.c:2781-2816 (irp: g1=D_J(g0), g2=RCJ(g0), g3=D_J(g2); s[0..8]; gout n*9 block)
    - docs/manual/Cubecl/*.md (#[cube] rules — MANDATORY)
    - .planning/phases/24-group-3-position-multipole-moment-integrals/24-RESEARCH.md (Cluster D irp pattern; Verbatim gout component order)
  </read_first>
  <action>
    Step 1 (manifest): clone the ipovlpip rank-9 cart/sph/spinor entries for int1e_irp, component_rank "9"
    (NOT smaller — research-resolved 3×3 r⊗∇), operator:"irp", symbol:"int1e_irp_cart"/sph/spinor. spinor
    oracle_covered:false; cart/sph oracle_covered:false until green. `cargo build -p cintx-ops`.
    Step 2 (raw.rs): add RawApiId consts INT1E_IRP_{CART,SPH,SPINOR}. NO new env-slot read — common_orig is read
    live at :677 (irp is a gauge-origin family like Cluster A).
    Step 3 (kernel): add an irp arm on the overlap-derivative engine (no Rys), ket headroom +2 (ng[1]=2). Build:
    g1 = G1E_D_J(g0) (∇ on ket), g2 = G1E_RCJ(g0) (r on ket, drj = rj - common_orig where common_orig =
    plan.operator_env_params.common_orig.unwrap_or([0.0;3])), g3 = G1E_D_J(g2). Emit the rank-9 s[0..8] tensor
    in the VERBATIM intor1.c:2788-2816 order — copy the s-index list character-for-character (DO NOT re-derive the
    ∇-axis ⊗ r-axis nesting). Reuse the r-level block from plan 02's moment kernel where convenient.
    Step 4 (dispatch): add is_irp to the :3764 ladder, drop from rejection guard. Spinor → UnsupportedApi.
    Step 5: flip oracle_covered:true for irp cart/sph; `cargo build -p cintx-ops`. Obey ALL #[cube] authoring rules.
  </action>
  <verify>
    <automated>cargo build -p cintx-ops && cargo test -p cintx-cubecl --features cpu --lib 2>&1 | tail -5 && CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test moment_nontensor_parity -- irp 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test moment_nontensor_parity -- irp` shows `running N>0 tests`, `0 failed`; irp cart+sph (9 components) pass at atol=1e-12 on the NON-SQUARE block on the non-zero gauge-origin fixture.
    - `grep -c '"int1e_irp_cart"' crates/cintx-ops/generated/compiled_manifest.lock.json` ≥ 1 with `"component_rank": "9"`.
    - `grep -c 'INT1E_IRP_CART' crates/cintx-compat/src/raw.rs` ≥ 1.
    - `grep -c 'is_irp' crates/cintx-cubecl/src/kernels/one_electron.rs` ≥ 1 (arm present, reads common_orig).
    - `cargo test -p cintx-cubecl --features cpu --lib` passes (no regression).
  </acceptance_criteria>
  <done>int1e_irp (rank 9) matches vendor at atol=1e-12 (cart+sph) on a non-square block on the non-zero gauge-origin fixture; 3×3 r⊗∇ order verbatim; reads PTR_COMMON_ORIG.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller env-array → eval_raw → irp kernel | gauge origin env[PTR_COMMON_ORIG] feeds the irp r-level; non-finite origin must be rejected |
| component emission order → output buffer | a permuted/transposed 9-component order is a correctness defect invisible on a square block |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05-01 | Tampering (input validation, ASVS V5) | non-finite (NaN/inf) gauge origin in env[1..3] | mitigate | validate_common_orig_env_params (validator.rs:210-223) rejects NaN/inf before the kernel; eval_raw bounds-guards the read (raw.rs:677). No NEW validation gap — irp consumes the already-validated slot. |
| T-24-05-02 | Information Disclosure (transposed/permuted tensor) | wrong 9-component emission order | mitigate | s[0..8] copied verbatim from intor1.c:2788-2816; the NON-SQUARE block parity test at atol=1e-12 catches any permutation (a square block is transpose-symmetric, D-07). component_rank "9" matches the true output (no truncation). |
</threat_model>

<verification>
- moment_nontensor_parity irp case `running N>0 tests`, `0 failed` at atol=1e-12 on a non-square block on the non-zero gauge fixture.
- irp reads PTR_COMMON_ORIG; 9-component order verbatim.
- `cargo build -p cintx-ops` green.
</verification>

<success_criteria>
- MOM-04 (irp portion) byte-identical to vendored libcint 6.1.3 at atol=1e-12 (cart+sph).
- No capi/legacy surface added (D-09).
</success_criteria>

<output>
After completion, create `.planning/phases/24-group-3-position-multipole-moment-integrals/24-05-SUMMARY.md`
</output>
