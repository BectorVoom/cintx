---
phase: 14-unstable-source-api-families
task: wave-2-merge
total_tasks: 5 plans (10 tasks)
status: in_progress
last_updated: 2026-04-05T10:07:14.868Z
---

<current_state>
Phase 14 Wave 2 partially complete. Wave 1 (14-01 infrastructure) is merged to main.
Plans 14-03 (grids) and 14-04 (breit) completed on worktree branches but NOT merged.
Plan 14-02 (origi/origk/ssc) was never executed — agent spawn was rejected by user.
Wave 3 (14-05 CI wiring) not started.
</current_state>

<completed_work>

- Plan 14-01: Infrastructure — Done, merged to main (commits d9f20ee, 0aa712e, 0229fbd)
  - Feature gates, 18 manifest entries, GridsEnvParams, kernel dispatch stubs, oracle scaffold
- Plan 14-03: Grids kernels — Done on worktree branch `worktree-agent-a483b96f` (commits 99caf1e, 6e2b372, d2851df)
  - launch_grids for 5 variants, oracle parity tests, several deviation fixes
  - IMPORTANT: Agent made fixes to shared code: DispatchFamily::UnstableSource, manifest component_rank, feature propagation, validate_profile_and_source_gate
- Plan 14-04: Breit kernels — Done on worktree branch `worktree-agent-a75ac31c` (commits ebdc69a, 38d96ea, 63974b6)
  - launch_breit for 2 spinor symbols, oracle parity tests
</completed_work>

<remaining_work>

1. **Merge worktree branches** for 14-03 and 14-04 to main
   - Both modified `crates/cintx-cubecl/src/kernels/unstable.rs` — WILL conflict
   - Both modified `crates/cintx-oracle/tests/unstable_source_parity.rs` — WILL conflict
   - 14-03 made shared fixes that 14-04 may not have — reconcile carefully
2. **Execute plan 14-02** (origi/origk/ssc — 11 symbols, Wave 2)
3. **Execute plan 14-05** (CI wiring, xtask validation, manifest lock — Wave 3)
4. **Phase verification** after all plans complete
</remaining_work>

<decisions_made>

- Wave 1 merged via fast-forward (clean, no conflicts)
- 14-03 agent discovered and fixed several issues in shared infrastructure:
  - Added DispatchFamily::UnstableSource variant
  - Fixed manifest component_rank values for derivative grids variants
  - Fixed unstable-source-api feature not propagating cintx-compat → cintx-cubecl
  - Fixed validate_profile_and_source_gate rejecting source-only symbols under "base" profile
- Pre-existing uncommitted changes (.github workflows, compare.rs, xtask lockfile, ci/api-value-baseline.sh) are from prior work, NOT phase 14
</decisions_made>

<blockers>
- None blocking, but merge conflicts expected when integrating worktree branches
</blockers>

<context>
This is the first execution session for phase 14. The phase implements 5 unstable-source
kernel families (origi, grids, breit, origk, ssc) totaling 18 symbols behind the
`unstable-source-api` feature gate. Wave structure: W1=infrastructure, W2=kernel
implementations (3 parallel plans), W3=CI wiring.

The 14-03 agent was the most complex — it discovered issues in shared code that required
deviation fixes. These fixes may affect how 14-02 and 14-04 integrate. When merging,
14-03's changes to shared files should generally take precedence since they fix real issues.
</context>

<next_action>
Start with: Merge `worktree-agent-a483b96f` (14-03) then `worktree-agent-a75ac31c` (14-04)
to main, resolving conflicts in unstable.rs and unstable_source_parity.rs. Then run
`/gsd:execute-phase 14` which will detect 14-02 and 14-05 as incomplete and continue.
</next_action>
