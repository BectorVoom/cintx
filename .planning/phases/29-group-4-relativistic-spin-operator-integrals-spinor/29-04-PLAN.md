---
phase: 29-group-4-relativistic-spin-operator-integrals-spinor
plan: 04
type: execute
wave: 2
depends_on: ["03"]
files_modified:
  - crates/cintx-oracle/build.rs
  - crates/cintx-oracle/src/vendor_ffi.rs
  - crates/cintx-oracle/tests/si_2e_transform_parity.rs
  - crates/cintx-cubecl/src/kernels/two_electron.rs
autonomous: true
requirements: [REL-03]
must_haves:
  truths:
    - "The 2e si/sf transform suite (c2s_si_2e1 + c2s_sf_2e2) is byte-identical to vendored libcint via the thinnest 2e si family int2e_spsp1_spinor at atol=1e-12"
    - "The transform micro-test is GREEN (the D-03 BLOCKING gate) before any further 2e family wires onto the transform"
    - "The micro-test runs non-skipped under the double gate (NO-SILENT-SKIP)"
  artifacts:
    - path: "crates/cintx-oracle/tests/si_2e_transform_parity.rs"
      provides: "D-03 BLOCKING transform-level byte-identity micro-test on int2e_spsp1_spinor"
      contains: "ATOL"
    - path: "crates/cintx-oracle/src/vendor_ffi.rs"
      provides: "vendor_int2e_spsp1_spinor shim"
      contains: "vendor_int2e_spsp1_spinor"
  key_links:
    - from: "crates/cintx-oracle/tests/si_2e_transform_parity.rs"
      to: "cart_to_spinor_si_2e1 + cart_to_spinor_sf_2e2"
      via: "int2e_spsp1_spinor driven against vendor_int2e_spsp1_spinor on the 2e kappa fixture"
      pattern: "vendor_int2e_spsp1_spinor"
---

<objective>
The D-03 BLOCKING gate: prove the brand-new 2e si/sf transform suite (29-03) is byte-identical to vendored libcint BEFORE any 2e family is wired onto it (29-06). Drive the thinnest 2e si family `int2e_spsp1_spinor` (`c2s_si_2e1`+`c2s_sf_2e2`, intor4.c:85) through the new transform and compare to `vendor_int2e_spsp1_spinor` at atol=1e-12 on `build_kappa_spinor_2e_fixture`. This is the structural mitigation for the no-spike risk (D-03): a wrong 2e layout/stride/sign surfaces HERE, on the thinnest family, not scattered across 16 families in 29-06.

Purpose: Pin the novel 2e transform layout at spike-level rigor without a separate spike phase. **[BLOCKING] — Wave 3 (29-05/06) does not begin until this test is GREEN.**
Output: spsp1 manifest row + vendor shim + the gating si_2e_transform_parity.rs; minimal spsp1 launcher arm to drive it.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-group-4-relativistic-spin-operator-integrals-spinor/29-RESEARCH.md
@.planning/phases/29-group-4-relativistic-spin-operator-integrals-spinor/29-PATTERNS.md
@.planning/phases/29-group-4-relativistic-spin-operator-integrals-spinor/29-03-SUMMARY.md

<interfaces>
From 29-03 (the transform suite under test):
- `cart_to_spinor_si_2e1`, `cart_to_spinor_sf_2e2` in c2spinor.rs — the pairing int2e_spsp1 uses.
- `build_kappa_spinor_2e_fixture` in fixtures.rs (4 shells, non-square, GT/LT, nctr>1).

libcint reference:
- int2e_spsp1_spinor driver (intor4.c:79-86): `f_gout = &CINTgout2e_int2e_spsp1` (gout rank 4: σ·p G-tensor on bra `+s5-s7,+s6-s2,+s1-s3,+s0+s4+s8`, intor4.c:19); `CINT2e_spinor_drv(out, dims, &envs, opt, cache, &c2s_si_2e1, &c2s_sf_2e2)`. ALREADY in oracle build.rs (intor4.c at L62/229) — NO build change needed for spsp1.

From crates/cintx-oracle:
- `vendor_int1e_sp_spinor` (vendor_ffi.rs:4139) — clone for 2e (shls: &[i32;4], out sized ni*nj*nk*nl*2).
- build.rs allowlist_function regex (L374) — append int2e_spsp1_spinor.
- si_transform_parity.rs (full file) — the test scaffold structure to clone.

From crates/cintx-cubecl/src/kernels/two_electron.rs:
- All 2e Spinor currently returns UnsupportedApi (~L1450/1665/1717). Add ONLY the spsp1 arm here (the rest land in 29-06).
- 1e σ arm template: one_electron.rs:10495-10595 (launch→assembler→host-transform→scatter shape).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add int2e_spsp1 manifest row + vendor_int2e_spsp1_spinor shim + allowlist</name>
  <read_first>
    - crates/cintx-ops/generated/compiled_manifest.lock.json: `int1e_sp_spinor` row (L10710) as the shape; arity becomes 4, canonical_family/category "2e".
    - crates/cintx-oracle/src/vendor_ffi.rs `vendor_int1e_sp_spinor` (L4139) — 2e clone (shls &[i32;4]).
    - crates/cintx-oracle/build.rs allowlist_function regex (L374). intor4.c ALREADY at L62/229 — NO .file() change for spsp1.
    - PATTERNS.md §"Manifest rows" + §"vendor_int2e_*_spinor shims".
    - RESEARCH §Per-Family Map 2e table: int2e_spsp1 → c2s_si_2e1+c2s_sf_2e2, component_rank "1", intor4.c wired.
  </read_first>
  <action>
    (a) Add the `int2e_spsp1_spinor` ManifestEntry to compiled_manifest.lock.json (and the generated api_manifest.rs): arity 4, canonical_family "2e", category "2e", complex_output true, **component_rank "1"**, forms ["spinor"], helper_kind "operator", id.symbol "int2e_spsp1_spinor", oracle_covered false (flipped in 29-06), stability "stable", RepresentationSupport::new(false,false,true) spinor-only. (b) Clone `vendor_int1e_sp_spinor` → `vendor_int2e_spsp1_spinor`: `pub fn vendor_int2e_spsp1_spinor(out, shls: &[i32;4], atm, natm, bas, nbas, env) -> i32` calling `ffi::int2e_spsp1_spinor(out.as_mut_ptr(), ptr::null_mut(), shls.as_ptr() as *mut i32, atm..., bas..., env..., ptr::null_mut(), ptr::null_mut())`, out sized `ni*nj*nk*nl*2` via vendor_CINTcgto_spinor. (c) Append `int2e_spsp1_spinor` to the build.rs L374 allowlist regex. Re-grep `OperatorId::new(` / `_OPERATOR_ID` for positional drift; resolve by symbol name.
  </action>
  <verify>
    <automated>grep -q "int2e_spsp1_spinor" crates/cintx-ops/generated/compiled_manifest.lock.json && grep -q "vendor_int2e_spsp1_spinor" crates/cintx-oracle/src/vendor_ffi.rs && grep -q "int2e_spsp1_spinor" crates/cintx-oracle/build.rs && cargo build --workspace --features cpu 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `int2e_spsp1_spinor` row in compiled_manifest.lock.json: arity 4, component_rank "1", forms ["spinor"], oracle_covered false
    - `vendor_int2e_spsp1_spinor` present in vendor_ffi.rs and appended to the build.rs allowlist regex
    - `cargo build --workspace --features cpu` exits 0 (no OperatorId drift)
  </acceptance_criteria>
  <done>spsp1 manifest row + vendor shim + allowlist in place; workspace builds.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the int2e_spsp1 Spinor launcher arm (drives the transform under test)</name>
  <read_first>
    - crates/cintx-cubecl/src/kernels/two_electron.rs: the UnsupportedApi Spinor returns (~L1450/1665/1717) — replace ONLY the spsp1 path.
    - crates/cintx-cubecl/src/kernels/one_electron.rs `int1e_sp` arm (10495-10595) — the launch→assembler→host-transform→scatter shape + staging guard (10550-10560).
    - crates/cintx-cubecl/src/kernels/center_4c1e.rs `test_device_matches_host_spsp` (L1878) — the 2e σ·p-on-both-sides cart gout to mine (Open Q2 — the 2e σ·p assembler is expected NEW code, a thin 2e sibling).
    - 29-03 transform fns `cart_to_spinor_si_2e1` + `cart_to_spinor_sf_2e2`.
    - RESEARCH §Per-Family Map 2e (spsp1 gout rank 4) + intor4.c:19/85.
  </read_first>
  <action>
    Replace the `int2e_spsp1` Spinor UnsupportedApi return in two_electron.rs with a launcher arm following the 1e σ arm shape: device σ·p assembler producing the 4 contiguous gc blocks (gout rank 4 σ·p G-tensor on bra `+s5-s7,+s6-s2,+s1-s3,+s0+s4+s8`; mine the 2e σ·p cart gout from `test_device_matches_host_spsp` — this is the expected-new thin 2e assembler, Open Q2), then the host transform pairing `cart_to_spinor_si_2e1` (electron 1) + `cart_to_spinor_sf_2e2` (electron 2). **Fail-closed staging guard `required = di*dj*dk*dl*2` BEFORE any write** (Phase-28 CR-01 — inline 2e arm bypasses any launch_*_pair guard). Sizing all via spinor_len. NO launcher transpose — the transform owns KET→BRA. Suspect the CubeCL CpuRuntime ~1e-11 FP-env side effect before chasing kernel numerics on the flat atol=1e-12 gate.
  </action>
  <verify>
    <automated>cargo build -p cintx-cubecl --features cpu 2>&1 | tail -3; grep -c "BufferTooSmall" crates/cintx-cubecl/src/kernels/two_electron.rs; grep -n "int2e_spsp1\|Int2eSpsp1\|spsp1" crates/cintx-cubecl/src/kernels/two_electron.rs | grep -vi "UnsupportedApi"</automated>
  </verify>
  <acceptance_criteria>
    - two_electron.rs references `cart_to_spinor_si_2e1` and `cart_to_spinor_sf_2e2`
    - the spsp1 arm has a fail-closed staging guard (`grep -v '^#' crates/cintx-cubecl/src/kernels/two_electron.rs | grep -c "BufferTooSmall"` increased by ≥1)
    - `cargo build -p cintx-cubecl --features cpu` exits 0
  </acceptance_criteria>
  <done>int2e_spsp1 Spinor arm wired onto the new 2e transform with its own staging guard.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] si_2e_transform_parity.rs micro-test GREEN on int2e_spsp1 at atol=1e-12</name>
  <read_first>
    - crates/cintx-oracle/tests/si_transform_parity.rs (full file) — clone the gate structure: collect_cintx/collect_vendor, count_mismatches, assert_any_nonzero, PRIMARY GATE `#[cfg(has_vendor_libcint)] #[cfg(feature="cpu")]` byte-identity at ATOL=1e-12, test_no_silent_skip (327-363), non-vendor smoke.
    - `build_kappa_spinor_2e_fixture` (29-03), `vendor_int2e_spsp1_spinor` (Task 1).
    - PATTERNS.md §"si_2e_transform_parity.rs" + §"NO-SILENT-SKIP parity assertion".
    - RESEARCH §Validation Architecture (D-03 ordering invariant: this MUST be GREEN before Wave 3).
  </read_first>
  <action>
    Create `crates/cintx-oracle/tests/si_2e_transform_parity.rs` cloning the si_transform_parity.rs structure. Drive `int2e_spsp1_spinor` (cintx, via eval_raw on the new 2e transform) against `vendor_int2e_spsp1_spinor` on `build_kappa_spinor_2e_fixture` (4 shells, non-square, GT/LT kappa, nctr>1). PRIMARY GATE: byte-identity at `ATOL=1e-12` over all ni*nj*nk*nl*2 elements (assert_any_nonzero confirms N>0). NO-SILENT-SKIP: under `has_vendor_libcint` the vendor arm MUST run + produce nonzero (FAIL not skip); double gate `--features cpu` AND `CINTX_ORACLE_BUILD_VENDOR=1`. Add a kappa-sizing assertion (GT 2l+2 / LT 2l, not 4l+2). If the gate fails: per D-03 the suspect is the 2e transform layout/stride/sign (apply_2d_spinor_zi or the zcopy_iklj store), NOT the family — debug the transform here. Also wire the same micro-comparison into the cintx-cubecl c2spinor lib tests if a host-only path is feasible, so a fast unit signal exists. **This test GREEN is the D-04 Wave-2 gate; Wave 3 (29-05/06) is blocked until it passes.**
  </action>
  <verify>
    <automated>CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test si_2e_transform_parity 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `crates/cintx-oracle/tests/si_2e_transform_parity.rs` exists, contains `ATOL`, `test_no_silent_skip`, `build_kappa_spinor_2e_fixture`, `vendor_int2e_spsp1_spinor`
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test si_2e_transform_parity` exits 0 (byte-identity atol=1e-12, vendor arm ran non-skipped)
    - the kappa-sizing assertion passes (GT/LT, not 4l+2)
  </acceptance_criteria>
  <done>The D-03 BLOCKING gate is GREEN: the 2e si/sf transform suite is byte-identical to vendored libcint via int2e_spsp1 at atol=1e-12. Wave 3 unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → eval_raw (raw atm/bas/env) | Caller shell/env arrays cross into the 2e launcher/transform; only untrusted-input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-07 | Tampering/DoS | int2e_spsp1 Spinor launcher output buffer | mitigate | Fail-closed staging guard `required = di*dj*dk*dl*2` → BufferTooSmall BEFORE any write (Phase-28 CR-01; inline 2e arm bypasses launch_*_pair). OOM-safe stop, no partial writes. ASVS V5. |
| T-29-08 | Spoofing | oracle_covered claim without a real vendor reference | mitigate | NO-SILENT-SKIP asserts the vendor arm executed under the double gate; spsp1 stays oracle_covered=false until 29-06. |

Per RESEARCH §Security Domain: numerical-integral library feature, no auth/session/network/untrusted-network-input surface. Only ASVS V5 applies, satisfied by fail-closed guards. No HIGH threats.
</threat_model>

<verification>
- `cargo build --workspace --features cpu` exits 0.
- `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test si_2e_transform_parity` GREEN (the BLOCKING gate).
- D-04 invariant: Wave 3 plans (29-05/06) MUST NOT execute until this is green.
</verification>

<success_criteria>
- The 2e si/sf transform suite proven byte-identical to vendored libcint via int2e_spsp1 at atol=1e-12 on the D-02 fixture.
- NO-SILENT-SKIP green; kappa-sizing asserted.
- The D-03 structural mitigation discharged — Wave 3 unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/29-group-4-relativistic-spin-operator-integrals-spinor/29-04-SUMMARY.md`
</output>
