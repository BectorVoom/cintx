---
phase: 29-group-4-relativistic-spin-operator-integrals-spinor
plan: 03
type: execute
wave: 2
depends_on: ["02"]
files_modified:
  - crates/cintx-cubecl/src/transform/c2spinor.rs
  - crates/cintx-oracle/src/fixtures.rs
autonomous: true
requirements: [REL-03, REL-04]
must_haves:
  truths:
    - "The full 2e si/sf transform suite (cart_to_spinor_si_2e1/2e2/2e1i/2e2i + sf_2e1/2e2) exists in c2spinor.rs and compiles"
    - "The electron-2 si σ-mix apply_2d_spinor_zi (2×2 Pauli expansion on complex blocks) is transcribed from libcint cart2sph.c:4118-4186"
    - "build_kappa_spinor_2e_fixture exists: 4 spinor shells, non-square, kappa≠0 GT/LT mix, ≥1 shell nctr>1 (D-02)"
  artifacts:
    - path: "crates/cintx-cubecl/src/transform/c2spinor.rs"
      provides: "2e cart→spinor si/sf transform suite + apply_2d_spinor_zi"
      contains: "fn cart_to_spinor_si_2e1"
    - path: "crates/cintx-oracle/src/fixtures.rs"
      provides: "build_kappa_spinor_2e_fixture (D-02 4-shell adversarial 2e)"
      contains: "fn build_kappa_spinor_2e_fixture"
  key_links:
    - from: "cart_to_spinor_si_2e2"
      to: "apply_2d_spinor_zi"
      via: "electron-2 2×2 Pauli σ-mix on complex gx/gy/gz/g1 blocks"
      pattern: "apply_2d_spinor_zi"
    - from: "cart_to_spinor_si_2e1"
      to: "spinor_len"
      via: "kappa≠0 GT/LT sizing, KET→BRA transpose owned inside transform"
      pattern: "spinor_len"
---

<objective>
Wave-2 foundation (the only genuinely-new structural code in Phase 29): build the full 2e cart→spinor transform suite — `cart_to_spinor_si_2e1`, `_si_2e2`, `_si_2e1i`, `_si_2e2i`, `_sf_2e1`, `_sf_2e2` (D-01) — in c2spinor.rs by cloning the existing `cart_to_spinor_sf_4d` two-stage skeleton, plus the genuinely-novel electron-2 si σ-mix `apply_2d_spinor_zi` (the 2×2 Pauli expansion on complex blocks, transcribed verbatim from libcint), and the D-02 `build_kappa_spinor_2e_fixture`.

Purpose: cintx currently routes ALL 2e spinor through UnsupportedApi — there is no cart_to_spinor_si_4d analog. This plan creates the transform foundation REL-03/04 wire onto. Per D-03 the novel 2e layout is unproven until the 29-04 micro-test gate.
Output: 6 transform fns + apply_2d_spinor_zi + apply_2d_spinor_zi-i variants + build_kappa_spinor_2e_fixture.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-group-4-relativistic-spin-operator-integrals-spinor/29-RESEARCH.md
@.planning/phases/29-group-4-relativistic-spin-operator-integrals-spinor/29-PATTERNS.md
@.claude/skills/spike-findings-cintx/references/spinor-layout.md

<interfaces>
From crates/cintx-cubecl/src/transform/c2spinor.rs (the skeleton + analogs):
- `cart_to_spinor_sf_4d` (L1199-1337) — the existing 2e two-stage skeleton. Sizing+guards (1213-1244): di/dj/dk/dl from spinor_len; expected_cart = nci*ncj*nck*ncl → ChunkPlanFailed; required = di*dj*dk*dl*2 → BufferTooSmall. Step 1 (1246-1262): loop (l_cart,k_cart), slice each nci*ncj kl-block, call the 2D electron-1 transform into opij[nck*ncl*di*dj*2]. Step 2 (1264-1334): per (j_sp,i_sp) extract complex [nck*ncl] slice (1297-1304), apply electron-2 transform apply_2d_spinor_zf (1309-1322), zcopy_iklj store staging[(((l_sp*dk+k_sp)*dj+j_sp)*di+i_sp)*2] (1326-1332).
- `apply_2d_spinor_zf` (L1353) — the SF (scalar-only) electron-2 step; the sibling apply_2d_spinor_zi must add the σ expansion.
- `cart_to_spinor_si_2d` (L673) — the 1e si bra σ-mix body to lift into the electron-1 step.
- `cart_to_spinor_iket_si` (L457), `spinor_len` (L25).

libcint reference (transcribe verbatim, D-03):
- c2s_si_2e1 (cart2sph.c:5592): `a_bra_cart2spinor_si(...)` then `a_ket_cart2spinor(opij,...)`. gc_x/gc_y/gc_z/gc_1 = 4 contiguous blocks each nf*i_ctr*j_ctr*k_ctr*l_ctr long; loop i_ctr*j_ctr*k_ctr*l_ctr, advance each gc by nf, opij by no*OF_CMPLX.
- c2s_si_2e2 (cart2sph.c:5687): `a_bra1_cart2spinor_zi(tmp1, ox,oy,oz,o1, di, nfl*dj, k_kp, k_l)` then `a_ket1_cart2spinor(...)` then `zcopy_iklj(...)`. ox/oy/oz/o1 = 4 contiguous blocks each nop*OF_CMPLX*i_ctr*j_ctr*k_ctr*l_ctr long.
- a_bra1_cart2spinor_zi (cart2sph.c:4118-4186): the 2×2 Pauli σ·n expansion:
  v11R = g1R - gzI;  v11I = g1I + gzR;
  v12R = gyR - gxI;  v12I = gyI + gxR;
  v21R = -gyR - gxI; v21I = -gyI + gxR;
  v22R = g1R + gzI;  v22I = g1I - gzR;
  gspaR += caR*v11R + caI*v11I + cbR*v21R + cbI*v21I;  (α-I, β-R, β-I analogously)
- a_bra1_cart2spinor_zf (cart2sph.c:4188): the sf partner — only g1R/g1I, no σ mix.
- c2s_si_2e1i (cart2sph.c:5639) / c2s_si_2e2i (cart2sph.c:5752): the imaginary-ket variants (a_iket*).

From crates/cintx-oracle/src/fixtures.rs:
- `build_adversarial_spinor_fixture` (L209-300) — extend to 4 shells. nctr>1: p_coeff = [c0_p0,c0_p1,c0_p2, c1_p0,c1_p1,c1_p2] COLUMN-major env layout (L222), bas[NCTR_OF]=2 (L278).
- Phase-28 `build_kappa_spinor_fixture` (p kappa=+1 LT × d kappa=−1 GT, nctr=2) — the 1e template.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the electron-2 si σ-mix apply_2d_spinor_zi (+ imaginary sibling)</name>
  <read_first>
    - crates/cintx-cubecl/src/transform/c2spinor.rs `apply_2d_spinor_zf` (L1353) — the sf sibling (scalar-only) to mirror.
    - libcint cart2sph.c:4118-4186 (`a_bra1_cart2spinor_zi`) — the 2×2 Pauli σ·n expansion (the verbatim source); cart2sph.c:4188 (`a_bra1_cart2spinor_zf`) for the sf structure.
    - RESEARCH §"Pattern 3: The electron-2 σ-mix" (the genuinely novel layout) + PATTERNS.md §"2e si/sf transform suite".
    - skill references/spinor-layout.md (interleaved-complex `[re,im]` fastest axis).
  </read_first>
  <action>
    Add `apply_2d_spinor_zi` to c2spinor.rs as the si sibling of `apply_2d_spinor_zf` (L1353). Transcribe the 2×2 Pauli σ·n expansion VERBATIM from libcint cart2sph.c:4118-4186: given the four COMPLEX blocks gx/gy/gz/g1 (each split R/I across nket*nf*ngrids), form v11=1+iz, v12=y+ix, v21=-y+ix, v22=1-iz as `v11R=g1R-gzI; v11I=g1I+gzR; v12R=gyR-gxI; v12I=gyI+gxR; v21R=-gyR-gxI; v21I=-gyI+gxR; v22R=g1R+gzI; v22I=g1I-gzR;`, then accumulate into the α/β half-blocks `gspaR += caR*v11R + caI*v11I + cbR*v21R + cbI*v21I;` and the α-I, β-R, β-I analogs (read all four accumulation lines from libcint). Differs from `apply_2d_spinor_zf` which uses only g1R/g1I (scalar, no σ). Also add the imaginary variant (`apply_2d_spinor_zi` used by 2e1i/2e2i multiplies by i — mirror the `cart_to_spinor_iket_si` vs `cart_to_spinor_si` multiply-by-i convention `(re,im)->(-im,re)`); factor as a flag or a sibling fn at executor discretion. This is the D-03-risk piece — the 29-04 micro-test pins it.
  </action>
  <verify>
    <automated>grep -q "fn apply_2d_spinor_zi" crates/cintx-cubecl/src/transform/c2spinor.rs && cargo build -p cintx-cubecl --features cpu 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fn apply_2d_spinor_zi" crates/cintx-cubecl/src/transform/c2spinor.rs` returns 1
    - the fn body contains the four v11/v12/v21/v22 R/I assignments matching cart2sph.c:4118-4186 (grep for `v11R` and `v22R`)
    - `cargo build -p cintx-cubecl --features cpu` exits 0
  </acceptance_criteria>
  <done>apply_2d_spinor_zi transcribes the 2×2 Pauli σ·n expansion verbatim; sf sibling untouched; compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Build the 2e si/sf transform suite (cart_to_spinor_si_2e1/2e2/2e1i/2e2i + sf_2e1/2e2)</name>
  <read_first>
    - crates/cintx-cubecl/src/transform/c2spinor.rs `cart_to_spinor_sf_4d` (L1199-1337) — the two-stage skeleton; `cart_to_spinor_si_2d` (L673) bra step; the `apply_2d_spinor_zi` from Task 1; `spinor_len` (L25); zcopy_iklj store convention (L1326-1332).
    - libcint cart2sph.c: c2s_si_2e1 (L5592), c2s_si_2e2 (L5687), c2s_si_2e1i (L5639), c2s_si_2e2i (L5752), c2s_sf_2e1 (L5385), c2s_sf_2e2 (L5469).
    - PATTERNS.md §"2e si/sf transform suite" (clone the sf_4d skeleton; index conventions) + §Shared "KET→BRA transpose" + §"Spinor sizing".
  </read_first>
  <action>
    Add the 6-fn suite to c2spinor.rs (executor's discretion on internal factoring per CONTEXT Claude's Discretion). Mirror `cart_to_spinor_sf_4d` (1199) structure exactly: sizing+guards (di/dj/dk/dl from spinor_len; expected_cart = nci*ncj*nck*ncl → ChunkPlanFailed; required = di*dj*dk*dl*2 → BufferTooSmall; NO writes before guards). **cart_to_spinor_si_2e1**: electron-1 transform — swap sf_4d's Step-1 electron-1 transform for the si bra σ-mix (lift `cart_to_spinor_si_2d` body: bra `apply_bra_si_block` + ordinary `a_ket_cart2spinor` ket), producing opij ordered `<ik|lj>`; own the KET→BRA transpose INSIDE the transform (device cart KET-major). **cart_to_spinor_si_2e2**: electron-2 transform — Step-2 uses `apply_2d_spinor_zi` (Task 1) + `a_ket1_cart2spinor` + zcopy_iklj store `staging[(((l_sp*dk+k_sp)*dj+j_sp)*di+i_sp)*2]`. **_si_2e1i / _si_2e2i**: imaginary-ket variants (multiply-by-i on the ket step, mirror iket_si). **_sf_2e1 / _sf_2e2**: extract the existing sf_4d's two stages into reusable per-electron fns (the partner transforms for the non-σ electron — the 1-block scalar `apply_2d_spinor_zf` electron-2 step). Index conventions per PATTERNS: cart `cart[((l*nck+k)*ncj+j)*nci+i]` (i innermost, l outermost); opij `opij[((l*nck+k)*dj*di + j_sp*di + i_sp)*2]`. ALL sizing via spinor_len — NEVER hardcode 4l+2. Confirm the contraction loop (i_ctr*j_ctr*k_ctr*l_ctr) matches libcint's so nctr>1 coeffs are not transposed (project memory raw_nctr_coeff_transpose).
  </action>
  <verify>
    <automated>for f in si_2e1 si_2e2 si_2e1i si_2e2i sf_2e1 sf_2e2; do grep -q "fn cart_to_spinor_${f}" crates/cintx-cubecl/src/transform/c2spinor.rs || { echo "MISSING $f"; exit 1; }; done; cargo test -p cintx-cubecl --features cpu --lib c2spinor 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - all 6 fns `cart_to_spinor_{si_2e1,si_2e2,si_2e1i,si_2e2i,sf_2e1,sf_2e2}` present in c2spinor.rs
    - cart_to_spinor_si_2e2 references `apply_2d_spinor_zi`
    - each fn uses `spinor_len` for sizing; no literal `4 * l + 2` in the new fns
    - `cargo test -p cintx-cubecl --features cpu --lib c2spinor` exits 0 (existing transform unit tests stay green; new fns compile)
  </acceptance_criteria>
  <done>The 6-fn 2e si/sf suite compiles, owns its transpose, sizes via spinor_len, wires apply_2d_spinor_zi into the electron-2 si step.</done>
</task>

<task type="auto">
  <name>Task 3: Build build_kappa_spinor_2e_fixture (D-02 4-shell adversarial 2e)</name>
  <read_first>
    - crates/cintx-oracle/src/fixtures.rs `build_adversarial_spinor_fixture` (L209-300) — env/atm/bas construction to extend to 4 shells; nctr>1 column-major coeff layout L222, bas[NCTR_OF]=2 L278.
    - Phase-28 `build_kappa_spinor_fixture` (p kappa=+1 LT × d kappa=−1 GT, nctr=2) — copy the momenta.
    - PATTERNS.md §"build_kappa_spinor_2e_fixture" + RESEARCH §"Sampling Rate".
    - CONTEXT.md D-02 (4 shells, non-square, kappa≠0 GT/LT mix, ≥1 nctr>1) — Claude's Discretion on exact molecule/element/kappa.
  </read_first>
  <action>
    Add `build_kappa_spinor_2e_fixture` to fixtures.rs by extending `build_adversarial_spinor_fixture` to 4 spinor shells (a 2-electron quartet config). HARD CONSTRAINTS (D-02): exactly 4 spinor shells; NON-SQUARE (the i/j/k/l spinor dims must not all be equal — defeats transpose symmetry that hides KET→BRA omission); GENUINE kappa≠0 GT/LT mix (use `p kappa=+1` LT and `d kappa=−1` GT — copy the Phase-28 momenta, so the `2l`/`2l+2` sizing path is exercised, NOT just 4l+2); ≥1 shell with nctr>1 (copy the COLUMN-major `[c0_p0,c0_p1,c0_p2, c1_p0,c1_p1,c1_p2]` env layout from L222 + `bas[NCTR_OF]=2` from L278 — catches the coeff transpose). Exact molecule/element + kappa assignments are Claude's Discretion subject to those constraints. Also add (or assert presence of) a secondary `build_heavy_atom_spinor_fixture` realism cross-check — asserted FINITE, NOT the primary gate (RESEARCH §Sampling Rate); if it already exists, reuse it.
  </action>
  <verify>
    <automated>grep -q "fn build_kappa_spinor_2e_fixture" crates/cintx-oracle/src/fixtures.rs && cargo build -p cintx-oracle --features cpu 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fn build_kappa_spinor_2e_fixture" crates/cintx-oracle/src/fixtures.rs` returns 1
    - the fixture constructs exactly 4 spinor shells (grep/assert: 4 bas rows), at least one with `NCTR_OF`=2, with a GT/LT kappa mix (both a kappa<0 and a kappa>0 shell present)
    - a unit test asserts the fixture is non-square (not all spinor dims equal) and exercises spinor_len GT (2l+2) and LT (2l) sizing
    - `cargo build -p cintx-oracle --features cpu` exits 0
  </acceptance_criteria>
  <done>build_kappa_spinor_2e_fixture meets all D-02 constraints (4 shells, non-square, GT/LT kappa mix, nctr>1); compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → eval_raw (raw atm/bas/env) | Caller-supplied shell/env arrays cross into the 2e transform; only untrusted-input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-05 | Tampering/DoS | 2e si/sf transform output buffer | mitigate | Guards cloned from sf_4d: expected_cart = nci*ncj*nck*ncl → ChunkPlanFailed; required = di*dj*dk*dl*2 → BufferTooSmall; NO writes before guards pass (OOM-safe stop, no partial writes). ASVS V5. |
| T-29-06 | Tampering | wrong 2×2 Pauli expansion / stride in apply_2d_spinor_zi | accept | Numerical correctness, not an attack surface; caught by the D-03 29-04 micro-test at atol=1e-12 before any family wires onto it. |

Per RESEARCH §Security Domain: numerical-integral library feature, no auth/session/network/untrusted-network-input surface. Only ASVS V5 applies, satisfied by fail-closed guards. No HIGH threats.
</threat_model>

<verification>
- `cargo build -p cintx-cubecl --features cpu` and `cargo build -p cintx-oracle --features cpu` exit 0.
- `cargo test -p cintx-cubecl --features cpu --lib c2spinor` green.
- grep confirms all 6 transform fns, apply_2d_spinor_zi, and build_kappa_spinor_2e_fixture.
- NOTE: byte-identity of the 2e suite is NOT proven here — that is the 29-04 [BLOCKING] micro-test (D-03). This plan only delivers compiling structural code.
</verification>

<success_criteria>
- The 6-fn 2e si/sf transform suite exists, compiles, owns KET→BRA transpose, sizes via spinor_len.
- apply_2d_spinor_zi transcribes the 2×2 Pauli σ·n expansion verbatim from cart2sph.c:4118.
- build_kappa_spinor_2e_fixture meets all D-02 constraints.
</success_criteria>

<output>
After completion, create `.planning/phases/29-group-4-relativistic-spin-operator-integrals-spinor/29-03-SUMMARY.md`
</output>
