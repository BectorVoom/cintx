---
phase: 29-group-4-relativistic-spin-operator-integrals-spinor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cintx-cubecl/src/transform/c2spinor.rs
  - crates/cintx-ops/generated/compiled_manifest.lock.json
  - crates/cintx-ops/src/generated/api_manifest.rs
  - crates/cintx-oracle/src/vendor_ffi.rs
  - crates/cintx-oracle/build.rs
  - crates/cintx-oracle/tests/rel_1e_sigma_parity.rs
autonomous: true
requirements: [REL-01, REL-02]
must_haves:
  truths:
    - "The new 1e imaginary-ket spinor transform cart_to_spinor_si_2di exists and compiles"
    - "Manifest rows exist for every 1e Group-4 family (spsp/spnucsp/sprinvsp/srsr/srnucsr/sr/sigma), spinor-only, component_rank=1, oracle_covered=false"
    - "vendor_int1e_*_spinor FFI shims link against the real libcint 6.1.3 drivers under the double gate"
    - "The rel_1e_sigma_parity.rs test scaffold exists and compiles (RED — families not yet wired)"
  artifacts:
    - path: "crates/cintx-cubecl/src/transform/c2spinor.rs"
      provides: "cart_to_spinor_si_2di imaginary-ket bra-σ-mix 1e transform"
      contains: "fn cart_to_spinor_si_2di"
    - path: "crates/cintx-oracle/tests/rel_1e_sigma_parity.rs"
      provides: "Wave-1 1e parity test scaffold"
    - path: "crates/cintx-oracle/src/vendor_ffi.rs"
      provides: "vendor_int1e_{spsp,spnucsp,sprinvsp,srsr,srnucsr,sr,sigma}_spinor shims"
  key_links:
    - from: "crates/cintx-ops/generated/compiled_manifest.lock.json"
      to: "api_manifest.rs"
      via: "lock auto-sync; component_rank '1' on every new row"
      pattern: "int1e_(spsp|spnucsp|sprinvsp|srsr|srnucsr|sr|sigma)_spinor"
---

<objective>
Wave-1 foundation for the 1e relativistic σ families (REL-01/02): add the one structurally-new 1e transform (`cart_to_spinor_si_2di`, the imaginary-ket bra-σ-mix variant for `sr`/`sigma`), register all 1e Group-4 manifest rows (spinor-only, component_rank=1, oracle_covered=false), add the `vendor_int1e_*_spinor` FFI shims, and create the `rel_1e_sigma_parity.rs` test scaffold (Wave 0 — RED until 29-02 wires the launcher arms).

Purpose: Establish the transform + manifest + vendor + test contract so 29-02 only has to wire launcher arms and flip coverage. Per D-04, Wave 1 adds nothing structurally new beyond Phase 28 except `si_2di`.
Output: `cart_to_spinor_si_2di` in c2spinor.rs; ~7 new 1e manifest rows; ~7 vendor shims; `rel_1e_sigma_parity.rs` scaffold.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-group-4-relativistic-spin-operator-integrals-spinor/29-RESEARCH.md
@.planning/phases/29-group-4-relativistic-spin-operator-integrals-spinor/29-PATTERNS.md
@.claude/skills/spike-findings-cintx/references/spinor-layout.md

<interfaces>
<!-- Extracted from RESEARCH/PATTERNS — executor uses these directly, no codebase exploration -->

From crates/cintx-cubecl/src/transform/c2spinor.rs (existing, the templates):
- `spinor_len(l: u8, kappa: i32) -> usize` (L25): kappa<0 → 2l+2 (GT); kappa>0 → 2l (LT); kappa==0 → 4l+2. NEVER hardcode 4l+2.
- `cart_to_spinor_si_2d(...)` (L673): the real-ket 1e si transform. Stages: guards (689-717), KET→BRA transpose (719-735), bra σ-mix `apply_bra_si_block` (737-757), ket transform (Stage 2), interleaved zcopy (777-784).
- `cart_to_spinor_iket_si` (L457): the single-block imaginary-ket variant; its ket coefficient path lives at c2spinor.rs:489-514.
- `cart_to_spinor_si` (L392), `cart_to_spinor_sf_2d` (L547).

From crates/cintx-oracle/src/vendor_ffi.rs:
- `vendor_int1e_sp_spinor` (L4139-4162) — the clone template (signature + body).

From crates/cintx-ops manifest:
- `int1e_sp_spinor` row — lock at compiled_manifest.lock.json:10710-10742, generated Rust at api_manifest.rs:6256-6273. component_rank "1", complex_output true, forms ["spinor"], oracle_covered false, RepresentationSupport::new(false,false,true).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the cart_to_spinor_si_2di imaginary-ket 1e transform</name>
  <read_first>
    - crates/cintx-cubecl/src/transform/c2spinor.rs (the file being modified): `cart_to_spinor_si_2d` body at L673-784, `cart_to_spinor_iket_si` at L457 and its ket coefficient path L489-514, `spinor_len` at L25.
    - PATTERNS.md §"cart_to_spinor_si_2di" (analog: si_2d bra + iket_si ket).
    - libcint source: cart2sph.c c2s_si_1e vs c2s_si_1ei — the ONLY difference is the ket step uses `a_iket_cart2spinor` (multiply-by-i) instead of `a_ket_cart2spinor` (RESEARCH §Per-Family Map note).
  </read_first>
  <action>
    Add `fn cart_to_spinor_si_2di` to c2spinor.rs by cloning `cart_to_spinor_si_2d` (L673) VERBATIM and swapping ONLY the Stage-2 ket transform from the ordinary `apply_ket_transform` to the imaginary-ket variant (the same coefficient path `cart_to_spinor_iket_si` uses internally at L489-514; imaginary ket = (re,im)->(-im,re)). Keep IDENTICAL: the buffer guards (need = nci*ncj per gc block → ChunkPlanFailed; required = di*dj*2 → BufferTooSmall; NO writes before guards pass), the KET→BRA transpose owned inside the transform (`dst[i*ncj+j] = src[j*nci+i]` for each of the four gc_x/gc_y/gc_z/gc_1 blocks — device cart is KET-major), the Stage-1 `apply_bra_si_block` σ-mix, and the Stage-3 interleaved column-major zcopy `staging[(j*di+i)*2]=re, +1=im`. All di/dj from `spinor_len(l, kappa)` — NEVER hardcode 4l+2. This transform is consumed by `int1e_sr` and `int1e_sigma` (c2s_si_1ei path) in 29-02.
  </action>
  <verify>
    <automated>grep -q "fn cart_to_spinor_si_2di" crates/cintx-cubecl/src/transform/c2spinor.rs && cargo build -p cintx-cubecl --features cpu 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fn cart_to_spinor_si_2di" crates/cintx-cubecl/src/transform/c2spinor.rs` returns 1
    - `cargo build -p cintx-cubecl --features cpu` exits 0
    - the new fn body contains the KET→BRA transpose loop (grep for the transpose comment or `i*ncj+j`) and uses `spinor_len` for sizing (no literal `4 * l + 2` in the new fn)
  </acceptance_criteria>
  <done>cart_to_spinor_si_2di exists, compiles, owns its transpose and uses spinor_len sizing.</done>
</task>

<task type="auto">
  <name>Task 2: Register 1e Group-4 manifest rows (spinor-only, component_rank=1)</name>
  <read_first>
    - crates/cintx-ops/generated/compiled_manifest.lock.json: the `int1e_sp_spinor` row at L10710-10742 (the exact JSON shape to clone).
    - crates/cintx-ops/src/generated/api_manifest.rs: `int1e_sp_spinor` ManifestEntry at L6256-6273; `OperatorId::new(` derivation at L6275+.
    - PATTERNS.md §"Manifest rows" + §"component_rank = '1'" + §"OperatorId by symbol name".
    - RESEARCH §"Per-Family gout & Transform Map" 1e table (component_rank column) and §"component_rank guard": every Group-4 row = "1".
  </read_first>
  <action>
    Add ManifestEntry rows to compiled_manifest.lock.json (source of truth; auto-syncs both audit sides) AND the generated api_manifest.rs for: `int1e_spsp_spinor`, `int1e_spnucsp_spinor`, `int1e_sprinvsp_spinor`, `int1e_srsr_spinor`, `int1e_srnucsr_spinor`, `int1e_sr_spinor`, `int1e_sigma_spinor`. Each row: arity 2, canonical_family "1e", category "1e", complex_output true, **component_rank "1"** (the σ_x/σ_y/σ_z fold is internal to the c2s transform — NOT an output axis; a wrong rank>1 mis-strides the interleaved output), forms ["spinor"], helper_kind "operator", id.symbol "int1e_<op>_spinor", **oracle_covered false** (flipped in 29-02 after parity green), stability "stable", RepresentationSupport::new(false, false, true) — spinor-only, do NOT over-claim cart/sph (SC#5). Leave `int1e_sigma_spinor` component_rank at "1" provisionally; 29-02 Task 1 empirically confirms the output shape == di*dj*2 (Open Q1/A2) before locking. **OperatorId landmine:** after adding rows, re-grep `OperatorId::new(` and `_OPERATOR_ID` across the workspace; positional ids shift — resolve any hardcoded test const by SYMBOL NAME, not integer. Verify the existing `int1e_sp_spinor` row stays component_rank "1".
  </action>
  <verify>
    <automated>for s in spsp spnucsp sprinvsp srsr srnucsr sr sigma; do grep -q "int1e_${s}_spinor" crates/cintx-ops/generated/compiled_manifest.lock.json || { echo "MISSING $s"; exit 1; }; done; cargo build -p cintx-ops 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - all 7 symbols `int1e_{spsp,spnucsp,sprinvsp,srsr,srnucsr,sr,sigma}_spinor` present in compiled_manifest.lock.json
    - each new row has `"component_rank": "1"` and `"forms": ["spinor"]` and `"oracle_covered": false`
    - `cargo build -p cintx-ops` exits 0
    - `grep -c "int1e_sp_spinor" crates/cintx-ops/generated/compiled_manifest.lock.json` still shows the existing sp row with component_rank "1"
    - `cargo build --workspace --features cpu` exits 0 (no OperatorId const drift breakage)
  </acceptance_criteria>
  <done>All 7 1e Group-4 spinor rows registered, spinor-only, rank=1, oracle_covered=false; workspace builds; no positional-id breakage.</done>
</task>

<task type="auto">
  <name>Task 3: Add vendor_int1e_*_spinor FFI shims + bindgen allowlist + rel_1e parity scaffold</name>
  <read_first>
    - crates/cintx-oracle/src/vendor_ffi.rs: `vendor_int1e_sp_spinor` at L4139-4162 (clone template).
    - crates/cintx-oracle/build.rs: the `allowlist_function(...)` regex at L374 (|-joined list; `int1e_sp_spinor` already present).
    - crates/cintx-oracle/tests/si_transform_parity.rs (full file, ~379 lines) — the test scaffold template: collect_cintx_*/collect_vendor_* (140-172), count_mismatches (178-201), assert_any_nonzero (204-210), test_kappa_sizing_non_4l_plus_2 (219-237), primary gate (271-293), test_no_silent_skip (327-363), non-vendor smoke (369-378).
    - PATTERNS.md §"vendor_int1e_*_spinor shims" + §"rel_1e_sigma_parity.rs".
    - RESEARCH §"Don't Hand-Roll": symbols ALREADY in cint_funcs.h → no suppl-header, only .file() (intor3.c already built) + allowlist + Rust wrappers.
  </read_first>
  <action>
    (a) In vendor_ffi.rs, clone `vendor_int1e_sp_spinor` (L4139) for each of `int1e_{spsp,spnucsp,sprinvsp,srsr,srnucsr,sr,sigma}_spinor`, changing only the `ffi::int1e_X_spinor` symbol. Signature: `pub fn vendor_int1e_X_spinor(out, shls: &[i32;2], atm, natm, bas, nbas, env) -> i32` calling `ffi::int1e_X_spinor(out.as_mut_ptr(), ptr::null_mut(), shls.as_ptr() as *mut i32, atm.as_ptr() as *mut i32, natm, bas.as_ptr() as *mut i32, nbas, env.as_ptr() as *mut f64, ptr::null_mut(), ptr::null_mut())`. `out` sized `ni_sp*nj_sp*2` via `vendor_CINTcgto_spinor` (component_rank=1 for all). (b) In build.rs L374, append all 7 new `int1e_*_spinor` symbol names to the `allowlist_function` |-regex (intor3.c is already in the build — NO .file() change for 1e). (c) Create `crates/cintx-oracle/tests/rel_1e_sigma_parity.rs` by cloning `si_transform_parity.rs` structure: `collect_cintx_<fam>`/`collect_vendor_<fam>` per family, count_mismatches, assert_any_nonzero, test_kappa_sizing_non_4l_plus_2, the PRIMARY GATE `#[cfg(has_vendor_libcint)] #[cfg(feature="cpu")]` byte-identity at ATOL=1e-12 on `build_kappa_spinor_fixture` (Phase-28 1e: p kappa=+1 LT nctr=2 × d kappa=−1 GT), the test_no_silent_skip assertion (vendor arm MUST run + nonzero; assert manifest oracle_covered matches post-flip expectation), and the non-vendor smoke arm. Test bodies for the families will be RED (UnsupportedApi) until 29-02 wires the launcher arms — leave the family-call sites in place with a TODO referencing 29-02; the scaffold must COMPILE.
  </action>
  <verify>
    <automated>for s in spsp spnucsp sprinvsp srsr srnucsr sr sigma; do grep -q "vendor_int1e_${s}_spinor" crates/cintx-oracle/src/vendor_ffi.rs || { echo "MISSING vendor $s"; exit 1; }; done; test -f crates/cintx-oracle/tests/rel_1e_sigma_parity.rs && cargo build -p cintx-oracle --features cpu --tests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - all 7 `vendor_int1e_{spsp,spnucsp,sprinvsp,srsr,srnucsr,sr,sigma}_spinor` present in vendor_ffi.rs
    - all 7 symbol names appended to the `allowlist_function` regex in build.rs (`grep -c "int1e_spsp_spinor" crates/cintx-oracle/build.rs` ≥ 1)
    - `crates/cintx-oracle/tests/rel_1e_sigma_parity.rs` exists, contains `ATOL` and `test_no_silent_skip` and `build_kappa_spinor_fixture`
    - `cargo build -p cintx-oracle --features cpu --tests` exits 0 (scaffold compiles)
  </acceptance_criteria>
  <done>7 vendor shims added + allowlisted; rel_1e_sigma_parity.rs compiles as a RED scaffold ready for 29-02 wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → eval_raw (raw atm/bas/env) | Caller-supplied shell/env arrays cross into the kernel/transform; the only untrusted-input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Tampering/DoS | cart_to_spinor_si_2di output buffer | mitigate | Fail-closed guards cloned from si_2d: `need = nci*ncj` per gc block → ChunkPlanFailed; `required = di*dj*2` → BufferTooSmall; NO writes before guards pass (OOM-safe stop, no partial writes). ASVS V5. |
| T-29-02 | Tampering | manifest component_rank | accept | Numerical-library metadata; wrong rank caught by the atol=1e-12 vendor gate (29-02), not an external attack surface. |

Per RESEARCH §Security Domain: numerical-integral library feature, no auth/session/network/untrusted-network-input surface. Only ASVS V5 (Input Validation) applies, satisfied by the existing fail-closed buffer guards. No HIGH threats.
</threat_model>

<verification>
- `cargo build --workspace --features cpu` exits 0.
- `grep` confirms `cart_to_spinor_si_2di`, all 7 manifest rows, all 7 vendor shims, the allowlist entries, and the rel_1e_sigma_parity.rs scaffold.
- No `OperatorId::new(<int>)` test const points at the wrong family after row insertion (workspace build green proves it).
</verification>

<success_criteria>
- cart_to_spinor_si_2di compiles, owns its KET→BRA transpose, sizes via spinor_len.
- 7 1e Group-4 manifest rows registered spinor-only, component_rank=1, oracle_covered=false.
- 7 vendor_int1e_*_spinor shims link under the double gate; allowlist updated.
- rel_1e_sigma_parity.rs scaffold compiles (RED — wired in 29-02).
</success_criteria>

<output>
After completion, create `.planning/phases/29-group-4-relativistic-spin-operator-integrals-spinor/29-01-SUMMARY.md`
</output>
