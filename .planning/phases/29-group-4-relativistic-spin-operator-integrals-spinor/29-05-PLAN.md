---
phase: 29-group-4-relativistic-spin-operator-integrals-spinor
plan: 05
type: execute
wave: 3
depends_on: ["04"]
files_modified:
  - crates/cintx-oracle/build.rs
  - crates/cintx-oracle/src/vendor_ffi.rs
  - crates/cintx-ops/generated/compiled_manifest.lock.json
  - crates/cintx-ops/src/generated/api_manifest.rs
  - crates/cintx-oracle/tests/rel_2e_sigma_parity.rs
autonomous: true
requirements: [REL-03, REL-04]
must_haves:
  truths:
    - "gaunt1.c and dkb.c are wired into the oracle vendor build so REL-04 ssp/sps/vsp/spv drivers compile and link"
    - "Manifest rows exist for every remaining 2e Group-4 family (REL-03/04), spinor-only, component_rank=1, oracle_covered=false"
    - "vendor_int2e_*_spinor shims link against the real libcint 6.1.3 2e drivers under the double gate"
    - "rel_2e_sigma_parity.rs test scaffold exists and compiles (RED — families wired in 29-06)"
  artifacts:
    - path: "crates/cintx-oracle/build.rs"
      provides: "gaunt1.c + dkb.c .file() entries + allowlist regex (REL-04 BLOCKING)"
      contains: "gaunt1.c"
    - path: "crates/cintx-oracle/src/vendor_ffi.rs"
      provides: "vendor_int2e_{srsr1,spsp1spsp2,srsr1srsr2,ssp1ssp2,sps1sps2,vsp1,spv1,...}_spinor shims"
    - path: "crates/cintx-oracle/tests/rel_2e_sigma_parity.rs"
      provides: "REL-03/04 parity test scaffold"
  key_links:
    - from: "crates/cintx-oracle/build.rs"
      to: "src/autocode/gaunt1.c + src/autocode/dkb.c"
      via: ".file() chain after intor4.c + allowlist_function regex"
      pattern: "autocode/(gaunt1|dkb)\\.c"
---

<objective>
Wave-3 foundation for the remaining 2e Group-4 families (REL-03/04): the **[BLOCKING] build.rs change** adding `gaunt1.c` (ssp/sps drivers) + `dkb.c` (vsp/spv drivers) to the oracle vendor build — without which REL-04 has NO vendor reference (CONTEXT.md's "no build change" claim is corrected by RESEARCH). Plus register all remaining 2e manifest rows, add the `vendor_int2e_*_spinor` shims, and create the `rel_2e_sigma_parity.rs` scaffold (RED until 29-06).

Purpose: REL-04 ssp/sps live in gaunt1.c, vsp/spv in dkb.c — NEITHER is in build.rs (RESEARCH Pitfall 1, grep-confirmed). This is the load-bearing build fix. REL-03 (intor4.c) is already wired.
Output: build.rs +2 .file() + allowlist; ~15 2e manifest rows; ~15 vendor shims; rel_2e_sigma_parity.rs scaffold.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-group-4-relativistic-spin-operator-integrals-spinor/29-RESEARCH.md
@.planning/phases/29-group-4-relativistic-spin-operator-integrals-spinor/29-PATTERNS.md
@.planning/phases/29-group-4-relativistic-spin-operator-integrals-spinor/29-04-SUMMARY.md

<interfaces>
From crates/cintx-oracle/build.rs:
- The `.file()` chain at L217-265 (intor4.c at L229). The `allowlist_function(...)` regex at L374 (|-joined).
- ADD after intor4.c: `.file(libcint_root.join("src/autocode/gaunt1.c"))` and `.file(libcint_root.join("src/autocode/dkb.c"))`. Both use the same include set as intor4.c (no suppl-header; all symbols in cint_funcs.h). REL-03 intor4.c ALREADY wired.

From crates/cintx-oracle/src/vendor_ffi.rs:
- `vendor_int1e_sp_spinor` (L4139) + the `vendor_int2e_spsp1_spinor` from 29-04 — clone template (shls &[i32;4], out ni*nj*nk*nl*2).

2e family → transform pairing + source file (RESEARCH §Per-Family Map 2e table):
- int2e_spsp1 (intor4.c:85) — c2s_si_2e1+c2s_sf_2e2 — [spsp1 row already exists from 29-04]
- int2e_srsr1 (intor4.c:349) — c2s_si_2e1+c2s_sf_2e2 — intor4.c (wired)
- int2e_spsp1spsp2 (intor4.c:277) — c2s_si_2e1+c2s_si_2e2 — intor4.c
- int2e_srsr1srsr2 (intor4.c:541) — c2s_si_2e1+c2s_si_2e2 — intor4.c
- int2e_ssp1ssp2 (gaunt1.c:111) — c2s_si_2e1i+c2s_si_2e2i — gaunt1.c (NEEDS BUILD)
- int2e_ssp1sps2 (gaunt1.c:209), int2e_sps1ssp2 (gaunt1.c:307), int2e_sps1sps2 (gaunt1.c:405) — c2s_si_2e1i+c2s_si_2e2i — gaunt1.c
- int2e_spv1 (dkb.c:249), int2e_vsp1 (dkb.c:333) — c2s_si_2e1+c2s_sf_2e2 — dkb.c (NEEDS BUILD)
- int2e_spv1spv2 (dkb.c:501), int2e_vsp1spv2 (dkb.c:597), int2e_spv1vsp2 (dkb.c:693), int2e_vsp1vsp2 (dkb.c:789), int2e_spv1spsp2 (dkb.c:911), int2e_vsp1spsp2 (dkb.c:1033) — c2s_si_2e1+c2s_si_2e2 — dkb.c
All component_rank "1".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Wire gaunt1.c + dkb.c into the oracle vendor build + allowlist</name>
  <read_first>
    - crates/cintx-oracle/build.rs: the `.file()` chain L217-265 (intor4.c at L229), the `allowlist_function` regex L374.
    - RESEARCH §"Pitfall 1: REL-04 source files not in oracle build (BLOCKING)" + §Environment Availability (gaunt1.c/dkb.c present, NOT wired, no fallback) + Assumption A3 (compile cleanly with the existing include set).
    - PATTERNS.md §"build.rs — add gaunt1.c + dkb.c".
  </read_first>
  <action>
    In crates/cintx-oracle/build.rs, add to the `.file()` chain immediately after the intor4.c entry (L229): `.file(libcint_root.join("src/autocode/gaunt1.c"))` (provides int2e_ssp1ssp2/ssp1sps2/sps1ssp2/sps1sps2) and `.file(libcint_root.join("src/autocode/dkb.c"))` (provides int2e_spv1/vsp1/spv1spv2/vsp1spv2/spv1vsp2/vsp1vsp2/spv1spsp2/vsp1spsp2). Both use the same include set + cc flags as intor4.c — no suppl-header, all symbols in cint_funcs.h (verified RESEARCH). Append ALL the new 2e `int2e_*_spinor` symbol names (REL-03 srsr1/spsp1spsp2/srsr1srsr2 + REL-04 ssp1ssp2/ssp1sps2/sps1ssp2/sps1sps2/spv1/vsp1/spv1spv2/vsp1spv2/spv1vsp2/vsp1vsp2/spv1spsp2/vsp1spsp2) to the `allowlist_function` regex at L374. Confirm the vendor build still compiles under `CINTX_ORACLE_BUILD_VENDOR=1`. **This is the REL-04 enablement gate — without it the vendor shims have no symbol to link (undefined-symbol link error).** CONTEXT.md's "intor4.c already wired → no build change" is correct ONLY for REL-03; REL-04 REQUIRES this.
  </action>
  <verify>
    <automated>grep -q "autocode/gaunt1.c" crates/cintx-oracle/build.rs && grep -q "autocode/dkb.c" crates/cintx-oracle/build.rs && CINTX_ORACLE_BUILD_VENDOR=1 cargo build -p cintx-oracle --features cpu 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "autocode/gaunt1.c" crates/cintx-oracle/build.rs` ≥ 1 AND `grep -c "autocode/dkb.c" crates/cintx-oracle/build.rs` ≥ 1
    - the allowlist regex contains at least `int2e_ssp1ssp2_spinor` and `int2e_vsp1_spinor`
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo build -p cintx-oracle --features cpu` exits 0 (vendor build compiles gaunt1.c + dkb.c)
  </acceptance_criteria>
  <done>gaunt1.c + dkb.c wired into the vendor build; allowlist updated; vendor build compiles. REL-04 has a vendor reference.</done>
</task>

<task type="auto">
  <name>Task 2: Register remaining 2e Group-4 manifest rows (spinor-only, component_rank=1)</name>
  <read_first>
    - crates/cintx-ops/generated/compiled_manifest.lock.json: the `int2e_spsp1_spinor` row (added in 29-04) as the shape.
    - crates/cintx-ops/src/generated/api_manifest.rs: the spsp1 ManifestEntry from 29-04; OperatorId derivation.
    - RESEARCH §Per-Family Map 2e table (all component_rank "1") + PATTERNS.md §"Manifest rows" + §"component_rank = '1'" + §"OperatorId by symbol name".
  </read_first>
  <action>
    Add ManifestEntry rows (lock + api_manifest.rs) for every remaining 2e Group-4 family: REL-03 `int2e_{srsr1,spsp1spsp2,srsr1srsr2}_spinor`; REL-04 `int2e_{ssp1ssp2,ssp1sps2,sps1ssp2,sps1sps2,spv1,vsp1,spv1spv2,vsp1spv2,spv1vsp2,vsp1vsp2,spv1spsp2,vsp1spsp2}_spinor`. Each: arity 4, canonical_family "2e", category "2e", complex_output true, **component_rank "1"** (σ-component fold is internal to the c2s_si_2e transform, NOT an output axis), forms ["spinor"], helper_kind "operator", id.symbol "int2e_<op>_spinor", oracle_covered false (flipped per-family in 29-06 after parity green), stability "stable", RepresentationSupport::new(false,false,true) spinor-only. After adding ~15 rows, re-grep `OperatorId::new(` / `_OPERATOR_ID` across the workspace — positional ids shift; resolve any hardcoded test const by SYMBOL NAME. Confirm the existing int1e_sp + int2e_spsp1 rows stay component_rank "1".
  </action>
  <verify>
    <automated>for s in srsr1 spsp1spsp2 srsr1srsr2 ssp1ssp2 ssp1sps2 sps1ssp2 sps1sps2 spv1 vsp1 spv1spv2 vsp1spv2 spv1vsp2 vsp1vsp2 spv1spsp2 vsp1spsp2; do grep -q "int2e_${s}_spinor" crates/cintx-ops/generated/compiled_manifest.lock.json || { echo "MISSING $s"; exit 1; }; done; cargo build --workspace --features cpu 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - all 15 remaining 2e symbols present in compiled_manifest.lock.json
    - each new row has `"component_rank": "1"`, `"forms": ["spinor"]`, `"oracle_covered": false`
    - `cargo build --workspace --features cpu` exits 0 (no OperatorId const drift)
  </acceptance_criteria>
  <done>All remaining 2e Group-4 spinor rows registered spinor-only, rank=1, oracle_covered=false; workspace builds; no positional-id breakage.</done>
</task>

<task type="auto">
  <name>Task 3: Add vendor_int2e_*_spinor shims + rel_2e_sigma_parity.rs scaffold</name>
  <read_first>
    - crates/cintx-oracle/src/vendor_ffi.rs `vendor_int2e_spsp1_spinor` (29-04) — clone template (shls &[i32;4]).
    - crates/cintx-oracle/tests/si_transform_parity.rs (full file) + the 29-04 si_2e_transform_parity.rs — the scaffold structure.
    - PATTERNS.md §"vendor_int2e_*_spinor shims" + §"rel_2e_sigma_parity.rs" + §"NO-SILENT-SKIP parity assertion".
    - `build_kappa_spinor_2e_fixture` (29-03).
  </read_first>
  <action>
    (a) Clone `vendor_int2e_spsp1_spinor` for each remaining 2e family `int2e_{srsr1,spsp1spsp2,srsr1srsr2,ssp1ssp2,ssp1sps2,sps1ssp2,sps1sps2,spv1,vsp1,spv1spv2,vsp1spv2,spv1vsp2,vsp1vsp2,spv1spsp2,vsp1spsp2}_spinor`, changing only the `ffi::int2e_X_spinor` symbol; shls &[i32;4], out sized ni*nj*nk*nl*2 via vendor_CINTcgto_spinor. (b) Create `crates/cintx-oracle/tests/rel_2e_sigma_parity.rs` cloning the si_transform_parity.rs structure: per-family collect_cintx/collect_vendor, count_mismatches, assert_any_nonzero, the PRIMARY GATE byte-identity at ATOL=1e-12 on `build_kappa_spinor_2e_fixture`, the kappa-sizing assertion, test_no_silent_skip (reads MANIFEST_ENTRIES at runtime, asserts the post-flip oracle_covered set + each vendor arm executed under the double gate FAIL-not-skip), and the non-vendor smoke arm. Family-call sites are RED (UnsupportedApi) until 29-06 wires the launcher arms — leave them with a TODO referencing 29-06; the scaffold must COMPILE.
  </action>
  <verify>
    <automated>for s in srsr1 spsp1spsp2 srsr1srsr2 ssp1ssp2 sps1sps2 spv1 vsp1; do grep -q "vendor_int2e_${s}_spinor" crates/cintx-oracle/src/vendor_ffi.rs || { echo "MISSING vendor $s"; exit 1; }; done; test -f crates/cintx-oracle/tests/rel_2e_sigma_parity.rs && cargo build -p cintx-oracle --features cpu --tests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - all 15 `vendor_int2e_*_spinor` shims present in vendor_ffi.rs
    - `crates/cintx-oracle/tests/rel_2e_sigma_parity.rs` exists, contains `ATOL`, `test_no_silent_skip`, `build_kappa_spinor_2e_fixture`
    - `cargo build -p cintx-oracle --features cpu --tests` exits 0 (scaffold compiles)
  </acceptance_criteria>
  <done>15 2e vendor shims added; rel_2e_sigma_parity.rs compiles as a RED scaffold ready for 29-06 wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → eval_raw (raw atm/bas/env) | Caller shell/env arrays cross into the 2e transform; only untrusted-input surface. |
| vendored C build (gaunt1.c/dkb.c) | C sources compiled into the oracle harness behind CINTX_ORACLE_BUILD_VENDOR=1; test-only, not shipped in the library. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-09 | Tampering | adding C sources to the vendor build | accept | gaunt1.c/dkb.c are frozen libcint 6.1.3 sources, compiled only under the test-gated CINTX_ORACLE_BUILD_VENDOR flag; not part of the shipped library surface. |
| T-29-10 | Spoofing | oracle_covered claim without a real linked vendor symbol | mitigate | Task 1 ensures the symbols link; NO-SILENT-SKIP (Task 3) asserts the vendor arm executed; rows stay oracle_covered=false until 29-06. |

Per RESEARCH §Security Domain: numerical-integral library feature, no auth/session/network/untrusted-network-input surface. Only ASVS V5 applies, satisfied by fail-closed guards. No HIGH threats.
</threat_model>

<verification>
- `CINTX_ORACLE_BUILD_VENDOR=1 cargo build -p cintx-oracle --features cpu` exits 0 (gaunt1.c + dkb.c compile + link).
- `cargo build --workspace --features cpu` exits 0; all 15 remaining 2e manifest rows + 15 vendor shims present.
- rel_2e_sigma_parity.rs scaffold compiles (RED — wired in 29-06).
</verification>

<success_criteria>
- gaunt1.c + dkb.c wired into the vendor build (REL-04 BLOCKING enablement); allowlist updated.
- All remaining 2e Group-4 manifest rows registered spinor-only, component_rank=1, oracle_covered=false.
- 15 vendor_int2e_*_spinor shims link under the double gate.
- rel_2e_sigma_parity.rs scaffold compiles.
</success_criteria>

<output>
After completion, create `.planning/phases/29-group-4-relativistic-spin-operator-integrals-spinor/29-05-SUMMARY.md`
</output>
