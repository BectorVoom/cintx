---
phase: 29-group-4-relativistic-spin-operator-integrals-spinor
plan: 02
type: execute
wave: 1
depends_on: ["01"]
files_modified:
  - crates/cintx-cubecl/src/kernels/one_electron.rs
  - crates/cintx-cubecl/src/kernels/sigma_p.rs
  - crates/cintx-ops/generated/compiled_manifest.lock.json
  - crates/cintx-oracle/tests/rel_1e_sigma_parity.rs
autonomous: true
requirements: [REL-01, REL-02]
must_haves:
  truths:
    - "int1e_spsp/spnucsp/sprinvsp/srsr/srnucsr/sr/sigma evaluate through the correct per-family transform (sf_2d / si_2d / si_2di) and match vendored libcint at atol=1e-12 (spinor)"
    - "int1e_sp is flipped oracle_covered=true and matches at atol=1e-12 (spinor)"
    - "int1e_sigma output rank is empirically resolved (component_rank confirmed) before its manifest row is locked"
    - "every 1e Group-4 vendor parity test runs non-skipped under the double gate (NO-SILENT-SKIP)"
  artifacts:
    - path: "crates/cintx-cubecl/src/kernels/one_electron.rs"
      provides: "1e σ Spinor launcher arms for all 7 new families"
      contains: "cart_to_spinor_si_2di"
    - path: "crates/cintx-oracle/tests/rel_1e_sigma_parity.rs"
      provides: "GREEN REL-01/02 byte-identity parity on the kappa fixture"
  key_links:
    - from: "crates/cintx-cubecl/src/kernels/one_electron.rs"
      to: "cart_to_spinor_si_2d / si_2di / sf_2d"
      via: "per-family transform selection per RESEARCH map"
      pattern: "cart_to_spinor_si_2d|cart_to_spinor_si_2di|cart_to_spinor_sf_2d"
    - from: "crates/cintx-ops/generated/compiled_manifest.lock.json"
      to: "oracle_covered=true"
      via: "flip after parity green, spinor-only"
      pattern: "int1e_(sp|spsp|spnucsp|sprinvsp|srsr|srnucsr|sr|sigma)_spinor"
---

<objective>
Wave-1 completion (REL-01/02): wire the 1e σ Spinor launcher arms for all 7 new families onto the correct per-family transform, empirically resolve `int1e_sigma`'s output rank, flip `int1e_sp` (the Phase-28 proof vehicle), and turn `rel_1e_sigma_parity.rs` GREEN at atol=1e-12 on the kappa fixture. Flip `oracle_covered=true` spinor-only per family after each passes.

Purpose: Close REL-01/02 on the existing Phase-28 si_2d + sigma_p.rs foundation. Lowest-risk wave (D-04) — nothing structurally new beyond 29-01's si_2di.
Output: 7 launcher arms + sp flip; sigma rank resolved; rel_1e_sigma_parity.rs green; 8 rows flipped oracle_covered=true.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-group-4-relativistic-spin-operator-integrals-spinor/29-RESEARCH.md
@.planning/phases/29-group-4-relativistic-spin-operator-integrals-spinor/29-PATTERNS.md
@.planning/phases/29-group-4-relativistic-spin-operator-integrals-spinor/29-01-SUMMARY.md

<interfaces>
From crates/cintx-cubecl/src/kernels/one_electron.rs:
- The live `int1e_sp` Spinor arm at L10495-10595 — the template. Body: extract n_prim/n_ctr/exps/coeff (10504-10515); device σ·p assembler `run_sigma_p_on_backend(backend, tensor_rank, ...)` (10518-10533); s/p norm `common_fac_sp(li)*common_fac_sp(lj)` (10535-10541); `di = spinor_len(li, kappa_i); ni_sp = n_ctr_i*di` (10545-10548); FAIL-CLOSED STAGING GUARD `required = ni_sp*nj_sp*2` → BufferTooSmall (10550-10560); per-(ci,cj) fold + scatter via `cart_to_spinor_si_2d` (10566-end), NO launcher transpose.
- crates/cintx-cubecl/src/kernels/sigma_p.rs: `launch_int1e_sp_spinor_pair` (L565), generic σ·p `#[cube]` assembler (tensor_rank param).

Per-Family Transform Map (RESEARCH §, AUTHORITATIVE):
- int1e_sp — gout rank 4 — cart_to_spinor_si_2d — flip only
- int1e_spsp — gout rank 1 (+s0+s4+s8) — cart_to_spinor_sf_2d (c2s_sf_1e, scalar ∇², NOT si!)
- int1e_spnucsp — gout rank 4 (+s5-s7,+s6-s2,+s1-s3,+s0+s4+s8) — cart_to_spinor_si_2d (nuc 2-center)
- int1e_sprinvsp — gout rank 4 (same ordering) — cart_to_spinor_si_2d (rinv 1-center)
- int1e_srsr — gout rank 4 (same ordering) — cart_to_spinor_si_2d
- int1e_srnucsr — gout rank 4 (same ordering) — cart_to_spinor_si_2d
- int1e_sr — gout rank 4 (-s0,-s1,-s2,0) — cart_to_spinor_si_2di (imaginary-ket, from 29-01)
- int1e_sigma — gout rank 12 (gout[n*12+..]) — cart_to_spinor_si_2di — rank empirically resolved (Task 1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve int1e_sigma output rank empirically, then lock its manifest row</name>
  <read_first>
    - 29-VALIDATION.md §"Manual-Only Verifications" + RESEARCH §"Open Questions" Q1 / Assumption A2 (sigma gout is gout[n*12+..] = 3 σ-components × 4 cart-blocks; routes through c2s_si_1ei; ng[7]=3).
    - crates/cintx-oracle/src/vendor_ffi.rs `vendor_int1e_sigma_spinor` (added in 29-01).
    - crates/cintx-cubecl/src/transform/c2spinor.rs `spinor_len` (L25).
    - libcint intor3.c sigma driver L18/81 (c2s_si_1ei).
  </read_first>
  <action>
    Before wiring `int1e_sigma`, call `vendor_int1e_sigma_spinor` on the `build_kappa_spinor_fixture` shells and measure the returned output element count against `di*dj*2` where `di=spinor_len(li,kappa_i)`, `dj=spinor_len(lj,kappa_j)`. If vendor output length == `di*dj*2`, sigma is a single fused spinor matrix → keep `component_rank "1"` (the 3 σ-components folded by c2s_si_1ei). If == `3*di*dj*2`, it is 3 stacked σ-matrices → set `component_rank "3"` in compiled_manifest.lock.json and structure the Task-2 launcher arm with a 3-component loop. Record the measured length in the SUMMARY. Strong prior from the gout structure + the existing sp pattern is "1".
  </action>
  <verify>
    <automated>CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test rel_1e_sigma_parity sigma_rank 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - a test asserts the measured `vendor_int1e_sigma_spinor` output length equals exactly `di*dj*2 * component_rank` for the locked component_rank
    - the `int1e_sigma_spinor` row in compiled_manifest.lock.json carries the empirically-confirmed `component_rank`
    - the SUMMARY records the measured output length and the resolved rank
  </acceptance_criteria>
  <done>int1e_sigma component_rank set from measured vendor output shape, not assumption; documented in SUMMARY.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the 7 1e σ Spinor launcher arms with per-family transform selection</name>
  <read_first>
    - crates/cintx-cubecl/src/kernels/one_electron.rs: the `int1e_sp` arm L10495-10595 (clone shape), the staging guard L10550-10560.
    - crates/cintx-cubecl/src/kernels/sigma_p.rs: `launch_int1e_sp_spinor_pair` (L565), `run_sigma_p_on_backend`.
    - crates/cintx-cubecl/src/kernels/center_4c1e.rs `test_device_matches_host_spsp` (L1878) — the σ·p-on-both-sides cart gout for spsp/srsr (Open Q2: verify the both-side assembler vs this harness).
    - PATTERNS.md §"1e σ Spinor launcher arms" (per-family adaptations + transform selection).
    - RESEARCH §Per-Family Map + intor3.c gout lines (spsp L411, spnucsp L537, sprinvsp L607, srsr L229, srnucsr L677, sr L285, sigma L81).
  </read_first>
  <behavior>
    - int1e_spsp on the kappa fixture → byte-identical to vendor_int1e_spsp_spinor at atol=1e-12 (routes through cart_to_spinor_sf_2d — scalar ∇² contraction, NOT si)
    - int1e_spnucsp, int1e_sprinvsp, int1e_srsr, int1e_srnucsr → byte-identical via cart_to_spinor_si_2d at atol=1e-12
    - int1e_sr, int1e_sigma → byte-identical via cart_to_spinor_si_2di (imaginary-ket) at atol=1e-12
    - each arm sizes via spinor_len (kappa≠0 GT/LT), never hardcoded 4l+2; non-square fixture passes (transpose owned in transform)
  </behavior>
  <action>
    Add a Spinor launcher arm in one_electron.rs for each of `int1e_{spsp,spnucsp,sprinvsp,srsr,srnucsr,sr,sigma}` by cloning the `int1e_sp` arm body (10504-10595). Per-family adaptations: (1) Transform selection — `spsp`→`cart_to_spinor_sf_2d` (c2s_sf_1e, scalar — two σ·p contract to ∇², gout rank 1 `+s0+s4+s8`); `spnucsp`/`sprinvsp`/`srsr`/`srnucsr`→`cart_to_spinor_si_2d` (gout rank 4 `+s5-s7,+s6-s2,+s1-s3,+s0+s4+s8`); `sr`→`cart_to_spinor_si_2di` (gout rank 4 `-s0,-s1,-s2,0`); `sigma`→`cart_to_spinor_si_2di` (gout rank 12, with a component loop ONLY if Task 1 measured rank 3). (2) `tensor_rank` arg to `run_sigma_p_on_backend` per the family gout (sp/sr/sigma are σ-on-bra; spsp/spnucsp/sprinvsp/srsr/srnucsr are σ-on-both / σ·p² — confirm the cart-block extent against `test_device_matches_host_spsp` and intor3.c; if the both-side gout needs an assembler sigma_p.rs cannot express, build a thin 1e both-side σ·p variant in sigma_p.rs). (3) **Each arm gets its OWN fail-closed staging guard** `required = ni_sp*nj_sp*2` BEFORE any write (Phase-28 CR-01 — copy 10550-10560 verbatim into every arm; inline arms bypass any launch_*_pair guard). (4) s/p norm `common_fac_sp(li)*common_fac_sp(lj)`. NO launcher transpose — the transform owns KET→BRA. Confirm gout component ordering for each family against intor3.c verbatim. Suspect the CubeCL CpuRuntime ~1e-11 FP-env side effect (project memory) before chasing kernel numerics if the flat atol=1e-12 gate is marginally off — mitigate by keeping that band host or batching launches.
  </action>
  <verify>
    <automated>CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test rel_1e_sigma_parity 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - one_electron.rs references `cart_to_spinor_sf_2d` (spsp), `cart_to_spinor_si_2d` (spnucsp/sprinvsp/srsr/srnucsr), and `cart_to_spinor_si_2di` (sr/sigma)
    - `grep -v '^#' crates/cintx-cubecl/src/kernels/one_electron.rs | grep -c "BufferTooSmall"` increased by ≥7 vs the 29-01 baseline (one staging guard per new arm)
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test rel_1e_sigma_parity test_spsp test_spnucsp test_sprinvsp test_srsr test_srnucsr test_sr test_sigma` exits 0 (byte-identity atol=1e-12)
    - the kappa-sizing test asserts GT/LT values (no hardcoded 4l+2)
  </acceptance_criteria>
  <done>All 7 1e σ families byte-identical to vendor at atol=1e-12 via the correct per-family transform; each arm fail-closed guarded.</done>
</task>

<task type="auto">
  <name>Task 3: Flip int1e_sp + 7 new families oracle_covered=true (spinor-only); NO-SILENT-SKIP + manifest-audit green</name>
  <read_first>
    - xtask/src/oracle_covered_update.rs — the flip mechanism + SC#4 skipped-fixture-flip-refusal guard (Phase 28).
    - crates/cintx-oracle/tests/rel_1e_sigma_parity.rs `test_no_silent_skip` (cloned from si_transform_parity.rs:327-363 in 29-01).
    - PATTERNS.md §"NO-SILENT-SKIP parity assertion" + §"component_rank = '1'" + §"OperatorId by symbol name".
    - 29-01-SUMMARY.md (the existing int1e_sp arm rejects non-Spinor at one_electron.rs:10496-10502 — keep that; only flip the manifest).
  </read_first>
  <action>
    For `int1e_sp` and each of the 7 new families, flip `oracle_covered` false→true in compiled_manifest.lock.json **spinor-only** (RepresentationSupport stays sph=false,cart=false,spinor=true — do NOT over-claim cart/sph σ intermediates, SC#5), ONLY after its parity test is green (Task 2). Run `xtask oracle-covered-update`; confirm the SC#4 guard refuses none (each fixture ran non-skipped). Confirm `test_no_silent_skip` reads the post-flip FLIPPED set from MANIFEST_ENTRIES at runtime and asserts each vendor arm executed (FAIL not skip) under the double gate. Re-grep `OperatorId::new(` / `_OPERATOR_ID` for positional drift (resolve by symbol name). Run `manifest-audit` — must be green (both audit sides derive from the lock).
  </action>
  <verify>
    <automated>cargo run -p xtask -- manifest-audit 2>&1 | tail -5; CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test rel_1e_sigma_parity test_no_silent_skip 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `int1e_{sp,spsp,spnucsp,sprinvsp,srsr,srnucsr,sr,sigma}_spinor` all show `"oracle_covered": true` and spinor-only support in compiled_manifest.lock.json
    - `cargo run -p xtask -- manifest-audit` exits 0
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test rel_1e_sigma_parity test_no_silent_skip` exits 0
    - `cargo build --workspace --features cpu` exits 0 (no OperatorId const drift)
  </acceptance_criteria>
  <done>8 rows flipped spinor-only oracle_covered=true; NO-SILENT-SKIP green; manifest-audit green; no positional-id breakage. Wave 1 gated GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → eval_raw (raw atm/bas/env) | Caller-supplied shell/env arrays cross into the kernel/transform; only untrusted-input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03 | Tampering/DoS | each 1e σ Spinor launcher arm output buffer | mitigate | Per-arm fail-closed staging guard `required = ni_sp*nj_sp*2` → BufferTooSmall BEFORE any write (Phase-28 CR-01; inline arms bypass launch_*_pair). OOM-safe stop, no partial writes. ASVS V5. |
| T-29-04 | Spoofing/Repudiation | oracle_covered flip on a skipped fixture | mitigate | xtask SC#4 guard refuses to flip a family whose only fixture was skipped; NO-SILENT-SKIP asserts the vendor arm executed under the double gate. |

Per RESEARCH §Security Domain: numerical-integral library feature, no auth/session/network/untrusted-network-input surface. Only ASVS V5 applies, satisfied by fail-closed guards. No HIGH threats.
</threat_model>

<verification>
- `cargo build --workspace --features cpu` exits 0.
- `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test rel_1e_sigma_parity` fully green (all 7 + sp + no_silent_skip + kappa-sizing).
- `cargo run -p xtask -- manifest-audit` exits 0; 8 rows oracle_covered=true spinor-only.
</verification>

<success_criteria>
- REL-01: spsp (sf_2d), spnucsp, sprinvsp (si_2d) byte-identical at atol=1e-12 (spinor).
- REL-02: srsr, srnucsr, sp (si_2d), sr, sigma (si_2di) byte-identical at atol=1e-12 (spinor).
- int1e_sigma rank empirically resolved; int1e_sp flipped.
- NO-SILENT-SKIP green; manifest-audit green; spinor-only flips. Wave 1 GREEN gate for Wave 2.
</success_criteria>

<output>
After completion, create `.planning/phases/29-group-4-relativistic-spin-operator-integrals-spinor/29-02-SUMMARY.md`
</output>
