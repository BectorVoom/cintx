---
phase: 29-group-4-relativistic-spin-operator-integrals-spinor
plan: 06
type: execute
wave: 6
depends_on: ["05"]
files_modified:
  - crates/cintx-cubecl/src/kernels/two_electron.rs
  - crates/cintx-cubecl/src/kernels/sigma_p.rs
  - crates/cintx-ops/generated/compiled_manifest.lock.json
  - crates/cintx-oracle/tests/rel_2e_sigma_parity.rs
autonomous: true
requirements: [REL-03, REL-04]
must_haves:
  truths:
    - "int2e_spsp1/srsr1 (+spsp1spsp2/srsr1srsr2) match vendored libcint at atol=1e-12 (spinor) via c2s_si_2e1+c2s_sf_2e2 / +c2s_si_2e2 (REL-03)"
    - "int2e_ssp1ssp2/sps1sps2 + the ssp/sps i-variants match at atol=1e-12 via c2s_si_2e1i+c2s_si_2e2i (REL-04)"
    - "int2e_vsp1*/spv1* match at atol=1e-12 via c2s_si_2e1+c2s_sf_2e2 / +c2s_si_2e2 (REL-04)"
    - "every 2e Group-4 family runs non-skipped under the double gate, is flipped oracle_covered=true spinor-only, and manifest-audit is green"
  artifacts:
    - path: "crates/cintx-cubecl/src/kernels/two_electron.rs"
      provides: "2e σ Spinor launcher arms for all REL-03/04 families"
      contains: "cart_to_spinor_si_2e1i"
    - path: "crates/cintx-oracle/tests/rel_2e_sigma_parity.rs"
      provides: "GREEN REL-03/04 byte-identity parity on the 2e kappa fixture"
  key_links:
    - from: "crates/cintx-cubecl/src/kernels/two_electron.rs"
      to: "cart_to_spinor_si_2e1 / si_2e2 / si_2e1i / si_2e2i / sf_2e2"
      via: "per-family transform pairing per RESEARCH 2e map"
      pattern: "cart_to_spinor_si_2e1i|cart_to_spinor_sf_2e2"
    - from: "crates/cintx-ops/generated/compiled_manifest.lock.json"
      to: "oracle_covered=true"
      via: "flip per family after parity green, spinor-only"
      pattern: "int2e_(spsp1|srsr1|ssp1ssp2|sps1sps2|vsp1|spv1)"
---

<objective>
Wave-3 completion (REL-03/04): wire the 2e σ Spinor launcher arms for every remaining Group-4 2e family onto the correct per-family transform pairing (from RESEARCH's authoritative 2e map), turn `rel_2e_sigma_parity.rs` GREEN at atol=1e-12 on `build_kappa_spinor_2e_fixture`, and flip each family `oracle_covered=true` spinor-only. Closes Group 4.

Purpose: With the transform suite proven (29-04 gate) and the build/manifest/vendor foundation laid (29-05), this plan wires launcher arms onto the proven transform and proves byte-identity per family. The 2e σ·p assembler is expected-new (Open Q2) — mine the spsp harness.
Output: all REL-03/04 2e launcher arms; rel_2e_sigma_parity.rs green; all 2e Group-4 rows flipped oracle_covered=true; manifest-audit green. Phase 29 complete.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-group-4-relativistic-spin-operator-integrals-spinor/29-RESEARCH.md
@.planning/phases/29-group-4-relativistic-spin-operator-integrals-spinor/29-PATTERNS.md
@.planning/phases/29-group-4-relativistic-spin-operator-integrals-spinor/29-04-SUMMARY.md
@.planning/phases/29-group-4-relativistic-spin-operator-integrals-spinor/29-05-SUMMARY.md

<interfaces>
From crates/cintx-cubecl/src/kernels/two_electron.rs:
- The int2e_spsp1 Spinor arm from 29-04 — the template for the rest. Remaining 2e Spinor returns UnsupportedApi (~L1450/1665/1717) — replace per family.
- center_4c1e.rs `test_device_matches_host_spsp` (L1878) — the 2e σ·p-on-both-sides cart gout (Open Q2 — expected-new thin 2e assembler).
- sigma_p.rs — the 1e σ·p assembler; the 2e sibling builds on the same pattern.

2e per-family transform pairing (RESEARCH §Per-Family Map 2e, AUTHORITATIVE):
- int2e_spsp1, int2e_srsr1, int2e_vsp1, int2e_spv1 → c2s_si_2e1 + c2s_sf_2e2 (1-sided σ)
- int2e_spsp1spsp2, int2e_srsr1srsr2, int2e_spv1spv2, int2e_vsp1spv2, int2e_spv1vsp2, int2e_vsp1vsp2, int2e_spv1spsp2, int2e_vsp1spsp2 → c2s_si_2e1 + c2s_si_2e2 (2-sided σ)
- int2e_ssp1ssp2, int2e_ssp1sps2, int2e_sps1ssp2, int2e_sps1sps2 → c2s_si_2e1i + c2s_si_2e2i (both IMAGINARY)
gouts: int2e_spsp1 rank 4 `+s5-s7,+s6-s2,+s1-s3,+s0+s4+s8` (intor4.c:19); transcribe each from intor4.c (REL-03) / gaunt1.c (ssp/sps) / dkb.c (vsp/spv). All component_rank "1".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire REL-03 2e σ launcher arms (srsr1, spsp1spsp2, srsr1srsr2) + prove byte-identity</name>
  <read_first>
    - crates/cintx-cubecl/src/kernels/two_electron.rs: the int2e_spsp1 arm (29-04) — clone shape + staging guard `required = di*dj*dk*dl*2`.
    - crates/cintx-cubecl/src/kernels/center_4c1e.rs `test_device_matches_host_spsp` (L1878) — 2e σ·p cart gout.
    - 29-03 transforms `cart_to_spinor_si_2e1`, `cart_to_spinor_si_2e2`, `cart_to_spinor_sf_2e2`.
    - RESEARCH §Per-Family Map 2e (srsr1→si_2e1+sf_2e2; spsp1spsp2/srsr1srsr2→si_2e1+si_2e2) + intor4.c gout lines (srsr1 L349, spsp1spsp2 L277, srsr1srsr2 L541).
    - PATTERNS.md §"2e σ Spinor launcher arms".
  </read_first>
  <behavior>
    - int2e_srsr1 → byte-identical to vendor_int2e_srsr1_spinor at atol=1e-12 (c2s_si_2e1+c2s_sf_2e2)
    - int2e_spsp1spsp2, int2e_srsr1srsr2 → byte-identical via c2s_si_2e1+c2s_si_2e2 at atol=1e-12
    - each arm sizes via spinor_len (GT/LT), non-square 2e fixture passes
  </behavior>
  <action>
    Replace the UnsupportedApi Spinor returns in two_electron.rs for `int2e_srsr1` (→ c2s_si_2e1+c2s_sf_2e2), `int2e_spsp1spsp2` and `int2e_srsr1srsr2` (→ c2s_si_2e1+c2s_si_2e2), cloning the 29-04 spsp1 arm shape. Device σ·p assembler emits the 4 contiguous gc blocks per electron carrying σ (gout per family from intor4.c — srsr1 gout structure matches spsp1 rank 4; the 2-sided families carry σ on both electrons); reuse/extend the thin 2e σ·p assembler from 29-04 (mine `test_device_matches_host_spsp` for the both-side gout). Transform pairing exactly per the 2e map. **Each arm gets its OWN fail-closed staging guard `required = di*dj*dk*dl*2` BEFORE any write** (Phase-28 CR-01). Sizing via spinor_len; NO launcher transpose. Suspect CubeCL CpuRuntime ~1e-11 FP-env side effect before chasing kernel numerics on the flat atol=1e-12 gate.
  </action>
  <verify>
    <automated>CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test rel_2e_sigma_parity test_spsp1 test_srsr1 test_spsp1spsp2 test_srsr1srsr2 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - two_electron.rs references `cart_to_spinor_si_2e1`, `cart_to_spinor_si_2e2`, `cart_to_spinor_sf_2e2`
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test rel_2e_sigma_parity test_spsp1 test_srsr1 test_spsp1spsp2 test_srsr1srsr2` exits 0 (byte-identity atol=1e-12)
    - `grep -v '^#' crates/cintx-cubecl/src/kernels/two_electron.rs | grep -c "BufferTooSmall"` increased by ≥3 vs the 29-04 baseline
  </acceptance_criteria>
  <done>REL-03 families (spsp1 from 29-04 + srsr1/spsp1spsp2/srsr1srsr2) byte-identical to vendor at atol=1e-12; each arm guarded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire REL-04 2e σ launcher arms (ssp/sps imaginary + vsp/spv) + prove byte-identity</name>
  <read_first>
    - crates/cintx-cubecl/src/kernels/two_electron.rs: the REL-03 arms from Task 1.
    - 29-03 transforms `cart_to_spinor_si_2e1i`, `cart_to_spinor_si_2e2i` (both imaginary), `cart_to_spinor_si_2e1/2e2`, `cart_to_spinor_sf_2e2`.
    - RESEARCH §Per-Family Map 2e: ssp/sps→si_2e1i+si_2e2i (both imaginary); vsp1/spv1→si_2e1+sf_2e2; 2-sided spv/vsp→si_2e1+si_2e2. gaunt1.c lines (ssp1ssp2 L111, ssp1sps2 L209, sps1ssp2 L307, sps1sps2 L405); dkb.c lines (spv1 L249, vsp1 L333, spv1spv2 L501, vsp1spv2 L597, spv1vsp2 L693, vsp1vsp2 L789, spv1spsp2 L911, vsp1spsp2 L1033).
    - PATTERNS.md §"2e σ Spinor launcher arms".
  </read_first>
  <behavior>
    - int2e_ssp1ssp2, int2e_ssp1sps2, int2e_sps1ssp2, int2e_sps1sps2 → byte-identical via c2s_si_2e1i+c2s_si_2e2i (both imaginary) at atol=1e-12
    - int2e_vsp1, int2e_spv1 → byte-identical via c2s_si_2e1+c2s_sf_2e2 at atol=1e-12
    - int2e_{spv1spv2,vsp1spv2,spv1vsp2,vsp1vsp2,spv1spsp2,vsp1spsp2} → byte-identical via c2s_si_2e1+c2s_si_2e2 at atol=1e-12
  </behavior>
  <action>
    Replace the UnsupportedApi Spinor returns in two_electron.rs for the REL-04 families per the 2e map: ssp/sps (`int2e_{ssp1ssp2,ssp1sps2,sps1ssp2,sps1sps2}`) → `cart_to_spinor_si_2e1i` + `cart_to_spinor_si_2e2i` (BOTH imaginary — gaunt1.c drivers); vsp1/spv1 → `cart_to_spinor_si_2e1` + `cart_to_spinor_sf_2e2`; the 2-sided spv/vsp (`int2e_{spv1spv2,vsp1spv2,spv1vsp2,vsp1vsp2,spv1spsp2,vsp1spsp2}`) → `cart_to_spinor_si_2e1` + `cart_to_spinor_si_2e2`. Transcribe each gout from gaunt1.c (ssp/sps) / dkb.c (vsp/spv) verbatim into the device σ·p assembler. **Each arm gets its OWN fail-closed staging guard `required = di*dj*dk*dl*2` BEFORE any write.** Sizing via spinor_len; NO launcher transpose. The vendor symbols link only because 29-05 wired gaunt1.c/dkb.c into the build.
  </action>
  <verify>
    <automated>CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test rel_2e_sigma_parity test_ssp1ssp2 test_sps1sps2 test_vsp1 test_spv1 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - two_electron.rs references `cart_to_spinor_si_2e1i` and `cart_to_spinor_si_2e2i` (the imaginary ssp/sps pairing)
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test rel_2e_sigma_parity test_ssp1ssp2 test_ssp1sps2 test_sps1ssp2 test_sps1sps2 test_vsp1 test_spv1` exits 0 (byte-identity atol=1e-12)
    - the 2-sided spv/vsp family tests pass (`…test_spv1spv2 test_vsp1spv2 test_spv1vsp2 test_vsp1vsp2 test_spv1spsp2 test_vsp1spsp2` exits 0)
  </acceptance_criteria>
  <done>All REL-04 families (ssp/sps imaginary + vsp/spv) byte-identical to vendor at atol=1e-12; each arm guarded.</done>
</task>

<task type="auto">
  <name>Task 3: Flip all 2e Group-4 families oracle_covered=true (spinor-only); NO-SILENT-SKIP + manifest-audit green; phase gate</name>
  <read_first>
    - xtask/src/oracle_covered_update.rs — the flip mechanism + SC#4 skipped-fixture-flip-refusal guard.
    - crates/cintx-oracle/tests/rel_2e_sigma_parity.rs `test_no_silent_skip` (from 29-05).
    - PATTERNS.md §"NO-SILENT-SKIP parity assertion" + §"component_rank = '1'" + §"OperatorId by symbol name".
    - ROADMAP §Phase 29 SC#5 (flip spinor-only; do not over-claim cart/sph σ intermediates).
  </read_first>
  <action>
    For every 2e Group-4 family (`int2e_{spsp1,srsr1,spsp1spsp2,srsr1srsr2,ssp1ssp2,ssp1sps2,sps1ssp2,sps1sps2,spv1,vsp1,spv1spv2,vsp1spv2,spv1vsp2,vsp1vsp2,spv1spsp2,vsp1spsp2}_spinor`), flip `oracle_covered` false→true in compiled_manifest.lock.json **spinor-only** (RepresentationSupport stays sph=false,cart=false,spinor=true — SC#5), ONLY after its parity test is green. Run `xtask oracle-covered-update`; confirm the SC#4 guard refuses none. Confirm `test_no_silent_skip` reads the post-flip FLIPPED set from MANIFEST_ENTRIES at runtime and asserts each vendor arm executed under the double gate (FAIL not skip). Re-grep `OperatorId::new(` / `_OPERATOR_ID` for positional drift. Run `manifest-audit` (green) and the full vendor parity suite for Phase 29. **Phase gate (SC#1-5):** all REL-01..04 families oracle_covered=true spinor-only; both the 1e (rel_1e_sigma_parity) and 2e (rel_2e_sigma_parity) suites + the si_2e_transform_parity micro-test green under the double gate; manifest-audit green; `cargo test --workspace --features cpu` green.
  </action>
  <verify>
    <automated>cargo run -p xtask -- manifest-audit 2>&1 | tail -5; CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test rel_2e_sigma_parity 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - all 16 2e Group-4 symbols show `"oracle_covered": true` + spinor-only support in compiled_manifest.lock.json
    - `cargo run -p xtask -- manifest-audit` exits 0
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test rel_2e_sigma_parity` fully green (all families + test_no_silent_skip + kappa-sizing)
    - `cargo test --workspace --features cpu` exits 0 (no OperatorId drift, all suites green)
  </acceptance_criteria>
  <done>All 16 2e Group-4 rows flipped spinor-only oracle_covered=true; NO-SILENT-SKIP green; manifest-audit green; Phase 29 SC#1-5 satisfied. Group 4 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → eval_raw (raw atm/bas/env) | Caller shell/env arrays cross into the 2e launcher/transform; only untrusted-input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-11 | Tampering/DoS | each 2e σ Spinor launcher arm output buffer | mitigate | Per-arm fail-closed staging guard `required = di*dj*dk*dl*2` → BufferTooSmall BEFORE any write (Phase-28 CR-01; inline 2e arms bypass launch_*_pair). OOM-safe stop, no partial writes. ASVS V5. |
| T-29-12 | Spoofing/Repudiation | oracle_covered flip on a skipped/unlinked fixture | mitigate | xtask SC#4 guard refuses to flip a skipped-fixture family; NO-SILENT-SKIP asserts the vendor arm executed under the double gate; flips spinor-only (SC#5, no cart/sph over-claim). |

Per RESEARCH §Security Domain: numerical-integral library feature, no auth/session/network/untrusted-network-input surface. Only ASVS V5 applies, satisfied by fail-closed guards. No HIGH threats.
</threat_model>

<verification>
- `cargo test --workspace --features cpu` exits 0.
- `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test '*spinor*'` (rel_1e_sigma_parity + si_2e_transform_parity + rel_2e_sigma_parity) fully green.
- `cargo run -p xtask -- manifest-audit` exits 0; all 24 Group-4 spinor rows (8 1e + 16 2e) oracle_covered=true spinor-only.
</verification>

<success_criteria>
- REL-03: spsp1, srsr1, spsp1spsp2, srsr1srsr2 byte-identical at atol=1e-12 (spinor).
- REL-04: ssp1ssp2, ssp1sps2, sps1ssp2, sps1sps2 (imaginary), vsp1, spv1, and the 2-sided spv/vsp families byte-identical at atol=1e-12 (spinor).
- All 16 2e rows flipped oracle_covered=true spinor-only; NO-SILENT-SKIP green; manifest-audit green.
- Phase 29 SC#1-5 satisfied. Group 4 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/29-group-4-relativistic-spin-operator-integrals-spinor/29-06-SUMMARY.md`
</output>
