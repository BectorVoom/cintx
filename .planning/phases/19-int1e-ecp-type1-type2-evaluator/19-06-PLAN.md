---
phase: 19-int1e-ecp-type1-type2-evaluator
plan: 06
type: execute
wave: 2
depends_on: [19-05]
files_modified:
  - crates/cintx-cubecl/src/kernels/ecp.rs
  - crates/cintx-oracle/tests/safe_api_ecp_parity.rs
  - crates/cintx-ops/src/generated/api_manifest.csv
  - crates/cintx-ops/src/generated/api_manifest.rs
  - crates/cintx-ops/generated/compiled_manifest.lock.json
autonomous: true
requirements: [ECP-01, ECP-02, ECP-03, ECP-04]
canonical_family: ecp
forms: [cart, sph]

must_haves:
  truths:
    - "compute_type1_pair and compute_type2_pair in kernels/ecp.rs no longer use the direct-quadrature form (gauss_hermite_nodes_weights_host / gauss_chebyshev_nodes_weights_host + the generic modified_spherical_bessel_in_host); they call the 19-05 K-Taylor radial machinery (ecprad_part_host + type1_rad_part_host for Type-1, ecprad_part_host + type2_facs_rad_host + ecpsph_ine_opt_host for Type-2) plus PySCF's angular splice."
    - "int1e_ecp_cart and int1e_ecp_sph pass byte-identity vs vendored PySCF nr_ecp (ECPscalar_cart / ECPscalar_sph) at atol=1e-12, rtol=0.0 over the full Cu/LANL2DZ Cartesian product (8 AO shells x 8 AO shells x 3 ECP projectors) through both eval_raw and SessionRequest::evaluate."
    - "The two scalar parity tests in safe_api_ecp_parity.rs (test_int1e_ecp_cart_safe_api_parity, test_int1e_ecp_sph_safe_api_parity) no longer carry #[ignore] and run by default under CINTX_ORACLE_BUILD_VENDOR=1 + --features cpu."
    - "api_manifest.csv has oracle_covered=true on the int1e_ecp_cart and int1e_ecp_sph rows; compiled_manifest.lock.json is regenerated and manifest-audit reports zero diff. The two ipnuc rows stay oracle_covered=false (closed by 19-07)."
    - "The ECP kernel is host-only (D-16); the file rustdoc documents this as a CLAUDE.md-constraint deviation (CubeCL is the primary backend) with the #[cube] GPU counterpart deferred per CONTEXT Deferred Ideas."
  artifacts:
    - path: "crates/cintx-cubecl/src/kernels/ecp.rs"
      provides: "K-Taylor-based compute_type1_pair / compute_type2_pair replacing the direct-quadrature bodies"
      contains: "ecprad_part_host"
    - path: "crates/cintx-oracle/tests/safe_api_ecp_parity.rs"
      provides: "Two scalar parity tests un-ignored and passing at atol=1e-12"
    - path: "crates/cintx-ops/src/generated/api_manifest.csv"
      provides: "int1e_ecp_{cart,sph} rows flipped to oracle_covered=true"
  key_links:
    - from: "crates/cintx-cubecl/src/kernels/ecp.rs::compute_type1_pair"
      to: "crates/cintx-cubecl/src/math/ecp_k_taylor.rs::type1_rad_part_host"
      via: "Type-1 radial assembly over the K-Taylor Bessel table"
      pattern: "type1_rad_part_host|ecprad_part_host"
    - from: "crates/cintx-cubecl/src/kernels/ecp.rs::compute_type2_pair"
      to: "crates/cintx-cubecl/src/math/ecp_k_taylor.rs::type2_facs_rad_host"
      via: "Type-2 radial-factor assembly + angular splice"
      pattern: "type2_facs_rad_host|ecpsph_ine_opt_host"
    - from: "crates/cintx-oracle/tests/safe_api_ecp_parity.rs"
      to: "vendor_ECPscalar_cart / vendor_ECPscalar_sph"
      via: "byte-identity parity at atol=1e-12 over Cu/LANL2DZ"
      pattern: "vendor_ECPscalar_(cart|sph)"
---

<objective>
Close the scalar half of Phase 19 to exact byte-identity. Replace the 19-04 direct-quadrature `compute_type1_pair` / `compute_type2_pair` bodies in `kernels/ecp.rs` with the K-Taylor radial machinery from 19-05 (`ecprad_part_host`, `type1_rad_part_host`, `type2_facs_rad_host`, `ecpsph_ine_opt_host`) plus PySCF's per-(li, lj, l_c) angular splice. Remove the `#[ignore]` from the two scalar parity tests, iterate until they pass at `atol=1e-12, rtol=0.0` over the full Cu/LANL2DZ Cartesian product, and flip the two scalar manifest rows to `oracle_covered=true`.

Purpose: this is the requirement-closing plan for the scalar arm — ECP-01 (Type-1 kernel), ECP-02 (Type-2 kernel), ECP-03 (SessionRequest dispatch), and the scalar half of ECP-04 (parity sweep) all close here. D-13 commits to exact byte-identity, NOT relaxed tolerance and NOT compiled-but-unverified.
Output: rewritten scalar kernel bodies, two un-ignored passing parity tests, two flipped manifest rows + regenerated lock.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-CONTEXT.md
@.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-04-SUMMARY.md
@.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-05-PLAN.md
@CLAUDE.md
@crates/cintx-cubecl/src/kernels/ecp.rs
@crates/cintx-oracle/tests/safe_api_ecp_parity.rs

<interfaces>
19-05 host primitives (crates/cintx-cubecl/src/math/ecp_k_taylor.rs):
  - ecpsph_ine_opt_host(order: u32, z: f64) -> Vec<f64>            (table-interpolation i_l)
  - ecprad_part_host(...) -> usize (nrs_max)                       (per-shell radial Gaussian sum + RADI_POWER)
  - type1_rad_part_host(...)                                       (Type-1 radial assembly, calls ecpsph_ine_opt_host)
  - type2_facs_rad_host(...)                                       (Type-2 radial-factor assembly)
  - ecp_k_taylor_data::sph_ine_tab() / sph_ine_tab_order7()        (embedded blobs)

Current launch_ecp signature (crates/cintx-cubecl/src/kernels/ecp.rs):
  pub fn launch_ecp(backend: &ResolvedBackend, plan: &ExecutionPlan<'_>,
                    specialization: &SpecializationKey, staging: &mut [f64])
      -> Result<ExecutionStats, cintxRsError>
  Scalar arm (operator_name == "ecp") dispatches per-ECP-shell on EcpChannel::{Local, Projected(l)}
  to compute_type1_pair / compute_type2_pair. The gradient arm (operator_name == "ecp_ipnuc")
  returns UnsupportedApi — left for 19-07.

PySCF per-pair drivers being matched (vendor/pyscf-nr-ecp/src/nr_ecp.c):
  - ECPtype1_cart (line 5808): calls ECPrad_part (5910) then type1_rad_part (5931); the
    angular splice uses cache_3dfac / ang_nuc_in_cart (4966, 5003) and the int_unit_xyz
    angular integral (4952).
  - ECPtype2_cart (line 5337): calls ECPrad_part (5441), type2_facs_rad (5450/5452),
    type2_facs_ang (5033), with the (li, lj, lc) Clebsch-Gordan-like angular splice.
  - ECPscalar_sph (6179) / ECPscalar_cart (6223): the combined wrappers cintx-oracle compares
    against; they dispatch ECPtype_scalar_* which sums Type-1 + Type-2 contributions across
    the ecpbas slab. Per 19-04 the FFI reads ecpbas from env[AS_ECPBAS_OFFSET]/env[AS_NECPBAS].

Cu/LANL2DZ fixture: crates/cintx-oracle/data/cu_lanl2dz.json carries exactly 8 AO shells and
  3 ECP projectors (Plan 01 coverage invariant; re-verified by coverage_invariant_holds test).
  Vendor FFI wrappers vendor_ECPscalar_{cart,sph} are already wired (19-04 Task 2) and
  smoke-tested. Tolerance: const ATOL=1e-12, RTOL=0.0 (already declared at the test-file top).

Manifest: api_manifest.csv rows 28-29 are int1e_ecp_{cart,sph} at oracle_covered=false today.
  Regeneration command: `cargo run -p xtask --locked -- oracle-covered-update` regenerates from
  the lock, OR edit the lock JSON directly then run `manifest-audit`; per 19-01 SUMMARY the
  canonical source is crates/cintx-ops/generated/compiled_manifest.lock.json and build.rs
  regenerates api_manifest.csv + api_manifest.rs from it. Confirm the exact flip mechanics by
  reading xtask/src/oracle_covered_update.rs and manifest_audit.rs before editing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace compute_type1_pair / compute_type2_pair with the K-Taylor radial port + angular splice</name>
  <files>crates/cintx-cubecl/src/kernels/ecp.rs</files>
  <read_first>
    - /home/user/Documents/workspace/cintx/crates/cintx-cubecl/src/kernels/ecp.rs (the direct-quadrature compute_type1_pair at line ~145 and compute_type2_pair at line ~273 being replaced; the file rustdoc "Implementation note (Phase 19 Wave 2)" at lines 64-82 to rewrite; launch_ecp dispatch and cart_to_sph_1e usage)
    - /home/user/Documents/workspace/cintx/crates/cintx-cubecl/src/math/ecp_k_taylor.rs (19-05 output — ecpsph_ine_opt_host, ecprad_part_host, type1_rad_part_host, type2_facs_rad_host signatures and the radial grid contract)
    - /home/user/Documents/workspace/cintx/vendor/pyscf-nr-ecp/src/nr_ecp.c (lines 5808-5991 ECPtype1_cart: ecploc setup, the rs grid `nrs = 1<<LEVEL_MAX`, the ECPrad_part call at 5910, type1_rad_part at 5931, and the angular accumulation into gctr; lines 5337-5515 ECPtype2_cart: the (li, lj, lc) angular splice with type2_facs_ang + type2_facs_rad; lines 4848-4865 ECPgauss_chebyshev for the rs grid; lines 4952-5031 the angular helpers int_unit_xyz / ang_nuc_in_cart / cache_3dfac)
    - /home/user/Documents/workspace/cintx/crates/cintx-cubecl/src/kernels/one_electron.rs (launch_one_electron line 434 — the F-order [ao_i, ao_j] cart buffer convention and cart_to_sph_1e application the ECP scalar arm must match)
    - /home/user/Documents/workspace/cintx/.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-04-SUMMARY.md (the normalization + PA/PB convention notes, the np_helper shim deviation, and the "Deferred to Plan 04b" worklist this task executes)
  </read_first>
  <action>
    Rewrite the body of compute_type1_pair (currently the Gauss-Hermite direct-quadrature form ~line 145) to follow ECPtype1_cart (nr_ecp.c:5808-5991):
      - Build the radial grid `rs` the way PySCF does (`nrs = 1 << LEVEL_MAX`; use ECPgauss_chebyshev nodes/weights — the existing gauss_chebyshev_nodes_weights_host produces the SAME ECPgauss_chebyshev grid per 19-02 SUMMARY, so reuse it as the rs/ws source; do NOT reintroduce Gauss-Hermite).
      - Call `ecprad_part_host(...)` to accumulate the per-ECP-shell radial Gaussian `ur` over the rs grid (the RADI_POWER weighting is applied inside ecprad_part_host).
      - Call `type1_rad_part_host(rad_all, li+lj, k, aij, ur, rs, nrs, ...)` with `k = 2*aij*|rij|` per nr_ecp.c:5931 to produce the rad_all radial moments.
      - Perform the Type-1 angular accumulation into the Cartesian buffer using PySCF's int_unit_xyz / cache_3dfac angular factors (port the splice from ECPtype1_cart's gctr accumulation loop). Write F-order [ao_i, ao_j] (ao_i fastest) matching launch_one_electron.
      - Multiply by the AO contraction coefficients and the common factor exactly as ECPtype1_cart does (coefficients read verbatim from shell.coefficients per the 19-04 normalization note — no extra N factor).

    Rewrite the body of compute_type2_pair (currently Gauss-Chebyshev + generic Bessel ~line 273) to follow ECPtype2_cart (nr_ecp.c:5337-5515):
      - Same rs grid + ecprad_part_host radial sum.
      - Call `type2_facs_rad_host(radi, ish, lc, dca, rs, nrs, ...)` and `type2_facs_rad_host(radj, jsh, lc, dcb, rs, nrs, ...)` for both AO centers (the i_l Bessel evaluation flows through ecpsph_ine_opt_host inside the radial assembler — replacing the generic modified_spherical_bessel_in_host).
      - Apply the type2_facs_ang angular factors and the per-(li, lj, lc) Clebsch-Gordan-like splice exactly as ECPtype2_cart, summing over the projector channel l = lc. Write the same F-order [ao_i, ao_j] cart buffer.

    Rewrite the file rustdoc: remove the "Implementation note (Phase 19 Wave 2)" direct-quadrature disclaimer (lines 64-82); replace with a "K-Taylor port (Phase 19 replan)" section citing nr_ecp.c:5808-5991 / 5337-5515 / 4687-4837 and the 19-05 host primitives, and a "CubeCL constraint deviation" paragraph stating the kernel is host-only this phase (D-16) with the `#[cube]` GPU counterpart deferred per CONTEXT Deferred Ideas.

    Remove the now-dead direct-quadrature imports (gauss_hermite_nodes_weights_host if no longer used; keep gauss_chebyshev_nodes_weights_host only if it is the rs-grid source). Remove the TYPE1_HERMITE_N const if unused.

    Keep the existing module-internal tests passing (resolver registration, cart_comps count). Update or remove the `type1_hermite_node_count_within_envelope` test if TYPE1_HERMITE_N is dropped.
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/cintx &amp;&amp; cargo test --locked -p cintx-cubecl --lib kernels::ecp 2>&amp;1 | tail -15 &amp;&amp; cargo --locked build -p cintx-cubecl 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --locked -p cintx-cubecl --lib kernels::ecp` exits 0
    - `cargo --locked build -p cintx-cubecl` exits 0
    - `grep -F 'ecprad_part_host' crates/cintx-cubecl/src/kernels/ecp.rs` matches (Type-1 + Type-2 use the K-Taylor radial sum)
    - `grep -F 'type1_rad_part_host' crates/cintx-cubecl/src/kernels/ecp.rs` matches
    - `grep -F 'type2_facs_rad_host' crates/cintx-cubecl/src/kernels/ecp.rs` matches
    - `grep -F 'gauss_hermite_nodes_weights_host' crates/cintx-cubecl/src/kernels/ecp.rs` returns 0 matches (the Gauss-Hermite direct-quadrature form is gone from compute_type1_pair)
    - `grep -F 'direct-quadrature' crates/cintx-cubecl/src/kernels/ecp.rs` returns 0 matches (the Wave-2 disclaimer is replaced)
    - `grep -cE '// Source: vendor/pyscf-nr-ecp/src/nr_ecp\.c:[0-9]+-[0-9]+' crates/cintx-cubecl/src/kernels/ecp.rs` returns at least 2
  </acceptance_criteria>
  <done>compute_type1_pair and compute_type2_pair are rewritten to use the 19-05 K-Taylor radial machinery + PySCF's angular splice; the direct-quadrature form is fully removed; the kernel builds and module tests pass. Byte-identity is proved in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Un-ignore the scalar parity tests, iterate to atol=1e-12, flip oracle_covered=true on the two scalar rows</name>
  <files>crates/cintx-oracle/tests/safe_api_ecp_parity.rs, crates/cintx-ops/src/generated/api_manifest.csv, crates/cintx-ops/src/generated/api_manifest.rs, crates/cintx-ops/generated/compiled_manifest.lock.json</files>
  <read_first>
    - /home/user/Documents/workspace/cintx/crates/cintx-oracle/tests/safe_api_ecp_parity.rs (the two #[ignore]'d scalar tests + the collectors collect_safe_api_ecp_matrix / collect_ecp_matrix_vendor / count_mismatches; the file-top Status rustdoc to update)
    - /home/user/Documents/workspace/cintx/crates/cintx-cubecl/src/kernels/ecp.rs (Task 1 output — the kernel under test)
    - /home/user/Documents/workspace/cintx/xtask/src/oracle_covered_update.rs (how oracle_covered flips are regenerated)
    - /home/user/Documents/workspace/cintx/xtask/src/manifest_audit.rs (the zero-diff lock check the verify gate runs)
    - /home/user/Documents/workspace/cintx/.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-01-SUMMARY.md (the manifest-lock regeneration mechanics: lock JSON is canonical, build.rs regenerates the CSV + .rs)
  </read_first>
  <action>
    Step 1 — Remove the `#[ignore]` attribute from test_int1e_ecp_cart_safe_api_parity and test_int1e_ecp_sph_safe_api_parity in safe_api_ecp_parity.rs. Update the file-top "## Status (Phase 19 Wave 2)" rustdoc to a "## Status (Phase 19 replan — scalar closed)" note stating byte-identity is achieved via the K-Taylor port.

    Step 2 — Run the parity gate: `CINTX_ORACLE_BUILD_VENDOR=1 cargo test --locked -p cintx-oracle --features cpu --test safe_api_ecp_parity`. Iterate on the Task-1 kernel until both scalar tests pass at atol=1e-12, rtol=0.0 across the full Cu/LANL2DZ Cartesian product (8x8 AO shell pairs x 3 ECP projectors). Record the worst-case |diff| observed. Iteration on kernel bugs is in-scope (sign conventions, normalization, angular-splice index order, the cart-vs-sph transform); the gate is the source of truth.

    Step 3 — Once both pass, flip oracle_covered to true on the int1e_ecp_cart and int1e_ecp_sph rows. Per 19-01 SUMMARY the lock JSON is canonical: edit crates/cintx-ops/generated/compiled_manifest.lock.json to set oracle_covered=true on the two scalar ECP entries (across all four profiles), then run `cargo run -p xtask --locked -- oracle-covered-update` (or the build.rs regeneration path) so api_manifest.csv and api_manifest.rs regenerate to match. The two ipnuc rows STAY oracle_covered=false (19-07 closes them).

    Step 4 — Run `cargo run -p xtask --locked -- manifest-audit` and confirm zero diff (the lock matches the regenerated CSV/rs). Confirm the full ECP parity file is green and no other oracle parity test regressed: `CINTX_ORACLE_BUILD_VENDOR=1 cargo test --locked -p cintx-oracle --features cpu --test safe_api_ecp_parity`.
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/cintx &amp;&amp; bash -o pipefail -c "CINTX_ORACLE_BUILD_VENDOR=1 cargo test --locked -p cintx-oracle --features cpu --test safe_api_ecp_parity 2>&amp;1 | tail -25" &amp;&amp; bash -o pipefail -c "cargo run -p xtask --locked -- manifest-audit 2>&amp;1 | tail -5" &amp;&amp; grep -E '"int1e_ecp_cart".*,true,' crates/cintx-ops/src/generated/api_manifest.csv &amp;&amp; grep -E '"int1e_ecp_sph".*,true,' crates/cintx-ops/src/generated/api_manifest.csv</automated>
  </verify>
  <acceptance_criteria>
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test --locked -p cintx-oracle --features cpu --test safe_api_ecp_parity` exits 0 with the two scalar parity tests PASSING (not ignored); `bash -o pipefail` propagates the test-runner exit through the tail pipe
    - `grep -F '#[ignore]' crates/cintx-oracle/tests/safe_api_ecp_parity.rs` returns 0 matches on the two scalar tests (the ignore attributes are removed)
    - `grep -E '"int1e_ecp_cart".*,true,"operator","ecp"' crates/cintx-ops/src/generated/api_manifest.csv` matches
    - `grep -E '"int1e_ecp_sph".*,true,"operator","ecp"' crates/cintx-ops/src/generated/api_manifest.csv` matches
    - `grep -E '"int1e_ecp_ipnuc_(cart|sph)".*,false,' crates/cintx-ops/src/generated/api_manifest.csv` returns 2 matches (ipnuc rows still false — 19-07 closes them)
    - `cargo run -p xtask --locked -- manifest-audit` exits 0 (zero diff; `bash -o pipefail` propagates the xtask exit through the tail pipe)
  </acceptance_criteria>
  <done>The two scalar ECP parity tests pass at atol=1e-12 byte-identity vs PySCF nr_ecp, run by default (no #[ignore]); the int1e_ecp_{cart,sph} manifest rows are oracle_covered=true with the lock regenerated to zero diff; ECP-01/02/03 and the scalar half of ECP-04 are closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kernel scalar output → oracle byte-identity gate | The parity test compares cintx's host kernel against the vendored C; a wrong angular-splice index silently produces correct-shape wrong-value output. |
| manifest oracle_covered flag → CI gate | Flipping the flag before parity passes would be a false coverage claim (the exact 19-04 honesty-fix). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-19 | Tampering (silent wrong value) | compute_type1_pair / compute_type2_pair angular splice | mitigate | The atol=1e-12 byte-identity parity over the full Cu/LANL2DZ Cartesian product (8x8x3) catches any sign/index/normalization error; the gate runs by default once #[ignore] is removed. |
| T-19-20 | Repudiation (false coverage claim) | oracle_covered flip | mitigate | Flag is flipped only in Task 2 Step 3 AFTER Step 2's parity gate is green; manifest-audit zero-diff confirms the lock matches; ipnuc rows stay false. |
| T-19-21 | Integrity (allocation under OOM) | host kernel buffers | accept | Host buffers are fixed-size per shell-pair (li, lj, lc bounded by ECP_LMAX=5); no fallible-allocation path is introduced here (CLAUDE.md OOM-safe contract is a runtime-layer concern, not this pure host kernel). Low risk, bounded sizes. |
</threat_model>

<verification>
- `CINTX_ORACLE_BUILD_VENDOR=1 cargo test --locked -p cintx-oracle --features cpu --test safe_api_ecp_parity` exits 0 with the two scalar tests passing (not ignored)
- `cargo test --locked -p cintx-cubecl --lib kernels::ecp` exits 0
- `cargo --locked build --workspace` exits 0
- int1e_ecp_{cart,sph} rows are oracle_covered=true; ipnuc rows stay false; manifest-audit zero diff
</verification>

<success_criteria>
ECP-01 (Type-1 kernel byte-identity), ECP-02 (Type-2 kernel byte-identity), ECP-03 (SessionRequest dispatch verified through the safe-API parity path), and the scalar half of ECP-04 (parity sweep) are closed at exact atol=1e-12. The direct-quadrature approximation is fully removed. Gradient (ipnuc) closure is the only remaining requirement work, handled by 19-07.
</success_criteria>

<output>
After completion, create `.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-06-SUMMARY.md` capturing: the worst-case |diff| observed for cart and sph parity, any kernel-level corrections required during iteration (sign conventions on the angular splice, normalization, cart-vs-sph index order), whether the rs grid reuse of gauss_chebyshev_nodes_weights_host matched ECPgauss_chebyshev exactly or needed adjustment, and the exact manifest-flip mechanics used (lock-edit + regenerate vs xtask subcommand).
</output>
