---
phase: 19
plan: 03
type: execute
wave: 1
depends_on: [19-01]
files_modified:
  - crates/cintx-core/src/ecp.rs
  - crates/cintx-core/src/basis.rs
  - crates/cintx-core/src/lib.rs
  - crates/cintx-core/src/operator.rs
  - crates/cintx-compat/src/raw.rs
  - crates/cintx-rs/src/error.rs
  - crates/cintx-rs/src/api.rs
  - crates/cintx-rs/src/prelude.rs
  - crates/cintx-ops/src/resolver.rs
autonomous: true
requirements: [ECP-03]
canonical_family: ecp
forms: [cart, sph]

must_haves:
  truths:
    - "EcpShell has all eight fields (atom_index, channel, radial_power, nprim, nctr, so_type, exponents, coefficients) with try_new validation matching Shell::try_new's invariants (exponents.len()==nprim, coefficients.len()==nprim*nctr, finite-check)"
    - "BasisSet exposes ecp_shells() -> &[Arc<EcpShell>] returning &[] when no ECP attached, and try_new_with_ecp(atoms, shells, ecp_shells) is the new typed constructor; try_new keeps its existing signature (SemVer preserved)"
    - "OperatorId::is_ecp() returns true for the four ECP operator IDs and false for all others; the four IDs are derived from Plan 01's regenerated api_manifest.rs (not hardcoded) and reflected as monotonic constants"
    - "cintx-compat::raw exposes AS_ECPBAS_OFFSET=18, AS_NECPBAS=19, RADI_POWER=3, SO_TYPE_OF=4, ECP_LMAX=5 as pub const usize with rustdoc citing PySCF nr_ecp.h, plus an EcpBasArray typed view mirroring BasArray"
    - "RawApiId gains INT1E_ECP_CART, INT1E_ECP_SPH, INT1E_ECP_IPNUC_CART, INT1E_ECP_IPNUC_SPH constants and eval_raw rejects ECP operator dispatch when env[AS_NECPBAS]==0"
    - "FacadeError::MissingEcpBasis { operator: String } variant exists with FacadeErrorKind::MissingEcpBasis discriminant, and SessionRequest::query_workspace returns it when the operator is ECP and basis.ecp_shells() is empty — fails before runtime_query_workspace"
    - "INT4C1E_CART_OPERATOR_ID = 24 in crates/cintx-rs/src/api.rs is preserved unchanged; INT2E_STG_SPH_OPERATOR_ID and INT2E_IPIP1_SPH_OPERATOR_ID are updated to whatever values Plan 01's regenerated manifest assigns (expected 106 and 116 — see Plan 01 SUMMARY)"
    - "A typed `#[test]` in cintx-ops asserts that the four `OPERATOR_DESCRIPTORS` entries for `int1e_ecp_*` symbols carry the same `OperatorId` integer that the four `cintx_core::operator::OperatorId::INT1E_ECP_*` constants declare — manifest-agreement invariant enforced at compile-time + test-run-time"
  artifacts:
    - path: "crates/cintx-core/src/ecp.rs"
      provides: "EcpShell struct (8 fields) + EcpChannel enum + try_new validation"
      min_lines: 80
    - path: "crates/cintx-core/src/basis.rs"
      provides: "BasisSet::ecp_shells field, accessor, try_new_with_ecp constructor (additive)"
    - path: "crates/cintx-core/src/operator.rs"
      provides: "OperatorId::is_ecp() + four new INT1E_ECP_ operator IDs as pub const (values derived from Plan 01's regenerated api_manifest.rs)"
    - path: "crates/cintx-compat/src/raw.rs"
      provides: "ECP slot constants + EcpBasArray view + eval_raw ECP dispatch arm + INT1E_ECP_ RawApiId constants"
    - path: "crates/cintx-rs/src/error.rs"
      provides: "FacadeError::MissingEcpBasis variant + FacadeErrorKind::MissingEcpBasis discriminant"
    - path: "crates/cintx-rs/src/api.rs"
      provides: "SessionRequest::query_workspace preflight: returns MissingEcpBasis when operator.is_ecp() and basis.ecp_shells().is_empty(); F12 + IPIP1 OperatorId constants updated to post-regeneration values"
    - path: "crates/cintx-ops/src/resolver.rs"
      provides: "ecp_operator_ids_match_constants #[test] asserting OPERATOR_DESCRIPTORS ↔ cintx_core::operator::OperatorId::INT1E_ECP_* agreement plus int4c1e_cart preservation invariant"
  key_links:
    - from: "crates/cintx-rs/src/api.rs::SessionRequest::query_workspace"
      to: "crates/cintx-core/src/basis.rs::BasisSet::ecp_shells"
      via: "preflight check next to existing aosym preflight"
      pattern: "is_ecp.*ecp_shells"
    - from: "crates/cintx-compat/src/raw.rs::eval_raw"
      to: "env[AS_NECPBAS]"
      via: "is_ecp_family_symbol gate at the same insertion point as is_f12_family_symbol"
      pattern: "AS_NECPBAS"
    - from: "crates/cintx-core/src/lib.rs"
      to: "crates/cintx-core/src/ecp.rs"
      via: "pub use ecp::{EcpShell, EcpChannel}; (already added in Plan 01)"
      pattern: "pub use ecp::"
    - from: "crates/cintx-ops/src/resolver.rs::ecp_operator_ids_match_constants"
      to: "crates/cintx-ops/src/generated/api_manifest.rs::OPERATOR_DESCRIPTORS"
      via: "iter().find(|d| d.entry.symbol_name == \"int1e_ecp_*\").id == OperatorId::INT1E_ECP_*"
      pattern: "ecp_operator_ids_match_constants"
---

<objective>
Build the typed-API and raw-compat surfaces ECP needs. After this plan, a caller can construct `BasisSet::try_new_with_ecp(atoms, shells, ecp_shells)`, ask `SessionRequest::evaluate` to compute `int1e_ecp_{cart,sph}`, and receive a typed `FacadeError::MissingEcpBasis` if they forget to attach ECP shells. The raw-compat layer exposes the slot constants and the `EcpBasArray` view that Plans 04/05 read.

This plan parallels Plan 02 inside Wave 1: both depend only on Plan 01's stubs. Plan 04 (Type-1+Type-2 kernel + parity) depends on both.

Purpose: Make the safe API call shape `SessionRequest::new(INT1E_ECP_SPH, basis, shells, options).evaluate()` legal at the type level and fail-fast at the preflight before any runtime work. Also re-syncs the F12 OperatorId constants that Plan 01's CSV insertion shifted by +4.
Output: EcpShell + BasisSet extension + OperatorId constants (read-and-derived from manifest) + raw slot constants + EcpBasArray + FacadeError variant + query_workspace preflight + F12/IPIP1 OperatorId constant resync + manifest-agreement typed `#[test]`.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-CONTEXT.md
@.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-RESEARCH.md
@.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-PATTERNS.md
@.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-01-SUMMARY.md
@CLAUDE.md
@crates/cintx-core/src/shell.rs
@crates/cintx-core/src/basis.rs
@crates/cintx-compat/src/raw.rs
@crates/cintx-rs/src/error.rs
@crates/cintx-rs/src/api.rs
@crates/cintx-ops/src/resolver.rs

<interfaces>
Existing Shell::try_new from cintx-core/src/shell.rs lines 14-100 (direct field/validation analog for EcpShell::try_new):

```
struct Shell { atom_index: u32, ang_momentum: u8, nprim: u16, nctr: u16, kappa: i16, representation: Representation, exponents: Arc<[f64]>, coefficients: Arc<[f64]> }
impl Shell { fn try_new(atom_index, ang_momentum, nprim, nctr, kappa, representation, exponents, coefficients) -> CoreResult<Self> { /* validates nprim/nctr/finite */ } }
```

Existing BasisSet from cintx-core/src/basis.rs lines 47-89 (additive-field extension target):

```
struct BasisSet { atoms: Arc<[Atom]>, shells: Arc<[Arc<Shell>]>, meta: BasisMeta }
impl BasisSet { fn try_new(atoms, shells) -> CoreResult<Self>; fn atoms(&self); fn shells(&self); fn meta(&self) }
```

Existing raw slot constants from cintx-compat/src/raw.rs lines 22-29 plus RawApiId constants block lines 68-103:

```
pub const ATOM_OF: usize = 0;
pub const ANG_OF: usize = 1;
pub const NPRIM_OF: usize = 2;
pub const NCTR_OF: usize = 3;
pub const KAPPA_OF: usize = 4;
pub const PTR_EXP: usize = 5;
pub const PTR_COEFF: usize = 6;
pub const BAS_SLOTS: usize = 8;
pub const PTR_ENV_START: usize = 20;

impl RawApiId {
    pub const INT1E_OVLP_CART: Self = Self::Symbol("int1e_ovlp_cart");
    /* through INT4C1E_SPH */
    pub const INT4C1E_CART: Self = Self::Symbol("int4c1e_cart");
    pub const INT4C1E_SPH: Self = Self::Symbol("int4c1e_sph");
}
```

Existing FacadeError::UnsupportedAoSymmetry from Phase 18, in cintx-rs/src/error.rs (direct pattern to copy):

```
enum FacadeErrorKind { UnsupportedApi, Layout, Memory, Validation, UnsupportedAoSymmetry }
enum FacadeError { ..., UnsupportedAoSymmetry { requested: String } }
impl FacadeError { const fn kind(&self) -> FacadeErrorKind { match self { ..., Self::UnsupportedAoSymmetry { .. } => FacadeErrorKind::UnsupportedAoSymmetry } } }
```

Existing aosym preflight in SessionRequest::query_workspace, cintx-rs/src/api.rs lines 63-90 (Phase 18 D-04 precedent — sibling insertion point for MissingEcpBasis preflight).

Existing F12 dispatch arm in raw.rs (lines 446-455 per 19-PATTERNS.md) plus is_f12_family_symbol helper (line 572) — direct insertion-point analog for the new is_ecp_family_symbol gate.

Existing OperatorId constants in cintx-rs/src/api.rs (lines 541, 543, 545) — these are the post-Plan-01-shift updates this plan owns:

```
const INT4C1E_CART_OPERATOR_ID: u32 = 24;   // PRESERVED — must NOT change
const INT2E_STG_SPH_OPERATOR_ID: u32 = 102; // SHIFTS to 106 after Plan 01 inserts 4 ECP rows ahead of F12
const INT2E_IPIP1_SPH_OPERATOR_ID: u32 = 112; // SHIFTS to 116 after Plan 01 inserts 4 ECP rows ahead of source rows
```

Existing OPERATOR_DESCRIPTORS table layout in cintx-ops/src/generated/api_manifest.rs (confirmed 2026-05-12):

- `MANIFEST_ENTRIES` lives at the top of the file (lines ~10-2254). Each entry includes a `symbol_name: "..."` field.
- `OPERATOR_DESCRIPTORS` is a SEPARATE table starting around line 2255. Each `OperatorDescriptor` carries `id: OperatorId::new(K)` and `entry: &MANIFEST_ENTRIES[K]` — positionally paired by index `K`.
- IMPORTANT: `OperatorId::new(N)` is NOT adjacent to `symbol_name` in the source. Any agreement check that relies on grep-correlation (e.g. `grep -B3 symbol_name | grep OperatorId::new`) silently returns empty because the two tables are far apart. The typed `#[test]` in Task 1 Step 6 is the authoritative agreement check.

Existing `#[cfg(test)]` module in cintx-ops/src/resolver.rs (line 290+, lines 347+ for actual `#[test]` functions) — sibling-insertion point for the new `ecp_operator_ids_match_constants` test. It already imports `OPERATOR_DESCRIPTORS` (resolver.rs line 1), so the new test fits in cleanly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fill EcpShell, extend BasisSet, add ECP OperatorId constants (read-and-derived), resync F12+IPIP1 OperatorId constants in cintx-rs/api.rs, and land manifest-agreement typed #[test]</name>
  <files>
    crates/cintx-core/src/ecp.rs,
    crates/cintx-core/src/basis.rs,
    crates/cintx-core/src/operator.rs,
    crates/cintx-core/src/lib.rs,
    crates/cintx-rs/src/api.rs,
    crates/cintx-ops/src/resolver.rs
  </files>
  <read_first>
    - /home/user/Documents/workspace/cintx/crates/cintx-core/src/ecp.rs (Plan 01 placeholder — replace)
    - /home/user/Documents/workspace/cintx/crates/cintx-core/src/shell.rs (lines 14-105 — Shell::try_new field/validation analog)
    - /home/user/Documents/workspace/cintx/crates/cintx-core/src/basis.rs (lines 47-89 — BasisSet struct and try_new)
    - /home/user/Documents/workspace/cintx/crates/cintx-core/src/operator.rs (lines 50-75 — OperatorId newtype)
    - /home/user/Documents/workspace/cintx/crates/cintx-ops/src/generated/api_manifest.rs (POST Plan 01 xtask regeneration — find the four `int1e_ecp_*` entries inside `OPERATOR_DESCRIPTORS` and record the four `OperatorId::new(K)` integers; the K integer IS the OperatorId)
    - /home/user/Documents/workspace/cintx/crates/cintx-ops/src/resolver.rs (line 1 imports `OPERATOR_DESCRIPTORS`; line 290 `#[cfg(test)]` block; lines 347+ existing `#[test]` patterns — sibling-insertion point for the new test in Step 6)
    - /home/user/Documents/workspace/cintx/.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-01-SUMMARY.md (Plan 01's recorded OperatorId values for the four new ECP rows and the post-shift values for INT2E_STG_SPH_OPERATOR_ID / INT2E_IPIP1_SPH_OPERATOR_ID)
    - /home/user/Documents/workspace/cintx/.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-PATTERNS.md (sections on ecp.rs / basis.rs / operator.rs)
    - /home/user/Documents/workspace/cintx/.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-CONTEXT.md (D-03, D-04 EcpShell field layout — `radial_power` not `r_power`)
    - /home/user/Documents/workspace/cintx/crates/cintx-rs/src/api.rs (lines 540-546 — INT4C1E_CART_OPERATOR_ID=24 preservation invariant; INT2E_STG_SPH_OPERATOR_ID=102 and INT2E_IPIP1_SPH_OPERATOR_ID=112 need the +4 shift)
  </read_first>
  <behavior>
    - Test 1 (EcpShell::try_new validates field invariants): `EcpShell::try_new(0, EcpChannel::Local, 0, 2, 1, 0, Arc::from([1.0, 2.0]), Arc::from([0.5, 0.5]))` succeeds (nprim=2, nctr=1, exponents.len()==2, coefficients.len()==2).
    - Test 2 (length mismatch): exponents.len() != nprim returns an Err matching whichever CoreError variant Shell::try_new uses for the same condition.
    - Test 3 (non-finite rejected): NaN in exponents returns the corresponding CoreError.
    - Test 4 (BasisSet::try_new keeps ecp_shells empty): `BasisSet::try_new(atoms, shells)?.ecp_shells()` returns an empty slice.
    - Test 5 (BasisSet::try_new_with_ecp attaches): the new constructor stores ecp_shells and the accessor returns the slice.
    - Test 6 (OperatorId::is_ecp): is_ecp returns true for the four new IDs (whatever values Plan 01 assigned — likely 26..=29) and false for INT1E_OVLP_CART (0), INT1E_KIN_SPH (4), INT4C1E_CART (24).
    - Test 7 (`ecp_operator_ids_match_constants` in cintx-ops/src/resolver.rs): iterates the four `int1e_ecp_*` symbol names, finds each in `OPERATOR_DESCRIPTORS`, and asserts `descriptor.id == OperatorId::INT1E_ECP_*` for each (typed equality, not stringly-typed grep). ALSO asserts the preservation invariant: `int4c1e_cart` still maps to `OperatorId::new(24)`. This test fails fast if the four constants in `cintx-core::operator` ever drift from the generated `OPERATOR_DESCRIPTORS` table.
  </behavior>
  <action>
    Step 0 — **Read-and-derive the four ECP OperatorId integers directly from `OPERATOR_DESCRIPTORS` in Plan 01's regenerated manifest, BEFORE writing any code, and declare the four typed constants symbolically.**

    `OPERATOR_DESCRIPTORS` is positional: `OPERATOR_DESCRIPTORS[K]` carries `id: OperatorId::new(K)` paired with `entry: &MANIFEST_ENTRIES[K]`. So the index K of the table entry IS the `OperatorId` integer. Find the four entries whose `entry.symbol_name == "int1e_ecp_*"` and record their K values.

    A one-liner to print all four assignments (verify against Plan 01's SUMMARY — both must agree):

    ```bash
    awk '/OPERATOR_DESCRIPTORS:/,/^\];/' crates/cintx-ops/src/generated/api_manifest.rs \
      | awk '/OperatorId::new\(([0-9]+)\)/{id=$0; getline; if (/entry: &MANIFEST_ENTRIES\[/){print id, $0}}' \
      | grep -E 'int1e_ecp_(cart|sph|ipnuc_cart|ipnuc_sph)'
    ```

    The exact awk recipe may need adjusting based on the generated formatting; if it returns empty, fall back to reading the `OPERATOR_DESCRIPTORS` block directly (`awk '/OPERATOR_DESCRIPTORS:/,/^\];/' crates/cintx-ops/src/generated/api_manifest.rs | less`) and locate the four entries by eye. The Step 6 `#[test]` is the authoritative agreement check — once the four constants are declared, the test verifies the consistency at test-run time.

    Also derive the new post-shift values for the F12 + IPIP1 constants by the same method (find `int2e_stg_sph` and `int2e_ipip1_sph` in `OPERATOR_DESCRIPTORS`, record their K values — expected 106 and 116 per Plan 01's CSV insertion). If they differ from the expected +4 shift, STOP and reconcile against Plan 01's SUMMARY before proceeding (a mismatch means Plan 01's insertion landed at the wrong position).

    Step 1 — Replace `crates/cintx-core/src/ecp.rs` placeholder with the full struct per 19-PATTERNS.md. Set the field set verbatim: atom_index: u32, channel: EcpChannel, radial_power: i16, nprim: u16, nctr: u16, so_type: i16, exponents: Arc<[f64]>, coefficients: Arc<[f64]>. The `EcpChannel` enum is `Local` (Type-1 Coulomb-like, PySCF ANG_OF=-1) or `Projected(u8)` (Type-2 semi-local with angular momentum l ≤ 5). EcpShell::try_new mirrors Shell::try_new's invariants:
      - `exponents.len() == nprim as usize`
      - `coefficients.len() == (nprim * nctr) as usize`
      - all exponents finite (no NaN/Inf)
      - all coefficients finite
      - if channel is Projected(l), then `l <= 5` (= ECP_LMAX from PySCF nr_ecp.h)

    Reuse the existing CoreError variants Shell::try_new uses; add a new variant ONLY if a needed invariant has no existing match (e.g. `EcpAngularMomentumTooHigh` for the l > 5 case is plausible — match against existing variants first). Include a rustdoc comment at the module top stating "Phase 19 Plan 03: replaces Plan 01 stub; field set sourced from 19-PATTERNS.md ecp.rs Phase 19 deviations".

    Add `#[derive(Clone, Debug, PartialEq)]` on EcpShell and `#[derive(Clone, Copy, Debug, PartialEq, Eq)]` on EcpChannel.

    Add `#[cfg(test)] mod tests` with tests 1-3.

    Step 2 — `crates/cintx-core/src/basis.rs`: extend the struct and constructors per 19-PATTERNS.md. Concrete edit:
      - Add field `ecp_shells: Arc<[Arc<EcpShell>]>` after `meta` in the BasisSet struct.
      - Update `try_new(atoms, shells)` to default `ecp_shells: Arc::from(Vec::<Arc<EcpShell>>::new().into_boxed_slice())` so existing callers compile unchanged.
      - Add new constructor `pub fn try_new_with_ecp(atoms: Arc<[Atom]>, shells: Arc<[Arc<Shell>]>, ecp_shells: Arc<[Arc<EcpShell>]>) -> CoreResult<Self>`: same body as try_new plus an atom-index validation loop over ecp_shells. Each EcpShell::atom_index must satisfy `(atom_index as usize) < atoms.len()`; on bad index return `CoreError::InvalidShellAtomIndex` (the existing variant Phase 1 uses for the analogous AO-shell check — see basis.rs current body to confirm the exact name and use the same one). Use the same `BasisMeta::from_shells(&shells)` call: ECP shells do NOT contribute to AO meta (per D-03).
      - Add accessor `pub fn ecp_shells(&self) -> &[Arc<EcpShell>] { &self.ecp_shells }` after the existing `meta()` accessor.

    Add tests 4 and 5 in the existing basis.rs `#[cfg(test)]` module.

    Step 3 — `crates/cintx-core/src/operator.rs`: add OperatorId constants and `is_ecp()` helper. Use the values RECORDED IN STEP 0 verbatim (NOT hardcoded 26..=29 — derive from the regenerated manifest):

    Add the following inside `impl OperatorId`, replacing `<N_CART>`, `<N_SPH>`, `<N_IPNUC_CART>`, `<N_IPNUC_SPH>` with the four integers read from `api_manifest.rs::OPERATOR_DESCRIPTORS` in Step 0:

    - `pub const INT1E_ECP_CART: OperatorId = OperatorId::new(<N_CART>);`
    - `pub const INT1E_ECP_SPH: OperatorId = OperatorId::new(<N_SPH>);`
    - `pub const INT1E_ECP_IPNUC_CART: OperatorId = OperatorId::new(<N_IPNUC_CART>);`
    - `pub const INT1E_ECP_IPNUC_SPH: OperatorId = OperatorId::new(<N_IPNUC_SPH>);`
    - `pub const fn is_ecp(self) -> bool { matches!(self.raw(), N_CART | N_SPH | N_IPNUC_CART | N_IPNUC_SPH) }` (with the actual four integers in the matches! arm)

    Document the resolved IDs in the task SUMMARY and add an inline `// derived from crates/cintx-ops/src/generated/api_manifest.rs::OPERATOR_DESCRIPTORS (Plan 01 regeneration)` rustdoc comment above the constants.

    Step 4 — `crates/cintx-core/src/lib.rs`: confirm `pub use ecp::{EcpShell, EcpChannel};` from Plan 01 is present (no edit needed if so). OperatorId::INT1E_ECP_* ride on the existing `pub use operator::OperatorId`.

    Step 5 — `crates/cintx-rs/src/api.rs`: resync the F12 + IPIP1 OperatorId constants whose values shifted by +4 when Plan 01 inserted four rows ahead of the F12 block. Concrete surgical edits (use the actual post-regeneration values recorded in Step 0):
      - Line 541 `const INT4C1E_CART_OPERATOR_ID: u32 = 24;` — **preserve unchanged** (preservation invariant).
      - Line 543 `const INT2E_STG_SPH_OPERATOR_ID: u32 = 102;` — update to the value derived in Step 0 (expected `106`).
      - Line 545 `const INT2E_IPIP1_SPH_OPERATOR_ID: u32 = 112;` — update to the value derived in Step 0 (expected `116`).

    After both edits, run `cargo build --locked -p cintx-rs` and confirm the OperatorDescriptor lookups using these constants still resolve (lines 688, 731, 752 in api.rs are the consumers). If `cargo build` fails because the constant value no longer matches the manifest, STOP — Plan 01's insertion landed at the wrong position, and the symptom is misaligned `unsupported_unstable_request` / 4c1e gate lookups.

    Add tests for behavior 6 inside operator.rs `#[cfg(test)]`, using the actual integer values recorded in Step 0 for both the positive and negative cases.

    Step 6 — Land the manifest-agreement typed `#[test]` in `crates/cintx-ops/src/resolver.rs`.

    First, look for an existing `#[test]` in `resolver.rs` (line 290+ `#[cfg(test)]` block already exists; lines 347+ already use `OPERATOR_DESCRIPTORS` in test helpers per the resolver imports). If a sibling test already iterates `OPERATOR_DESCRIPTORS` with a similar shape, extend it. Otherwise add the new `#[test]` to the existing `#[cfg(test)] mod tests` block. Use this body:

    ```rust
    #[test]
    fn ecp_operator_ids_match_constants() {
        use cintx_core::operator::OperatorId;

        // Pair each ECP symbol with the typed constant cintx-core publishes.
        // The test fails if cintx-ops's generated OPERATOR_DESCRIPTORS table
        // and the typed constants in cintx-core::operator ever drift.
        let expected = [
            ("int1e_ecp_cart", OperatorId::INT1E_ECP_CART),
            ("int1e_ecp_sph", OperatorId::INT1E_ECP_SPH),
            ("int1e_ecp_ipnuc_cart", OperatorId::INT1E_ECP_IPNUC_CART),
            ("int1e_ecp_ipnuc_sph", OperatorId::INT1E_ECP_IPNUC_SPH),
        ];

        for (symbol, expected_id) in expected {
            let descriptor = crate::generated::OPERATOR_DESCRIPTORS
                .iter()
                .find(|d| d.entry.symbol_name == symbol)
                .unwrap_or_else(|| panic!("OPERATOR_DESCRIPTORS missing {symbol}"));
            assert_eq!(
                descriptor.id, expected_id,
                "OperatorId drift for {symbol}: manifest says {:?}, constant says {:?}",
                descriptor.id, expected_id,
            );
        }

        // Preservation invariant: existing int4c1e_cart entry must still carry
        // OperatorId::new(24). This is the central guarantee of the entire
        // +4 ID-shift containment plan from Plan 01.
        let int4c1e_cart_id = crate::generated::OPERATOR_DESCRIPTORS
            .iter()
            .find(|d| d.entry.symbol_name == "int4c1e_cart")
            .expect("int4c1e_cart missing")
            .id;
        assert_eq!(int4c1e_cart_id, OperatorId::new(24));
    }
    ```

    Import-path notes (adjust during execution if the modules disagree):

    - `crate::generated::OPERATOR_DESCRIPTORS` matches the existing import at `crates/cintx-ops/src/resolver.rs:1` (`use crate::generated::{MANIFEST_ENTRIES, OPERATOR_DESCRIPTORS};`). If the test ends up in a different module (or the existing import is local-only), use the actual path — confirm during execution by reading the top of resolver.rs.
    - `cintx_core::operator::OperatorId` matches the Plan 03 Step 3 declaration site. The four `OperatorId::INT1E_ECP_*` typed constants live as `impl OperatorId { pub const INT1E_ECP_CART: OperatorId = ...; }` per Step 3. Add `cintx-core = { path = "../cintx-core" }` to `[dev-dependencies]` in `crates/cintx-ops/Cargo.toml` if it is not already present in `[dependencies]` (check first — cintx-ops likely already depends on cintx-core; if so no Cargo.toml edit needed).
    - `OperatorId::new` is the existing newtype constructor (cintx-core/src/operator.rs:50-75). It accepts a `u32` integer and returns an `OperatorId`.
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/cintx &amp;&amp; cargo nextest run --locked -p cintx-core 2>&amp;1 | tail -15 &amp;&amp; cargo nextest run --locked -p cintx-ops -- ecp_operator_ids_match_constants 2>&amp;1 | tail -10 &amp;&amp; cargo build --locked -p cintx-rs 2>&amp;1 | tail -5 &amp;&amp; grep -F 'INT4C1E_CART_OPERATOR_ID: u32 = 24' crates/cintx-rs/src/api.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo nextest run --locked -p cintx-core` exits 0
    - `cargo build --locked -p cintx-rs` exits 0
    - **OperatorId preservation invariant:** `grep -F 'INT4C1E_CART_OPERATOR_ID: u32 = 24' crates/cintx-rs/src/api.rs` STILL matches unchanged (this MUST pass — it is the central guarantee of the entire +4 ID-shift containment plan)
    - `grep -F 'pub channel: EcpChannel' crates/cintx-core/src/ecp.rs` matches
    - `grep -F 'pub radial_power: i16' crates/cintx-core/src/ecp.rs` matches
    - `grep -F 'pub so_type: i16' crates/cintx-core/src/ecp.rs` matches
    - `grep -F 'ecp_shells: Arc<' crates/cintx-core/src/basis.rs` matches
    - `grep -F 'pub fn try_new_with_ecp' crates/cintx-core/src/basis.rs` matches
    - `grep -F 'pub fn ecp_shells' crates/cintx-core/src/basis.rs` matches
    - `grep -F 'pub const INT1E_ECP_CART' crates/cintx-core/src/operator.rs` matches
    - `grep -F 'pub const INT1E_ECP_SPH' crates/cintx-core/src/operator.rs` matches
    - `grep -F 'pub const INT1E_ECP_IPNUC_CART' crates/cintx-core/src/operator.rs` matches
    - `grep -F 'pub const INT1E_ECP_IPNUC_SPH' crates/cintx-core/src/operator.rs` matches
    - `grep -F 'pub const fn is_ecp' crates/cintx-core/src/operator.rs` matches
    - **F12 / IPIP1 constants resynced:** `grep -nE 'INT2E_STG_SPH_OPERATOR_ID: u32 = (102|106)' crates/cintx-rs/src/api.rs` matches (and the value matches what was derived from the regenerated manifest in Step 0)
    - **Manifest agreement invariant:** `cargo nextest run --locked -p cintx-ops -- ecp_operator_ids_match_constants` exits 0 (typed test asserts each of the four `int1e_ecp_*` symbols in `OPERATOR_DESCRIPTORS` carries the same `OperatorId` integer that `cintx_core::operator::OperatorId::INT1E_ECP_*` declares, AND the preservation invariant `int4c1e_cart` → `OperatorId::new(24)` still holds)
    - `grep -F 'fn ecp_operator_ids_match_constants' crates/cintx-ops/src/resolver.rs` matches (test landed in the target file)
    - `wc -l crates/cintx-core/src/ecp.rs` returns ≥ 80
  </acceptance_criteria>
  <done>EcpShell has 8 typed fields with validation; BasisSet exposes ecp_shells() and try_new_with_ecp(); OperatorId has four ECP constants derived (not hardcoded) from Plan 01's regenerated manifest, plus is_ecp(); INT4C1E_CART_OPERATOR_ID = 24 preserved; INT2E_STG_SPH_OPERATOR_ID and INT2E_IPIP1_SPH_OPERATOR_ID resynced to the post-shift values; a typed `#[test]` in cintx-ops/src/resolver.rs asserts OPERATOR_DESCRIPTORS ↔ cintx_core::operator::OperatorId::INT1E_ECP_* agreement plus the int4c1e_cart preservation invariant; cintx-core + cintx-ops + cintx-rs tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ECP slot constants, EcpBasArray view, RawApiId constants, and eval_raw ECP dispatch arm to cintx-compat</name>
  <files>
    crates/cintx-compat/src/raw.rs
  </files>
  <read_first>
    - /home/user/Documents/workspace/cintx/crates/cintx-compat/src/raw.rs (lines 22-29 slot constants block; lines 68-103 RawApiId constants block; lines 222-260 BasArray typed view; lines 446-455 F12 dispatch arm; line 572 is_f12_family_symbol helper)
    - /home/user/Documents/workspace/cintx/.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-PATTERNS.md (section on crates/cintx-compat/src/raw.rs — exact slot-constant text with rustdoc)
    - /home/user/Documents/workspace/cintx/.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-CONTEXT.md (D-05 REVISED: use upstream PySCF names verbatim — AS_ECPBAS_OFFSET=18, AS_NECPBAS=19, RADI_POWER=3, SO_TYPE_OF=4, ECP_LMAX=5; ecpbas reuses BAS_SLOTS=8 width — no separate ECP_BAS_SLOTS constant)
    - /home/user/Documents/workspace/cintx/vendor/pyscf-nr-ecp/include/nr_ecp.h (verify the constant values match upstream)
  </read_first>
  <behavior>
    - Test 1 (slot constants present and correctly valued): assert `RADI_POWER == 3`, `SO_TYPE_OF == 4`, `AS_ECPBAS_OFFSET == 18`, `AS_NECPBAS == 19`, `ECP_LMAX == 5`.
    - Test 2 (EcpBasArray::new accepts a slab whose length is a multiple of BAS_SLOTS): `EcpBasArray::new(&[0i32; 16])` succeeds (2 rows × 8 slots).
    - Test 3 (EcpBasArray::new rejects non-multiple length): `EcpBasArray::new(&[0i32; 9])` returns an error (mirror BasArray's existing error path).
    - Test 4 (EcpBasArray named getters): construct a 1-row slab with radial_power=2, so_type=0 at the correct slot offsets and verify `radial_power(0) == 2`, `so_type(0) == 0`.
    - Test 5 (eval_raw rejects ECP dispatch when env[AS_NECPBAS] == 0): construct an eval_raw call with `RawApiId::INT1E_ECP_SPH`, env pre-padded to PTR_ENV_START with env[AS_NECPBAS]=0.0, and verify the call returns the typed InvalidEnvParam-style error (or whichever existing error variant the F12 zeta=0 path uses) WITHOUT calling into the kernel.
    - Test 6 (RawApiId constants): `RawApiId::INT1E_ECP_CART.as_symbol() == Some("int1e_ecp_cart")` etc.
  </behavior>
  <action>
    Step 1 — Append the ECP slot-constant block to raw.rs after the existing slot constants block (after PTR_GRIDS at ~line 56) per 19-PATTERNS.md. Use upstream PySCF names verbatim (D-05 REVISED):

    Lines to append (place each on its own line with the cited rustdoc):

    - `pub const RADI_POWER: usize = 3;` — rustdoc: "ECP basis row slot: angular momentum power of r in V_l(r). Matches PySCF nr_ecp.h `RADI_POWER`. Reinterprets the BAS slot index 3 (NCTR_OF for ordinary bas rows). ecpbas rows reuse BAS_SLOTS=8 width."
    - `pub const SO_TYPE_OF: usize = 4;` — rustdoc citing PySCF nr_ecp.h `SO_TYPE_OF`, noting it reinterprets KAPPA_OF (slot 4). 0 = scalar; nonzero = SO (out of scope per D-12).
    - `pub const AS_ECPBAS_OFFSET: usize = 18;` — rustdoc citing PySCF nr_ecp.h `AS_ECPBAS_OFFSET = 18`.
    - `pub const AS_NECPBAS: usize = 19;` — rustdoc citing PySCF nr_ecp.h `AS_NECPBAS = 19`.
    - `pub const ECP_LMAX: usize = 5;` — rustdoc citing PySCF nr_ecp.h `ECP_LMAX = 5`.

    Do NOT add a separate `ECP_BAS_SLOTS` constant — the ecpbas row width is the existing `BAS_SLOTS = 8` (per 19-RESEARCH.md §"ECP_BAS_SLOTS value": slots 3 and 4 are reinterpreted; row width unchanged).

    Step 2 — Add `EcpBasArray` typed view in raw.rs immediately after the existing `BasArray` typed view (lines 222-260). Mirror BasArray's shape:
      - `pub struct EcpBasArray<'a> { slab: &'a [i32] }`
      - `impl<'a> EcpBasArray<'a> { pub fn new(slab: &'a [i32]) -> Result<Self, RawError> { if slab.len() % BAS_SLOTS != 0 { return Err(/* same error variant BasArray uses */); } Ok(Self { slab }) } pub fn len(&self) -> usize { self.slab.len() / BAS_SLOTS } pub fn is_empty(&self) -> bool { self.len() == 0 } pub fn row(&self, index: usize) -> &[i32] { let start = index.checked_mul(BAS_SLOTS).expect("ecpbas row index overflow"); &self.slab[start..start + BAS_SLOTS] } pub fn radial_power(&self, index: usize) -> i32 { self.row(index)[RADI_POWER] } pub fn so_type(&self, index: usize) -> i32 { self.row(index)[SO_TYPE_OF] } pub fn iter_rows(&self) -> std::slice::ChunksExact<'_, i32> { self.slab.chunks_exact(BAS_SLOTS) } }`
      - Reuse the exact error variant `BasArray::new` returns for the length-not-multiple-of-BAS_SLOTS case (do not invent a new one — the existing BasArray case at lines 222-260 already handles this).

    Step 3 — Extend the `impl RawApiId` constants block (lines 68-103) with four constants after `INT4C1E_SPH`:

    - `pub const INT1E_ECP_CART: Self = Self::Symbol("int1e_ecp_cart");`
    - `pub const INT1E_ECP_SPH: Self = Self::Symbol("int1e_ecp_sph");`
    - `pub const INT1E_ECP_IPNUC_CART: Self = Self::Symbol("int1e_ecp_ipnuc_cart");`
    - `pub const INT1E_ECP_IPNUC_SPH: Self = Self::Symbol("int1e_ecp_ipnuc_sph");`

    Step 4 — Add `is_ecp_family_symbol` helper near the existing `is_f12_family_symbol` (around line 572):

    `fn is_ecp_family_symbol(symbol: &str) -> bool { symbol.starts_with("int1e_ecp_") }`

    Step 5 — Add the ECP dispatch arm in `eval_raw` mirroring the F12 arm at lines 446-455. Concrete insertion (place near the existing F12 block):

    ```
    if is_ecp_family_symbol(plan.descriptor.operator_symbol()) {
        let necpbas = env.get(AS_NECPBAS).copied().unwrap_or(0.0);
        if necpbas <= 0.0 || !necpbas.is_finite() {
            return Err(cintxRsError::InvalidEnvParam {
                param: "AS_NECPBAS",
                reason: "env[AS_NECPBAS] is 0 — no ecpbas attached for ECP operator".to_owned(),
            });
        }
        // ecpbas pointer lives at env[AS_ECPBAS_OFFSET]; ecpbas rows are packed
        // after bas in the same i32 slab per PySCF convention (19-RESEARCH.md A2).
        // Kernel-side dispatch in Plan 04 reads the ecpbas slice via EcpBasArray.
    }
    ```

    Use the actual cintxRsError variant that the F12 path uses for `InvalidEnvParam` (read `crates/cintx-runtime/src/validator.rs` to find the exact variant; if the F12 path uses a different error type, use the same one for ECP). The exact variant name is less important than ensuring it is a TYPED error, not a stringly-typed `UnsupportedApi`.

    Add a `#[cfg(test)] mod tests` block at the end of the file (or extend the existing one) with all six behavior tests above.
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/cintx && cargo nextest run --locked -p cintx-compat 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo nextest run --locked -p cintx-compat` exits 0
    - `grep -F 'pub const RADI_POWER: usize = 3;' crates/cintx-compat/src/raw.rs` matches
    - `grep -F 'pub const SO_TYPE_OF: usize = 4;' crates/cintx-compat/src/raw.rs` matches
    - `grep -F 'pub const AS_ECPBAS_OFFSET: usize = 18;' crates/cintx-compat/src/raw.rs` matches
    - `grep -F 'pub const AS_NECPBAS: usize = 19;' crates/cintx-compat/src/raw.rs` matches
    - `grep -F 'pub const ECP_LMAX: usize = 5;' crates/cintx-compat/src/raw.rs` matches
    - `grep -F 'pub struct EcpBasArray' crates/cintx-compat/src/raw.rs` matches
    - `grep -F 'INT1E_ECP_CART' crates/cintx-compat/src/raw.rs` matches
    - `grep -F 'INT1E_ECP_SPH' crates/cintx-compat/src/raw.rs` matches
    - `grep -F 'INT1E_ECP_IPNUC_CART' crates/cintx-compat/src/raw.rs` matches
    - `grep -F 'INT1E_ECP_IPNUC_SPH' crates/cintx-compat/src/raw.rs` matches
    - `grep -F 'fn is_ecp_family_symbol' crates/cintx-compat/src/raw.rs` matches
    - `grep -F 'AS_NECPBAS' crates/cintx-compat/src/raw.rs` returns 3 or more matches (constant + dispatch arm + test)
    - `grep -F 'ECP_BAS_SLOTS' crates/cintx-compat/src/raw.rs` returns 0 matches (no new width constant — reuses BAS_SLOTS per D-05)
  </acceptance_criteria>
  <done>cintx-compat exposes the five PySCF-named ECP slot constants and the matching EcpBasArray typed view; RawApiId has four new ECP constants; eval_raw fails fast with a typed env-param error when an ECP operator is dispatched with env[AS_NECPBAS]==0; ≥6 unit tests cover the slot values, the view, and the dispatch guard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add FacadeError::MissingEcpBasis variant and SessionRequest::query_workspace preflight</name>
  <files>
    crates/cintx-rs/src/error.rs,
    crates/cintx-rs/src/api.rs,
    crates/cintx-rs/src/prelude.rs
  </files>
  <read_first>
    - /home/user/Documents/workspace/cintx/crates/cintx-rs/src/error.rs (lines 6-13 FacadeErrorKind enum; lines 15-27 FacadeError enum; lines 29-38 kind() match — Phase 18 UnsupportedAoSymmetry precedent)
    - /home/user/Documents/workspace/cintx/crates/cintx-rs/src/api.rs (lines 63-90 — query_workspace aosym preflight at lines 67-73 is the sibling-insertion-point analog; also lines 540-546 for the OperatorId constants Task 1 resynced)
    - /home/user/Documents/workspace/cintx/crates/cintx-rs/src/prelude.rs (re-export block — FacadeError and OperatorId already re-exported; verify no new re-export needed)
    - /home/user/Documents/workspace/cintx/.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-PATTERNS.md (Pattern B: typed FacadeError variant; Pattern C: query_workspace preflight)
    - /home/user/Documents/workspace/cintx/.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-CONTEXT.md (D-06 — MissingEcpBasis fields and preflight insertion point)
  </read_first>
  <behavior>
    - Test 1 (FacadeErrorKind::MissingEcpBasis exists): direct construction `FacadeErrorKind::MissingEcpBasis` compiles.
    - Test 2 (FacadeError::MissingEcpBasis variant carries operator: String): `let e = FacadeError::MissingEcpBasis { operator: "int1e_ecp_sph".to_owned() }; assert_eq!(e.kind(), FacadeErrorKind::MissingEcpBasis);` succeeds.
    - Test 3 (Display message): `format!("{e}")` matches the regex `operator '.*' requires ECP basis, but BasisSet::ecp_shells\\(\\) is empty`.
    - Test 4 (query_workspace returns MissingEcpBasis): construct a SessionRequest with `OperatorId::INT1E_ECP_SPH` and a BasisSet whose `ecp_shells()` is empty; assert query_workspace returns `Err(FacadeError::MissingEcpBasis { operator })` where `operator == "int1e_ecp_sph"`.
    - Test 5 (query_workspace passes through for non-ECP operators): same construction with `OperatorId::INT1E_OVLP_SPH` returns a non-MissingEcpBasis result (either Ok or whatever the existing path returns).
    - Test 6 (query_workspace passes through for ECP operator WITH ecp_shells attached): construct a BasisSet via try_new_with_ecp with at least one EcpShell; assert query_workspace does NOT return MissingEcpBasis (it may return another error from later runtime planning, but the preflight does not block).
  </behavior>
  <action>
    Step 1 — `crates/cintx-rs/src/error.rs`: add the `MissingEcpBasis` discriminant and variant per 19-PATTERNS.md Pattern B. Three surgical edits:

      (a) Append `MissingEcpBasis,` to `FacadeErrorKind` after the existing `UnsupportedAoSymmetry,` discriminant. Keep the existing `#[derive]` chain.

      (b) Append a new `FacadeError` variant after `UnsupportedAoSymmetry { requested: String }`:

      `#[error("operator '{operator}' requires ECP basis, but BasisSet::ecp_shells() is empty")]`
      `MissingEcpBasis { operator: String },`

      (c) Add an arm to the `kind()` match: `Self::MissingEcpBasis { .. } => FacadeErrorKind::MissingEcpBasis,`.

    The `From<cintxRsError> for FacadeError` impl does NOT change — MissingEcpBasis is facade-only and is only emitted by `query_workspace`.

    Step 2 — `crates/cintx-rs/src/api.rs`: insert the preflight check inside `SessionRequest::query_workspace`, immediately after the existing aosym preflight (after lines 67-73) and before the `runtime_query_workspace` call. Concrete edit:

    ```
    // Phase 19 D-06: ECP-basis preflight — operator.is_ecp() && basis.ecp_shells().is_empty()
    // returns FacadeError::MissingEcpBasis. Fails fast before any runtime work,
    // mirroring the aosym preflight above.
    if self.operator.is_ecp() && self.basis.ecp_shells().is_empty() {
        let symbol = Resolver::descriptor(self.operator)
            .map(|d| d.operator_symbol().to_string())
            .unwrap_or_else(|_| format!("{}", self.operator));
        return Err(FacadeError::MissingEcpBasis { operator: symbol });
    }
    ```

    Resolve the operator symbol via `Resolver::descriptor(self.operator)` — the descriptor's `operator_symbol()` returns the canonical name (e.g. "int1e_ecp_sph"). If the resolver returns Err (operator id missing from manifest), fall back to the Display impl of OperatorId so the error message is never empty. Use the `Resolver` import path already present in api.rs (if absent, add `use cintx_ops::resolver::Resolver;` near the existing imports).

    Step 3 — `crates/cintx-rs/src/prelude.rs`: confirm `FacadeError` and `FacadeErrorKind` are already re-exported. No change needed if so. If the prelude exports a `FacadeError` enum but not the kind, leave it; the kind is rarely needed by downstream consumers.

    Add unit tests for behaviors 1-6 in either the existing `#[cfg(test)]` module at the bottom of api.rs or a new one. The tests construct a small `BasisSet` (one H atom, one s-shell) and the safe API call shape; test 4 deliberately omits ecp_shells, test 6 adds one minimal EcpShell.
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/cintx && cargo nextest run --locked -p cintx-rs 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo nextest run --locked -p cintx-rs` exits 0
    - `grep -F 'MissingEcpBasis' crates/cintx-rs/src/error.rs` returns ≥ 3 matches (kind discriminant, variant, kind() arm)
    - `grep -F 'requires ECP basis' crates/cintx-rs/src/error.rs` matches the Display message
    - `grep -F 'self.operator.is_ecp()' crates/cintx-rs/src/api.rs` matches the preflight predicate
    - `grep -F 'self.basis.ecp_shells().is_empty()' crates/cintx-rs/src/api.rs` matches the preflight predicate
    - `grep -F 'FacadeError::MissingEcpBasis' crates/cintx-rs/src/api.rs` matches the return statement
    - `cargo build --locked -p cintx-rs` exits 0 with no `dead_code` warnings on the new variant
    - **Preservation invariant (still holds after this plan's edits):** `grep -F 'INT4C1E_CART_OPERATOR_ID: u32 = 24' crates/cintx-rs/src/api.rs` still matches
  </acceptance_criteria>
  <done>FacadeError::MissingEcpBasis variant is wired through Display, kind(), and the SessionRequest::query_workspace preflight; the preflight runs immediately after the aosym preflight and fails fast before runtime planning; ≥6 unit tests cover the variant construction, Display message, and preflight gating.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| safe API → raw API | SessionRequest::query_workspace runs typed preflight before any unsafe FFI dispatch |
| raw API → vendored C | eval_raw env-slot validation prevents UB on malformed ecpbas slabs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07 | Information disclosure | `FacadeError::MissingEcpBasis { operator: String }` | mitigate | operator field is bounded to the canonical operator symbol resolved via Resolver::descriptor; never carries user-supplied path or arbitrary text. |
| T-19-08 | Integrity (silent UB) | `EcpBasArray::new` length check | mitigate | Rejects slabs not a multiple of BAS_SLOTS via typed error; prevents indexing past the buffer in launch_ecp. |
| T-19-09 | DoS (panic in safe API) | `query_workspace` preflight | mitigate | unwrap_or_else fallback on Resolver::descriptor so a missing manifest entry never panics. |
| T-19-09b | Integrity (silent ID drift) | OperatorId constants vs manifest | mitigate | Task 1 Step 0 read-and-derives the four ECP IDs and the post-shift F12/IPIP1 IDs from the regenerated manifest; the typed `ecp_operator_ids_match_constants` `#[test]` in cintx-ops/src/resolver.rs (Step 6) asserts the four constants in cintx-core::operator agree with OPERATOR_DESCRIPTORS at test-run time, so the constants never silently desync from the manifest. |
</threat_model>

<verification>
- `cargo nextest run --locked -p cintx-core -p cintx-compat -p cintx-ops -p cintx-rs` exits 0
- `grep -c 'MissingEcpBasis' crates/cintx-rs/src/error.rs` ≥ 3
- `grep -F 'is_ecp()' crates/cintx-rs/src/api.rs` matches the preflight
- `cargo build --locked --workspace` exits 0 (the additive changes do not regress any consumer crate)
- `INT4C1E_CART_OPERATOR_ID = 24` preservation invariant in api.rs intact
- `cargo nextest run --locked -p cintx-ops -- ecp_operator_ids_match_constants` exits 0 — typed agreement check between `OPERATOR_DESCRIPTORS` and `cintx_core::operator::OperatorId::INT1E_ECP_*`, plus the `int4c1e_cart` → `OperatorId::new(24)` preservation invariant
</verification>

<success_criteria>
Typed and raw surfaces for ECP are complete and fail-fast. Plan 04 (kernel + parity) can now write `SessionRequest::new(OperatorId::INT1E_ECP_SPH, &basis, &shells, options).evaluate()` and read the ecpbas slab via `EcpBasArray` inside `launch_ecp`. The OperatorId preservation + manifest-agreement invariants are intact and enforced by a typed `#[test]`, not by an unreliable grep gate.
</success_criteria>

<output>
After completion, create `.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-03-SUMMARY.md` capturing: actual OperatorId values that ended up in operator.rs (derived from manifest — record the four integers), the actual post-shift values written to INT2E_STG_SPH_OPERATOR_ID and INT2E_IPIP1_SPH_OPERATOR_ID in cintx-rs/src/api.rs, any new CoreError variant added for EcpShell::try_new validation, a note confirming whether `BasArray`'s error variant transfers cleanly to `EcpBasArray::new` (or whether a new variant was needed), and confirmation that `ecp_operator_ids_match_constants` exits 0 in cintx-ops on the recorded toolchain.
</output>
</content>
