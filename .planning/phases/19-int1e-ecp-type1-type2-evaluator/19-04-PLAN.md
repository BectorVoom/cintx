---
phase: 19
plan: 04
type: execute
wave: 2
depends_on: [19-01, 19-02, 19-03]
files_modified:
  - crates/cintx-cubecl/src/kernels/ecp.rs
  - crates/cintx-cubecl/src/kernels/mod.rs
  - crates/cintx-oracle/src/vendor_ffi.rs
  - crates/cintx-oracle/tests/safe_api_ecp_parity.rs
  - crates/cintx-ops/src/generated/api_manifest.csv
autonomous: true
requirements: [ECP-01, ECP-02, ECP-03, ECP-04]
canonical_family: ecp
forms: [cart, sph]
component_rank: scalar

must_haves:
  truths:
    - "launch_ecp(backend, plan, key, staging) is the canonical_family=ecp launcher; kernels::mod.rs's resolve_family_name returns Some(launch_ecp) for the literal canonical family 'ecp' and supports_canonical_family includes 'ecp' in the unconditional-true arm (no #[cfg(feature)] gate per D-09 stability=stable)"
    - "launch_ecp dispatches Type-1 (channel == Local) via Gauss-Hermite radial expansion + Boys-family Gaussian product geometry, and Type-2 (channel == Projected(l)) via modified spherical Bessel angular projection + Gauss-Chebyshev radial quadrature, accumulating per (i,j,c) shell triple into the F-order Cartesian buffer"
    - "kernels/ecp.rs carries at least two `// Source: vendor/pyscf-nr-ecp/src/nr_ecp.c:LXX-LYY (ECPtype1_cart|ECPtype2_cart)` rustdoc-line citations pointing at the upstream algorithm range that compute_type1_pair / compute_type2_pair port; normalization convention (PySCF vs cintx) and PA/PB displacement convention are documented in the same rustdoc block"
    - "Spherical output reuses the existing crate::transform::c2s::cart_to_sph_1e transform; no new transform code"
    - "vendor_ECPscalar_sph and vendor_ECPscalar_cart wrap PySCF nr_ecp's C entry points with the (ecpbas, necpbas) extension to the 1e arity-2 vendor FFI signature"
    - "safe_api_ecp_parity.rs contains two #[test] functions, test_int1e_ecp_cart_safe_api_parity and test_int1e_ecp_sph_safe_api_parity, both gated by #[cfg(all(has_vendor_libcint, has_vendor_pyscf_nr_ecp))], both asserting byte-identity vs PySCF nr_ecp at atol=1e-12, rtol=0.0 on the full Cu/LANL2DZ shell Cartesian product (≥8 AO shells × ≥3 ECP projectors per the Plan 01 Task 2 coverage invariant)"
    - "api_manifest.csv has oracle_covered = true on the int1e_ecp_cart and int1e_ecp_sph rows once parity passes; compiled_manifest.lock.json regenerated"
  artifacts:
    - path: "crates/cintx-cubecl/src/kernels/ecp.rs"
      provides: "launch_ecp host-side launcher with Type-1+Type-2 internal branch, calls into math/bessel.rs and math/radial_quadrature.rs; cites nr_ecp.c source line ranges in rustdoc"
      min_lines: 250
    - path: "crates/cintx-cubecl/src/kernels/mod.rs"
      provides: "resolve_family_name and supports_canonical_family arms for 'ecp'"
    - path: "crates/cintx-oracle/src/vendor_ffi.rs"
      provides: "vendor_ECPscalar_sph + vendor_ECPscalar_cart wrappers (illustrative — confirm exact C names against vendored nr_ecp.h)"
    - path: "crates/cintx-oracle/tests/safe_api_ecp_parity.rs"
      provides: "Per-symbol byte-identity tests at atol=1e-12 vs PySCF nr_ecp over Cu/LANL2DZ"
      min_lines: 200
  key_links:
    - from: "crates/cintx-cubecl/src/kernels/mod.rs::resolve_family_name"
      to: "crates/cintx-cubecl/src/kernels/ecp.rs::launch_ecp"
      via: "literal canonical_family 'ecp'"
      pattern: "\"ecp\" => Some\\(ecp::launch_ecp"
    - from: "crates/cintx-cubecl/src/kernels/ecp.rs"
      to: "crates/cintx-cubecl/src/math/bessel.rs::modified_spherical_bessel_in_host"
      via: "Type-2 angular projector evaluation"
      pattern: "modified_spherical_bessel_in_host"
    - from: "crates/cintx-cubecl/src/kernels/ecp.rs"
      to: "crates/cintx-cubecl/src/math/radial_quadrature.rs"
      via: "Gauss-Chebyshev nodes for Type-2 radial integral; Gauss-Hermite for Type-1"
      pattern: "gauss_chebyshev_nodes_weights_host|gauss_hermite_nodes_weights_host"
    - from: "crates/cintx-oracle/tests/safe_api_ecp_parity.rs"
      to: "vendor_ECPscalar_(cart|sph)"
      via: "byte-identity comparison at atol=1e-12 over Cu/LANL2DZ shell Cartesian product"
      pattern: "vendor_ECPscalar_(cart|sph)"
---

<objective>
Land the scalar Type-1 (local, Coulomb-like) and Type-2 (semi-local, spin-orbit-like) ECP integral kernel as `launch_ecp` in `crates/cintx-cubecl/src/kernels/ecp.rs`, register it in `kernels::resolve_family_name` under the literal canonical_family "ecp", and prove byte-identity against the vendored PySCF nr_ecp reference at atol=1e-12 on the Cu/LANL2DZ fixture for both cart and sph representations.

Purpose: Close ECP-01 (Type-1), ECP-02 (Type-2), ECP-03 (SessionRequest dispatch), and the scalar half of ECP-04 (byte-identity parity for `int1e_ecp_{cart,sph}`). Plan 05 handles the gradient half (`int1e_ecp_ipnuc_*`); Plan 06 handles the optional libecpint secondary cross-check.
Output: One new kernel file (with explicit upstream-line citations), three-touchpoint registration in kernels/mod.rs, two FFI wrappers, two parity tests, manifest oracle_covered flag flipped, regenerated lock.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-CONTEXT.md
@.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-RESEARCH.md
@.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-PATTERNS.md
@.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-VALIDATION.md
@CLAUDE.md
@crates/cintx-cubecl/src/kernels/one_electron.rs
@crates/cintx-cubecl/src/kernels/f12.rs
@crates/cintx-cubecl/src/kernels/mod.rs
@crates/cintx-cubecl/src/transform/c2s.rs
@crates/cintx-cubecl/src/math/bessel.rs
@crates/cintx-cubecl/src/math/radial_quadrature.rs
@vendor/pyscf-nr-ecp/include/nr_ecp.h
@vendor/pyscf-nr-ecp/src/nr_ecp.c
@crates/cintx-oracle/src/vendor_ffi.rs
@crates/cintx-oracle/src/fixtures.rs
@crates/cintx-oracle/tests/safe_api_arity2_parity.rs

<interfaces>
launch_one_electron signature (cintx-cubecl/src/kernels/one_electron.rs:434), direct shape analog for launch_ecp:

```
pub fn launch_one_electron(
    backend: &ResolvedBackend,
    plan: &ExecutionPlan<'_>,
    specialization: &SpecializationKey,
    staging: &mut [f64],
) -> Result<ExecutionStats, cintxRsError>
```

The launcher early-returns ChunkPlanFailed if canonical_family mismatches. Lets _ = backend for host-side pipelines. Reads shells via plan.shells.as_slice(). Cartesian accumulation buffer, then apply_representation_transform via cart_to_sph_1e for sph output.

Kernel registry from kernels/mod.rs lines 25-58:

```
fn resolve_family_name(canonical_family: &str) -> Option<FamilyLaunchFn> {
    match canonical_family {
        "1e" => Some(one_electron::launch_one_electron as FamilyLaunchFn),
        // ...
        #[cfg(feature = "with-f12")] "f12" => Some(f12::launch_f12 as FamilyLaunchFn),
        // ...
        _ => None,
    }
}
pub fn supports_canonical_family(canonical_family: &str) -> bool {
    match canonical_family {
        "1e" | "2e" | "2c2e" | "3c1e" | "3c2e" => true,
        "4c1e" => cfg!(feature = "with-4c1e"),
        "f12" => cfg!(feature = "with-f12"),
        _ => false,
    }
}
```

Cart-to-sph transform reuse (cintx-cubecl/src/transform/c2s.rs):

```
pub fn cart_to_sph_1e(cart: &[f64], sph: &mut [f64], li: u8, lj: u8);
```

Vendor FFI wrapper analog (cintx-oracle/src/vendor_ffi.rs lines 21-44):

```
pub fn vendor_int1e_ovlp_sph(out: &mut [f64], shls: &[i32; 2], atm: &[i32], natm: i32, bas: &[i32], nbas: i32, env: &[f64]) -> i32 {
    unsafe { ffi::int1e_ovlp_sph(out.as_mut_ptr(), ptr::null_mut(), shls.as_ptr() as *mut i32, atm.as_ptr() as *mut i32, natm, bas.as_ptr() as *mut i32, nbas, env.as_ptr() as *mut f64, ptr::null_mut(), ptr::null_mut()) }
}
```

ECP function pointer typedef from vendor/pyscf-nr-ecp/include/nr_ecp.h (PER 19-RESEARCH.md):

```
typedef int Function_cart(double *gctr, int *shls, int *ecpbas, int necpbas, int *atm, int natm, int *bas, int nbas, double *env, ECPOpt *opt, double *cache);
```

Tolerance literals: const ATOL: f64 = 1e-12; const RTOL: f64 = 0.0; (Phase 15 unified, Phase 17/18 file-top precedent).

Parity test pattern from safe_api_arity2_parity.rs lines 591-635 — one named #[test] per (operator, representation), NOT a parametric loop, for per-symbol CI failure visibility.

Plan 01 Task 2 coverage invariant (consumed here): `crates/cintx-oracle/data/cu_lanl2dz.json` carries ≥8 AO shell entries and ≥3 ECP projector entries (verified via `jq '(.shells | length) >= 8 and ((.ecp.shells | length) >= 3)'`). The "full Cartesian product" truth claim in this plan's parity tests rests on this invariant — i.e. parity is exercised over at minimum 8×8 = 64 shell-pair tuples paired against ≥3 ECP projectors.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement launch_ecp kernel (Type-1 + Type-2 scalar) with cited upstream line ranges, and register canonical_family "ecp"</name>
  <files>
    crates/cintx-cubecl/src/kernels/ecp.rs,
    crates/cintx-cubecl/src/kernels/mod.rs
  </files>
  <read_first>
    - /home/user/Documents/workspace/cintx/crates/cintx-cubecl/src/kernels/one_electron.rs (lines 434-560 — launch_one_electron is the closest 1e arity-2 shape analog)
    - /home/user/Documents/workspace/cintx/crates/cintx-cubecl/src/kernels/f12.rs (lines 1517-1568 — operator-suffix dispatch pattern; we use this for the (scalar ecp) vs (ipnuc gradient) split, though Plan 05 will add the gradient half)
    - /home/user/Documents/workspace/cintx/crates/cintx-cubecl/src/kernels/mod.rs (lines 25-58 — resolve_family_name + supports_canonical_family arms)
    - /home/user/Documents/workspace/cintx/crates/cintx-cubecl/src/transform/c2s.rs (cart_to_sph_1e signature + usage)
    - /home/user/Documents/workspace/cintx/crates/cintx-cubecl/src/math/bessel.rs (Plan 02 output — modified_spherical_bessel_in_host signature)
    - /home/user/Documents/workspace/cintx/crates/cintx-cubecl/src/math/radial_quadrature.rs (Plan 02 output — gauss_chebyshev_nodes_weights_host and gauss_hermite_nodes_weights_host signatures)
    - /home/user/Documents/workspace/cintx/vendor/pyscf-nr-ecp/src/nr_ecp.c (the ECPtype1_cart and ECPtype2_cart functions — algorithm reference, copy the shell-pair loop structure)
    - /home/user/Documents/workspace/cintx/.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-PATTERNS.md (sections on kernels/ecp.rs and kernels/mod.rs)
    - /home/user/Documents/workspace/cintx/.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-RESEARCH.md (§"Algorithmic Foundation Type-1 + Type-2" and the data-flow diagram)
    - /home/user/Documents/workspace/cintx/.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-CONTEXT.md (D-07, D-08, D-09)
  </read_first>
  <action>
    **Step 0 (MANDATORY pre-implementation upstream survey) — Record the source-line ranges of the algorithms being ported.**

    Read `vendor/pyscf-nr-ecp/src/nr_ecp.c` and locate the upstream entry points the kernel ports. The exact symbol names depend on Plan 01's vendor pull, but they are almost certainly `ECPtype1_cart` and `ECPtype2_cart` (or `ECPtype1` / `ECPtype2` if the vendored source uses the dispatched-form name). For each entry point:

    - Record the line range where the function definition lives (use `grep -nE '^(static |int )?ECPtype(1|2)' vendor/pyscf-nr-ecp/src/nr_ecp.c` to find the starting line, and read forward to the closing `}` to find the end line).
    - Document the PySCF normalization convention used inside the function body — specifically how the Gaussian primitive normalization factor `N(α, l)` is applied (PySCF embeds it in the contraction coefficients vs cintx's separate `normalization_factor` field — note any difference).
    - Document the PA / PB (atom-to-ECP-center displacement) convention — which order, which sign, whether the center is stored relative to atom A or atom B.

    Embed these notes as `// Source: vendor/pyscf-nr-ecp/src/nr_ecp.c:LXX-LYY (ECPtypeN_cart)` rustdoc-line comments at the top of `crates/cintx-cubecl/src/kernels/ecp.rs`. The file MUST contain AT LEAST TWO such citation comments — one per ECP type — pointing at concrete `nr_ecp.c` line ranges. Also include a rustdoc subsection titled `## Normalization & coordinate convention` documenting the per-primitive normalization mapping and the PA/PB displacement convention you ported.

    Step 1 — Create `crates/cintx-cubecl/src/kernels/ecp.rs`. Use launch_one_electron (one_electron.rs:434) as the direct shape analog. Signature:

    ```
    pub fn launch_ecp(
        backend: &ResolvedBackend,
        plan: &ExecutionPlan<'_>,
        specialization: &SpecializationKey,
        staging: &mut [f64],
    ) -> Result<ExecutionStats, cintxRsError>
    ```

    Body outline (host-side pipeline; `let _ = backend;` per one_electron.rs:451):
      1. **canonical_family guard** — if `specialization.canonical_family() != "ecp"` return `ChunkPlanFailed { from: "cubecl_ecp", detail: format!("canonical_family mismatch: expected ecp, got {}", specialization.canonical_family()) }`.
      2. **Operator-name branch** — read `plan.descriptor.operator_name()`. For the scalar half landing in Plan 04, accept "ecp" (= `int1e_ecp_{cart,sph}`); for "ecp_ipnuc" return `cintxRsError::UnsupportedApi { requested: ... }` — Plan 05 will replace that arm with the gradient implementation. Do NOT inline the gradient algorithm here; leave a clear `// Plan 05: gradient branch lands here.` marker.
      3. **Shell access** — `let shells = plan.shells.as_slice();` (matches one_electron.rs:447). Assert `shells.len() == 2`; else `ChunkPlanFailed`.
      4. **ECP shell access** — `let ecp_shells = plan.basis.ecp_shells();` (the new accessor from Plan 03). Assert `!ecp_shells.is_empty()` else `ChunkPlanFailed` with detail "ecp_shells empty at kernel layer — query_workspace preflight should have caught this".
      5. **Cartesian buffer allocation** — using `transform::c2s::ncart(li)` × `ncart(lj)` as in one_electron.rs.
      6. **Shell-pair loop** — for each (i, j) pair from `shells[0]` / `shells[1]`, iterate over their primitive (exp, coeff) pairs. For each ECP shell `c` in `ecp_shells`:
         - **Algorithm branch on `c.channel`** (NOT on the operator name — 19-CONTEXT.md `<specifics>` and 19-PATTERNS.md "Phase 19 deviations"):
           - `EcpChannel::Local` ⇒ Type-1: call `compute_type1_pair(&ai, &aj, c.radial_power, &gauss_hermite_nodes_weights_host(N_HERMITE), …)`. The algorithm follows nr_ecp.c `ECPtype1_cart` (cited line range from Step 0). A Gaussian product × `r_C^{radial_power - 2}` × `e^{-zeta_c r_C^2}` integral, evaluated via Gauss-Hermite radial expansion (Plan 02's radial_quadrature). Boys-function machinery from `math::boys::boys_gamma_inc_host` handles the radial_power=0 special case.
           - `EcpChannel::Projected(l)` ⇒ Type-2: call `compute_type2_pair(&ai, &aj, l, c.radial_power, …)`. Algorithm: spherical-wave expansion of each AO Gaussian around the ECP center using `modified_spherical_bessel_in_host(l_max=li+lj+l, beta=2*zeta_ao*r_ao)`, Wigner / Clebsch-Gordan angular collapse to the projector `Y_lm`, then radial integral via Gauss-Chebyshev second-kind nodes from `gauss_chebyshev_nodes_weights_host(LEVEL0)`. Cite the nr_ecp.c line range for the algorithm in a rustdoc comment.
         - Accumulate into the Cartesian buffer in F-order `[ao_j, ao_i]` (ao_i fastest-varying — matches the 1e convention from one_electron.rs).
      7. **Representation transform** — if `plan.representation == Representation::Spheric`, call `crate::transform::c2s::cart_to_sph_1e(&cart_buf, &mut sph_buf, li, lj)` and copy sph_buf into staging. Else copy cart_buf directly. Mirror one_electron.rs:514+ for the application shape.
      8. **Return** `Ok(ExecutionStats { … })` per the one_electron.rs convention (zero-copy stats; the exact ExecutionStats field set is the existing one used by every 1e launcher).

    Define `compute_type1_pair` and `compute_type2_pair` as private host-side helpers inside the same file. They take typed primitives (a shell-pair specification + an EcpShell + the precomputed nodes/weights) and return a contribution into the caller-provided slice. The hot loops use Plan 02's `*_host()` math wrappers (no `#[cube]` invocations — D-07 / Phase 8 P02: host-side pipeline for now). A future Plan can promote the inner loop to `#[cube]` if profiling motivates it.

    Document at file top:
      - `// Source: vendor/pyscf-nr-ecp/src/nr_ecp.c:LXX-LYY (ECPtype1_cart)` with the upstream line range FROM STEP 0.
      - `// Source: vendor/pyscf-nr-ecp/src/nr_ecp.c:LXX-LYY (ECPtype2_cart)` with the upstream line range FROM STEP 0.
      - `## Normalization & coordinate convention` rustdoc subsection covering PySCF vs cintx primitive normalization and PA/PB displacement.
      - Note: "host-side pipeline; let _ = backend per one_electron.rs:451."
      - Note: "Gradient branch (operator_name == \"ecp_ipnuc\") returns UnsupportedApi; Plan 05 lands the gradient algorithm here."

    Step 2 — `crates/cintx-cubecl/src/kernels/mod.rs`: three-touchpoint registration per 19-PATTERNS.md Pattern D:

      (a) Add `pub mod ecp;` near the existing module declarations (after line 8).

      (b) In `resolve_family_name`, add `"ecp" => Some(ecp::launch_ecp as FamilyLaunchFn),` in the unconditional section (no `#[cfg(feature = ...)]` guard per D-09 stability=stable). Insert between the existing `"3c2e"` arm and the F12 `#[cfg]` arm — the exact position is fine anywhere in the unconditional block.

      (c) In `supports_canonical_family`, extend the unconditional `true` arm: `"1e" | "2e" | "2c2e" | "3c1e" | "3c2e" | "ecp" => true,`.

      (d) `unresolved_families` (lines 62-74) does NOT need changes — ECP is unconditional.

      (e) The test `family_registry_resolves_base_slice` (lines 175-188) gains one new line:

      ```
      let ecp = build_plan(basis_with_ecp, OperatorId::INT1E_ECP_CART, Representation::Cart, 2);
      assert!(resolve_family(&ecp).is_ok());
      ```

      The test helper `build_plan` may need an `ecp_shells` version — extend the helper minimally so the new assertion compiles. If `build_plan` is shared across other tests and editing it is too invasive, add a sibling `build_plan_with_ecp` helper local to the test module.

    Step 3 — Add `#[cfg(test)]` module-internal unit tests at the bottom of `kernels/ecp.rs`:
      - Test (`launch_ecp_rejects_wrong_canonical_family`): construct an ExecutionPlan with canonical_family "1e" and call launch_ecp; expect Err(ChunkPlanFailed{ from: "cubecl_ecp", ..}).
      - Test (`launch_ecp_rejects_empty_ecp_shells`): construct an ExecutionPlan with canonical_family "ecp" but basis.ecp_shells() empty; expect Err(ChunkPlanFailed{ from: "cubecl_ecp", ..}). This is a defense-in-depth check below the query_workspace preflight from Plan 03.
      - Test (`launch_ecp_rejects_gradient_operator_name_for_now`): canonical_family "ecp" + operator_name "ecp_ipnuc"; expect Err(UnsupportedApi). Plan 05 will replace this expectation with a success path.
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/cintx &amp;&amp; bash -o pipefail -c "cargo nextest run --locked -p cintx-cubecl --lib kernels::ecp 2>&amp;1 | tail -20" &amp;&amp; bash -o pipefail -c "cargo build --locked -p cintx-cubecl 2>&amp;1 | tail -3"</automated>
    <automated>cd /home/user/Documents/workspace/cintx &amp;&amp; grep -cE '^// Source: vendor/pyscf-nr-ecp/src/nr_ecp\.c:[0-9]+-[0-9]+' crates/cintx-cubecl/src/kernels/ecp.rs | awk '$1 >= 2 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --locked -p cintx-cubecl` exits 0 (`bash -o pipefail` propagates cargo's exit through the `tail` pipe)
    - `cargo nextest run --locked -p cintx-cubecl --lib kernels::ecp` exits 0 with at least 3 tests passing
    - **Upstream-citation requirement:** `grep -cE '^// Source: vendor/pyscf-nr-ecp/src/nr_ecp\.c:[0-9]+-[0-9]+' crates/cintx-cubecl/src/kernels/ecp.rs` returns ≥ 2 (one citation per ECP type — `ECPtype1_cart` and `ECPtype2_cart` — each pointing at a concrete `LXX-LYY` line range)
    - **Normalization-convention documentation:** `grep -F 'Normalization & coordinate convention' crates/cintx-cubecl/src/kernels/ecp.rs` matches (rustdoc subsection present); `grep -E 'PA|PB' crates/cintx-cubecl/src/kernels/ecp.rs` matches in a rustdoc context (PA/PB displacement convention documented)
    - `grep -F 'pub mod ecp;' crates/cintx-cubecl/src/kernels/mod.rs` matches
    - `grep -F '"ecp" => Some(ecp::launch_ecp' crates/cintx-cubecl/src/kernels/mod.rs` matches
    - `grep -F '| "ecp" => true' crates/cintx-cubecl/src/kernels/mod.rs` matches (extended unconditional arm)
    - `grep -F 'pub fn launch_ecp' crates/cintx-cubecl/src/kernels/ecp.rs` matches
    - `grep -F 'EcpChannel::Local' crates/cintx-cubecl/src/kernels/ecp.rs` matches
    - `grep -F 'EcpChannel::Projected' crates/cintx-cubecl/src/kernels/ecp.rs` matches
    - `grep -F 'modified_spherical_bessel_in_host' crates/cintx-cubecl/src/kernels/ecp.rs` matches
    - `grep -F 'gauss_chebyshev_nodes_weights_host' crates/cintx-cubecl/src/kernels/ecp.rs` matches
    - `grep -F 'gauss_hermite_nodes_weights_host' crates/cintx-cubecl/src/kernels/ecp.rs` matches
    - `grep -F 'cart_to_sph_1e' crates/cintx-cubecl/src/kernels/ecp.rs` matches
    - `grep -F 'cubecl_ecp' crates/cintx-cubecl/src/kernels/ecp.rs` matches (error-source identifier)
    - `wc -l crates/cintx-cubecl/src/kernels/ecp.rs` returns ≥ 250
  </acceptance_criteria>
  <done>launch_ecp dispatches Type-1 and Type-2 ECP shell contributions through paired host-side helpers calling math::bessel and math::radial_quadrature; the file carries at least two `// Source: vendor/pyscf-nr-ecp/src/nr_ecp.c:LXX-LYY` upstream-line citations and documents the normalization + PA/PB conventions ported from PySCF; canonical_family "ecp" is registered in kernels/mod.rs unconditionally; gradient operator name returns UnsupportedApi pending Plan 05; in-module tests cover the canonical-family guard, the empty-ecp_shells guard, and the gradient-deferred guard.</done>
</task>

<task type="auto">
  <name>Task 2: Add PySCF nr_ecp scalar vendor FFI wrappers + bindgen allowlist entries</name>
  <files>
    crates/cintx-oracle/src/vendor_ffi.rs,
    crates/cintx-oracle/build.rs
  </files>
  <read_first>
    - /home/user/Documents/workspace/cintx/crates/cintx-oracle/src/vendor_ffi.rs (lines 1-95 — vendor_int1e_ovlp_sph and sibling wrappers; exact extern "C" signature shape)
    - /home/user/Documents/workspace/cintx/crates/cintx-oracle/build.rs (locate the bindgen .allowlist_function regex — per 19-PATTERNS.md it lives around line 287)
    - /home/user/Documents/workspace/cintx/vendor/pyscf-nr-ecp/include/nr_ecp.h (the actual exported C symbols and their full signatures — the wrapper signature MUST match these exactly)
    - /home/user/Documents/workspace/cintx/vendor/pyscf-nr-ecp/src/nr_ecp.c (top of file for the public-symbol declarations; cross-check with nr_ecp.h)
    - /home/user/Documents/workspace/cintx/.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-PATTERNS.md (section on cintx-oracle/src/vendor_ffi.rs)
  </read_first>
  <action>
    Step 1 — Inspect `vendor/pyscf-nr-ecp/include/nr_ecp.h` to confirm the exact exported scalar-ECP symbol names. Per 19-RESEARCH.md §"Public symbols exposed" the likely names are `ECPscalar_sph` and `ECPscalar_cart` (PySCF's combined Type-1 + Type-2 wrappers). If the actual header exports `ECPtype1_cart`/`ECPtype2_cart` separately, write either:
      (a) Two pairs of wrappers (one each for Type-1 and Type-2), and let the test sum their contributions; OR
      (b) A single combined wrapper that calls both upstream symbols and accumulates — but this duplicates the algorithm in C and is less honest.
    Choose (a) if the combined `ECPscalar_*` wrapper does not exist in the vendored header; document the choice in the task SUMMARY.

    Step 2 — Add `crates/cintx-oracle/src/vendor_ffi.rs` wrappers immediately after the existing `vendor_int1e_*_sph` block. Use module gating `#[cfg(all(has_vendor_libcint, has_vendor_pyscf_nr_ecp))]` on the new function block (these cfg flags are emitted by build.rs — `has_vendor_libcint` already exists; `has_vendor_pyscf_nr_ecp` was added by Plan 01).

    Wrapper signature pattern (mirrors `vendor_int1e_ovlp_sph` but adds `ecpbas: &[i32], necpbas: i32`):

    ```
    pub fn vendor_ECPscalar_sph(
        out: &mut [f64],
        shls: &[i32; 2],
        atm: &[i32], natm: i32,
        bas: &[i32], nbas: i32,
        ecpbas: &[i32], necpbas: i32,
        env: &[f64],
    ) -> i32 {
        unsafe {
            ffi::ECPscalar_sph(
                out.as_mut_ptr(),
                shls.as_ptr() as *mut i32,
                ecpbas.as_ptr() as *mut i32,
                necpbas,
                atm.as_ptr() as *mut i32, natm,
                bas.as_ptr() as *mut i32, nbas,
                env.as_ptr() as *mut f64,
                std::ptr::null_mut(),  // ECPOpt *opt = NULL
                std::ptr::null_mut(),  // double *cache = NULL
            )
        }
    }
    ```

    Add a sibling `vendor_ECPscalar_cart` with the same signature pointing at `ffi::ECPscalar_cart`. If the header names differ, use the actual names verbatim (planner-illustrative names above).

    Step 3 — Extend `crates/cintx-oracle/build.rs` bindgen allowlist. Locate the existing `.allowlist_function(...)` regex in build.rs (around line 287 per 19-PATTERNS.md). Add the four ECP symbol names to the allowlist: `ECPscalar_sph`, `ECPscalar_cart`, `ECPscalar_ipnuc_sph`, `ECPscalar_ipnuc_cart` (all four at once, even though only two are wired in Plan 04 — Plan 05 wires the ipnuc pair; landing all four allowlist entries now avoids reopening build.rs in Plan 05).

    The allowlist edit is small — typically appending an alternation: `|ECPscalar.*`. Use the actual regex form present in build.rs; document the exact edit in the task SUMMARY.

    Add a `#[cfg(all(test, has_vendor_libcint, has_vendor_pyscf_nr_ecp))]` smoke test in `vendor_ffi.rs` (or a sibling test file) that constructs a minimal H-atom basis + a 1-row ecpbas, calls `vendor_ECPscalar_sph` and `vendor_ECPscalar_cart`, and asserts the return code is 0 (success — non-zero indicates a C-side failure). This catches FFI ABI mismatches before Plan 04 Task 3's parity tests run.
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/cintx &amp;&amp; bash -o pipefail -c "CINTX_ORACLE_BUILD_VENDOR=1 cargo build --locked -p cintx-oracle 2>&amp;1 | tail -10" &amp;&amp; grep -F 'vendor_ECPscalar_sph' crates/cintx-oracle/src/vendor_ffi.rs &amp;&amp; grep -F 'vendor_ECPscalar_cart' crates/cintx-oracle/src/vendor_ffi.rs &amp;&amp; grep -F 'ECPscalar' crates/cintx-oracle/build.rs</automated>
  </verify>
  <acceptance_criteria>
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo build --locked -p cintx-oracle` exits 0 (`bash -o pipefail` propagates cargo's exit through the `tail` pipe)
    - `grep -F 'pub fn vendor_ECPscalar_sph' crates/cintx-oracle/src/vendor_ffi.rs` matches
    - `grep -F 'pub fn vendor_ECPscalar_cart' crates/cintx-oracle/src/vendor_ffi.rs` matches
    - `grep -F 'has_vendor_pyscf_nr_ecp' crates/cintx-oracle/src/vendor_ffi.rs` matches the module gate
    - `grep -F 'ECPscalar' crates/cintx-oracle/build.rs` matches the bindgen allowlist edit
    - `grep -F 'ECPscalar_ipnuc' crates/cintx-oracle/build.rs` matches (the ipnuc allowlist entries land in Plan 04 too, even if wired in Plan 05)
    - The FFI smoke test exits 0 under `CINTX_ORACLE_BUILD_VENDOR=1 cargo nextest run --locked -p cintx-oracle vendor_ffi`
  </acceptance_criteria>
  <done>vendor_ECPscalar_sph and vendor_ECPscalar_cart wrap PySCF nr_ecp's scalar entry points with the (ecpbas, necpbas) extended signature; bindgen allowlist covers all four ECP symbols including the ipnuc pair Plan 05 wires; smoke test confirms the FFI ABI is sound.</done>
</task>

<task type="auto">
  <name>Task 3: Add safe_api_ecp_parity.rs with two byte-identity tests at atol=1e-12 vs PySCF nr_ecp on the full Cu/LANL2DZ Cartesian product, then flip oracle_covered=true on the two scalar manifest rows</name>
  <files>
    crates/cintx-oracle/tests/safe_api_ecp_parity.rs,
    crates/cintx-ops/src/generated/api_manifest.csv,
    crates/cintx-ops/src/generated/api_manifest.rs,
    crates/cintx-ops/src/generated/compiled_manifest.lock.json
  </files>
  <read_first>
    - /home/user/Documents/workspace/cintx/crates/cintx-oracle/tests/safe_api_arity2_parity.rs (Phase 17 output — direct file shape analog; tolerance literals at file top; per-symbol named tests at lines 591-635)
    - /home/user/Documents/workspace/cintx/crates/cintx-oracle/src/fixtures.rs (Plan 01's build_cu_lanl2dz function — the (atm, bas, ecpbas, env) tuple)
    - /home/user/Documents/workspace/cintx/crates/cintx-oracle/data/cu_lanl2dz.json (Plan 01 Task 2 output — the JSON source for the fixture; coverage invariant: ≥8 AO shells, ≥3 ECP projectors — re-verify via `jq` before relying on the "full Cartesian product" claim)
    - /home/user/Documents/workspace/cintx/crates/cintx-oracle/src/vendor_ffi.rs (Task 2 output — vendor_ECPscalar_sph and vendor_ECPscalar_cart signatures)
    - /home/user/Documents/workspace/cintx/.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-PATTERNS.md (section on safe_api_ecp_parity.rs)
    - /home/user/Documents/workspace/cintx/.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-CONTEXT.md (tolerance: atol=1e-12, rtol=0.0; Cu/LANL2DZ full Cartesian product per Claude's Discretion)
  </read_first>
  <action>
    Step 0 — Re-verify the Plan 01 Task 2 coverage invariant before the parity test makes the "full Cartesian product" truth claim:

    ```bash
    jq '(.shells | length) >= 8 and ((.ecp.shells | length) >= 3)' crates/cintx-oracle/data/cu_lanl2dz.json
    ```

    MUST print `true`. If it prints `false`, STOP — the fixture is undersized vs the published LANL2DZ definition, and the "full Cartesian product" claim in the truth list (≥8 AO shells × ≥3 ECP projectors) is not grounded. Re-run Plan 01 Task 2 to extend the JSON before proceeding.

    Step 1 — Create `crates/cintx-oracle/tests/safe_api_ecp_parity.rs` per 19-PATTERNS.md section on this file. Use the safe_api_arity2_parity.rs file shape verbatim:

    File-top:
    ```
    //! Safe-API parity tests for the two scalar arity-2 ECP operators (int1e_ecp_{cart,sph}).
    //!
    //! Asserts byte-identity vs vendored PySCF nr_ecp at the Phase 15 unified
    //! tolerance (atol=1e-12, rtol=0.0). Cu/LANL2DZ full Cartesian product per
    //! 19-CONTEXT.md Claude's Discretion; coverage invariant (Plan 01 Task 2):
    //! the fixture JSON carries ≥8 AO shell entries and ≥3 ECP projector entries.

    #![cfg(any(feature = "cpu", feature = "rocm"))]

    const ATOL: f64 = 1e-12;
    const RTOL: f64 = 0.0;

    use cintx_compat::raw::{
        ATM_SLOTS, ANG_OF, ATOM_OF, BAS_SLOTS, CHARGE_OF, NCTR_OF, NPRIM_OF,
        NUC_MOD_OF, POINT_NUC, PTR_COEFF, PTR_COORD, PTR_ENV_START, PTR_EXP,
        AS_ECPBAS_OFFSET, AS_NECPBAS, RADI_POWER, SO_TYPE_OF, ECP_LMAX,
    };
    use cintx_core::{Atom, BasisSet, NuclearModel, OperatorId, Representation, Shell, ShellTuple, ecp::{EcpShell, EcpChannel}};
    use cintx_oracle::fixtures::build_cu_lanl2dz;
    use cintx_rs::SessionRequest;
    use cintx_runtime::ExecutionOptions;
    use std::sync::Arc;
    ```

    Step 2 — Implement two helpers (mirroring safe_api_arity2_parity.rs `build_h2o_sto3g_safe_basis` and `collect_safe_api_matrix`):

      (a) `fn build_cu_lanl2dz_safe_basis(rep: Representation) -> (BasisSet, Vec<ShellTuple>)` — translates the JSON-derived AO shells AND ECP shells into typed Shell + EcpShell objects, builds `BasisSet::try_new_with_ecp(...)`, and returns shell tuples covering the FULL Cartesian product (all Cu AO shells × all Cu AO shells, grounded on the ≥8 AO shell coverage invariant). The function MUST be deterministic — same inputs across calls.

      (b) `fn collect_safe_api_ecp_matrix(op: OperatorId, rep: Representation, basis: &BasisSet, shells: &[ShellTuple]) -> Vec<f64>` — mirrors `collect_safe_api_matrix` from safe_api_arity2_parity.rs (lines 236-292) but routes through `SessionRequest::new(op, basis, &tup, ExecutionOptions::default())` for each shell pair and concatenates the flat staging output. Returns the same flat-buffer shape PySCF's vendor wrapper writes into.

      (c) `fn collect_ecp_matrix_vendor(rep: &str, atm: &[i32], bas: &[i32], ecpbas: &[i32], env: &[f64]) -> Vec<f64>` — iterates the same shell-pair sweep and calls `vendor_ECPscalar_sph` (or `vendor_ECPscalar_cart` per rep) for each pair, concatenating results.

    Step 3 — Two named #[test] functions per 19-PATTERNS.md Pattern F (per-symbol named tests, NOT a parametric loop, for per-symbol CI failure visibility):

    ```
    #[test]
    #[cfg(all(has_vendor_libcint, has_vendor_pyscf_nr_ecp))]
    fn test_int1e_ecp_cart_safe_api_parity() {
        let (atm, bas, ecpbas, env) = build_cu_lanl2dz();
        let (basis, shells) = build_cu_lanl2dz_safe_basis(Representation::Cart);

        let safe_matrix = collect_safe_api_ecp_matrix(
            OperatorId::INT1E_ECP_CART, Representation::Cart, &basis, &shells,
        );
        let vendor_matrix = collect_ecp_matrix_vendor("cart", &atm, &bas, &ecpbas, &env);

        let mismatches = count_mismatches(&vendor_matrix, &safe_matrix, ATOL, RTOL);
        assert_eq!(
            mismatches, 0,
            "int1e_ecp_cart safe API: {mismatches} elements exceed atol={ATOL:.0e}/rtol={RTOL:.0e} vs vendored PySCF nr_ecp"
        );
    }

    #[test]
    #[cfg(all(has_vendor_libcint, has_vendor_pyscf_nr_ecp))]
    fn test_int1e_ecp_sph_safe_api_parity() {
        // Same shape, OperatorId::INT1E_ECP_SPH + Representation::Sph + vendor_ECPscalar_sph
    }
    ```

    Reuse the `count_mismatches` helper from safe_api_arity2_parity.rs lines 300-321 — either re-import it or copy the implementation verbatim with a `// Source: safe_api_arity2_parity.rs:300-321 — kept local to avoid public-API churn` comment.

    Step 4 — After both tests pass at atol=1e-12, flip `oracle_covered = false` to `oracle_covered = true` on the two scalar rows in `crates/cintx-ops/src/generated/api_manifest.csv`:

    - `int1e_ecp_cart` row: change `,false,"operator","ecp"` to `,true,"operator","ecp"`.
    - `int1e_ecp_sph` row: same change.

    Leave the two ipnuc rows at `oracle_covered = false` — Plan 05 will flip those after the gradient parity lands.

    Regenerate the lock and helper code:
    ```
    cargo run -p xtask --locked -- manifest-audit --update
    ```

    Step 5 — Document any Cu basis simplifications you needed to land the parity tests (e.g., if Plan 01's `build_cu_lanl2dz` JSON contained only a subset of shells and the test exercises that subset, note it in the test rustdoc and the task SUMMARY). The truth claim is "Cu/LANL2DZ Cartesian product at atol=1e-12, ≥8 AO shells × ≥3 ECP projectors" — if you exercise a subset, the SUMMARY must explicitly say so AND the deferred-shell coverage must be re-validated in a follow-up.
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/cintx &amp;&amp; jq '(.shells | length) >= 8 and ((.ecp.shells | length) >= 3)' crates/cintx-oracle/data/cu_lanl2dz.json | grep -Fxq true &amp;&amp; bash -o pipefail -c "CINTX_ORACLE_BUILD_VENDOR=1 cargo nextest run --locked -p cintx-oracle --test safe_api_ecp_parity 2>&amp;1 | tail -25" &amp;&amp; bash -o pipefail -c "cargo run -p xtask --locked -- manifest-audit 2>&amp;1 | tail -5" &amp;&amp; grep -E '"int1e_ecp_cart".*,true,"operator","ecp"' crates/cintx-ops/src/generated/api_manifest.csv &amp;&amp; grep -E '"int1e_ecp_sph".*,true,"operator","ecp"' crates/cintx-ops/src/generated/api_manifest.csv</automated>
  </verify>
  <acceptance_criteria>
    - **Coverage invariant precondition:** `jq '(.shells | length) >= 8 and ((.ecp.shells | length) >= 3)' crates/cintx-oracle/data/cu_lanl2dz.json` returns `true`
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo nextest run --locked -p cintx-oracle --test safe_api_ecp_parity` exits 0 with exactly 2 tests passing under `#[cfg(all(has_vendor_libcint, has_vendor_pyscf_nr_ecp))]` (`bash -o pipefail` propagates the nextest exit through the `tail` pipe so a parity failure is not masked by the successful `tail`)
    - `grep -F 'test_int1e_ecp_cart_safe_api_parity' crates/cintx-oracle/tests/safe_api_ecp_parity.rs` matches
    - `grep -F 'test_int1e_ecp_sph_safe_api_parity' crates/cintx-oracle/tests/safe_api_ecp_parity.rs` matches
    - `grep -F 'const ATOL: f64 = 1e-12;' crates/cintx-oracle/tests/safe_api_ecp_parity.rs` matches
    - `grep -F 'const RTOL: f64 = 0.0;' crates/cintx-oracle/tests/safe_api_ecp_parity.rs` matches
    - `grep -F 'vendor_ECPscalar_sph' crates/cintx-oracle/tests/safe_api_ecp_parity.rs` matches
    - `grep -F 'vendor_ECPscalar_cart' crates/cintx-oracle/tests/safe_api_ecp_parity.rs` matches
    - `grep -E '"int1e_ecp_cart".*,true,' crates/cintx-ops/src/generated/api_manifest.csv` matches (oracle_covered flipped)
    - `grep -E '"int1e_ecp_sph".*,true,' crates/cintx-ops/src/generated/api_manifest.csv` matches (oracle_covered flipped)
    - `grep -E '"int1e_ecp_ipnuc_cart".*,false,' crates/cintx-ops/src/generated/api_manifest.csv` still matches (Plan 05 flips this)
    - `wc -l crates/cintx-oracle/tests/safe_api_ecp_parity.rs` returns ≥ 200
    - `cargo run -p xtask --locked -- manifest-audit` exits 0 (regenerated lock matches CSV with no diff; `bash -o pipefail` propagates xtask's exit through the `tail` pipe)
  </acceptance_criteria>
  <done>Two named per-symbol parity tests pass at atol=1e-12 against the vendored PySCF nr_ecp byte-identity reference on the Cu/LANL2DZ Cartesian product (grounded on the ≥8 AO shell + ≥3 ECP projector invariant); manifest oracle_covered flag flipped to true on the two scalar rows (ipnuc rows stay false pending Plan 05); lock regenerated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| safe API → kernel | launch_ecp validates canonical_family + shell counts + non-empty ecp_shells before any algorithm work |
| kernel → C oracle FFI | vendor_ECPscalar_* wrappers pass typed slabs; bindgen allowlist limits visible C symbols |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-10 | Tampering (numerical) | `launch_ecp` algorithm correctness | mitigate | Byte-identity gate vs vendored PySCF nr_ecp at atol=1e-12, rtol=0.0 over the full Cu/LANL2DZ Cartesian product (≥8 AO shells × ≥3 ECP projectors). Any algorithmic regression fails CI before merge. Upstream-line citations (`// Source:` rustdoc) make the port auditable against the vendored C source. |
| T-19-11 | Integrity (UB via FFI) | `vendor_ECPscalar_*` wrappers | mitigate | Typed slabs (&[i32], &[f64]) with explicit length args (natm, nbas, necpbas); ECPOpt and cache passed NULL. Smoke test in Task 2 catches ABI mismatches. |
| T-19-12 | DoS (panic on empty ecp_shells) | launch_ecp empty-shells guard | mitigate | Defense-in-depth: query_workspace preflight catches it first; launch_ecp re-checks with a typed ChunkPlanFailed in case the preflight is bypassed (e.g., compat-raw path). |
</threat_model>

<verification>
- `CINTX_ORACLE_BUILD_VENDOR=1 cargo nextest run --locked -p cintx-oracle --test safe_api_ecp_parity` exits 0
- `cargo nextest run --locked -p cintx-cubecl --lib kernels::ecp` exits 0
- `cargo build --locked --workspace` exits 0
- Two scalar ECP rows have `oracle_covered=true` in CSV and the lock matches
- `safe_api_ecp_parity.rs` contains both #[test] functions, both #[cfg]-gated on the vendor cfgs
- `kernels/ecp.rs` carries at least two `// Source: vendor/pyscf-nr-ecp/src/nr_ecp.c:LXX-LYY` rustdoc citations with normalization + PA/PB documentation
- `crates/cintx-oracle/data/cu_lanl2dz.json` continues to satisfy `(.shells | length) >= 8 and ((.ecp.shells | length) >= 3)`
</verification>

<success_criteria>
ECP-01, ECP-02, ECP-03, and the scalar half of ECP-04 are closed: Type-1 + Type-2 kernels produce libcint-compatible (= PySCF nr_ecp-compatible) results at atol=1e-12 through both `eval_raw` (via Plan 03's compat dispatch arm) and `SessionRequest::evaluate`. The Cu/LANL2DZ coverage invariant (≥8 AO shells × ≥3 ECP projectors) is reaffirmed. Plan 05 handles the gradient half.
</success_criteria>

<output>
After completion, create `.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-04-SUMMARY.md` capturing: exact PySCF C symbol names that ended up in vendor_ffi.rs (combined ECPscalar_* vs separate ECPtype{1,2}_*), Cu basis subset/full status (full per Plan 01 ≥8/≥3 invariant — confirm), the recorded upstream line ranges from Step 0 (`ECPtype1_cart:LXX-LYY` and `ECPtype2_cart:LXX-LYY`), observed worst-case |diff| in the parity sweep (should be ≪ atol), and any kernel-level corrections that were needed during execution (e.g. PA/PB displacement convention, normalization factors).
</output>
