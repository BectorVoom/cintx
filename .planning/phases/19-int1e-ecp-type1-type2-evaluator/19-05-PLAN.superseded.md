---
phase: 19
plan: 05
type: execute
wave: 3
depends_on: [19-04]
files_modified:
  - crates/cintx-cubecl/src/kernels/ecp.rs
  - crates/cintx-oracle/src/vendor_ffi.rs
  - crates/cintx-oracle/tests/safe_api_ecp_parity.rs
  - crates/cintx-ops/src/generated/api_manifest.csv
  - crates/cintx-ops/src/generated/api_manifest.rs
  - crates/cintx-ops/src/generated/compiled_manifest.lock.json
autonomous: true
requirements: [ECP-01, ECP-02, ECP-04, ECP-05]
canonical_family: ecp
forms: [cart, sph]
component_rank: 3

must_haves:
  truths:
    - "launch_ecp dispatches the ecp_ipnuc operator branch by computing 3-component (∂/∂A_C^{x,y,z}) gradients using PySCF nr_ecp_deriv.c's algorithm for Type-1 + Type-2 derivatives; the UnsupportedApi placeholder Plan 04 left for operator_name == \"ecp_ipnuc\" is replaced with a real implementation"
    - "Output flat-buffer layout for gradients is F-order with the component axis slowest-varying: [axis ∈ {x,y,z}, ao_j, ao_i] — matches the int3c2e_ip1_* precedent established in Phase 10/11"
    - "vendor_ECPscalar_ipnuc_sph and vendor_ECPscalar_ipnuc_cart wrap PySCF nr_ecp_deriv.c's gradient entry points (or whatever names the vendored header actually exports); bindgen allowlist already includes them from Plan 04 Task 2"
    - "safe_api_ecp_parity.rs gains test_int1e_ecp_ipnuc_cart_safe_api_parity and test_int1e_ecp_ipnuc_sph_safe_api_parity; both pass at atol=1e-12, rtol=0.0 across the Cu/LANL2DZ Cartesian product × 3 derivative components"
    - "api_manifest.csv flips oracle_covered=true on the two ipnuc rows; compiled_manifest.lock.json regenerated; manifest-audit reports zero diff"
  artifacts:
    - path: "crates/cintx-cubecl/src/kernels/ecp.rs"
      provides: "Gradient branch (operator_name == \"ecp_ipnuc\") for both Type-1 and Type-2 channels"
    - path: "crates/cintx-oracle/src/vendor_ffi.rs"
      provides: "vendor_ECPscalar_ipnuc_sph + vendor_ECPscalar_ipnuc_cart wrappers"
    - path: "crates/cintx-oracle/tests/safe_api_ecp_parity.rs"
      provides: "Two additional named #[test] functions for ipnuc cart + sph at atol=1e-12"
  key_links:
    - from: "crates/cintx-cubecl/src/kernels/ecp.rs gradient branch"
      to: "vendor/pyscf-nr-ecp/src/nr_ecp_deriv.c"
      via: "Algorithm reference for ∂/∂A_C^μ ECPtype1_* and ECPtype2_*"
      pattern: "nr_ecp_deriv|ECPscalar_ipnuc"
    - from: "test_int1e_ecp_ipnuc_{cart,sph}_safe_api_parity"
      to: "vendor_ECPscalar_ipnuc_{cart,sph}"
      via: "byte-identity gradient parity at atol=1e-12 over Cu/LANL2DZ"
      pattern: "vendor_ECPscalar_ipnuc"
---

<objective>
Close the gradient half of Phase 19. Replace Plan 04's `UnsupportedApi` stub on the `operator_name == "ecp_ipnuc"` arm with a real implementation that ports PySCF `nr_ecp_deriv.c`'s Type-1 and Type-2 gradient algorithms, writes the 3-component result in F-order `[axis, ao_j, ao_i]` (axis slowest-varying per D-11), and proves byte-identity vs the vendored reference at atol=1e-12 across Cu/LANL2DZ.

Purpose: Deliver the symbols `int1e_ecp_ipnuc_{cart,sph}` declared in the ROADMAP, satisfy ECP-05 (gradient scope decision locked in CONTEXT D-10 = "in scope"), and complete ECP-04 (byte-identity parity across all four scalar+gradient symbols).
Output: Filled gradient branch in launch_ecp, two new vendor FFI wrappers, two new parity tests, manifest oracle_covered flag flipped on the two ipnuc rows.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-CONTEXT.md
@.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-RESEARCH.md
@.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-PATTERNS.md
@CLAUDE.md
@crates/cintx-cubecl/src/kernels/ecp.rs
@crates/cintx-cubecl/src/kernels/f12.rs
@crates/cintx-oracle/src/vendor_ffi.rs
@crates/cintx-oracle/tests/safe_api_ecp_parity.rs
@vendor/pyscf-nr-ecp/include/nr_ecp.h
@vendor/pyscf-nr-ecp/src/nr_ecp_deriv.c

<interfaces>
Plan 04's launch_ecp signature (cintx-cubecl/src/kernels/ecp.rs::launch_ecp):

```
pub fn launch_ecp(
    backend: &ResolvedBackend,
    plan: &ExecutionPlan<'_>,
    specialization: &SpecializationKey,
    staging: &mut [f64],
) -> Result<ExecutionStats, cintxRsError>
```

The function currently dispatches the scalar arm (operator_name == "ecp") via compute_type1_pair / compute_type2_pair. The gradient arm (operator_name == "ecp_ipnuc") currently returns Err(UnsupportedApi); this plan replaces it.

Component-rank-3 F-order layout precedent: cintx's existing int3c2e_ip1_{cart,sph} writes [axis, ao_..., ao_i] with axis slowest-varying. Phase 11 / Phase 13 confirm this convention for 1e-shaped gradient operators.

Vendor wrapper signature analog (Plan 04 Task 2 output):

```
pub fn vendor_ECPscalar_sph(out, shls, atm, natm, bas, nbas, ecpbas, necpbas, env) -> i32;
pub fn vendor_ECPscalar_cart(out, shls, atm, natm, bas, nbas, ecpbas, necpbas, env) -> i32;
```

The ipnuc gradient wrappers reuse this signature; the only difference is the output buffer is 3× larger (component_rank=3).

PySCF nr_ecp_deriv.c exports the analog gradient symbols — exact names confirmed by reading the vendored header. Per 19-RESEARCH.md §"Gradient form" these are likely `ECPscalar_ipnuc_{cart,sph}`. Cite the actual names found in the header during execution.

Per 19-CONTEXT.md D-11 the gradient kernel SHARES one launcher with the scalar base kernel and branches internally on operator_name.

CubeCL constraints from Phase 8 P02 (host-side helpers only — same as Plan 04).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement gradient branch in launch_ecp (Type-1 + Type-2 derivatives) and add vendor ipnuc wrappers</name>
  <files>
    crates/cintx-cubecl/src/kernels/ecp.rs,
    crates/cintx-oracle/src/vendor_ffi.rs
  </files>
  <read_first>
    - /home/user/Documents/workspace/cintx/crates/cintx-cubecl/src/kernels/ecp.rs (Plan 04 output — scalar branch is implemented; gradient arm returns UnsupportedApi)
    - /home/user/Documents/workspace/cintx/vendor/pyscf-nr-ecp/src/nr_ecp_deriv.c (the gradient algorithm reference — read the entry points and the per-pair derivative routines)
    - /home/user/Documents/workspace/cintx/vendor/pyscf-nr-ecp/include/nr_ecp.h (confirm the exact C names exported for the ipnuc gradient — illustrative: ECPscalar_ipnuc_sph and ECPscalar_ipnuc_cart)
    - /home/user/Documents/workspace/cintx/crates/cintx-cubecl/src/kernels/f12.rs (lines 1517-1568 — operator-suffix dispatch pattern; reuses for the (scalar) vs (ipnuc gradient) split here)
    - /home/user/Documents/workspace/cintx/crates/cintx-oracle/src/vendor_ffi.rs (Plan 04 Task 2 output — vendor_ECPscalar_{sph,cart} signature is the direct analog)
    - /home/user/Documents/workspace/cintx/.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-RESEARCH.md (§"Gradient form (int1e_ecp_ipnuc_*)" — algorithm shape: ∂/∂A_C^μ adds (r - A_C) factor, shifts angular momentum ±1, shifts radial power ±1)
    - /home/user/Documents/workspace/cintx/.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-CONTEXT.md (D-10 gradients in scope; D-11 gradient + base share one launcher; component_rank=3 axis slowest-varying)
    - /home/user/Documents/workspace/cintx/.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-PATTERNS.md (Phase 19 deviations on kernels/ecp.rs gradient component layout)
  </read_first>
  <action>
    Step 1 — Read `vendor/pyscf-nr-ecp/src/nr_ecp_deriv.c` to confirm:
      (a) the exact upstream C symbol names for the scalar gradient (likely `ECPscalar_ipnuc_cart` and `ECPscalar_ipnuc_sph`),
      (b) the per-pair derivative algorithm — typically a variant of the base ECPtype1/ECPtype2 inner loop that includes a `(r - A_C)^μ` factor in the integrand, which shifts angular momentum ±1 and radial power ±1,
      (c) the output buffer convention — confirm component layout (PySCF typically writes [3, nao_j, nao_i] in row-major C order; cintx wants F-order [axis, ao_j, ao_i] with axis slowest-varying per D-11 — same memory layout, naming convention).

    If the exact names differ from the illustrative `ECPscalar_ipnuc_*`, use whatever the vendored header actually exports. Document the resolved names in the task SUMMARY.

    Step 2 — Replace the `operator_name == "ecp_ipnuc"` UnsupportedApi arm in `launch_ecp` (Plan 04 stub) with a real implementation. Concrete edit:

    Replace:
    ```
    } else if operator_name == "ecp_ipnuc" {
        return Err(cintxRsError::UnsupportedApi {
            requested: format!("ecp gradient (operator_name={operator_name}) — Plan 05 lands"),
        });
    }
    ```

    With a parallel branch to the scalar arm that:
      1. Asserts the staging output buffer length is `3 * ncart(li) * ncart(lj)` (Cartesian path; sph buffer is `3 * nsph(li) * nsph(lj)` after transform).
      2. Iterates the same shell-pair loop as the scalar arm.
      3. For each (i, j) pair × each ECP shell c, branches on `c.channel`:
         - `EcpChannel::Local` ⇒ Type-1 gradient: call new helper `compute_type1_pair_grad(i, j, c, &mut grad_cart_buf, &gh_nodes, &gh_weights)`. Algorithm: same Gaussian-product geometry as the scalar arm, but each radial integrand picks up a `(r - A_C)^μ` factor for μ ∈ {x, y, z}; this shifts angular momentum ±1 and radial power ±1. Port the corresponding inner loop from nr_ecp_deriv.c — cite the C line range in a rustdoc comment.
         - `EcpChannel::Projected(l)` ⇒ Type-2 gradient: call new helper `compute_type2_pair_grad(i, j, c, l, &mut grad_cart_buf, &cheb_nodes, &cheb_weights, &bessel_in_table)`. Algorithm: same Bessel-modulated radial integral as the scalar arm, but the angular projector channel shifts ±1 due to the derivative coupling rule for `Y_{lm}`. Port the inner loop from nr_ecp_deriv.c with a cited line range.
      4. For sph output, apply `crate::transform::c2s::cart_to_sph_1e` **per component**: loop axis ∈ 0..3 and transform the (`ncart(li)` × `ncart(lj)`) slice of grad_cart_buf into the corresponding (`nsph(li)` × `nsph(lj)`) slice of grad_sph_buf. Component axis remains slowest-varying. This matches the per-axis transform shape Phase 10/11 used for `int3c2e_ip1_*`.

    Document at the gradient arm:
      - `// Source: vendor/pyscf-nr-ecp/src/nr_ecp_deriv.c::ECPscalar_ipnuc_cart (Type-1 + Type-2 gradient)` with line range.
      - `// F-order component layout [axis ∈ {x,y,z}, ao_j, ao_i] (axis slowest) — Phase 10/11 int3c2e_ip1_* precedent (CONTEXT D-11).`

    Add inline #[cfg(test)] tests covering:
      - `launch_ecp_gradient_buffer_size`: assert the gradient arm rejects a staging slice whose length is less than `3 * ncart(li) * ncart(lj)` with a typed Layout-class error (or returns ChunkPlanFailed with a clear "buffer too small for component_rank=3" detail).
      - `launch_ecp_gradient_zero_for_no_overlap`: construct a shell pair on a far-separated atom from the ECP center; assert the gradient slice is element-wise within machine epsilon of zero (numerical sanity).

    Step 3 — Replace the Plan 04 stub test `launch_ecp_rejects_gradient_operator_name_for_now` with a positive test: assert that operator_name "ecp_ipnuc" on a valid plan with ecp_shells attached returns Ok(_).

    Step 4 — Extend `crates/cintx-oracle/src/vendor_ffi.rs` with two ipnuc wrappers. Mirror the scalar pattern from Plan 04 Task 2:

    ```
    pub fn vendor_ECPscalar_ipnuc_sph(
        out: &mut [f64],
        shls: &[i32; 2],
        atm: &[i32], natm: i32,
        bas: &[i32], nbas: i32,
        ecpbas: &[i32], necpbas: i32,
        env: &[f64],
    ) -> i32 {
        unsafe {
            ffi::ECPscalar_ipnuc_sph(
                out.as_mut_ptr(),
                shls.as_ptr() as *mut i32,
                ecpbas.as_ptr() as *mut i32,
                necpbas,
                atm.as_ptr() as *mut i32, natm,
                bas.as_ptr() as *mut i32, nbas,
                env.as_ptr() as *mut f64,
                std::ptr::null_mut(),
                std::ptr::null_mut(),
            )
        }
    }
    ```

    And a sibling `vendor_ECPscalar_ipnuc_cart` pointing at `ffi::ECPscalar_ipnuc_cart`. The bindgen allowlist already includes the ipnuc names from Plan 04 Task 2 — no build.rs edit needed.

    Verify the out buffer length the caller passes is `3 * nao_i * nao_j` per PySCF's convention; document in rustdoc.
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/cintx &amp;&amp; bash -o pipefail -c "cargo nextest run --locked -p cintx-cubecl --lib kernels::ecp 2>&amp;1 | tail -20" &amp;&amp; bash -o pipefail -c "CINTX_ORACLE_BUILD_VENDOR=1 cargo build --locked -p cintx-oracle 2>&amp;1 | tail -5" &amp;&amp; grep -F 'pub fn vendor_ECPscalar_ipnuc_sph' crates/cintx-oracle/src/vendor_ffi.rs &amp;&amp; grep -F 'pub fn vendor_ECPscalar_ipnuc_cart' crates/cintx-oracle/src/vendor_ffi.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo nextest run --locked -p cintx-cubecl --lib kernels::ecp` exits 0 (`bash -o pipefail` propagates nextest's exit through the `tail` pipe); the previous "rejects gradient operator name" test is updated to a positive assertion
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo build --locked -p cintx-oracle` exits 0 with the new wrappers (`bash -o pipefail` propagates cargo's exit through the `tail` pipe)
    - `grep -F 'compute_type1_pair_grad' crates/cintx-cubecl/src/kernels/ecp.rs` matches
    - `grep -F 'compute_type2_pair_grad' crates/cintx-cubecl/src/kernels/ecp.rs` matches
    - `grep -F '"ecp_ipnuc"' crates/cintx-cubecl/src/kernels/ecp.rs` matches the dispatch arm
    - `grep -F 'UnsupportedApi' crates/cintx-cubecl/src/kernels/ecp.rs` no longer matches the gradient arm (the stub UnsupportedApi return is gone — defensive errors against bad inputs may still emit other typed errors, but the gradient is implemented)
    - `grep -F 'nr_ecp_deriv' crates/cintx-cubecl/src/kernels/ecp.rs` matches the cited algorithm source comment
    - `grep -F 'cart_to_sph_1e' crates/cintx-cubecl/src/kernels/ecp.rs` returns ≥ 2 matches (one for scalar — Plan 04 — and one for the per-axis gradient transform in Plan 05)
    - `grep -F 'pub fn vendor_ECPscalar_ipnuc_sph' crates/cintx-oracle/src/vendor_ffi.rs` matches
    - `grep -F 'pub fn vendor_ECPscalar_ipnuc_cart' crates/cintx-oracle/src/vendor_ffi.rs` matches
  </acceptance_criteria>
  <done>Gradient branch lands inside the same `launch_ecp` launcher, computes 3-component ∂/∂A_C^μ contributions for Type-1 and Type-2 channels, writes F-order [axis, ao_j, ao_i], reuses cart_to_sph_1e per axis for spherical output; two new vendor FFI wrappers expose the PySCF nr_ecp_deriv reference symbols; in-module tests assert the buffer-size invariant and the zero-overlap sanity.</done>
</task>

<task type="auto">
  <name>Task 2: Add ipnuc parity tests at atol=1e-12 and flip oracle_covered=true on the two ipnuc rows</name>
  <files>
    crates/cintx-oracle/tests/safe_api_ecp_parity.rs,
    crates/cintx-ops/src/generated/api_manifest.csv,
    crates/cintx-ops/src/generated/api_manifest.rs,
    crates/cintx-ops/src/generated/compiled_manifest.lock.json
  </files>
  <read_first>
    - /home/user/Documents/workspace/cintx/crates/cintx-oracle/tests/safe_api_ecp_parity.rs (Plan 04 Task 3 output — direct shape analog; reuse build_cu_lanl2dz_safe_basis, collect_safe_api_ecp_matrix, collect_ecp_matrix_vendor, count_mismatches helpers)
    - /home/user/Documents/workspace/cintx/crates/cintx-oracle/src/vendor_ffi.rs (Task 1 output — vendor_ECPscalar_ipnuc_{sph,cart} signatures)
    - /home/user/Documents/workspace/cintx/.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-CONTEXT.md (Tolerance atol=1e-12 rtol=0.0; Cu/LANL2DZ full Cartesian product)
  </read_first>
  <action>
    Step 1 — Append two more named #[test] functions to `crates/cintx-oracle/tests/safe_api_ecp_parity.rs` after the existing scalar tests:

    ```
    #[test]
    #[cfg(all(has_vendor_libcint, has_vendor_pyscf_nr_ecp))]
    fn test_int1e_ecp_ipnuc_cart_safe_api_parity() {
        let (atm, bas, ecpbas, env) = build_cu_lanl2dz();
        let (basis, shells) = build_cu_lanl2dz_safe_basis(Representation::Cart);

        // Each shell pair contributes 3 * ncart(li) * ncart(lj) elements (axis slowest).
        let safe_matrix = collect_safe_api_ecp_matrix(
            OperatorId::INT1E_ECP_IPNUC_CART, Representation::Cart, &basis, &shells,
        );
        let vendor_matrix = collect_ecp_ipnuc_matrix_vendor("cart", &atm, &bas, &ecpbas, &env);

        let mismatches = count_mismatches(&vendor_matrix, &safe_matrix, ATOL, RTOL);
        assert_eq!(
            mismatches, 0,
            "int1e_ecp_ipnuc_cart safe API: {mismatches} elements exceed atol={ATOL:.0e}/rtol={RTOL:.0e} vs vendored PySCF nr_ecp_deriv"
        );
    }

    #[test]
    #[cfg(all(has_vendor_libcint, has_vendor_pyscf_nr_ecp))]
    fn test_int1e_ecp_ipnuc_sph_safe_api_parity() {
        // Same shape with OperatorId::INT1E_ECP_IPNUC_SPH, Representation::Sph, vendor_ECPscalar_ipnuc_sph
    }
    ```

    Step 2 — Add a sibling `collect_ecp_ipnuc_matrix_vendor` helper inside the same test file. It mirrors `collect_ecp_matrix_vendor` from Plan 04 Task 3 but:
      - Allocates an output buffer of size `3 * nao_i * nao_j` per shell pair.
      - Calls `vendor_ECPscalar_ipnuc_{cart,sph}` instead of `vendor_ECPscalar_{cart,sph}`.
      - Returns the flat-buffer concatenation matching the safe-API collector's F-order [axis, ao_j, ao_i] layout. **Important:** if PySCF writes the component axis as fastest-varying internally and cintx writes it slowest-varying, this collector must transpose appropriately so the byte-identity comparison is meaningful. Determine the actual PySCF convention by reading nr_ecp_deriv.c — cite the C output convention in the helper's rustdoc and any transpose step.

    Step 3 — Flip oracle_covered=true on the two ipnuc rows in `crates/cintx-ops/src/generated/api_manifest.csv`:
      - `int1e_ecp_ipnuc_cart` row: change `,false,"operator","ecp"` to `,true,"operator","ecp"`.
      - `int1e_ecp_ipnuc_sph` row: same change.

    Regenerate the lock and helper code:
    ```
    cargo run -p xtask --locked -- manifest-audit --update
    ```

    All four ECP rows now have `oracle_covered=true`.

    Step 4 — Run the FULL parity suite (all 4 tests under safe_api_ecp_parity.rs) to make sure the gradient changes did not regress the scalar parity:

    ```
    CINTX_ORACLE_BUILD_VENDOR=1 cargo nextest run --locked -p cintx-oracle --test safe_api_ecp_parity
    ```

    Must report 4 passed (2 scalar from Plan 04 + 2 gradient from Plan 05). Document worst-case |diff| observed in the SUMMARY.
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/cintx &amp;&amp; bash -o pipefail -c "CINTX_ORACLE_BUILD_VENDOR=1 cargo nextest run --locked -p cintx-oracle --test safe_api_ecp_parity 2>&amp;1 | tail -25" &amp;&amp; bash -o pipefail -c "cargo run -p xtask --locked -- manifest-audit 2>&amp;1 | tail -5" &amp;&amp; grep -E '"int1e_ecp_ipnuc_cart".*,true,"operator","ecp"' crates/cintx-ops/src/generated/api_manifest.csv &amp;&amp; grep -E '"int1e_ecp_ipnuc_sph".*,true,"operator","ecp"' crates/cintx-ops/src/generated/api_manifest.csv</automated>
  </verify>
  <acceptance_criteria>
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo nextest run --locked -p cintx-oracle --test safe_api_ecp_parity` exits 0 with exactly 4 tests passing (2 scalar from Plan 04 + 2 gradient from Plan 05) (`bash -o pipefail` propagates the nextest exit through the `tail` pipe so a parity regression is not masked by a successful `tail`)
    - `grep -F 'test_int1e_ecp_ipnuc_cart_safe_api_parity' crates/cintx-oracle/tests/safe_api_ecp_parity.rs` matches
    - `grep -F 'test_int1e_ecp_ipnuc_sph_safe_api_parity' crates/cintx-oracle/tests/safe_api_ecp_parity.rs` matches
    - `grep -F 'vendor_ECPscalar_ipnuc_sph' crates/cintx-oracle/tests/safe_api_ecp_parity.rs` matches
    - `grep -F 'vendor_ECPscalar_ipnuc_cart' crates/cintx-oracle/tests/safe_api_ecp_parity.rs` matches
    - `grep -F 'collect_ecp_ipnuc_matrix_vendor' crates/cintx-oracle/tests/safe_api_ecp_parity.rs` matches
    - `grep -E '"int1e_ecp_ipnuc_cart".*,true,' crates/cintx-ops/src/generated/api_manifest.csv` matches
    - `grep -E '"int1e_ecp_ipnuc_sph".*,true,' crates/cintx-ops/src/generated/api_manifest.csv` matches
    - `grep -E '"int1e_ecp_.*,false,' crates/cintx-ops/src/generated/api_manifest.csv` returns 0 matches (all four ECP rows now oracle_covered=true)
    - `cargo run -p xtask --locked -- manifest-audit` exits 0 (zero diff; `bash -o pipefail` propagates xtask's exit through the `tail` pipe)
  </acceptance_criteria>
  <done>Two new ipnuc parity tests pass at atol=1e-12 against the vendored PySCF nr_ecp_deriv reference; all four ECP manifest rows have oracle_covered=true; the regenerated lock matches the CSV with zero diff; the full Phase 19 byte-identity gate (4 named tests) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kernel gradient branch → vendored C derivative | New extern "C" surface; bindgen allowlist already in place from Plan 04 Task 2 |
| component-axis F-order layout → downstream consumer | Wrong axis ordering silently produces correct shape but wrong values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-13 | Tampering (silent axis bug) | `launch_ecp` gradient F-order layout | mitigate | Per-component parity test catches a transposed axis; the collect_ecp_ipnuc_matrix_vendor helper must explicitly document and convert the PySCF axis order. If PySCF writes a different convention, the collector transposes — never silently. |
| T-19-14 | Integrity (FFI UB) | `vendor_ECPscalar_ipnuc_*` out buffer size | mitigate | Wrapper rustdoc states `out` must be `3 * nao_i * nao_j` f64s; debug_assert in the wrapper enforces it before the unsafe call. |
| T-19-15 | Numerical regression on scalar parity | Plan 05 edits launch_ecp body | mitigate | Task 2 runs the FULL parity suite (4 tests = scalar from Plan 04 + gradient from Plan 05) to catch any regression in the scalar arm. |
</threat_model>

<verification>
- `CINTX_ORACLE_BUILD_VENDOR=1 cargo nextest run --locked -p cintx-oracle --test safe_api_ecp_parity` exits 0 with 4 passes
- `cargo nextest run --locked -p cintx-cubecl --lib kernels::ecp` exits 0
- `cargo build --locked --workspace` exits 0
- All four ECP rows in api_manifest.csv have oracle_covered=true; compiled_manifest.lock.json matches with zero diff
- The gradient component axis is slowest-varying per D-11
</verification>

<success_criteria>
ECP-04 fully closed (byte-identity across all 4 scalar+gradient symbols), ECP-05 satisfied (gradients delivered in this phase). Phase 19 is feature-complete pending only the optional Plan 06 libecpint cross-check.
</success_criteria>

<output>
After completion, create `.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-05-SUMMARY.md` capturing: actual PySCF nr_ecp_deriv C symbol names used, observed worst-case |diff| for the gradient parity (per axis), whether the collect_ecp_ipnuc_matrix_vendor needed a transpose for axis ordering, and any algorithmic corrections required during execution (e.g. sign conventions on the (r - A_C) factor, normalization of the ±1 angular-momentum shifted terms).
</output>
