---
phase: 19-int1e-ecp-type1-type2-evaluator
plan: 08
type: execute
wave: 4
depends_on: [19-06]
files_modified:
  - crates/cintx-oracle/build.rs
  - crates/cintx-oracle/src/libecpint_ffi.rs
  - crates/cintx-oracle/src/lib.rs
  - crates/cintx-oracle/tests/ecp_libecpint_crosscheck_parity.rs
  - .planning/notes/ecp-libecpint-crosscheck.md
autonomous: true
requirements: [ECP-04]
canonical_family: ecp

must_haves:
  truths:
    - "A non-blocking secondary cross-check oracle against libecpint (Shaw & Hill, JCP 147 074108, 2017, MIT) exists behind a build cfg (has_libecpint_oracle) emitted only when CINTX_LIBECPINT_ORACLE=1, and a sibling parity test file gated #[ignore] + the same env var (D-02 REVISED; Phase 16 ROCm opt-in precedent)."
    - "The cross-check is informational only: it does NOT block CI and uses a loose empirically-set tolerance (atol≈1e-9, rtol=0.0) documented in the test file as 'libecpint and PySCF nr_ecp use different recurrence + quadrature conventions internally'."
    - "When CINTX_LIBECPINT_ORACLE is unset, the build does NOT attempt to compile libecpint and the crate builds exactly as before (no regression to the default oracle build)."
  artifacts:
    - path: "crates/cintx-oracle/tests/ecp_libecpint_crosscheck_parity.rs"
      provides: "Env-gated #[ignore] cross-check tests for int1e_ecp_{cart,sph} vs libecpint at informational tolerance"
    - path: "crates/cintx-oracle/src/libecpint_ffi.rs"
      provides: "extern \"C\" shim wrappers for the libecpint integral entry points"
    - path: ".planning/notes/ecp-libecpint-crosscheck.md"
      provides: "Provenance + tolerance-rationale note for the secondary oracle"
  key_links:
    - from: "crates/cintx-oracle/tests/ecp_libecpint_crosscheck_parity.rs"
      to: "crates/cintx-oracle/src/libecpint_ffi.rs"
      via: "informational cross-check vs libecpint at atol≈1e-9"
      pattern: "CINTX_LIBECPINT_ORACLE|has_libecpint_oracle"
---

<objective>
Add the optional, non-blocking secondary cross-check oracle against libecpint (the actually-published JCP 2017 ECP library, MIT, robashaw/libecpint) per D-02 REVISED. This is the only non-blocking item in Phase 19: it satisfies ROADMAP SC#4's "secondary oracle" wording without gating CI. The primary byte-identity gate (vs PySCF nr_ecp at atol=1e-12) is already closed by 19-06/19-07; this plan adds a cross-implementation drift detector behind a `CINTX_LIBECPINT_ORACLE=1` opt-in.

Purpose: cross-implementation agreement metadata for ECP-04's secondary-oracle clause. libecpint uses different recurrence + quadrature conventions internally, so byte-identity is not expected; the tolerance is informational (atol≈1e-9). Failures never block CI.
Output: optional build cfg + libecpint FFI shim + env-gated #[ignore] cross-check test file + provenance note.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-CONTEXT.md
@.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-RESEARCH.md
@CLAUDE.md
@crates/cintx-oracle/build.rs
@crates/cintx-oracle/tests/safe_api_ecp_parity.rs
@crates/cintx-oracle/src/lib.rs

<interfaces>
Opt-in build/test gate precedent (Phase 16 ROCm): an env var (CINTX_ROCM_ORACLE=1) gates both
  a build cfg and #[ignore]'d tests. ECP mirrors this with CINTX_LIBECPINT_ORACLE=1 →
  has_libecpint_oracle cfg.

Vendor build precedent (crates/cintx-oracle/build.rs):
  - CINTX_ORACLE_BUILD_VENDOR gate at line ~83 (early return when unset).
  - rustc-check-cfg registration at lines 16-19 (has_vendor_libcint, has_vendor_pyscf_nr_ecp).
  - the libcint cc::Build chain + the parallel PySCF nr_ecp cc::Build (emits has_vendor_pyscf_nr_ecp
    at line ~362).
  libecpint is C++ (CMake or manual cc::Build with -std=c++17); the FFI needs an extern "C" shim
  layer. RESEARCH §"libecpint" (lines 125-138) documents the trade-offs and the MIT license.

Primary parity test shape (crates/cintx-oracle/tests/safe_api_ecp_parity.rs): the collector +
  count_mismatches pattern; the new cross-check file reuses build_cu_lanl2dz + the safe-API
  collector but compares against libecpint at the looser tolerance.

libecpint upstream: https://github.com/robashaw/libecpint (MIT, JCP 147 074108 2017).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Optional libecpint build cfg + extern "C" FFI shim + provenance note</name>
  <files>crates/cintx-oracle/build.rs, crates/cintx-oracle/src/libecpint_ffi.rs, crates/cintx-oracle/src/lib.rs, .planning/notes/ecp-libecpint-crosscheck.md</files>
  <read_first>
    - /home/user/Documents/workspace/cintx/crates/cintx-oracle/build.rs (the CINTX_ORACLE_BUILD_VENDOR gate, the rustc-check-cfg registration block lines 16-19, and the parallel PySCF nr_ecp cc::Build emitting has_vendor_pyscf_nr_ecp at line ~362 — the structural analog for a third optional cc::Build)
    - /home/user/Documents/workspace/cintx/crates/cintx-oracle/src/vendor_ffi.rs (the extern "C" wrapper + bindgen include pattern; the libecpint shim follows the same #[cfg(...)]-gated module shape but for C++-via-extern-C symbols)
    - /home/user/Documents/workspace/cintx/crates/cintx-oracle/src/lib.rs (module declarations — register the new libecpint_ffi module behind its cfg)
    - /home/user/Documents/workspace/cintx/.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-RESEARCH.md (§"libecpint" lines 125-138 — license, getIntegrals API shape, C++17 build trade-offs)
  </read_first>
  <action>
    Step 1 — Write .planning/notes/ecp-libecpint-crosscheck.md documenting: the upstream URL (https://github.com/robashaw/libecpint), the MIT license + JCP 147 074108 (2017) citation, the rationale that libecpint and PySCF nr_ecp use different internal recurrence + quadrature conventions (so byte-identity is NOT expected and the cross-check tolerance is informational ≈1e-9), and how to obtain/build libecpint locally for the opt-in run. State that this oracle is non-blocking per D-02 and never enters CI's required gates.

    Step 2 — In crates/cintx-oracle/build.rs, add an OPTIONAL third build branch guarded by `if env::var_os("CINTX_LIBECPINT_ORACLE").is_some()`. When set: compile/link libecpint (manual cc::Build with -std=c++17 over a vendored/installed libecpint source, OR link a system-installed libecpint — pick whichever is reachable on the dev host; document the choice in the note). Emit `println!("cargo:rustc-cfg=has_libecpint_oracle");`. Register the cfg via `println!("cargo::rustc-check-cfg=cfg(has_libecpint_oracle));` near lines 16-19. When the env var is unset, this branch is skipped entirely — the default oracle build is byte-for-byte unchanged. Also add `rerun-if-env-changed=CINTX_LIBECPINT_ORACLE`.

    Step 3 — Create crates/cintx-oracle/src/libecpint_ffi.rs gated `#![cfg(has_libecpint_oracle)]` with extern "C" shim wrappers for libecpint's ECP integral entry points (libecpint is C++; declare a thin extern "C" surface — either via a small C++ shim .cpp compiled in build.rs, or libecpint's own C-compatible API if it exposes one). Mirror the vendor_ffi.rs wrapper shape: a safe-ish Rust fn taking slices, calling the unsafe extern "C". Register the module in crates/cintx-oracle/src/lib.rs behind `#[cfg(has_libecpint_oracle)] pub mod libecpint_ffi;`.

    Step 4 — Confirm the default build is unaffected: `cargo --locked build -p cintx-oracle` (no env vars) builds with no new warnings and does NOT attempt libecpint. If libecpint cannot be obtained/built on the dev host this session, ship the FFI shim + build branch behind the cfg (so it compiles only when the env var + library are present) and record in the SUMMARY that the live cross-check run is deferred to a host with libecpint installed — the harness still lands per D-02's "optional, non-blocking" framing.
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/cintx &amp;&amp; cargo --locked build -p cintx-oracle 2>&amp;1 | tail -5 &amp;&amp; grep -F 'has_libecpint_oracle' crates/cintx-oracle/build.rs &amp;&amp; grep -F 'CINTX_LIBECPINT_ORACLE' crates/cintx-oracle/build.rs &amp;&amp; test -f .planning/notes/ecp-libecpint-crosscheck.md</automated>
  </verify>
  <acceptance_criteria>
    - `cargo --locked build -p cintx-oracle` (no env vars) exits 0 and does NOT compile libecpint (default build unchanged)
    - `grep -F 'has_libecpint_oracle' crates/cintx-oracle/build.rs` matches (cfg emitted under the env gate)
    - `grep -F 'rustc-check-cfg=cfg(has_libecpint_oracle)' crates/cintx-oracle/build.rs` matches
    - `grep -F 'CINTX_LIBECPINT_ORACLE' crates/cintx-oracle/build.rs` matches the env gate
    - `grep -F '#![cfg(has_libecpint_oracle)]' crates/cintx-oracle/src/libecpint_ffi.rs` matches
    - `.planning/notes/ecp-libecpint-crosscheck.md` exists with the MIT/JCP-2017 citation and the informational-tolerance rationale
  </acceptance_criteria>
  <done>An optional libecpint build branch + extern "C" FFI shim land behind has_libecpint_oracle (emitted only when CINTX_LIBECPINT_ORACLE=1); the default oracle build is unchanged; the provenance + tolerance note documents the non-blocking, informational nature.</done>
</task>

<task type="auto">
  <name>Task 2: Env-gated #[ignore] libecpint cross-check parity test at informational tolerance</name>
  <files>crates/cintx-oracle/tests/ecp_libecpint_crosscheck_parity.rs</files>
  <read_first>
    - /home/user/Documents/workspace/cintx/crates/cintx-oracle/tests/safe_api_ecp_parity.rs (the primary parity file — reuse build_cu_lanl2dz, the safe-API collector, and count_mismatches; the cross-check file is the same shape with a libecpint vendor side and a looser tolerance)
    - /home/user/Documents/workspace/cintx/crates/cintx-oracle/src/libecpint_ffi.rs (Task 1 output — the libecpint FFI wrappers)
    - /home/user/Documents/workspace/cintx/.planning/notes/ecp-libecpint-crosscheck.md (Task 1 output — the tolerance rationale to cite in the test rustdoc)
  </read_first>
  <action>
    Create crates/cintx-oracle/tests/ecp_libecpint_crosscheck_parity.rs:
      - Module gate `#![cfg(any(feature = "cpu", feature = "rocm"))]` matching safe_api_ecp_parity.rs.
      - File-top rustdoc: state this is the D-02 non-blocking secondary cross-check vs libecpint, informational tolerance (atol≈1e-9, rtol=0.0), opt-in via CINTX_LIBECPINT_ORACLE=1, and that it never blocks CI.
      - `const CROSSCHECK_ATOL: f64 = 1e-9; const CROSSCHECK_RTOL: f64 = 0.0;` with a rustdoc citing the note.
      - Two tests test_int1e_ecp_cart_libecpint_crosscheck and test_int1e_ecp_sph_libecpint_crosscheck, each `#[ignore = "libecpint secondary oracle is opt-in via CINTX_LIBECPINT_ORACLE=1"]` AND `#[cfg(has_libecpint_oracle)]`. Each test early-returns with an eprintln if std::env::var("CINTX_LIBECPINT_ORACLE").is_err() (defense-in-depth), then compares the cintx safe-API matrix (built via build_cu_lanl2dz + the safe-API collector) against the libecpint matrix at CROSSCHECK_ATOL. The assertion message frames any mismatch as informational drift, not a failure to block on.
      - Reuse the safe-API collector + a new collect_ecp_matrix_libecpint helper calling the Task-1 FFI. Document any normalization/convention adapter needed to map libecpint's output ordering onto cintx's layout.

    Confirm the file compiles in the default build (the #[cfg(has_libecpint_oracle)] tests are simply absent when the cfg is off, so the file is a no-op compile without libecpint).
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/cintx &amp;&amp; cargo --locked test -p cintx-oracle --features cpu --test ecp_libecpint_crosscheck_parity --no-run 2>&amp;1 | tail -5 &amp;&amp; grep -F 'CINTX_LIBECPINT_ORACLE' crates/cintx-oracle/tests/ecp_libecpint_crosscheck_parity.rs &amp;&amp; grep -F 'has_libecpint_oracle' crates/cintx-oracle/tests/ecp_libecpint_crosscheck_parity.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo --locked test -p cintx-oracle --features cpu --test ecp_libecpint_crosscheck_parity --no-run` exits 0 (file compiles in the default build with the cross-check tests cfg'd out)
    - `grep -F 'test_int1e_ecp_cart_libecpint_crosscheck' crates/cintx-oracle/tests/ecp_libecpint_crosscheck_parity.rs` matches
    - `grep -F 'test_int1e_ecp_sph_libecpint_crosscheck' crates/cintx-oracle/tests/ecp_libecpint_crosscheck_parity.rs` matches
    - `grep -F '#[ignore' crates/cintx-oracle/tests/ecp_libecpint_crosscheck_parity.rs` matches (both tests ignored by default)
    - `grep -F '1e-9' crates/cintx-oracle/tests/ecp_libecpint_crosscheck_parity.rs` matches (informational tolerance)
    - `grep -F 'CINTX_LIBECPINT_ORACLE' crates/cintx-oracle/tests/ecp_libecpint_crosscheck_parity.rs` matches
  </acceptance_criteria>
  <done>The libecpint cross-check parity file lands with two env-gated #[ignore]'d tests at informational atol≈1e-9; it compiles cleanly in the default build (cross-check tests cfg'd out without libecpint) and never blocks CI per D-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| optional libecpint C++ link → cintx-oracle | A C++ library linked only under an explicit env opt-in; FFI crosses an extern "C" shim. Not in the default build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-25 | Tampering (cross-check masks a real bug) | informational tolerance atol≈1e-9 | accept | The cross-check is informational and non-blocking by design (D-02); the BLOCKING gate is the atol=1e-12 PySCF nr_ecp parity (19-06/19-07). A loose secondary tolerance cannot mask a primary-gate failure because the primary gate runs independently and strictly. |
| T-19-26 | Integrity (FFI UB across C++ boundary) | libecpint_ffi extern "C" shim | mitigate | The shim is gated behind has_libecpint_oracle (off by default), takes Rust slices and bounds the out buffer; only exercised under explicit opt-in on a host with libecpint installed. |
| T-19-27 | Build regression on default path | optional cc::Build branch | mitigate | The libecpint build branch is fully skipped when CINTX_LIBECPINT_ORACLE is unset; Task 1 verifies the default `cargo build -p cintx-oracle` is unchanged. |
</threat_model>

<verification>
- `cargo --locked build -p cintx-oracle` (no env) exits 0; libecpint NOT compiled (default build unchanged)
- `cargo --locked test -p cintx-oracle --features cpu --test ecp_libecpint_crosscheck_parity --no-run` exits 0
- The cross-check tests are #[ignore]'d + cfg-gated; CI is never blocked by this plan
</verification>

<success_criteria>
ROADMAP SC#4's secondary-oracle clause is satisfied with a non-blocking, env-gated libecpint cross-check at informational tolerance (D-02 REVISED). The primary byte-identity gate remains the PySCF nr_ecp atol=1e-12 parity from 19-06/19-07; this plan adds cross-implementation drift detection without affecting the default build or CI gates. This is the optional close of Phase 19.
</success_criteria>

<output>
After completion, create `.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-08-SUMMARY.md` capturing: whether libecpint was buildable/linkable on the dev host this session (live cross-check run vs deferred-to-equipped-host), the observed cross-implementation |diff| if run, the normalization/convention adapter needed to map libecpint output onto cintx's layout, and confirmation the default oracle build is unchanged.
</output>
