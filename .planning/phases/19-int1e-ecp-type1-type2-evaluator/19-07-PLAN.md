---
phase: 19-int1e-ecp-type1-type2-evaluator
plan: 07
type: execute
wave: 3
depends_on: [19-06]
files_modified:
  - crates/cintx-cubecl/src/kernels/ecp.rs
  - crates/cintx-oracle/src/vendor_ffi.rs
  - crates/cintx-oracle/tests/safe_api_ecp_parity.rs
  - crates/cintx-ops/src/generated/api_manifest.csv
  - crates/cintx-ops/src/generated/api_manifest.rs
  - crates/cintx-ops/generated/compiled_manifest.lock.json
autonomous: true
requirements: [ECP-01, ECP-02, ECP-04, ECP-05]
canonical_family: ecp
forms: [cart, sph]
component_rank: 3

must_haves:
  truths:
    - "launch_ecp's operator_name == \"ecp_ipnuc\" arm computes the 3-component (∂/∂A_C^{x,y,z}) ECP gradient for both Type-1 and Type-2 channels using PySCF nr_ecp_deriv.c's _deriv1_cart algorithm on top of the 19-05 K-Taylor radial machinery; the 19-04 UnsupportedApi placeholder is replaced with a real implementation."
    - "Gradient output flat-buffer layout is F-order with the component axis slowest-varying: [axis ∈ {x,y,z}, ao_j, ao_i] — matching the int3c2e_ip1_* precedent (D-11). The collector transposes if PySCF's internal component order differs (determined by reading nr_ecp_deriv.c, not assumed)."
    - "vendor_ECPscalar_ipnuc_cart and vendor_ECPscalar_ipnuc_sph wrap PySCF nr_ecp_deriv.c's ECPscalar_ipnuc_cart (line 366) / ECPscalar_ipnuc_sph (line 453) gradient entry points; the bindgen allowlist already includes them from 19-04 Task 2."
    - "safe_api_ecp_parity.rs gains test_int1e_ecp_ipnuc_cart_safe_api_parity and test_int1e_ecp_ipnuc_sph_safe_api_parity; both pass at atol=1e-12, rtol=0.0 across the Cu/LANL2DZ Cartesian product × 3 derivative components, run by default (no #[ignore])."
    - "api_manifest.csv flips oracle_covered=true on the two ipnuc rows; compiled_manifest.lock.json regenerated; manifest-audit reports zero diff. All four ECP rows are now oracle_covered=true."
  artifacts:
    - path: "crates/cintx-cubecl/src/kernels/ecp.rs"
      provides: "compute_type1_pair_grad / compute_type2_pair_grad gradient branch under operator_name == \"ecp_ipnuc\""
      contains: "ecp_ipnuc"
    - path: "crates/cintx-oracle/src/vendor_ffi.rs"
      provides: "vendor_ECPscalar_ipnuc_cart + vendor_ECPscalar_ipnuc_sph wrappers"
    - path: "crates/cintx-oracle/tests/safe_api_ecp_parity.rs"
      provides: "Two ipnuc parity tests at atol=1e-12 (component_rank=3)"
  key_links:
    - from: "crates/cintx-cubecl/src/kernels/ecp.rs gradient branch"
      to: "vendor/pyscf-nr-ecp/src/nr_ecp_deriv.c::_deriv1_cart"
      via: "∂/∂A_C^μ ECPtype1 + ECPtype2 derivative on the K-Taylor radial foundation"
      pattern: "nr_ecp_deriv|ecp_ipnuc"
    - from: "test_int1e_ecp_ipnuc_{cart,sph}_safe_api_parity"
      to: "vendor_ECPscalar_ipnuc_{cart,sph}"
      via: "byte-identity gradient parity at atol=1e-12 over Cu/LANL2DZ × 3 components"
      pattern: "vendor_ECPscalar_ipnuc"
---

<objective>
Close the gradient half of Phase 19. Replace 19-04's `UnsupportedApi` stub on the `operator_name == "ecp_ipnuc"` arm of `launch_ecp` with a real port of PySCF `nr_ecp_deriv.c`'s `_deriv1_cart` (component_rank=3) algorithm — Type-1 and Type-2 derivatives w.r.t. the ECP center, built on the same 19-05 K-Taylor radial foundation. Write the 3-component result in F-order `[axis, ao_j, ao_i]` (axis slowest-varying per D-11), wire the two ipnuc vendor FFI wrappers, add the two gradient parity tests, and flip the two ipnuc manifest rows to `oracle_covered=true`.

Purpose: deliver `int1e_ecp_ipnuc_{cart,sph}` (ROADMAP symbol list), satisfy ECP-05 (gradients in scope per D-10), and complete ECP-04 (byte-identity across all four scalar+gradient symbols). This supersedes the halted 19-05-PLAN (now .superseded.md) which assumed a direct-quadrature foundation that could not reach byte-identity.
Output: gradient branch in launch_ecp, two vendor FFI wrappers, two passing gradient parity tests, two flipped manifest rows + regenerated lock.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-CONTEXT.md
@.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-RESEARCH.md
@.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-05-PLAN.md
@.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-05-PLAN.superseded.md
@CLAUDE.md
@crates/cintx-cubecl/src/kernels/ecp.rs
@crates/cintx-oracle/src/vendor_ffi.rs
@crates/cintx-oracle/tests/safe_api_ecp_parity.rs
@vendor/pyscf-nr-ecp/src/nr_ecp_deriv.c
@vendor/pyscf-nr-ecp/include/gto/nr_ecp.h

<interfaces>
launch_ecp signature (crates/cintx-cubecl/src/kernels/ecp.rs):
  pub fn launch_ecp(backend: &ResolvedBackend, plan: &ExecutionPlan<'_>,
                    specialization: &SpecializationKey, staging: &mut [f64])
      -> Result<ExecutionStats, cintxRsError>
  After 19-06 the scalar arm (operator_name == "ecp") uses the K-Taylor radial port.
  The gradient arm (operator_name == "ecp_ipnuc") still returns UnsupportedApi — this plan
  replaces it. compute_type1_pair / compute_type2_pair (19-06) are the scalar analogs the
  *_grad helpers parallel.

19-05 K-Taylor host primitives (crates/cintx-cubecl/src/math/ecp_k_taylor.rs):
  ecpsph_ine_opt_host, ecprad_part_host, type1_rad_part_host, type2_facs_rad_host.
  The gradient adds a (r - A_C)^μ factor that shifts angular momentum ±1 and radial power ±1;
  it reuses these same radial primitives (nr_ecp_deriv.c introduces no new radial machinery
  beyond nr_ecp.c's ECPrad_part / ECPsph_ine_opt).

PySCF gradient entry points (vendor/pyscf-nr-ecp/src/nr_ecp_deriv.c):
  - ECPscalar_ipnuc_cart (line 366): _cart_factory(_deriv1_cart, out, comp=3, ...).
  - ECPscalar_ipnuc_sph  (line 453): _sph_factory(_deriv1_cart, out, comp=3, ...) — applies
    the per-component cart→sph transform via ECPscalar_c2s_factory + ECPscalar_distribute.
  - _deriv1_cart: the per-pair 3-component derivative driver. necpbas + ecpbas are read from
    env[AS_NECPBAS] / env[AS_ECPBAS_OFFSET] (lines 369-370 / 456-457) — same convention the
    scalar wrappers use (19-04 SUMMARY confirms no separate ecpbas arg in the C ABI).
  Component layout: PySCF writes comp=3 as [comp, dij] (comp outer / slowest). cintx wants
  F-order [axis, ao_j, ao_i] (axis slowest). Confirm PySCF's exact internal order by reading
  _deriv1_cart + ECPscalar_distribute; transpose in the collector if it differs (do not assume).

Vendor wrapper analog (crates/cintx-oracle/src/vendor_ffi.rs, 19-04 Task 2 output):
  vendor_ECPscalar_sph / vendor_ECPscalar_cart — same C ABI shape (out, shls, atm, natm, bas,
  nbas, env, opt=NULL, cache=NULL). The ipnuc wrappers reuse this signature; the only
  difference is the out buffer is 3× larger (component_rank=3). The bindgen allowlist already
  includes ECPscalar_ipnuc_{sph,cart} (19-04 Task 2 landed all four allowlist entries) — no
  build.rs edit needed.

Manifest: api_manifest.csv rows 30-31 are int1e_ecp_ipnuc_{cart,sph} with component_rank=3 at
  oracle_covered=false today. After 19-06 the two scalar rows are already true. Flip mechanics:
  same as 19-06 Task 2 (lock JSON canonical → regenerate via oracle-covered-update / build.rs).

F-order component-rank-3 precedent: int3c2e_ip1_{cart,sph} writes [axis, ao_..., ao_i] with
  axis slowest-varying. The halted 19-05-PLAN.superseded.md carries the same convention notes —
  reuse them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the gradient branch in launch_ecp and add the two ipnuc vendor FFI wrappers</name>
  <files>crates/cintx-cubecl/src/kernels/ecp.rs, crates/cintx-oracle/src/vendor_ffi.rs</files>
  <read_first>
    - /home/user/Documents/workspace/cintx/crates/cintx-cubecl/src/kernels/ecp.rs (19-06 output — scalar compute_type1_pair / compute_type2_pair are the structural analogs; the operator_name == "ecp_ipnuc" arm currently returns UnsupportedApi)
    - /home/user/Documents/workspace/cintx/crates/cintx-cubecl/src/math/ecp_k_taylor.rs (19-05 radial primitives the gradient reuses)
    - /home/user/Documents/workspace/cintx/vendor/pyscf-nr-ecp/src/nr_ecp_deriv.c (ECPscalar_ipnuc_cart line 366, ECPscalar_ipnuc_sph line 453, _cart_factory / _sph_factory, and _deriv1_cart — the per-pair 3-component derivative; read the (r - A_C)^μ factor and the ±1 angular-momentum / ±1 radial-power shift, and confirm the [comp, dij] component ordering + the ECPscalar_distribute layout)
    - /home/user/Documents/workspace/cintx/crates/cintx-oracle/src/vendor_ffi.rs (19-04 output — vendor_ECPscalar_{sph,cart} signature is the direct analog; the ipnuc allowlist entries already exist)
    - /home/user/Documents/workspace/cintx/crates/cintx-cubecl/src/kernels/f12.rs (operator-suffix dispatch pattern for the scalar-vs-gradient split, lines ~1517-1568)
    - /home/user/Documents/workspace/cintx/.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-05-PLAN.superseded.md (the halted gradient plan — its F-order [axis, ao_j, ao_i] layout + transpose-convention notes carry forward verbatim)
  </read_first>
  <action>
    Step 1 — Read nr_ecp_deriv.c to confirm: (a) the exact C symbol names are ECPscalar_ipnuc_cart (366) and ECPscalar_ipnuc_sph (453); (b) the _deriv1_cart per-pair algorithm — the derivative adds a (r - A_C)^μ factor in the integrand, shifting angular momentum ±1 and radial power ±1, reusing ECPrad_part / type1_rad_part / type2_facs_rad with shifted arguments; (c) the output component order PySCF writes (read _deriv1_cart + ECPscalar_distribute — typically [comp, dij] with comp slowest). Record the resolved convention in the SUMMARY.

    Step 2 — Replace the operator_name == "ecp_ipnuc" UnsupportedApi arm in launch_ecp with a real implementation that parallels the 19-06 scalar arm:
      1. Assert the staging output length is 3 * ncart(li) * ncart(lj) (cart) or 3 * nsph(li) * nsph(lj) (sph after transform); return a typed Layout/ChunkPlanFailed error with a clear "buffer too small for component_rank=3" detail if undersized.
      2. Iterate the same shell-pair × ECP-shell loop as the scalar arm. For each (i, j) pair × ECP shell c, branch on c.channel:
         - EcpChannel::Local ⇒ compute_type1_pair_grad(...): same K-Taylor radial assembly as compute_type1_pair, but each axis component μ ∈ {x,y,z} picks up the (r - A_C)^μ factor (±1 ang-mom / ±1 radial-power shift). Port the _deriv1_cart Type-1 inner loop; cite the nr_ecp_deriv.c line range in a rustdoc comment.
         - EcpChannel::Projected(l) ⇒ compute_type2_pair_grad(...): same Type-2 angular splice, but the projector channel shifts ±1 due to the Y_lm derivative coupling. Port the _deriv1_cart Type-2 inner loop; cite the line range.
      3. Write the 3-component result in F-order [axis, ao_j, ao_i] (axis slowest-varying). For sph, apply crate::transform::c2s::cart_to_sph_1e PER COMPONENT: loop axis in 0..3, transform the (ncart(li) × ncart(lj)) slice into the (nsph(li) × nsph(lj)) slice; axis stays slowest.
      Document at the gradient arm:
        - `// Source: vendor/pyscf-nr-ecp/src/nr_ecp_deriv.c::ECPscalar_ipnuc_cart / _deriv1_cart` with line range.
        - `// F-order component layout [axis ∈ {x,y,z}, ao_j, ao_i] (axis slowest) — int3c2e_ip1_* precedent (CONTEXT D-11).`

    Step 3 — Replace the Plan 04 stub test that rejected the gradient operator name with a positive test: operator_name "ecp_ipnuc" on a valid plan with ecp_shells attached resolves and returns Ok(_) (or the kernel-level helper produces a 3-component buffer). Add an inline test asserting the gradient arm rejects an undersized staging slice with the typed Layout/ChunkPlanFailed error, and a zero-overlap sanity test (a shell pair far-separated from the ECP center yields a near-zero gradient slice within machine epsilon).

    Step 4 — Extend crates/cintx-oracle/src/vendor_ffi.rs with vendor_ECPscalar_ipnuc_cart and vendor_ECPscalar_ipnuc_sph, mirroring the 19-04 vendor_ECPscalar_{cart,sph} wrappers (same C ABI: out, shls, atm, natm, bas, nbas, env, opt=NULL, cache=NULL). The out buffer is 3 * nao_i * nao_j f64s; document this in rustdoc and add a debug_assert on the buffer length before the unsafe call. Both gated #[cfg(has_vendor_pyscf_nr_ecp)] with #[allow(non_snake_case)]. The bindgen allowlist already declares both symbols (19-04 Task 2) — no build.rs edit.
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/cintx &amp;&amp; cargo test --locked -p cintx-cubecl --lib kernels::ecp 2>&amp;1 | tail -15 &amp;&amp; bash -o pipefail -c "CINTX_ORACLE_BUILD_VENDOR=1 cargo build --locked -p cintx-oracle 2>&amp;1 | tail -5" &amp;&amp; grep -F 'pub fn vendor_ECPscalar_ipnuc_cart' crates/cintx-oracle/src/vendor_ffi.rs &amp;&amp; grep -F 'pub fn vendor_ECPscalar_ipnuc_sph' crates/cintx-oracle/src/vendor_ffi.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --locked -p cintx-cubecl --lib kernels::ecp` exits 0; the previous "rejects gradient operator name" stub test is updated to a positive assertion
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo build --locked -p cintx-oracle` exits 0 (`bash -o pipefail` propagates cargo's exit through the tail pipe)
    - `grep -F 'compute_type1_pair_grad' crates/cintx-cubecl/src/kernels/ecp.rs` matches
    - `grep -F 'compute_type2_pair_grad' crates/cintx-cubecl/src/kernels/ecp.rs` matches
    - `grep -F '"ecp_ipnuc"' crates/cintx-cubecl/src/kernels/ecp.rs` matches the dispatch arm
    - `grep -F 'nr_ecp_deriv' crates/cintx-cubecl/src/kernels/ecp.rs` matches the cited algorithm source
    - `grep -cF 'cart_to_sph_1e' crates/cintx-cubecl/src/kernels/ecp.rs` returns at least 2 (scalar from 19-06 + per-axis gradient transform)
    - `grep -F 'pub fn vendor_ECPscalar_ipnuc_cart' crates/cintx-oracle/src/vendor_ffi.rs` matches
    - `grep -F 'pub fn vendor_ECPscalar_ipnuc_sph' crates/cintx-oracle/src/vendor_ffi.rs` matches
  </acceptance_criteria>
  <done>The gradient branch lands inside the same launch_ecp launcher, computes 3-component ∂/∂A_C^μ contributions for Type-1 and Type-2 on the K-Taylor foundation, writes F-order [axis, ao_j, ao_i], reuses cart_to_sph_1e per axis for sph; two new vendor FFI wrappers expose the PySCF nr_ecp_deriv reference symbols; in-module tests assert the buffer-size invariant and zero-overlap sanity. Byte-identity is proved in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Add the two ipnuc parity tests at atol=1e-12 and flip oracle_covered=true on the two ipnuc rows</name>
  <files>crates/cintx-oracle/tests/safe_api_ecp_parity.rs, crates/cintx-ops/src/generated/api_manifest.csv, crates/cintx-ops/src/generated/api_manifest.rs, crates/cintx-ops/generated/compiled_manifest.lock.json</files>
  <read_first>
    - /home/user/Documents/workspace/cintx/crates/cintx-oracle/tests/safe_api_ecp_parity.rs (19-06 output — the two scalar tests now pass; reuse build_cu_lanl2dz_safe_basis, collect_safe_api_ecp_matrix, collect_ecp_matrix_vendor, count_mismatches; the ipnuc collector mirrors the scalar one with a 3× buffer + axis handling)
    - /home/user/Documents/workspace/cintx/crates/cintx-oracle/src/vendor_ffi.rs (Task 1 output — vendor_ECPscalar_ipnuc_{cart,sph} signatures)
    - /home/user/Documents/workspace/cintx/crates/cintx-cubecl/src/kernels/ecp.rs (Task 1 output — the F-order [axis, ao_j, ao_i] convention the collector must match)
    - /home/user/Documents/workspace/cintx/xtask/src/oracle_covered_update.rs (manifest oracle_covered flip mechanics)
    - /home/user/Documents/workspace/cintx/.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-06-PLAN.md (Task 2 manifest-flip mechanics established for the scalar rows — reuse identically)
  </read_first>
  <action>
    Step 1 — Append test_int1e_ecp_ipnuc_cart_safe_api_parity and test_int1e_ecp_ipnuc_sph_safe_api_parity to safe_api_ecp_parity.rs after the scalar tests. Each is gated #[cfg(all(has_vendor_libcint, has_vendor_pyscf_nr_ecp))], uses OperatorId::INT1E_ECP_IPNUC_CART / INT1E_ECP_IPNUC_SPH, ATOL=1e-12, RTOL=0.0, and asserts zero mismatches over the full Cu/LANL2DZ Cartesian product × 3 components. NO #[ignore].

    Step 2 — Add a collect_ecp_ipnuc_matrix_vendor helper mirroring collect_ecp_matrix_vendor but: allocates a 3 * nao_i * nao_j output buffer per shell pair; calls vendor_ECPscalar_ipnuc_{cart,sph}; returns the flat-buffer concatenation matching the safe-API collector's F-order [axis, ao_j, ao_i] layout. IMPORTANT: if PySCF writes the component axis in a different order ([comp, dij] vs cintx's [axis, ao_j, ao_i]), the collector must transpose explicitly — never silently. Cite PySCF's actual component output convention (resolved in Task 1) in the helper rustdoc and document any transpose step.

    Step 3 — Flip oracle_covered=true on the int1e_ecp_ipnuc_cart and int1e_ecp_ipnuc_sph rows using the same mechanics 19-06 Task 2 used for the scalar rows: edit crates/cintx-ops/generated/compiled_manifest.lock.json (canonical source) to set oracle_covered=true on the two ipnuc entries across all four profiles, then regenerate via `cargo run -p xtask --locked -- oracle-covered-update` (or the build.rs regeneration path) so api_manifest.csv + api_manifest.rs match. All four ECP rows are now oracle_covered=true.

    Step 4 — Run the FULL ECP parity suite to confirm the gradient changes did not regress the scalar parity: `CINTX_ORACLE_BUILD_VENDOR=1 cargo test --locked -p cintx-oracle --features cpu --test safe_api_ecp_parity`. Must report 4 parity tests passing (2 scalar from 19-06 + 2 gradient). Run `cargo run -p xtask --locked -- manifest-audit` and confirm zero diff. Record the worst-case |diff| per axis in the SUMMARY.
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/cintx &amp;&amp; bash -o pipefail -c "CINTX_ORACLE_BUILD_VENDOR=1 cargo test --locked -p cintx-oracle --features cpu --test safe_api_ecp_parity 2>&amp;1 | tail -25" &amp;&amp; bash -o pipefail -c "cargo run -p xtask --locked -- manifest-audit 2>&amp;1 | tail -5" &amp;&amp; grep -E '"int1e_ecp_ipnuc_cart".*,true,' crates/cintx-ops/src/generated/api_manifest.csv &amp;&amp; grep -E '"int1e_ecp_ipnuc_sph".*,true,' crates/cintx-ops/src/generated/api_manifest.csv</automated>
  </verify>
  <acceptance_criteria>
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test --locked -p cintx-oracle --features cpu --test safe_api_ecp_parity` exits 0 with all 4 ECP parity tests passing (2 scalar + 2 gradient); `bash -o pipefail` propagates the test-runner exit through the tail pipe
    - `grep -F 'test_int1e_ecp_ipnuc_cart_safe_api_parity' crates/cintx-oracle/tests/safe_api_ecp_parity.rs` matches
    - `grep -F 'test_int1e_ecp_ipnuc_sph_safe_api_parity' crates/cintx-oracle/tests/safe_api_ecp_parity.rs` matches
    - `grep -F 'collect_ecp_ipnuc_matrix_vendor' crates/cintx-oracle/tests/safe_api_ecp_parity.rs` matches
    - `grep -E '"int1e_ecp_ipnuc_cart".*,true,"operator","ecp"' crates/cintx-ops/src/generated/api_manifest.csv` matches
    - `grep -E '"int1e_ecp_ipnuc_sph".*,true,"operator","ecp"' crates/cintx-ops/src/generated/api_manifest.csv` matches
    - `grep -E '"int1e_ecp_.*,false,' crates/cintx-ops/src/generated/api_manifest.csv` returns 0 matches (all four ECP rows now oracle_covered=true)
    - `cargo run -p xtask --locked -- manifest-audit` exits 0 (zero diff; `bash -o pipefail` propagates the xtask exit through the tail pipe)
  </acceptance_criteria>
  <done>Two ipnuc parity tests pass at atol=1e-12 against PySCF nr_ecp_deriv (component_rank=3, F-order axis slowest); all four ECP manifest rows are oracle_covered=true; the regenerated lock matches the CSV with zero diff; the full Phase 19 byte-identity gate (4 tests) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kernel gradient branch → vendored C derivative | New extern "C" surface; bindgen allowlist already in place from 19-04 Task 2. |
| component-axis F-order layout → downstream consumer | A transposed axis silently produces correct-shape wrong-value gradients. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-22 | Tampering (silent axis bug) | launch_ecp gradient F-order layout | mitigate | The per-component parity test catches a transposed axis; collect_ecp_ipnuc_matrix_vendor explicitly documents and converts the PySCF axis order — if PySCF writes a different convention the collector transposes, never silently. |
| T-19-23 | Integrity (FFI UB) | vendor_ECPscalar_ipnuc_* out buffer size | mitigate | Wrapper rustdoc states out must be 3 * nao_i * nao_j f64s; a debug_assert enforces it before the unsafe call. |
| T-19-24 | Numerical regression on scalar parity | gradient edits to launch_ecp body | mitigate | Task 2 runs the FULL parity suite (4 tests = 2 scalar from 19-06 + 2 gradient) to catch any regression in the scalar arm. |
</threat_model>

<verification>
- `CINTX_ORACLE_BUILD_VENDOR=1 cargo test --locked -p cintx-oracle --features cpu --test safe_api_ecp_parity` exits 0 with 4 parity tests passing
- `cargo test --locked -p cintx-cubecl --lib kernels::ecp` exits 0
- `cargo --locked build --workspace` exits 0
- All four ECP rows oracle_covered=true; compiled_manifest.lock.json matches with zero diff
- The gradient component axis is slowest-varying per D-11
</verification>

<success_criteria>
ECP-04 fully closed (byte-identity across all 4 scalar+gradient symbols at atol=1e-12), ECP-05 satisfied (gradients delivered in this phase per D-10). All five Phase 19 requirements (ECP-01..05) are now closed at exact byte-identity. Phase 19 is feature-complete pending only the optional 19-08 libecpint secondary cross-check.
</success_criteria>

<output>
After completion, create `.planning/phases/19-int1e-ecp-type1-type2-evaluator/19-07-SUMMARY.md` capturing: the actual PySCF nr_ecp_deriv C symbol names + line ranges used, the observed worst-case |diff| per axis for the gradient parity, whether collect_ecp_ipnuc_matrix_vendor needed a transpose for axis ordering, and any algorithmic corrections required during execution (sign conventions on the (r - A_C) factor, normalization of the ±1 angular-momentum-shifted terms).
</output>
