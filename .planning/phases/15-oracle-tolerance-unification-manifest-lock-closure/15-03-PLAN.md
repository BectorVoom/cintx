---
phase: 15-oracle-tolerance-unification-manifest-lock-closure
plan: 03
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - .github/workflows/compat-governance-pr.yml
  - xtask/src/oracle_update.rs
autonomous: true
requirements: [ORAC-03]

must_haves:
  truths:
    - "CI oracle_parity_gate runs four parallel matrix jobs, one per profile"
    - "All four profile jobs must pass for the gate to succeed"
    - "Single-profile oracle-compare invocations are accepted without validation error"
  artifacts:
    - path: ".github/workflows/compat-governance-pr.yml"
      provides: "Matrix strategy oracle_parity_gate job"
      contains: "matrix:"
    - path: "xtask/src/oracle_update.rs"
      provides: "Single-profile oracle-compare support"
  key_links:
    - from: ".github/workflows/compat-governance-pr.yml"
      to: "xtask/src/oracle_update.rs"
      via: "cargo run -- oracle-compare --profiles single_profile"
      pattern: "matrix.profile"
---

<objective>
Switch CI oracle_parity_gate to GitHub Actions matrix strategy over four profiles.

Purpose: Run oracle parity for each profile in parallel instead of sequentially, making failures profile-specific and CI faster (per D-09).

Output: Updated CI workflow with matrix strategy; xtask oracle-compare accepts single-profile invocations.
</objective>

<execution_context>
@/home/chemtech/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/chemtech/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-oracle-tolerance-unification-manifest-lock-closure/15-CONTEXT.md
@.planning/phases/15-oracle-tolerance-unification-manifest-lock-closure/15-02-SUMMARY.md

<interfaces>
From xtask/src/oracle_update.rs (line 195-231):
```rust
fn validate_required_profile_scope(profiles: &[String]) -> Result<Vec<String>> {
    // Currently requires ALL four standard profiles to be passed together
    // Single-profile calls fail with "profile scope mismatch"
    // Unstable-source must be standalone
}
```

From .github/workflows/compat-governance-pr.yml (lines 73-107):
```yaml
oracle_parity_gate:
    name: oracle_parity_gate
    runs-on: ubuntu-latest
    steps:
        # ... checkout, toolchain, cache ...
        - name: Run oracle parity gate
          run: |
              CINTX_BACKEND=cpu cargo run --manifest-path xtask/Cargo.toml -- oracle-compare \
                --profiles "${CINTX_REQUIRED_PROFILES}" --include-unstable-source false
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relax oracle-compare profile validation to accept single profiles</name>
  <files>xtask/src/oracle_update.rs</files>
  <read_first>
    - xtask/src/oracle_update.rs
  </read_first>
  <action>
**In `xtask/src/oracle_update.rs`:**

Modify `validate_required_profile_scope()` to accept a subset of the four standard profiles (not just the full set). The CI matrix will pass one profile at a time.

Replace the current validation logic (lines 195-231). The new logic:
- Still reject unknown profiles
- Still enforce unstable-source standalone rule
- Accept any non-empty subset of the four standard profiles (remove the "must pass all four" check)

```rust
fn validate_required_profile_scope(profiles: &[String]) -> Result<Vec<String>> {
    // Validate all requested profiles are known
    for p in profiles {
        if !ALL_KNOWN_PROFILES.contains(&p.as_str()) {
            bail!(
                "unknown profile `{p}`, expected one of: {}",
                ALL_KNOWN_PROFILES.join(", ")
            );
        }
    }
    // If requesting unstable-source, it runs standalone (per D-02)
    if profiles.iter().any(|p| p == "unstable-source") {
        if profiles.len() != 1 || profiles[0] != "unstable-source" {
            bail!("unstable-source profile must be run standalone, not combined with other profiles");
        }
        return Ok(vec!["unstable-source".to_owned()]);
    }
    // Accept any non-empty subset of standard profiles.
    // The CI matrix ensures all four are covered across parallel jobs.
    if profiles.is_empty() {
        bail!("at least one profile must be specified");
    }
    Ok(profiles.to_vec())
}
```

This allows `oracle-compare --profiles base` to work (previously it would bail with "profile scope mismatch").
  </action>
  <verify>
    <automated>cd /home/chemtech/workspace/cintx && cargo check -p xtask 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - oracle_update.rs `validate_required_profile_scope` does NOT contain "profile scope mismatch" bail
    - oracle_update.rs does NOT compare `requested` against `required` BTreeSet with difference check for standard profiles
    - oracle_update.rs still contains the unstable-source standalone check
    - `cargo check -p xtask` exits 0
  </acceptance_criteria>
  <done>oracle-compare accepts single-profile invocations for CI matrix use</done>
</task>

<task type="auto">
  <name>Task 2: Update CI workflow to matrix strategy for oracle_parity_gate</name>
  <files>.github/workflows/compat-governance-pr.yml</files>
  <read_first>
    - .github/workflows/compat-governance-pr.yml
  </read_first>
  <action>
**In `.github/workflows/compat-governance-pr.yml`:**

Replace the `oracle_parity_gate` job (currently a single job running all 4 profiles sequentially) with a matrix strategy that runs each profile as a parallel job.

Find the `oracle_parity_gate:` job block and replace it with:

```yaml
    oracle_parity_gate:
        name: oracle_parity_gate (${{ matrix.profile }})
        runs-on: ubuntu-latest
        strategy:
            fail-fast: false
            matrix:
                profile: [base, with-f12, with-4c1e, "with-f12+with-4c1e"]
        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Resolve pinned Rust channel
              id: rust
              run: |
                  python <<'PY'
                  import os
                  import tomllib
                  from pathlib import Path

                  data = tomllib.loads(Path("rust-toolchain.toml").read_text())
                  toolchain = data.get("toolchain", {})
                  channel = toolchain.get("channel")
                  if not channel:
                      raise SystemExit("failed to resolve channel from rust-toolchain.toml")
                  with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as fh:
                      fh.write(f"channel={channel}\n")
                  PY

            - name: Install pinned Rust toolchain
              uses: dtolnay/rust-toolchain@master
              with:
                  toolchain: ${{ steps.rust.outputs.channel }}

            - name: Cache Rust artifacts
              uses: Swatinem/rust-cache@v2

            - name: Run oracle parity gate for ${{ matrix.profile }}
              run: |
                  CINTX_BACKEND=cpu cargo run --manifest-path xtask/Cargo.toml -- oracle-compare --profiles "${{ matrix.profile }}" --include-unstable-source false
```

Key changes from existing:
- Added `strategy.fail-fast: false` so all profiles report independently (D-09)
- Added `matrix.profile` with all four standard profiles
- Changed job name to include profile for identification
- Changed `--profiles` to pass single `${{ matrix.profile }}` instead of `${CINTX_REQUIRED_PROFILES}`
- The `"with-f12+with-4c1e"` value is quoted in the YAML array because `+` can be special

Preserve everything else in the workflow file (env block, other jobs, artifact uploads). Do NOT modify `manifest_drift_gate`, `helper_legacy_parity_gate`, `oom_contract_gate`, or any other job.

Also verify: if any other job has `needs: [oracle_parity_gate]`, the matrix strategy still satisfies that dependency (GitHub Actions waits for all matrix jobs to complete before downstream `needs` are satisfied).
  </action>
  <verify>
    <automated>cd /home/chemtech/workspace/cintx && grep -A 15 "oracle_parity_gate:" .github/workflows/compat-governance-pr.yml | head -20</automated>
  </verify>
  <acceptance_criteria>
    - compat-governance-pr.yml contains `matrix:` under `oracle_parity_gate`
    - compat-governance-pr.yml contains `profile: [base, with-f12, with-4c1e,` in the matrix block
    - compat-governance-pr.yml contains `fail-fast: false`
    - compat-governance-pr.yml contains `--profiles "${{ matrix.profile }}"` in the oracle-compare run step
    - compat-governance-pr.yml still contains `manifest_drift_gate`, `helper_legacy_parity_gate`, `oom_contract_gate` jobs unchanged
  </acceptance_criteria>
  <done>CI oracle_parity_gate uses matrix strategy over four profiles with fail-fast:false; each profile runs in parallel</done>
</task>

</tasks>

<verification>
- `cargo check -p xtask` confirms oracle-compare accepts single profiles
- CI workflow YAML is valid (matrix strategy, four profiles, fail-fast: false)
- Other CI jobs are unchanged
</verification>

<success_criteria>
- ORAC-03: CI oracle-parity gate runs all four profiles (via matrix) at atol=1e-12
- D-09: GitHub Actions matrix strategy over four profiles, all must pass
</success_criteria>

<output>
After completion, create `.planning/phases/15-oracle-tolerance-unification-manifest-lock-closure/15-03-SUMMARY.md`
</output>
