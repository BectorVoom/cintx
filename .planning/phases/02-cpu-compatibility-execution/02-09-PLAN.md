---
phase: 02-cpu-compatibility-execution
plan: "09"
type: execute
gap_closure: true
wave: 7
depends_on:
  - 02-01
files_modified:
  - tests/phase2_cpu_backend_routing.rs
  - src/runtime/execution_plan.rs
autonomous: true
requirements:
  - EXEC-01
must_haves:
  truths:
    - The execution-request regression contract reflects normalized feature-flag behavior rather than insertion-order assumptions.
    - Safe and raw request construction continue using one shared normalization path for execution memory options.
    - The previously failing Phase 2 verification gate is restored to green without widening phase scope.
  artifacts:
    - tests/phase2_cpu_backend_routing.rs updates `execution_request_contract` to assert the intended normalization contract.
    - src/runtime/execution_plan.rs keeps the normalization behavior explicit for `ExecutionMemoryOptions::from`.
    - Verification reruns demonstrate `phase2_cpu_backend_routing` and the full Phase 2 suite pass.
  key_links:
    - src/runtime/execution_plan.rs is the source of normalized feature-flag ordering used by execution requests.
    - tests/phase2_cpu_backend_routing.rs contains the failing gate called out in `02-VERIFICATION.md`.
---

<objective>
Close the single unresolved Phase 2 verification gap by aligning the execution-request contract test with the runtime's normalized feature-flag semantics.

Purpose: Restore a fully green Phase 2 regression gate without replanning completed implementation work.
Output: corrected execution-request contract assertion and verification evidence that the previously failing test now passes.
</objective>

<execution_context>
@/home/chemtech/.codex/get-shit-done/workflows/execute-plan.md
@/home/chemtech/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cpu-compatibility-execution/02-VERIFICATION.md
@.planning/phases/02-cpu-compatibility-execution/02-01-SUMMARY.md
@src/runtime/execution_plan.rs
@tests/phase2_cpu_backend_routing.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align execution-request contract assertions with normalized feature-flag semantics</name>
  <files>tests/phase2_cpu_backend_routing.rs</files>
  <action>Update `execution_request_contract` so expected feature-flag ordering matches the canonical normalized ordering produced by runtime options, or compare order-insensitively if ordering is intentionally non-contractual. Keep the test focused on contract behavior, not insertion-order artifacts.</action>
  <verify>
    <automated>cargo test --workspace --test phase2_cpu_backend_routing execution_request_contract</automated>
  </verify>
  <done>The regression test no longer fails due to insertion-order drift and now validates the intended execution-request contract.</done>
</task>

<task type="auto">
  <name>Task 2: Keep normalization intent explicit at the request-construction boundary</name>
  <files>src/runtime/execution_plan.rs, tests/phase2_cpu_backend_routing.rs</files>
  <action>Ensure `ExecutionMemoryOptions::from` and related assertions make normalization intent explicit (through naming/comments/assertions) so future changes do not reintroduce ordering ambiguity between query options and execution requests.</action>
  <verify>
    <automated>cargo test --workspace --test phase2_cpu_backend_routing execution_request_contract</automated>
  </verify>
  <done>The contract between `WorkspaceQueryOptions` normalization and `ExecutionRequest` memory fields is explicit and regression-protected.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run the Phase 2 verification gate to close the gap</name>
  <files>tests/phase2_cpu_backend_routing.rs</files>
  <action>Run the same verification commands cited in `02-VERIFICATION.md` to confirm the only failing gate is resolved and no secondary regressions were introduced.</action>
  <verify>
    <automated>cargo test --workspace --test phase2_cpu_backend_routing --test phase2_raw_contracts --test phase2_raw_query_execute --test phase2_safe_evaluate_layout --test phase2_no_partial_write --test phase2_memory_limits --test phase2_allocation_failures --test phase2_cpu_execution_matrix --test phase2_safe_raw_equivalence --test phase2_oracle_tolerance --test phase2_raw_failure_semantics --test phase2_memory_contracts</automated>
  </verify>
  <done>Phase 2 verification gate is fully green, clearing the last blocker noted in the verification report.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo test --workspace --test phase2_cpu_backend_routing
- [ ] cargo test --workspace --test phase2_raw_contracts --test phase2_raw_query_execute --test phase2_safe_evaluate_layout --test phase2_no_partial_write --test phase2_memory_limits --test phase2_allocation_failures --test phase2_cpu_execution_matrix --test phase2_safe_raw_equivalence --test phase2_oracle_tolerance --test phase2_raw_failure_semantics --test phase2_memory_contracts
- [ ] cargo test --workspace --lib --quiet
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `execution_request_contract` no longer fails from feature-flag order mismatch
- Phase 2 verification status can be promoted from `gaps_found` once rerun evidence is recorded

</success_criteria>

<output>
After completion, create `.planning/phases/02-cpu-compatibility-execution/02-09-SUMMARY.md`
</output>
