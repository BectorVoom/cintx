---
phase: 02-cpu-compatibility-execution
plan: "07"
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-06
files_modified:
  - src/lib.rs
  - src/api/raw.rs
  - src/runtime/mod.rs
  - src/runtime/raw/query.rs
  - src/runtime/raw/evaluate.rs
  - tests/phase2_raw_query_execute.rs
autonomous: true
requirements:
  - RAW-02
must_haves:
  truths:
    - Raw workspace query and raw evaluate use one canonical planner/dispatch path.
    - Null-equivalent query semantics are preserved for libcint compatibility callers.
    - Query and execute memory/buffer contracts are checked consistently before dispatch.
  artifacts:
    - src/runtime/raw/query.rs exposes raw query functions that preserve sentinel semantics.
    - src/runtime/raw/evaluate.rs converts validated raw inputs into canonical execution requests and dispatches through CPU routing from 02-06.
    - src/api/raw.rs and src/runtime/mod.rs wire query/evaluate integration without bypassing validator boundaries from 02-02.
    - tests/phase2_raw_query_execute.rs locks RAW-02 query-then-execute behavior.
  key_links:
    - src/runtime/raw/query.rs produces workspace metadata consumed unchanged by src/runtime/raw/evaluate.rs.
    - src/runtime/raw/evaluate.rs dispatches through src/runtime/backend/cpu/router.rs and inherits stable-family coverage (including `3c1e` spinor) from 02-06.
---

<objective>
Integrate raw query and execute flows on top of validated contracts and CPU routing.

Purpose: Deliver RAW-02 behavior in a dedicated plan, separate from raw boundary validation scope.
Output: raw query/evaluate runtime modules, API threading, and query-then-execute contract tests.
</objective>

<execution_context>
@/home/chemtech/.codex/get-shit-done/workflows/execute-plan.md
@/home/chemtech/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cpu-compatibility-execution/02-RESEARCH.md
@.planning/phases/02-cpu-compatibility-execution/02-02-SUMMARY.md
@.planning/phases/02-cpu-compatibility-execution/02-06-SUMMARY.md
@src/api/raw.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement raw workspace query flow with null-equivalent semantics</name>
  <files>src/runtime/raw/query.rs, src/api/raw.rs</files>
  <action>Expose raw query functions that accept libcint-style optional output/cache semantics and return required workspace data for later execution. Preserve sentinel query behavior and diagnostics fields needed for compatibility debugging.</action>
  <verify>
    <automated>cargo test --workspace --test phase2_raw_query_execute raw_query_null_equivalent_contract</automated>
  </verify>
  <done>Raw query path supports sentinel/NULL-equivalent contract and returns deterministic workspace metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Implement raw evaluate path using shared execution request model</name>
  <files>src/runtime/raw/evaluate.rs, src/runtime/mod.rs, src/lib.rs, src/api/raw.rs</files>
  <action>Convert validated raw inputs into canonical execution requests and dispatch through CPU backend routing from 02-06. Enforce query-then-execute compatibility by verifying provided buffers/cache satisfy queried requirements before dispatch.</action>
  <verify>
    <automated>cargo test --workspace --test phase2_raw_query_execute raw_query_then_execute_success</automated>
  </verify>
  <done>Raw execute flow succeeds when using query-compliant buffers and fails with typed errors when contracts diverge.</done>
</task>

<task type="auto">
  <name>Task 3: Lock RAW-02 integration behavior with requirement tests</name>
  <files>tests/phase2_raw_query_execute.rs</files>
  <action>Add integration tests for query-only requests, query→execute handoff, and mismatch failure diagnostics to protect RAW-02 behavior from regressions.</action>
  <verify>
    <automated>cargo test --workspace --test phase2_raw_query_execute</automated>
  </verify>
  <done>RAW-02 query-then-execute semantics are test-locked for the raw compatibility surface.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo test --workspace --test phase2_raw_query_execute
- [ ] cargo test --workspace --lib --quiet
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- RAW-02 query-then-execute flow is deterministic and enforced across raw path
</success_criteria>

<output>
After completion, create `.planning/phases/02-cpu-compatibility-execution/02-07-SUMMARY.md`
</output>
