---
phase: 02-cpu-compatibility-execution
plan: "08"
type: execute
wave: 5
depends_on:
  - 02-04
  - 02-07
files_modified:
  - src/api/safe.rs
  - src/api/raw.rs
  - src/runtime/executor.rs
  - tests/phase2_memory_limits.rs
  - tests/phase2_allocation_failures.rs
autonomous: true
requirements:
  - MEM-01
  - MEM-02
  - RAW-02
must_haves:
  truths:
    - Safe and raw APIs thread memory options into the same runtime memory-policy path with no bypasses.
    - Allocation failures observed from API entrypoints are typed and diagnosable.
    - Raw query→execute memory contracts remain consistent when memory limits are applied.
  artifacts:
    - src/api/safe.rs and src/api/raw.rs pass memory options through shared executor contracts.
    - src/runtime/executor.rs exposes diagnostics-rich memory-policy outcomes consumable by both API layers.
    - tests/phase2_memory_limits.rs and tests/phase2_allocation_failures.rs lock API-level memory and allocation failure behavior.
  key_links:
    - src/api/raw.rs query/evaluate flow uses 02-07 semantics while inheriting 02-04 memory policy.
    - src/api/safe.rs uses the same executor memory hooks as raw path, preserving cross-surface consistency.
---

<objective>
Thread memory policy guarantees through safe/raw API surfaces and lock API-visible failure semantics.

Purpose: Finish MEM/RAW integration scope that was split out of 02-04 to keep memory core and API threading independently reviewable.
Output: API memory threading changes and allocation/memory contract regression tests.
</objective>

<execution_context>
@/home/chemtech/.codex/get-shit-done/workflows/execute-plan.md
@/home/chemtech/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cpu-compatibility-execution/02-04-SUMMARY.md
@.planning/phases/02-cpu-compatibility-execution/02-07-SUMMARY.md
@src/api/safe.rs
@src/api/raw.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread memory options through safe/raw API entrypoints</name>
  <files>src/api/safe.rs, src/api/raw.rs, src/runtime/executor.rs</files>
  <action>Ensure both API layers pass `memory_limit_bytes` and related policy options to shared runtime execution without ad hoc behavior differences.</action>
  <verify>
    <automated>cargo test --workspace --test phase2_memory_limits api_memory_policy_threading</automated>
  </verify>
  <done>Safe and raw APIs observe identical memory-policy decisions for equivalent requests.</done>
</task>

<task type="auto">
  <name>Task 2: Add API-visible allocation failure regression tests</name>
  <files>tests/phase2_allocation_failures.rs, src/runtime/executor.rs</files>
  <action>Add simulated allocation failure tests that assert typed `AllocationFailure` behavior and diagnostics completeness through both safe and raw entrypoints.</action>
  <verify>
    <automated>cargo test --workspace --test phase2_allocation_failures allocation_failures_are_typed</automated>
  </verify>
  <done>Allocation failures are exposed as typed errors and never bubble up as unhandled abort behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Lock RAW-02 + MEM contract integration under memory limits</name>
  <files>tests/phase2_memory_limits.rs, src/api/raw.rs, src/api/safe.rs</files>
  <action>Extend memory tests to validate raw query→execute compatibility and safe execute behavior under constrained limits, including explicit `MemoryLimitExceeded` infeasible cases.</action>
  <verify>
    <automated>cargo test --workspace --test phase2_memory_limits raw_query_execute_memory_contract</automated>
  </verify>
  <done>RAW-02 and MEM guarantees stay aligned under memory constraints across API surfaces.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo test --workspace --test phase2_memory_limits
- [ ] cargo test --workspace --test phase2_allocation_failures
- [ ] cargo test --workspace --lib --quiet
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- MEM-01 and MEM-02 guarantees are API-consistent and regression-protected
- RAW-02 query→execute behavior remains consistent under memory policy constraints
</success_criteria>

<output>
After completion, create `.planning/phases/02-cpu-compatibility-execution/02-08-SUMMARY.md`
</output>
