---
phase: 02-cpu-compatibility-execution
plan: "03"
type: execute
wave: 3
depends_on:
  - 02-06
files_modified:
  - src/lib.rs
  - src/api/safe.rs
  - src/runtime/mod.rs
  - src/runtime/planner.rs
  - src/runtime/layout.rs
  - src/runtime/output_writer.rs
  - src/runtime/executor.rs
  - tests/phase2_safe_evaluate_layout.rs
  - tests/phase2_no_partial_write.rs
autonomous: true
requirements:
  - SAFE-03
  - RAW-03
  - COMP-01
must_haves:
  truths:
    - Safe API exposes `evaluate_into` and `evaluate` with representation-correct layout and deterministic tensor shape semantics.
    - Planner-derived dims and output-size contracts are validated before backend writes, preventing silent truncation and partial-write success paths.
    - Stable-family safe execution routes through the same backend planner contract as raw execution to maintain compatibility parity.
  artifacts:
    - src/runtime/planner.rs and src/runtime/layout.rs compute contracted dims (including representation rules) and required element counts.
    - src/runtime/output_writer.rs enforces no-partial-write semantics via prevalidation and staged commit strategy.
    - src/api/safe.rs and src/runtime/executor.rs implement safe evaluate/evaluate_into on top of canonical execution requests.
    - tests/phase2_safe_evaluate_layout.rs and tests/phase2_no_partial_write.rs lock SAFE-03/RAW-03 behavior.
  key_links:
    - src/api/safe.rs evaluate paths call src/runtime/planner.rs and src/runtime/executor.rs without exposing raw dims in public signatures.
    - src/runtime/output_writer.rs is reused by raw and safe executor paths to keep RAW-03 semantics consistent.
    - src/runtime/executor.rs dispatches through the 02-06 CPU routing map, including the `3c1e` spinor envelope.
    - tests/phase2_no_partial_write.rs validates output buffers remain unchanged on dims/buffer contract failures.
---

<objective>
Implement safe execution surfaces and output-layout enforcement on the shared CPU backend contract.

Purpose: Deliver SAFE-03 user-facing evaluate APIs while establishing RAW-03 no-partial-write guarantees in shared executor/writer logic.
Output: safe evaluate/evaluate_into APIs, canonical planner/layout modules, and partial-write prevention tests.
</objective>

<execution_context>
@/home/chemtech/.codex/get-shit-done/workflows/execute-plan.md
@/home/chemtech/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cpu-compatibility-execution/02-RESEARCH.md
@.planning/phases/02-cpu-compatibility-execution/02-01-SUMMARY.md
@.planning/phases/02-cpu-compatibility-execution/02-06-SUMMARY.md
@src/api/safe.rs
@src/runtime/workspace_query.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical execution planner and representation-aware layout engine</name>
  <files>src/runtime/planner.rs, src/runtime/layout.rs, src/runtime/mod.rs</files>
  <action>Implement one planner path that derives natural dims and execution metadata for cart/sph/spinor representations using contracted-shell semantics needed for Phase-2 execution. Ensure planner output is reusable by both safe and raw APIs.</action>
  <verify>
    <automated>cargo test --workspace --test phase2_safe_evaluate_layout planner_representation_dims</automated>
  </verify>
  <done>Planner/layout modules produce deterministic dims and element counts for all supported Phase-2 family/representation pairs.</done>
</task>

<task type="auto">
  <name>Task 2: Implement safe evaluate_into and evaluate APIs on top of shared executor</name>
  <files>src/api/safe.rs, src/runtime/executor.rs, src/lib.rs, tests/phase2_safe_evaluate_layout.rs</files>
  <action>Add `evaluate_into` (caller-owned output) and `evaluate` (fallible convenience allocation) APIs that consume planner output and dispatch through CPU backend routing. Preserve typed error semantics and diagnostics fields from prior phases.</action>
  <verify>
    <automated>cargo test --workspace --test phase2_safe_evaluate_layout safe_evaluate_representation_layout</automated>
  </verify>
  <done>Safe API callers can execute supported stable-family requests and receive representation-correct output layouts.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce no-partial-write semantics for dims and buffer mismatch paths</name>
  <files>src/runtime/output_writer.rs, src/runtime/executor.rs, tests/phase2_no_partial_write.rs</files>
  <action>Introduce output writer logic that validates required elements/bytes before backend execution or commits via temporary buffers only after full success. Add tests proving mismatched dims/buffer conditions fail with typed errors and unchanged output bytes.</action>
  <verify>
    <automated>cargo test --workspace --test phase2_no_partial_write no_partial_write_on_contract_error</automated>
  </verify>
  <done>RAW-03 semantics hold for shared executor paths: no silent truncation and no partial writes on error.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo test --workspace --test phase2_safe_evaluate_layout
- [ ] cargo test --workspace --test phase2_no_partial_write
- [ ] cargo test --workspace --lib --quiet
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- SAFE-03 safe evaluate/evaluate_into APIs are implemented with representation-correct output contracts
- RAW-03 no-partial-write guarantees are enforced and test-locked in shared execution writer logic
- COMP-01 execution parity groundwork is established for stable-family safe paths
</success_criteria>

<output>
After completion, create `.planning/phases/02-cpu-compatibility-execution/02-03-SUMMARY.md`
</output>
