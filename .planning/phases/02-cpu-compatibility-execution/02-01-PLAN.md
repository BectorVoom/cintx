---
phase: 02-cpu-compatibility-execution
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - build.rs
  - src/lib.rs
  - src/runtime/mod.rs
  - src/runtime/execution_plan.rs
  - tests/phase2_cpu_backend_routing.rs
autonomous: true
requirements:
  - EXEC-01
must_haves:
  truths:
    - Vendored libcint symbols required by the Phase-2 stable-family scope are compiled and linked deterministically.
    - A single execution request model is defined once and shared by safe and raw runtime paths.
    - Backend capability contracts are codified up front, including a mandatory `3c1e` spinor execution envelope to be implemented in 02-06 (not an unsupported fallback).
  artifacts:
    - build.rs and Cargo.toml build/link the vendored libcint CPU sources required by the Phase-2 operator set.
    - src/runtime/execution_plan.rs defines canonical execution request/dispatch structs consumed by downstream planner/executor tasks.
    - src/runtime/mod.rs and src/lib.rs re-export the shared execution request surface.
    - tests/phase2_cpu_backend_routing.rs contains contract tests that pin the required stable-family envelope matrix expected from 02-06 routing.
  key_links:
    - src/runtime/execution_plan.rs is consumed by src/runtime/raw/* and src/runtime/executor.rs to keep dispatch behavior shared.
    - tests/phase2_cpu_backend_routing.rs defines the required matrix consumed by 02-06 router implementation.
---

<objective>
Establish deterministic CPU linkage and a single execution-request contract before backend routing and API-level execution work.

Purpose: Freeze EXEC-01 build/runtime foundations so downstream plans implement behavior, not competing dispatch models.
Output: libcint CPU linkage, canonical execution request model, and explicit routing-capability contract tests.
</objective>

<execution_context>
@/home/chemtech/.codex/get-shit-done/workflows/execute-plan.md
@/home/chemtech/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cpu-compatibility-execution/02-RESEARCH.md
@Cargo.toml
@src/runtime/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire deterministic CPU backend build and linkage for stable families</name>
  <files>Cargo.toml, build.rs</files>
  <action>Add build-dependencies and build script logic that compiles vendored libcint CPU sources used by Phase-2 stable-family operators. Ensure the crate links reproducibly in local and CI environments, and fail fast with typed build-time diagnostics when required source files are missing.</action>
  <verify>
    <automated>cargo test --workspace --test phase2_cpu_backend_routing cpu_backend_symbols_link</automated>
  </verify>
  <done>CPU baseline symbols for the targeted family/operator set are built and linkable from Rust test binaries.</done>
</task>

<task type="auto">
  <name>Task 2: Introduce canonical execution request model shared by safe/raw layers</name>
  <files>src/runtime/mod.rs, src/runtime/execution_plan.rs, src/lib.rs</files>
  <action>Define execution request/dispatch data structures that normalize family/operator/representation, shell tuple, dims, and memory options. Re-export this model through runtime/lib so all Phase-2 flows consume one planner path.</action>
  <verify>
    <automated>cargo test --workspace --test phase2_cpu_backend_routing execution_request_contract</automated>
  </verify>
  <done>Shared execution request structs exist and are consumable without safe/raw-specific routing models.</done>
</task>

<task type="auto">
  <name>Task 3: Codify stable-family routing obligations for downstream implementation</name>
  <files>tests/phase2_cpu_backend_routing.rs</files>
  <action>Add matrix contract tests that enumerate the Phase-2 required stable-family envelopes (`1e/2e/2c2e/3c1e/3c2e` x `cart/sph/spinor`) and mark them as mandatory pass cases for the router implementation delivered in 02-06.</action>
  <verify>
    <automated>cargo test --workspace --test phase2_cpu_backend_routing stable_family_required_matrix_contract</automated>
  </verify>
  <done>Required stable-family envelope obligations are executable and cannot be silently downgraded to unsupported without test failures.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo test --workspace --test phase2_cpu_backend_routing
- [ ] cargo test --workspace --lib --quiet
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- EXEC-01 CPU baseline linkage and shared execution request contract are implemented
- Stable-family routing obligations are explicitly locked for downstream router implementation
</success_criteria>

<output>
After completion, create `.planning/phases/02-cpu-compatibility-execution/02-01-SUMMARY.md`
</output>
