---
phase: 02-cpu-compatibility-execution
plan: "06"
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/runtime/backend/mod.rs
  - src/runtime/backend/cpu/mod.rs
  - src/runtime/backend/cpu/ffi.rs
  - src/runtime/backend/cpu/router.rs
  - src/runtime/backend/cpu/spinor_3c1e.rs
  - tests/phase2_cpu_backend_routing.rs
autonomous: true
requirements:
  - EXEC-01
  - COMP-01
must_haves:
  truths:
    - CPU backend routing is implemented for all Phase-2 stable-family envelopes across cart/sph/spinor.
    - The upstream `3c1e` spinor gap is resolved by an explicit adapter path and must not return unsupported for in-scope COMP-01 combinations.
    - Unsupported routing outcomes are limited to out-of-phase envelopes only and are returned as typed errors before unsafe calls.
  artifacts:
    - src/runtime/backend/cpu/ffi.rs and src/runtime/backend/cpu/router.rs define typed symbol bindings and family/operator/representation routing rules.
    - src/runtime/backend/cpu/spinor_3c1e.rs implements the dedicated `3c1e` spinor execution adapter used by router fallback.
    - tests/phase2_cpu_backend_routing.rs verifies full stable-family routing coverage and typed unsupported behavior only for out-of-scope paths.
  key_links:
    - src/runtime/backend/cpu/router.rs consumes src/runtime/execution_plan.rs request models from 02-01.
    - src/runtime/backend/cpu/spinor_3c1e.rs is wired as the mandatory route for `3c1e` spinor envelopes.
    - tests/phase2_cpu_backend_routing.rs enforces COMP-01 matrix completeness at dispatch level.
---

<objective>
Implement the CPU backend router and close the known `3c1e` spinor execution gap required by COMP-01.

Purpose: Resolve Phase-2 blocker by delivering implementation tasks for the previously unresolved stable-family envelope.
Output: typed FFI/router modules, explicit `3c1e` spinor adapter path, and routing matrix tests.
</objective>

<execution_context>
@/home/chemtech/.codex/get-shit-done/workflows/execute-plan.md
@/home/chemtech/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cpu-compatibility-execution/02-RESEARCH.md
@.planning/phases/02-cpu-compatibility-execution/02-01-SUMMARY.md
@src/runtime/backend/cpu/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement typed CPU FFI bindings and stable-family router map</name>
  <files>src/runtime/backend/mod.rs, src/runtime/backend/cpu/mod.rs, src/runtime/backend/cpu/ffi.rs, src/runtime/backend/cpu/router.rs</files>
  <action>Create narrow unsafe FFI bindings and a typed router keyed by integral family, operator kind, and representation. Route all in-scope stable-family envelopes and return typed unsupported errors only for out-of-phase combinations.</action>
  <verify>
    <automated>cargo test --workspace --test phase2_cpu_backend_routing backend_route_matrix</automated>
  </verify>
  <done>Backend router deterministically resolves all Phase-2 stable-family combinations without silent fallback behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Implement `3c1e` spinor adapter path and wire it as required route</name>
  <files>src/runtime/backend/cpu/spinor_3c1e.rs, src/runtime/backend/cpu/router.rs, src/runtime/backend/cpu/mod.rs</files>
  <action>Add a dedicated `3c1e` spinor execution adapter (for example via supported driver + explicit transform path) and integrate it into router dispatch so `3c1e` spinor is executable under COMP-01 instead of unsupported.</action>
  <verify>
    <automated>cargo test --workspace --test phase2_cpu_backend_routing three_c_one_e_spinor_supported</automated>
  </verify>
  <done>`3c1e` spinor requests in Phase-2 stable-family scope execute through a defined CPU route and never hit unsupported-path short-circuiting.</done>
</task>

<task type="auto">
  <name>Task 3: Expand routing tests to enforce full stable-family matrix</name>
  <files>tests/phase2_cpu_backend_routing.rs</files>
  <action>Extend matrix assertions to cover all required stable families and representations, including explicit `3c1e` spinor pass coverage and typed failures for out-of-scope envelopes only.</action>
  <verify>
    <automated>cargo test --workspace --test phase2_cpu_backend_routing stable_family_route_matrix_complete</automated>
  </verify>
  <done>Routing contract tests fail if any stable-family cart/sph/spinor envelope regresses to unsupported or unresolved state.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo test --workspace --test phase2_cpu_backend_routing
- [ ] cargo test --workspace --lib --quiet
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- EXEC-01 backend router is complete for Phase-2 stable-family scope
- COMP-01 blocker is resolved by explicit `3c1e` spinor implementation work (no re-scope required)
</success_criteria>

<output>
After completion, create `.planning/phases/02-cpu-compatibility-execution/02-06-SUMMARY.md`
</output>
