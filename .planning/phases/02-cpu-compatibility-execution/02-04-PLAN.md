---
phase: 02-cpu-compatibility-execution
plan: "04"
type: execute
wave: 4
depends_on:
  - 02-03
  - 02-07
files_modified:
  - src/runtime/mod.rs
  - src/runtime/memory/mod.rs
  - src/runtime/memory/allocator.rs
  - src/runtime/memory/chunking.rs
  - src/runtime/executor.rs
  - src/runtime/workspace_query.rs
  - tests/phase2_memory_limits.rs
autonomous: true
requirements:
  - MEM-01
  - MEM-02
must_haves:
  truths:
    - `memory_limit_bytes` is enforced by planner/executor with chunking when feasible and explicit `MemoryLimitExceeded` when infeasible.
    - Supported execution paths use fallible allocation boundaries and return typed allocation failures instead of process abort behavior.
    - Runtime-level memory accounting remains deterministic and consistent between query estimates and execution behavior.
  artifacts:
    - src/runtime/memory/allocator.rs defines centralized fallible allocation helpers used by execution and temporary-output paths.
    - src/runtime/memory/chunking.rs defines chunk-plan generation and infeasibility criteria for memory-limited requests.
    - src/runtime/executor.rs and src/runtime/workspace_query.rs apply unified memory accounting policy and surface typed memory errors.
    - tests/phase2_memory_limits.rs validates chunk-or-fail behavior at runtime layer boundaries.
  key_links:
    - src/runtime/memory/chunking.rs consumes planner outputs from 02-03 and emits chunk metadata used by src/runtime/executor.rs.
    - src/runtime/workspace_query.rs uses the same accounting contract as src/runtime/executor.rs to keep query/execute memory estimates aligned.
---

<objective>
Implement core memory policy guarantees inside shared runtime execution paths.

Purpose: Deliver MEM-01 and MEM-02 core runtime behavior before API-specific threading and regression test layering.
Output: memory subsystem modules, runtime integration, and core memory-limit tests.
</objective>

<execution_context>
@/home/chemtech/.codex/get-shit-done/workflows/execute-plan.md
@/home/chemtech/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cpu-compatibility-execution/02-RESEARCH.md
@.planning/phases/02-cpu-compatibility-execution/02-03-SUMMARY.md
@.planning/phases/02-cpu-compatibility-execution/02-07-SUMMARY.md
@src/runtime/executor.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add centralized fallible allocation policy for runtime buffers</name>
  <files>src/runtime/memory/mod.rs, src/runtime/memory/allocator.rs, src/runtime/mod.rs</files>
  <action>Implement allocator helpers that wrap all major runtime allocations in fallible APIs and map failures to `LibcintRsError::AllocationFailure`. Replace ad hoc allocation points in execution paths to remove unhandled abort-prone behavior.</action>
  <verify>
    <automated>cargo test --workspace --test phase2_memory_limits allocation_paths_use_fallible_policy</automated>
  </verify>
  <done>Execution-critical allocations return typed errors on failure and no supported path relies on unchecked allocation panics.</done>
</task>

<task type="auto">
  <name>Task 2: Implement chunk planner and memory-limit feasibility rules</name>
  <files>src/runtime/memory/chunking.rs, src/runtime/workspace_query.rs, src/runtime/executor.rs</files>
  <action>Define chunking strategy that splits feasible requests under `memory_limit_bytes` and formalize infeasible conditions that must return `MemoryLimitExceeded`. Keep accounting deterministic and consistent with workspace query estimates.</action>
  <verify>
    <automated>cargo test --workspace --test phase2_memory_limits chunk_or_memory_limit_exceeded</automated>
  </verify>
  <done>Memory-limited requests either execute through deterministic chunk plans or fail explicitly with required/limit byte context.</done>
</task>

<task type="auto">
  <name>Task 3: Add runtime-level MEM regression tests for chunking and explicit failure</name>
  <files>tests/phase2_memory_limits.rs, src/runtime/executor.rs, src/runtime/workspace_query.rs</files>
  <action>Add focused tests asserting memory-limit enforcement and chunking behavior at runtime boundaries independent of API-entry threading work moved to 02-08.</action>
  <verify>
    <automated>cargo test --workspace --test phase2_memory_limits</automated>
  </verify>
  <done>MEM-01/MEM-02 runtime guarantees are regression-protected before API-level integration changes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo test --workspace --test phase2_memory_limits
- [ ] cargo test --workspace --lib --quiet
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- MEM-01 chunk-or-explicit-failure behavior is implemented and deterministic in shared runtime core
- MEM-02 fallible allocation policy is enforced across runtime execution-critical paths
</success_criteria>

<output>
After completion, create `.planning/phases/02-cpu-compatibility-execution/02-04-SUMMARY.md`
</output>
