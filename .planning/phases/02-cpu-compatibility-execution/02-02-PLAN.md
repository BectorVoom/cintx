---
phase: 02-cpu-compatibility-execution
plan: "02"
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/api/raw.rs
  - src/runtime/raw/mod.rs
  - src/runtime/raw/views.rs
  - src/runtime/raw/validator.rs
  - tests/phase2_raw_contracts.rs
autonomous: true
requirements:
  - RAW-01
must_haves:
  truths:
    - Raw API callers can supply libcint-style `atm/bas/env`, `shls`, `dims`, `cache`, and `opt` through a strict validation boundary.
    - Invalid raw layout or pointer-offset contracts fail before planner/backend execution boundaries.
    - All unsafe pointer interpretation remains confined to raw view/validator modules.
  artifacts:
    - src/runtime/raw/views.rs defines typed borrowed raw views with slot-width and range guards.
    - src/runtime/raw/validator.rs validates shell arity, dims contracts, env offsets, and optional cache/opt invariants.
    - src/api/raw.rs exposes validation-backed raw entrypoints that return typed errors for invalid contracts.
    - tests/phase2_raw_contracts.rs locks RAW-01 validation behavior.
  key_links:
    - src/api/raw.rs delegates libcint-layout checks to src/runtime/raw/views.rs and src/runtime/raw/validator.rs.
    - tests/phase2_raw_contracts.rs asserts validation failures occur before runtime dispatch is attempted.
---

<objective>
Deliver the raw compatibility validation boundary before query/evaluate execution integration.

Purpose: Shrink plan scope and freeze RAW-01 layout contracts so later raw query/evaluate work can focus on execution semantics.
Output: raw view/validator modules, raw API validation boundary, and requirement-mapped raw contract tests.
</objective>

<execution_context>
@/home/chemtech/.codex/get-shit-done/workflows/execute-plan.md
@/home/chemtech/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cpu-compatibility-execution/02-RESEARCH.md
@.planning/phases/02-cpu-compatibility-execution/02-01-SUMMARY.md
@src/api/raw.rs
@src/runtime/validator.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build raw compatibility view types for libcint buffers</name>
  <files>src/runtime/raw/mod.rs, src/runtime/raw/views.rs</files>
  <action>Implement raw view wrappers for `atm`, `bas`, `env`, shell tuple, dims, cache, and opt that enforce slot counts, signed offset checks, and range checks before any pointer conversion.</action>
  <verify>
    <automated>cargo test --workspace --test phase2_raw_contracts raw_layout_slot_and_offset_checks</automated>
  </verify>
  <done>Malformed raw layout inputs are rejected deterministically with typed validation errors.</done>
</task>

<task type="auto">
  <name>Task 2: Implement RAW-01 validator boundary rules</name>
  <files>src/runtime/raw/validator.rs, src/runtime/raw/views.rs</files>
  <action>Validate shell tuple arity, dims length/value contracts, env offset bounds, and optional cache/opt invariants for raw request envelopes. Ensure all failures occur prior to execution-plan handoff.</action>
  <verify>
    <automated>cargo test --workspace --test phase2_raw_contracts raw_validation_matrix</automated>
  </verify>
  <done>RAW-01 contract checks are centralized and enforceable at one runtime boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Expose validation-backed raw API surface and tests</name>
  <files>src/api/raw.rs, tests/phase2_raw_contracts.rs</files>
  <action>Wire public raw API entrypoints to the new validator/view layer and add tests for valid and invalid libcint-style contract inputs.</action>
  <verify>
    <automated>cargo test --workspace --test phase2_raw_contracts</automated>
  </verify>
  <done>Raw public calls enforce the RAW-01 validation contract with typed diagnostics.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo test --workspace --test phase2_raw_contracts
- [ ] cargo test --workspace --lib --quiet
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- RAW-01 raw API contract and validator boundary are implemented and test-locked
</success_criteria>

<output>
After completion, create `.planning/phases/02-cpu-compatibility-execution/02-02-SUMMARY.md`
</output>
