---
phase: 27-spinor-derivative-transform-gap-b1
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 02a]
files_modified:
  - crates/cintx-cubecl/src/kernels/center_3c2e.rs
  - crates/cintx-cubecl/src/kernels/center_3c1e.rs
autonomous: true
requirements: [FND-04]
must_haves:
  truths:
    - "int3c2e_ip1_spinor (rank-3) evaluates via cart_to_spinor_sf_derivative_3c2e with a SPHERICAL aux-k axis (nsph(lk)) and is byte-identical to vendored libcint 6.1.3 at atol=1e-12 on the D-08 adversarial fixture (p×d×s kappa=0 buffer = 360, NOT 720)."
    - "int3c2e_ip2_spinor evaluates via the 3c2e derivative wrapper (ncomp=3, ket-center derivative ordering per the spike, spherical aux-k)."
    - "int3c1e_ip1/iprinv_spinor evaluate via the dedicated THIN SIBLING cart_to_spinor_sf_derivative_3c1e (spike D3 decision) applied to the host-side out_buf — the UnsupportedApi rejections in center_3c1e.rs (L1006-1010, L1130-1134) are GONE — and are byte-identical to vendored libcint 6.1.3 at atol=1e-12 on the D-08 fixture (iprinv with a NON-ZERO rinv origin), with a SPHERICAL aux-k axis."
    - "No launcher owns a KET->BRA transpose on the 3c2e/3c1e path — it lives in the Plan 02 wrapper (D-06)."
  artifacts:
    - path: "crates/cintx-cubecl/src/kernels/center_3c2e.rs"
      provides: "int3c2e ip1/ip2 spinor arms dispatch to the 3c2e derivative wrapper (spherical aux-k)"
      contains: "cart_to_spinor_sf_derivative_3c2e"
    - path: "crates/cintx-cubecl/src/kernels/center_3c1e.rs"
      provides: "int3c1e ip1/iprinv spinor arms dispatch to the THIN SIBLING cart_to_spinor_sf_derivative_3c1e (UnsupportedApi rejections removed)"
      contains: "cart_to_spinor_sf_derivative_3c1e"
  key_links:
    - from: "crates/cintx-cubecl/src/kernels/center_3c2e.rs"
      to: "cart_to_spinor_sf_derivative_3c2e"
      via: "int3c2e ip1/ip2 Spinor arms"
      pattern: "cart_to_spinor_sf_derivative_3c2e::<F>"
    - from: "crates/cintx-cubecl/src/kernels/center_3c1e.rs"
      to: "cart_to_spinor_sf_derivative_3c1e"
      via: "int3c1e ip1/iprinv Spinor arms (rejections removed)"
      pattern: "cart_to_spinor_sf_derivative_3c1e::<F>"
---

<objective>
Rewire the 3c2e-path spinor derivative launcher arms to call the Plan-02 derivative wrappers: `int3c2e_ip1_spinor`, `int3c2e_ip2_spinor` (in `center_3c2e.rs`) via `cart_to_spinor_sf_derivative_3c2e`, and `int3c1e_ip1/iprinv_spinor` (in `center_3c1e.rs`) via the dedicated THIN SIBLING `cart_to_spinor_sf_derivative_3c1e` (27-SPIKE-FINDINGS D3 decision). Lift the `UnsupportedApi` rejections for all four arms.

Purpose: Move every sf_3c2e-path family from `UnsupportedApi` to a real evaluation, satisfying ROADMAP success criterion 1+2 for the 3c2e path. This turns the Plan 01 sf_3c2e parity tests (including the two int3c1e tests) GREEN.

AUX-K CONTRACT (27-SPIKE-FINDINGS ⚠ CORRECTION NOTICE — AUTHORITATIVE): every arity-3 spinor
derivative family (int3c2e_ip1/ip2, int3c1e_ip1/iprinv) sizes its auxiliary-k axis SPHERICALLY as
`nsph(lk) = (2lk+1)*nctr_k` (libcint CINT3c2e_spinor_drv is_ssc=0, cint3c2e.c:631-636), NOT
`CINTcgto_spinor(4lk+2)`. Only bra i and ket j are spinor-sized (4l+2). The Plan-02 wrappers
already enforce this (they reuse the inner `cart_to_spinor_sf_3c2e` nsk=nsph(lk)); this plan must
NOT pass a spinor-sized k-axis or compute a 720-element buffer. The correct p×d×s kappa=0 nctr=1
rank-3 buffer is 360 (3·6·10·1·2). The Plan-02a-corrected oracle collectors size the vendor and
cintx buffers identically (spherical aux-k), so the parity comparison is well-formed.

Output: rewired center_3c2e.rs AND center_3c1e.rs; the int3c2e_ip1 and BOTH int3c1e D-09 vendor parity tests pass byte-identically at atol=1e-12 under both gate flags.

PRECONDITION (file ownership): The D-11 spike (Plan 01 Task 1) records the int3c1e launcher file path in 27-SPIKE-FINDINGS.md. It is `crates/cintx-cubecl/src/kernels/center_3c1e.rs` (reject sites: launch_center_3c1e_ip1 L1006-1010, launch_center_3c1e_iprinv L1130-1134). This file is a HARD deliverable of this plan (in files_modified), not an optional SUMMARY note.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-spinor-derivative-transform-gap-b1/27-CONTEXT.md
@.planning/phases/27-spinor-derivative-transform-gap-b1/27-RESEARCH.md
@.planning/phases/27-spinor-derivative-transform-gap-b1/27-PATTERNS.md
@.planning/phases/27-spinor-derivative-transform-gap-b1/27-SPIKE-FINDINGS.md

<interfaces>
Wrappers (from Plan 02, crates/cintx-cubecl/src/transform/c2spinor.rs):
  pub fn cart_to_spinor_sf_derivative_3c2e<F: CintFloat>(
      staging: &mut [F], cart: &[f64], ncomp: usize,
      li: u8, kappa_i: i16, lj: u8, kappa_j: i16, lk: u8, nctr_i: usize, nctr_j: usize,
  ) -> Result<(), cintxRsError>   // aux-k axis SPHERICAL nsph(lk); reuses inner sf_3c2e nsk=nsph(lk)
  pub fn cart_to_spinor_sf_derivative_3c1e<F: CintFloat>(
      staging: &mut [F], cart: &[f64], ncomp: usize,
      li: u8, kappa_i: i16, lj: u8, kappa_j: i16, lk: u8, nctr_i: usize, nctr_j: usize,
  ) -> Result<(), cintxRsError>   // THIN SIBLING (spike D3); same per-(comp,k) [ket][bra] spherical-aux-k fold for the int3c1e host out_buf

center_3c2e.rs sites (grep-confirmed):
  L27   use crate::transform::c2spinor::cart_to_spinor_sf_3c2e;   // EXTEND with cart_to_spinor_sf_derivative_3c2e
  L2394-2396  int3c2e_ip1 Spinor early-guard -> UnsupportedApi (R5/T-21-06-04); unreachable arm @ L2572
  L2632-2634  int3c2e_ip2 Spinor early-guard -> UnsupportedApi (D-06); unreachable arm @ L2810
  L2868-2870  int3c2e Hessian Spinor guard (NOT in scope — leave as-is); unreachable arm @ L3044
  L3276-3284  scalar-spinor 3c2e analog:
      cart_to_spinor_sf_3c2e::<F>(staging, &cart_out, li_in, kappa_i, lj_in, kappa_j, lk)?
      (NOTE: this scalar analog already uses the spherical-aux-k inner transform — the derivative
       wrapper is the same shape with ncomp=3 prepended and the nctr counts appended.)

center_3c1e.rs sites (CONFIRMED — grep'd):
  use crate::transform::c2s::{cart_to_sph_3c1e, ncart, nsph};   // L51 — EXTEND with cart_to_spinor_sf_derivative_3c1e
  launch_center_3c1e_ip1     Spinor reject -> UnsupportedApi @ L1006-1010 ("spinor int3c1e_ip1 gradient")
  launch_center_3c1e_iprinv  Spinor reject -> UnsupportedApi @ L1130-1134 ("spinor int3c1e_iprinv gradient")
  Both are HOST-side. The host scatter builds a component-leading cart/sph derivative buffer
  (`out_buf`, out_total = 3*(nctr_i*nblk_i)*(nctr_j*nblk_j)*(nctr_k*nblk_k), L1064-1110) where
  nblk_k = nsph (spheric) for the spheric forms — i.e. the aux-k axis of the host buffer is ALREADY
  spherical. The F::from_f64_lossy copy into staging is at L1112-1115. The iprinv path reads
  env[PTR_RINV_ORIG] via plan.operator_env_params.rinv_orig (L1156). Apply the spinor fold to that
  host cart buffer with the SPHERICAL aux-k.

int3c1e launcher file ownership: CONFIRMED center_3c1e.rs (in files_modified).

component_rank: int3c2e_ip1=3, int3c2e_ip2=3, int3c1e_ip1=3, int3c1e_iprinv=3 (verified in lock).
SCOPE FENCE: do NOT touch int3c2e Hessian/ipip spinor arms (out of scope; arity-4 int2e_ip* and ECP are deferred per D-03/D-04).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire int3c2e_ip1/ip2 spinor arms to cart_to_spinor_sf_derivative_3c2e (spherical aux-k)</name>
  <files>crates/cintx-cubecl/src/kernels/center_3c2e.rs</files>
  <read_first>
    - crates/cintx-cubecl/src/kernels/center_3c2e.rs (L27 import; ip1 guard L2394-2396 + arm L2572; ip2 guard L2632-2634 + arm L2810; scalar-spinor analog L3276-3284)
    - crates/cintx-cubecl/src/transform/c2spinor.rs (cart_to_spinor_sf_derivative_3c2e signature from Plan 02 — aux-k SPHERICAL)
    - .planning/phases/27-spinor-derivative-transform-gap-b1/27-SPIKE-FINDINGS.md (⚠ CORRECTION NOTICE: spherical aux-k; 3c2e ip1 vs ip2 ordering; transpose granularity)
    - crates/cintx-oracle/tests/spinor_deriv_parity.rs (the test_int3c2e_ip1_spinor_adversarial_parity, with Plan-02a-corrected spherical aux-k collectors)
  </read_first>
  <action>
    Extend the import at L27 to include `cart_to_spinor_sf_derivative_3c2e`.

    Remove the int3c2e_ip1 Spinor early-guard (L2394-2396) and the int3c2e_ip2 Spinor early-guard (L2632-2634). In the corresponding unreachable arms (L2572 for ip1, L2810 for ip2), replace `unreachable!("spinor int3c2e_ipN rejected above")` with:
      `cart_to_spinor_sf_derivative_3c2e::<F>(staging, &cart_out, 3, li_in, kappa_i, lj_in, kappa_j, lk, n_ctr_i, n_ctr_j)?;`
    mirroring the scalar-spinor analog at L3276-3284 (same `(li_in, kappa_i, lj_in, kappa_j, lk)` argument shape, now with the derivative component count 3 and the contraction counts). The wrapper sizes the aux-k axis SPHERICALLY (nsph(lk)) — do NOT pre-size staging with a spinor-sized k-axis. Use the ip1 vs ip2 cart buffer/ordering exactly as 27-SPIKE-FINDINGS.md recorded (ip2 is the ket/aux-center derivative — same buffer SHAPE as ip1 per the spike; the launcher picks the gradient gout nabla1i vs nabla1l, as the cart/sph paths already do).

    Do NOT touch the int3c2e Hessian / ipip Spinor guard at L2868-2870 (out of scope). Do NOT re-introduce any KET→BRA transpose in this file (D-06: transpose lives in the wrapper).
  </action>
  <verify>
    <automated>cargo build -p cintx-cubecl --features cpu && CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test spinor_deriv_parity test_int3c2e_ip1_spinor_adversarial_parity</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'cart_to_spinor_sf_derivative_3c2e::<F>' crates/cintx-cubecl/src/kernels/center_3c2e.rs` returns >= 2 (ip1 + ip2).
    - `grep -c 'unreachable!("spinor int3c2e_ip1 rejected above")\|unreachable!("spinor int3c2e_ip2 rejected above")' crates/cintx-cubecl/src/kernels/center_3c2e.rs` returns 0.
    - `grep -c 'unreachable!("spinor int3c2e Hessian rejected above")' crates/cintx-cubecl/src/kernels/center_3c2e.rs` returns 1 (Hessian arm untouched — scope fence).
    - No spinor-sized aux-k pre-sizing leaks into this file: `grep -c 'CINTcgto_spinor.*lk\|4 \* lk + 2\|4\*lk+2' crates/cintx-cubecl/src/kernels/center_3c2e.rs` returns 0 (the wrapper owns aux-k sizing as nsph(lk)).
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test spinor_deriv_parity test_int3c2e_ip1_spinor_adversarial_parity` exits 0 (byte-identical at atol=1e-12 on the 360-element spherical-aux-k buffer).
  </acceptance_criteria>
  <done>int3c2e_ip1/ip2 spinor arms dispatch to cart_to_spinor_sf_derivative_3c2e with spherical aux-k; the int3c2e_ip1 vendor parity test is byte-identical at atol=1e-12 (360-element buffer); the Hessian arm is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Wire int3c1e_ip1/iprinv spinor arms in center_3c1e.rs via the THIN SIBLING (spherical aux-k)</name>
  <files>crates/cintx-cubecl/src/kernels/center_3c1e.rs</files>
  <read_first>
    - .planning/phases/27-spinor-derivative-transform-gap-b1/27-SPIKE-FINDINGS.md (D3: int3c1e uses a THIN SIBLING, NOT the shared _3c2e wrapper; launcher file = center_3c1e.rs; ⚠ CORRECTION NOTICE: aux-k SPHERICAL — AUTHORITATIVE)
    - crates/cintx-cubecl/src/kernels/center_3c1e.rs (int3c1e_ip1 Spinor reject L1006-1010; int3c1e_iprinv Spinor reject L1130-1134; host cart/sph scatter into out_buf L1064-1110, nblk_k = nsph already spherical; F::from_f64_lossy copy L1112-1115; iprinv rinv origin read L1156; import L51)
    - crates/cintx-cubecl/src/transform/c2spinor.rs (the THIN SIBLING cart_to_spinor_sf_derivative_3c1e from Plan 02)
    - crates/cintx-oracle/tests/spinor_deriv_parity.rs (test_int3c1e_ip1_spinor_adversarial_parity, test_int3c1e_iprinv_spinor_adversarial_parity — Plan-02a-corrected spherical aux-k collectors)
  </read_first>
  <action>
    PRECONDITION: 27-SPIKE-FINDINGS.md D3 records that the int3c1e launchers live in THIS file (center_3c1e.rs) and decides the THIN SIBLING (not the shared _3c2e wrapper). If the spike findings contradict this file path, STOP and flag — do not guess.

    Extend the import (L51 area) to bring in `cart_to_spinor_sf_derivative_3c1e` from `crate::transform::c2spinor`.

    For BOTH `launch_center_3c1e_ip1` and `launch_center_3c1e_iprinv`: remove the `Representation::Spinor` early-guard that returns `UnsupportedApi` (L1006-1010 for ip1, L1130-1134 for iprinv). These launchers already build a host-side component-leading cart/sph derivative buffer (`out_buf`, L1064-1110) whose aux-k axis is ALREADY spherical (nblk_k = nsph). When `plan.representation == Representation::Spinor`, instead of the plain `F::from_f64_lossy` copy into `staging` (L1112-1115), apply the THIN SIBLING fold to `out_buf`:
      `cart_to_spinor_sf_derivative_3c1e::<F>(staging, &out_buf, 3, li, kappa_i, lj, kappa_j, lk, n_ctr_i, n_ctr_j)?;`
    The sibling sizes the aux-k axis SPHERICALLY (nsph(lk)) — matching the host out_buf's existing spherical k-axis. Keep the existing cart/sph copy path intact for the non-spinor representations (branch on `plan.representation`). ncomp=3 for both int3c1e_ip1 and int3c1e_iprinv (verified in lock).

    For int3c1e_iprinv: the rinv center origin is already read at L1156 (plan.operator_env_params.rinv_orig). Do NOT short-circuit it — the D-08 fixture sets a NON-ZERO rinv origin, so a zero-origin shortcut would diverge from vendor (Phase 24-03 / 25-03 landmine). The kappa_i/kappa_j for the spinor fold come from the shell spinor kappa (mirror how center_3c2e.rs L3276-3284 derives them for the scalar-spinor path).
  </action>
  <verify>
    <automated>cargo build -p cintx-cubecl --features cpu && CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test spinor_deriv_parity test_int3c1e_ip1_spinor_adversarial_parity test_int3c1e_iprinv_spinor_adversarial_parity</automated>
  </verify>
  <acceptance_criteria>
    - The int3c1e_ip1/iprinv spinor UnsupportedApi rejections are GONE: `grep -c '"spinor int3c1e_ip1 gradient"\|"spinor int3c1e_iprinv gradient"' crates/cintx-cubecl/src/kernels/center_3c1e.rs` returns 0.
    - The arms dispatch to the THIN SIBLING: `grep -c 'cart_to_spinor_sf_derivative_3c1e::<F>' crates/cintx-cubecl/src/kernels/center_3c1e.rs` returns >= 2 (ip1 + iprinv).
    - No spinor-sized aux-k pre-sizing leaks into this file: `grep -c '4 \* lk + 2\|4\*lk+2' crates/cintx-cubecl/src/kernels/center_3c1e.rs` returns 0 (aux-k stays spherical nsph(lk)).
    - `cargo build -p cintx-cubecl --features cpu` exits 0.
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test spinor_deriv_parity test_int3c1e_ip1_spinor_adversarial_parity test_int3c1e_iprinv_spinor_adversarial_parity` exits 0 — BOTH int3c1e tests GREEN, byte-identical at atol=1e-12 (spherical-aux-k buffer), under both gate flags. The iprinv test exercises a NON-ZERO rinv origin.
  </acceptance_criteria>
  <done>int3c1e_ip1/iprinv spinor arms in center_3c1e.rs dispatch to the THIN SIBLING cart_to_spinor_sf_derivative_3c1e (spherical aux-k); the UnsupportedApi rejections are removed; iprinv reads the non-zero rinv origin; BOTH int3c1e vendor parity tests are byte-identical at atol=1e-12 under both gate flags; crate builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device 3c2e derivative buffer → host wrapper | The 3c2e derivative cart block layout (`[comp][k][ket][bra]`) was the MEDIUM-confidence unknown; the spike pinned it (and the aux-k spherical correction) before this rewire depends on it. |
| host 3c1e out_buf → spinor fold | The int3c1e launchers are host-side; the THIN SIBLING fold is applied to the component-leading host out_buf (aux-k already spherical), not a device buffer. |
| iprinv env origin → integral | A zero rinv origin makes the integral trivially-passing; the D-08 fixture forces a non-zero origin (Phase 24/25 landmine). |
| spike CORRECTION NOTICE → aux-k sizing | The aux-k axis is SPHERICAL nsph(lk); the launchers must pass that through to the wrapper (which enforces it). A spinor-sized k-axis (720) would never match the 360-element vendor buffer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-10 | Tampering | 3c2e ip2 vs ip1 center-derivative ordering | mitigate | Task 1 applies the ip1/ip2 ordering exactly as 27-SPIKE-FINDINGS.md recorded; int3c2e_ip1 vendor parity at atol=1e-12 on the non-square spherical-aux-k fixture surfaces any ordering error. |
| T-27-11 | Tampering | trivially-passing iprinv (zero origin) | mitigate | int3c1e_iprinv reads env[PTR_RINV_ORIG] (L1156); the D-08 fixture sets a non-zero rinv origin so a zero-origin shortcut would diverge from vendor; test_int3c1e_iprinv_spinor_adversarial_parity must pass under both flags. |
| T-27-12 | Tampering | scope creep into Hessian/ipip 3c2e spinor | mitigate | The Hessian guard at L2868-2870 is explicitly left untouched (grep asserts the unreachable string survives); arity-4/ECP stay deferred (D-03/D-04). |
| T-27-16 | Repudiation | int3c1e flipped without a real parity comparison | mitigate | Task 2 wires BOTH test_int3c1e_ip1/iprinv vendor parity tests as GREEN acceptance under both gate flags — the D-10 oracle_covered assertion (Plan 05) does NOT substitute for this byte-identity comparison. |
| T-27-22 | Tampering | aux-k sized as spinor (720) on the 3c2e/3c1e launch path | mitigate | The Plan-02 wrappers own aux-k sizing as nsph(lk); acceptance greps that no 4lk+2 / spinor-k pre-sizing leaks into either launcher file; parity compares against the Plan-02a-corrected 360-element vendor buffer. |
</threat_model>

<verification>
- `cargo build -p cintx-cubecl --features cpu` green.
- int3c2e_ip1 vendor parity GREEN under both gate flags at atol=1e-12 (360-element spherical-aux-k buffer).
- BOTH int3c1e_ip1/iprinv vendor parity GREEN under both gate flags at atol=1e-12 (iprinv with non-zero rinv origin, spherical aux-k).
- Hessian/ipip 3c2e spinor guard untouched (scope fence held).
</verification>

<success_criteria>
- int3c2e_ip1/ip2 spinor families evaluate via cart_to_spinor_sf_derivative_3c2e; int3c1e_ip1/iprinv via the THIN SIBLING cart_to_spinor_sf_derivative_3c1e — all with SPHERICAL aux-k.
- int3c2e_ip1 AND both int3c1e families byte-identical to libcint 6.1.3 at atol=1e-12 on the D-08 adversarial fixture (360-element buffer) under both gate flags.
- D-06 satisfied on the 3c2e/3c1e path; deferred families and out-of-scope arms untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/27-spinor-derivative-transform-gap-b1/27-04-SUMMARY.md`
</output>
</content>
