---
phase: 27-spinor-derivative-transform-gap-b1
plan: 04
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - crates/cintx-cubecl/src/kernels/center_3c2e.rs
autonomous: true
requirements: [FND-04]
must_haves:
  truths:
    - "int3c2e_ip1_spinor (rank-3) evaluates via cart_to_spinor_sf_derivative_3c2e and is byte-identical to vendored libcint 6.1.3 at atol=1e-12 on the D-08 adversarial fixture."
    - "int3c2e_ip2_spinor evaluates via the 3c2e derivative wrapper (ncomp=3, ket-center derivative ordering per the spike)."
    - "int3c1e_ip1/iprinv_spinor evaluate via the spike-determined path (shared 3c2e wrapper or the dedicated sibling)."
    - "No launcher owns a KET->BRA transpose on the 3c2e path — it lives in the Plan 02 wrapper (D-06)."
  artifacts:
    - path: "crates/cintx-cubecl/src/kernels/center_3c2e.rs"
      provides: "int3c2e ip1/ip2 (+ int3c1e ip1/iprinv) spinor arms dispatch to the 3c2e derivative wrapper"
      contains: "cart_to_spinor_sf_derivative_3c2e"
  key_links:
    - from: "crates/cintx-cubecl/src/kernels/center_3c2e.rs"
      to: "cart_to_spinor_sf_derivative_3c2e"
      via: "int3c2e ip1/ip2 Spinor arms"
      pattern: "cart_to_spinor_sf_derivative_3c2e::<F>"
---

<objective>
Rewire the 3c2e-path spinor derivative launcher arms in `center_3c2e.rs` to call `cart_to_spinor_sf_derivative_3c2e` (from Plan 02): `int3c2e_ip1_spinor`, `int3c2e_ip2_spinor`, and `int3c1e_ip1/iprinv_spinor` (the latter via the shared wrapper OR the dedicated sibling, exactly as 27-SPIKE-FINDINGS.md decided). Lift the `UnsupportedApi` rejections for these arms.

Purpose: Move every sf_3c2e-path family from `UnsupportedApi` to a real evaluation, satisfying ROADMAP success criterion 1+2 for the 3c2e path. This turns the Plan 01 sf_3c2e parity test GREEN.

Output: rewired center_3c2e.rs; the int3c2e_ip1 D-09 vendor parity test passes.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-spinor-derivative-transform-gap-b1/27-CONTEXT.md
@.planning/phases/27-spinor-derivative-transform-gap-b1/27-RESEARCH.md
@.planning/phases/27-spinor-derivative-transform-gap-b1/27-PATTERNS.md
@.planning/phases/27-spinor-derivative-transform-gap-b1/27-SPIKE-FINDINGS.md

<interfaces>
Wrapper (from Plan 02, crates/cintx-cubecl/src/transform/c2spinor.rs):
  pub fn cart_to_spinor_sf_derivative_3c2e<F: CintFloat>(
      staging: &mut [F], cart: &[f64], ncomp: usize,
      li: u8, kappa_i: i16, lj: u8, kappa_j: i16, lk: u8, nctr_i: usize, nctr_j: usize,
  ) -> Result<(), cintxRsError>
  // plus a `cart_to_spinor_sf_derivative_3c1e` sibling IFF 27-SPIKE-FINDINGS.md mandated one

center_3c2e.rs sites (grep-confirmed):
  L27   use crate::transform::c2spinor::cart_to_spinor_sf_3c2e;   // EXTEND with the derivative wrapper(s)
  L2394-2396  int3c2e_ip1 Spinor early-guard -> UnsupportedApi (R5/T-21-06-04); unreachable arm @ L2572
  L2632-2634  int3c2e_ip2 Spinor early-guard -> UnsupportedApi (D-06); unreachable arm @ L2810
  L2868-2870  int3c2e Hessian Spinor guard (NOT in scope — leave as-is); unreachable arm @ L3044
  L3276-3284  scalar-spinor 3c2e analog:
      cart_to_spinor_sf_3c2e::<F>(staging, &cart_out, li_in, kappa_i, lj_in, kappa_j, lk)?

int3c1e_ip1/iprinv launcher: locate the int3c1e_ip1/iprinv Spinor arms (search center_3c1e or center_3c2e per the spike's finding on where they live) and apply the shared-or-sibling decision.

component_rank: int3c2e_ip1=3, int3c2e_ip2=3, int3c1e_ip1=3, int3c1e_iprinv=3 (verified in lock).
SCOPE FENCE: do NOT touch int3c2e Hessian/ipip spinor arms (out of scope; arity-4 int2e_ip* and ECP are deferred per D-03/D-04).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire int3c2e_ip1/ip2 spinor arms to cart_to_spinor_sf_derivative_3c2e</name>
  <files>crates/cintx-cubecl/src/kernels/center_3c2e.rs</files>
  <read_first>
    - crates/cintx-cubecl/src/kernels/center_3c2e.rs (L27 import; ip1 guard L2394-2396 + arm L2572; ip2 guard L2632-2634 + arm L2810; scalar-spinor analog L3276-3284)
    - crates/cintx-cubecl/src/transform/c2spinor.rs (cart_to_spinor_sf_derivative_3c2e signature from Plan 02)
    - .planning/phases/27-spinor-derivative-transform-gap-b1/27-SPIKE-FINDINGS.md (3c2e ip1 vs ip2 ordering; transpose granularity)
    - crates/cintx-oracle/tests/spinor_deriv_parity.rs (the RED test_int3c2e_ip1_spinor_adversarial_parity)
  </read_first>
  <action>
    Extend the import at L27 to include `cart_to_spinor_sf_derivative_3c2e` (and the `cart_to_spinor_sf_derivative_3c1e` sibling iff the spike mandated one).

    Remove the int3c2e_ip1 Spinor early-guard (L2394-2396) and the int3c2e_ip2 Spinor early-guard (L2632-2634). In the corresponding unreachable arms (L2572 for ip1, L2810 for ip2), replace `unreachable!("spinor int3c2e_ipN rejected above")` with:
      `cart_to_spinor_sf_derivative_3c2e::<F>(staging, &cart_out, 3, li_in, kappa_i, lj_in, kappa_j, lk, n_ctr_i, n_ctr_j)?;`
    mirroring the scalar-spinor analog at L3276-3284 (same `(li_in, kappa_i, lj_in, kappa_j, lk)` argument shape, now with the derivative component count 3 and the contraction counts). Use the ip1 vs ip2 cart buffer/ordering exactly as 27-SPIKE-FINDINGS.md recorded (ip2 is the ket/aux-center derivative — if the spike found ip2 needs a different center-derivative ordering than ip1, apply it).

    Do NOT touch the int3c2e Hessian / ipip Spinor guard at L2868-2870 (out of scope). Do NOT re-introduce any KET→BRA transpose in this file (D-06: transpose lives in the wrapper).
  </action>
  <verify>
    <automated>cargo build -p cintx-cubecl --features cpu && CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test spinor_deriv_parity test_int3c2e_ip1_spinor_adversarial_parity</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'cart_to_spinor_sf_derivative_3c2e::<F>' crates/cintx-cubecl/src/kernels/center_3c2e.rs` returns >= 2 (ip1 + ip2).
    - `grep -c 'unreachable!("spinor int3c2e_ip1 rejected above")\|unreachable!("spinor int3c2e_ip2 rejected above")' crates/cintx-cubecl/src/kernels/center_3c2e.rs` returns 0.
    - `grep -c 'unreachable!("spinor int3c2e Hessian rejected above")' crates/cintx-cubecl/src/kernels/center_3c2e.rs` returns 1 (Hessian arm untouched — scope fence).
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test spinor_deriv_parity test_int3c2e_ip1_spinor_adversarial_parity` exits 0 (byte-identical at atol=1e-12).
  </acceptance_criteria>
  <done>int3c2e_ip1/ip2 spinor arms dispatch to cart_to_spinor_sf_derivative_3c2e; the int3c2e_ip1 vendor parity test is byte-identical at atol=1e-12; the Hessian arm is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Wire int3c1e_ip1/iprinv spinor arms (shared wrapper or sibling per the spike)</name>
  <files>crates/cintx-cubecl/src/kernels/center_3c2e.rs</files>
  <read_first>
    - .planning/phases/27-spinor-derivative-transform-gap-b1/27-SPIKE-FINDINGS.md (the int3c1e shared-vs-sibling decision — AUTHORITATIVE)
    - crates/cintx-cubecl/src/kernels/center_3c2e.rs (the int3c1e_ip1/iprinv Spinor arms, wherever the spike found they live)
    - crates/cintx-cubecl/src/transform/c2spinor.rs (the 3c2e wrapper and/or the 3c1e sibling from Plan 02)
  </read_first>
  <action>
    Locate the int3c1e_ip1 and int3c1e_iprinv Spinor derivative arms (the spike findings state whether they are in center_3c2e.rs or a sibling launcher file — if they are NOT in center_3c2e.rs, note that in the SUMMARY and the file ownership for this task should be corrected, but per RESEARCH.md they fold through the 3c2e path). Remove their `UnsupportedApi` rejections and dispatch to whichever transform 27-SPIKE-FINDINGS.md decided:
      - If SHARED: `cart_to_spinor_sf_derivative_3c2e::<F>(staging, &cart_out, 3, li, kappa_i, lj, kappa_j, lk, n_ctr_i, n_ctr_j)?;`
      - If SIBLING: `cart_to_spinor_sf_derivative_3c1e::<F>(...)` with the sibling's signature.
    ncomp=3 for both int3c1e_ip1 and int3c1e_iprinv (verified in lock). For int3c1e_iprinv, ensure the rinv center origin is read correctly (env[PTR_RINV_ORIG]) — the D-08 fixture sets a non-zero rinv origin, so a zero-origin shortcut would be trivially-passing (Phase 24-03 / 25-03 landmine).
  </action>
  <verify>
    <automated>cargo build -p cintx-cubecl --features cpu && cargo test -p cintx-cubecl --lib kernels::center_3c2e</automated>
  </verify>
  <acceptance_criteria>
    - The int3c1e_ip1/iprinv spinor arms no longer return UnsupportedApi: `grep -c 'spinor int3c1e_ip\|spinor int3c1e_iprinv' crates/cintx-cubecl/src/kernels/center_3c2e.rs` shows no UnsupportedApi-rejection strings remain for these arms (0 reject strings).
    - The arms dispatch to the spike-decided transform: `grep -c 'cart_to_spinor_sf_derivative_3c2e::<F>\|cart_to_spinor_sf_derivative_3c1e::<F>' crates/cintx-cubecl/src/kernels/center_3c2e.rs` increased relative to Task 1.
    - `cargo build -p cintx-cubecl --features cpu` exits 0.
    - 27-04-SUMMARY notes the int3c1e shared-vs-sibling decision actually implemented and (if the arms live in a different launcher file) flags the file-ownership correction.
  </acceptance_criteria>
  <done>int3c1e_ip1/iprinv spinor arms dispatch to the spike-decided 3c2e wrapper or 3c1e sibling; iprinv reads the non-zero rinv origin; crate builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device 3c2e derivative buffer → host wrapper | The 3c2e derivative cart block layout (`[comp][k][ket][bra]` vs `[comp][k][bra][ket]`) is the MEDIUM-confidence unknown; the spike pinned it before this rewire depends on it. |
| iprinv env origin → integral | A zero rinv origin makes the integral trivially-passing; the D-08 fixture forces a non-zero origin (Phase 24/25 landmine). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-10 | Tampering | 3c2e ip2 vs ip1 center-derivative ordering | mitigate | Task 1 applies the ip1/ip2 ordering exactly as 27-SPIKE-FINDINGS.md recorded; int3c2e_ip1 vendor parity at atol=1e-12 on the non-square fixture surfaces any ordering error. |
| T-27-11 | Tampering | trivially-passing iprinv (zero origin) | mitigate | int3c1e_iprinv reads env[PTR_RINV_ORIG]; the D-08 fixture sets a non-zero rinv origin so a zero-origin shortcut would diverge from vendor. |
| T-27-12 | Tampering | scope creep into Hessian/ipip 3c2e spinor | mitigate | The Hessian guard at L2868-2870 is explicitly left untouched (grep asserts the unreachable string survives); arity-4/ECP stay deferred (D-03/D-04). |
</threat_model>

<verification>
- `cargo build -p cintx-cubecl --features cpu` green.
- int3c2e_ip1 vendor parity GREEN under both gate flags at atol=1e-12.
- Hessian/ipip 3c2e spinor guard untouched (scope fence held).
</verification>

<success_criteria>
- int3c2e_ip1/ip2 + int3c1e_ip1/iprinv spinor families evaluate via the spike-decided 3c2e wrapper/sibling.
- int3c2e_ip1 byte-identical to libcint 6.1.3 at atol=1e-12 on the D-08 adversarial fixture.
- D-06 satisfied on the 3c2e path; deferred families and out-of-scope arms untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/27-spinor-derivative-transform-gap-b1/27-04-SUMMARY.md`
</output>
