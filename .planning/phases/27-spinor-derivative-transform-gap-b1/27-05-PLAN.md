---
phase: 27-spinor-derivative-transform-gap-b1
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 02a, 03, 04]
files_modified:
  - crates/cintx-ops/generated/compiled_manifest.lock.json
  - xtask/src/oracle_covered_update.rs
  - crates/cintx-oracle/tests/spinor_deriv_parity.rs
autonomous: true
requirements: [FND-04]
must_haves:
  truths:
    - "The 20 vendor-backed sf-derivative spinor families read oracle_covered=true in compiled_manifest.lock.json: the 18 arity-2 1e ip families that fold through cart_to_spinor_sf_2d (D-01 — ranks 3/9/27/81) plus int3c2e_ip1_spinor and int3c2e_ip2_spinor that fold through cart_to_spinor_sf_3c2e (revised D-02)."
    - "The byte-identity backing is a REAL vendor parity test per transform-path × rank-tier that ran GREEN under both gate flags in Plan 03/04: int1e_ipovlp (sf_2d rank-3), int1e_ipovlpip (sf_2d rank-9), int1e_ipipipiprinv (sf_2d rank-81), int3c2e_ip1 (sf_3c2e rank-3, against the Plan-02a-corrected 360-element SPHERICAL-aux-k buffer). The remaining flipped families share these byte-identity-proven inner transforms via the manifest/launcher path; int3c2e_ip2 shares the sf_3c2e wrapper int3c2e_ip1 proved. The Plan-05 flip RECORDS coverage Plans 03/04 already proved — it does not itself verify."
    - "The 4 D-12 vendor-stub arms (int2c2e_ip1_spinor, int2c2e_ip2_spinor, int3c1e_ip1_spinor, int3c1e_iprinv_spinor) remain oracle_covered=false — libcint 6.1.3 ships these as unimplemented stubs (int2c2e_ip1/ip2_spinor return 0; CINT3c1e_spinor_drv exit(1)s), so NO vendor byte-identity reference is achievable; their parity tests stay #[ignore]'d and must NOT count as coverage. FD verification is a deferred follow-up."
    - "The D-03 deferred families (int2e_ip1/ip2/ipip1/ipvip1/ip1ip2/ipip1ipip2_spinor) and the D-04 deferred family (int1e_ecp_iprinv_spinor) also remain oracle_covered=false."
    - "The D-10 no-silent-skip assertion (test_no_silent_skip) passes and FAILS (not skips) if: the vendor arm compiled out, OR the fixture is skipped, OR any of the 20 flipped families reads oracle_covered=false at runtime, OR any of the 4 D-12 arms / D-03 / D-04 deferred families reads oracle_covered=true. It reads MANIFEST_ENTRIES (cintx_ops::generated, regenerated from the lock by build.rs) at runtime."
    - "manifest-audit is green; no capi enum / legacy cint* wrapper surface was added (new-family surface policy)."
  artifacts:
    - path: "crates/cintx-ops/generated/compiled_manifest.lock.json"
      provides: "oracle_covered=true for the 20 vendor-backed sf-derivative spinor families; oracle_covered=false retained for the 4 D-12 vendor-stub arms + D-03/D-04 deferrals"
      contains: "oracle_covered"
    - path: "crates/cintx-oracle/tests/spinor_deriv_parity.rs"
      provides: "completed D-10 no-silent-skip assertion: asserts 20 flipped families oracle_covered=true AND the 4 D-12 + D-03/D-04 deferred families oracle_covered=false, AND the vendor arm ran (not skipped)"
      contains: "oracle_covered"
    - path: "xtask/src/oracle_covered_update.rs"
      provides: "updated deferral note recording D-12: sf-derivative spinor families now covered EXCEPT the 4 vendor-stub arms which stay false pending FD; D-03/D-04 skipped-fixture guard retained"
      contains: "skipped"
  key_links:
    - from: "compiled_manifest.lock.json"
      to: "crates/cintx-ops/src/generated/api_manifest.rs (MANIFEST_ENTRIES)"
      via: "crates/cintx-ops/build.rs regenerates oracle_covered from the lock at build time"
      pattern: "oracle_covered"
    - from: "compiled_manifest.lock.json"
      to: "xtask manifest-audit"
      via: "oracle_covered flag auto-syncs both audit sides from the lock"
      pattern: "oracle_covered"
    - from: "crates/cintx-oracle/tests/spinor_deriv_parity.rs"
      to: "cintx_ops::generated::MANIFEST_ENTRIES"
      via: "test_no_silent_skip reads .oracle_covered per .symbol_name at runtime"
      pattern: "oracle_covered"
---

<objective>
Re-scoped coverage-flip plan (re-planned 2026-05-31 at the Wave-3 pause). Flip `oracle_covered=true` in `compiled_manifest.lock.json` for ONLY the 20 vendor-backed sf-derivative spinor families (D-01 sf_2d 1e families + revised D-02 int3c2e_ip1/ip2), complete the D-10 no-silent-skip assertion in the parity test so it defends both the no-skip property AND the exact flipped/deferred split, update the deferral note in `oracle_covered_update.rs` to record D-12, and prove `manifest-audit` green. The 4 D-12 vendor-stub arms (int2c2e_ip1/ip2_spinor, int3c1e_ip1/iprinv_spinor) and the D-03/D-04 deferrals STAY `false`.

Purpose: This records the coverage that Plans 03/04 already proved with real vendor byte-identity parity tests (against the Plan-02a-corrected 360-element SPHERICAL aux-k buffer for the 3c2e path). GROUND TRUTH established at the pause checkpoint: libcint 6.1.3 ships `int2c2e_ip1/ip2_spinor` as stubs that `return 0` and `CINT3c1e_spinor_drv` as a stub that `exit(1)`s — so byte-identity is impossible for those 4 arms and the original 24-family flip is disproven. Honest-scope decision (D-12): flip only the families with a working vendor reference now; FD verification of the 4 stub arms is a deferred follow-up. Satisfies ROADMAP §"Phase 27" success criterion 3.

This plan changes NO sizing/dims and adds NO capi/legacy-wrapper surface (new-family surface policy). It flips coverage flags, completes one assertion, and updates one note. The flip propagates into the runtime `MANIFEST_ENTRIES` automatically because `crates/cintx-ops/build.rs` regenerates `api_manifest.rs` from the lock at build time.

Output: re-scoped flipped lock (20 true, 4 D-12 + D-03/D-04 false); completed D-10 assertion; updated D-12 deferral note; full vendor parity suite + manifest-audit green.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-spinor-derivative-transform-gap-b1/27-CONTEXT.md
@.planning/phases/27-spinor-derivative-transform-gap-b1/27-RESEARCH.md
@.planning/phases/27-spinor-derivative-transform-gap-b1/27-PATTERNS.md

<interfaces>
FLIP set — set oracle_covered false -> true (EXACTLY these 20; all currently false in the lock,
all component_rank already correct — verified 2026-05-31):
  sf_2d / 1e rank-3:  int1e_ipovlp_spinor, int1e_ipkin_spinor, int1e_ipnuc_spinor, int1e_iprinv_spinor
  sf_2d / 1e rank-9:  int1e_ipovlpip_spinor, int1e_ipkinip_spinor, int1e_ipnucip_spinor,
                      int1e_ipipovlp_spinor, int1e_ipipnuc_spinor, int1e_ipipkin_spinor, int1e_ipiprinv_spinor
  sf_2d / 1e rank-27: int1e_ipipnucip_spinor, int1e_ipiprinvip_spinor, int1e_ipipipnuc_spinor, int1e_ipipiprinv_spinor
  sf_2d / 1e rank-81: int1e_ipipipiprinv_spinor, int1e_ipiprinvipip_spinor, int1e_ipipiprinvip_spinor
  sf_3c2e rank-3:     int3c2e_ip1_spinor, int3c2e_ip2_spinor

  EXACT component_rank values in the lock (verified 2026-05-31, DO NOT change — already correct):
    rank "3":  int1e_ipovlp_spinor, int1e_ipkin_spinor, int1e_ipnuc_spinor, int1e_iprinv_spinor,
               int3c2e_ip1_spinor, int3c2e_ip2_spinor
    rank "9":  int1e_ipovlpip_spinor, int1e_ipkinip_spinor, int1e_ipnucip_spinor,
               int1e_ipipovlp_spinor, int1e_ipipnuc_spinor, int1e_ipipkin_spinor, int1e_ipiprinv_spinor
    rank "27": int1e_ipipnucip_spinor, int1e_ipiprinvip_spinor, int1e_ipipipnuc_spinor, int1e_ipipiprinv_spinor
    rank "81": int1e_ipipipiprinv_spinor, int1e_ipiprinvipip_spinor, int1e_ipipiprinvip_spinor

MUST STAY oracle_covered=false (11 families — DO NOT flip):
  D-12 vendor-stub arms (4): int2c2e_ip1_spinor, int2c2e_ip2_spinor, int3c1e_ip1_spinor, int3c1e_iprinv_spinor
  D-03 arity-4 int2e (6):    int2e_ip1_spinor, int2e_ip2_spinor, int2e_ipip1_spinor,
                             int2e_ipvip1_spinor, int2e_ip1ip2_spinor, int2e_ipip1ipip2_spinor
  D-04 ECP (1):              int1e_ecp_iprinv_spinor

Backing vendor parity tests (already GREEN under both flags before this plan runs — depends_on [03,04]):
  test_int1e_ipovlp_spinor_adversarial_parity      (sf_2d rank-3)
  test_int1e_ipovlpip_spinor_adversarial_parity     (sf_2d rank-9)
  test_int1e_ipipipiprinv_spinor_adversarial_parity (sf_2d rank-81)
  test_int3c2e_ip1_spinor_adversarial_parity        (sf_3c2e rank-3, 360-element spherical aux-k)
  test_orientation_negative_control                 (i-fastest orientation pin)
The 4 D-12 arms' parity tests (test_int2c2e_ip1_spinor_*, test_int3c1e_ip1_spinor_*,
test_int3c1e_iprinv_spinor_*) are #[ignore]'d with documented vendor-stub reasons — they MUST NOT
be counted as coverage and MUST stay ignored. int3c2e_ip2_spinor has no dedicated test; it shares
the sf_3c2e wrapper int3c2e_ip1 proved (manifest/launcher path).

Lock entry shape (alphabetical fields; component_rank precedes id.symbol, oracle_covered follows it):
  { ..., "component_rank": "3", ..., "id": { "symbol": "int1e_ipovlp_spinor", ... },
    "complex_output": true, "oracle_covered": false, ... }   -> set oracle_covered true

Runtime read path for the D-10 assertion:
  cintx_ops::generated::MANIFEST_ENTRIES : &[ManifestEntry]
  each entry: .symbol_name (&'static str), .oracle_covered (bool)
  build.rs regenerates these from the lock, so after the flip+rebuild the runtime flags match the lock.

Audit auto-syncs: both audit sides derive from the lock; no separate fixtures list to edit.
xtask is a STANDALONE cargo project (own Cargo.lock), NOT a workspace member — run from xtask/ dir:
  cd xtask && cargo run -- manifest-audit   (Phase 25-03 landmine: `cargo run -p xtask` from root fails)

oracle_covered_update.rs note (L31-39): the `if fixture.skipped { continue; }` guard — KEEP it
  (still correct for the 4 D-12 arms + D-03/D-04 deferred families); UPDATE the comment to record D-12.
  Do NOT stamp any fixture that remains `skipped`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip oracle_covered=true for the 20 vendor-backed families only + update D-12 deferral note + manifest-audit green</name>
  <files>crates/cintx-ops/generated/compiled_manifest.lock.json, xtask/src/oracle_covered_update.rs</files>
  <read_first>
    - crates/cintx-ops/generated/compiled_manifest.lock.json (the 20 flip entries + the 11 deferred entries; verify each component_rank against the table in the interfaces block)
    - xtask/src/oracle_covered_update.rs (the deferral note + the `if fixture.skipped { continue; }` guard, L31-39)
    - .planning/phases/27-spinor-derivative-transform-gap-b1/27-CONTEXT.md (D-01 flip scope; revised D-02; D-12 vendor-stub deferral; D-03/D-04 deferrals)
  </read_first>
  <action>
    In compiled_manifest.lock.json, set `"oracle_covered": false` -> `"oracle_covered": true` for EXACTLY these 20 symbols and NO others:
      int1e_ipovlp_spinor, int1e_ipkin_spinor, int1e_ipnuc_spinor, int1e_iprinv_spinor,
      int1e_ipovlpip_spinor, int1e_ipkinip_spinor, int1e_ipnucip_spinor,
      int1e_ipipovlp_spinor, int1e_ipipnuc_spinor, int1e_ipipkin_spinor, int1e_ipiprinv_spinor,
      int1e_ipipnucip_spinor, int1e_ipiprinvip_spinor, int1e_ipipipnuc_spinor, int1e_ipipiprinv_spinor,
      int1e_ipipipiprinv_spinor, int1e_ipiprinvipip_spinor, int1e_ipipiprinvip_spinor,
      int3c2e_ip1_spinor, int3c2e_ip2_spinor.

    BEFORE flipping each, confirm its `component_rank` already equals the value in the interfaces-block table
    (rank 3 / 9 / 27 / 81). All 20 are already correct per the 2026-05-31 verification; if any does NOT match,
    STOP and report — do not silently edit a rank (a wrong rank truncates trailing components, the known landmine).
    NOTE: the entry fields are alphabetical, so `component_rank` appears a few lines ABOVE the `"symbol"` line and
    `oracle_covered` a few lines BELOW it within the SAME entry object (between `id` and `profiles`). Flip the
    `oracle_covered` that belongs to the same entry as the target symbol, not a neighbor's.

    Do NOT flip the 11 deferred families. They MUST stay `"oracle_covered": false`:
      D-12 (4): int2c2e_ip1_spinor, int2c2e_ip2_spinor, int3c1e_ip1_spinor, int3c1e_iprinv_spinor
      D-03 (6): int2e_ip1_spinor, int2e_ip2_spinor, int2e_ipip1_spinor, int2e_ipvip1_spinor,
                int2e_ip1ip2_spinor, int2e_ipip1ipip2_spinor
      D-04 (1): int1e_ecp_iprinv_spinor

    In xtask/src/oracle_covered_update.rs, KEEP the `if fixture.skipped { continue; }` guard (currently L35-37) —
    it is still correct for the deferred families — and UPDATE the comment block above it (currently L31-37) to
    record D-12. The new comment must state, in substance: sf-derivative SPINOR families are now oracle_covered
    via Plan 27 vendor byte-identity parity, EXCEPT the 4 vendor-stub arms (int2c2e_ip1/ip2_spinor and
    int3c1e_ip1/iprinv_spinor) which stay false because libcint 6.1.3 ships them as stubs (return 0 / exit(1)) —
    no byte-identity reference is achievable; FD verification is a deferred follow-up. Also retain the existing
    note that the D-03 (int2e_ip* spinor) and D-04 (int1e_ecp_iprinv_spinor) families remain intentionally
    skipped/deferred. Do NOT add any logic that stamps a `skipped` fixture as covered.

    Then run the manifest audit from the standalone xtask dir (Phase 25-03 landmine: `cargo run -p xtask` from
    root fails because xtask is not a workspace member). Building the workspace regenerates api_manifest.rs
    (MANIFEST_ENTRIES) from the flipped lock via crates/cintx-ops/build.rs.
  </action>
  <verify>
    <automated>cd xtask &amp;&amp; cargo run -- manifest-audit</automated>
  </verify>
  <acceptance_criteria>
    - All 20 flip families read oracle_covered=true. Verify via JSON parse, e.g.:
      `python3 -c "import json; d=json.load(open('crates/cintx-ops/generated/compiled_manifest.lock.json')); m={e['id']['symbol']:e['oracle_covered'] for e in d['entries']}; flip=['int1e_ipovlp_spinor','int1e_ipkin_spinor','int1e_ipnuc_spinor','int1e_iprinv_spinor','int1e_ipovlpip_spinor','int1e_ipkinip_spinor','int1e_ipnucip_spinor','int1e_ipipovlp_spinor','int1e_ipipnuc_spinor','int1e_ipipkin_spinor','int1e_ipiprinv_spinor','int1e_ipipnucip_spinor','int1e_ipiprinvip_spinor','int1e_ipipipnuc_spinor','int1e_ipipiprinv_spinor','int1e_ipipipiprinv_spinor','int1e_ipiprinvipip_spinor','int1e_ipipiprinvip_spinor','int3c2e_ip1_spinor','int3c2e_ip2_spinor']; assert all(m[s] for s in flip), [s for s in flip if not m[s]]; print('all 20 flipped true')"`
      prints `all 20 flipped true`.
    - The 11 deferred families still read oracle_covered=false. Same parser idiom with
      `defer=['int2c2e_ip1_spinor','int2c2e_ip2_spinor','int3c1e_ip1_spinor','int3c1e_iprinv_spinor','int2e_ip1_spinor','int2e_ip2_spinor','int2e_ipip1_spinor','int2e_ipvip1_spinor','int2e_ip1ip2_spinor','int2e_ipip1ipip2_spinor','int1e_ecp_iprinv_spinor']; assert not any(m[s] for s in defer)` prints/asserts cleanly (none true).
    - `cd xtask &amp;&amp; cargo run -- manifest-audit` exits 0 (green).
    - `grep -c 'fixture.skipped' xtask/src/oracle_covered_update.rs` >= 1 (skipped guard retained).
    - The updated note mentions the vendor stubs: `grep -Ec 'int2c2e_ip1|int3c1e|stub|exit\(1\)|return 0|D-12' xtask/src/oracle_covered_update.rs` >= 1.
    - No capi enum / legacy `cint*` wrapper added: `git diff --name-only` lists NO file under `crates/cintx-capi/` and NOT `crates/cintx-compat/src/legacy.rs`.
  </acceptance_criteria>
  <done>Exactly 20 vendor-backed families flipped oracle_covered=true with verified-correct component_rank; the 11 deferred families (4 D-12 stub arms + 6 D-03 int2e + 1 D-04 ECP) remain false; D-12 deferral note updated; skipped guard retained; manifest-audit green; no capi/legacy surface added.</done>
</task>

<task type="auto">
  <name>Task 2: Complete the D-10 no-silent-skip assertion (flipped=true / deferred=false split) + run the full vendor parity suite</name>
  <files>crates/cintx-oracle/tests/spinor_deriv_parity.rs</files>
  <read_first>
    - crates/cintx-oracle/tests/spinor_deriv_parity.rs (the existing test_no_silent_skip, L437-455 — currently only asserts the vendor+cintx arms produced nonzero output; the #[ignore]'d D-12 arm tests at L290-364)
    - crates/cintx-ops/generated/compiled_manifest.lock.json (the now-flipped flags this test will assert against)
    - crates/cintx-ops/src/generated/mod.rs (confirms `pub use api_manifest::{MANIFEST_ENTRIES, ...}` — the runtime read path: `cintx_ops::generated::MANIFEST_ENTRIES`, fields `.symbol_name` + `.oracle_covered`)
    - crates/cintx-oracle/src/compare.rs (the `skipped` semantics, L91-94 / L1108-1130 — a skipped fixture carries no parity obligation and must never be stamped)
  </read_first>
  <action>
    Extend `test_no_silent_skip` (D-10) in spinor_deriv_parity.rs. Keep its existing assertions that the vendor
    arm AND the cintx arm of a supported family (int1e_ipovlp_spinor) produced nonzero output (proving the vendor
    link is live and the fixture is NOT skipped). ADD a runtime manifest-coverage assertion using
    `cintx_ops::generated::MANIFEST_ENTRIES`:

      1. Define a `const FLIPPED: &[&str]` array holding the SAME 20 symbols flipped in Task 1, and a
         `const DEFERRED: &[&str]` array holding the 11 families that must stay false (the 4 D-12 arms +
         6 D-03 int2e + 1 D-04 ECP). Use the exact symbol strings from the interfaces block.
      2. For each symbol in FLIPPED, look it up in MANIFEST_ENTRIES by `.symbol_name` and assert
         `.oracle_covered == true`; panic with the symbol name if it is false or missing.
      3. For each symbol in DEFERRED, look it up and assert `.oracle_covered == false`; panic with the symbol
         name if it was accidentally flipped true.

    The test MUST fail (not skip) if: (a) the vendor arm compiled out, OR (b) the fixture is skipped (both already
    covered by the nonzero-output assertions), OR (c) any FLIPPED family reads oracle_covered=false, OR (d) any
    DEFERRED family reads oracle_covered=true. Keep the existing `#[cfg(has_vendor_libcint)] #[cfg(feature="cpu")]`
    gate so it runs in the same vendor-gated context as the parity tests (the MANIFEST_ENTRIES read works in any
    build, but gating it here ties the coverage claim to a live vendor run).

    Do NOT un-ignore the 4 D-12 arm tests (test_int2c2e_ip1_spinor_*, test_int3c1e_ip1_spinor_*,
    test_int3c1e_iprinv_spinor_*) — they stay #[ignore]'d; running them against a vendor build returns 0 /
    exit(1)s the harness. Add NO dedicated parity test for int3c2e_ip2_spinor (it shares the sf_3c2e wrapper that
    int3c2e_ip1's test already proves).

    Then run the FULL vendor parity suite under BOTH gate flags and confirm it is green and ran N>0 tests. The
    parity tests compare against the Plan-02a-corrected 360-element SPHERICAL aux-k buffers (not the disproven 720).
  </action>
  <verify>
    <automated>CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test spinor_deriv_parity 2>&1 | tee /tmp/27-05-parity.log; grep -E 'running [1-9][0-9]* tests' /tmp/27-05-parity.log</automated>
  </verify>
  <acceptance_criteria>
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test spinor_deriv_parity` exits 0 (all non-ignored tests green: the 4 GREEN parity tests + negative control + test_no_silent_skip; the 4 D-12 arm tests reported as `ignored`).
    - /tmp/27-05-parity.log matches `running [1-9]` (NOT `running 0 tests`) — the vendor arm actually executed.
    - test_no_silent_skip references both the flipped-true and deferred-false sets and reads the flag:
      `grep -c 'oracle_covered' crates/cintx-oracle/tests/spinor_deriv_parity.rs` >= 1 AND
      `grep -c 'MANIFEST_ENTRIES' crates/cintx-oracle/tests/spinor_deriv_parity.rs` >= 1 AND
      `grep -Ec 'FLIPPED|DEFERRED' crates/cintx-oracle/tests/spinor_deriv_parity.rs` >= 2.
    - The 4 D-12 arm tests remain ignored: `grep -c '#\[ignore' crates/cintx-oracle/tests/spinor_deriv_parity.rs` >= 3 (the three ignored vendor-stub parity tests).
    - test_no_silent_skip body references int2c2e_ip1_spinor and int3c1e_ip1_spinor (the deferred set) AND int1e_ipovlp_spinor / int3c2e_ip1_spinor (the flipped set) — verifiable by reading the FLIPPED/DEFERRED const arrays.
  </acceptance_criteria>
  <done>test_no_silent_skip asserts N>0 ran + fixture not skipped + all 20 FLIPPED families read oracle_covered=true + all 11 DEFERRED families (incl. the 4 D-12 stub arms) read oracle_covered=false; the 4 D-12 arm tests stay #[ignore]'d; the full vendor parity suite is green under both flags.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lock oracle_covered flag → CI coverage claim | Flipping the flag IS the act of claiming verification; test_no_silent_skip is the boundary that ensures the claim is backed by a real (non-skipped) vendor run and matches the exact flipped/deferred split. |
| Plan 03/04 parity result → Plan 05 flip | The flip records coverage Plans 03/04's byte-identity tests proved (against the corrected 360-element buffer); the flip is NOT itself a verification, so depends_on [03, 04] is load-bearing. |
| libcint vendor stub → D-12 arms | int2c2e_ip1/ip2_spinor return 0 and CINT3c1e_spinor_drv exit(1)s; no byte-identity reference exists, so these arms must NOT be stamped covered. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-13 | Repudiation | stamping oracle_covered on a skipped fixture (false verification claim) | mitigate | test_no_silent_skip fails (not skips) if the vendor arm compiled out or the fixture is skipped; oracle_covered_update.rs retains the `if fixture.skipped { continue; }` guard so deferred families are never stamped (threat T-21-08-02). |
| T-27-14 | Tampering | accidentally flipping a D-12 / D-03 / D-04 deferred family to true | mitigate | Task 1 acceptance asserts all 11 deferred families read false; test_no_silent_skip's DEFERRED loop fails if any reads true. |
| T-27-15 | Tampering | wrong component_rank silently truncating a flipped family | mitigate | Task 1 verifies each component_rank against the rank-tier table BEFORE flipping and STOPS on any mismatch; the rank-81 parity (Plan 03) and rank-3 3c2e parity (Plan 04, spherical aux-k) exercise the extremes. |
| T-27-18 | Repudiation | claiming int2c2e/int3c1e byte-identity coverage that libcint cannot provide (the disproven original D-02) | mitigate | D-12 keeps those 4 arms oracle_covered=false; their parity tests stay #[ignore]'d and are NOT counted; the updated oracle_covered_update.rs note records the vendor-stub gap and the deferred FD follow-up. |
| T-27-19 | Spoofing | a flipped family with no backing vendor reference masquerading as covered | mitigate | Only the 20 families whose transform path (sf_2d / sf_3c2e) has a GREEN per-rank-tier vendor byte-identity test (ipovlp/ipovlpip/ipipipiprinv/int3c2e_ip1) are flipped; int3c2e_ip2 shares the int3c2e_ip1-proven sf_3c2e wrapper. |
</threat_model>

<verification>
- `cd xtask &amp;&amp; cargo run -- manifest-audit` green.
- `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test spinor_deriv_parity` fully green, `running N>0 tests`, 4 D-12 arm tests reported `ignored`.
- 20 vendor-backed families oracle_covered=true; 11 deferred families (4 D-12 + 6 D-03 + 1 D-04) oracle_covered=false.
- test_no_silent_skip asserts the exact flipped-true / deferred-false split AND the no-skip property.
- No capi/legacy-wrapper surface added.
</verification>

<success_criteria>
- The 20 vendor-backed sf-derivative spinor families flipped oracle_covered=true; the 4 D-12 vendor-stub arms + D-03/D-04 deferrals untouched (false).
- No truth or coverage claim references int3c1e_ip1/iprinv or int2c2e_ip1/ip2 as vendor-byte-identity backed (they are vendor stubs).
- The flips are backed by Plans 03/04's vendor byte-identity parity tests per transform-path × rank-tier (no coverage stamped without a real comparison, against the corrected 360-element buffer for the 3c2e path).
- D-10 no-silent-skip assertion complete and passing under both gate flags, defending both the no-skip property and the flipped/deferred split.
- oracle_covered_update.rs deferral note records D-12; manifest-audit green; no capi/legacy surface added.
- ROADMAP §"Phase 27" success criterion 3 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/27-spinor-derivative-transform-gap-b1/27-05-SUMMARY.md`
</output>
