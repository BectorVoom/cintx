---
phase: 27-spinor-derivative-transform-gap-b1
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 02a, 03, 04]
files_modified:
  - crates/cintx-ops/generated/compiled_manifest.lock.json
  - xtask/src/oracle_covered_update.rs
  - crates/cintx-oracle/tests/spinor_deriv_parity.rs
autonomous: true
requirements: [FND-04]
must_haves:
  truths:
    - "Every in-scope sf-derivative spinor family (all arity-2 1e ip families + int2c2e_ip1/ip2 + int3c2e_ip1/ip2 + int3c1e_ip1/iprinv) reads oracle_covered=true in compiled_manifest.lock.json."
    - "Each int3c1e_ip1/iprinv flip is backed by a real vendor byte-identity parity test that ran GREEN under both gate flags in Plan 04 (test_int3c1e_ip1/iprinv_spinor_adversarial_parity, comparing against the Plan-02a-corrected 360-element spherical-aux-k buffer) — the flip here records coverage that Plan 04 already proved; the D-10 oracle_covered assertion does NOT substitute for that comparison."
    - "The deferred families (int2e_ip*_spinor per D-03, int1e_ecp_iprinv_spinor per D-04) remain oracle_covered=false."
    - "The D-10 no-silent-skip assertion passes: the parity binary runs N>0 tests under both flags, the fixture is NOT skipped, and the flipped families read oracle_covered=true."
    - "manifest-audit is green; no capi/legacy-wrapper surface was added (new-family surface policy)."
  artifacts:
    - path: "crates/cintx-ops/generated/compiled_manifest.lock.json"
      provides: "oracle_covered=true for in-scope sf-derivative spinor families"
      contains: "oracle_covered"
    - path: "crates/cintx-oracle/tests/spinor_deriv_parity.rs"
      provides: "completed D-10 no-silent-skip assertion (now asserts flipped families oracle_covered=true)"
      contains: "test_no_silent_skip"
    - path: "xtask/src/oracle_covered_update.rs"
      provides: "updated R5/D-03 deferral note (sf-derivative spinor now covered; skipped-fixture guard retained for D-03/D-04)"
      contains: "skipped"
  key_links:
    - from: "compiled_manifest.lock.json"
      to: "xtask manifest-audit"
      via: "oracle_covered flag auto-syncs both audit sides"
      pattern: "oracle_covered"
    - from: "crates/cintx-oracle/tests/spinor_deriv_parity.rs"
      to: "compiled_manifest.lock.json"
      via: "test_no_silent_skip reads oracle_covered for flipped families"
      pattern: "oracle_covered"
---

<objective>
Flip `oracle_covered=true` in `compiled_manifest.lock.json` for every in-scope sf-derivative spinor family (D-01/D-02), complete the D-10 no-silent-skip assertion in the parity test, update the R5/D-03 deferral note in `oracle_covered_update.rs`, and prove `manifest-audit` green. Leave the D-03/D-04 deferred families `false`.

Purpose: This closes the gap between "works" (Plans 03/04 made the families byte-identical and proved it with vendor parity tests against the Plan-02a-corrected spherical-aux-k collectors, INCLUDING test_int3c1e_ip1/iprinv_spinor_adversarial_parity) and "covered" (the manifest records them as verified). The flip records coverage that Plan 04's parity tests already proved — it does NOT itself verify the families; the D-10 guard ensures coverage is never stamped on a silently-skipped fixture (the known double-gate false-pass landmine). Satisfies ROADMAP success criterion 3.

Aux-k note: this plan changes NO sizing/dims — it only flips coverage flags and completes the no-silent-skip assertion. The ⚠ CORRECTION NOTICE (spherical aux-k) affects only the upstream wrappers/launchers/collectors (Plans 02/02a/04); here it matters only insofar as the parity tests this plan re-runs are the corrected ones (they were made GREEN in Plan 04 against the 360-element buffer).

Output: flipped lock; completed D-10 assertion; updated deferral note; full vendor suite + manifest-audit green.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-spinor-derivative-transform-gap-b1/27-CONTEXT.md
@.planning/phases/27-spinor-derivative-transform-gap-b1/27-RESEARCH.md
@.planning/phases/27-spinor-derivative-transform-gap-b1/27-PATTERNS.md

<interfaces>
In-scope families to flip oracle_covered false->true (FULL list, verified in lock):
  sf_2d / 1e:  int1e_ipovlp_spinor, int1e_ipkin_spinor, int1e_ipnuc_spinor, int1e_iprinv_spinor,
               int1e_ipovlpip_spinor, int1e_ipkinip_spinor, int1e_ipnucip_spinor,
               int1e_ipipovlp_spinor, int1e_ipipnuc_spinor, int1e_ipipkin_spinor, int1e_ipiprinv_spinor,
               int1e_ipipnucip_spinor, int1e_ipiprinvip_spinor, int1e_ipipipnuc_spinor, int1e_ipipiprinv_spinor,
               int1e_ipipipiprinv_spinor, int1e_ipiprinvipip_spinor, int1e_ipipiprinvip_spinor
  sf_2d / 2c2e: int2c2e_ip1_spinor, int2c2e_ip2_spinor
  sf_3c2e:     int3c2e_ip1_spinor, int3c2e_ip2_spinor, int3c1e_ip1_spinor, int3c1e_iprinv_spinor

Each int3c1e_ip1/iprinv flip is backed by Plan 04's test_int3c1e_ip1/iprinv_spinor_adversarial_parity
(GREEN under both gate flags before this plan runs — depends_on includes 04; the comparison is
against the Plan-02a-corrected 360-element spherical-aux-k buffer).

MUST stay oracle_covered=false (D-03/D-04 deferred):
  int2e_ip1_spinor, int2e_ip2_spinor, int2e_ipip1_spinor, int2e_ipvip1_spinor,
  int2e_ip1ip2_spinor, int2e_ipip1ipip2_spinor, int1e_ecp_iprinv_spinor

Lock entry shape (single-field flip):
  { "id": { "symbol": "int1e_ipovlp_spinor" }, ..., "oracle_covered": false, ... }  -> set true

Audit auto-syncs (A4): both audit sides derive from the lock; no separate fixtures list to edit.
xtask is a STANDALONE cargo project (own Cargo.lock), NOT a workspace member — run from xtask/ dir:
  cd xtask && cargo run -- manifest-audit   (Phase 25-03 landmine: `cargo run -p xtask` from root fails)

oracle_covered_update.rs note (L13-40): the `if fixture.skipped { continue; }` guard — KEEP it
  (still correct for D-03/D-04 deferred families); UPDATE the comment to say sf-derivative spinor
  families are now covered (no longer skipped). Do NOT stamp any fixture that remains `skipped`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip oracle_covered for in-scope families + verify component_rank + manifest-audit green</name>
  <files>crates/cintx-ops/generated/compiled_manifest.lock.json, xtask/src/oracle_covered_update.rs</files>
  <read_first>
    - crates/cintx-ops/generated/compiled_manifest.lock.json (the 24 in-scope family entries + the 7 deferred entries; component_rank fields)
    - xtask/src/oracle_covered_update.rs (the R5/D-03 deferral note + skipped-fixture guard L13-40)
    - .planning/phases/27-spinor-derivative-transform-gap-b1/27-CONTEXT.md (D-01/D-02 in-scope; D-03/D-04 deferred)
  </read_first>
  <action>
    For each of the 24 in-scope families listed in the interfaces block, set `"oracle_covered": false` -> `"oracle_covered": true` in compiled_manifest.lock.json. BEFORE flipping each, verify its `component_rank` matches its known rank tier (Pitfall 2 / A5): ipovlp/ipkin/ipnuc/iprinv + int2c2e_ip1/ip2 + int3c2e_ip1/ip2 + int3c1e_ip1/iprinv = "3"; ipovlpip/ipkinip/ipnucip + ipipovlp/ipipnuc/ipipkin/ipiprinv = "9"; ipipnucip/ipiprinvip/ipipipnuc/ipipiprinv = "27"; ipipipiprinv/ipiprinvipip/ipipiprinvip = "81". If any component_rank is wrong, FIX it in the lock (a wrong rank silently truncates trailing components).

    The int3c1e_ip1/iprinv flips are backed by Plan 04's test_int3c1e_ip1/iprinv_spinor_adversarial_parity (GREEN under both flags, against the Plan-02a-corrected 360-element spherical-aux-k buffer). Do NOT flip any family whose Plan 03/04 parity test is not yet GREEN.

    Do NOT flip the 7 deferred families (int2e_ip*_spinor, int1e_ecp_iprinv_spinor) — they stay false.

    In oracle_covered_update.rs, KEEP the `if fixture.skipped { continue; }` guard (L31-39) — it is still correct for D-03/D-04 deferred families — and UPDATE the comment (L31-39) to record that sf-derivative spinor families are now oracle_covered (no longer skipped), while D-03/D-04 (arity-4 int2e_ip* + ECP spinor) remain intentionally skipped/deferred. Do NOT add logic that stamps a `skipped` fixture.

    Run the manifest audit from the standalone xtask dir (Phase 25-03 landmine — `cargo run -p xtask` from root fails because xtask is not a workspace member).
  </action>
  <verify>
    <automated>cd xtask && cargo run -- manifest-audit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A2 '"symbol": "int1e_ipovlp_spinor"' crates/cintx-ops/generated/compiled_manifest.lock.json` — and a scan of all 24 in-scope symbols (incl. int3c1e_ip1_spinor + int3c1e_iprinv_spinor) — shows `"oracle_covered": true`.
    - The 7 deferred families still read `"oracle_covered": false`: `grep -A20 '"symbol": "int2e_ip1_spinor"\|"symbol": "int1e_ecp_iprinv_spinor"' compiled_manifest.lock.json | grep -c '"oracle_covered": false'` >= 2.
    - `cd xtask && cargo run -- manifest-audit` exits 0 (green).
    - `grep -c 'fixture.skipped' xtask/src/oracle_covered_update.rs` >= 1 (skipped guard retained).
    - No capi enum / legacy `cint*` wrapper added: `git diff --stat` shows no changes to capi crate or legacy-wrapper files.
  </acceptance_criteria>
  <done>24 in-scope families flipped oracle_covered=true with verified component_rank (int3c1e flips backed by Plan 04 parity); 7 deferred families remain false; deferral note updated; skipped guard retained; manifest-audit green.</done>
</task>

<task type="auto">
  <name>Task 2: Complete the D-10 no-silent-skip assertion + run the full vendor parity suite</name>
  <files>crates/cintx-oracle/tests/spinor_deriv_parity.rs</files>
  <read_first>
    - crates/cintx-oracle/tests/spinor_deriv_parity.rs (the test_no_silent_skip stub from Plan 01, with Plan-02a-corrected spherical-aux-k collectors)
    - crates/cintx-ops/generated/compiled_manifest.lock.json (now-flipped oracle_covered flags)
    - crates/cintx-oracle/src/compare.rs (how oracle_covered / manifest is read at runtime; `skipped` semantics L91-94)
    - .planning/phases/27-spinor-derivative-transform-gap-b1/27-VALIDATION.md (D-10 observable signals)
  </read_first>
  <action>
    Complete `test_no_silent_skip` (D-10): in addition to the Plan-01 assertion that the vendor arm executed (`running N>0 tests`, fixture NOT skipped, nonzero vendor output), add the assertion that every flipped in-scope family (including int3c1e_ip1_spinor + int3c1e_iprinv_spinor) reads `oracle_covered=true` in the manifest at runtime (read via the same path compare.rs uses, e.g. manifest_oracle_families / the lock). The test MUST fail (not skip) if: (a) the vendor arm compiled out, OR (b) the fixture is `skipped`, OR (c) any flipped family still reads `oracle_covered=false`, OR (d) any D-03/D-04 deferred family was accidentally flipped to true. Gate it `#[cfg(has_vendor_libcint)] #[cfg(feature="cpu")]` like the other vendor tests.

    Then run the FULL vendor parity suite under both gate flags (all D-09 tests incl. the two int3c1e tests + negative control + no-silent-skip) to confirm the whole phase is green. These tests compare against the Plan-02a-corrected spherical-aux-k buffers (360, not 720).
  </action>
  <verify>
    <automated>CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test spinor_deriv_parity 2>&1 | tee /tmp/27-05-parity.log; grep -E 'running [1-9][0-9]* tests' /tmp/27-05-parity.log</automated>
  </verify>
  <acceptance_criteria>
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test spinor_deriv_parity` exits 0 (ALL tests green: 7 parity incl. both int3c1e + negative control + no_silent_skip).
    - The run reports `running N>0 tests` (not `running 0 tests`) — grep on /tmp/27-05-parity.log matches `running [1-9]`.
    - `grep -c 'oracle_covered' crates/cintx-oracle/tests/spinor_deriv_parity.rs` >= 1 (test_no_silent_skip reads the flag).
    - test_no_silent_skip asserts BOTH the no-skip condition AND the flipped-families-true condition (verifiable by reading the test body — it references the flipped family symbols incl. int3c1e_ip1_spinor/int3c1e_iprinv_spinor and `oracle_covered`).
  </acceptance_criteria>
  <done>test_no_silent_skip asserts N>0 ran + fixture not skipped + flipped families (incl. int3c1e) oracle_covered=true + deferred families still false; the full vendor parity suite is green under both flags.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lock oracle_covered flag → CI coverage claim | Flipping the flag is the act of claiming verification; the D-10 assertion is the boundary that ensures the claim is backed by a real (non-skipped) vendor run. |
| Plan 04 parity result → int3c1e flip | The int3c1e flip records coverage Plan 04's byte-identity test proved (against the corrected 360-element buffer); the flip is NOT itself a verification, so depends_on [04] is load-bearing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-13 | Repudiation | stamping oracle_covered on a skipped fixture (false verification claim) | mitigate | test_no_silent_skip fails (not skips) if the vendor arm compiled out or the fixture is skipped; oracle_covered_update.rs retains the `if fixture.skipped { continue; }` guard so deferred families are never stamped (threat T-21-08-02). |
| T-27-14 | Tampering | accidentally flipping a D-03/D-04 deferred family | mitigate | Acceptance asserts the 7 deferred families still read false; test_no_silent_skip fails if any deferred family was flipped true. |
| T-27-15 | Tampering | wrong component_rank silently truncating a flipped family | mitigate | Task 1 verifies each component_rank against its rank tier BEFORE flipping; the rank-81 parity (Plan 03) and rank-3 3c2e/3c1e parity (Plan 04, spherical aux-k) exercise the extremes. |
| T-27-17 | Repudiation | int3c1e flipped without a backing parity test | mitigate | The int3c1e flip depends_on Plan 04, whose test_int3c1e_ip1/iprinv_spinor_adversarial_parity ran GREEN under both flags against the corrected 360-element buffer; test_no_silent_skip asserts int3c1e reads oracle_covered=true only after that proof. |
</threat_model>

<verification>
- `cd xtask && cargo run -- manifest-audit` green.
- `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test spinor_deriv_parity` fully green, `running N>0 tests`.
- 24 in-scope families oracle_covered=true (incl. int3c1e_ip1/iprinv, backed by Plan 04 parity); 7 deferred families false.
</verification>

<success_criteria>
- All in-scope sf-derivative spinor families flipped oracle_covered=true; deferred families untouched.
- The int3c1e flips are backed by Plan 04's vendor byte-identity parity tests (no coverage stamped without a real comparison, against the corrected 360-element buffer).
- D-10 no-silent-skip assertion complete and passing under both gate flags.
- manifest-audit green; no capi/legacy surface added.
- ROADMAP success criterion 3 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/27-spinor-derivative-transform-gap-b1/27-05-SUMMARY.md`
</output>
</content>
