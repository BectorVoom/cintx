---
phase: 27-spinor-derivative-transform-gap-b1
plan: 03
type: execute
wave: 3
depends_on: [01, 02, 02a]
files_modified:
  - crates/cintx-cubecl/src/kernels/one_electron.rs
  - crates/cintx-cubecl/src/kernels/center_2c2e.rs
autonomous: true
requirements: [FND-04]
must_haves:
  truths:
    - "int1e_ipovlp_spinor (rank-3) evaluates via the wrapper and is byte-identical to vendored libcint 6.1.3 at atol=1e-12 on the D-08 adversarial fixture (non-square + nctr>1 + kappa=0)."
    - "All rank-9/27/81 1e ip-spinor families that previously returned UnsupportedApi now evaluate via cart_to_spinor_sf_derivative_2d with their lock component_rank as ncomp."
    - "int2c2e_ip1/ip2_spinor evaluate via cart_to_spinor_sf_derivative_2d (ncomp=3)."
    - "No launcher owns a KET->BRA transpose any more — the inline rank-3 transpose in one_electron.rs is replaced by a single wrapper call."
  artifacts:
    - path: "crates/cintx-cubecl/src/kernels/one_electron.rs"
      provides: "all 1e ip-spinor arms dispatch to cart_to_spinor_sf_derivative_2d"
      contains: "cart_to_spinor_sf_derivative_2d"
    - path: "crates/cintx-cubecl/src/kernels/center_2c2e.rs"
      provides: "int2c2e ip1/ip2 spinor arm dispatches to cart_to_spinor_sf_derivative_2d"
      contains: "cart_to_spinor_sf_derivative_2d"
  key_links:
    - from: "crates/cintx-cubecl/src/kernels/one_electron.rs"
      to: "cart_to_spinor_sf_derivative_2d"
      via: "Representation::Spinor arms (rank 3/9/27/81)"
      pattern: "cart_to_spinor_sf_derivative_2d::<F>"
    - from: "crates/cintx-cubecl/src/kernels/center_2c2e.rs"
      to: "cart_to_spinor_sf_derivative_2d"
      via: "int2c2e ip1/ip2 Spinor arm"
      pattern: "cart_to_spinor_sf_derivative_2d::<F>"
---

<objective>
Rewire every sf_2d-path spinor derivative launcher arm to call `cart_to_spinor_sf_derivative_2d` (from Plan 02): the 1e families across all rank tiers in `one_electron.rs` and the 2c2e ip1/ip2 families in `center_2c2e.rs`. Replace the existing inline rank-3 transpose+loop (the byte-identity-proven block) with one wrapper call, and lift the rank-9/27/81 `UnsupportedApi` rejections so those arms compute via the wrapper.

Purpose: Move every sf_2d-path family from `UnsupportedApi` to a real evaluation, with the orientation transpose now owned by the wrapper (D-06) and nctr>1 supported (D-08). This satisfies ROADMAP success criterion 1+2 for the sf_2d path and turns the Plan 01 sf_2d/2c2e parity tests GREEN.

Aux-k scope note: this plan touches ONLY the sf_2d path (arity-2 1e + 2c2e). There is NO auxiliary-k axis here, so the aux-k correction (Plan 02a / the ⚠ CORRECTION NOTICE) does not affect any sizing in this plan. depends_on includes 02a only so the corrected oracle scaffolding (test collectors / fixtures) is in place when this plan's Task 2 runs the vendor parity binary that ALSO contains the arity-3 tests.

Output: rewired one_electron.rs + center_2c2e.rs; the sf_2d/2c2e D-09 vendor parity tests pass.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-spinor-derivative-transform-gap-b1/27-CONTEXT.md
@.planning/phases/27-spinor-derivative-transform-gap-b1/27-RESEARCH.md
@.planning/phases/27-spinor-derivative-transform-gap-b1/27-PATTERNS.md
@.planning/phases/27-spinor-derivative-transform-gap-b1/27-SPIKE-FINDINGS.md

<interfaces>
Wrapper (from Plan 02, crates/cintx-cubecl/src/transform/c2spinor.rs):
  pub fn cart_to_spinor_sf_derivative_2d<F: CintFloat>(
      staging: &mut [F], cart: &[f64], ncomp: usize,
      li: u8, kappa_i: i16, lj: u8, kappa_j: i16, nctr_i: usize, nctr_j: usize,
  ) -> Result<(), cintxRsError>

one_electron.rs sites (grep-confirmed):
  L24   use crate::transform::c2spinor::{cart_to_spinor_sf_2d, spinor_len};   // ADD cart_to_spinor_sf_derivative_2d
  L8734-8736  early-guard: if plan.representation == Spinor -> UnsupportedApi("spinor int1e_{op_name}")
  L8810-8812, L9016-9018, L9126-9128, L9210-9212 (gradient), L9300-9302 (gradient),
  L9453-9455 (3rd/4th-order), L9592-9594 (Hessian)   -- the rank-9/27/81 EARLY GUARDS to lift
  L9416, L9564, L9732   -- staging-copy match arms `Representation::Spinor => unreachable!("spinor rejected above")` to replace with the wrapper call
  L9919-9965  -- the inline rank-3 Spinor arm (transpose+loop) to REPLACE with one wrapper call; DROP the n_ctr_i!=1||n_ctr_j!=1 rejection @ L9925-9930

center_2c2e.rs sites (grep-confirmed):
  L53   use crate::transform::c2spinor::cart_to_spinor_sf_2d;   // ADD cart_to_spinor_sf_derivative_2d
  L629-631   ip1/ip2 Spinor early-guard -> UnsupportedApi  (unreachable arms @ L773, L930)
  L1082-1086  scalar-spinor analog: cart_to_spinor_sf_2d::<F>(staging, &cart_buf, li, kappa_i, lk, kappa_k)?

component_rank (ncomp) per family (from lock — verify before use, Pitfall 2):
  rank-3: int1e_ipovlp/ipkin/ipnuc/iprinv, int2c2e_ip1/ip2
  rank-9: int1e_ipovlpip/ipkinip/ipnucip, int1e_ipipovlp/ipipnuc/ipipkin/ipiprinv
  rank-27: int1e_ipipnucip/ipiprinvip/ipipipnuc/ipipiprinv
  rank-81: int1e_ipipipiprinv/ipiprinvipip/ipipiprinvip
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire one_electron.rs — replace inline rank-3, lift rank-9/27/81 rejections, dispatch to wrapper</name>
  <files>crates/cintx-cubecl/src/kernels/one_electron.rs</files>
  <read_first>
    - crates/cintx-cubecl/src/kernels/one_electron.rs (L24 import; inline rank-3 arm L9919-9965; early guards L8734/8810/9016/9126/9210/9300/9453/9592; unreachable match arms L9416/9564/9732)
    - crates/cintx-cubecl/src/transform/c2spinor.rs (the new cart_to_spinor_sf_derivative_2d signature from Plan 02)
    - .planning/phases/27-spinor-derivative-transform-gap-b1/27-SPIKE-FINDINGS.md (nctr passing convention for launchers)
    - crates/cintx-ops/generated/compiled_manifest.lock.json (verify component_rank for each rewired family)
  </read_first>
  <action>
    Extend the import at L24 to `use crate::transform::c2spinor::{cart_to_spinor_sf_2d, cart_to_spinor_sf_derivative_2d, spinor_len};`.

    Replace the inline rank-3 Spinor arm body (L9919-9965) with a single call:
      `cart_to_spinor_sf_derivative_2d::<F>(staging, cart_3comp, 3, li, shell_i.kappa, lj, shell_j.kappa, n_ctr_i, n_ctr_j)?;`
    and DROP the `n_ctr_i != 1 || n_ctr_j != 1 => UnsupportedApi` rejection at L9925-9930 (nctr now handled in the wrapper, D-08). Pass the actual contraction counts (`n_ctr_i`, `n_ctr_j`) from the shells in scope.

    For each rank-9/27/81 EARLY GUARD (L8734, L8810, L9016, L9126, L9210, L9300, L9453, L9592): remove the `if plan.representation == Representation::Spinor { return UnsupportedApi(...) }` block. Then in the corresponding staging-copy match arm currently `Representation::Spinor => unreachable!("spinor rejected above")` (L9416, L9564, L9732), replace `unreachable!()` with `cart_to_spinor_sf_derivative_2d::<F>(staging, <cart_buf_for_that_arm>, <ncomp>, li, shell_i.kappa, lj, shell_j.kappa, n_ctr_i, n_ctr_j)?;` where `<ncomp>` is the family's lock `component_rank` (3/9/27/81). Verify each `<ncomp>` against `compiled_manifest.lock.json` for the family that arm handles BEFORE wiring it (Pitfall 2 / A5) — do not hardcode a value that disagrees with the lock.

    If an arm already has a local `ncomp`/`n_comp` variable in scope derived from the manifest, reuse it; otherwise derive it from the family's component_rank. Do NOT re-introduce any KET→BRA transpose in this file (D-06: it lives only in the wrapper now).
  </action>
  <verify>
    <automated>cargo build -p cintx-cubecl --features cpu && cargo test -p cintx-cubecl --lib kernels::one_electron</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'cart_to_spinor_sf_derivative_2d::<F>' crates/cintx-cubecl/src/kernels/one_electron.rs` returns >= 4 (rank-3 arm + the lifted rank-9/27/81 arms).
    - `grep -c 'unreachable!("spinor rejected above")' crates/cintx-cubecl/src/kernels/one_electron.rs` returns 0 (all replaced).
    - `grep -vc '^[[:space:]]*//' crates/cintx-cubecl/src/kernels/one_electron.rs` then `grep -c 'format!("spinor int1e_{op_name}")' crates/cintx-cubecl/src/kernels/one_electron.rs` returns 0 (no ip-family early-guard UnsupportedApi remains).
    - `grep -c 'block_bra_major\[ic \* ncj + jc\]' crates/cintx-cubecl/src/kernels/one_electron.rs` returns 0 (no inline transpose remains).
    - `cargo build -p cintx-cubecl --features cpu` exits 0.
  </acceptance_criteria>
  <done>All 1e ip-spinor arms (rank 3/9/27/81) dispatch to cart_to_spinor_sf_derivative_2d; no inline transpose, no UnsupportedApi early guard for ip families; crate builds.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire center_2c2e.rs ip1/ip2 spinor arm + run the sf_2d/2c2e D-09 vendor parity</name>
  <files>crates/cintx-cubecl/src/kernels/center_2c2e.rs</files>
  <read_first>
    - crates/cintx-cubecl/src/kernels/center_2c2e.rs (L53 import; ip1/ip2 guard L629-631; unreachable arms L773, L930; scalar-spinor analog L1082-1086)
    - crates/cintx-cubecl/src/transform/c2spinor.rs (cart_to_spinor_sf_derivative_2d signature)
    - crates/cintx-oracle/tests/spinor_deriv_parity.rs (the RED tests from Plan 01, CORRECTED by Plan 02a: test_int1e_ipovlp/ipovlpip/ipipipiprinv + test_int2c2e_ip1 + test_orientation_negative_control)
  </read_first>
  <action>
    Extend the import at L53 to `use crate::transform::c2spinor::{cart_to_spinor_sf_2d, cart_to_spinor_sf_derivative_2d};`.

    Remove the ip1/ip2 Spinor early-guard at L629-631 (`if plan.representation == Representation::Spinor { return UnsupportedApi(...) }`). In the unreachable match arms (L773, L930), replace `unreachable!("spinor int2c2e ... rejected above")` with `cart_to_spinor_sf_derivative_2d::<F>(staging, &cart_buf, 3, li, shell_i.kappa, lk, shell_k.kappa, n_ctr_i, n_ctr_k)?;` mirroring the scalar-spinor analog at L1082-1086 (2c2e's two centers are i and k, using `(li, kappa_i, lk, kappa_k)`). ncomp=3 for both int2c2e_ip1 and int2c2e_ip2 (verified in lock). Pass the actual contraction counts. (2c2e folds through the sf_2d path — there is NO aux-k axis, so the ⚠ CORRECTION NOTICE does not apply.)

    Then run the sf_2d-path + 2c2e D-09 vendor parity from the Plan 01 test file (CORRECTED collectors from Plan 02a) under BOTH gate flags. The rank-3/9/81 1e tests and the 2c2e test must now be GREEN (they were RED in Plan 01), and the orientation negative control must DIVERGE.
  </action>
  <verify>
    <automated>cargo build -p cintx-cubecl --features cpu && CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test spinor_deriv_parity test_int1e_ipovlp_spinor_adversarial_parity test_int1e_ipovlpip_spinor_adversarial_parity test_int1e_ipipipiprinv_spinor_adversarial_parity test_int2c2e_ip1_spinor_adversarial_parity test_orientation_negative_control</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'cart_to_spinor_sf_derivative_2d::<F>' crates/cintx-cubecl/src/kernels/center_2c2e.rs` returns >= 2 (ip1 + ip2 arms).
    - `grep -c 'unreachable!("spinor int2c2e' crates/cintx-cubecl/src/kernels/center_2c2e.rs` returns 0.
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test spinor_deriv_parity test_int1e_ipovlp_spinor_adversarial_parity test_int1e_ipovlpip_spinor_adversarial_parity test_int1e_ipipipiprinv_spinor_adversarial_parity test_int2c2e_ip1_spinor_adversarial_parity` exits 0 (4 sf_2d/2c2e parity tests GREEN at atol=1e-12).
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test spinor_deriv_parity test_orientation_negative_control` exits 0 (the negative control passes because j-fastest correctly DIVERGES on the non-square block).
  </acceptance_criteria>
  <done>int2c2e ip1/ip2 spinor arms dispatch to the wrapper; the sf_2d rank-3/9/81 1e + 2c2e vendor parity tests are byte-identical at atol=1e-12 on the D-08 adversarial fixture; orientation negative control diverges.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval_raw request → launcher | The Representation::Spinor derivative arm crosses from request dispatch to the host transform; previously it fail-closed (UnsupportedApi), now it must size staging from the manifest component_rank and call the wrapper. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-07 | Tampering | wrong ncomp passed to the wrapper | mitigate | Each rewired arm passes `<ncomp>` verified against the lock `component_rank` (Pitfall 2). The wrapper itself sizes cart as `ncomp*block_len*nctr` and fails closed on mismatch; the rank-81 parity test (int1e_ipipipiprinv) exercises the largest tier and would surface a truncated trailing component. |
| T-27-08 | Tampering | launcher re-owning the transpose (orientation landmine) | mitigate | The inline transpose is DELETED from one_electron.rs (grep asserts `block_bra_major[ic*ncj+jc]` count == 0); transpose lives only in the Plan 02 wrapper (D-06). Non-square p×d fixture + negative control prove orientation. |
| T-27-09 | Repudiation | vendor parity silently skipping | mitigate | Task 2 runs the parity under BOTH `--features cpu` and `CINTX_ORACLE_BUILD_VENDOR=1`; the Plan 01 test_no_silent_skip (completed in Plan 05) guards the false-pass path. |
</threat_model>

<verification>
- `cargo build -p cintx-cubecl --features cpu` green.
- 4 sf_2d/2c2e vendor parity tests + the orientation negative control GREEN under both gate flags.
- No inline transpose and no ip-family UnsupportedApi early guard remain (grep-confirmed).
</verification>

<success_criteria>
- All sf_2d-path families (1e rank 3/9/27/81 + 2c2e ip1/ip2) evaluate via cart_to_spinor_sf_derivative_2d.
- int1e_ipovlp/ipovlpip/ipipipiprinv + int2c2e_ip1 byte-identical to libcint 6.1.3 at atol=1e-12 on the D-08 adversarial fixture.
- D-06 satisfied: zero launcher-owned transposes remain.
</success_criteria>

<output>
After completion, create `.planning/phases/27-spinor-derivative-transform-gap-b1/27-03-SUMMARY.md`
</output>
</content>
