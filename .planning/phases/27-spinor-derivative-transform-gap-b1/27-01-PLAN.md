---
phase: 27-spinor-derivative-transform-gap-b1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cintx-oracle/src/vendor_ffi.rs
  - crates/cintx-oracle/src/fixtures.rs
  - crates/cintx-oracle/src/compare.rs
  - crates/cintx-oracle/tests/spinor_deriv_parity.rs
autonomous: false
requirements: [FND-04]
must_haves:
  truths:
    - "The D-11 design spike has empirically confirmed the device-emitted derivative cart block layout [comp][ket][bra] component-outer for the sf_2d path AND resolved the 3c2e transpose granularity + nctr>1 spinor composition against hand-checked vendor values on a NON-SQUARE block. The spike also records the int3c1e_ip1/iprinv launcher file path (D-11)."
    - "A new test file crates/cintx-oracle/tests/spinor_deriv_parity.rs exists with all D-09 per-path × per-rank parity test bodies (including the two int3c1e arity-3 families per D-02), the orientation negative control, and the D-10 no-silent-skip assertion, currently RED (vendor families/wrappers not yet wired) — the Nyquist RED targets for Plans 03/04/05."
    - "The D-08 adversarial fixture builder (non-square p×d + nctr>1 + kappa=0 + non-zero rinv origin) exists in fixtures.rs and compiles."
    - "Vendor FFI extern decls + safe wrappers exist for every D-09-tested family (rank-9 1e ipovlpip, rank-81 1e ipipipiprinv, int3c2e_ip1, int2c2e_ip1, AND the two D-02 arity-3 int3c1e_ip1/iprinv) beyond the four rank-3 1e that already exist — 6 new wrappers total."
  artifacts:
    - path: "crates/cintx-oracle/tests/spinor_deriv_parity.rs"
      provides: "D-09 parity tests (incl. int3c1e_ip1/iprinv) + D-10 no-silent-skip assertion (RED until wrappers land)"
      contains: "test_no_silent_skip"
    - path: "crates/cintx-oracle/src/fixtures.rs"
      provides: "D-08 adversarial spinor fixture (non-square + nctr>1 + kappa=0 + non-zero rinv origin)"
      contains: "build_adversarial_spinor_fixture"
    - path: "crates/cintx-oracle/src/vendor_ffi.rs"
      provides: "vendor FFI for rank-9/81 1e ip-spinor + int3c2e_ip1 + int2c2e_ip1 + int3c1e_ip1/iprinv spinor"
      contains: "vendor_int1e_ipovlpip_spinor"
    - path: ".planning/phases/27-spinor-derivative-transform-gap-b1/27-SPIKE-FINDINGS.md"
      provides: "D-11 spike empirical findings: device block layout + 3c2e granularity + nctr composition + int3c1e launcher file path"
      contains: "transpose granularity"
  key_links:
    - from: "crates/cintx-oracle/tests/spinor_deriv_parity.rs"
      to: "crates/cintx-oracle/src/vendor_ffi.rs"
      via: "vendor_int1e_ipovlp_spinor / vendor_int1e_ipovlpip_spinor / vendor_int3c1e_ip1_spinor calls"
      pattern: "vendor_int.*_spinor"
    - from: "crates/cintx-oracle/tests/spinor_deriv_parity.rs"
      to: "crates/cintx-oracle/src/fixtures.rs"
      via: "build_adversarial_spinor_fixture()"
      pattern: "build_adversarial_spinor_fixture"
---

<objective>
Run the D-11 design spike (HARD GATE — the user explicitly does NOT want it shortcut) and build the Wave-0 verification scaffolding that becomes the RED Nyquist targets for Plans 03/04/05.

The spike must empirically resolve the two MEDIUM-confidence residual unknowns BEFORE any wrapper signature is finalized in Plan 02:
1. The exact device-emitted derivative cart block layout (confirm `[comp][ket][bra]` component-outer, per-component stride `block_len = nci*ncj` for 1e / `nci*ncj*nck` for 3c2e) verified against hand-checked vendor values on a NON-SQUARE block.
2. The 3c2e derivative transpose granularity (per-(comp,k) `[ket][bra]` vs per-comp before the k-fold; whether `int3c2e_ip2` differs from `ip1`; whether `int3c1e_ip1/iprinv` shares the `_3c2e` wrapper or needs a thin sibling) AND the nctr>1 spinor composition (`i_global = ci*di + ic`, column-major env coeff vs row-major Shell).

The spike must also RECORD the int3c1e_ip1/iprinv launcher file path (the reject sites are in `crates/cintx-cubecl/src/kernels/center_3c1e.rs` at L1006-1010 and L1130-1134 — confirm and record this so Plan 04 owns the right file).

Purpose: De-risk Plan 02's wrapper signatures and Plan 03/04's launcher rewires. The existing rank-3 inline code asserts the layout implicitly via a passing SQUARE-block test (H2O STO-3G) which is orientation-blind — the spike's non-square verification is load-bearing, not ceremonial.

Output: 27-SPIKE-FINDINGS.md (empirical layout/granularity/nctr decisions + int3c1e launcher file path); the new parity test file (RED); the D-08 adversarial fixture; the missing vendor FFI (6 new wrappers).
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-spinor-derivative-transform-gap-b1/27-CONTEXT.md
@.planning/phases/27-spinor-derivative-transform-gap-b1/27-RESEARCH.md
@.planning/phases/27-spinor-derivative-transform-gap-b1/27-PATTERNS.md
@.planning/phases/27-spinor-derivative-transform-gap-b1/27-VALIDATION.md
@.claude/skills/spike-findings-cintx/SKILL.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase. Do NOT re-explore. -->

Inner transforms (crates/cintx-cubecl/src/transform/c2spinor.rs):
  pub fn spinor_len(l: u8, kappa: i32) -> usize        // L25 — handles kappa==0 -> 4l+2
  pub fn cart_to_spinor_sf<F: CintFloat>(...)          // L290
  pub fn cart_to_spinor_sf_2d<F: CintFloat>(
      staging: &mut [F], cart: &[f64],
      li: u8, kappa_i: i16, lj: u8, kappa_j: i16,
  ) -> Result<(), cintxRsError>                        // L531 — reads cart BRA-major (cart[n*ncj+j])
  pub fn cart_to_spinor_sf_3c2e<F: CintFloat>(
      staging, cart, li, kappa_i, lj, kappa_j, lk,
  ) -> Result<(), cintxRsError>                        // L1281 — folds k cart->sph then sf_2d per k-slice (L1336-1343)

Existing rank-3 inline transform to mirror (one_electron.rs L9919-9965):
  for comp in 0..3 {
      block = &cart_3comp[comp*block_len .. +block_len];   // block_len = nci*ncj
      block_bra_major[ic*ncj + jc] = block[jc*nci + ic];   // KET->BRA transpose
      cart_to_spinor_sf_2d::<F>(&mut staging[comp*di*dj*2 .. +di*dj*2], &block_bra_major, ...);
  }

Existing vendor FFI (crates/cintx-oracle/src/vendor_ffi.rs) — ONLY four rank-3 1e exist:
  vendor_int1e_ipovlp_spinor   (L4193)
  vendor_int1e_ipkin_spinor    (L4221)
  vendor_int1e_ipnuc_spinor    (L4249)
  vendor_int1e_iprinv_spinor   (L4278)
  Shape (copy this verbatim):
    pub fn vendor_int1e_ipovlp_spinor(out, shls:&[i32;2], atm, natm, bas, nbas, env) -> i32 {
        unsafe { ffi::int1e_ipovlp_spinor(out.as_mut_ptr(), ptr::null_mut(),
            shls.as_ptr() as *mut i32, atm.as_ptr() as *mut i32, natm,
            bas.as_ptr() as *mut i32, nbas, env.as_ptr() as *mut f64,
            ptr::null_mut(), ptr::null_mut()) }
    }
  The int3c1e/int3c2e wrappers take a TRIPLE shls (&[i32;3]) — mirror the existing rank-3
  multi-center spinor wrapper section (starts L4303 "Multi-center spinor integral vendor FFI wrappers").
  Sizing helpers: vendor_CINTcgto_spinor (L3729), vendor_CINTshells_spinor_offset (L3773)

Fixture precedent (crates/cintx-oracle/tests/int3c1e_genctr_parity.rs):
  build_genctr_fixture (L56-129) — ALREADY non-square (p × d) + nctr=2 on the bra;
  env coeff is COLUMN-major env[ci*nprim+ip] (L63-67); displaced centers + env[PTR_RINV_ORIG..+3] (L80-82)

Parity test precedent (crates/cintx-oracle/tests/one_electron_grad_spinor_parity.rs):
  collect_cintx_spinor_grad (L183-214), stitch_block (L219-242, component-leading column-major),
  collect_vendor_spinor_grad<F> (L249-278), count_mismatches (L285+), ATOL=RTOL=1e-12 (L34-36),
  vendor test gating: #[cfg(has_vendor_libcint)] #[cfg(feature="cpu")] (L367-385),
  KAPPA_OF=0 spinor shell setup (L116-148)

int3c1e launcher (CONFIRMED — crates/cintx-cubecl/src/kernels/center_3c1e.rs):
  launch_center_3c1e_ip1     spinor reject @ L1006-1010 ("spinor int3c1e_ip1 gradient" UnsupportedApi)
  launch_center_3c1e_iprinv  spinor reject @ L1130-1134 ("spinor int3c1e_iprinv gradient" UnsupportedApi)
  Both are host-side; the iprinv path reads env[PTR_RINV_ORIG] (L1156). Plan 04 owns this file.

Component_rank values verified in lock (use these as ncomp; assert they match before relying on them):
  int1e_ipovlp_spinor=3, int1e_ipovlpip_spinor=9, int1e_ipipipiprinv_spinor=81,
  int3c2e_ip1_spinor=3, int3c2e_ip2_spinor=3, int2c2e_ip1_spinor=3, int2c2e_ip2_spinor=3,
  int3c1e_ip1_spinor=3, int3c1e_iprinv_spinor=3

Spinor output layout contract (.claude/skills/spike-findings-cintx/references/spinor-layout.md):
  out[comp*(ni_sp*nj_sp)*2 + (j*ni_sp + i)*2 + {0:re,1:im}]
    comp slowest (comp_stride = ni_sp*nj_sp*2); (j*ni_sp+i) ket-major, i(bra) fastest;
    ni_sp = 4l+2 at kappa=0; buffer is x2 real length
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: D-11 design spike — confirm device derivative cart block layout + 3c2e granularity + nctr composition + record int3c1e launcher file</name>
  <read_first>
    - .planning/phases/27-spinor-derivative-transform-gap-b1/27-RESEARCH.md (§ Open Questions Q1/Q2, Assumptions A1/A2/A3)
    - .planning/phases/27-spinor-derivative-transform-gap-b1/27-PATTERNS.md (Pattern 1; the `_3c2e` sibling note)
    - .claude/skills/spike-findings-cintx/references/device-block-layout.md
    - .claude/skills/spike-findings-cintx/references/spinor-layout.md
    - crates/cintx-cubecl/src/transform/c2spinor.rs (cart_to_spinor_sf_2d L531, cart_to_spinor_sf_3c2e L1281, the k-fold L1316-1343)
    - crates/cintx-cubecl/src/kernels/one_electron.rs (the inline rank-3 transform L9919-9965)
    - crates/cintx-cubecl/src/kernels/center_3c2e.rs (scalar-spinor arm L3276-3284)
    - crates/cintx-cubecl/src/kernels/center_3c1e.rs (int3c1e_ip1 reject L1006-1010; int3c1e_iprinv reject L1130-1134; the cart/sph scatter path)
    - crates/cintx-oracle/tests/int3c1e_genctr_parity.rs (build_genctr_fixture L56-129; the nctr>1 + non-square layout)
  </read_first>
  <what-built>
    Automated probe code (a throwaway `#[test]` or `xtask`/example binary, NOT shipped) that, on a NON-SQUARE p×d block with at least one nctr>1 shell and kappa=0:
    1. Dumps the device-emitted derivative cart buffer for `int1e_ipovlp_spinor` (rank 3) and confirms the layout is `[comp][ket][bra]` component-outer with per-component stride `block_len = nci*ncj`, verified element-by-element against hand-checked vendor values.
    2. Repeats for `int3c2e_ip1_spinor` to resolve the 3c2e transpose granularity: determine empirically whether the derivative buffer is `[comp][k][ket][bra]` (transpose per (comp,k) sub-block) or `[comp][k][bra][ket]`, and whether `int3c2e_ip2` differs from `ip1`. Decide whether `int3c1e_ip1/iprinv` (host-side in center_3c1e.rs) can share the `_3c2e` wrapper or needs a thin sibling.
    3. Confirms the nctr>1 spinor composition: `i_global = ci*di + ic`, `ni_full = nctr_i*di`, env coeff COLUMN-major (env[ci*nprim+ip]) vs cintx Shell ROW-major — verify against vendor on the nctr>1 fixture.
    4. RECORDS the int3c1e_ip1/iprinv launcher file path (confirmed: crates/cintx-cubecl/src/kernels/center_3c1e.rs, reject sites L1006-1010 / L1130-1134) so Plan 04 owns the correct file. The 3c1e launcher produces a host-side cart/sph scatter buffer (`out_buf`, component-leading, L1064-1110) — record its exact derivative cart layout so the spinor fold can be applied to it.
    Findings are written to .planning/phases/27-spinor-derivative-transform-gap-b1/27-SPIKE-FINDINGS.md with the concrete decisions Plan 02 will implement (exact transpose granularity for `_3c2e`, exact nctr loop nesting, sibling-vs-shared decision for int3c1e, and the int3c1e launcher file path).
  </what-built>
  <how-to-verify>
    1. Read 27-SPIKE-FINDINGS.md and confirm it states, with hand-checked vendor evidence on a NON-SQUARE block:
       - The sf_2d device derivative layout is `[comp][ket][bra]` component-outer, comp_stride = nci*ncj (confirm or correct).
       - The 3c2e derivative transpose granularity (per-(comp,k) `[ket][bra]` vs per-comp before k-fold) is settled with a definite answer, and whether ip2 differs from ip1.
       - Whether int3c1e_ip1/iprinv shares `_3c2e` or needs a sibling — a definite answer.
       - The int3c1e launcher FILE PATH (center_3c1e.rs) and its host-side derivative cart layout.
       - The nctr>1 spinor composition is confirmed (`i_global = ci*di + ic`, column->row coeff transpose location).
    2. Confirm the probe ran against the vendor (BOTH `--features cpu` AND `CINTX_ORACLE_BUILD_VENDOR=1`) — not a determinism-only run.
    3. Confirm the throwaway probe code is removed/not committed to shipped source (it lives in the spike findings as evidence, not in the crate).
  </how-to-verify>
  <acceptance_criteria>
    - File .planning/phases/27-spinor-derivative-transform-gap-b1/27-SPIKE-FINDINGS.md exists and contains the strings "transpose granularity", "nctr", and "[comp][ket][bra]".
    - 27-SPIKE-FINDINGS.md states a definite sibling-vs-shared decision for int3c1e AND records the int3c1e launcher file path (grep for "center_3c1e.rs" and "int3c1e").
    - The probe was run with both gate flags (recorded in the findings: "CINTX_ORACLE_BUILD_VENDOR=1" present).
    - No throwaway probe `#[test]`/binary remains in crates/ shipped source (`git status` shows no stray committed probe file under crates/).
  </acceptance_criteria>
  <action>
    Run `/gsd:spike` (or an equivalent throwaway probe) exercising the full per-component axis-fold across rank tiers and both transform paths, on a NON-SQUARE p×d block with nctr>1 and kappa=0. Drive the device kernel under BOTH `--features cpu` AND `CINTX_ORACLE_BUILD_VENDOR=1` so vendor comparison is real (not the silent-skip determinism-only path). For the sf_2d path, confirm `[comp][ket][bra]` component-outer, comp_stride = nci*ncj, by comparing each component slice to hand-checked vendor `int1e_ipovlp_spinor` values. For the 3c2e path, dump the `int3c2e_ip1_spinor` derivative buffer and determine whether it is `[comp][k][ket][bra]` (transpose per (comp,k)) or `[comp][k][bra][ket]`, and whether `int3c2e_ip2` (ket-center derivative) orders identically. Decide whether int3c1e_ip1/iprinv shares the `_3c2e` wrapper; RECORD that the int3c1e launchers live in crates/cintx-cubecl/src/kernels/center_3c1e.rs (launch_center_3c1e_ip1 reject L1006-1010, launch_center_3c1e_iprinv reject L1130-1134) and note the host-side derivative cart layout (`out_buf`, component-leading) so Plan 04 can apply the fold there. Confirm the nctr>1 composition `i_global = ci*di + ic`, `ni_full = nctr_i*di`, transposing the COLUMN-major env coeff (env[ci*nprim+ip]) to ROW-major internally. Write the empirical decisions to 27-SPIKE-FINDINGS.md. Remove the throwaway probe from shipped source.
  </action>
  <resume-signal>Type "approved" once 27-SPIKE-FINDINGS.md records the confirmed layout, the 3c2e granularity decision, the int3c1e sibling-vs-shared decision + launcher file path, and the nctr composition — or describe what is still unresolved.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add D-08 adversarial fixture + 6 missing vendor FFI wrappers + create RED parity test file</name>
  <files>crates/cintx-oracle/src/fixtures.rs, crates/cintx-oracle/src/vendor_ffi.rs, crates/cintx-oracle/src/compare.rs, crates/cintx-oracle/tests/spinor_deriv_parity.rs</files>
  <read_first>
    - crates/cintx-oracle/src/fixtures.rs (current spinor fixture dispatch)
    - crates/cintx-oracle/src/vendor_ffi.rs (the four rank-3 1e ip-spinor wrappers L4189-4301; the multi-center spinor section L4303+; sizing helpers L3713-3777; the int3c1e_ip1/iprinv scalar wrappers L3239-3300 for the triple-shls shape)
    - crates/cintx-oracle/src/compare.rs (symbol->RawApiId map L298-349; ATOL/RTOL L20-21; `skipped` semantics L91-94)
    - crates/cintx-oracle/tests/int3c1e_genctr_parity.rs (build_genctr_fixture L56-129; file gate `#![cfg(any(feature="cpu", feature="rocm"))]`)
    - crates/cintx-oracle/tests/one_electron_grad_spinor_parity.rs (collect/stitch/compare L183-385; KAPPA_OF=0 setup L116-148; vendor gating L367-385)
    - .planning/phases/27-spinor-derivative-transform-gap-b1/27-SPIKE-FINDINGS.md (from Task 1)
  </read_first>
  <behavior>
    - build_adversarial_spinor_fixture() returns (atm, bas, env) with: a NON-SQUARE bra/ket (p × d) shell pair; at least one shell with nctr>1 (e.g. p with nctr=2); KAPPA_OF=0 on every bas row (both GT+LT blocks fire, di=4l+2); displaced centers + env[PTR_RINV_ORIG..+3] set NON-ZERO for the iprinv path (so the rinv-center path is exercised, not a zero-origin shortcut). For the arity-3 int3c1e/int3c2e tests, the fixture exposes a third (aux k) shell.
    - Vendor FFI: vendor_int1e_ipovlpip_spinor (rank 9), vendor_int1e_ipipipiprinv_spinor (rank 81), vendor_int3c2e_ip1_spinor (rank 3, triple shls), vendor_int2c2e_ip1_spinor (rank 3), vendor_int3c1e_ip1_spinor (rank 3, triple shls), vendor_int3c1e_iprinv_spinor (rank 3, triple shls) each call the corresponding ffi::int*_spinor with the verbatim wrapper shape and out buffer sized ncomp*ni_sp*nj_sp*2 (1e/2c2e) or ncomp*ni_sp*nj_sp*nsk*2 (3c2e/3c1e). 6 new wrappers total.
    - spinor_deriv_parity.rs compiles and, BEFORE the wrappers land (Plans 03/04), every vendor parity test is RED (fails because cintx returns UnsupportedApi / wrong output) — confirming the Nyquist RED state. The smoke (non-vendor) arms compile and run.
  </behavior>
  <action>
    Add `build_adversarial_spinor_fixture()` to fixtures.rs modeled VERBATIM on `int3c1e_genctr_parity.rs::build_genctr_fixture` (L56-129): keep the non-square p×d + nctr=2 bra and the COLUMN-major env coeff layout (env[ci*nprim+ip], column 0 then column 1, L63-67) and the displaced centers + a NON-ZERO env[PTR_RINV_ORIG..+3] (L80-82); then set `bas[s*BAS_SLOTS + KAPPA_OF] = 0` on every bas row (copy the kappa=0 spinor setup from one_electron_grad_spinor_parity.rs L116-148). This single fixture covers all four landmines: non-square (orientation), nctr>1 (gc coeff transpose), kappa=0 (both blocks + 4l+2 sizing), non-zero rinv origin (iprinv path). Expose the third aux-k shell for the arity-3 families.

    In vendor_ffi.rs, add `extern "C"` `ffi::` decls + safe wrappers for: `vendor_int1e_ipovlpip_spinor` (rank 9, pair shls), `vendor_int1e_ipipipiprinv_spinor` (rank 81, pair shls), `vendor_int3c2e_ip1_spinor` (rank 3, triple shls [i,j,k]), `vendor_int2c2e_ip1_spinor` (rank 3, pair shls), `vendor_int3c1e_ip1_spinor` (rank 3, triple shls), `vendor_int3c1e_iprinv_spinor` (rank 3, triple shls — caller sets env[PTR_RINV_ORIG..+3]). Copy the verbatim shape from `vendor_int1e_ipovlp_spinor` (L4193) for the pair-shls 1e/2c2e wrappers and from the multi-center spinor section (L4303+) / the scalar int3c1e_ip1 triple-shls wrapper (L3243) for the triple-shls 3c2e/3c1e wrappers: `out.as_mut_ptr(), ptr::null_mut(), shls.as_ptr() as *mut i32, atm..natm, bas..nbas, env.as_ptr() as *mut f64, ptr::null_mut(), ptr::null_mut()`. Size the out buffer with `vendor_CINTcgto_spinor` (L3729): ncomp*ni_sp*nj_sp*2 for 1e/2c2e; ncomp*ni_sp*nj_sp*nsk*2 for 3c2e/3c1e. Gate all extern decls behind `#[cfg(has_vendor_libcint)]`.

    In compare.rs, ensure the symbol->RawApiId map (L298-349) and spinor dispatch cover INT1E_IPOVLP_SPINOR, INT1E_IPOVLPIP_SPINOR, INT1E_IPIPIPIPRINV_SPINOR, INT3C2E_IP1_SPINOR, INT2C2E_IP1_SPINOR, INT3C1E_IP1_SPINOR, INT3C1E_IPRINV_SPINOR (add any missing rows; do NOT change atol/rtol=1e-12).

    Create crates/cintx-oracle/tests/spinor_deriv_parity.rs with file gate `#![cfg(any(feature = "cpu", feature = "rocm"))]`. Clone the collect/stitch/compare helpers from one_electron_grad_spinor_parity.rs (collect_cintx_spinor, stitch_block component-leading column-major `out[comp*ni*nj*2 + (jj*ni+ii)*2 + {0:re,1:im}]`, collect_vendor_spinor, count_mismatches, ATOL=RTOL=1e-12), generalizing the hardcoded `3` to per-family `ncomp`. Add (each `#[cfg(has_vendor_libcint)] #[cfg(feature="cpu")] #[test]`, each using `build_adversarial_spinor_fixture()`, each asserting `assert_any_nonzero` on BOTH cintx and vendor then `assert_eq!(count_mismatches(&vendor,&cintx,1e-12,1e-12), 0)`):
      - test_int1e_ipovlp_spinor_adversarial_parity (ncomp=3)
      - test_int1e_ipovlpip_spinor_adversarial_parity (ncomp=9)
      - test_int1e_ipipipiprinv_spinor_adversarial_parity (ncomp=81; use the env_with_rinv_origin nonzero rinv center per Phase 25-03 landmine)
      - test_int3c2e_ip1_spinor_adversarial_parity (ncomp=3)
      - test_int2c2e_ip1_spinor_adversarial_parity (ncomp=3)
      - test_int3c1e_ip1_spinor_adversarial_parity (ncomp=3, D-02 arity-3 family)
      - test_int3c1e_iprinv_spinor_adversarial_parity (ncomp=3, D-02 arity-3 family; MUST use a NON-ZERO rinv origin — env[PTR_RINV_ORIG..+3] set by the fixture — so the rinv-center path is actually exercised, NOT a zero-origin shortcut)
    Add an orientation negative control: a `to_j_fastest` reindex helper and `test_orientation_negative_control` asserting `count_mismatches(&vendor, &cintx_jfastest, 1e-12, 1e-12) > 0` (j-fastest MUST diverge on the non-square block — proves i-fastest orientation).
    Add `test_no_silent_skip` (D-10): assert the vendor arm actually executed (not a `skipped` fixture) — fail if the fixture is skipped or 0 vendor tests ran. (The oracle_covered=true assertion part is wired in Plan 05 once the lock flips; here, assert the fixture is NOT skipped and produces nonzero vendor output.)

    DO NOT implement the wrappers or rewire launchers here — those are Plans 02/03/04. This file is the RED target. It is acceptable (expected) that the vendor parity tests FAIL at the end of this plan because cintx still returns UnsupportedApi for the non-rank-3 families AND for int3c1e_ip1/iprinv spinor.
  </action>
  <verify>
    <automated>cargo test -p cintx-oracle --features cpu --test spinor_deriv_parity --no-run</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p cintx-oracle --features cpu --test spinor_deriv_parity --no-run` exits 0 (the test binary COMPILES).
    - `grep -c 'fn build_adversarial_spinor_fixture' crates/cintx-oracle/src/fixtures.rs` returns >= 1.
    - `grep -c 'fn vendor_int1e_ipovlpip_spinor\|fn vendor_int1e_ipipipiprinv_spinor\|fn vendor_int3c2e_ip1_spinor\|fn vendor_int2c2e_ip1_spinor\|fn vendor_int3c1e_ip1_spinor\|fn vendor_int3c1e_iprinv_spinor' crates/cintx-oracle/src/vendor_ffi.rs` returns 6.
    - `grep -c 'fn test_no_silent_skip\|fn test_orientation_negative_control\|fn test_int1e_ipovlp_spinor_adversarial_parity\|fn test_int1e_ipovlpip_spinor_adversarial_parity\|fn test_int1e_ipipipiprinv_spinor_adversarial_parity\|fn test_int3c2e_ip1_spinor_adversarial_parity\|fn test_int2c2e_ip1_spinor_adversarial_parity\|fn test_int3c1e_ip1_spinor_adversarial_parity\|fn test_int3c1e_iprinv_spinor_adversarial_parity' crates/cintx-oracle/tests/spinor_deriv_parity.rs` returns 9.
    - `grep -c 'KAPPA_OF' crates/cintx-oracle/src/fixtures.rs` shows the fixture sets kappa (the kappa=0 spinor path).
    - The file gate `#![cfg(any(feature = "cpu"` is the first non-comment line of spinor_deriv_parity.rs.
  </acceptance_criteria>
  <done>
    Test binary compiles; D-08 fixture, 6 new vendor FFI wrappers (incl. int3c1e_ip1/iprinv), and 9 test bodies (7 parity + negative control + no-silent-skip) exist. Vendor parity tests are RED (cintx returns UnsupportedApi for non-rank-3 and for int3c1e spinor) — the intended Nyquist RED state handed to Plans 03/04/05.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device kernel → host transform | Untrusted block layout assumption crosses here; the spike empirically pins it before code depends on it. |
| oracle fixture → vendor FFI | The fixture must drive a REAL vendor comparison; the double-gate (`--features cpu` + `CINTX_ORACLE_BUILD_VENDOR=1`) is the boundary where a silent skip can fake a pass. |
| iprinv env origin → integral | A zero rinv origin makes int3c1e_iprinv trivially-passing; the D-08 fixture forces a non-zero origin (Phase 24/25 landmine). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Repudiation | spinor_deriv_parity.rs vendor gate | mitigate | The spike (Task 1) MUST run under both `--features cpu` and `CINTX_ORACLE_BUILD_VENDOR=1`; acceptance asserts the findings record the vendor-gated run. test_no_silent_skip fails (not skips) if the vendor arm compiled out. |
| T-27-02 | Tampering | build_adversarial_spinor_fixture | mitigate | Fixture is non-square (orientation), nctr>1 (gc transpose), kappa=0 (both-block sizing), non-zero rinv origin (iprinv) — each property defeats a distinct silent-false-pass landmine. Negative control (`to_j_fastest`) asserts j-fastest DIVERGES. |
| T-27-03 | Tampering | vendor out buffer sizing | mitigate | Each vendor wrapper sizes out = ncomp*ni_sp*nj_sp*2 (1e/2c2e) / ×nsk (3c2e/3c1e) via `vendor_CINTcgto_spinor`; under-sizing would truncate trailing components (component-truncation landmine). |
| T-27-04 | Tampering | trivially-passing int3c1e_iprinv (zero origin) | mitigate | test_int3c1e_iprinv_spinor_adversarial_parity uses the fixture's NON-ZERO env[PTR_RINV_ORIG]; a zero-origin shortcut would diverge from vendor. |
</threat_model>

<verification>
- `cargo test -p cintx-oracle --features cpu --test spinor_deriv_parity --no-run` compiles.
- 27-SPIKE-FINDINGS.md records the confirmed `[comp][ket][bra]` layout, the 3c2e granularity decision, the int3c1e sibling decision + launcher file path, and the nctr composition — each with vendor evidence.
- The 9 test bodies + D-08 fixture + 6 vendor FFI wrappers exist (grep-confirmed).
</verification>

<success_criteria>
- D-11 spike complete with empirical findings file (incl. int3c1e launcher file path) approved at the checkpoint.
- New parity test file compiles, is RED for the vendor families incl. int3c1e_ip1/iprinv (intended), and gates Plans 03/04/05.
- D-08 adversarial fixture + 6 missing vendor FFI wrappers landed.
</success_criteria>

<output>
After completion, create `.planning/phases/27-spinor-derivative-transform-gap-b1/27-01-SUMMARY.md`
</output>
</content>
</invoke>
