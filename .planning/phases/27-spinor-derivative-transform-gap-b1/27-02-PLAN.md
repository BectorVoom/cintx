---
phase: 27-spinor-derivative-transform-gap-b1
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - crates/cintx-cubecl/src/transform/c2spinor.rs
autonomous: true
requirements: [FND-04]
must_haves:
  truths:
    - "cart_to_spinor_sf_derivative_2d folds the ncomp axis (3/9/27/81) correctly: a ncomp*nci*ncj device-native KET-major cart buffer becomes a ncomp*di*dj*2 component-outer interleaved-complex spinor buffer, with the KET->BRA transpose owned INSIDE the wrapper."
    - "cart_to_spinor_sf_derivative_3c2e folds the ncomp axis on the 3c2e path with the transpose granularity confirmed by the D-11 spike."
    - "Both wrappers support nctr>1 via contraction-major composition (i_global = ci*di + ic), transposing the column-major env coeff to row-major internally — no coefficient transpose leaks to output."
    - "Both wrappers fail closed: an upfront size check returns a typed error before ANY write; no `if dst < staging.len()` scatter guards (FND-06 monolithic-writer contract)."
  artifacts:
    - path: "crates/cintx-cubecl/src/transform/c2spinor.rs"
      provides: "cart_to_spinor_sf_derivative_2d and cart_to_spinor_sf_derivative_3c2e wrappers + unit tests"
      contains: "fn cart_to_spinor_sf_derivative_2d"
      min_lines: 60
  key_links:
    - from: "cart_to_spinor_sf_derivative_2d"
      to: "cart_to_spinor_sf_2d"
      via: "per-component inner call after KET->BRA transpose"
      pattern: "cart_to_spinor_sf_2d::<F>"
    - from: "cart_to_spinor_sf_derivative_3c2e"
      to: "cart_to_spinor_sf_3c2e"
      via: "per-component inner call"
      pattern: "cart_to_spinor_sf_3c2e"
---

<objective>
Add the two thin generic derivative wrappers to `crates/cintx-cubecl/src/transform/c2spinor.rs` (D-05): `cart_to_spinor_sf_derivative_2d` and `cart_to_spinor_sf_derivative_3c2e`. Each loops the already-byte-identity-proven inner transform (`cart_to_spinor_sf_2d` / `cart_to_spinor_sf_3c2e`) `ncomp` times with the correct strides, owns the KET→BRA transpose internally (D-06), emits component-major-outer interleaved-complex output (D-07), supports nctr>1 (D-08 blocker, Pitfall 4), and fails closed on under-sized buffers (FND-06).

Purpose: This is the single audited place where the orientation transpose lives — no launcher may ever own it again (D-06 centralizes the landmine that caused the scalar-spinor orientation bug). The 3c2e wrapper signature/granularity follows the D-11 spike findings from Plan 01 — do NOT invent it; implement exactly what 27-SPIKE-FINDINGS.md records.

Output: two new `pub fn` wrappers + `#[test]` unit coverage for stride/transpose/nctr.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-spinor-derivative-transform-gap-b1/27-CONTEXT.md
@.planning/phases/27-spinor-derivative-transform-gap-b1/27-RESEARCH.md
@.planning/phases/27-spinor-derivative-transform-gap-b1/27-PATTERNS.md
@.planning/phases/27-spinor-derivative-transform-gap-b1/27-SPIKE-FINDINGS.md
@.claude/skills/spike-findings-cintx/references/spinor-layout.md
@.claude/skills/spike-findings-cintx/references/device-block-layout.md

<interfaces>
Inner transforms (same file, c2spinor.rs):
  pub fn spinor_len(l: u8, kappa: i32) -> usize                              // L25, kappa==0 -> 4l+2
  pub fn cart_to_spinor_sf_2d<F: CintFloat>(staging:&mut[F], cart:&[f64], li:u8, kappa_i:i16, lj:u8, kappa_j:i16) -> Result<(),cintxRsError>   // L531; reads cart BRA-major (cart[n*ncj+j])
  pub fn cart_to_spinor_sf_3c2e<F: CintFloat>(staging, cart, li, kappa_i, lj, kappa_j, lk) -> Result<(),cintxRsError>   // L1281; folds k cart->sph then sf_2d per k-slice (L1316-1343, staging_start = mk*di*dj*2)
  ncart(l)  -> usize   // cartesian count
  Errors: cintxRsError::ChunkPlanFailed{from, detail}, cintxRsError::BufferTooSmall{required, provided}

Existing inline rank-3 transform to generalize (one_electron.rs L9937-9964):
  let spinor_block = di * dj * 2;                 // D-07 component-outer stride
  for comp in 0..3 {
      let block = &cart_3comp[comp*block_len .. +block_len];   // block_len = nci*ncj
      let mut block_bra_major = vec![0.0f64; block_len];
      for ic in 0..nci { for jc in 0..ncj { block_bra_major[ic*ncj+jc] = block[jc*nci+ic]; } }  // D-06 transpose
      cart_to_spinor_sf_2d::<F>(&mut staging[comp*spinor_block .. +spinor_block], &block_bra_major, li, ki, lj, kj)?;
  }

nctr>1 composition (spike 005 / 27-SPIKE-FINDINGS.md):
  i_global = ci*di + ic   (ci in 0..nctr_i, ic in 0..di) ;  ni_full = nctr_i*di
  out index: out[comp*(ni_full*nj_full)*2 + (j_global*ni_full + i_global)*2 + {0:re,1:im}]
  env coeff COLUMN-major (env[ci*nprim+ip]); Shell ROW-major — transpose internally.

ncomp values (verified in lock; assert against caller-passed ncomp):
  rank-3 (ipovlp/ipkin/ipnuc/iprinv, int3c2e_ip1/ip2, int2c2e_ip1/ip2, int3c1e_ip1/iprinv),
  rank-9 (ipovlpip/ipkinip/ipnucip, ipipovlp/ipipnuc/ipipkin/ipiprinv),
  rank-27 (ipipnucip/ipiprinvip/ipipipnuc/ipipiprinv), rank-81 (ipipipiprinv/ipiprinvipip/ipipiprinvip)

Spinor output layout (verify against): out[comp*(ni_sp*nj_sp)*2 + (j*ni_sp+i)*2 + {0:re,1:im}]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement cart_to_spinor_sf_derivative_2d (ncomp loop + D-06 transpose + nctr>1 + fail-closed)</name>
  <files>crates/cintx-cubecl/src/transform/c2spinor.rs</files>
  <read_first>
    - crates/cintx-cubecl/src/transform/c2spinor.rs (cart_to_spinor_sf_2d L531-746 incl. size checks L544-559; spinor_len L25)
    - crates/cintx-cubecl/src/kernels/one_electron.rs (inline rank-3 transform L9919-9965 — the byte-identity-proven block to generalize)
    - .planning/phases/27-spinor-derivative-transform-gap-b1/27-SPIKE-FINDINGS.md (confirmed sf_2d layout + nctr composition)
    - .claude/skills/spike-findings-cintx/references/spinor-layout.md
  </read_first>
  <behavior>
    - ncomp=3, nctr=1, non-square p×d, kappa=0: output equals the existing inline rank-3 transform byte-for-byte (regression anchor).
    - ncomp=9 and ncomp=81: output splits into exactly ncomp non-overlapping all-nonzero di*dj*2 slices (no trailing zero — component-truncation guard).
    - nctr_i=2 (or nctr_j=2): output is ncomp*(nctr_i*di)*(nctr_j*dj)*2, composed contraction-major (i_global = ci*di + ic), no coefficient transpose leaking to output.
    - staging too small (staging.len() < ncomp*spinor_block_full): returns BufferTooSmall BEFORE any write (sentinel byte at staging[0] survives).
    - cart too small (cart.len() < ncomp*block_len*nctr_i*nctr_j): returns ChunkPlanFailed before any write.
  </behavior>
  <action>
    Add `pub fn cart_to_spinor_sf_derivative_2d<F: CintFloat>(staging: &mut [F], cart: &[f64], ncomp: usize, li: u8, kappa_i: i16, lj: u8, kappa_j: i16, nctr_i: usize, nctr_j: usize) -> Result<(), cintxRsError>` after `cart_to_spinor_sf_3c2e` (~L1346, before the `#[cfg(test)] mod`). Compute `nci = ncart(li)`, `ncj = ncart(lj)`, `block_len = nci*ncj`, `di = spinor_len(li, kappa_i as i32)`, `dj = spinor_len(lj, kappa_j as i32)`, `ni_full = nctr_i*di`, `nj_full = nctr_j*dj`, `spinor_block = ni_full*nj_full*2` (D-07 component-outer stride).

    FAIL-CLOSED upfront (FND-06, copy the shape from cart_to_spinor_sf_2d L544-559): if `cart.len() < ncomp * block_len * nctr_i * nctr_j` return `cintxRsError::ChunkPlanFailed{ from: "c2spinor_sf_derivative_2d", detail: ... }`; if `staging.len() < ncomp * spinor_block` return `cintxRsError::BufferTooSmall{ required: ncomp*spinor_block, provided: staging.len() }`. NO `if dst < staging.len()` scatter guards anywhere in the body — size-check once, then write unconditionally.

    For each `comp in 0..ncomp`: for each `(ci, cj)` contraction pair in `0..nctr_i × 0..nctr_j`: extract the device-native KET-major sub-block (use the contraction layout confirmed in 27-SPIKE-FINDINGS.md; if nctr==1 the sub-block is `cart[comp*block_len .. +block_len]`); allocate `block_bra_major = vec![0.0f64; block_len]` and apply the D-06 transpose `block_bra_major[ic*ncj + jc] = src[jc*nci + ic]` for `ic in 0..nci, jc in 0..ncj`; call `cart_to_spinor_sf_2d::<F>` into a scratch `di*dj*2` buffer, then scatter that block into `staging` at the contraction-major position `out[comp*spinor_block + (j_global*ni_full + i_global)*2 + {0,1}]` where `i_global = ci*di + ic`, `j_global = cj*dj + jc` (jc/ic over the di/dj spinor indices). If the spike confirms nctr==1 is the only case the launchers will pass for some families, still implement the general loop so D-08 (nctr>1) is satisfied.

    Implement the nctr loop nesting EXACTLY as recorded in 27-SPIKE-FINDINGS.md (contraction-major outer per the spike). The env coeff column→row transpose, if it belongs in this wrapper rather than the launcher, follows the spike decision — do not invent it.

    Add `#[test]` unit tests in the existing `#[cfg(test)] mod`: (a) `derivative_2d_rank3_matches_inline` — build a non-square p×d nctr=1 kappa=0 cart buffer, assert the wrapper output equals a manual replay of the inline rank-3 loop; (b) `derivative_2d_rank9_no_trailing_zero` — ncomp=9, assert each of the 9 di*dj*2 slices has at least one nonzero element; (c) `derivative_2d_nctr2_sizing` — nctr_i=2, assert output length and that the contraction-major layout places ci=1 at i_global=di..2*di; (d) `derivative_2d_staging_too_small_fails_closed` — undersized staging returns BufferTooSmall and a pre-written sentinel at staging[0] is untouched.
  </action>
  <verify>
    <automated>cargo test -p cintx-cubecl --lib transform::c2spinor::tests::derivative_2d</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub fn cart_to_spinor_sf_derivative_2d' crates/cintx-cubecl/src/transform/c2spinor.rs` returns 1.
    - `cargo test -p cintx-cubecl --lib transform::c2spinor::tests::derivative_2d` exits 0 (all 4 unit tests pass).
    - `grep -c 'if dst < staging.len()\|if dst < staging' crates/cintx-cubecl/src/transform/c2spinor.rs` does NOT increase for the new function (no scatter guards) — verify the new fn body has none.
    - The new fn references `cart_to_spinor_sf_2d::<F>` (grep `cart_to_spinor_sf_2d::<F>` count >= 1 inside the new fn region).
    - The body contains the transpose `block_bra_major[ic * ncj + jc] = ` (grep-verifiable).
  </acceptance_criteria>
  <done>cart_to_spinor_sf_derivative_2d exists with ncomp loop, D-06 internal transpose, nctr>1 contraction-major composition, FND-06 fail-closed sizing; 4 unit tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement cart_to_spinor_sf_derivative_3c2e (granularity per spike) + unit tests</name>
  <files>crates/cintx-cubecl/src/transform/c2spinor.rs</files>
  <read_first>
    - crates/cintx-cubecl/src/transform/c2spinor.rs (cart_to_spinor_sf_3c2e L1281-1346, the k-fold L1316-1343)
    - .planning/phases/27-spinor-derivative-transform-gap-b1/27-SPIKE-FINDINGS.md (3c2e transpose granularity + int3c1e sibling-vs-shared decision — AUTHORITATIVE)
    - crates/cintx-cubecl/src/kernels/center_3c2e.rs (scalar-spinor arm L3276-3284)
  </read_first>
  <behavior>
    - ncomp=3 (int3c2e_ip1), non-square p×d ket + s aux, nctr=1, kappa=0: per-component output matches the scalar 3c2e spinor transform applied per derivative component (the spike's hand-checked vendor layout).
    - The transpose granularity matches 27-SPIKE-FINDINGS.md exactly (per-(comp,k) vs per-comp before k-fold).
    - nctr>1 composes contraction-major as in Task 1.
    - Fail-closed sizing as in Task 1 (component-outer stride includes the k axis: spinor_block = ni_full*nj_full*nsk*2 where nsk is the k spherical count).
  </behavior>
  <action>
    Add `pub fn cart_to_spinor_sf_derivative_3c2e<F: CintFloat>(staging: &mut [F], cart: &[f64], ncomp: usize, li: u8, kappa_i: i16, lj: u8, kappa_j: i16, lk: u8, nctr_i: usize, nctr_j: usize) -> Result<(), cintxRsError>` immediately after `cart_to_spinor_sf_derivative_2d`. Mirror Task 1's structure: upfront fail-closed size checks (`cart.len() < ncomp * nci*ncj*nck * nctr_i*nctr_j` -> ChunkPlanFailed; `staging.len() < ncomp * ni_full*nj_full*nsk*2` -> BufferTooSmall, where `nsk` = spherical count of the k shell as `cart_to_spinor_sf_3c2e` computes it). The per-component inner call is `cart_to_spinor_sf_3c2e::<F>` (which already folds the k-axis cart→sph and applies sf_2d per k-slice), with `comp_stride = ni_full*nj_full*nsk*2`.

    The KET→BRA transpose granularity (per-(comp,k) on the `[ket][bra]` sub-block vs per-comp before the k-fold) and whether the derivative cart buffer is `[comp][k][ket][bra]` or `[comp][k][bra][ket]` MUST follow exactly what 27-SPIKE-FINDINGS.md recorded — implement that, do not guess. If the spike decided int3c1e_ip1/iprinv needs a thin sibling rather than sharing this wrapper, add that sibling here too (named per the spike, e.g. `cart_to_spinor_sf_derivative_3c1e`); if it shares, document that in a rustdoc comment on this fn.

    Add `#[test]` unit tests: (a) `derivative_3c2e_rank3_layout` — non-square p×d ket + s aux, ncomp=3, assert output splits into 3 non-overlapping all-nonzero comp_stride slices; (b) `derivative_3c2e_staging_too_small_fails_closed` — undersized staging returns BufferTooSmall before any write (sentinel survives). (Full vendor byte-identity is the Plan 04 parity test, not a unit test here.)
  </action>
  <verify>
    <automated>cargo test -p cintx-cubecl --lib transform::c2spinor::tests::derivative_3c2e</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub fn cart_to_spinor_sf_derivative_3c2e' crates/cintx-cubecl/src/transform/c2spinor.rs` returns 1.
    - `cargo test -p cintx-cubecl --lib transform::c2spinor::tests::derivative_3c2e` exits 0.
    - If 27-SPIKE-FINDINGS.md decided a sibling for int3c1e: `grep -c 'pub fn cart_to_spinor_sf_derivative_3c1e' c2spinor.rs` returns 1; else a rustdoc line on the 3c2e fn states int3c1e shares it (grep "int3c1e" in the fn region).
    - `cargo test -p cintx-cubecl --lib transform::c2spinor` exits 0 (no regression in existing c2spinor tests).
  </acceptance_criteria>
  <done>cart_to_spinor_sf_derivative_3c2e (and the int3c1e sibling iff the spike mandated one) exists with the spike-confirmed granularity, nctr>1 support, FND-06 fail-closed sizing; unit tests green; no regression in existing c2spinor tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller (launcher) → wrapper | Caller passes device-native KET-major blocks + ncomp + nctr; the wrapper must not trust ncomp blindly — it sizes buffers from ncomp and fails closed if cart/staging do not match. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04 | Tampering | cart_to_spinor_sf_derivative_2d/_3c2e staging write | mitigate | Upfront `staging.len() < ncomp*spinor_block` check returns BufferTooSmall before ANY write; no `if dst<len` scatter guards (FND-06 monolithic-writer). Unit test `*_staging_too_small_fails_closed` asserts a sentinel survives. |
| T-27-05 | Tampering | ncomp component fold | mitigate | Unit test `derivative_2d_rank9_no_trailing_zero` asserts all ncomp slices are non-zero (component-truncation landmine). Wrapper sizes cart as `ncomp*block_len*nctr_i*nctr_j` and rejects mismatch. |
| T-27-06 | Tampering | KET→BRA orientation transpose | mitigate | Transpose owned inside the wrapper (D-06); regression-anchored to the inline rank-3 code on a NON-SQUARE p×d block (`derivative_2d_rank3_matches_inline`). |
</threat_model>

<verification>
- `cargo test -p cintx-cubecl --lib transform::c2spinor` green (new wrappers + existing tests).
- Both `pub fn cart_to_spinor_sf_derivative_2d` and `_3c2e` present.
- No scatter guards introduced; transpose present inside the 2d wrapper.
</verification>

<success_criteria>
- Two wrappers (plus int3c1e sibling iff the spike mandated one) implemented per the D-11 findings.
- ncomp axis-fold, D-06 transpose, nctr>1 composition, FND-06 fail-closed sizing all unit-tested.
- No regression in existing c2spinor lib tests.
</success_criteria>

<output>
After completion, create `.planning/phases/27-spinor-derivative-transform-gap-b1/27-02-SUMMARY.md`
</output>
