---
phase: 28-spin-included-c2s-si-transform-p-module-gap-b2
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cintx-ops/generated/compiled_manifest.lock.json
  - crates/cintx-ops/src/generated/api_manifest.rs
  - crates/cintx-oracle/src/vendor_ffi.rs
  - xtask/src/oracle_covered_update.rs
autonomous: true
requirements: [FND-05]
must_haves:
  truths:
    - "An int1e_sp_spinor manifest row exists with oracle_covered=false (stays UnsupportedApi this phase)"
    - "A vendor_int1e_sp_spinor FFI shim exists for the byte-identity reference"
    - "oracle-covered-update mechanically refuses to flip a σ family whose only fixture was skipped"
    - "No capi enum variant or legacy cint* wrapper is added"
  artifacts:
    - path: "crates/cintx-oracle/src/vendor_ffi.rs"
      provides: "vendor_int1e_sp_spinor FFI binding + extern declaration"
      contains: "vendor_int1e_sp_spinor"
    - path: "crates/cintx-ops/generated/compiled_manifest.lock.json"
      provides: "int1e_sp_spinor row, oracle_covered=false"
      contains: "int1e_sp_spinor"
  key_links:
    - from: "vendor_int1e_sp_spinor"
      to: "ffi::int1e_sp_spinor"
      via: "extern FFI call"
      pattern: "int1e_sp_spinor"
    - from: "oracle_covered_update.rs guard"
      to: "skipped-fixture continue"
      via: "if fixture.skipped"
      pattern: "skipped"
---

<objective>
Add the manifest infrastructure for `int1e_sp_spinor` WITHOUT flipping its coverage (D-01:
infrastructure-only — it stays `UnsupportedApi` / `oracle_covered=false` this phase). Add the
`vendor_int1e_sp_spinor` FFI shim for the byte-identity reference, and verify+extend the SC#4 guard
that refuses to flip a σ family whose only fixture was `skipped`.

Purpose: gives Plan 04 the vendor reference + manifest row to run the FND-05 byte-identity proof,
while strictly honoring D-01/SC#4 (no σ flips this phase, all flips → Phase 29).

Output: int1e_sp_spinor manifest row (oracle_covered=false), vendor FFI shim + extern, SC#4 guard
assertion, OperatorId re-grep fix.
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-spin-included-c2s-si-transform-p-module-gap-b2/28-RESEARCH.md
@.planning/phases/28-spin-included-c2s-si-transform-p-module-gap-b2/28-PATTERNS.md

<interfaces>
<!-- Analogs to copy verbatim. -->
From crates/cintx-oracle/src/vendor_ffi.rs:
- vendor_int1e_ovlp_spinor(out:&mut[f64], shls:&[i32;2], atm, natm, bas, nbas, env) -> i32  // ~L4101 — copy verbatim, swap symbol
- extern block declares int1e_ovlp_spinor / int1e_ipovlp_spinor  // mimic for int1e_sp_spinor
- vendor_CINTcgto_spinor(...)  // ~L3729 — out sizing (ni_sp * nj_sp * 2 f64 interleaved)
From xtask/src/oracle_covered_update.rs:
- guard at ~L50: `if fixture.skipped { continue; }`  // SC#4 mechanism — assert it covers σ families
From crates/cintx-ops:
- ManifestEntry rows for existing *_spinor families (the lock is the source of truth; lock edit auto-syncs both audit sides)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add int1e_sp_spinor manifest row (oracle_covered=false) + re-grep OperatorId consts</name>
  <files>crates/cintx-ops/generated/compiled_manifest.lock.json, crates/cintx-ops/src/generated/api_manifest.rs</files>
  <read_first>
    - crates/cintx-ops/generated/compiled_manifest.lock.json (existing *_spinor rows — component_rank/forms/oracle_covered shape; find an arity-2 tensor-rank-1 spinor analog)
    - crates/cintx-ops/src/generated/api_manifest.rs (ManifestEntry format)
    - .planning/phases/28-spin-included-c2s-si-transform-p-module-gap-b2/28-PATTERNS.md (§"Manifest row" — Pitfall 6 OperatorId shift; component_rank = di*dj complex tensor rank 1, the 4 gc blocks are ncomp_e1 NOT tensor components)
    - .planning/phases/28-spin-included-c2s-si-transform-p-module-gap-b2/28-RESEARCH.md (§Spike Target E ng={1,0,0,0,1,4,1,1} → tensor rank 1; Runtime State Inventory — re-grep OperatorId after the row)
  </read_first>
  <action>
Add an `int1e_sp_spinor` row to `compiled_manifest.lock.json` (the source of truth; editing the lock
auto-syncs both audit sides — no separate fixtures list to touch, per project_ipovlpip_rank9_kernel).
Mirror an existing arity-2 spinor `*_spinor` row:
  - oracle_covered = false   (D-01 — STAYS UnsupportedApi this phase; the flip is Phase 29)
  - stability = stable
  - canonical arity = 2 (1e family)
  - component_rank: tensor rank 1 (ng[7]=1; output is di*dj complex). The 4 gc blocks are ncomp_e1,
    NOT tensor components — do NOT set component_rank to 4. Verify against an existing tensor-rank-1
    spinor family's convention; watch component_rank truncation (project_unstable_derivative_ports).

If `api_manifest.rs` carries a hand-maintained mirror row, add the matching entry there too;
otherwise rely on the lock auto-sync.

Pitfall 6 (OperatorId positional shift): adding a manifest row re-points hardcoded
`OperatorId::new(N)` / `_OPERATOR_ID: u32 = N` test consts at a different family. After adding the
row, grep the workspace for `OperatorId::new(` and `_OPERATOR_ID: u32 =` and any test asserting a
positional id; fix any that now point at the wrong family by resolving via SYMBOL name instead of the
positional int (project_operator_id_shift_breaks_hardcoded_test_consts).

NO capi enum variant, NO legacy `cint*` wrapper (new-family surface policy).
  </action>
  <verify>
    <automated>cargo run -p xtask -- manifest-audit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c int1e_sp_spinor crates/cintx-ops/generated/compiled_manifest.lock.json` >= 1.
    - The int1e_sp_spinor row has `oracle_covered` false: `grep -A8 'int1e_sp_spinor' crates/cintx-ops/generated/compiled_manifest.lock.json | grep -i 'oracle_covered' | grep -c false` >= 1.
    - No capi/legacy surface added: `git diff --name-only` shows no change under `crates/cintx-capi/` and no new `cint1e_sp` legacy wrapper symbol added.
    - `cargo run -p xtask -- manifest-audit` exits 0 (green).
    - `cargo test --workspace --locked` builds and any positional-OperatorId test still passes (re-grep fix applied).
  </acceptance_criteria>
  <done>int1e_sp_spinor row exists with oracle_covered=false, tensor rank 1, no capi/legacy surface; manifest-audit green; OperatorId positional shift resolved by symbol.</done>
</task>

<task type="auto">
  <name>Task 2: Add vendor_int1e_sp_spinor FFI shim + assert/extend the SC#4 skipped-fixture guard</name>
  <files>crates/cintx-oracle/src/vendor_ffi.rs, xtask/src/oracle_covered_update.rs</files>
  <read_first>
    - crates/cintx-oracle/src/vendor_ffi.rs (lines ~4101-4124 `vendor_int1e_ovlp_spinor`; the `ffi` extern block; ~L3729 `vendor_CINTcgto_spinor` sizing)
    - xtask/src/oracle_covered_update.rs (lines ~36-52 — the comment block + the `if fixture.skipped { continue; }` guard at ~L50)
    - .planning/phases/28-spin-included-c2s-si-transform-p-module-gap-b2/28-PATTERNS.md (§"vendor_int1e_sp_spinor"; §"SC#4 guard")
    - libcint-master/src/autocode/intor3.c:461-468 (int1e_sp_spinor driver — confirm the symbol exists in the vendored lib)
  </read_first>
  <action>
In `vendor_ffi.rs`: add `vendor_int1e_sp_spinor` by copying `vendor_int1e_ovlp_spinor` (~L4101)
verbatim and swapping `ffi::int1e_ovlp_spinor` → `ffi::int1e_sp_spinor`:
  pub fn vendor_int1e_sp_spinor(out:&mut[f64], shls:&[i32;2], atm:&[i32], natm:i32,
      bas:&[i32], nbas:i32, env:&[f64]) -> i32 {
      unsafe { ffi::int1e_sp_spinor(out.as_mut_ptr(), ptr::null_mut(), shls.as_ptr() as *mut i32,
          atm.as_ptr() as *mut i32, natm, bas.as_ptr() as *mut i32, nbas,
          env.as_ptr() as *mut f64, ptr::null_mut(), ptr::null_mut()) }
  }
ALSO add the `int1e_sp_spinor` extern declaration in the `ffi` extern block (there is no existing one;
mimic the `int1e_ovlp_spinor` / `int1e_ipovlp_spinor` extern signatures). `out` is sized
`ni_sp * nj_sp * 2` f64 (interleaved re/im); compute the size via vendor_CINTcgto_spinor (~L3729).
This is oracle/test glue → anyhow side, NOT thiserror (CLAUDE.md).

In `xtask/src/oracle_covered_update.rs`: VERIFY the existing guard at ~L50
(`if fixture.skipped { continue; }`, threat T-21-08-02) covers σ families — i.e. a σ family whose
only fixture is `skipped` is NOT stamped oracle_covered=true. If the guard already covers this case,
extend the comment block (~L36-49) to explicitly note that Phase-28 σ families (incl. int1e_sp_spinor)
stay deferred to Phase 29 and the skipped-fixture continue is the SC#4 mechanism. If the guard does
NOT cover σ families specifically (e.g. it only checks skipped but a σ family could be stamped via a
different path), extend it so int1e_sp_spinor cannot be flipped.
  </action>
  <verify>
    <automated>cargo run -p xtask -- oracle-covered-update</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'vendor_int1e_sp_spinor' crates/cintx-oracle/src/vendor_ffi.rs` >= 1 and the body calls `ffi::int1e_sp_spinor`: `grep -c 'int1e_sp_spinor' crates/cintx-oracle/src/vendor_ffi.rs` >= 2 (fn + extern + call).
    - The `int1e_sp_spinor` extern declaration is present in the ffi extern block (read the extern block to confirm).
    - `xtask/src/oracle_covered_update.rs` retains the skipped-fixture guard: `grep -c 'fixture.skipped\|skipped' xtask/src/oracle_covered_update.rs` >= 1; the comment block references Phase 29 deferral for σ families.
    - `cargo run -p xtask -- oracle-covered-update` completes AND int1e_sp_spinor stays oracle_covered=false afterward: `grep -A8 'int1e_sp_spinor' crates/cintx-ops/generated/compiled_manifest.lock.json | grep -i oracle_covered | grep -c false` >= 1.
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo build -p cintx-oracle --features cpu --locked` compiles the new FFI shim.
  </acceptance_criteria>
  <done>vendor_int1e_sp_spinor FFI shim + extern exist; the SC#4 guard provably keeps int1e_sp_spinor oracle_covered=false after oracle-covered-update; FFI compiles under the vendor gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cintx → vendored libcint FFI | Test/oracle-only boundary: cintx passes buffers to vendored C `int1e_sp_spinor`. No untrusted input (fixtures are author-controlled). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03-01 | Tampering | vendor FFI out-buffer (libcint writes past `out`) | mitigate | Size `out` from vendor_CINTcgto_spinor (ni_sp*nj_sp*2 f64); the parity harness (Plan 04) allocates the full vendor-expected size. |
| T-28-03-02 | Tampering (policy integrity) | oracle-covered-update flipping a skipped σ family | mitigate | Assert the skipped-fixture guard keeps int1e_sp_spinor oracle_covered=false (SC#4); CI gate verifies post-update. |

No other STRIDE categories apply: oracle/test glue, no auth (S/E), no production audit surface (R), no confidential data.
</threat_model>

<verification>
- `cargo run -p xtask -- manifest-audit` green; int1e_sp_spinor row present, oracle_covered=false.
- `cargo run -p xtask -- oracle-covered-update` keeps int1e_sp_spinor oracle_covered=false.
- `CINTX_ORACLE_BUILD_VENDOR=1 cargo build -p cintx-oracle --features cpu --locked` succeeds (FFI shim compiles).
- No capi/legacy surface added (git diff scope check).
</verification>

<success_criteria>
- int1e_sp_spinor manifest row exists, oracle_covered=false, no capi/legacy surface.
- vendor_int1e_sp_spinor FFI shim + extern exist and compile.
- SC#4 guard provably refuses to flip the skipped σ family.
- Maps to ROADMAP SC#4 (oracle-covered-update refuses to flip a σ family whose only fixture was skipped; no capi/legacy surface).
</success_criteria>

<output>
After completion, create `.planning/phases/28-spin-included-c2s-si-transform-p-module-gap-b2/28-03-SUMMARY.md`
</output>
