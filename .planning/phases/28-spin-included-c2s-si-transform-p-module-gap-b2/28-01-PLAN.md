---
phase: 28-spin-included-c2s-si-transform-p-module-gap-b2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cintx-cubecl/src/transform/c2spinor.rs
autonomous: true
requirements: [FND-05]
must_haves:
  truths:
    - "cart_to_spinor_si_2d consumes 4 cart blocks (gc_x/gc_y/gc_z/gc_1) and asserts block count at the boundary"
    - "The si bra step matches a_bra_cart2spinor_si signs, NOT apply_si_block signs"
    - "The ket step reuses the ordinary apply_ket_transform (no ket symmetrization)"
    - "All spinor buffer sizing comes from spinor_len, never a hardcoded 4l+2"
  artifacts:
    - path: "crates/cintx-cubecl/src/transform/c2spinor.rs"
      provides: "apply_bra_si_block (NEW si bra step) + cart_to_spinor_si_2d (host 2D si transform)"
      contains: "fn apply_bra_si_block"
  key_links:
    - from: "cart_to_spinor_si_2d"
      to: "apply_bra_si_block"
      via: "stage 1 bra call"
      pattern: "apply_bra_si_block"
    - from: "cart_to_spinor_si_2d"
      to: "apply_ket_transform"
      via: "stage 2 ordinary ket reuse"
      pattern: "apply_ket_transform"
---

<objective>
Add the host-side spin-included 2D cart→spinor transform `cart_to_spinor_si_2d` and its new
σ-coupled bra step `apply_bra_si_block` to `c2spinor.rs`. This is the bra+ket driver matching
libcint `c2s_si_1e` (`cart2sph.c:4947`): a Pauli-σ mix on the bra (`a_bra_cart2spinor_si`) and the
ORDINARY ket transform.

Purpose: The si_2d transform is the σ-coupling foundation FND-05 requires. It is the host analog of
`cart_to_spinor_sf_2d` with ONLY the bra step swapped for the 4-cart-input σ-mix. ~80% reuse.

Output: `apply_bra_si_block` + `cart_to_spinor_si_2d` in `c2spinor.rs`, with a unit test proving the
new bra-si sign convention (the single highest-risk landmine in this phase).
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-spin-included-c2s-si-transform-p-module-gap-b2/28-RESEARCH.md
@.planning/phases/28-spin-included-c2s-si-transform-p-module-gap-b2/28-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Already in c2spinor.rs — reuse verbatim, do NOT re-derive. -->
From crates/cintx-cubecl/src/transform/c2spinor.rs:
- pub fn cart_to_spinor_sf_2d<F: CintFloat>(staging:&mut[F], cart:&[f64], li:u8, kappa_i:i16, lj:u8, kappa_j:i16) -> Result<(), cintxRsError>  // L531 — STRUCTURAL TEMPLATE
- fn spinor_len(l: u8, kappa: i32) -> usize  // L25 — kappa==0→4l+2, kappa<0→2l+2 (GT), kappa>0→2l (LT)
- fn apply_bra_block(...)  // L668 — loop/index/coeff-layout structure to copy for apply_bra_si_block
- fn apply_bra_sf_block_all_kappa(...)  // L624 — GT/LT/both kappa block-pointer dispatch to mirror
- fn apply_ket_transform(...)  // L766 — ORDINARY ket; REUSE VERBATIM (c2s_si_1e ket is identical to c2s_sf_1e)
- fn bra_coeff_refs(...)  // L714 — CG table slices (cart2j_gt_l*/cart2j_lt_l*)
- fn apply_si_block(...)  // L124 — WRONG sign convention for this path; DO NOT call from si_2d
- ncart(l: u8) -> usize
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add apply_bra_si_block with the a_bra_cart2spinor_si sign convention + unit test</name>
  <files>crates/cintx-cubecl/src/transform/c2spinor.rs</files>
  <read_first>
    - crates/cintx-cubecl/src/transform/c2spinor.rs (lines 124-172 `apply_si_block` — the WRONG-signs contrast; lines 624-714 `apply_bra_sf_block_all_kappa`/`apply_bra_block`/`bra_coeff_refs` — the structure to copy)
    - .planning/phases/28-spin-included-c2s-si-transform-p-module-gap-b2/28-RESEARCH.md (§Spike Target A — `a_bra_cart2spinor_si` 4-block stride/ordering; §Code Examples — the sign-discrepancy artifact)
    - .planning/phases/28-spin-included-c2s-si-transform-p-module-gap-b2/28-PATTERNS.md (§"apply_bra_si_block" + §"Shared Pattern: SI Sign Convention" — the do-not-copy contrast block)
    - libcint-master/src/cart2sph.c:3920-3968 (`a_bra_cart2spinor_si`) and :4971-4992 (`c2s_si_1e` block setup)
  </read_first>
  <behavior>
    - Test: hand-derived l=1 (nf=3), kappa=-1 (GT, nd=4), single ket column (nket=1) case — fill gc_1/gc_x/gc_y/gc_z with 4 distinct cart vectors, pull CG coeffs CJ_GT_L1_R/I from c2spinor_coeffs.rs, compute (saR,saI,sbR,sbI) by hand using the a_bra_cart2spinor_si equations, assert apply_bra_si_block output matches to 1e-14.
    - Test: the bra-si output for the same inputs is NOT equal to what apply_si_block would produce (sign-discrepancy guard).
  </behavior>
  <action>
Add `fn apply_bra_si_block` to c2spinor.rs. Copy the loop/index/coeff-layout structure from
`apply_bra_block` (L668-710) and the GT/LT/both kappa block-pointer dispatch from
`apply_bra_sf_block_all_kappa` (L624-657), using `bra_coeff_refs` (L714) for the CG table slices.

The function takes FOUR cart blocks (gc_1 scalar, gc_x, gc_y, gc_z Pauli) instead of one `cart`.
Block order from `cart2sph.c:4971-4974` is x, y, z, 1 (scalar LAST) — but name the args by role.
Each block is read BRA-major `block[n * ncj + j]` (the bra step reads bra-major; the KET→BRA
transpose is owned by the caller cart_to_spinor_si_2d, Task 2).

CG coeff indexing (same as apply_bra_block, c2spinor.rs:694-697):
  ca_r = coeff_r[i*2*nci + n];  ca_i = coeff_i[i*2*nci + n];
  cb_r = coeff_r[i*2*nci + nci + n];  cb_i = coeff_i[i*2*nci + nci + n];

The accumulation MUST transcribe `a_bra_cart2spinor_si` (cart2sph.c:3958-3961) VERBATIM — these
exact signs, NOT apply_si_block's signs:
  sa_r +=  ca_r * v1 + ca_i * vz - cb_r * vy + cb_i * vx;
  sa_i += -ca_i * v1 + ca_r * vz + cb_i * vy + cb_r * vx;
  sb_r +=  cb_r * v1 - cb_i * vz + ca_r * vy + ca_i * vx;
  sb_i += -cb_i * v1 - cb_r * vz - ca_i * vy + ca_r * vx;

where v1 = gc_1[n*ncj+j], vx = gc_x[...], vy = gc_y[...], vz = gc_z[...].

DO NOT write the apply_si_block convention (`+ca_r*v1 - ca_i*vz + cb_r*vy - cb_i*vx`). DO NOT call
`apply_si_block`. The output writes the alpha (upper) spinor block then the beta (lower) block,
exactly like apply_bra_block (tmp1 layout: alpha first nci*nd, beta at +nci*nd; mirror apply_bra_block).

Write the unit test described in <behavior>. Computation internal in f64. Library errors use
`cintxRsError` (thiserror) per CLAUDE.md.
  </action>
  <verify>
    <automated>cargo test -p cintx-cubecl --lib apply_bra_si_block</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'fn apply_bra_si_block' crates/cintx-cubecl/src/transform/c2spinor.rs` returns a match.
    - `grep -F '+ ca_i * vz' crates/cintx-cubecl/src/transform/c2spinor.rs` AND `grep -F '- cb_r * vy' crates/cintx-cubecl/src/transform/c2spinor.rs` AND `grep -F '+ cb_i * vx' crates/cintx-cubecl/src/transform/c2spinor.rs` ALL return matches (the correct bra-si signs).
    - The body of `apply_bra_si_block` does NOT contain the substring `- ca_i * vz + cb_r * vy - cb_i * vx` (apply_si_block's WRONG signs); verify by reading the function body.
    - `apply_bra_si_block` does NOT call `apply_si_block`: `sed -n '/fn apply_bra_si_block/,/^}/p' crates/cintx-cubecl/src/transform/c2spinor.rs | grep -c 'apply_si_block'` returns 0.
    - `cargo test -p cintx-cubecl --lib apply_bra_si_block` passes (hand-derived l=1/kappa=-1 value match to 1e-14 + sign-discrepancy guard).
  </acceptance_criteria>
  <done>apply_bra_si_block exists with the verbatim a_bra_cart2spinor_si signs, does not delegate to apply_si_block, and its unit test passes against hand-derived vendor values.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add cart_to_spinor_si_2d host transform (4-block input, ordinary ket, spinor_len sizing)</name>
  <files>crates/cintx-cubecl/src/transform/c2spinor.rs</files>
  <read_first>
    - crates/cintx-cubecl/src/transform/c2spinor.rs (lines 531-612 `cart_to_spinor_sf_2d` — the 3-stage template; lines 1335-1354 — the KET→BRA in-transform transpose pattern to mirror)
    - .planning/phases/28-spin-included-c2s-si-transform-p-module-gap-b2/28-RESEARCH.md (§Spike Target B bra-Pauli/ket-ordinary split; §Spike Target D spinor_len sizing; §Pitfall 4 KET→BRA; Architecture data-flow diagram stages 0-3)
    - .planning/phases/28-spin-included-c2s-si-transform-p-module-gap-b2/28-PATTERNS.md (§"cart_to_spinor_si_2d" — signature/guard/3-stage skeleton; §"Shared Pattern: Spinor Buffer Sizing"; §"Error Handling")
    - libcint-master/src/cart2sph.c:4947-4992 (`c2s_si_1e` — lines 4983 bra=a_bra_cart2spinor_si, 4984 ket=a_ket_cart2spinor ORDINARY)
  </read_first>
  <behavior>
    - Test: spinor_len(1,+1)==2 (p LT) and spinor_len(2,-1)==6 (d GT) — the kappa≠0 sizing the transform must honor.
    - Test: undersized staging returns cintxRsError::BufferTooSmall with required == di*dj*2; undersized cart returns ChunkPlanFailed; no partial writes occur before the error.
    - Test: a non-square p(kappa=-1)×d(kappa=-1) round-trip produces finite output of length di*dj*2 with the bra step routed through apply_bra_si_block and the ket through apply_ket_transform.
  </behavior>
  <action>
Add `pub fn cart_to_spinor_si_2d<F: CintFloat>` to c2spinor.rs, copying the 3-stage skeleton of
`cart_to_spinor_sf_2d` (L531-612) VERBATIM except the bra step. Signature mirrors sf_2d but takes
FOUR cart blocks instead of one:

  pub fn cart_to_spinor_si_2d<F: CintFloat>(
      staging: &mut [F],
      gc_x: &[f64], gc_y: &[f64], gc_z: &[f64], gc_1: &[f64],
      li: u8, kappa_i: i16, lj: u8, kappa_j: i16,
  ) -> Result<(), cintxRsError>

Sizing (Spike Target D — ALWAYS spinor_len, NEVER hardcode 4l+2):
  let nci = ncart(li); let ncj = ncart(lj);
  let di = spinor_len(li, kappa_i as i32);  let dj = spinor_len(lj, kappa_j as i32);
  let required = di * dj * 2;

Buffer guards BEFORE any write (mirror sf_2d L544-559, OOM-safe stop, no partial writes):
  - assert/validate there are exactly 4 gc blocks each of length >= nci*ncj; each block too small →
    cintxRsError::ChunkPlanFailed { from: "c2spinor_si_2d", detail: ... }.
  - staging.len() < required → cintxRsError::BufferTooSmall { required, provided: staging.len() }.

Stage 0 — KET→BRA transpose, OWN IT INSIDE the transform (Pitfall 4 / Phase-27 D-06; mirror
c2spinor.rs:1342-1354). Device cart blocks arrive KET-major `block[j*nci+i]`; the bra step reads
BRA-major `block[i*ncj+j]`. Transpose EACH of the 4 gc blocks into bra-major scratch:
  for i in 0..nci { for j in 0..ncj { dst[i*ncj+j] = src[j*nci+i]; } }
(do this for gc_x, gc_y, gc_z, gc_1 independently).

Stage 1 — BRA: call `apply_bra_si_block` (Task 1) with the 4 bra-major blocks, producing alpha/beta
tmp blocks (di × ncj complex) exactly as sf_2d's apply_bra_sf_block_all_kappa produces.

Stage 2 — KET: REUSE `apply_ket_transform` (L766) VERBATIM. c2s_si_1e's ket is the ORDINARY
a_ket_cart2spinor (cart2sph.c:4984), identical to c2s_sf_1e. Do NOT introduce any `*_si` ket variant.

Stage 3 — zcopy interleave (copy sf_2d L603-608 VERBATIM): column-major, j outer, i inner:
  staging[(j*di+i)*2] = F::from_f64_lossy(out_r[j*di+i]);
  staging[(j*di+i)*2+1] = F::from_f64_lossy(out_i[j*di+i]);

Add the unit tests in <behavior>. Internal f64; F-output at the boundary via from_f64_lossy.
  </action>
  <verify>
    <automated>cargo test -p cintx-cubecl --lib transform::c2spinor</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'pub fn cart_to_spinor_si_2d' crates/cintx-cubecl/src/transform/c2spinor.rs` returns a match.
    - The si_2d function body calls `apply_bra_si_block` (not apply_si_block) AND `apply_ket_transform`: `sed -n '/pub fn cart_to_spinor_si_2d/,/^}/p' crates/cintx-cubecl/src/transform/c2spinor.rs | grep -c apply_bra_si_block` >= 1 AND `... | grep -c apply_ket_transform` >= 1.
    - No symbol `apply_ket_si` or `a_ket_cart2spinor_si` is introduced: `grep -rc 'apply_ket_si\|a_ket_cart2spinor_si' crates/cintx-cubecl/src/transform/c2spinor.rs` returns 0.
    - Zero hardcoded 4l+2 sizing in the si_2d body: `sed -n '/pub fn cart_to_spinor_si_2d/,/^}/p' crates/cintx-cubecl/src/transform/c2spinor.rs | grep -E -c '4 *\* *l *\+ *2|4l\+2'` returns 0; sizing uses `spinor_len`.
    - Body contains `BufferTooSmall` and `ChunkPlanFailed` guards before stage 1.
    - `cargo test -p cintx-cubecl --lib transform::c2spinor` passes.
  </acceptance_criteria>
  <done>cart_to_spinor_si_2d exists, owns the KET→BRA transpose, routes bra through apply_bra_si_block + ket through ordinary apply_ket_transform, sizes all buffers via spinor_len, fail-closes on undersized buffers, and its unit tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none external) | Pure-compute internal transform; no network, auth, user input, secrets, or external surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01-01 | Tampering/DoS | cart_to_spinor_si_2d buffer sizing | mitigate | Size every buffer from `spinor_len` (kappa≠0 → 2l/2l+2, never 4l+2); validate staging.len()/cart block lengths up front and return typed `cintxRsError::BufferTooSmall`/`ChunkPlanFailed` with no partial writes (CLAUDE.md OOM-safe stop). No `unsafe` indexing in the host transform. |
| T-28-01-02 | Tampering (correctness) | apply_bra_si_block sign convention | mitigate | Transcribe `a_bra_cart2spinor_si` verbatim; grep-gate the bra-si signs and assert no delegation to `apply_si_block`; unit test against hand-derived vendor values to 1e-14. |

No other STRIDE categories apply: no auth (S/E), no logging/audit surface (R), no confidential data (I beyond integrity already covered).
</threat_model>

<verification>
- `cargo test -p cintx-cubecl --lib transform::c2spinor` green (includes apply_bra_si_block + cart_to_spinor_si_2d unit tests).
- Grep gates in the acceptance criteria pass (correct signs, no apply_si_block delegation, no hardcoded 4l+2, ordinary ket reuse).
- `cargo build -p cintx-cubecl --locked` succeeds.
</verification>

<success_criteria>
- apply_bra_si_block transcribes the a_bra_cart2spinor_si convention and is unit-proven against hand-derived vendor values; it does not reuse apply_si_block.
- cart_to_spinor_si_2d consumes 4 gc blocks, owns the KET→BRA transpose, reuses the ordinary ket transform, sizes via spinor_len, and fail-closes on undersized buffers.
- Maps to ROADMAP SC#1 (the si transform + 4-block input + σ-coupling matches c2s_si_1e).
</success_criteria>

<output>
After completion, create `.planning/phases/28-spin-included-c2s-si-transform-p-module-gap-b2/28-01-SUMMARY.md`
</output>
