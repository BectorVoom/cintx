---
phase: 28-spin-included-c2s-si-transform-p-module-gap-b2
plan: 04
type: execute
wave: 2
depends_on: ["28-01", "28-02", "28-03"]
files_modified:
  - crates/cintx-cubecl/src/kernels/one_electron.rs
  - crates/cintx-oracle/src/fixtures.rs
  - crates/cintx-oracle/tests/si_transform_parity.rs
autonomous: true
requirements: [FND-05]
must_haves:
  truths:
    - "The int1e_sp Spinor dispatch wires the σ·p assembler → cart_to_spinor_si_2d (NOT sf_2d)"
    - "A kappa≠0 non-square nctr>1 fixture (p kappa=+1 LT, d kappa=-1 GT) exists in fixtures.rs"
    - "A small real heavy-atom 2c-basis fixture exists as a realism cross-check"
    - "End-to-end byte-identity vs vendor int1e_sp_spinor at atol=1e-12 passes on the kappa fixture"
    - "The parity test FAILS (not skips) when the double-gate env is expected but absent"
  artifacts:
    - path: "crates/cintx-oracle/tests/si_transform_parity.rs"
      provides: "End-to-end transform/component byte-identity proof (D-01, no flag flip)"
      contains: "vendor_int1e_sp_spinor"
    - path: "crates/cintx-oracle/src/fixtures.rs"
      provides: "build_kappa_spinor_fixture + heavy-atom fixture"
      contains: "build_kappa_spinor_fixture"
  key_links:
    - from: "one_electron.rs int1e_sp Spinor arm"
      to: "cart_to_spinor_si_2d"
      via: "dispatch after σ·p assembler"
      pattern: "cart_to_spinor_si_2d"
    - from: "si_transform_parity.rs"
      to: "vendor_int1e_sp_spinor"
      via: "byte-identity compare at atol=1e-12"
      pattern: "vendor_int1e_sp_spinor"
---

<objective>
Wire the `int1e_sp` Spinor dispatch arm in `one_electron.rs` (σ·p assembler → `cart_to_spinor_si_2d`,
handling nctr>1), add the two D-05 fixtures (primary adversarial kappa≠0 + secondary heavy-atom), and
add the end-to-end byte-identity parity test `si_transform_parity.rs` that proves FND-05 against
vendored `int1e_sp_spinor` at atol=1e-12 — WITHOUT flipping any manifest coverage flag (D-01).

Purpose: this is the FND-05 proof. It composes Plan 01 (transform), Plan 02 (assembler), and Plan 03
(manifest row + vendor FFI) into the validated end-to-end path.

Output: int1e_sp Spinor dispatch wiring, build_kappa_spinor_fixture + heavy-atom fixture, and the
si_transform_parity.rs vendor byte-identity test (with the Phase-27 D-10 no-silent-skip assertion).
</objective>

<execution_context>
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/cintx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-spin-included-c2s-si-transform-p-module-gap-b2/28-RESEARCH.md
@.planning/phases/28-spin-included-c2s-si-transform-p-module-gap-b2/28-PATTERNS.md
@.planning/phases/28-spin-included-c2s-si-transform-p-module-gap-b2/28-VALIDATION.md
@.planning/phases/28-spin-included-c2s-si-transform-p-module-gap-b2/28-01-SUMMARY.md
@.planning/phases/28-spin-included-c2s-si-transform-p-module-gap-b2/28-02-SUMMARY.md
@.planning/phases/28-spin-included-c2s-si-transform-p-module-gap-b2/28-03-SUMMARY.md

<interfaces>
<!-- Produced by Waves 1 plans; consume directly. -->
From crates/cintx-cubecl/src/transform/c2spinor.rs (Plan 01):
- pub fn cart_to_spinor_si_2d<F: CintFloat>(staging:&mut[F], gc_x:&[f64], gc_y:&[f64], gc_z:&[f64], gc_1:&[f64], li:u8, kappa_i:i16, lj:u8, kappa_j:i16) -> Result<(), cintxRsError>
From crates/cintx-cubecl/src/kernels/sigma_p.rs (Plan 02):
- the σ·p #[cube] assembler emitting pre-blocked gc_x/gc_y/gc_z/gc_1
From crates/cintx-oracle/src/vendor_ffi.rs (Plan 03):
- pub fn vendor_int1e_sp_spinor(out:&mut[f64], shls:&[i32;2], atm, natm, bas, nbas, env) -> i32
From crates/cintx-cubecl/src/kernels/one_electron.rs:
- the existing `Representation::Spinor =>` sf_2d dispatch arm (~L10101-10131); import site ~L24
- NOTE: the sf_2d arm REJECTS nctr>1 (~L10106-10110) — the si_2d path must HANDLE nctr>1, do NOT copy the rejection
From crates/cintx-oracle/src/fixtures.rs:
- build_adversarial_spinor_fixture(...)  // ~L209 — clone template (non-square p×d, nctr=2 p shell), then set kappa≠0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the int1e_sp Spinor dispatch arm (σ·p assembler → cart_to_spinor_si_2d, nctr>1 handled)</name>
  <files>crates/cintx-cubecl/src/kernels/one_electron.rs</files>
  <read_first>
    - crates/cintx-cubecl/src/kernels/one_electron.rs (lines ~24 import site; lines ~10101-10131 the sf_2d Spinor dispatch arm — mirror, but call si_2d and DO NOT copy the nctr>1 rejection)
    - crates/cintx-cubecl/src/kernels/sigma_p.rs (Plan 02 assembler — the call surface)
    - crates/cintx-cubecl/src/transform/c2spinor.rs (Plan 01 cart_to_spinor_si_2d signature)
    - .planning/phases/28-spin-included-c2s-si-transform-p-module-gap-b2/28-PATTERNS.md (§"Spinor dispatch arm" — nctr>1 note, raw.rs already transposes env COLUMN-major → Shell ROW-major)
    - .planning/phases/28-spin-included-c2s-si-transform-p-module-gap-b2/28-RESEARCH.md (§Spike Target C device→host hand-off; §Architectural Responsibility Map nctr>1 row)
  </read_first>
  <action>
Add an `int1e_sp` Spinor dispatch path in `one_electron.rs`, mirroring the existing
`Representation::Spinor =>` sf_2d arm (~L10101-10131) but:
  1. Add `cart_to_spinor_si_2d` to the import at ~L24:
     use crate::transform::c2spinor::{cart_to_spinor_sf_2d, cart_to_spinor_sf_derivative_2d, cart_to_spinor_si_2d};
  2. Detect the int1e_sp operator (by SYMBOL name, not positional id — Pitfall 6) and route to the
     σ·p assembler (Plan 02) which produces the 4 pre-blocked gc_x/gc_y/gc_z/gc_1 blocks, then call
     `cart_to_spinor_si_2d(staging, gc_x, gc_y, gc_z, gc_1, li, kappa_i, lj, kappa_j)` instead of
     `cart_to_spinor_sf_2d`.
  3. HANDLE nctr>1 — do NOT copy the sf_2d arm's nctr>1 rejection (~L10106-10110). raw.rs already
     transposes the env COLUMN-major coeff to Shell ROW-major (project_raw_nctr_coeff_transpose);
     loop over the (ci,cj) contraction blocks and apply the assembler+transform per block, scattering
     into the contraction-major spinor AO grid (the D-05 fixture keeps an nctr=2 p shell, so this path
     MUST work). cart_to_spinor_si_2d owns the KET→BRA transpose internally (Plan 01), so the launcher
     passes the gc blocks as the assembler emits them (KET-major device layout) — do NOT add a second
     transpose in the launcher.

int1e_sp stays UnsupportedApi at the manifest level (D-01); this wiring is what the parity test
drives directly through the kernel path, not a flipped public family.
  </action>
  <verify>
    <automated>cargo build -p cintx-cubecl --locked --features cpu</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'cart_to_spinor_si_2d' crates/cintx-cubecl/src/kernels/one_electron.rs` >= 2 (import + dispatch call).
    - The int1e_sp Spinor path does NOT reject nctr>1: the new arm does not contain a `nctr>1` UnsupportedApi rejection for the si_2d route (read the arm to confirm; the sf_2d rejection at ~L10106 stays only on the sf path).
    - The int1e_sp family is detected by symbol name, not a positional `OperatorId::new(<int>)` literal: `grep -c 'int1e_sp' crates/cintx-cubecl/src/kernels/one_electron.rs` >= 1.
    - `cargo build -p cintx-cubecl --locked --features cpu` succeeds.
    - `cargo test -p cintx-cubecl --lib transform::c2spinor` still green (no regression).
  </acceptance_criteria>
  <done>The int1e_sp Spinor dispatch routes σ·p assembler → cart_to_spinor_si_2d, handles nctr>1, detects the family by symbol; the crate builds under the cpu feature.</done>
</task>

<task type="auto">
  <name>Task 2: Add build_kappa_spinor_fixture + heavy-atom fixture, and the end-to-end byte-identity parity test</name>
  <files>crates/cintx-oracle/src/fixtures.rs, crates/cintx-oracle/tests/si_transform_parity.rs</files>
  <read_first>
    - crates/cintx-oracle/src/fixtures.rs (lines ~209-300 `build_adversarial_spinor_fixture` — clone template; KAPPA_OF / BAS_SLOTS slots)
    - crates/cintx-oracle/src/vendor_ffi.rs (Plan 03 `vendor_int1e_sp_spinor`; vendor_CINTcgto_spinor sizing)
    - crates/cintx-oracle/tests/ (Phase-27 `spinor_deriv_parity.rs` — the D-10 no-silent-skip pattern to mirror)
    - .planning/phases/28-spin-included-c2s-si-transform-p-module-gap-b2/28-RESEARCH.md (§Spike Target D — spinor_len(1,+1)==2, spinor_len(2,-1)==6, buffer di*dj*2=24; §Validation Architecture test map)
    - .planning/phases/28-spin-included-c2s-si-transform-p-module-gap-b2/28-PATTERNS.md (§"build_kappa_spinor_fixture"; §"si_transform_parity.rs"; §"No-Silent-Skip Vendor Parity")
    - .planning/phases/28-spin-included-c2s-si-transform-p-module-gap-b2/28-VALIDATION.md (vendor-gate command, Wave 0 reqs, no-silent-skip)
  </read_first>
  <action>
In `fixtures.rs`:
  1. Add `build_kappa_spinor_fixture` by cloning `build_adversarial_spinor_fixture` (~L209) — keep the
     non-square p×d geometry, the nctr=2 p shell, and the non-zero rinv origin — then set genuine
     kappa≠0 on the basis slots:
       p shell kappa = +1  → LT-only, di = spinor_len(1,+1) = 2*1 = 2
       d shell kappa = -1  → GT-only, dj = spinor_len(2,-1) = 2*2+2 = 6
     This is the FIRST cintx fixture exercising the non-(4l+2) sizing path; block is 2×6 (non-square),
     buffer di*dj*2 = 24 f64. Set bas[KAPPA_OF] and bas[BAS_SLOTS + KAPPA_OF] to +1 and -1 respectively.
  2. Add a SECOND fixture: a small real heavy-atom 2c-basis case (single-atom Dirac/dyall-style spinor
     basis; element/exponents are Claude's discretion) as a realism cross-check (D-05 secondary,
     non-primary gate).
  This is oracle/test glue → anyhow side.

Add NEW test file `crates/cintx-oracle/tests/si_transform_parity.rs`:
  - Drive the si_2d transform + σ·p assembler through the int1e_sp kernel path on
    build_kappa_spinor_fixture, compute the flat interleaved-complex spinor buffer.
  - Compare BYTE-IDENTITY to vendor_int1e_sp_spinor output at atol=1e-12 (use the oracle `compare`
    helper). The vendor `out` is sized via vendor_CINTcgto_spinor (ni_sp*nj_sp*2 f64).
  - This proves FND-05 WITHOUT flipping any manifest flag (D-01).
  - Add the Phase-27 D-10 NO-SILENT-SKIP assertion: when the vendor arm is expected (double-gate
    env present) the test must FAIL if vendor parity did not actually run — never silently skip. Mirror
    spinor_deriv_parity.rs. The vendor path is double-gated on `--features cpu` + CINTX_ORACLE_BUILD_VENDOR=1.
  - Also run the heavy-atom fixture through the path and assert finite output (realism cross-check;
    secondary, not the primary byte-identity gate).
  - Assert sizing: spinor_len(1,+1)==2, spinor_len(2,-1)==6, and the staging buffer length == 24
    for the kappa fixture (guards Pitfall 3).

If a ~1e-11 drift appears under the CpuRuntime, suspect the FP-env side effect (RESEARCH §Pitfall 5)
before adjusting the kernel — do not loosen the 1e-12 gate without root-causing.
  </action>
  <verify>
    <automated>CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test si_transform_parity</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'build_kappa_spinor_fixture' crates/cintx-oracle/src/fixtures.rs` >= 1 and it sets kappa to +1 and -1: `grep -A40 'fn build_kappa_spinor_fixture' crates/cintx-oracle/src/fixtures.rs | grep -E -c 'KAPPA_OF|kappa'` >= 1.
    - A second heavy-atom fixture fn exists in fixtures.rs (read to confirm; distinct from build_kappa_spinor_fixture).
    - `crates/cintx-oracle/tests/si_transform_parity.rs` exists and calls `vendor_int1e_sp_spinor`: `grep -c 'vendor_int1e_sp_spinor' crates/cintx-oracle/tests/si_transform_parity.rs` >= 1, and compares at 1e-12: `grep -E -c '1e-12|1e-?12' crates/cintx-oracle/tests/si_transform_parity.rs` >= 1.
    - The no-silent-skip assertion is present: `grep -E -c 'CINTX_ORACLE_BUILD_VENDOR|silent|must.*run|skip' crates/cintx-oracle/tests/si_transform_parity.rs` >= 1.
    - The test does NOT flip any manifest flag: `grep -c 'oracle_covered' crates/cintx-oracle/tests/si_transform_parity.rs` returns 0 (D-01 — proof via transform test, not a coverage flip).
    - `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test si_transform_parity` passes (kappa fixture byte-identical at atol=1e-12; sizing asserts hold).
  </acceptance_criteria>
  <done>Both fixtures exist (primary kappa≠0 non-square nctr>1 + secondary heavy-atom); si_transform_parity.rs proves int1e_sp byte-identity vs vendor at atol=1e-12 on the kappa fixture without flipping any flag, with the no-silent-skip assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cintx → vendored libcint FFI | Test/oracle-only boundary in the parity harness; fixtures are author-controlled (no untrusted input). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04-01 | Tampering (correctness) | kappa≠0 buffer sizing in the dispatch + fixture | mitigate | Fixture exercises spinor_len-derived non-(4l+2) sizing (di=2, dj=6, buffer=24); the test asserts the sizes, guarding Pitfall 3 out-of-bounds on undersized spinor buffers. |
| T-28-04-02 | Tampering (verification integrity) | vendor parity silently skipping | mitigate | Phase-27 D-10 no-silent-skip assertion: the parity test FAILS (not skips) when the double-gate env is expected but vendor parity did not run. |
| T-28-04-03 | Tampering | vendor FFI out-buffer overrun | mitigate | Allocate `out` at the full vendor-expected size via vendor_CINTcgto_spinor (ni_sp*nj_sp*2). |

No other STRIDE categories apply: oracle/test glue + device dispatch, no auth (S/E), no production audit surface (R), no confidential data.
</threat_model>

<verification>
- `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu --test si_transform_parity` green (byte-identity at atol=1e-12 on the kappa fixture).
- `cargo build -p cintx-cubecl --locked --features cpu` succeeds (dispatch wiring).
- `cargo run -p xtask -- oracle-covered-update` confirms int1e_sp_spinor stays oracle_covered=false (no flip).
- Full vendor suite: `CINTX_ORACLE_BUILD_VENDOR=1 cargo test -p cintx-oracle --features cpu` green.
</verification>

<success_criteria>
- int1e_sp Spinor dispatch routes σ·p assembler → cart_to_spinor_si_2d (handling nctr>1).
- Both D-05 fixtures exist; the kappa≠0 non-square nctr>1 fixture exercises the 2l/2l+2 sizing path.
- End-to-end byte-identity vs vendor int1e_sp_spinor at atol=1e-12 passes, with the no-silent-skip assertion, and NO manifest flag is flipped (D-01).
- Maps to ROADMAP SC#3 (kappa-bearing fixture + end-to-end byte-identity at atol=1e-12) and reinforces SC#4 (no flip).
</success_criteria>

<output>
After completion, create `.planning/phases/28-spin-included-c2s-si-transform-p-module-gap-b2/28-04-SUMMARY.md`
</output>
